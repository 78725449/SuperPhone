# 05 - 能力扩展规划

> **来源**：IPA改造计划.md 行 843-1173（Phase 11）+ 行 1174-1198（附录 A）
> **关注点**：能力元数据 schema 扩展、自动化编排能力、应用与启动能力组、屏幕感知增强、三端菜单分层渲染、批量操作场景化过滤、AI 调用通道预留、底层互斥逻辑
> **索引**：[返回主文档](IPA改造计划.md)

---

## Phase 11：能力扩展与场景化分层（v2.0 新增，2026-08-09）

> **背景**：Phase 0-10 已完成 97 控制型 + 38 配置型能力的注册与落地，9 组 category 分组渲染、批量操作栏、隧道、三端 UI 规范均已就绪。
>
> **本阶段目标**：基于 hermes-android 借鉴 + 三场景反推（单控/批量/AI 调用）+ AI 调用架构预留，扩展 9 个新能力、2 个新分组、3 个元数据字段，形成 106 控制型能力的完整能力即服务体系。
>
> **核心依据**：
> - 巨魔环境 UI 树读取不可行（task_for_pid 受 MAC 限制 / Accessibility 私有 API 需 entitlement / 代码注入需 amfi 关闭），视觉识别是唯一可行路径
> - 借鉴 hermes-android 的 macro/screen_hash/diff_screen/wait/event_stream/get_apps/current_app/open_app 6 个设计概念，不 fork 不拉取代码
> - 三场景（单控/批量/AI 调用）对能力的需求差异显著，需通过元数据字段区分

### 11.1 能力元数据 schema 扩展（P1 前置）

**目标**：为现有 97 项 + 新增 9 项能力补打场景标签，支撑前端菜单分层与批量过滤。

| 新字段 | 类型 | 取值 | 用途 |
|---|---|---|---|
| `menu` | string | `primary` / `secondary` / `internal` | 菜单分层；`internal`=AI 专用不显示在人工菜单 |
| `scenes` | array | `[single, batch, ai]` 子集 | 标记哪些场景可用，前端据此过滤批量操作栏 |
| `batch` | bool | true/false | 是否支持批量操作（`scenes` 含 batch 的快捷判断） |

**各 category 的字段值映射**：

| category | menu | scenes | batch | 说明 |
|---|---|---|---|---|
| 触控与手势 | primary | [single, ai] | false | 批量点击同坐标无意义 |
| 硬件按键 | primary | [single, batch, ai] | true | 批量 home/电源有意义 |
| 文本与剪贴板 | primary | [single, ai] | false | 批量输入无意义 |
| 屏幕与采集 | primary | [single, batch, ai] | true | 批量截图是群控核心 |
| 应用与启动 | primary | [single, batch, ai] | true | 批量打开同款 app |
| 客户端管理 | secondary | [single, batch, ai] | true | 批量断开 |
| 服务与网关 | secondary | [single, batch, ai] | true | 批量重启 |
| 系统与通知 | secondary | [ai] | false | 查询类，批量无意义 |
| 自动化编排 | secondary | [batch, ai] | true | 批量编排是群控核心 |

**`menu: internal` 的特殊能力**（AI 必需但人工不直接用）：

| 能力 | 理由 |
|---|---|
| `screen.hash` | 人看截图即可，hash 是 AI 判断变化的轻量原语 |
| `screen.diff` | 同上，AI 专用 |
| `screen.waitStable` | AI 等页面加载，人不需要 |
| `sys.stats.inflight` | 编码帧数，AI 性能调优用 |
| `sys.bonjour.txt` | Bonjour 发现信息，AI/运维用 |

**改动位置**：
- `TRCapabilityRegistry.mm` `_controlMetadata` 输出新增 3 字段
- `_inferCategoryForCapId:route:` 扩展 app.*/macro.*/screen.hash 等新前缀推断
- 现有 97 项能力按 category 映射表批量补打标签

**验收**：`query caps` 返回的每个能力元数据含 `menu`/`scenes`/`batch` 三字段，值与映射表一致

### 11.2 自动化编排能力（P2 · Batch 6a）

**目标**：借鉴 hermes `android_macro`，命令通道 v2 新增批量 invoke，解决 AI 群控多步操作的 RTT 累积延迟。

| 能力 ID | 标题 | 参数 | 返回 | menu | scenes | batch |
|---|---|---|---|---|---|---|
| `macro.run` | 步骤序列原子执行 | `{steps:[{cap,args}], stopOnFail:bool}` | `{completed:int, results:[{ok,data?}]}` | secondary | [batch, ai] | true |

**执行逻辑**：
1. 按 `steps` 数组顺序调用每个 `cap` 能力，传入对应 `args`
2. 每步执行结果记录到 `results` 数组
3. `stopOnFail=true` 时，遇失败立即停止，`completed` 返回已执行步数
4. `stopOnFail=false` 时，遇失败继续执行，`completed` 始终等于 `steps.length`
5. 单次 macro.run 不递归（steps 内不可再调用 macro.run）

**改动位置**：
- `TRCapabilityRegistry.mm` 新增 `_registerMacroCapabilities` 方法
- executor 内部循环调用 `[self invokeCapability:cap args:args error:&err]`

**验收**：`invoke macro.run {steps:[{cap:"home",args:{}},{cap:"screenshot",args:{}}], stopOnFail:true}` 返回两步结果数组

### 11.3 应用与启动能力组（P3 · Batch 6b）

**目标**：借鉴 hermes `get_apps`/`current_app`/`open_app`，巨魔环境通过 `LSApplicationWorkspace` + `SBSCopyFrontmostApplicationDisplayIdentifier` 实现应用管理。项目已链接 `FrontBoardServices.tbd` / `SpringBoardServices.tbd`，无需额外依赖。

| 能力 ID | 标题 | 参数 | 返回 | menu | scenes | batch |
|---|---|---|---|---|---|---|
| `app.list` | 应用列表 | `{includeIcons:bool}` | `{apps:[{bundleId,name,version?}]}` | primary | [single, batch, ai] | false |
| `app.current` | 前台应用 | 无 | `{bundleId,name}` | primary | [single, batch, ai] | false |
| `app.open` | 打开应用 | `{bundleId}` 或 `{scheme}` | `{ok:bool}` | primary | [single, batch, ai] | true |
| `app.icon` | 应用图标 | `{bundleId}` | `{base64, size}` | internal | [ai] | false |

**技术路径**：
- `app.list`：`[[LSApplicationWorkspace defaultWorkspace] allInstalledApplications]` 遍历，取 `applicationIdentifier`/`localizedName`/`shortVersionString`
- `app.current`：`SBSCopyFrontmostApplicationDisplayIdentifier()` 返回前台 bundle id
- `app.open`：优先 `openApplicationWithBundleIdentifier:options:error:`，回退 URL Scheme
- `app.icon`：`LSApplicationRecord.itemIcons` 取图标转 PNG base64

**改动位置**：
- 新增 `TRAppManager.h/.mm` 模块（封装 LSApplicationWorkspace 调用）
- `TRCapabilityRegistry.mm` 新增 `_registerAppCapabilities` 方法
- Makefile 加入 `TRAppManager.mm`

**验收**：`invoke app.list` 返回设备所有应用数组；`invoke app.open {bundleId:"com.apple.mobilesafari"}` 拉起 Safari

### 11.4 屏幕感知增强（P3-P5 · Batch 6b-6c）

#### 11.4.1 设计依据

巨魔环境下 iOS UI 树读取的三条路径全部不可行：
1. `task_for_pid` 跨进程读内存 → 受 MAC 策略限制，amfid 拒绝
2. Accessibility 私有 API（`AXUIElementCopyElementAtPosition` 等）→ 需 `com.apple.private.accessibility.client` entitlement，CoreTrust bypass 不注入
3. 代码注入目标 app → 需 amfi 关闭（越狱环境），巨魔不可达

**结论**：视觉识别是唯一可行路径。设备端只负责"看得见"的多种粒度（全图/哈希/差异/变化事件），AI 代理侧负责"看得懂"（识别元素/规划点击坐标）。

#### 11.4.2 pHash 计算管线（Accelerate framework）

**技术选型**：iOS 原生 Accelerate（vImage + vDSP），ARM NEON 加速，零第三方依赖，巨魔环境完全可用。对比方案：

| 方案 | 结论 |
|---|---|
| Accelerate (vImage+vDSP) | ✅ 选定，原生零依赖 |
| CoreImage | ❌ 无内置 DCT 滤镜 |
| Metal compute shader | ❌ 复杂度高，巨魔环境风险 |
| OpenCV | ❌ 30MB+ 依赖违反可控性原则 |

**模块划分**：新增独立模块 `TRScreenHasher.h/.mm`，单一职责。

```
ScreenCapturer（采集 CVImageBufferRef）→ TRScreenHasher（计算 pHash）→ TRCapabilityRegistry（注册能力）
```

**5 步计算管线**：

| 步骤 | 操作 | API | 输出 |
|---|---|---|---|
| 01 取帧 | ScreenCapturer 原始帧 | 新增 `captureSingleFrameBuffer` 返回 CVImageBufferRef（跳过 JPEG 编码，省 8ms） | IOSurface-backed CVImageBufferRef |
| 02 缩放 32×32 | vImage 缩放 | `vImageScale_ARGB8888` | 32×32 ARGB 缓冲区（1024 像素 × 4B = 4KB） |
| 03 转灰度 | vImage 矩阵乘法 | `vImageMatrixMultiply`（0.299R + 0.587G + 0.114B） | 32×32 8bit 灰度（1KB） |
| 04 DCT-II | vDSP 离散余弦变换 | `vDSP_DCT_CreateSetup`（type=kFFTDCT_TypeII）+ `vDSP_DCT_Execute` | 32×32 频域系数 |
| 05 取低频 + 哈希 | 取左上 8×8 + 求均值（排除 DC）+ 逐位比较 | 位运算 | uint64_t hash |

**关键实现细节**：

1. **DC 分量排除**：DCT 的 [0][0] 系数是直流分量，反映整体亮度而非结构。若纳入均值计算，亮度变化会污染结构哈希。取 8×8 低频后，**排除 [0][0]** 再求均值，每个位与均值比较生成 64bit hash。

2. **DCT setup 复用**：`vDSP_DCT_Setup` 创建开销较大，应在 `TRScreenHasher` 初始化时创建一次，后续复用。destructor 释放。

3. **取帧优化**：现有 `captureSingleFrameImage` 走完整链路（renderDisplayToScreenSurface → CVPixelBuffer → CIImage → UIImage → JPEG），pHash 不需要 UIImage/JPEG，应新增 `captureSingleFrameBuffer` 直接返回 CVImageBufferRef，跳过 JPEG 编码阶段。

**输出格式**：
- 内部计算：`uint64_t`（最快）
- API 返回：16 字符 hex 字符串（JSON 友好）
- 汉明距离：设备端直接算并返回 `distance` 字段（避免 AI 侧重复计算）

**性能预估**：
- 单次 pHash ≈ 0.3ms（vImage 缩放 0.1ms + vDSP DCT 0.15ms + 哈希生成 0.05ms）
- 对比 JPEG 编码 ≈ 8ms，快 26 倍
- 60fps 持续计算 CPU 占用 <1%
- 内存占用 4KB（32×32×4）

**汉明距离阈值经验值**（需真机校准）：

| 距离范围 | 含义 |
|---|---|
| <3 | 基本相同（同一画面噪声波动） |
| 3-8 | 轻微变化（光标闪烁、动画帧） |
| 8-15 | 明显变化（页面切换、弹窗） |
| >15 | 完全不同（切换 app） |

#### 11.4.3 三个消费能力

**11.4.3.1 `screen.hash`（P3 · Batch 6b）**

| 字段 | 值 |
|---|---|
| 标题 | 屏幕哈希 |
| 参数 | 无（先做全屏版本，region 参数后续扩展） |
| 返回 | `{hash: string, timestamp: int}` |
| route | native |
| menu | internal |
| scenes | [ai] |
| batch | false |

**执行逻辑**：调用 `TRScreenHasher computeHashForCurrentFrame`，返回当前屏幕的 pHash hex 字符串。

**11.4.3.2 `screen.diff`（P3 · Batch 6b）**

| 字段 | 值 |
|---|---|
| 标题 | 屏幕差异 |
| 参数 | `{baselineHash: string, threshold?: int}`（threshold 默认 5） |
| 返回 | `{distance: int, threshold: int, changed: bool, currentHash: string}` |
| route | native |
| menu | internal |
| scenes | [ai] |
| batch | false |

**执行逻辑**：
1. 计算当前帧 pHash（uint64_t）
2. 将 `baselineHash` hex 字符串转 uint64_t
3. `hamming = __builtin_popcountll(currentHash XOR baselineHash)`
4. `changed = hamming > threshold`

**用途**：AI 判断"操作是否生效"——点击后调用 screen.diff 比对基线，changed=true 说明屏幕变了。

**11.4.3.3 `screen.waitStable`（P5 · Batch 6c）**

| 字段 | 值 |
|---|---|
| 标题 | 等待画面稳定 |
| 参数 | `{maxMs?: int, stableMs?: int, intervalMs?: int, threshold?: int}` |
| 默认值 | `maxMs=3000, stableMs=500, intervalMs=200, threshold=3` |
| 返回 | `{stable: bool, durationMs: int, frameCount: int, lastHash: string}` |
| route | native |
| menu | internal |
| scenes | [ai] |
| batch | false |

**执行逻辑**：
1. 每 `intervalMs`（200ms）采集一帧计算 pHash
2. 比较相邻两帧的汉明距离（**注意：比较相邻帧，非与基线比较**）
3. 连续 `N = stableMs / intervalMs`（500/200=2.5→3）帧距离均 < threshold → 判定稳定，返回 `stable=true`
4. 超过 `maxMs`（3000ms）仍未稳定 → 返回 `stable=false`
5. `durationMs` 返回实际耗时，`frameCount` 返回采集帧数

**关键设计**：用"相邻帧"而非"与基线"比较。页面加载是渐变的（旧页面→过渡→新页面），若与固定基线比较，新页面稳定后与基线距离仍很大，检测不到"稳定"。

**11.4.3.4 `screen.subscribe`（P5 · Batch 6c）**

| 字段 | 值 |
|---|---|
| 标题 | 变化推送 |
| 参数 | `{enable: bool, throttleMs?: int, minDistance?: int}` |
| 默认值 | `throttleMs=150, minDistance=8` |
| 推送事件 | `{hash: string, distance: int, timestamp: int}` |
| route | native |
| menu | internal |
| scenes | [ai] |
| batch | false |

**执行逻辑**：
1. `enable=true` 时，在 ScreenCapturer 帧回调中每帧计算 pHash
2. 与上次推送的 hash 比较汉明距离
3. `distance >= minDistance`（8，明显变化才推）且距上次推送 `>= throttleMs`（150ms 防抖）→ 推送事件
4. `enable=false` 时停止计算，释放帧回调

**推送通道**：复用现有 `clients.subscribe` 推送机制（已实现防抖 150ms），避免新建推送通道。

**阈值选择理由**：`minDistance=8`（明显变化）而非 5（轻微变化），避免光标闪烁/动画帧等噪声触发频繁推送。

#### 11.4.4 改动位置汇总

| 文件 | 改动 |
|---|---|
| 新增 `TRScreenHasher.h/.mm` | pHash 计算核心模块（vImage 缩放 + vDSP DCT + 哈希生成 + 汉明距离） |
| `ScreenCapturer.h/.mm` | 新增 `captureSingleFrameBuffer` 返回 CVImageBufferRef（跳过 JPEG） |
| `TRCapabilityRegistry.mm` | 新增 `_registerScreenHashCapabilities` 方法，注册 4 项能力 |
| `Makefile` | 加入 `TRScreenHasher.mm`，链接 Accelerate.framework |

#### 11.4.5 验收标准

- `invoke screen.hash` 返回 16 字符 hex 字符串，同一画面多次调用结果一致
- `invoke screen.diff {baselineHash:"..."}` 返回 distance/changed 字段，切换 app 后 changed=true
- `invoke screen.waitStable {}` 在页面加载完成后 500ms 内返回 stable=true，超时返回 stable=false
- `invoke screen.subscribe {enable:true}` 后，切换 app 收到推送事件，distance>8
- 单次 screen.hash 计算耗时 <1ms
- 60fps 持续计算 CPU 占用 <1%

### 11.5 三端菜单分层渲染（P2 · 前端）

**目标**：基于 `menu` 字段分层渲染，`internal` 不进人工菜单。

| 层级 | menu 值 | 包含 | 数量 | 触发方式 |
|---|---|---|---|---|
| 一级 | primary | 触控/硬件按键/文本/屏幕采集/应用启动 | 82 项 | 侧边栏/FAB 首屏 |
| 二级 | secondary | 客户端管理/服务网关/系统通知/自动化编排 | 24 项 | "更多"折叠入口 |
| 隐藏 | internal | screen.hash/diff/waitStable + sys 内部 | 5 项 | AI 调用专用 |

**改动位置**：
- `caps.js` `groupByCategory` 过滤 `menu !== internal`
- `app.js` `renderCapOps` 按层级分组渲染
- 手机端新增"更多"折叠入口（6 项以上折叠）

### 11.6 批量操作场景化过滤（P2 · 前端）

**目标**：基于 `batch` 字段动态过滤批量操作栏。

| 批量类别 | 能力 | 理由 |
|---|---|---|
| 批量核心 | screenshot、service.restart、macro.run | 群控高频 |
| 批量可用 | home/power 键、app.open、clients.disconnect | 批量操作有意义 |
| 批量禁用 | touch.tap、type.text、clipboard.*、sys.* | 批量无意义 |

**改动位置**：
- `app.js` `showBatchMenu` 过滤 `cap.batch === true`
- restart/gateway 级批量操作沿用 Phase 10 二次确认弹窗机制

### 11.7 AI 调用通道预留（P2 · 网关）

**目标**：网关代理 API 暴露完整能力清单，AI 通过 invoke 调用。

| 端点 | 用途 |
|---|---|
| `GET /api/devices/:id/caps` | 返回完整元数据（含 menu/scenes/batch），AI 据此发现能力 |
| `POST /api/devices/:id/invoke` | AI 调用能力，已存在，扩展返回 scenes 字段 |

**AI 工作流**：感知（screen.hash/app.current）→ 编排（macro.run）→ 执行（tap/type）→ 验证（screen.waitStable/screen.diff）

### 11.8 批次编排

| 批次 | 内容 | 优先级 | 依赖 | 验证方式 |
|---|---|---|---|---|
| Batch 6a | 11.1 schema 扩展 + 11.2 macro.run | P1-P2 | 无 | `query caps` 返回 menu/scenes/batch；macro.run 多步执行 |
| Batch 6b | 11.3 app.* 4 项 + 11.4 screen.hash/diff | P3 | Batch 6a | `invoke app.list` 返回应用数组；`invoke screen.hash` 返回 hex |
| Batch 6c | 11.4 screen.waitStable/subscribe + 11.3 app.icon | P5 | Batch 6b | `invoke screen.waitStable` 等画面稳定返回 |
| 前端并行 | 11.5 菜单分层 + 11.6 批量过滤 + 11.7 AI 通道 | P2 | Batch 6a schema | 前端菜单分层渲染；批量栏按 batch 过滤 |

### 11.9 最终能力数字

| 维度 | Phase 10 后 | Phase 11 后 |
|---|---|---|
| 控制型 category | 7 组 | 9 组（+应用与启动、+自动化编排） |
| 控制型能力 | 97 项 | 106 项（+9 借鉴） |
| 元数据字段 | 7 个 | 10 个（+menu/scenes/batch） |
| 使用场景 | 单控 + 批量 | 单控 + 批量 + AI 调用 |

### 11.10 关键约束（沿用）

- 巨魔环境 UI 树读取不可行，视觉识别层在 AI 代理侧实现
- 9 个新能力走"能力即服务"注册表，即插即用
- `internal` 能力 AI 可见但人工菜单不可见
- 批量操作 restart/gateway 级二次确认
- 配置型 38 项不变，按 reload 策略分区显示
- pHash 采用 32×32 DCT 取 8×8 低频（排除 DC），先做全屏版本

---

## 附录 A：底层互斥逻辑（UI 与配置设计依据）

> 以下互斥关系来自 `trollvncserver.mm` 实际逻辑，UI 的显隐/校验必须遵守。
> **v1.3 更新**：反向模式（ReverseMode viewer/repeater）已彻底废弃，移除相关互斥规则 #1/#2/#3/#11，对应代码从 Root.plist 和 trollvncserver.mm 彻底删除。

| # | 互斥/联动点 | 规则 | UI 处理 | 状态 |
|---|---|---|---|---|
| ~~1~~ | ~~工作模式三选一~~ | ~~`ReverseMode = none / viewer / repeater`，互斥~~ | ~~3 张单选卡片~~ | ❌ 废弃 |
| ~~2~~ | ~~反向模式 vs 本地能力~~ | ~~反向开启 → 强制 `port=-1, http=0, bonjour=off`~~ | ~~选反向时隐藏字段~~ | ❌ 废弃 |
| ~~3~~ | ~~Viewer vs Repeater~~ | ~~repeater 才有 RepeaterID~~ | ~~二级显隐~~ | ❌ 废弃 |
| 4 | VNC 认证 | `FullPassword` 为空=无认证；经典 VNC 只认前 8 位 | 密码组提示 | ✅ 保留 |
| 5 | 全局只读 vs 只读密码 | 全局 `ViewOnly` 优先生效 | 标注优先级 | ✅ 保留 |
| 6 | SSL 证书/私钥 | 必须成对，缺一不启用 TLS | 成对校验 | ✅ 保留 |
| 7 | HTTP Port=0 | 关闭网页服务 | 高级页显示状态 | ✅ 保留 |
| 8 | 脏区检测 | `FullscreenThresholdPercent=0` 关闭 | 进阶折叠 | ✅ 保留 |
| 9 | 滚轮/保活 | `WheelStepPx=0`、`KeepAliveSec=0` 分别禁用 | 滑块带"0=关闭"说明 | ✅ 保留 |
| 10 | 方向同步 | `OrientationSync` 控制画面采集方向跟随设备物理方向 | 设置页标注 | ✅ 保留 |
| ~~11~~ | ~~Bonjour 依附反向~~ | ~~反向模式下被强制关闭~~ | ~~见 #2~~ | ❌ 废弃 |

> **v1.6 修正**：#10 原引用 `OrientationPadFix`（仅 iPad 非标准方向用）已废弃（见附录 E.10），改为引用 `OrientationSync`。

> **Phase 4 扩展**：远程 `set` 配置后，本地 UI 需监听 NSUserDefaults 变更并同步显隐状态。

---
