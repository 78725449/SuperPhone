# TrollVNC 内网群控项目 · 开发宪法

> 版本：v2.0 · 更新日期：2026-08-11
> 本宪法基于项目全部开发历程与讨论结论制定，作为后续所有开发行为的最高约定，
> 用于消除歧义、避免重复造轮子、防止偏离设计轨迹。新增/修订均须注明变更点与原因。
> v1.2（2026-08-07）：落地 B1——第七章新增 7.3 能力清单协议、7.4 命令通道；4.2 指向 7.3；7.1 标注单通道。
> v1.3（2026-08-07）：B4 落地——命令通道实现 ack 关联往返（7.4 细化）；B3 已实现能力清单上报。
> v1.4（2026-08-07）：U1 落地——5801 网页操作集由共享 caps.js 能力清单驱动（6.6 注明三处同源）。
> v1.5（2026-08-07）：U2 落地——UIKit Tab 工程（6.9）：连接/客户端/控制端/设置四页；设置复用 Preferences 列表；控制端 viewer 留 U4。
> v1.6（2026-08-07）：U2.1+U3 落地——设置页 8 分组 UIKit 化（spec 驱动表单，网关搜索/日志/重置/生成 CA 复刻）；连接页扫码直连卡加复制/打开网页。
> v1.7（2026-08-07）：U4 落地——控制端 viewer（libvncclient 直连设备 5901）：网关设备目录 + 卡片列表 + 全屏查看/操作；App 链接 libvncclient+ssl/crypto/jpeg/turbojpeg/lzo2/sasl2/z（SASL 静态插件用链接桩）。
> v1.8（2026-08-07）：修复启动即崩溃（元凶=连接页二维码 CoreImage 同步生成；改为 viewDidAppear 后台线程 + @try 保护 + 失败降级）；UI 视觉基线：Hero 渐变正确绘制、卡片/分段可见、控制端过滤胶囊选中态、设置分组图标、客户端 Tab 去 X；SceneDelegate 保留 Tab 启动 @try/@catch 回退旧设置页兜底。
> v1.9（2026-08-11）：新增 6.10 直控模式与同设备单会话约束——PC 控制台卡片直达控制（6.7"点击卡片=进大屏"的例外）；设备仅 1 条隧道 + 1 个 5901 连接 → 同设备同时仅允许 1 个活跃 VNC 会话（多会话共享驱动协议状态机串流 → noVNC "Unexpected server message (type N)" 断开）；退出直控必须真正 close WS 防残留。
> v2.0（2026-08-11）：容器化与纯隧道定版——2.6/2.9 反向模式与直连回退废弃；6.9 App 外壳改三 Tab（控制 Tab = WKWebView 容器加载网关 H5）；7.4 通用 `/command` 端点删除（专用端点 invoke/configs/restart/ping）；新增 6.11 变化拉图唯一机制；附录 B 反向连接条目移除。

---

## 第一章 项目愿景与定位

1.1 本项目是**内网自用**的 iOS 设备群控系统：手机装一次、初始化一次，之后零操作；
     通过服务器（软路由）网页随时看到并操作所有设备。
1.2 操作能力与手机自带 web 完全一致；**能力层（RFB → IOHID 注入）是唯一地基**，
     手机 web 与服务器控制台是同一地基上的两扇门。
1.3 明确不做（内网自用边界）：注册鉴权、过度 Agent 命令通道（批量启停/装 App 等）、
     TLS 加固、多租户、审计、计费、商业化。
     **注意**：不做的是“过度 Agent 通道”；“能力清单上报 + 配置命令（scale/帧率等参数）”
     属于能力模型的一部分，属计划内（见第五章）。

## 第二章 架构基线

2.1 **连接方向**：手机**主动注册**到网关（TCP JSON 注册/心跳，默认 18081）；
     网关被动等待，不做轮询保活。
2.2 **设备身份**：首启生成 UUID（`DeviceUUID`）作为唯一 `deviceId`；注册名使用
     设备真实名称（`[[UIDevice currentDevice] name]`）。身份唯一键 = `deviceId`。
2.3 **在线状态**：由网关维护 = 注册连接存活 + 心跳（默认 90s 超时判离线）。
2.4 **重复注册**：同 `deviceId` 只保留最新一条连接，旧连接关闭不误判离线。
2.5 **初始化**：打开一次 App → 搜索网关（mDNS `_trollvnc-farm._tcp`）/手动输入 → 保存，
     之后全自动。Managed.plist 可预置网关（secrets）实现免初始化。
2.6 **连接模式**：设备主动注册到网关 + 18181 隧道（主路径）；**反向模式（reverse）已整体删除**（Phase 6 全链路清理，无残留）。

### 2.7 端口与协议（不可混淆，本宪章最高频歧义点）

| 端口 | 角色 | 协议 | 说明 |
|---|---|---|---|
| 手机 **5901** | **原生双向控制通道**（画面下行 + 操作上行） | RFB/VNC | 网关桥接、VNC 客户端、noVNC(WS) 都走它；**非包装、非外网专用** |
| 手机 **5801** | **网页文件服务器**（只发 noVNC 网页文件） | HTTP | 网页打开后真正连的还是 5901（WS）；**无任何配置 API** |
| 网关 **8080** | 控制台（外网访问的“包装面”） | HTTP + WS | 外部经 frp/隧道到这里，再由网关桥到 5901 |
| 网关 **18081** | 注册/心跳 | TCP JSON | 手机拨入 |

**铁则**：控制台/网关**只连 5901**，不连 5801；5801 仅作手机自带的浏览器直连网页，控制台不做 5801 路由入口。

### 2.8 连接链路（自然语言）

```
浏览器控制台(8080) ──WebSocket──► 网关(软路由) ──TCP 桥──► 手机 5901（RFB）
                                    │  字节双向透传          │ trollvncserver
                                    └─ 广播复制到其它设备 ────┘ STHIDEventGenerator → IOHID → iOS
```
- 画面：手机抓屏(IOSurface) → 5901 → 网关 → WS → 浏览器 noVNC 渲染
- 操作：浏览器 noVNC/按钮 → RFB 事件 → 网关 → 5901 → 手机执行
- 群控广播：主屏 RFB 输入字节在**网关侧**复制到同组其它设备的 5901 连接

### 2.9 外部访问三种方法（准确版）

| # | 方法 | 链路 | 适用 |
|---|---|---|---|
| ① | **经网关（控制台）** | 外部 → frp/隧道 → 网关 8080 → 网关桥 → 手机 5901 | 主路径（网页控制、支持群控） |
| ② | **VNC 客户端直连** | VNC 客户端 → 手机 5901（需同网段或端口映射公网） | 单设备、需客户端 |
| ③ | ~~手机反向连接~~ | 已废弃（Phase 6 全链路删除：reverse/repeater/5500） | — |

### 2.9b 网关 → 中继器出口（云手机跳出局域网，远期）

- **思路**：网关已连接满内网设备，由网关作为**统一 RFB 代理**拨向公网中继器：
  外部标准 VNC 客户端 → 中继器 → 网关 → 内网设备 5901。
- 网关角色：对设备 = RFB 客户端；对中继器 = RFB 服务端；中间双向转发画面/输入。
- 价值：内网设备无需逐个配中继、无需暴露内网；一条统一出口。
- 前提：中继器**可查询**（自建 + 目录 API，避免遍历 ID）；公网场景需**强 ID/口令**。
- 与 frp 互补：frp 暴露控制台网页；中继器出口让标准 VNC 客户端接入设备。

### 2.10 配置通道（纠正“通过 5801 配置 5901 参数”的错误认知）

- 5801/5901 **不承载任何配置 API**
- 配置参数的正确途径：手机设置页 / Managed.plist 预置；未来走**能力清单 + 配置命令通道**
  （经网关 → 手机写 NSUserDefaults → 应用，部分热生效、部分需重启 server）

### 2.11 直连规则

浏览器只能访问 HTTP（5801/8080）；5901/18081 是二进制协议端口，浏览器直连会得到
无效响应（如 ERR_INVALID_HTTP_RESPONSE），属正常行为，不是故障。

## 第三章 开发铁律

1. **能力层唯一**：设备操作只走 RFB→IOHID，禁止任何前端自造输入协议。
2. **能力清单单一来源（控制 UI 数据驱动）**：手机注册上报“能力清单”
   （操作能力 + 可配置参数）；控制台**按清单渲染**操作按钮与设置面板。
   **禁止**：复制组件代码、嵌入手机 5801 页面、做 5801 路由入口。
3. **手机主动注册、网关被动等待**：不做轮询保活；重连靠手机拨号。
4. **设备身份唯一**：以 `deviceId` 为准；mDNS/探测只是发现补充，必须合并去重，
   禁止同一手机出现两条记录。
5. **单注册通道**：只保留一条（TCP JSON），不维护冗余通道。
6. **协议先行**：跨端新能力先定契约（更新本宪章协议节），两端各自自测后再联调。
7. **复用不造轮子**：noVNC core、LibVNCServer 直接复用；不自研 RFB 客户端/服务端。
8. **状态以网关为准**：前端不持久化设备状态，刷新一律从网关拉取。
9. **改动可验证**：IPA 改动必须 CI 编译通过；网关改动必须有测试；未验证不声称完成。
10. **边界明确**：不做过度 Agent 通道/TLS/商业化（见 1.3）。

## 第四章 代码结构与职责边界

4.1 **控制台 = 路由 + 管理 + 命令转发**：设备墙、注册/心跳、在线状态、添加/删除/编辑、
     转发、鉴权；操作 UI 由能力清单驱动渲染，不内置写死的按钮逻辑。
4.2 **能力清单（capability manifest）**（已定稿，协议见 7.3）：
     - 注册消息上报：capabilities（deviceOps 数组）、configs（可配置参数快照）、
       screen（宽高）、httpPort
     - 控制台据此渲染：操作按钮（来自 capabilities）+ 设备详情（configs/screen/httpPort）
4.3 **操作命令**：经网关转发到手机执行。
     - 现阶段：RFB 事件（keysym/pointer）经 5901 执行（现成通道）
     - 未来可选：扩展双向命令通道 `{"type":"op","op":"home"}`（更纯的命令 API）
4.4 **配置命令**（未来实现）：`{"type":"set","key":"scale","value":0.75}` →
     手机写 prefs + 应用（部分热生效、部分重启 server）。
4.5 **控制台本地操作**（不进能力清单）：适配、全屏、断开（浏览器/会话侧）。
4.6 **IPA 结构**：`TRGatewayClient`（注册/心跳/真实设备名/未来上报能力清单）、
     `trollvncmanager`（守护+看门狗）、App（初始化页、模式化设置、BGTask 保活）；
     RFB 内核（`trollvncserver`+`STHIDEventGenerator`）零改动。

## 第五章 能力模型（手机 = 能力提供方）

5.1 **能力清单 = 操作能力 + 可配置参数**，由手机注册时上报。
5.2 **操作能力（deviceOps）**：home、power、volup、voldn、mute、briup、bridn、
     keyboard、clipboard（对应 RFB keysym / 中键注入）。
     **适配/全屏/断开是控制台本地操作，不属于手机能力。**
5.3 **可配置参数（configs）**：scale、frameRateSpec、port、passwords、orientationSync、
     naturalScroll、keepAliveSec、httpPort、bonjourEnabled 等（IPA 设置全部项）。
5.4 **控制台职责**：按清单渲染 + 转发两类命令（操作命令 / 配置命令）；
     不复制代码、不嵌 5801、不做 5801 路由入口。

## 第六章 UI/UX 规范

6.1 **设备墙**：显示**全部设备**（在线+离线）；离线置灰并标注「离线 · 上次在线」；
     仅手动删除才移除。
6.2 **卡片比例**：未连接默认 9:16；连接后按设备真实宽高比适配统一画布。
6.3 **墙缩放**：滑块统一缩放所有卡片（`--zoom`，持久化）。
6.4 **聚焦视图（桌面）**：点击卡片 → 左侧大屏（进入前用已知设备比例预置宽度，禁止闪烁）
     + 操作列紧贴屏幕右缘（图标 + hover）+ 最右侧保留其它设备墙。
6.5 **移动端（H5）**：点击卡片 → 全屏只显示屏幕 + 悬浮按钮 → 菜单（全部操作 + 退出控制/返回）。
6.6 **手机 web（5801）**：自动连接页；桌面=居中屏幕+右侧紧贴面板；移动=全屏+悬浮菜单；
     操作集与控制台一致（同一能力清单驱动）——webclients/caps.js 与网关 web/caps.js 同源、
     与 IPA TRGatewayClient._capabilities 一致，禁止单侧改动。
6.7 **墙缩略图只读**：viewOnly + pointer-events:none；点击卡片=进大屏控制，不误触设备。
6.8 **群控**：选主屏 + 勾选广播；广播输入到同组其它设备的墙会话（网关侧写入）。

6.9 **App 外壳（v2.0 修订）**：App 根为 UITabBarController，**三 Tab** = 连接（Hero/网关/扫码直连/客户端入口）、
     控制（**WKWebView 容器 `TVNCConsoleWebViewController` 加载网关 H5 手机控制台**——原生设备墙/大屏已删除，见 6.11）、设置（prefs 分组表单：
     网关/直连参数/安全/画面与性能/输入/通知与保活/高级/关于与诊断）。

6.10 **直控模式（PC 控制台，2026-08-11 新增）**：
    - PC 控制台批量按钮左侧「直控」按钮：为全部在线真实设备建立**非 ctrl 可输入 RFB**，卡片直达控制——
      这是 6.7「点击卡片=进大屏」的**例外**（直控期间点击卡片不聚焦，悬停放大/引导提示失效）。
    - **同设备单会话硬约束**：设备仅 1 条隧道 + 1 个 5901 连接（`_localFd` 单数），
      同设备同时仅允许 **1 个活跃 VNC 会话**；网关 `handleVncSocket` 新会话顶掉同设备其他非 ctrl 会话（4001），
      随后按"首会话"触发 5901 重建保证干净握手。多会话共享 5901 会同时驱动协议状态机
      → 握手/输入字节串扰 → noVNC「Unexpected server message (type N)」断开。
    - **退出直控必须真正 close WS**（前端 `exitDirectMode` 显式 `closeRfb`），
      否则残留会话持续占用设备推流、再次进入时会话堆积（曾致 `sessions=2` 残留）。
    - 直控/同步推流 canvas 居中放置（等比 contain），与截图缩略图显示位置一致。

6.11 **卡片墙变化拉图（唯一机制，2026-08-11 定版）**：
    - 控制台卡片墙 = **H5 hash 门控变化拉图**：每设备 `invoke screen.hash`（轻量 pHash）与 `lastHash` 比对，
      **变化才 `invoke screenshot` 拉帧渲染**；静止零图片流量；双速退避（变化 1s / 静止 base×1.5^n 至 15s 封顶）。
    - **screen_changed 增量链路已整体删除**（设备 `screen.subscribe` 订阅上报 → 网关 `screenChangedAt` 标记 → 前端 `changedSince` 增量拉图），
      回归 hash 门控唯一拉图；`screen.subscribe` 能力/命令/`tvCtlBroadcastToSubscribers` 已删，
      tvCtl 广播基础设施保留（服务 `clients.subscribe`，连接页客户端卡在用）。
    - IPA 原生卡片墙（TRWallTileWebView / 原生 hash 轮询）已删除——控制 Tab = WKWebView 容器加载网关 H5，卡片墙由网页端统一承担。

## 第七章 协议约定

7.1 **注册/心跳（TCP JSON，18081，唯一注册通道）**：
   - {"type":"register","deviceId":"<uuid>","name":"<真实设备名>","vncPort":5901,"capabilities":[...],"configs":{...},"screen":{...},"httpPort":5801}
   - 能力清单字段见 7.3；旧客户端缺省不报错，网关按默认全集渲染
   - 服务端回 {"type":"ack","deviceId":...,"name":...}
   - 每 30s 发 {"type":"hello"}；90s 无心跳判离线
   - **单通道铁律**：仅 TCP JSON 一条注册通道；WS 注册端点已废弃（2026-08-07）
7.2 **设备操作（RFB）**：Home=keysym 0xff50；电源=中键注入；音量=XF86 0x1008ff13/11/12；
     亮度=0x1008ff03/02；剪贴板=RFB 剪贴板扩展。
7.3 **能力清单（capability manifest，v1 只上报不下发）**：
   - capabilities：操作能力数组，固定枚举 = home、power、volup、voldn、mute、briup、
     bridn、keyboard、clipboard；**适配/全屏/断开是控制台本地操作，不入清单**。
   - configs：可配置参数快照（值=当前 NSUserDefaults），白名单 =
     scale、frameRateSpec、port、httpPort、bonjourEnabled、orientationSync、
     orientationPadFix、naturalScroll、keepAliveSec、clipboardEnabled、viewOnly、
     modifierMap、wheelStepPx、serverCursor、reverseMode；
     **密码类只上报存在性（hasPassword / hasViewOnlyPassword），不上报明文**。
   - screen：{"width":1170,"height":2532}（UIScreen 原生像素）。
   - httpPort：手机 HTTP 网页端口（如 5801；仅信息展示，控制台不做 5801 路由）。
   - 网关按清单渲染控制台操作面板/详情；缺省 = 默认全集（9 项）。
7.4 **命令通道（v2.0 修订）**：
   - 网关 → 手机（**隧道 FT_CMD 帧优先**，注册通道 JSON 行回退）：`{"type":"cmd","cmd":"ping","id":"<关联ID>","ts":<ms>}`
   - 手机 → 网关：`{"type":"ack","cmd":"ping","id":"<关联ID>","ok":true}`
   - 网关**专用 REST 端点**：`POST /api/devices/:id/invoke|configs|restart|ping` 下发并等待 ack（默认 5s，可传 `timeout`，超时 504；离线发送失败 504）
   - **通用 `POST /api/devices/:id/command` 已删除（2026-08-11）**——H5 全走专用端点，禁止回归。
## 第八章 测试与验收

8.1 **测试设备分辨率**：一律使用真实手机分辨率（如 1170×2532），禁止 640×480。
8.2 **网关测试**：冒烟测试（桥接/广播/鉴权）、注册/心跳测试、去重测试必须全绿。
8.3 **IPA 验收**：任何 IPA 改动必须 CI 编译通过；真机按《验收测试清单》执行。
8.4 **真机验收点**：装包→配网关→自动注册→网页点开即连→操控→后台存活实测记录。

## 第九章 已知边界与决策记录

9.1 **iOS 后台挂起**：心跳无法阻止挂起；BGTask 后台刷新（15–60min 周期）延长存活；
     锁屏很久仍可能掉线 → 离线可见 + 手机解锁自动重连。
9.2 **Silent Push 唤醒**：技术上可做（TrollStore 有环境基础），但当前 IPA 未注册推送；
     依赖 Apple 开发者账号 + APNs 外网 + 真机可靠性实测；列为“后期可选”，默认不投入。
9.3 **通知治理**：连接/断开本地通知（`ClientNotifsEnabled`/`SingleNotifEnabled`）在农场
     场景会被控制台自身连接刷屏 → **默认关闭**（Managed.plist 预置）；保留“首连单条”可选。
9.4 **反向模式与 HTTP 互斥**：反向开启会关本地端口/HTTP/Bonjour。
9.5 **mDNS 部分 AP 不可达**：手动输入网关兜底。
9.6 **私有 API 随 iOS 升级失效**：锁定 TrollStore 支持范围，升级前灰度。

## 第十章 变更流程

10.1 任何改动先对照本宪法：是否违反铁律？是否动了不该动的层（能力层/清单模型）？
10.2 跨端改动：先定协议（第七章）→ 两端各自实现 → 自测 → CI 编译 → 真机/联调验收。
10.3 能力清单相关：以 IPA 能力为准定义；控制台只消费清单，不复制代码。
10.4 本宪法随重大决策更新，更新需说明变更点与原因。

---

## 附录 A：端口与访问模型速查（开发依仗）

- 手机 5901 = 原生 RFB 双向控制通道（画面+操作）→ **控制台/网关唯一连它**
- 手机 5801 = HTTP 网页文件服务器（noVNC 页面）→ 仅浏览器直连手机时用，无配置 API
- 网关 8080 = 控制台 = 外网“包装面”（frp/隧道入口）
- 网关 18081 = 注册/心跳
- 外部访问：① 经网关（主） ② VNC 客户端直连 5901（③ 手机反向连接已废弃）
- 配置：不走 5801/5901；走设置/Managed.plist/未来配置命令通道

## 附录 B：IPA 能力全景与开发提示

| IPA 能力 | 说明 | 开发提示 |
|---|---|---|
| RFB 服务 + IOHID 注入（30 点触控/键盘/Home/电源/音量/亮度/媒体） | 能力层 | 控制台操作命令都走它 |
| ScreenCapturer（IOSurface 抓屏/缩放/脏区） | 画面 | 卡片按 `screen` 上报渲染 |
| ~~反向连接（viewer/repeater）~~ | ~~配置能力~~ | **已删除**（Phase 6 全链路清理） |
| Bonjour（_rfb._tcp / _http._tcp） | 发现 | mDNS 只作发现补充 |
| Keep-alive（-A）+ hardwareUnlock | 保活/唤醒 | 控制台“保持唤醒”开关（远期） |
| 剪贴板同步（UltraVNC UTF-8） | 能力 | 可加“读取设备剪贴板→控制台” |
| 通知（BulletinManager） | 本地通知 | 农场默认关闭（9.3） |
| 看门狗 + SBS 预启动 + BGTask | 后台存活 | 已实现 BGTask |
| 自签证书（ZTSelfSignedCertificate） | TLS 能力 | 远期公网直连用（宪法内网不做） |
| TRGatewayClient 注册/心跳 | 我们的 | 未来上报能力清单 |



## 附录 D：控制端（云手机操作台）与中继器出口（远期设计）

> **2026-08-11 注**：D1「手机端 viewer」定位已由 **H5 手机控制台**取代（浏览器访问网关 / IPA 控制 Tab 容器内即网页），本节保留作远期参考。

### D1 控制端（手机端 viewer）
- 定位：手机作为**移动轻量控制台**（RFB viewer），与网页控制台互补；能力层唯一。
- 页面：底部导航「控制端」→ **进入即设备画面卡片墙**。
- 卡片来源分两类：
  1. **直连设备**（主动拨入本机监听端口，无中继 ID）→ 卡片实时画面
  2. **中继设备**（携带中继 ID 接入中继器）→ 同样卡片实时画面
- 交互：**点击画面卡片 → 全屏控制**（Home/电源/音量…操作面板 + 退出控制）。
- 接入配置：监听 VNC（端口）+ 中继器地址 + 设备 ID 列表。
- 清单策略：直连=谁拨进来看谁（无清单问题）；中继=依赖**可查询中继器/网关目录**，**禁止遍历中继 ID**。

### D2 中继器策略
- 优先**自建中继器并带目录 API**（可查询在线设备），控制端/网关据此拿清单。
- 不采用：第三方哑中继 + 遍历 ID（性能不可接受）；完全独立 + 无清单（信息不对称，行不通）。

### D3 公网安全
- 中继器按 ID 撮合，公网暴露需**强 ID/口令**；宪法内网自用默认不做鉴权，公网场景单独评估。
## 附录 C：通知治理

- 触发：VNC 客户端连接/断开本地通知（ClientNotifsEnabled / SingleNotifEnabled）
- 农场场景：控制台自身连接会刷屏 → 默认关闭（Managed.plist 预置），保留“首连单条”可选
- 如需有意义的通知：仅对非网关来源连接提示（协议层打来源标记，远期）
