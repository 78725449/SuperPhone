# TrollVNC 能力追踪清单（v1.1 · 2026-08-09 Phase 11 规划版）

> 本文档是 IPA 所有能力的**唯一追踪载体**，独立于改造计划文档，专注支持能力的**增删查改**与**开发编排**。
> 改造计划文档负责"为什么这样做"，本文档负责"做了什么、做到哪、谁先做"。
> **每次开发前必读本文档**，确认当前进度与批次后再启动；每完成一项能力注册/实现，**立即更新对应行的状态与变更记录**。

---

## 0. 文档使用规范

### 0.1 状态标记

| 标记 | 含义 | 动作 |
|---|---|---|
| ✅ | 已注册可用 | 无需动作 |
| 🆕 | 待注册（原生方法已实现，仅需注册表加行） | 加行注册 |
| 🐛 | BUG（原生方法有问题） | 修复原生实现 |
| 🐛空 | 空壳（注册了但 executor 无实现） | 真正实现 executor |
| 🔁 | 合并/重构（语义混乱需重构） | 重构后更新 |
| 🆕待实现 | 需新代码（原生无对应方法） | 编写新代码 |
| ❌ | 废弃/移除 | 从注册表删除 |

### 0.2 优先级定义

| 优先级 | 含义 | 触发条件 |
|---|---|---|
| P0 | 修复空壳/BUG/重复造轮子 | 阻塞正确性，必须最先处理 |
| P1 | 即用即注册 | 原生方法完全齐备，零开发成本 |
| P2 | 桥接实现 | 需新增路由执行器（如 localcmd 桥接器） |
| P3 | 真正实现 | 需编写新代码（原生无对应方法） |
| P4 | 补声明注册 | .mm 已实现但未在 .h 暴露，需补声明 |
| P5 | 扩展增强 | 可选能力，非核心路径 |

### 0.3 编排批次（按依赖排序）

| 批次 | 内容 | 依赖 | 验证方式 |
|---|---|---|---|
| Batch 0 | 修复 P0 空壳/BUG/合并 | 无 | 修复后单测：shake 注入/screenshot 返回 base64/clipboard.get 返回 text |
| Batch 1 | 注册 P1 即用能力 | Batch 0 | `query caps` 返回新增能力 ID |
| Batch 2 | 实现 localcmd 桥接器 + 注册 P2 | Batch 1 | `invoke clients.count` 返回数字 |
| Batch 3 | 实现 P3 新代码 | Batch 2 | screenshot/type.paste 功能验证 |
| Batch 4 | 补 .h 声明 + 注册 P4 | Batch 1 | 数组版多指触控验证 |
| Batch 5 | 注册 P5 扩展能力 | Batch 1 | 系统查询/ScreenCapturer 扩展验证 |
| Batch 6a | Phase 11：schema 扩展（menu/scenes/batch）+ macro.run | 无 | `query caps` 返回 3 新字段；macro.run 多步执行 |
| Batch 6b | Phase 11：app.list/current/open + screen.hash/diff | Batch 6a | `invoke app.list` 返回应用数组；`invoke screen.hash` 返回 hex |
| Batch 6c | Phase 11：app.icon + screen.waitStable/subscribe | Batch 6b | `invoke screen.waitStable` 等画面稳定返回 |
| 前端并行 | Phase 11：三端菜单分层 + 批量过滤 + AI 通道 | Batch 6a | 前端菜单分层渲染；批量栏按 batch 过滤 |

### 0.4 增删查改操作规范

- **增**：在对应模块表格末尾加行 → 在「变更记录」加一行
- **删**：状态改为 ❌ → 在「变更记录」加一行说明原因 → 代码层移除注册
- **查**：按模块分组浏览 / 按 Ctrl+F 搜索能力 ID / 按状态列筛选
- **改**：修改表格字段（状态/优先级/批次）→ 在「变更记录」加一行

---

## 1. 控制型能力清单

> 路由类型：`hid`=STHIDEventGenerator / `touch`=归一化坐标+HID / `stylus`=触控笔 / `localcmd`=5901 RFB 扩展消息（type 0x50/0x80，Phase 2 起；原 46752 控制端口已删除） / `native`=ObjC方法 / `watchdog`=TRWatchDog
>
> **菜单分层（07 §3.3，2026-08-12 能力与菜单统一 Phase 1 落地）**：每项能力带 `menu` 元数据字段——
> - `primary`=人工遥控（按键 12 对象 + 动作区 5 项），进控制台侧边栏/悬浮、卡片菜单
> - `secondary`=管理/查询/配置（~28 项），进卡片菜单、批量菜单
> - `internal`=AI 底层原语（~40 项），不进任何菜单，invoke 可用
>
> 已标 internal：成对按下/抬起原语（home.down/up 等 14 项）、key.down/up、consumer.*/hid.*、touch.*（含 touch.tap/swipe/longPress，画布直操语义）、stylus.*、screen.hash/diff/waitStable、screen.capture.* 等。**触控从菜单移除，改画布直操**（07 §4.1）。

### 1.1 HID 硬件按键 - 单击类（route: hid）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `home` | Home 键 | 🏠 | 无 | `[hid menuPress]` | ✅ | — | — |
| `power` | 电源 | ⏻ | 无 | `[hid powerPress]` | ✅ | — | — |
| `volup` | 音量+ | 🔊 | 无 | `[hid volumeIncrementPress]` | ✅ | — | — |
| `voldn` | 音量− | 🔉 | 无 | `[hid volumeDecrementPress]` | ✅ | — | — |
| `mute` | 静音 | 🔇 | 无 | `[hid mutePress]` | ✅ | — | — |
| `briup` | 亮度+ | ☀️ | 无 | `[hid displayBrightnessIncrementPress]` | ✅ | — | — |
| `bridn` | 亮度− | 🌙 | 无 | `[hid displayBrightnessDecrementPress]` | ✅ | — | — |
| `keyboard` | 切换屏幕键盘 | ⌨️ | 无 | `[hid toggleOnScreenKeyboard]` | ✅ | — | — |
| `spotlight` | 切换搜索 | 🔍 | 无 | `[hid toggleSpotlight]` | ✅ | — | — |

### 1.2 HID 硬件按键 - 多击/长按类（route: hid）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `home.double` | 双击Home（多任务） | 🏠×2 | 无 | `[hid menuDoublePress]` | ✅ | — | — |
| `home.long` | 长按Home（Siri） | 🏠⌛ | 无 | `[hid menuLongPress]` | ✅ | — | — |
| `power.double` | 双击电源（截屏存相册） | ⏻×2 | 无 | `[hid powerDoublePress]` | ✅ | — | — |
| `power.triple` | 三击电源 | ⏻×3 | 无 | `[hid powerTriplePress]` | ✅ | — | — |
| `power.long` | 长按电源（关机界面） | ⏻⌛ | 无 | `[hid powerLongPress]` | ✅ | — | — |
| `snapshot` | Home+Power截屏 | 📸 | 无 | `[hid snapshotPress]` | ✅ | — | — |
| `shake` | 摇晃设备 | 📳 | 无 | — | ❌ | P0 | Batch 0 |

> **P0 已移除**：`shakeIt` 方法体为空且无现成加速度计注入基础设施。基于群控场景分析（摇晃手势仅 shake-to-undo/微信摇一摇等低频应用特定场景，非核心能力），实现需 IOHIDEventCreateAccelerometer 私有 API（复杂、随 iOS 升级失效风险高），收益与风险不匹配。Batch 0 已从 .h/.mm 删除方法，能力移除。

### 1.3 HID 硬件按键 - 底层 Down/Up（route: hid）

> 支持长按/组合键，按下与抬起分离。

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `home.down` | Home按下 | 无 | `[hid menuDown]` | ✅ | — | — |
| `home.up` | Home抬起 | 无 | `[hid menuUp]` | ✅ | — | — |
| `power.down` | 电源按下 | 无 | `[hid powerDown]` | ✅ | — | — |
| `power.up` | 电源抬起 | 无 | `[hid powerUp]` | ✅ | — | — |
| `volup.down` | 音量+按下 | 无 | `[hid volumeIncrementDown]` | ✅ | — | — |
| `volup.up` | 音量+抬起 | 无 | `[hid volumeIncrementUp]` | ✅ | — | — |
| `voldn.down` | 音量−按下 | 无 | `[hid volumeDecrementDown]` | ✅ | — | — |
| `voldn.up` | 音量−抬起 | 无 | `[hid volumeDecrementUp]` | ✅ | — | — |
| `mute.down` | 静音按下 | 无 | `[hid muteDown]` | ✅ | — | — |
| `mute.up` | 静音抬起 | 无 | `[hid muteUp]` | ✅ | — | — |
| `briup.down` | 亮度+按下 | 无 | `[hid displayBrightnessIncrementDown]` | ✅ | — | — |
| `briup.up` | 亮度+抬起 | 无 | `[hid displayBrightnessIncrementUp]` | ✅ | — | — |
| `bridn.down` | 亮度−按下 | 无 | `[hid displayBrightnessDecrementDown]` | ✅ | — | — |
| `bridn.up` | 亮度−抬起 | 无 | `[hid displayBrightnessDecrementUp]` | ✅ | — | — |
| `hwlock` | 硬件键盘锁 | 无 | `[hid hardwareLock]` | ✅ | — | — |
| `hwunlock` | 硬件键盘解锁 | 无 | `[hid hardwareUnlock]` | ✅ | — | — |
| `releasekeys` | 释放所有按键 | 无 | `[hid releaseEveryKeys]` | ✅ | — | — |

### 1.4 HID 通用 Consumer/Page（route: hid）

> 支持任意 HID 页+用法，高级能力。

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `consumer.press` | Consumer用法按下 | `{usage:int}` | `[hid otherConsumerUsagePress:usage]` | ✅ | — | — |
| `consumer.down` | Consumer按下 | `{usage:int}` | `[hid otherConsumerUsageDown:usage]` | ✅ | — | — |
| `consumer.up` | Consumer抬起 | `{usage:int}` | `[hid otherConsumerUsageUp:usage]` | ✅ | — | — |
| `hid.press` | 任意页+用法按下 | `{page:int,usage:int}` | `[hid otherPage:page usagePress:usage]` | ✅ | — | — |
| `hid.down` | 任意页+用法按下 | `{page:int,usage:int}` | `[hid otherPage:page usageDown:usage]` | ✅ | — | — |
| `hid.up` | 任意页+用法抬起 | `{page:int,usage:int}` | `[hid otherPage:page usageUp:usage]` | ✅ | — | — |

> **执行器优化**：`consumer.*` 是 `hid.*` 在 `page=kHIDPage_Consumer` 时的特例，executor 应复用 hid.* 实现避免重复代码。

### 1.5 触控类（route: touch，归一化0-1坐标）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `touch.tap` | 点击 | 👆 | `{x,y}` | `[hid tap:pt]` | ✅ | — | — |
| `touch.swipe` | 线性滑动 | ↔ | `{x1,y1,x2,y2,duration?}` | `[hid dragLinearWithStartPoint:endPoint:duration:]` | ✅ | — | — |
| `touch.doubleTap` | 双击 | 👆×2 | `{x,y}` | `[hid doubleTap:pt]` | ✅ | — | — |
| `touch.twoFingerTap` | 双指点击 | ✌️ | `{x,y}` | `[hid twoFingerTap:pt]` | ✅ | — | — |
| `touch.threeFingerTap` | 三指点击 | 🖐️ | `{x,y}` | `[hid threeFingerTap:pt]` | ✅ | — | — |
| `touch.longPress` | 长按 | 👆⌛ | `{x,y}` | `[hid longPress:pt]` | ✅ | — | — |
| `touch.curveSwipe` | 曲线滑动 | 〰 | `{x1,y1,x2,y2,duration?}` | `[hid dragCurveWithStartPoint:endPoint:duration:]` | ✅ | — | — |
| `touch.pinch` | 捏合缩放 | 🤏 | `{bounds,scale,angle,duration}` | `[hid pinchLinearInBounds:bounds scale:angle:duration:]` | ✅ | — | — |
| `touch.down` | 触摸按下 | 👇 | `{x,y}` | `[hid touchDown:pt]` | ✅ | — | — |
| `touch.up` | 触摸抬起 | 👆 | `{x,y}` | `[hid liftUp:pt]` | ✅ | — | — |
| `touch.downMulti` | 多指同点按下 | 👥 | `{x,y,count}` | `[hid touchDown:pt touchCount:count]` | ✅ | — | — |
| `touch.upMulti` | 多指同点抬起 | 👥 | `{x,y,count}` | `[hid liftUp:pt touchCount:count]` | ✅ | — | — |
| `touch.downMultiAt` | 多指异点按下 | 👥 | `{points:[{x,y},...],count}` | `[hid touchDownAtPoints:pts touchCount:count]` | ✅ | — | — |
| `touch.upMultiAt` | 多指异点抬起 | 👥 | `{points:[{x,y},...],count}` | `[hid liftUpAtPoints:pts touchCount:count]` | ✅ | — | — |
| `touch.taps` | 通用N击M指 | 👆✖️N | `{x,y,tapCount,touchCount,delay}` | `[hid sendTaps:tapCount location:pt numberOfTouches:touchCount delayBetweenTaps:delay]` | ✅ | — | — |
| `touch.reset` | 重置触摸状态 | 🔄 | 无 | `[hid dispatchHandResetEvent]` | ✅ | — | — |
| `touch.event` | 单事件派发 | 🎞️ | `{eventInfo}` | `[hid dispatchEventWithInfo:eventInfo]` | ✅ | — | — |
| `touch.eventStream` | 自定义事件流 | 🎞️ | `{eventInfo}` | `[hid sendEventStream:eventInfo]` | ✅ | — | — |

> **P4 已完成**：`touchDownAtPoints:`/`liftUpAtPoints:`/`dispatchHandResetEvent`/`dispatchEventWithInfo:` 已在 .h 补公开声明，4 项能力已注册。多指异点能力参数为 `{points:[{x,y},...]}` 数组，executor 内 malloc CGPoint C 数组转换归一化坐标后调用原生方法。
> **执行器优化**：tap/doubleTap/twoFingerTap/threeFingerTap 都是 `sendTaps:` 的特例，executor 应统一调用避免重复。

### 1.6 触控笔类（route: stylus，归一化0-1坐标）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `stylus.tap` | 触控笔点击 | ✏️ | `{x,y,azimuth,altitude,pressure}` | `[hid stylusTapAtPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ | — | — |
| `stylus.down` | 触控笔按下 | ✏️⬇ | `{x,y,azimuth,altitude,pressure}` | `[hid stylusDownAtPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ | — | — |
| `stylus.move` | 触控笔移动 | ✏️➡ | `{x,y,azimuth,altitude,pressure}` | `[hid stylusMoveToPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ | — | — |
| `stylus.up` | 触控笔抬起 | ✏️⬆ | `{x,y}` | `[hid stylusUpAtPoint:pt]` | ✅ | — | — |

### 1.7 键盘类（route: hid）

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `key.press` | 按键 | `{char:string(1)}` | `[hid keyPress:char]`（同步0.05s，仅ASCII） | ✅ | — | — |
| `key.down` | 按键按下 | `{char:string(1)}` | `[hid keyDown:char]` | ✅ | — | — |
| `key.up` | 按键抬起 | `{char:string(1)}` | `[hid keyUp:char]` | ✅ | — | — |

### 1.8 文本输入类（route: native/hid）

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `type.text` | 文本输入（ASCII逐字符） | `{text}` | 逐字符 `[hid keyPress:]`（仅ASCII，0.05s/字符） | ✅ | — | — |
| `type.paste` | 粘贴输入（任意文本） | `{text}` | `[ClipboardManager setStringFromRemote:text]` + Cmd+V 键序列 | ✅ | — | — |

> **避免重复造轮子**：`type.paste` 内部复用 `clipboard.set` 设置剪贴板，再模拟粘贴键，支持中文/emoji。

### 1.9 剪贴板类（route: native）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `clipboard.get` | 获取剪贴板 | 📋 | 无 | `[ClipboardManager currentString]` 返回 `{text}` | ✅ | — | — |
| `clipboard.set` | 设置剪贴板 | 📋 | `{text}` | `[ClipboardManager setStringFromRemote:text]` | ✅ | — | — |

> **P0 合并修正**：原 `clipboard.paste` 实现是「读 currentString → 再 setStringFromRemote 写回」的空操作（读+写回同一内容），既非真正粘贴也不返回文本。**合并为 `clipboard.get`**（返回 `{text}`），移除 `clipboard.paste`。

### 1.10 截屏类（route: native）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `screenshot` | 静默截图 | 📷 | 无 | `[ScreenCapturer.captureSingleFrameImage]` → JPEG base64 | ✅ | — | — |
| `screenshot.system` | 系统截屏 | 📸 | 无 | `[hid snapshotPress]` 或 `[hid powerDoublePress]` | ✅ | — | — |

> **P0 已修复**：原 `screenshot` 注册为空壳（executor 返回 `code:99 "待实现"`），未调用 ScreenCapturer。Batch 0 已在 ScreenCapturer 新增 `captureSingleFrameImage` 公共方法（renderDisplayToScreenSurface→CVPixelBuffer→CIImage→UIImage），executor 调用返回 JPEG base64 + width/height。
> **互补关系**：`screenshot` 静默不存相册（AI自动化用）；`screenshot.system` 触发系统动画存相册（用户用）。

### 1.11 通知类（route: native）

| ID | 标题 | 图标 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|
| `notify.banner` | 推送横幅 | 🔔 | `{content,userInfo?}` | `[BulletinManager popBannerWithContent:content userInfo:userInfo]` | ✅ | — | — |
| `notify.banner.update` | 更新横幅 | 🔔 | `{content,badgeCount,userInfo?}` | `[BulletinManager updateSingleBannerWithContent:content badgeCount:badgeCount userInfo:userInfo]` | ✅ | — | — |
| `notify.revoke` | 撤销通知 | 🔕 | 无 | `[BulletinManager revokeSingleNotification]` | ✅ | — | — |
| `notify.revokeAll` | 撤销全部通知 | 🔕 | 无 | `[BulletinManager revokeAllNotifications]` | ✅ | — | — |

### 1.12 本地命令类（route: localcmd，经 5901 RFB 扩展消息）

> **Phase 2 落地（2026-08-12）**：46752 控制端口已整体删除。本地命令能力改走 5901 RFB 扩展消息（client→server type 0x50，server→client type 0x80，JSON 负载），执行体由 `_executeLocalCmd`（socket 连 46752）替换为 `_rfbCommand`（持久 RFB 5901 连接 + cap.hello 管理标记 + 扩展消息）。`clients.subscribe` 已删除（无消费方，H5 用轮询 hash）。

| ID | 标题 | 参数 | 扩展消息 op | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `clients.count` | 客户端数量 | 无 | `clients.count` | ✅ | — | — |
| `clients.list` | 客户端列表 | 无 | `clients.list` | ✅ | — | — |
| `clients.disconnect` | 断开客户端 | `{clientId}` 或 `{clientId:"ALL"}` | `clients.disconnect` | ✅ | — | — |
| `clients.block` | 阻止并加黑名单 | `{clientId}` | `clients.block`（断开+加黑名单） | ✅ | — | — |
| `clients.unblock` | 解除主机黑名单 | `{host}` | `clients.unblock` | ✅ | — | — |
| `clients.blocked.list` | 黑名单列表 | 无 | `clients.blocked.list` | ✅ | — | — |
| ~~`clients.subscribe`~~ | ~~订阅推送~~ | ~~`{enable:bool}`~~ | **已删除**（Phase 2：随 46752 移除，无消费方） | ❌ | — | — |

> **P2 桥接**：`TRCapRouteLocalCmd` 路由类型经 `_rfbCommand` 走 5901 RFB 扩展消息（原 socket 127.0.0.1:46752 文本命令桥接已删除）。
> **P3 已完成**：`blocked.list` 已迁移为 `clients.blocked.list` 扩展消息 op（遍历 gBlockedHosts 返回 hosts 数组）。

### 1.13 服务控制类（route: watchdog）

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `service.restart` | 重启服务 | 无 | `[TRGatewayClient.sharedClient.restartHandler]()`（注入 [gWatchDog restart]） | ✅ | — | — |
| `service.signal` | 发送信号 | `{signal:int}` | `[gWatchDog sendSignal:signal]` | ✅ | — | — |

> **P0 统一**：原 `restart` 仅通过网关 cmd 通道调用 `restartHandler`，未注册为能力。统一注册为 `service.restart`，使 `invoke` 与网关 cmd 双入口均可调用。不保留 `stop`（设备离线无法远程恢复）。

### 1.14 服务状态查询类（route: native）

| ID | 标题 | 返回值 | 原生属性/方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `service.state` | 服务状态 | `{state:stopped/starting/running/stopping/crashed/throttled}` | `gWatchDog.state` | ✅ | — | — |
| `service.info` | 服务详情 | `{pid,restartCount,uptime,lastExitTime,lastExitStatus,lastUncaughtSignal,lastTerminationReason,timeUntilNextRestart}` | `gWatchDog` 9个readonly属性聚合 | ✅ | — | — |
| `service.isActive` | 是否活跃 | bool | `gWatchDog.isActive` | ✅ | — | — |
| `service.isThrottled` | 是否限流 | bool | `gWatchDog.isThrottled` | ✅ | — | — |
| `service.validate` | 校验配置 | `{ok:bool,error?:string}` | `[gWatchDog validateConfigurationWithError:]` | ✅ | — | — |

### 1.15 系统查询类（route: native）

| ID | 标题 | 返回值 | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `sys.version` | 版本信息 | `{scheme,version}` | `sPackageScheme`/`sPackageVersion` | ✅ | — | — |
| `sys.configSnapshot` | 配置快照 | `{port,httpPort,scale,fps,...}` 30+参数 | `tvConfigSnapshot()` 函数（已现成） | ✅ | — | — |
| `sys.resolution` | 屏幕分辨率 | `{width,height,nativeScale}` | `gWidth`/`gHeight`/`UIScreen.nativeScale` | ✅ | — | — |
| `sys.rotation` | 当前旋转 | `{quad:0-3}` | `gRotationQuad` 原子变量 | ✅ | — | — |
| `sys.stats.inflight` | 编码帧数 | `{current,max,isThrottled}` | `gInflight`/`gMaxInflightUpdates` | ✅ | — | — |
| `sys.bonjour.txt` | Bonjour TXT | `{vn,vo,hp,fps}` | `bonjourTXTRecord()` 函数 | ✅ | — | — |

### 1.16 ScreenCapturer 扩展类（route: native）

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `screen.capture.start` | 开始流式采集 | 无 | `[ScreenCapturer startCaptureWithFrameHandler:]` | ✅ | — | — |
| `screen.capture.stop` | 停止流式采集 | 无 | `[ScreenCapturer endCapture]` | ✅ | — | — |
| `screen.fps` | 设置帧率范围 | `{min,preferred,max}` | `[ScreenCapturer setPreferredFrameRateWithMin:preferred:max:]` | ✅ | — | — |
| `screen.resolution` | 查询分辨率 | 无 | `ScreenCapturer.renderProperties` | ✅ | — | — |
| `screen.forceRefresh` | 强制下一帧脏 | 无 | `[ScreenCapturer forceNextFrameUpdate]` | ✅ | — | — |

### 1.17 网关客户端类（route: native）

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|
| `gateway.isConnected` | 网关连接状态 | 无 | 返回 `{connected,retryDelay}`（需暴露私有状态） | ✅ | — | — |
| `gateway.reconnect` | 手动重连 | 无 | `[TRGatewayClient stop]; [TRGatewayClient start]` | ✅ | — | — |
| `gateway.deviceInfo` | 设备元数据 | 无 | 返回 `{deviceId,deviceName,vncPort,httpPort,screen}` | ✅ | — | — |

### 1.18 屏幕感知类（route: native，Phase 11 新增）

> 借鉴 hermes-android screen_hash/diff_screen/wait/event_stream。基于 Accelerate framework（vImage + vDSP）实现 pHash 计算管线。
> 技术细节详见 IPA改造计划 Phase 11.4。

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 | menu | scenes | batch |
|---|---|---|---|---|---|---|---|---|---|
| `screen.hash` | 屏幕哈希 | 无 | `[TRScreenHasher computeHashForCurrentFrame]` → pHash 64bit hex | ✅已实现 | P3 | Batch 6b | internal | [ai] | false |
| `screen.diff` | 屏幕差异 | `{baselineHash, threshold?}` | `[TRScreenHasher diffWithBaselineHash:threshold:]` → 汉明距离 | ✅已实现 | P3 | Batch 6b | internal | [ai] | false |
| `screen.waitStable` | 等待画面稳定 | `{maxMs?, stableMs?, intervalMs?, threshold?}` | `[TRScreenHasher waitStableWithMaxMs:stableMs:intervalMs:threshold:]` | ✅已实现 | P5 | Batch 6c | internal | [ai] | false |
| ~~`screen.subscribe`~~ | ~~变化推送~~ | ~~`{enable, throttleMs?, minDistance?}`~~ | ~~`[TRScreenHasher subscribeWithThrottle:minDistance:handler:]`~~ | ❌已删除（2026-08-11，screen_changed 增量链路移除，回归 H5 hash 门控） | P5 | Batch 6c | internal | [ai] | false |

> **pHash 计算管线**（32×32 DCT，先做全屏版本）：
> 01 取帧（ScreenCapturer.captureSingleFrameBuffer，跳过 JPEG）→ 02 缩放 32×32（vImageScale_ARGB8888）→ 03 转灰度（vImageMatrixMultiply，0.299R+0.587G+0.114B）→ 04 DCT-II（vDSP_DCT_Execute，setup 复用）→ 05 取左上 8×8 低频（排除 DC [0][0]）+ 求均值 + 逐位比较 → uint64_t hash
>
> **默认参数**：screen.diff threshold=5；screen.waitStable maxMs=3000/stableMs=500/intervalMs=200/threshold=3
>
> **汉明距离经验值**：<3 基本相同 / 3-8 轻微变化 / 8-15 明显变化 / >15 完全不同
>
> **关键设计**：waitStable 比较相邻帧距离（非与基线），因页面加载渐变
>
> **性能**：单次 pHash ≈0.3ms（vs JPEG 8ms，快 26 倍），60fps CPU <1%，内存 4KB

### 1.19 应用与启动类（route: native，Phase 11 新增）

> 借鉴 hermes-android get_apps/current_app/open_app。巨魔环境通过 LSApplicationWorkspace + SBSCopyFrontmostApplicationDisplayIdentifier 实现。项目已链接 FrontBoardServices.tbd / SpringBoardServices.tbd。

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 | menu | scenes | batch |
|---|---|---|---|---|---|---|---|---|---|
| `app.list` | 应用列表 | `{includeIcons:bool}` | `[TRAppManager allApplications]` → LSApplicationWorkspace 枚举 | 🆕待实现 | P3 | Batch 6b | primary | [single,batch,ai] | false |
| `app.current` | 前台应用 | 无 | `[TRAppManager frontmostApplication]` → SBSCopyFrontmostApplicationDisplayIdentifier | 🆕待实现 | P3 | Batch 6b | primary | [single,batch,ai] | false |
| `app.open` | 打开应用 | `{bundleId}` 或 `{scheme}` | `[TRAppManager openApplicationWithBundleId:]` → openApplicationWithBundleIdentifier / URL Scheme | 🆕待实现 | P3 | Batch 6b | primary | [single,batch,ai] | true |
| `app.icon` | 应用图标 | `{bundleId}` | `[TRAppManager iconForBundleId:]` → LSApplicationRecord.itemIcons → PNG base64 | 🆕待实现 | P5 | Batch 6c | internal | [ai] | false |

### 1.20 自动化编排类（route: native，Phase 11 新增）

> 借鉴 hermes-android android_macro。命令通道 v2 批量 invoke，解决 AI 群控多步操作 RTT 累积延迟。

| ID | 标题 | 参数 | 原生方法 | 状态 | 优先级 | 批次 | menu | scenes | batch |
|---|---|---|---|---|---|---|---|---|---|
| `macro.run` | 步骤序列原子执行 | `{steps:[{cap,args}], stopOnFail:bool}` | `[TRCapabilityRegistry invokeMacroSteps:stopOnFail:]` → 循环 invokeCapability | 🆕待实现 | P2 | Batch 6a | secondary | [batch,ai] | true |

> **执行逻辑**：按 steps 顺序 invoke，每步结果记录到 results 数组；stopOnFail=true 遇失败立即停止，stopOnFail=false 继续执行；不递归（steps 内不可调 macro.run）

### 1.21 设置动作类（route: native，能力与菜单统一 Phase 1 新增）

> 设置项动作入口暴露（07 §7）：Root.plist 的 PSButtonCell 动作注册为**无参 invoke 能力**（不做配置 schema 读写）。对齐 TVNCRootListController generateKeys/searchGateway 非 UI 核心逻辑；invoke 无 UI 场景直接执行落盘。

| ID | 标题 | 参数 | 原生行为 | 状态 | 优先级 | 批次 | menu | scenes | batch |
|---|---|---|---|---|---|---|---|---|---|
| `settings.generateKeys` | 生成证书 | 无 | TRGenerateSelfSignedCert 生成 RSA2048 自签 CA + 写 SslCertFile/SslKeyFile（reload=restart） | ✅ | — | Phase 1 | secondary | [single,batch,ai] | true |
| `settings.searchGateway` | 搜索网关 | 无 | TRSearchGatewaySync 搜索 `_superphone-farm._tcp`，取第一个可用网关写 GatewayHost/GatewayPort（reload=gateway 自动重发 register） | ✅ | — | Phase 1 | secondary | [single,batch,ai] | true |

### 1.22 控制型能力汇总

| 模块 | ✅已注册 | 🆕待注册 | 🐛/🐛空/🔁 | 🆕待实现 | 合计 |
|---|---|---|---|---|---|
| HID 单击 | 9 | 0 | 0 | 0 | 9 |
| HID 多击/长按 | 6 | 0 | 0 | 0 | 6 |
| HID 底层 Down/Up | 17 | 0 | 0 | 0 | 17 |
| HID 通用 Consumer | 6 | 0 | 0 | 0 | 6 |
| 触控 | 14 | 0 | 0 | 0 | 14 |
| 触控（P4补声明） | 4 | 0 | 0 | 0 | 4 |
| 触控笔 | 4 | 0 | 0 | 0 | 4 |
| 键盘 | 3 | 0 | 0 | 0 | 3 |
| 文本输入 | 2 | 0 | 0 | 0 | 2 |
| 剪贴板 | 2 | 0 | 0 | 0 | 2 |
| 截屏 | 2 | 0 | 0 | 0 | 2 |
| 通知 | 4 | 0 | 0 | 0 | 4 |
| 本地命令 | 6 | 0 | 0 | 0 | 6 |
| 服务控制 | 2 | 0 | 0 | 0 | 2 |
| 服务状态 | 5 | 0 | 0 | 0 | 5 |
| 系统查询 | 6 | 0 | 0 | 0 | 6 |
| ScreenCapturer 扩展 | 5 | 0 | 0 | 0 | 5 |
| 网关客户端 | 3 | 0 | 0 | 0 | 3 |
| 屏幕感知（Phase 11） | 3 | 0 | 0 | 0 | 3 |
| 应用与启动（Phase 11） | 0 | 0 | 0 | 4 | 4 |
| 自动化编排（Phase 11） | 0 | 0 | 0 | 1 | 1 |
| 设置动作类（Phase 1） | 2 | 0 | 0 | 0 | 2 |
| **合计** | **97** | **0** | **0** | **9** | **106** |

> **2026-08-11**：`screen.subscribe` 移除（screen_changed 增量链路删除，回归 H5 hash 门控唯一拉图）。文档口径 105 项控制能力；**代码直接计数 75 控制 + 45 配置**，最终总数以运行期 `query caps` 为准。
> **2026-08-12**：新增 settings.generateKeys/searchGateway 2 项（设置动作类，07 §7），文档口径 107 项控制能力（98 已注册 + 9 待实现）；代码直接计数随注册表为准。
> **2026-08-12 Phase 2（端口收敛）**：`clients.subscribe` 删除（随 46752 控制端口整体移除，无消费方），本地命令 7→6，文档口径 106 项控制能力（97 已注册 + 9 待实现）。

---

## 2. 配置型能力清单

> reload 策略：`instant`=写入即生效 / `hot`=热重载 / `gateway`=网关刷新 / `restart`=必须重启

### 2.1 状态（E.1）

| key | 标题 | 类型 | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|
| `Enabled` | 服务开关 | bool | true | restart | NSUserDefaults | ✅ | — | — |

### 2.2 网关（E.2）

| key | 标题 | 类型 | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|
| `GatewayHost` | 网关地址 | string | "" | reconnect | NSUserDefaults | ✅ | — | — |
| `GatewayPort` | 网关注册端口 | number | 18081 | reconnect | NSUserDefaults | ✅ | — | — |
| `GatewayToken` | 网关令牌 | password | "" | reconnect | NSUserDefaults | ✅ | — | — |

### 2.3 网络（E.3）

| key | 标题 | 类型 | min | max | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|---|
| `Port` | VNC TCP端口 | number | 1 | 65535 | 5901 | restart | `gPort` C全局变量 | ✅ | — | — |
| `BindHost` | 绑定地址 | string | — | — | "" | restart | `gBindHost` | ✅ | — | — |
| `HttpPort` | HTTP端口 | number | 0 | 65535 | 0 | restart | `gHttpPort` | ✅ | — | — |
| `HttpDir` | HTTP根目录 | string | — | — | "" | restart | `gHttpDir` | ✅ | — | — |
| `KeepAliveSec` | TCP保活间隔 | number | 0 | 300 | 0 | hot | `gKeepAliveSec` | ✅ | — | — |
| `BonjourEnabled` | 自动发现 | bool | — | — | true | gateway | `gBonjourEnabled` | ✅ | — | — |

### 2.4 安全（E.4）

| key | 标题 | 类型 | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|
| `FullPassword` | 完全访问密码 | password | "" | restart | `gAuthPasswdVec` | ✅ | — | — |
| `ViewOnlyPassword` | 只读密码 | password | "" | restart | `gAuthPasswdVec` | ✅ | — | — |
| `ViewOnly` | 全局只读 | bool | false | instant | `gViewOnly` | ✅ | — | — |
| `ClipboardEnabled` | 剪贴板同步 | bool | true | instant | `gClipboardEnabled` | ✅ | — | — |

### 2.5 画面与性能（E.5）

| key | 标题 | 类型 | min | max | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|---|
| `Scale` | 输出缩放 | number | 0.1 | 1.0 | 1.0 | hot | `gScale` | ✅ | — | — |
| `FrameRateSpec` | 帧率 | string | — | — | "60" | hot | `gFpsMin`/`gFpsPref`/`gFpsMax` | ✅ | — | — |
| `OrientationSync` | 方向同步 | bool | — | — | true | hot | `gOrientationSyncEnabled` | ✅ | — | — |
| `ServerCursor` | 服务端光标 | bool | — | — | false | hot | `gCursorEnabled` | ✅ | — | — |

### 2.6 性能模式（E.6）

| key | 标题 | 类型 | enum | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|
| `PerformanceMode` | 性能模式 | enum | balanced/quality/performance/custom | balanced | hot | 组合映射 | ✅ | — | — |

**custom 展开项**：

| key | 标题 | 类型 | min | max | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|---|
| `DeferWindowSec` | 延迟窗口 | number | 0 | 0.5 | 0.015 | hot | `gDeferWindowSec` | ✅ | — | — |
| `MaxInflight` | 最大并行帧 | number | 0 | 8 | 2 | hot | `gMaxInflightUpdates` | ✅ | — | — |
| `TileSize` | 分块大小 | number | 8 | 128 | 32 | restart | `gTileSize` | ✅ | — | — |
| `FullscreenThresholdPercent` | 脏区阈值 | number | 0 | 100 | 0 | hot | `gFullscreenThresholdPercent` | ✅ | — | — |
| `MaxRects` | 最大矩形数 | number | 1 | 4096 | 256 | restart | `gMaxRects` | ✅ | — | — |
| `AsyncSwap` | 非阻塞交换 | bool | — | — | false | restart | `gAsyncSwap` | ✅ | — | — |

### 2.7 输入（E.7）

| key | 标题 | 类型 | min | max | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|---|
| `NaturalScroll` | 自然滚动 | bool | — | — | true | instant | `gNaturalScroll` | ✅ | — | — |
| `ModifierMap` | 修饰键映射 | enum | std/altcmd | — | "std" | hot | `gModifierMap` | ✅ | — | — |
| `AutoAssistEnabled` | 辅助触控自动激活 | bool | — | — | false | instant | `gAutoAssistEnabled` | ✅ | — | — |
| `WheelStepPx` | 滚轮步进 | number | 0 | 1000 | 48 | hot | `gWheelStepPx` | ✅ | — | — |
| `WheelTuning` | 滚轮调优 | string | — | — | "" | hot | `gWheelTuning` | ✅ | — | — |

### 2.8 通知（E.8）

| key | 标题 | 类型 | enum | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|
| `Notifications` | 通知模式 | enum | all/connectOnly/silent | all | instant | 映射自旧key | ✅ | — | — |

### 2.9 高级（E.9）

| key | 标题 | 类型 | min | max | 默认 | reload | 原生数据源 | 状态 | 优先级 | 批次 |
|---|---|---|---|---|---|---|---|---|---|---|
| `KeyLogging` | 键盘事件日志 | bool | — | — | false | instant | `tvncLoggingEnabled` | ✅ | — | — |
| `SslCertFile` | SSL证书文件 | string | — | — | "" | restart | `gSslCertFile` | ✅ | — | — |
| `SslKeyFile` | SSL私钥文件 | string | — | — | "" | restart | `gSslKeyFile` | ✅ | — | — |
| `WatchdogThrottleInterval` | 重启节流间隔 | number | 1 | 300 | 10 | hot | `gWatchDog.throttleInterval` | ✅ | — | — |
| `WatchdogKeepAlive` | 崩溃自动重启 | bool | — | — | true | hot | `gWatchDog.keepAlive` | ✅ | — | — |
| `WatchdogExitTimeout` | 退出超时 | number | 1 | 60 | 5 | hot | `gWatchDog.exitTimeOut` | ✅ | — | — |
| `HIDKeepAliveInterval` | HID防休眠间隔 | number | 0 | 300 | 30 | hot | `STHIDEventGenerator.keepAliveInterval` | ✅ | — | — |

### 2.10 配置型能力汇总

| 分组 | ✅已注册 | 🆕待注册 | 合计 |
|---|---|---|---|
| 状态 | 1 | 0 | 1 |
| 网关 | 3 | 0 | 3 |
| 网络 | 6 | 0 | 6 |
| 安全 | 4 | 0 | 4 |
| 画面与性能 | 4 | 0 | 4 |
| 性能模式 | 7 | 0 | 7 |
| 输入 | 5 | 0 | 5 |
| 通知 | 1 | 0 | 1 |
| 高级 | 7 | 0 | 7 |
| **合计** | **38** | **0** | **38** |

---

## 3. 变更记录

> 每次能力增删改必须在此记录。

| 日期 | 变更类型 | 能力ID | 说明 | 操作人 |
|---|---|---|---|---|
| 2026-08-08 | 创建 | — | 初始化能力追踪清单 v1.0，基于 IPA改造计划 v1.6 核查结果 | — |
| 2026-08-08 | 移除 | shake | Batch 0：shakeIt 方法体为空且无现成加速度计注入基础设施，群控场景需求低（摇晃仅 shake-to-undo/微信摇一摇等低频应用特定场景），私有 API 实现风险高，移除能力（.h/.mm 已删除方法） | — |
| 2026-08-08 | 修复 | screenshot | Batch 0：原 executor 返回 code:99 空壳。ScreenCapturer 新增 captureSingleFrameImage 公共方法（renderDisplayToScreenSurface→CVPixelBuffer→CIImage→UIImage），executor 调用返回 JPEG base64 + width/height | — |
| 2026-08-08 | 合并 | clipboard.get | Batch 0：原 clipboard.paste 是读+写回空操作。合并为 clipboard.get，executor 读 currentString 返回 {text}（空串不报错），移除 clipboard.paste | — |
| 2026-08-08 | 注册 | service.restart | Batch 0：原 restart 仅网关 cmd 通道。统一注册为 service.restart 能力（route native），executor 调 [TRGatewayClient.sharedClient.restartHandler]，复用 trollvncmanager 注入的 [gWatchDog restart]，invoke 与 cmd 双入口 | — |
| 2026-08-08 | 注册 | HID 33 项 | Batch 1：24 项无参数 HID（spotlight/home.double/power.*/snapshot/down.up类/hwlock/releasekeys）+ screenshot.system + consumer(3) + hid.page(3) + key.down/up(2)，数组驱动批量注册 | — |
| 2026-08-08 | 注册 | 触控 12 项 | Batch 1：doubleTap/twoFingerTap/threeFingerTap/longPress/curveSwipe/pinch/down/up/downMulti/upMulti/taps/eventStream，归一化坐标转原生像素，复用 _denormalizePoint | — |
| 2026-08-08 | 注册 | 触控笔 4 项 | Batch 1：stylus.tap/down/move/up，新增 _registerStylusCapabilities 方法，方位角/高度角/压力参数 | — |
| 2026-08-08 | 注册 | 通知 4 项 | Batch 1：notify.banner/banner.update/revoke/revokeAll，新增 _registerBulletinCapabilities 方法，调用 BulletinManager | — |
| 2026-08-08 | 注册 | 服务状态 6 项 | Batch 1：service.signal/state/info/isActive/isThrottled/validate，新增 _registerWatchdogCapabilities 方法，通过 TRGatewayClient.watchdog 访问 gWatchDog | — |
| 2026-08-08 | 注入 | gWatchDog | Batch 1：TRGatewayClient.h 新增 watchdog 属性（@class TRWatchDog 前向声明），trollvncmanager.mm 注入 gWatchDog 实例，解决 service.* 能力跨模块访问 | — |
| 2026-08-08 | 注册 | 附录E 配置 4 项 | Batch 1：WatchdogThrottleInterval/WatchdogKeepAlive/WatchdogExitTimeout/HIDKeepAliveInterval（hot 级别），setConfig 中即时应用到 gWatchDog/hid 活属性 | — |
| 2026-08-08 | 桥接 | localcmd 执行器 | Batch 2：新增 _executeLocalCmd:error: 辅助方法（同步 socket 连 127.0.0.1:46752，3秒超时，\n 结束），TRCapRouteLocalCmd 路由首次启用 | — |
| 2026-08-08 | 注册 | clients.* 6 项 | Batch 2：clients.count/list/disconnect/block/unblock/subscribe，新增 _registerLocalCmdCapabilities 方法，list 响应 TSV 解析为 JSON 数组，ERR/NOT_FOUND 显式报错 | — |
| 2026-08-08 | 新增命令 | blocked.list | Batch 3：trollvncserver.mm 控制端口新增 blocked.list 命令，遍历 gBlockedHosts 返回每行一个 host | — |
| 2026-08-08 | 实现 | type.paste | Batch 3：复用 clipboard.set 写剪贴板 + 模拟 Cmd+V 键序列（COMMAND→LeftGUI，v→KeyboardV），支持中文/emoji | — |
| 2026-08-08 | 注册 | clients.blocked.list | Batch 3：调 _executeLocalCmd "blocked.list" 解析为 {hosts:[]} 数组 | — |
| 2026-08-08 | 补声明 | 4 项触控方法 | Batch 4：STHIDEventGenerator.h 补 touchDownAtPoints:/liftUpAtPoints:/dispatchHandResetEvent/dispatchEventWithInfo: 公开声明 | — |
| 2026-08-08 | 注册 | 触控异点 4 项 | Batch 4：touch.downMultiAt/upMultiAt（points 数组→malloc CGPoint C 数组）/reset/event，HIDMaxTouchCount 上限校验，malloc/free 配对 | — |
| 2026-08-08 | 新增函数 | tvGetInflightStats/tvGetBonjourTXT | Batch 5：trollvncserver.mm 新增 2 个非 static 公开函数，供 sys.stats.inflight/sys.bonjour.txt 能力调用 | — |
| 2026-08-08 | 新增属性 | TRGatewayClient 3 属性 | Batch 5：TRGatewayClient.h 新增 isConnected/retryDelay/deviceInfo 公开只读属性，.mm 实现聚合 | — |
| 2026-08-08 | 注册 | 系统查询 6 项 | Batch 5：sys.version（NSBundle）/configSnapshot（currentConfigs）/resolution（UIScreen）/rotation（UIDevice）/stats.inflight（tvGetInflightStats）/bonjour.txt（tvGetBonjourTXT） | — |
| 2026-08-08 | 注册 | ScreenCapturer 扩展 5 项 | Batch 5：screen.capture.start/stop/fps/resolution/forceRefresh | — |
| 2026-08-08 | 注册 | 网关客户端 3 项 | Batch 5：gateway.isConnected/reconnect/deviceInfo | — |
| 2026-08-09 | 规划 | Phase 11 整体 | 基于 hermes-android 借鉴 + 三场景反推（单控/批量/AI 调用）+ 巨魔环境 UI 树读取不可行性结论，规划 Phase 11 能力扩展与场景化分层。新增 9 项能力（screen.hash/diff/waitStable/subscribe + app.list/current/open/icon + macro.run）、2 个新分组（应用与启动/自动化编排）、3 个元数据字段（menu/scenes/batch）。控制型 97→106，category 7→9。详见 IPA改造计划 Phase 11 | — |
| 2026-08-09 | 待实现 | macro.run | Batch 6a：借鉴 hermes android_macro，命令通道 v2 批量 invoke，steps 数组顺序执行 + stopOnFail 策略。menu=secondary scenes=[batch,ai] batch=true | — |
| 2026-08-09 | 待实现 | app.list/current/open | Batch 6b：借鉴 hermes get_apps/current_app/open_app，LSApplicationWorkspace + SBSCopyFrontmostApplicationDisplayIdentifier。新增 TRAppManager 模块。menu=primary scenes=[single,batch,ai] | — |
| 2026-08-09 | 待实现 | app.icon | Batch 6c：LSApplicationRecord.itemIcons → PNG base64。menu=internal scenes=[ai] | — |
| 2026-08-09 | 待实现 | screen.hash/diff | Batch 6b：Accelerate framework（vImage+vDSP）pHash 计算管线。32×32 DCT 取 8×8 低频（排除 DC）。新增 TRScreenHasher 模块。menu=internal scenes=[ai]。详见 IPA改造计划 11.4 | — |
| 2026-08-09 | 待实现 | screen.waitStable/subscribe | Batch 6c：基于 pHash 的滑动窗口稳定检测 + 变化推送。waitStable 比较相邻帧（非基线）；subscribe 复用 clients.subscribe 推送通道，minDistance=8 避噪声。menu=internal scenes=[ai] | — |
| 2026-08-09 | **已实现** | screen.hash/diff/waitStable/subscribe | **Phase 11.4 pHash 全部实现**。新增 TRScreenHasher.h/.mm（Accelerate vImage+vDSP，32×32 DCT 取 8×8 低频排除 DC）；ScreenCapturer 新增 captureSingleFrameBuffer（跳过 JPEG 省 8ms）；trollvncserver.mm 扩展 46752 命令（screen.hash/diff/waitStable/subscribe）+ tvCtlBroadcastToSubscribers 广播函数；TRCapabilityRegistry 注册 4 项能力（route=LocalCmd）+ 扩展元数据 schema（menu/scenes/batch 三字段）+ _inferCategoryForCapId 支持 screen.* 前缀 + _categoryTitle 新增 screen 标题；Makefile 加入 TRScreenHasher.mm。4 项全部标记 menu=internal scenes=[ai] batch=false | Batch 6b+6c |
| 2026-08-09 | 修复 | screen.waitStable 超时 | 修复 waitStable 默认 maxMs=3000 与 _executeLocalCmd 默认 3 秒 socket 超时冲突：新增 _executeLocalCmd:timeoutMs:error: 方法支持自定义超时（毫秒级精度 tv_sec+tv_usec）；原 _executeLocalCmd:error: 委托新方法默认 3000ms；waitStable executor 调用时传入 timeoutMs=MAX(maxMs+2000, 5000)，含取帧/计算/网络耗时缓冲，最少 5 秒 | — |
| 2026-08-09 | 重构 | _executeLocalCmd 接口统一 | 消除兼容方法，所有 46752 命令桥接统一走 _executeLocalCmd:timeoutMs:error: 单一接口。删除 _executeLocalCmd:error: 兼容方法；新增 kLocalCmdDefaultTimeoutMs=3000 常量（文件顶部，集中管理）；12 处调用点全部改为显式传超时值——11 处短命令（count/list/disconnect/block/unblock/subscribe/blocked.list/screen.hash/screen.diff/screen.subscribe on/off）用 kLocalCmdDefaultTimeoutMs 常量，1 处长耗时命令（screen.waitStable）用动态值 MAX(maxMs+2000, 5000)。接口无兼容层，调用方显式声明超时语义 | — |
| 2026-08-11 | 移除 | screen.subscribe | **screen_changed 增量链路全链路删除**（回归 H5 hash 门控唯一拉图）：TRCapabilityRegistry 注册移除 + `_inferCategoryForCapId` 前缀清理；trollvncserver 46752 命令分支移除 + `tvCtlBroadcastToSubscribers` 删除；TRScreenHasher 订阅实现删除（h 常量/typedef/方法声明 + mm 6 ivar/init/`subscribeWithEnable`/`_onFrameForSubscribe`）；tvCtl 广播基础设施（gTvCtlSubscribers/tvCtlAddSubscriber/tvCtlBroadcastChanged）保留（服务 clients.subscribe）。控制型合计 97→96 | 2026-08-11 |
| 2026-08-11 | 清理 | iOS 死类 + 网关端点 | iOS 删 TVNCSettingsViewController/TVNCSettingFormController/DpkgVersion（TaskProcess app 版仅移除 pbxproj 引用，文件保留因 src symlink 依赖）；网关删 `POST /api/devices/:id/command`（register-test 适配 invoke/configs） | 2026-08-11 |
| 2026-08-12 | 注册 | settings.generateKeys / settings.searchGateway | 能力与菜单统一 Phase 1（07 §7）：Root.plist PSButtonCell 动作暴露为无参 invoke 能力（route native）。generateKeys=TRGenerateSelfSignedCert 生成自签 CA + 写 SslCertFile/SslKeyFile；searchGateway=TRSearchGatewaySync 搜索网关自动落盘 GatewayHost/GatewayPort。menu=secondary | — |
| 2026-08-12 | 分层 | 全部控制能力 menu 标记 | 能力与菜单统一 Phase 1（07 §3.3/§4 落地）：primary=按键 12 对象+动作区 5 项；secondary=管理查询 ~28 项；internal=AI 原语 ~40 项（成对按下/抬起 14 项、key.down/up、consumer.*/hid.*、touch.*、stylus.*、screen.hash/diff/waitStable、screen.capture.*）。触控从菜单移除改画布直操。前端 menuCaps（console/tile/batch 三场景过滤）+ keyDefs + PressRecognizer（300ms 多击窗口/800ms 长按）+ 5801 按钮抽 keyDefs 渲染去重 | — |
| 2026-08-12 | **迁移** | clients.* / screen.* | **Phase 2 端口收敛（46752→5901 RFB 扩展）**：trollvncserver 注册 `rfbProtocolExtension`（handleMessage 接管 type 0x50），46752 文本命令（count/list/disconnect/kick/block/unblock/blocked.list + screen.hash/diff/waitStable）迁移为 5901 扩展消息（client→server type 0x50，server→client type 0x80，JSON 负载），新增 cap.hello/cap.list。TRCapabilityRegistry `_executeLocalCmd`（socket→46752）替换为 `_rfbCommand`（持久 RFB 5901 连接 + cap.hello 管理标记豁免计数/帧推送）。**46752 监听基建（gTvCtl*/subscribe 推送）全部删除**。控制型合计 98→97 已注册 | — |

---

## 4. 开发检查清单（每次开发前确认）

> 启动任何能力开发前，逐项确认：

- [ ] 已阅读本文档当前能力状态
- [ ] 确认目标能力属于哪个批次（Batch 0-5）
- [ ] 确认前置批次已完成（如 Batch 2 依赖 Batch 1 的 localcmd 桥接器）
- [ ] 确认原生方法是否已在 .h 暴露（P4 类需先补声明）
- [ ] 确认是否重复造轮子（检查原生模块是否已有实现）
- [ ] 开发完成后更新本文档状态标记（🆕→✅）
- [ ] 在变更记录中加一行

---

## 5. 模块文件索引

| 模块 | 头文件 | 实现文件 | 能力数 |
|---|---|---|---|
| HID注入器 | `TrollVNC/src/STHIDEventGenerator.h` | `TrollVNC/src/STHIDEventGenerator.mm` | 39+6触控+4触控笔=49 |
| RFB内核 | — | `TrollVNC/src/trollvncserver.mm` | 7本地命令+6系统查询=13 |
| 剪贴板 | `TrollVNC/src/ClipboardManager.h` | `TrollVNC/src/ClipboardManager.mm` | 2 |
| 屏幕采集 | `TrollVNC/src/ScreenCapturer.h` | `TrollVNC/src/ScreenCapturer.mm` | 2截屏+5扩展=7 |
| 进程守护 | `TrollVNC/src/TRWatchDog.h` | `TrollVNC/src/TRWatchDog.mm` | 2控制+5状态=7 |
| 通知管理 | `TrollVNC/src/BulletinManager.h` | `TrollVNC/src/BulletinManager.mm` | 4 |
| 网关客户端 | `TrollVNC/src/TRGatewayClient.h` | `TrollVNC/src/TRGatewayClient.mm` | 3 |
| 能力注册表 | `TrollVNC/src/TRCapabilityRegistry.h` | `TrollVNC/src/TRCapabilityRegistry.mm` | — |
| 端口常量 | `TrollVNC/app/TrollVNC/TrollVNC/Control.h` | — | — |
| 日志 | `TrollVNC/src/Logging.h` | — | 1配置项 |
| 内存守护 | — | `TrollVNC/src/OhMyJetsam.mm` | 0（启动期一次性，无可暴露能力） |

> **OhMyJetsam**：纯启动期 constructor，无运行时接口，无可暴露能力，不纳入能力清单。
