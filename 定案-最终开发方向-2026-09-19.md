# 定案：最终开发方向（2026-09-19）

> **本文档是本轮全部讨论的收口。** 它取代在此之前的若干次中间结论（凡与本文冲突的，以本文为准）。
> **写法**：定案条款用编号（便于引用）+ 自然语言场景走查 + 明确"不做什么"。
>
> **一句话**：
> **手机是"手 + 一张嘴"，网关是"大脑 + 总机"，客户端是"下命令的人"。**
> **技能库只在网关（一份），手机只做单向缓存。**

---

## 一、★ 定位定案（三层）

### A1. 手机 = **MCP 服务端**（每台一个）

```
· /mcp 端点：initialize / tools/list / tools/call
· 能力来源：现有 64 个能力 + script.exec（★ 都已存在）
· ★ 协议在手机自己身上说话 —— 这正是【让网关不必翻译协议】的原因
· 附带两样：
  ① 薄 AI 请求客户端（只为 generate 步用，见 B1）
  ② 技能卡缓存（离线自治用；★ 由网关【单向下发】）
```

### A2. 网关 = **中继器 + 大脑**（不做协议翻译）

```
【对手机】它是 MCP 客户端 + 中继：
   · 通过隧道把指令送到各台手机的 /mcp
   · ★ 字节原样转发，不翻译协议

【对客户端】它是 MCP 服务端的【聚合门面】：
   · ① 路由：这条命令发给哪台手机
   · ② 聚合：把 N 台的 tools/list 合成一张表
   · ③ 转发：请求/响应原样过
   ★ 只多这两件事，不做翻译

【它是"大脑"的部分】
   · agent 循环（决策 / 规划 / 纠错）
   · ★ 技能库：唯一真相源（天然全群共享）
   · 学习与沉淀（它能看到所有设备的 trace）
   · AI 凭证集中管理
```

### A3. 客户端 = **MCP 客户端**，连网关【一个地址】

```
· 你（DSH）/ hermes / 任何 MCP 客户端
· 看到的是【聚合后的工具面】，不是 22 个独立服务
★ 为什么必须这样：MCP 没有"聚合多个 server"的语义，而手机在 NAT 后客户端连不上
```

---

## 二、★★ AI 归属定案（关键分工）

### B1. **薄 AI 请求客户端 → 放【手机】**

```
它只做一件事："把这个 prompt 发给 LLM，拿回一段文本"
★★★ 为什么必须在手机：script.exec 是【原子批处理】——
    如果它不在手机上，一条含 generate 的卡就得拆成两段脚本，
    "整条链一次跑完"的收益就没了。

在手机：script.exec 里 [open, 找未读, 点进, read_context,
                        ★ generate（本地调 AI）, 输入, 发送, expect]
        → ★ 仍然只是【一次 MCP 调用】✓
不在手机：[...read_context] 返回 → 网关调 LLM → [输入,发送] 再下发
        → ✗ 两条脚本、两次往返
```

### B2. **agent 循环 + 技能库 + 学习 → 放【网关】**

```
★ 为什么不能在手机：agent 循环要查技能库 → 技能库就得下沉到手机
  → 22 份副本 → 回到"蜂群同步/冲突/灰度"那套复杂度 ✗
★ 放网关则：技能库天然一份，全群共享；改逻辑不用重编设备端（不用 CI + 装 .tipa）
★ 而且只有它能统一管理 AI 凭证、统一计量调用成本、看到所有设备的 trace
```

---

## 三、★★ 技能定案

### C1. **技能卡分【两类】**

```
【A 类：纯确定性卡】手机侧零 AI
   open_app / search_in_app / back_to_home / tap_text / swipe_find / list_scroll

【B 类：带 generate 步的生成型卡】恰好一处要 AI
   reply_message / post_comment（按人设生成内容）

★ 区分依据：「操作骨架」可固化，「回复内容」不可固化（每次要读上下文生成）
```

### C2. **格式：`SKILL.md`（人/模型读）+ `procedure.steps[]`（设备跑）**

```
这【是】我们唯一真正的新 substrate：
  · SKILL.md 的正文给模型读（沿用 hermes 的 HARDLINE 写作标准与章节顺序）
  · 额外一段可执行 steps（script.exec 格式）给设备跑
  → ★ 模型读前者，设备跑后者
```

### C3. **技能库在网关（唯一真相源）；手机只做【单向缓存】**

```
★ 同步方向：网关 → 手机（单向）
★★ 因此【不需要】：双向同步、冲突解决、manifest + origin hash、灰度、环境指纹合并
```

---

## 四、★★ 执行方式定案

### D1. `script.exec` 一次跑完（★ 已有；一期补了一个 op）

```
语义：批量步骤在设备端连续执行 · 失败即中止 · 返回逐步 trace
op：★ tap{x,y} / find_and_click / input_text / scroll / back / home / open / wait_for / expect
已有暴露：superphone_script_exec（DSH 插件，19 工具之一，3 分钟超时）
★ tap{x,y}（0-1 归一化）= 策略链第①档「缓存坐标」，也是点【图标类控件】的唯一办法
```

> **★ 一期实测补记（2026-09-19）**：跑「打开抖音搜南京美食」时发现——
> 抖音首页的搜索入口是**放大镜图标**，`find_and_click{text:"搜索"}` **必然失败**（实测 20 行 OCR 里没有任何"搜索"）；
> 而 op 清单里**原本没有 `tap{x,y}`**，等于连最基本的退路都没有。
> **→ 已补 `tap` op**（`TRCapabilityRegistry.mm` 的 `script.exec` 分发，纯新增）。
> 手工验证：`tap{0.928, 0.066}` 一次点中搜索图标，进去后屏幕上才有"搜索"文字（此时文字档可用）。
> **整条链 9 步：补前 8 通 1 卡；补后应全通。**

### D2. 定位用【策略链】，不是裸坐标

```
① 缓存坐标（归一化 0-1，跨屏幕尺寸可用）
② 精确文字（vision.find_text）★ 已有
③ 图标模板（vision.find_image）⚠️ 机制已有、缺素材库
   ★★ 2026-09-19 更新：★【素材来源已找到】—— 用 OmniParser 的 icon_detect
      离线解析我们自己的历史截图，取「纯图标元素」的 bbox 裁剪即成素材
      （实测：462 个元素中 200 个是纯图标元素 = 43%，正是 OCR 给不了的那一半）
      详见《实施设计》§7.5.1；L0 图标库建法见同节
④ 结构描述（→ 给 AI 判断）兜底
⑤ 翻页穷举
```

### D3. 失败先走【本地降级链】，不行才由网关问 AI

```
★ 关键事实：自愈的 90% 不需要 AI ——
  换策略档 / 等待重试 / 关弹窗 / 翻页 —— 全是确定性的
→ 只有 5–10% 的失败（UI 大改、流程变了、全新场景）才需要 AI
```

---

## 五、★ 借力定案（hermes）

### E1. **借它的设计，不借它的代码**

```
要借（直接照抄它的模式）：
  · skill 生命周期：active / stale / archived / pinned + sidecar 计数（不写进卡里）
  · sync 的黄金规则：「本地改过 → 跳过，不覆盖」（★ 若将来真需要双向时）
  · skill_linter 的两级校验（硬 blocker + 软 advisory）
  · skill_ledger 的 JSONL 审计 + 单次回滚
  · ★ provenance 的归属概念：「用户要求的永不被自动整理」
  · skills over MCP 的表面（SEP-2640：skill:// resource + skills/list/get）
  · ★ 三条铁律：遥测绝不阻断操作 · 绝不自动硬删（只归档）· 一文件一 blob
```

### E2. **不 fork hermes；让它以【MCP 客户端】身份接进来**

```
· 我们做 MCP 服务端（A1/A2）
· 它加一个 optional-mcps/superphone/manifest.yaml → ★ 零改动接上
· ★★ 好处：它的 skill 生态 + background_review 自省 fork
         会【替我们做技能沉淀】—— 这正是我们最缺的一块
★ hermes-plus 之所以 fork，是因为它要做【产品】；我们自己用，不需要 ✓
```

---

## 六、★★★ 明确【不做】的（这一节同等重要）

| # | 不做 | 理由 |
|---|---|---|
| **F1** | **不做蜂群同步** | 技能库在网关就一份，天然全群共享 |
| **F2** | **不做步骤级置信度 + 四维统计 + 层级回退** | 样本还不够证明需要；先用简单计数（借 hermes 的 sidecar）|
| **F3** | **不做三护栏 / 灰度 / 环境指纹合并** | 它们是"分发"的产物；单向下发不需要 |
| **F4** | **不自造技能格式** | 用 `SKILL.md` + 一层可执行 steps（能互通）|
| **F5** | **不自造 MCP skill 表面** | 照 SEP-2640 |
| **F6** | **不继续投 Mobile-Agent** | 它的使命已兑现（证明 9 方法接口可行 / 字典注入有效 / 坐标换算正确 / 测出 GUI-Owl-4B 不认新动作）|
| **F7** | **不 fork hermes / hermes-plus** | 自己用，不需要背 1.2GB 的 fork |
| **F8** | **不先做 MCP**（对内部开发而言）| DSH 已有 `superphone_script_exec`，先跑起来；MCP 等接外部客户端时做 |

---

## 七、★★★★★ 场景走查（自然语言）

### 场景 1：在 DSH 里做一个任务（**现在就能做**）

```
你说：「打开抖音，搜索南京美食，看 3 个作品」
  ↓
DSH（agent）判断 → 调 superphone_script_exec，steps =
  [ open{抖音}, find_and_click{搜索}, input_text{南京美食},
    wait_for{hashDiff}, expect{南京} ]
  ↓
手机：script.exec 一次跑完 → 返回逐步 trace
  ↓
你看结果；失败时看 trace 知道卡在第几步
★ 全程零新增代码 —— 这【就是】起点
```

### 场景 2：值守回复（**B 类卡的典型**）

```
你说：「打开微信，按 xx 人设对私聊消息值守回复」
  ↓ ★ 客户端 AI 把它变成意图命令（人话→命令，这一步永远在）
网关：查技能库 → 命中 reply_message（B 类卡）
  ↓ 下发到目标手机的 /mcp
手机：script.exec 跑（★ 一次调用走完）：
     [ open{微信}, 找未读, 点进, read_context(读 3 屏),
       ★ generate{persona:xx, context:...}  ← 手机直接调 AI 服务，拿回复文本
       找输入框, input_text, 找发送, 点发送, expect{消息已出现} ]
  ↓ 返回 trace
网关：沉淀 —— 成功的路径更新卡；失败的记成坑
★ 注意：那条卡【能一次跑完】，正因为薄 AI 客户端在手机上（B1）
```

### 场景 3：第一次做一件没有卡的事（**学习**）

```
你说：「把这个页面上的商品价格都记下来」
  ↓
网关查技能库 → 没命中 → ★ 请求 AI 规划（agent 循环在网关）
  ↓ AI 给一份 steps 草稿 → 下发给手机
手机：script.exec 执行 → 失败 → 带 trace + 失败点 OCR 证据回传
  ↓
网关：把证据 + trace 给 AI → AI 修正 steps → 再下发
  ↓ 成功
★ 沉淀成卡（网关侧）→ 以后任何人做同类事直接命中
```

### 场景 4：App 更新导致卡失效（**自愈**）

```
执行到第 2 步 → 缓存坐标点偏 → verify 失败
  ↓ ★ 先走【本地降级链】（零 AI）：
     换文字定位 → 命中 ✓ → 更新缓存坐标 → 继续
  ↓ 若文字也不中 → 换图标模板
  ↓ 全不中 → ★ 才回传网关 → 网关问 AI → 修 → 沉淀
★ 90% 的失效在第一步就自愈了，不需要 AI
```

### 场景 5：让 hermes 也能操作我们的手机（**对外**）

```
做 MCP 服务端（网关聚合门面）
  ↓
hermes 加 optional-mcps/superphone/manifest.yaml
  ↓ ★ 零改动接上
hermes 的 agent 调我们的工具 → ★ 它自己的 learning 机制会自动沉淀 skill
★ 一块我们最缺的能力，白拿
```

---

## 八、★★★★ 分期路线（每步独立可用，每步有验收）

| 期 | 做什么 | 验收标准 | 依赖 |
|---|---|---|---|
| **一** | ★ **跑一次真实任务**（用现有 `superphone_script_exec`）| 抖音搜索任务跑通，并把"实际卡在哪"记下来 | 无（现在就能做）|
| **二** | **把跑通的 steps 存起来**（网关侧一个目录 + 命名约定）| 同类任务第二次能直接取用 | 一期产出 |
| **三** | **加【缓存坐标】兜底**（定位策略链第①档）| 故意把某步改错，系统能用缓存坐标自愈 | 二期 |
| **四** | **加 `generate` 步 + 手机侧薄 AI 客户端** | 一条 B 类卡（回复消息）能一次跑完 | 三期 |
| **五** | **手机内嵌 MCP + 网关聚合门面** | 一个 MCP 客户端能看到工具面并操作手机 | 四期 |
| **六** | **借 hermes 的 skill 生命周期 + ledger**（照抄模式）| 技能有 active/stale 状态、可回滚 | 五期 |
| **✗** | ~~蜂群同步 / 置信度体系 / 三护栏 / fork hermes~~ | **不做**（见 §六）| — |

---

## 九、一句话总结

> **手机是"手 + 一张嘴"**（MCP 服务端 + 薄 AI 客户端 + 技能缓存）
> **网关是"大脑 + 总机"**（agent 循环 + 技能库 + 学习 + MCP 聚合门面，**不做协议翻译**）
> **客户端是"下命令的人"**（连网关一个地址）
>
> **★ 技能库只在网关（一份），所以蜂群同步那一整套不需要。**
> **★★ AI 分两种：薄客户端在手机（因 script.exec 原子性），agent 循环在网关（因技能库要共享）。**
> **★★★ 起点是现在就能做的：用已有的 `superphone_script_exec` 跑一次真实任务。**

---

## 十、执行引擎的正式形态（2026-09-19 二次收口 · 用户确立）

### 10.1 ★ 模型只做两件事（引擎的完整职责）

```
路径 A【没做过】→ 自己做，创造经验
   模型全权驱动：看图 → 决策一步 → 执行 → 结构验证 → 步步落盘原始 trace
   验证通过 → 沉淀资产（元素/页面/技能卡）；失败 → 沉淀【坑】
路径 B【做过】→ 从基线开始编排完整执行流水线去执行，自己是【监督者和纠错者】
   ① 意图分析（模型）→ ② 拿基线（机械采集，模型读）→ ③ 查库裁决（模型）→
   ④ 命中则编排下发（模型做这一次决策）→ ⑤ 每步验证结果【回流模型】→ 继续/纠错/中止

★ 监督/纠错不是姿态，需要【证据回流】——
  screenBand 断言 / wantBand 的 bandBefore·bandAfter 即监督数据（已装机验收 ✓）
```

### 10.2 ★★ 「零模型」的合法范围（精确化，替换一切含糊说法）

```
✓ 合法范围：命中资产后的【那一段序列的内部步进】不需要每步请模型
   —— 模型在任务级做一次编排决策，序列由确定性机制连续执行（这是 100s→2~5s 的真正来源）
✗ 非法引申："流水线不依赖模型"
   意图分析、查库裁决、复用还是探索、纠错、资产沉淀裁决 —— 全部是模型的工作
深刻来历：§一.5 用户已纠正过一次（"第一次也是经过 AI 给手机下达的任务"）；
   2026-09-19 再次修正为"模型=执行引擎"的完整表述 ✓
```

### 10.3 ★★ 飞轮：每次执行恰好产生三种沉淀之一（零浪费）

```
① 新任务探索成功 → 新资产       ② 复用命中 → 该序列成功计数 +1（简单计数，F2 合规）
③ 失败 → 新【坑】（负样本更值钱）→ 下次同页执行前，模型先查坑 → 绕开已知故障
"越用越聪明"的度量（四项简单计数）：
   意图命中率 ↑（任务从探索转为复用）· 探索步数 ↓（同类任务）
   步骤失败率 ↓ · ★ 坑拦截接收 ↑（负样本在拦截真实事故）
```

### 10.4 ★★★ 命中用什么查：三层确定性索引（不建向量库）

| 查的事 | 原语 | 现状 |
|---|---|---|
| **我在哪一页** | 文字签名（page.py ✓）+ **结构签名 screenBand（✓ 已装机）** | 全可验证 |
| **图标/组件在哪** | 模板匹配 `vision.find_image` + L0 图标库 | 素材已备（859 裁片待质检）|
| **怎么执行（行为）** | **技能卡（步骤序列）—— ★ 行为不检索** | "按钮→行为"的绑定住在卡里 |

```
★ 正确性纪律：召回可以概率性，【最终裁决必须确定性】——
   向量召回是概率性的、不透明的；"猜错在哪一页"类错误的代价已被多次实测 ✗
★ 现在不做向量库的理由：痛点是【覆盖不足】不是【检索慢】（资产库里每页 1~3 帧，
   模板库 O(几百) → 直接匹配亚毫秒 ✗ 向量无收益）；且一旦命中必须复核
向量唯一值得上的位置：【任务级语义召回】（"我要做 X，有没有接近的既有做法"）
   → 届时直接复用 OpenViking + 8081 嵌入（自家基建常驻 ✓ 零新建），
     且召回结果仍须走确定性验证。
```

### 10.5 业内参照（2026-09-19 检索；均与我们独立撞出的结论同构）

```
· GUIClaw（桌面 GUI 技能）：模板匹配(matchTemplate)→检测(YOLO+OCR)→LLM 识别→存 memory→点
  —— 与 D2 策略链 + "验证成功才沉淀"【同构】
· UI-Voyager（2026-03，靠【失败经验】自进化）：页面匹配 = 裁状态栏/降分辨率/多帧时窗/遮罩高方差区/结构+OCR 混合
  —— 与我们实测撞出的"状态栏剔除/跨内容采样/结构+文字双签名"同一套 ✗ 可直接吸收其多帧时窗
· SE-GA：情景/语义/经验三层记忆 —— 与资产四层同构（L0/L1≈语义 · trace≈情景 · 卡≈经验）
· Action-Effect Verification（"不要盲动作"）：执行后效果验证 + 自纠错 —— 即 §9.2 的实测结论
```
