# 模拟对抗能力台账（2026-08-29 建立）

> 纪律：后续所有模拟对抗开发，必须先在本台账登记（能力→验证状态→风险），
> 再投入开发。未验证的能力不得纳入防线（"接口存在 ≠ 能力真实"）。
> 声明来源：udevs 头文件（Geranium/TrollBox/Andromeda 同款逆向公开资料）；
> 真机验证 = 唯一能力判据。

---

## 一、CLSimulationManager 接口全集（能力归属）

### 已验证（可纳入防线）
| 能力 | 调用点 | 验证证据 |
|---|---|---|
| 坐标注入（appendSimulatedLocation→flush→start） | SimLocationManager injectPoint: | anchor/itinerary 生产运行；抖音收到模拟坐标 |
| 投递参数（deliveryBehavior/interval/repeat） | injectPoint: 内设置 | 1s 投递 + 重复投递行为实测 |
| WiFi 扫描模拟（setWifiScanResults/setSimulatedWifiPower/start/stop） | injectWifiScanResults:/stopWifiScanSimulation | 日志 "wifi simulation start, N APs" + window inject |

### 未验证（不得纳入防线，开发前必须实测）
| 能力 | 状态 | 待验证内容 |
|---|---|---|
| 基站模拟（setSimulatedCell/startCellSimulation） | 从未调用 | locationd 是否消费；所有 App 能否读到；需要目标城市基站数据源（WPS cell 接口） |
| 场景文件（loadScenarioFromURL） | 已弃用（GPX 被拒） | 格式无公开文档——不投入 |
| 投递参数各枚举值（deliveryBehavior/repeat 其它值） | 只用 1 | 各值行为差异未测（低优先级） |

### 待复验
| 能力 | 状态 | 说明 |
|---|---|---|
| 系统定位开关（CLLocationManager.setLocationServicesEnabled:） | entitlements 补丁后未装包复验 | 补 locationd 权限簇后需真机确认"关模拟→系统定位关闭"生效 |

---

## 二、定位对抗防线全景（按信号通道归类）

| 信号通道 | 防线 | 能力归属 | 验证状态 |
|---|---|---|---|
| CoreLocation 坐标 | CLSimulationManager 坐标注入 + 编排 | ✅ 已验证 | anchor/itinerary 生产 |
| 投递节奏 | locationInterval=1s + repeat | ✅ 已验证 | — |
| WiFi 扫描输入 | setWifiScanResults 注入 | ✅ 已验证 | 但**对抖音读当前连接 BSSID 无效**（待整改——软路由方向） |
| 基站 | setSimulatedCell | ❌ 未验证 | 待实测 + WPS cell 数据源 |
| 系统定位开关 | setLocationServicesEnabled | 🟡 待复验 | entitlements 已补 |
| 崩溃窗口 | L4 哨兵 + JSON 恢复 + L5 启动兜底 | ✅ 已实现（L3 GPX 已弃用） | — |
| 相册 GPS | EXIF 包装 + 来源归零 | ✅ 已验证 | — |
| IP | 上层 AP | ✅（用户完成） | — |
| 通讯录/通话/短信 | TRDataFiller | ✅ 已验证 | — |
| 已装应用/设备名/传感器 | 无（注入被用户排除） | ❌ 未覆盖 | 农场特征清单 P0/P1 |

---

## 三、后续开发归类纪律

1. 新模拟对抗能力 → 先登记本台账（能力/数据源/验证路径）
2. 未验证能力禁止纳入生产防线
3. 每项能力验证 = 真机实验（日志/行为证据），不靠接口存在性
