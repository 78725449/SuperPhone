# 00 · 当前进度基线（2026-08-09 · 全栈对照版）

> 本文档由 Codex 对照「文档体系」与「实际代码」逐文件整理，作为后续开发的统一基线。
> 目的：明确 **已实现 / 文档声称但需真机验证 / 未完成 / 风险**，避免后续重复造轮子或误判。
> 对照对象：01~06 + 能力追踪清单 + 开发宪法 + IPA改造计划；代码为**未提交工作区快照**（TrollVNC + trollvnc-farm）。
> 本文档应随开发持续更新；每完成一项即同步勾选。

---

## 1. 总体状态

- **架构已从「UI 逐批修补」切换到「能力即服务 + 隧道 + 三端统一」**（用户确认的最终方向）。
- 文档里程碑 M1–M20 几乎全部标记 ✅；**最终卡点 = 真机联调**（需实体 iOS 设备）。
- **代码与文档大体一致**，但存在 **文档口径 vs 代码实测差异**（见 §3），且**全部改动未提交、未跑通 CI/测试**。
- 当前 `TrollVNC/dist/TrollVNC_3.2-272.tipa`（2.3MB，2026-08-07）为**旧包**，非最新代码产物。

---

## 2. 已实现（代码实测存在）

### 2.1 能力即服务（Phase 4 / Batch 0-5）
- 新增 `src/TRCapabilityRegistry.h/.mm`（103KB，2026-08-09 14:58）：数据驱动注册表。
  - 直接计数：`_registerControl:` × **75**、`_registerConfig:` × **45**（含循环批量注册：hidNoParam 25 项、consumer 3、hidPage 3、tap 4、stylus 3 等数组展开）。
  - 文档称「97 控制 + 38 配置」；**代码直接计数 75+45，循环展开后需运行期 `query caps` 确认最终总数**（§3 待验证）。
  - 提供 `invoke:capId:params:error:` / `setConfig:key:value:error:` 统一入口，按 route 分派（hid/touch/stylus/localcmd/native/watchdog）。
  - **Phase 11.1 元数据扩展已落地**：`menuLevel`（primary/secondary/internal）/ `scenes` / `batchSupport` 属性存在，`_registerControl` 按 category 映射表自动填充默认值，screen.* 四能力标记 internal + scenes=[ai]。
- `src/TRScreenHasher.h/.mm`（Phase 11.4，21KB）：pHash 管线（取帧→32×32→灰度→DCT-II→8×8 低频哈希）**已实现且已接入**：
  - `trollvncserver.mm` 46752 控制端口新增 `screen.hash` / `screen.diff` / `screen.waitStable` / `screen.subscribe` 命令处理（行 3775-3845）。
  - `TRCapabilityRegistry.mm` 已注册这 4 项能力（localcmd 路由，internal/[ai]）。
  - **即 Batch 6c 的 screen.* 部分实际已落地**（§4 修正文档）。

### 2.2 命令通道 v2 + 隧道（Phase 4.2 / Phase 7）
- `trollvnc-farm/server/index.js`（1039 行，ESM，package.json `"type":"module"` 匹配）：
  - 新增 `TUNNEL_PORT=18181`、`FT_*` 帧协议（FT_DATA 0x01 / FT_PING 0x02 / FT_PONG 0x03 / FT_CMD 0x04 / FT_CMDACK 0x05）。
  - `tunnelServer`（18181）行缓冲握手 `tunnel_hello`→`tunnel_ack`，握手后帧封装。
  - `sendDeviceCmd()` 支持 query/set/invoke/restart，**隧道优先、注册通道回退**。
  - `handleVncSocket()` 支持**隧道桥接**（`tunnels.get(deviceId)`）+ 直连回退。
  - **单通道铁律已落地**：WS 仅保留 `/ws/vnc/:id`（画面）与 `/ws/control/:id`（AI 工具），**无 `/ws/register` 端点**；register-test.js 已断言该路径被拒。
- `src/TRTunnelClient.h/.mm`（设备侧 18181 隧道客户端，20KB）存在；`trollvncserver.mm` 有隧道接入。
- `src/TRGatewayClient.mm`（21KB）：命令通道 v2（ping/query/set/invoke/restart）完整；**Phase 10.5 时序修正已落地**（收到注册 ack 后才 `_startTunnel`，带 `_tunnelStarted` 防重；`_needsReregister` 设置变更重发 register）。

### 2.3 UI 重构（Phase 12 / M20）
- `app/TrollVNC/TrollVNC/TRMainTabBarController.h/.m`：**三 Tab**（连接 / 控制 / 设置），紫色 tint，defensive 回退旧设置页；已加入 pbxproj（编译目标）。
- `TVNCConnectViewController.m`（连接页）：
  - Hero 渐变卡 **3 行**：第 1 行 = `UIDevice name`（关于本机名）+ 状态点/文字；第 2 行 = 连接状态文字；第 3 行 = 服务状态（VNC 服务运行中 · N 个客户端在线）。
  - 扫码直连卡：QR（110×110，`http://IP:HttpPort`）+ URL + 提示；二维码后台线程生成 + @try 兜底（防启动崩溃）。
  - 客户端卡：在线/冻结计数 + 「全部断开」+ 客户端列表（复用 TVNCClientListController）。
- `TVNCClientListController.m`：右上「全部断开」；客户端 cell 支持**冻结/解冻/断开**（freeze/unfreeze/block/unblock，frozenHosts 持久化）。
- `TVNCControllerViewController.m`（控制端，+1697 行）：UICollectionView 卡片墙（TVNCDeviceCardCell：画面+名称+在线点+⋯按钮+多选 checkbox）、布局切换器（横屏1-6/竖屏1-6 下拉面板）、批量操作按钮、底部批量配置按钮、卡片 ⋯ → 能力分组菜单。
- `TVNCViewerViewController.m`（+935 行）：**WKWebView + noVNC**（`ViewerWeb.html`，替代 libvncclient），悬浮 ⚙ 可拖动按钮 → 竖排能力菜单；`useGatewayTunnel` 属性（跨网走网关 8080 桥接，局域网直连 5801）。
- 新增 `TVNCDeviceListCell.h/.m`（已入 pbxproj）。

### 2.4 5801 网页黑屏问题（历史修复，代码已落地）
- `layout/usr/share/trollvnc/webclients/index.vnc`（+580 行）当前**只 `import RFB`**（行 211，无静态 caps import），caps.js 改为动态 `import('./caps.js')` 可选增强（行 558，失败不影响连接）。
- 这解决了「caps.js 静态导入 404 → 模块脚本死亡 → 不连接 → 黑屏」根因。**但真机是否出画面仍需验证**。

### 2.5 网关前端（Phase 4.7 / 10.2-10.3 / 11.5）
- `web/app.js`（+804 行）：垂直列表、**批量操作栏（selectedDevices Set + updateBatchBar + showBatchMenu 三分组）**、配置面板（按 reload 四区分区）、卡片墙完整 RFB（v2.3 弃用 invoke screenshot 轮询）。
- `web/caps.js`（+192 行）：`DEFAULT_CAPS`（9 项，**含 clipboard.paste**）/ `CAP_FALLBACK` / `RELOAD_LABELS` / `CATEGORY_LABELS` / `groupByCategory()` / `deviceCaps()` / `configSchemaByReload()` / invoke/set/batch 封装。
- `web/index.html`：布局切换器（12 项下拉）、墙缩放、批量按钮、focus 面板 + 操作列 + FAB。

### 2.6 清理类（Phase 6 / 2）
- **反向连接残留**：trollvncserver.mm / TVNCConnectViewController.m / TRCapabilityRegistry.mm 中 `ReverseMode|ReverseSocket|ReverseRepeaterID|gRepeater|isRepeaterEnabled` **零匹配**（已彻底清理）。
- **营销入口**：`havoc|OwnGoalStudio|View Source Code|Made with|付费作品|GitHubReleaseUpdater` 在 app/prefs/src **零匹配**（GitHubReleaseUpdater 文件已删除）。
- CI `build.yml`：已移除 reverse_mode/reverse_socket/reverse_repeater_id 三个 input。

---

## 3. 文档口径 vs 代码实测的差异（重要）

| 项 | 文档口径 | 代码实测 | 结论/建议 |
|---|---|---|---|
| 能力数量 | 97 控制 + 38 配置 | 直接计数 75 `_registerControl:` + 45 `_registerConfig:`（循环数组展开更多） | 需运行期 `query caps` 确认最终总数，**文档数字需校准** |
| Phase 11.1 元数据 | Batch 6a 未实现 | menu/scenes/batch 字段+默认映射**已实现**（screen.* 标 internal/[ai]） | 文档过时，实际已落地 |
| screen.* 能力 | Batch 6b/6c 未实现 | **已实现并注册**（screen.hash/diff/waitStable/subscribe，46752 + TRScreenHasher） | 文档过时，实际已落地（需真机验证） |
| macro.run / app.* | Batch 6a/6b 规划 | **未实现**（grep macro.run/app.list/TRAppManager 零匹配） | 待实现，保持规划 |
| caps-test.js | 「npm test 全绿」 | **引用已改名的 `CAP_META`（现为 `CAP_FALLBACK`），import 即失败**；且断言字段 op/label 与新结构不符 | **测试与实现断链，npm test 必挂**，需修复测试 |
| 注册通道 | 「单通道 TCP JSON」 | 已确认无 `/ws/register`；register-test 断言拒绝 | ✅ 一致 |
| farm 模块 | — | ESM（`import` + package.json type:module）| ✅ 一致，非风险 |
| 5801 黑屏 | — | index.vnc 已移除静态 caps import | 大概率已解决，需真机打开验证 |
| UI Phase 12 | M20 已完成 | TRMainTabBarController + 卡片墙 + 批量 + 配置面板存在 | 未 build，需真机验证 |
| 隧道 | Phase 7 已完成 | farm tunnelServer + TRTunnelClient 存在 | 未真机联调，需验证跨网络透传 |
| 里程碑 M15 反向清理 | ✅ | 代码零匹配 | ✅ 已可信（未编译验证） |

---

## 4. 未完成 / 未验证

- **未提交**：TrollVNC 子模块、trollvnc-farm、根文档全部在**工作区脏**，未 commit（farm 仅 1 条 auto commit）。
- **未构建**：无新 tipa 产物；`dist/TrollVNC_3.2-272.tipa` 是 2026-08-07 旧包（2.3MB）。
- **未跑测试**：本沙箱 node/npm 因 `EPERM: lstat C:\Users\Administrator` 无法运行 farm 测试（环境限制，非代码问题）；且 **caps-test.js 本身断链（§3）**，需在正常环境修复后跑 `npm test`。
- **真机联调**：M2 验收 + M14-M20 实现 + 全链路验证（5801 出画面、App 三 Tab、能力按钮渲染、隧道透传、卡片墙）均需实体 iOS 设备。
- **Batch 6a/6b 剩余**：macro.run / app.list / app.current / app.open / app.icon **未实现**（screen.* 已提前落地）。
- **前端并行**：三端菜单分层（primary/secondary/internal）+ 批量过滤（scenes/batch）+ AI 通道的前端渲染**未完成**（元数据已有，前端未消费）。

---

## 5. 风险与已知边界

1. **构建风险**：新增文件（TRCapabilityRegistry/TRTunnelClient/TRScreenHasher 走 Makefile：trollvncmanager_FILES/trollvncserver_FILES 已加；TRMainTabBarController/TVNCDeviceListCell/ViewerWeb.html 走 pbxproj 已加）——需 CI 编译确认。
2. **bootstrap scheme**：此前多次 bootstrap 因 clang modules / import 顺序失败；本次大改需重点验证 bootstrap 编译。
3. **能力真实可用性**：文档标 ✅ 不代表全部经真机验证（尤其 HID 注入、截图、剪贴板、触摸、screen.*）。
4. **隧道安全**：18181 隧道无鉴权设计（内网自用），反向模式已废弃，符合既定边界。
5. **5801 端口语义**：5801 仅发网页，画面走 5901 WebSocket；若真机仍黑，按「连接/首帧诊断」区分锁屏黑帧 vs 5901 未监听。
6. **caps-test.js 断链**：若直接跑 `npm test` 会在 caps-test 处失败，需先修复（对齐 CAP_FALLBACK 与断言字段）。
7. **Node 版本**：smoke.js 用 `import.meta.dirname`（Node 20.11+）；本机 node v22.14.0 满足，软路由部署时需注意。

---

## 6. 建议的下一步（按序）

1. **修复 caps-test.js**：将 `CAP_META` → `CAP_FALLBACK`，断言对齐新结构（id/title/icon/category/route/params），补齐缺失导出校验。
2. **跑 farm 测试**：在正常环境 `cd trollvnc-farm && npm test`（smoke/register/dedupe/caps），确认注册/去重/单通道/隧道框架全绿。
3. **构建验证**：把 TrollVNC 改动提交并 push 到 fork（li78725449-ship-it/TrollVNC），触发 CI 4 scheme（default/rootless/roothide/bootstrap），确认当前代码能编译（重点 bootstrap）。
4. **真机冒烟**：安装新 tipa → 验证 5801 打开出画面、App 三 Tab、能力按钮按清单渲染、网关控制台卡片墙、批量操作。
5. **再纵深**：实现 Batch 6a/6b（macro.run / app.*），以及三端菜单分层前端渲染。
6. **文档校准**：能力数量（97+38 → 运行期实测值）、Phase 11 状态（screen.* 已落地）同步回「能力追踪清单.md」与「06-构建交付与里程碑.md」。

---

## 7. 附：文档体系索引

| 文档 | 内容 | 状态 |
|---|---|---|
| 00-当前进度基线 | 本文档 | 持续更新 |
| 01-愿景与设计基线 | 核心原则/端口/连接方向/UI 定位 | 已提炼 |
| 02-能力即服务架构 | 能力注册/命令 v2/配置上报/热重载/REST/前端渲染 | 大量 ✅ |
| 03-隧道与连接架构 | 18181 隧道/三端接入/命令复用 | ✅ 已完成 |
| 04-UI交互规范 | 三端 UI 规范（垂直列表/卡片墙/悬浮菜单） | ✅ 已完成 |
| 05-能力扩展规划 | Phase 11 场景化分层 + app.* / screen.* | **部分已提前落地**（screen.* ✅，macro/app 待做） |
| 06-构建交付与里程碑 | M1-M20 里程碑 | 几乎全 ✅，卡点=真机 |
| 能力追踪清单 | 逐能力状态（97 控制 + 38 配置） | **数字待校准** |
| 99-历史归档 | 历史上下文归档（v1.8 反推） | — |
| IPA改造计划 | 主计划聚合（v2.4） | 已重写 |
| 开发宪法 | 能力模型/铁律/协议（v1.8） | 已更新 |
