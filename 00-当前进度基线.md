# 00 · 当前进度基线（2026-08-11 · 全栈对照版）

> 本文档由 Codex 对照「文档体系」与「实际代码」逐文件整理，作为后续开发的统一基线。
> 目的：明确 **已实现 / 文档声称但需真机验证 / 未完成 / 风险**，避免后续重复造轮子或误判。
> 对照对象：01~06 + 能力追踪清单 + 开发宪法 + IPA改造计划；代码为**未提交工作区快照**（TrollVNC + trollvnc-farm）。
> 本文档应随开发持续更新；每完成一项即同步勾选。
> 最近更新：2026-08-13 追加 §2.3l（统一浮层粘贴输入 v2 实现）+ §2.3k（四个输入/剪贴板能力研究）+ §2.3j 手势粘贴实现**已回退**（用户判定不成熟：长按/Ctrl+V 劫持与"被控设备输入框长按=IPA 剪贴板"概念冲突，回退 farmlongpress patch + 粘贴条 + Ctrl+V 拦截，保留 type.paste/clipboard.get 按钮）。此前为 §2.3i（能力精简）与 §2.3f（打包 #116）与 §2.3e（上报去除）与 §2.3d（卡片 ⋯ 菜单）。

---

## 1. 总体状态

- **架构已从「UI 逐批修补」切换到「能力即服务 + 隧道 + 三端统一」**（用户确认的最终方向）。
- 文档里程碑 M1–M20 几乎全部标记 ✅；**最终卡点 = 真机联调**（需实体 iOS 设备）。
- **代码与文档大体一致**，但存在 **文档口径 vs 代码实测差异**（见 §3），且**全部改动未提交、未跑通 CI/测试**。
- 当前 `TrollVNC/dist/TrollVNC_3.2-272.tipa`（2.3MB，2026-08-07）为**旧包**，非最新代码产物。

---

## 2. 已实现（代码实测存在）

> **2026-08-11 晚同步**：本节的「已实现」含**已删除**项——IPA 原生设备墙/大屏（§2.3/§2.7 的 TVNCControllerViewController/TVNCViewerViewController/TRWallTileWebView）已由**控制 Tab WKWebView 容器**（`TVNCConsoleWebViewController` 加载网关 H5）替代；`screen.subscribe` 与 screen_changed 增量链路已删；网关 `/command` 端点已删。**以 §8 为准**，本节历史记录见标注。

### 2.1 能力即服务（Phase 4 / Batch 0-5）
- 新增 `src/TRCapabilityRegistry.h/.mm`（103KB，2026-08-09 14:58）：数据驱动注册表。
  - 直接计数：`_registerControl:` × **75**、`_registerConfig:` × **45**（含循环批量注册：hidNoParam 25 项、consumer 3、hidPage 3、tap 4、stylus 3 等数组展开）。
  - 文档称「97 控制 + 38 配置」；**代码直接计数 75+45，循环展开后需运行期 `query caps` 确认最终总数**（§3 待验证）。
  - 提供 `invoke:capId:params:error:` / `setConfig:key:value:error:` 统一入口，按 route 分派（hid/touch/stylus/localcmd/native/watchdog）。
  - **Phase 11.1 元数据扩展已落地**：`menuLevel`（primary/secondary/internal）/ `scenes` / `batchSupport` 属性存在，`_registerControl` 按 category 映射表自动填充默认值，screen.* 三能力标记 internal + scenes=[ai]（~~screen.subscribe~~ 已删 2026-08-11）。
- `src/TRScreenHasher.h/.mm`（Phase 11.4，21KB）：pHash 管线（取帧→32×32→灰度→DCT-II→8×8 低频哈希）**已实现且已接入**：
  - `trollvncserver.mm` 46752 控制端口新增 `screen.hash` / `screen.diff` / `screen.waitStable` 命令处理（~~screen.subscribe~~ 已删）。
  - `TRCapabilityRegistry.mm` 已注册这 3 项能力（localcmd 路由，internal/[ai]；subscribe 已删）。
  - **即 Batch 6c 的 screen.* 部分实际已落地**（§4 修正文档）。

### 2.2 命令通道 v2 + 隧道（Phase 4.2 / Phase 7）
- `trollvnc-farm/server/index.js`（1039 行，ESM，package.json `"type":"module"` 匹配）：
  - 新增 `TUNNEL_PORT=18181`、`FT_*` 帧协议（FT_DATA 0x01 / FT_PING 0x02 / FT_PONG 0x03 / FT_CMD 0x04 / FT_CMDACK 0x05）。
  - `tunnelServer`（18181）行缓冲握手 `tunnel_hello`→`tunnel_ack`，握手后帧封装。
  - `sendDeviceCmd()` 支持 query/set/invoke/restart，**隧道优先、注册通道回退**。
  - `handleVncSocket()` 支持**隧道桥接**（`tunnels.get(deviceId)`）；**直连回退已废弃**（无隧道 4003 拒绝，2026-08-11）。
  - **单通道铁律已落地**：WS 仅保留 `/ws/vnc/:id`（画面）与 `/ws/control/:id`（AI 工具），**无 `/ws/register` 端点**；register-test.js 已断言该路径被拒。
- `src/TRTunnelClient.h/.mm`（设备侧 18181 隧道客户端，20KB）存在；`trollvncserver.mm` 有隧道接入。
- `src/TRGatewayClient.mm`（21KB）：命令通道 v2（ping/query/set/invoke/restart）完整；**Phase 10.5 时序修正已落地**（收到注册 ack 后才 `_startTunnel`，带 `_tunnelStarted` 防重；`_needsReregister` 设置变更重发 register）。

### 2.3 UI 重构（Phase 12 / M20）⚠️ 原生实现已删除（2026-08-11 容器化取代）
- `app/TrollVNC/TrollVNC/TRMainTabBarController.h/.m`：**三 Tab**（连接 / 控制 / 设置），紫色 tint，defensive 回退旧设置页；已加入 pbxproj（编译目标）。
- `TVNCConnectViewController.m`（连接页）：
  - Hero 渐变卡 **3 行**：第 1 行 = `UIDevice name`（关于本机名）+ 状态点/文字；第 2 行 = 连接状态文字；第 3 行 = 服务状态（VNC 服务运行中 · N 个客户端在线）。
  - 扫码直连卡：QR（110×110，`http://IP:HttpPort`）+ URL + 提示；二维码后台线程生成 + @try 兜底（防启动崩溃）。
  - 客户端卡：在线/冻结计数 + 「全部断开」+ 客户端列表（复用 TVNCClientListController）。
- `TVNCClientListController.m`：右上「全部断开」；客户端 cell 支持**冻结/解冻/断开**（freeze/unfreeze/block/unblock，frozenHosts 持久化）。
- `TVNCControllerViewController.m`（控制端，+1697 行）：UICollectionView 卡片墙（TVNCDeviceCardCell：画面+名称+在线点+⋯按钮+多选 checkbox）、布局切换器（横屏1-6/竖屏1-6 下拉面板）、批量操作按钮、底部批量配置按钮、卡片 ⋯ → 能力分组菜单。
- `TVNCViewerViewController.m`（+935 行）：**WKWebView + noVNC**（`ViewerWeb.html`，替代 libvncclient），悬浮 ⚙ 可拖动按钮 → 竖排能力菜单；`useGatewayTunnel` 属性（跨网走网关 8080 桥接，局域网直连 5801）。
- 新增 `TVNCDeviceListCell.h/.m`（已入 pbxproj）。

### 2.7 IPA 卡片墙 WKWebView 改造（2026-08-10 新增）⚠️ 已删除（2026-08-11 hash 门控 + 容器化取代）
- 新增 `app/TrollVNC/TrollVNC/TRWallTileWebView.h/.m`：卡片墙 RFB 连接管理器（WKWebView + noVNC viewOnly），隧道优先/直连回退 URL 构造、连接状态回调、帧率节流（`setFrameRate:`）、start/stop 生命周期、共享 WKProcessPool。
- 新增 `app/TrollVNC/TrollVNC/WallTileWeb.html`：卡片墙专用 viewOnly 桥接页面（`webkit.messageHandlers.wallTile` 通道，与 Viewer 的 `viewer` 通道隔离），支持帧率节流（JS 定时隐藏 canvas）。
- 改造 `TVNCControllerViewController.m`：**移除 libvncclient 首帧抓取**（`TVNCFetchThumbnailViaRFB`/`TVNCThumbMallocFrameBuffer`/`TVNCImageFromFrameBuffer`/`TVNCTCPReachable`）、移除 `refreshThumbnails`/`refreshThumbIntervalFromGateway`/`thumbnailQueue`/`thumbnailTimer`；卡片画面 `_thumbView` 升级为 `TRWallTileWebView _wallWebView`（实时 RFB），保留 `thumbView` 作为不可见 cell 的缓存帧占位；`willDisplayCell`/`didEndDisplayingCell` 按可见性管理连接，**最大同时连接数 6**；视图模式切换/页面进出自动启停连接。
- 打包：`WallTileWeb.html` 已加入 pbxproj Resources（真机+Simulator），`TRWallTileWebView.m` 已加入 Sources；noVNC 经 `before-package.sh` 复制到 bundle `webclients/` 子目录，`WallTileWeb.html` import 路径为 `./webclients/novnc/core/rfb.js`。
- **Viewer 打包修复**（2026-08-10）：`ViewerWeb.html` 原未加入 pbxproj（`URLForResource:@"ViewerWeb"` 返回 nil，大屏无法打开），且 import 路径 `./novnc/core/rfb.js` 与实际 bundle 结构不符；已加入 pbxproj Resources（真机+Simulator）并将 import 修正为 `./webclients/novnc/core/rfb.js`。
- **前端点击卡片黑屏根因修复（2026-08-10，已验证）**：
  - **根因 1（网关 WS 订阅丢失）**：`trollvnc-farm/server/index.js` `handleVncSocket` 中 `tun.wsSet.add(ws)` 在 `e07df8c`（on-demand RFB）被误删，WS 会话从未注册进 RFB 数据订阅集合 → 设备回传的 RFB 握手+画面字节全部积压 `pending` 缓冲（截断 64KB）→ noVNC 收不到任何字节 → 黑屏。
  - **根因 2（viewOnly 不触发设备 5901）**：设备端 on-demand RFB（TrollVNC `976ac44`）只在收到 `rfb.start` 才连接本地 5901，而网关仅 `ctrl`（大屏）会话下发 `rfb.start`，卡片墙 viewOnly 会话不触发 → 设备 5901 一直 standby → 无画面。
  - **修复**：恢复 `tun.wsSet.add(ws)`；改为按 `tun.wsSet.size` 引用计数——**首个会话（viewOnly 或 ctrl）即下发 `rfb.start`，末个会话断开下发 `rfb.stop`**；cleanup 增加幂等保护（防 close/error 双触发）；pending 握手头补发给新会话。
  - **验证**：`trollvnc-farm/test/verify-rfb-fix.js` 端到端 6/6 PASS（注册 ack / 隧道建立 / viewOnly 首会话触发 rfb.start / FT_DATA 经 wsSet 转发 / viewOnly 输入只读不转发 / 末会话触发 rfb.stop）；真机 iPhone 实测网关日志确认全链路：`tunnel bridge sessions=1` → `FT_CMDACK cmd=rfb.start ok=true` → `FT_DATA ws=1`（WS 收到 12 字节 RFB 握手头）→ `FT_CMDACK cmd=rfb.stop ok=true`。
- **前端点击卡片黑屏根因（2 处，2026-08-10 已修复）**：
  - **根因 A（前端 enterFocus 崩溃，直接根因）**：`web/app.js` `enterFocus()` 在 `focus = {...}` 初始化**之前**执行 `focus.capMetadata = ...`（L658），此时 `focus` 为 null → `Uncaught TypeError: Cannot set properties of null` → `createRfb` 从未执行 → noVNC 从未连接 → **点击卡片黑屏**。已修复：先 `focus = { device: d, rfb: null }` 再挂载 `capMetadata/configSchema`，最后创建 `focus.rfb`；`index.html` app.js 版本 `v=15→v=16` 强制刷新。
  - **根因 B（viewOnly 会话握手被网关拦截）**：`handleVncSocket` 原 `canWrite = isCtrl || (isBroadcast && grp)`，viewOnly 会话完全禁止上行 → noVNC 的握手必需字节（版本响应 12B/ClientInit）无法转发 → viewOnly（IPA 卡片墙 TRWallTileWebView）握手卡死黑屏。已修复：允许所有会话上行转发（noVNC viewOnly 本身不发输入事件，安全），广播输入转发逻辑保留。
  - **握手兼容性结论（无需改代码）**：标准 `@novnc/novnc 1.5.0` 本就发送 **12 字节版本字符串** `"RFB 003.008\n"` 且 SecurityResult 后直接 ClientInitialisation，与 TrollVNC libvncserver 完全兼容；真机 `rfb-handshake-diag.js` 完整握手通过（版本行 → NoAuth → SecurityResult → ServerInit 1172x2536 → 后续消息）。
  - **验证**：`test/verify-bidirectional.js` 双向 6/6 PASS；`test/rfb-handshake-diag.js` 真机 viewOnly + ctrl 完整握手均成功。
- **控制台残留报错清理（2026-08-10）**：
  - `closeRfb` 对已断开 RFB 对象调 `disconnect()` → noVNC 抛 `Tried changing state of a disconnected RFB object`：修复为先检查 `_rfbConnectionState`（`disconnected`/`disconnecting` 跳过）。
  - `4003 no tunnel` 提示文案由「请配置网关」改为「设备隧道未建立（可能刚注册或正在重连），请稍候重试」（避免误导）。
  - `noVNC requires a secure context (TLS)`：已在网关 serveStatic 层对 `/novnc/core/rfb.js` 做内存替换屏蔽（`server/index.js` 静态服务分支），`Cache-Control: no-cache`，不动 node_modules；日志确认 `rfb.js patch: before=true after=false`。
  - `index.html` app.js 版本 `v=16→v=17`；serveStatic 带 `Cache-Control: no-cache`，改动即时生效。
- **频繁进出大屏 "Invalid server version" 修复（2026-08-10）**：根因 = 快速"点进→退出→再点进"时，网关 `rfb.stop` 尚未到达设备、旧 5901 连接仍挂着，而新会话 `rfb.start` 在设备端 `_localFd >= 0` 时**不重连**，导致 noVNC 版本请求被转发到已握手的旧连接 → libvncserver 协议错乱。修复：①设备端 `TRTunnelClient` 的 `rfb.start` 改为**无条件重连**（先 close 旧 fd 再 connect，保证全新握手，需重打包 IPA）；②网关 cleanup 时**清空 pending**（旧残留失效防补发）；③网关 `rfb.stop` **延迟 800ms 下发**（debounce，快速重进时 5901 连接保持，避免 stop/start 乒乓抖动）。
- **卡片墙服务懒加载 + 去重 + 纯隧道（2026-08-10）**：`TVNCControllerViewController.viewDidLoad` 移除立即 `refreshDevices`，卡片墙服务（拉取设备 + RFB）仅由首次切换到「控制」Tab（`viewWillAppear`）触发，右上角刷新按钮随时重新拉取；`handleDevices` 增加**按 id 去重**（`NSMutableSet` 去重，防重复上报/重复注册产生重复卡片），并保留**排除自身设备**（`selfDeviceId` 过滤）逻辑；设备列表**仅保留 `source=register` 隧道设备**，直连 host 设备模式已废弃（`pushViewerForDevice` 同步移除直连分支，统一走网关隧道）；**移除双指捏合调列数**（`pinchHandler`/`kGridColumnsKey` 已删），宫格固定 2 列，仅保留宫格/列表两档。
- **IPA 卡片墙改 hash 门控 + 变化拉图（2026-08-10，待打包）**：`TVNCControllerViewController` 卡片墙由 `TRWallTileWebView`（WKWebView + noVNC RFB 推流）改为——`UIImageView`（thumbView）+ 每设备 `NSTimer` 轮询：先 `invoke screen.hash`（0.3ms 级轻量 pHash），与上次相同则跳过（静止时零图片流量），**变化才 `invoke screenshot` 拉新帧渲染**；可见性管理（`willDisplayCell` 启动 ≤6 路轮询、`didEndDisplayingCell` 停止并缓存帧）、防重入（`wallBusy`）、间隔取设备 `ThumbInterval` 配置（默认 5s）；新增 `invokeCapAck:` 带回调的网关 invoke 封装。**连带根治 Jetsam 闪退**：删除 `TRWallTileWebView.h/m` + `WallTileWeb.html` + pbxproj 引用（WKWebView 进程与强引用环不再存在）。
- **Viewer 纯隧道化（2026-08-10，待打包）**：`TVNCViewerViewController` 删除全部直连分支——`buildWebSocketURL` 仅保留隧道（NSAssert 强制 useGatewayTunnel）、`invokeCap` 去掉直连本地分发（performOp/keyboardTapped/clipboardTapped/tagForCapId 等死代码及其 keysym 常量、sendKey/pasteText 桥接已删）、`fetchCapMetadata` 去掉直连 nil 分支。
- **4002 tunnel closed 根因修复（2026-08-10，待打包）**：设备端 `TRTunnelClient._passthroughLoop` 中本地 5901 连接 `read` 返回 EOF（rfb.stop 或服务端断开）时原 `return NO` 导致**整个隧道退出重连** → 网关关停 WS 会话（4002）。改为仅清理本地 fd 回 standby、继续等待 rfb.start，隧道保持常驻。
- **Web 端 wheel 警告屏蔽（2026-08-10）**：网关 serveStatic 对 `/novnc/core/rfb.js` 追加内存替换——`addEventListener("wheel", ...)` 显式 `{ passive: false }`（noVNC 滚轮缩放需 preventDefault），消除 Chrome `[Violation] Added non-passive event listener` 警告。
- **Web 端 touchstart Violation 屏蔽（2026-08-11）**：`rfb.js` 的 `touchstart` 聚焦监听（`_focusCanvas` 只调 `focus({preventScroll:true})`，不 preventDefault）追加 `{ passive: true }`（内存替换），消除 Chrome `scroll-blocking 'touchstart'` Violation 警告。纯网关侧，已重启生效。
- **Web 端 gesturehandler touchmove Violation 屏蔽（2026-08-11）**：`core/input/gesturehandler.js` 的 `touchstart/touchmove` 监听（手势检测需 `preventDefault`，不可 passive:true）显式 `{ passive: false }`（内存替换），消除 Chrome `scroll-blocking 'touchmove'` Violation 警告；`detach` 的 removeEventListener 不受影响。新增 `/novnc/core/input/gesturehandler.js` 内存 patch 分支。已重启生效。
- **移动端悬浮按钮改造（2026-08-11）**：原「≡ 拖动条 + ⚙ 圆形按钮」双元素结构（拖动只在 head 上，用户拖圆形无反应）改为——**单个圆形 WiFi 信号按钮（白色圆底）**：内部为三弧 WiFi 图标（SVG），整钮可拖动（pointer 拖拽 + 6px 位移阈值区分拖动/点击，安全边界含 safe-area），点击（未拖动）展开/收起操作菜单；控制会话期间每 3s `command ping` 测设备往返延迟，按延迟切换 WiFi 弧亮起数量与颜色：`<150ms` 全弧绿 / `<400ms` 中弧+点黄 / 更差或失败仅点红（未亮弧浅灰），title 显示延迟 ms。移除双指缩放。app.js v22。
- **移动端聚焦页移除顶部条（2026-08-11）**：连接状态已由 FAB 信号图标表示、退出由 FAB 菜单「退出控制」提供，移动端 `#workspace.focus-open .focus-head` 隐藏（画面全屏）；电脑端无悬浮按钮，保留顶部条（状态点/设备名/返回按钮）。CSS 改动，已生效。
- **悬浮菜单点空白收起修复（2026-08-11）**：原 `document click` 监听在触摸画面（canvas）时失效——noVNC gesturehandler 在 `touchstart` 里 `preventDefault/stopPropagation` 阻止了合成 click 冒泡 → 点画面关不掉菜单。改用 `document pointerdown`（先于 touch 事件派发，不受其 preventDefault 影响）。app.js v22。
- **Web 端 focus-head 组件改造（2026-08-10）**：布局改为「左=状态圆点（蓝=已连接 / 红=已断开）+ 设备名 (IP:port)，右=返回设备墙按钮」；状态不再显示文字，改红/蓝圆点（`focusStatusDot.on/.off`），app.js 版本 v20→v21。
- **网关 5901 重建改 ack 驱动（2026-08-10，2026-08-11 已打包 #97）**：接管/重开场景"有时没画面"根因修复——①**ctrl 会话无条件重建 5901**（`needRfbRebuild = isCtrl || isFirstSession`）：接管（新 ctrl 顶旧 ctrl 4001）时若不重建，新 noVNC 握手字节会转发到旧 5901 连接导致协议错乱黑屏；②**固定 400ms 窗口改 ack 驱动**：设备端 `rfb.start` 同步 connect 后回 ack 携带结果（`TRTunnelClient` 由"标记 → select 循环处理"改为命令解析处同步执行，`_startRequested/_stopRequested` 已删除），网关收到匹配 `tun.rebuild.id` 的 ack 才精确放行缓冲握手字节（消除设备冷启动/慢连接竞态），connect 失败显式 close ctrl 会话（4005"画面服务不可用"，不静默黑屏），3s 超时强制放行兜底（旧设备兼容）；会话全断时清理 rebuild 状态。**已打包 IPA**（commit `2a4df98` → fork/release → Actions #97，`dist/TrollVNC_0.0.1.tipa` 2.36MB）。
- **测试体系修复（2026-08-10）**：`verify-rfb-fix.js`/`verify-bidirectional.js`/`tunnel-test.js` 模拟设备新增 FT_CMD(rfb.start/stop) ack 回放（echo id, ok:true）；断言对齐当前行为（首会话 stop→start 重建、viewOnly 会话上行可转发、ctrl 接管 4001、广播输入）；`tunnel-test` broadcast 步骤等重建完成再发输入（重建窗口内首个上行字节被缓冲为握手字节）；`register-test` mDNS 断言 `_trollvnc-farm`→`_superphone-farm`（服务器改名未同步）。**npm test 全绿**（smoke/tunnel/register/dedupe/caps），verify 7/7 + 6/6。
- **重进黑屏 "Invalid server version" 根因修复（2026-08-11）**：退出会话时 `rfb.stop` 延迟 800ms 下发，期间设备 5901 旧连接仍在推最后几帧 → 网关 `wsSet.size==0` 缓冲进 `tun.pending`；重进新会话时 `handleVncSocket` 把 `tun.pending` 旧帧**直接 `ws.send` 补发给新 noVNC** → noVNC 把旧帧当版本响应解析 → `Failed when connecting: Invalid server version`（乱码 `Ów8Îæ` 即旧会话画面帧数据）+ 黑屏。修复：pending 补发仅限**不触发重建**的会话（`!needRfbRebuild`），触发重建（ctrl/首会话）时丢弃旧 pending；重建分支显式 `tun.pending = Buffer.alloc(0)`。新增 `test/pending-replay-test.js`（会话A 退出→残留帧入 pending→会话B 重进断言不收到残留+新帧正常）并纳入 npm test。**npm test 全绿（6 个测试文件）+ verify 7/7 + 6/6**。网关已重启生效（无需重打包 IPA，纯网关侧改动）。
- **手机端控制台点击卡片黑屏修复（2026-08-11）**：根因 = 移动端 `fitFocusPanel` 对 `max-width:900px` 直接 `return`，`focusStage` 无显式宽高（canvas absolute 不撑开父容器）→ stage 高度 0 → noVNC `scaleViewport` 把画面缩放到 0 → 黑屏；电脑端因 `fitFocusPanel` 设置了 `stage.style.height` 正常。修复：①CSS 移动端 media query 加 `.focus-stage { width:100%; height:100% }`；②`fitFocusPanel` 移动端分支显式设置 stage 100%（双保险）。纯前端，已生效（app.js v22）。
- **5801 直连页 FAB 同步 WiFi 白底样式（2026-08-11，待打包 IPA）**：`layout/usr/share/trollvnc/webclients/index.vnc` 手机端悬浮按钮由「紫色 ≡、右下角」改为与网关 8080 一致——**白色圆底 + 三弧 WiFi 图标**，默认位置 `right: max(16px, env(safe-area-inset-right)); top: 25%`（靠右边缘、垂直 1/4 高度）；信号颜色按连接状态驱动（直连页无网关 ping 通道）：`connect`→`data-sig=sig-high` 全绿 / `disconnect`→移除 data-sig 全灰，title 同步提示；横屏小屏 48px 缩小适配。拖动边界仍走 `getSafeBounds`（含 safe-area）。**该文件在 IPA 包内，需重新打包生效**。
- **IPA 查看器悬浮按钮同步 WiFi 信号样式（2026-08-11，待打包 IPA）**：`TVNCViewerViewController.m` 悬浮按钮由「⚙ gearshape.fill、暗底、右下角」改为——**白色圆底（56pt、边框 rgba(0,0,0,.12)、阴影 0 6 12）**，图标用 `UIGraphicsImageRenderer` 绘制三弧 WiFi（a1 外弧 / a2 中弧 / a3 底部三角，与网页 SVG 一致）；默认位置右上（`right:16 / top:25%`，加 `MAX(safeTop+8, ...)` 防刘海遮挡）；新增 `startSignalPoll/stopSignalPoll` 每 3s `POST /api/devices/:id/ping` 测网关往返延迟，阈值与网页一致（<150ms 全绿 / <400ms 中+点黄 / 失败仅点红），`viewDidLoad` 启动、`viewWillDisappear` 停止；菜单行为不变（primaryAction 弹出能力菜单）。**需重新打包 IPA 生效**。
- **8080 移动端悬浮菜单"点穿"修复（2026-08-11，已生效）**：根因 = `opsMenu` 固定定位 `right:12px; bottom:96px`，手机竖屏菜单较高时顶部伸到右上角 FAB 区域并被 z-index 65>60 覆盖，用户二次点击"按钮"实际点中菜单项 → 直接触发配置/参数子窗口（以为"点穿、没显示列表"）。修复：①新增 `positionOpsMenu()`——菜单按 FAB 位置动态定位（优先 FAB 下方、空间不足上方），按可用空间裁剪 `max-height` + `overflow-y:auto`，**任何情况下不覆盖悬浮按钮**；②FAB `pointerdown` 加 `preventDefault + stopPropagation` 防穿透；③拖动 FAB 时菜单实时跟随重定位、窗口 resize 重定位。app.js v23，serveStatic no-cache 已生效。
- **5801 直连页状态并入信号按钮 + 延迟显示（2026-08-11，待打包 IPA）**：左上角成功提示「已连接 · 画面接收中」不再常驻（收到帧后隐藏 hint），状态统一由信号按钮表达；新增 `startSigPoll/stopSigPoll`——连接后每 3s **同源 fetch `./caps.js`（no-store）测 HTTP RTT**（直连页无网关 ping 通道，以同源静态资源往返为网络质量代理），阈值与 8080 一致（<150ms 全绿 / <400ms 中+点黄 / 失败红点），未连接保持全灰，title 显示「已连接 · 延迟 Xms」；断开/失败停止轮询并回灰。面板定位同步升级为避开 FAB（优先下方、上方兜底，按空间裁剪 max-height，超高滚动）。**已打包 IPA（2026-08-11，Actions #98）**。
- **IPA 打包部署（2026-08-11，Actions #98）**：commit `8cd5b6f`（WiFi 信号 FAB on viewer + 5801 直连页）推送 fork/release 触发构建。**build 矩阵 4 方案全部成功**（默认/rootless/roothide/bootstrap，包内 `webclients/index.vnc` 已验证含 startSigPoll/sig-high/wifi SVG/top:25%，旧紫色样式零残留）；**release job 失败**——tag `v0.0.1` 已在 #97 创建，softprops/action-gh-release 重试超时，**不影响 artifact**。产物 `packages-bootstrap` → `dist/TrollVNC_0.0.1.tipa`（2.37MB，新包）。下载方式：git 凭据复用 `GET /actions/artifacts/9073579625/zip`（401 需 Bearer，git credential fill 提取 GCM 缓存 token）。**注意：后续再 push release 触发 Actions 会继续在 release job 失败，需先删旧 tag v0.0.1 或 bump PACKAGE_VERSION**。
- **电脑端控制台去除宫格布局切换按钮（2026-08-11，已生效）**：`layoutBtn`（四宫格图标）在电脑端（≥901px）隐藏（CSS media query，移动端保留），设备墙固定平铺、从左到右逐行显示（默认竖屏 2 列 `v2`，`#wall` grid）。serveStatic no-cache 已生效，无需重启。
- **硬件按键能力端到端验证（2026-08-11，真机在线实测通过）**：链路核查 + 在线设备 `iPhone`（DCFF7F84-...）实测——前端能力按钮 → `doInvoke` → `caps.js invokeCap` → `POST /api/devices/:id/invoke` → 网关 `sendDeviceCmd`(FT_CMD) → 设备命令通道 → `TRCapabilityRegistry` executor → `STHIDEventGenerator` 真实注入。实测 `service.state`（返回 running）与 `home`（ack ok，设备回桌面）均成功。全部 hid 按键映射确认：home→menuPress / power→powerPress / volup/voldn→音量 / mute→mutePress / briup/bridn→亮度 / keyboard→呼出键盘，另有 home.double/long、power.double/triple/long、*.down/up 等扩展（无参、可批量）。**注意 power 会真实锁屏设备**。
- **5801 RFB 键盘 Home 键失效根因修复（2026-08-11，已提交待打包）**：5801 直连页（PC 右侧栏）点击 Home 无反应——根因 = 设备端 `kbdAddEvent` 把 RFB KeyEvent 的 `XK_Home`(0xff50) 经 `keysymToString` 映射为 `"HOME"` → `STHIDEventGenerator keyDown:@"HOME"` → **Keyboard Page Home(0x4A)**，而 iOS 无该 usage 的"回桌面"语义（必须 **Consumer Page Menu**）。修复：`kbdAddEvent` 特判 `XK_Home` → `menuDown/menuUp`（与 invoke 链路 `menuPress` 一致），任意 RFB 键盘 Home 均能回桌面。volup/voldn/mute/briup/bridn（XF86 keysym）与 power（中键 PointerEvent→powerDown，5801 页 `pointer(2)`）原本即正确。**需重打包 IPA**。
- **8080 能力执行加 toast 反馈 + 设备墙无线列平铺（2026-08-11，已生效）**：①`doInvoke` 成功/失败均弹右上角 toast（`✓ 已执行：xx` / `✗ 失败原因`，2s 自动消失），替代原静默成功/`alert`，解决"点击能力无反馈以为没执行"；②电脑端（≥901px）`#wall` 改为 `grid-template-columns: repeat(auto-fill, minmax(min(var(--card-w),100%),1fr)) !important`——**无线列、按容器宽度自适应平铺，从左到右逐行**（覆盖 applyLayout 内联列数）。app.js v24。**注意**：本次 push fork/release 需 `git -c http.version=HTTP/1.1 push`（schannel HTTP/2 握手失败，HTTP/1.1 正常）。
- **IPA 打包部署（2026-08-11，Actions #99）**：commit `fd9941f`（XK_Home → Consumer Menu 修复）推送 fork/release。**build 矩阵 4 方案全部成功**（编译通过）；release job 仍因 tag v0.0.1 已存在失败（不影响产物）。产物 `packages-bootstrap` → `dist/TrollVNC_0.0.1.tipa`（2.37MB，覆盖 #98 包）。**该包含：WiFi 信号 FAB（5801+IPA）、5801 状态并入信号按钮+延迟轮询、XK_Home 回桌面修复**。
- **墙屏"缩放百分比"改为"卡片 px"模型（2026-08-11，已生效）**：去除原 `--zoom` 百分比缩放（50%~220%）及其与 auto-fill 的冲突（tile 显式宽度不填满列 → 缩放时溢出/留白）。改为：顶部控件显示「卡片 [160~400px] 280px」直接调单卡像素宽（`--card-w`，localStorage key `farm_cardw`，app.js v25）；PC 端 `#wall` = `repeat(auto-fill, minmax(var(--card-w),1fr))` + `.tile{width:auto}`——**窗口任意宽度下卡片拉伸填满整行（grid stretch 紧挨无空隙）、从左到右逐行、第一行排满自动换第二行**；聚焦态侧墙 `width: calc(var(--card-w)+12px)`；移动端 tile `width: min(var(--card-w),46vw)`。全局移除 `--zoom`。
- **卡片重叠根因修复（2026-08-11，已生效）**：用户反馈"第二行盖住第一行"。Edge headless + CDP 实测复现：卡片渲染高 498px，但 **grid 行高只有 84px** —— 根因 = **CSS `aspect-ratio` 计算出的高度不贡献给 grid 行高**（Chromium 行为），卡片溢出各自行高导致下一行覆盖。修复：`.tile` 改用 `height:0 + padding-bottom:177.7778%`（9:16，padding 计入行高，行高 = 卡片高），内部 `.tv` 改 `absolute inset:0`；`layout-h/v` 对应 `56.25%/177.7778%`；聚焦侧墙 tile 固定 `width:var(--card-w)`（flex 下防 width:auto 塌缩）。另外 `minmax(min(var(--card-w),100%),1fr)` 中 `min()` 使 `1fr` 拉伸失效（列固定 280 右侧留白），改为 `minmax(var(--card-w),1fr)`。1400×900 实测：4 列 × 330px 填满整行、行距正确、overlaps=[]。
- **5801 直连页状态提示改画布居中浮层（2026-08-11，已打包 IPA #100）**：原左上角小胶囊 `#hint`（连接中/已连接等待/已断开/连接失败）改为 `#screen` 内**画布居中状态浮层** `#status`——初始「连接中…」+ 主题色转圈加载动画（`.spin` 旋转，`#status.loading` 显示）；连接成功且收到首帧后浮层隐藏进入画面；「已连接但未收到画面」（5s 超时）、「已断开+原因」、「连接失败」均在画布中央展示（半透明黑底 `rgba(0,0,0,.5)`，`pointer-events:none` 不挡画面操作，FAB z-index 10 在其上仍可点）。状态文案与逻辑与原版一致（"同逻辑仅换位置"），新增 `setStatus(text, load)/hideStatus()` 两个函数级注释方法。`layout/usr/share/trollvnc/webclients/index.vnc`。
- **5801 直连页 PC 端画面居中 + 操作栏贴画面右边框（2026-08-11，已打包 IPA #100）**：两步修复——①删除 `#screen canvas { position:absolute; left:0; top:0 }`（该规则覆盖了 noVNC 内部 flex+margin:auto 居中，导致竖屏手机"靠左上、右侧大片黑边"），改由 noVNC 自动居中；②`#bar` 改为 `position:fixed` 脱离 flex 流（z-index:6），新增 `syncBarPosition()` 运行时按画布实际 rect 定位到其右缘并匹配高度（画布 0×0 未出画面时隐藏操作栏防错位闪现），触发点：画布 `ResizeObserver` + `window resize` + 初始化调用。**Edge headless 实测**（1440×900、竖屏画布 505×899）：修复前画布左上锚定 left_gap=0 / right_gap=935px、按钮列在窗口最右缘；修复后画布窗口水平居中（left_gap=468 / right_gap=467），操作栏贴画面右缘（bar.left − canvas.right ≈ 1px）。手机端 FAB 逻辑未动（探针实测窄视口 fab.display=flex / bar.display=none）。`layout/usr/share/trollvnc/webclients/index.vnc`。
- **设置面板清理「状态组/版本IP/Clients 按钮」+ 连接页客户端卡能力核对（2026-08-11，待打包 IPA）**：①Root.plist 移除首个「状态」组与 `Enabled` 开关（App 启动即启动服务，无需开关）；②`TVNCRootListController` 移除首组 footer 动态注入的版本号+当前 IP（`defaultFooterText/currentStatusText/updateFirstGroupAndReload/TVNCGetEn0IPAddress` 及网络路径监控一并删除，`applyChanges` 弹窗不再附带 IP 行）；③移除设置页左上角「Clients」按钮（`leftBarButtonItem`+`showClients`，客户端管理已由连接页内嵌卡片承载）；④连接页客户端卡片**能力核对通过**：内嵌的正是同一 `TVNCClientListController`（`embedded=YES`），右滑冻结/断开/解冻、长按复制 Host/ID、全部断开、实时订阅、冻结持久化、在线/冻结计数全部齐备，无需补齐；⑤`TVNCSettingsViewController`（查看器内配置面板）同步移除「直连参数→服务开关 Enabled」。设置项对照原始仓库（upstream bc9f6e0）：**无需要补齐的缺失项**——Enabled/DesktopName（统一真实设备名）/Reverse 三件套（硬约束废弃）/营销按钮均属故意移除。**已打包 IPA #100**。
- **IPA 打包部署（2026-08-11，Actions #100）**：commit `f1c7684`（5801 居中状态浮层 + PC 画面居中/操作栏贴画布右缘 + 设置面板清理）推送 fork/release 触发构建。**build 矩阵 4 方案全部成功**（default/rootless/roothide/bootstrap，包内 `webclients/index.vnc` 已验证：旧 `#hint` 零残留、`#status` 居中浮层 + `.spin` 加载动画、`syncBarPosition` 均在，`#screen canvas` 绝对定位已移除；`Root.plist`「状态」组零残留）；**release job 失败**——tag `v0.0.1` 已存在（softprops/action-gh-release，不影响产物）。产物 `packages-bootstrap` → `dist/TrollVNC_0.0.1.tipa`（2.36MB，覆盖 #99 包）。**真机验证要点**：5801 加载中心转圈→首帧进入画面、断开居中提示；PC 端画面居中+操作栏贴画面右缘；设置页无状态组/版本/IP/Clients 按钮；连接页客户端卡冻结/断开正常。
- **架构三阶段落地（2026-08-11，commit `515cef6`，待打包）**：①**Phase A AppStore 单一数据源**——新增 `TVNCAppStore.h/.m`（网关状态机 Idle→ServiceUp→Registered→Disconnected + 设备目录缓存 60s TTL + 结果驱动注册判定 `isRegistered` 含 selfDeviceId，退避重试 1→2→4→8→15s 封顶，替代固定延迟）；②**Phase B GatewayClient 网络层**——新增 `TVNCGatewayClient.h/.m`（统一 URLSession 封装 devices/invoke/ping/caps/config/changedSince，Token 注入 + 主线程回调），TVNCControllerViewController 全部 URLSession 迁移；③**Phase C 事件驱动画面变化**——设备端 `TRGatewayClient` 订阅本地 46752 `screen.subscribe` → `screen.event` 经注册通道上报 `screen_changed` → 网关 `index.js` 记录 `screenChangedAt` 支持 `/api/devices?changedSince=` 增量查询 → 控制端 1.5s 增量通道（`wallChangedTimer`）+ 双速 hash 兜底（变化 1s 快检 / 静止 ×1.5 退避至 15s 封顶，`wallSkipHash` 吸收防重复拉图）；④iOS14 控制 Tab 闪退修复（`UIButtonConfiguration` iOS15 API 加 `@available` 回退）；⑤客户端右滑 leading→trailing 修复；⑥Connect Hero 卡改真注册状态（绿/黄/红/灰四态）。
- **bootstrap 构建失败根因修复（2026-08-11，commit `71448f1`）**：Actions #101（`515cef6`）**仅 bootstrap scheme 失败**（其余 3 个成功）——根因 = `project.pbxproj` 新增 `TVNCAppStore.h/m` + `TVNCGatewayClient.h/m` 时只加了 **PBXBuildFile + Group children + Sources**，**漏加 PBXFileReference 四条目** → xcodebuild 找不到源文件；bootstrap 是唯一编译 app 子工程的 scheme，故其余 scheme 不受影响。已补全 FileReference 四条目；同时修复 `TVNCAppStore.m isSameDirectory` 首次拉取时 `[NSSet setWithArray:nil]` 崩溃风险（nil 兜底）。commit `71448f1` 推送 fork/release 触发 **Actions #102，build 矩阵 4 方案全部成功**；release job 仍因 tag `v0.0.1` 已存在失败（不影响产物）。
- **.tipa 结构验证（2026-08-11，Actions #102 @ `71448f1`）**：产物 `packages-bootstrap`（artifact 9090940983，git 凭据提取 GCM token 下载）→ `TrollVNC_0.0.1.tipa`（2,485,278 字节）。**结构核验全部通过**：①zip 魔数 `PK` 合法；②标准 TrollStore 布局 `Payload/TrollVNC.app/`（181 条目）；③**Info.plist**：`CFBundleIdentifier=com.82flex.TrollVNCApp` / `CFBundleExecutable=TrollVNC` / `CFBundleName=TrollVNC` / 版本 `0.0.1(1)` / `MinimumOSVersion=14.0` / `CFBundlePackageType=APPL`；④`PkgInfo=APPL????`；⑤**Mach-O 三二进制全部 arm64 且含 LC_CODE_SIGNATURE**：`TrollVNC`、`trollvncserver`、`trollvncmanager`（bootstrap 专属，确认新包）；⑥`_CodeSignature/CodeResources` 存在（52,773B，代码签名完整）；⑦bundle 内含 `TrollVNCPrefs.bundle`、`webclients/novnc`、`Assets.car`、AppIcon 全套。**该包包含：架构三阶段（AppStore/GatewayClient/screen_changed 链路）+ iOS14 崩溃修复 + XK_Home 修复 + WiFi FAB 等全部此前累积改动**。
- **控制 Tab 崩溃根因修复（2026-08-11，commit `ff7e10c`，待打包）**：**症状**——连接网关后点击「控制」Tab，App 直接崩溃；未连接时一切正常。**根因**——设备卡片/列表 cell 离线分支对 `lastSeen` 字段调用 `.length`，但网关 `/api/devices` 返回的 `lastSeen` 是 **毫秒时间戳 NSNumber**（`Date.now()`），`[NSNumber length]` 触发 **unrecognized selector 崩溃**。此模式潜伏已久（`lastSeen.length` 自 ce32c5c 就有），但此前设备恒为 online（不进入离线分支）；本次连接后设备被判离线（注册心跳超时/隧道断开）**首次触发**，且网关残留 8 台 E2E 测试设备（全部 offline + lastSeen=ms）——**任何一台离线设备渲染即崩，故"连接后必崩"**。**修复**——`TVNCUtil.h` 新增类型安全工具 `TVNCFormatLastSeen(id)`（NSString 原样 / NSNumber 毫秒→`MM-dd HH:mm` / 空值返回空串），`TVNCControllerViewController.m`（卡片 cell）与 `TVNCDeviceListCell.m`（列表 cell）两处离线分支改用该函数；全量排查确认其余设备字段（host/online/source/configs/iosVersion 缺失）类型均安全。commit `ff7e10c` 推送 fork/release 触发 **Actions #103**。
- **自身设备识别修复（2026-08-11，commit `bac4b02`+`5e72853`，已打包 #105）**：**两个症状同源**——①卡片墙显示自己设备（未过滤 selfDeviceId）；②连接页 Hero 卡连接已建立却恒显「连接中」（#102 引入 TVNCAppStore 真注册判定后暴露；旧版用 isServiceRunning 存活判定）。**根因**——设备端 UUID 由 **trollvncmanager 以 root (uid 0)** 生成（`TVNCServiceCoordinator spawnService` 的 `setUserIdentifier:0`，且不 drop），经 `initWithSuiteName:@"com.82flex.trollvnc"` 写入 **root 用户域**（/var/root/Library/Preferences/com.82flex.trollvnc.plist）；App 控制端（mobile uid 501）读同一 suite 只能到 **mobile 用户域**（/var/mobile/Library/Preferences/...）→ **跨用户隔离读不到 DeviceUUID → selfDeviceId=nil** → 不过滤自身 + `isRegistered` 永远 NO（状态卡 ServiceUp）。网关配置等配置项由 App/trollvncserver（均 mobile）读写故正常，仅 UUID 隔离。**修复**——`TVNCUtil.h` 新增 `TVNCReadSelfDeviceId()`：优先直接读 root 用户 preferences 文件（App 有 `com.apple.private.security.storage.preferences` entitlement），回退当前用户域；`TVNCAppStore.isRegistered` 与 `TVNCControllerViewController handleDevices` 改为**动态读取**（不缓存，兼容服务未启动时序），删除两处 init 缓存的 `selfDeviceId` 属性/赋值。**编译修复**（`5e72853`）——`TVNCUtil.h` 缺 UIKit import（`TVNCRestartVNCService` THEBOOTSTRAP 分支用 `UIApplication`），此前所有 import 它的文件恰好带 UIKit，`TVNCAppStore.m`（纯 Foundation）新 import 后 #104 bootstrap 编译失败；补 `#import <UIKit/UIKit.h>`。**Actions #105 build 4 方案全部成功**，产物 `packages-bootstrap` → `dist/TrollVNC_0.0.1.tipa`（2,485,667B，已验证二进制含 `/var/root/Library/Preferences` 特征）。**推送备注**：schannel TLS 握手失败时用 `git -c http.sslBackend=openssl -c http.sslVerify=false -c http.version=HTTP/1.1 push`。

#### 2.3b 卡片墙增强 + 同步控制 + 直控模式（2026-08-11，全部已生效）

- **卡片墙尺寸阈值动态化（2026-08-11）**：`computeCardWBounds()` 按当前容器宽度反推列数——每行 5 列取最大 px（动态）、固定最小 110px（约 15 列）、固定默认 175px（约 10 列），滑块范围实时自适应。`--card-w` 驱动。`web/app.js` L1699。
- **多数设备比例自适应 + 变化驱动签名（2026-08-11）**：`majorityTilePb()` 按 0.01 精度分组取占比最高的设备屏幕比例设置 `--tile-pb`（卡片高宽比）；`devicesSignature()` 拼接在线设备 id+屏幕尺寸，**签名变化才重算**（避免每次轮询重排的额外性能开销）。`web/app.js` L124/L1795/L1822。
- **30 台虚拟预览设备（2026-08-11）**：`MOCK_COUNT=30` + `makeMockDevices()` 注入布局预览（`.tile-mock`，SVG 等比占位），`refreshDevices` 中 `devices = data.devices.concat(MOCK_DEVICES)`；置 0 即关闭。`web/app.js` L71-92/L134。
- **卡片墙隐藏滚动条（2026-08-11）**：`#wall` 用 `scrollbar-width:none` + `::-webkit-scrollbar{display:none}` 隐藏滚动条（默认滚动逻辑保留，仅去组件外观）。style.css。
- **聚焦态布局恢复 grid 平铺（2026-08-11）**：`#workspace.focus-open #wall { flex:1 1 auto; margin-left:12px; overflow:auto }`，聚焦时右侧卡片墙恢复 grid 网格平铺（不再挤成紧贴操作列的单列）。
- **大屏头部紧凑化（2026-08-11）**：`prefitFocusPanel` 头部高度 48→26；focus head `padding:3px 10px; font-size:13px`，仅状态圆点+标题，返回按钮移至操作列顶部 ←（`#btnBack`）。
- **遗留引用清理（2026-08-11）**：完成剩余验证并清理 `batchCount` 等残留引用（`web/` 目录已零匹配）。
- **操作列细滚动条主题色 + 去除分组标题（2026-08-11）**：`.focus-ops` 滚动条由默认白色改主题色（Firefox `scrollbar-color: var(--line)`；WebKit 6px 圆角 + 悬停 accent）；`.focus-ops-cap .cap-group-title { display:none }` **去除操作列分组标题**（移动端悬浮菜单标题保留）。style.css L84-88/L329。
- **同步控制完整实现（2026-08-11）**：`toggleSyncMode` 进入同步选择模式（🔗 按钮**红色激活** `sync-active`，图标恒为 🔗）；`toggleSync(deviceId)` 为勾选设备建立 `grp=wallSession&viewOnly` RFB 订阅（卡片实时推流显示同步执行情况 + 作为广播接收成员），卡片边框高亮 `tile-selected` + 中央「同步中」徽标（不遮挡推流、不可触控）；**主控连接始终携带 `grp+broadcast`**（勾选/取消同步不重建主控连接 → 主控画面零跳动）；退出同步/取消勾选恢复截图轮询。`web/app.js` L876-986、style.css L90。
- **同步入口改版：移除 chkFocusBroadcast 复选框（2026-08-11）**：主控改为始终广播后，操作列 `chkFocusBroadcast` 复选框已无实际作用 → **彻底移除**（`web/app.js`/`index.html` 已零匹配）；同步入口唯一化为「🔗 同步」按钮两态，进入同步选择模式后**点卡片即切换同步**（无复选框），选中态 = 边框高亮 + 「同步中」徽标。
- **大屏操控频繁断开隐患排查（2026-08-11，结论无隐患）**：核查主控连接链路——主控始终携带 `grp+broadcast`（勾选/取消同步不重建主控连接，无 ctrl 抢占/重连抖动）；`chkFocusBroadcast` 移除后同步入口唯一；叠加网关 ack 驱动重建（`needRfbRebuild = isCtrl || isFirstSession`）与同设备单会话约束（6.10），确认**无导致主控频繁断开的隐患**。
- **直控模式实现（2026-08-11）**：批量按钮左侧新增「直控」按钮（`#directBtn`，两态：激活主题蓝高亮 `direct-active` + 显示设备数 `直控 (N)`），点击为所有在线真实设备建立**非 ctrl 可输入 RFB**（互不抢占、输入直达设备 5901），此时悬停放大/引导提示失效、卡片 canvas 直接接收鼠标/触控输入（无需进大屏）；再次点击退出并恢复截图轮询。`web/app.js` L1009-1107、style.css L348-358/L179-180。
- **直控模式"操作后丢失 / 推流约 5 秒断开"根因修复（2026-08-11）**：
  - **根因 1（前端轮询覆盖画面）**：`updateWallTile` 每 6s（`refreshDevices` 周期）见 `inst.rfb` 为空（直控 RFB 存于独立 `directRfbs` Map，不挂 inst.rfb）即恢复截图轮询并 `tv.innerHTML='连接中…'` **覆盖直控 canvas** → 下一次轮询后画面变静态截图、点击失效（用户观感"操作两下/5 秒后丢失"）。修复：直控模式且该设备在 `directRfbs` 中时跳过恢复逻辑。
  - **根因 2（前端退出不关连接）**：旧 `exitDirectMode` 只 `directRfbs.clear()` **从不 close RFB** → 直控 WS 残留服务端（此前日志 `sessions=2` 的来源），多次进出会话堆积。修复：退出时显式 `closeRfb` 全部直控 RFB；设备离线/被移除时同步清理其直控连接。
  - **根因 3（服务端协议串流，真凶 → type 10）**：设备端仅 1 条隧道 + 1 个 5901 连接（`_localFd` 单数），残留会话与新直控会话**同时上行共同驱动 5901 协议状态机** → noVNC 在消息循环收到非法字节 `Unexpected server message (type 10)` 断开（`Tried changing state of a disconnected RFB object` 仅为 _fail 附带日志，非独立错误）。修复：`handleVncSocket` 新会话建立时**顶掉同设备其他非 ctrl 会话**（4001 preempted by new session），随后按"首会话"语义触发 5901 重建拿到干净握手（ctrl 间抢占仍由原分支处理）。`server/index.js` L331-343。
  - 附带：抽取 `startDirectRfb(d)`（直控模式下**新上线设备自动补建直控 RFB**，保持"所有在线设备直达控制"语义）；直控断开 toast 显示断开码便于诊断。`web/app.js` L1018-1046/L168-180。
- **直控推流画面居中修复（2026-08-11）**：卡片固定 9:16（0.5625）、iPhone 竖屏 1170×2532（0.462 更瘦长），noVNC scaleViewport **等比缩放**后 canvas（153px）窄于卡片（184px），旧 `.tile canvas{left:0;top:0}` 钉在左上角 → 画面偏左、右侧余白。修复：`.tile canvas` 改为 `left:50%; top:50%; transform:translate(-50%,-50%)` 居中，与截图缩略图 `object-fit:contain` 的显示位置一致（模式切换无跳变）；点击坐标经 `getBoundingClientRect` 映射不受 transform 影响。style.css L182-189。实测：水平/垂直偏差 0.0px、等比 0.4621、完整显示入卡。
- **验证（2026-08-11）**：`verify-sync-ui.mjs` 13/13 全绿；直控持续推流 12s 保持（超过此前 5s 断开点）、循环进出 3 次 `sessions` 恒为 1（无残留堆积）、无任何协议串流错误；`node --check` 通过。服务已重启（8080）生效。
- **token 鉴权默认禁用确认（2026-08-11，运维确认，无代码改动）**：`FARM_TOKEN` 环境变量未设置时网关启动打印 `token auth: disabled (LAN only!)`，控制台 `http://0.0.0.0:8080` 直接访问无需令牌（内网自用边界，宪法 1.3）；注意前端 localStorage `farm_token` 会被 URL `?token=` 参数覆盖，下次不带 token 访问根路径时会清空该值。
- **手机端 H5 布局对齐 IPA 控制端（2026-08-11，已生效）**：移动端 header 精简为**仅「批量操作」+「布局切换」两个操作组件**（与 IPA 控制端顶部导航一致），PC 端继承组件（卡片滑杆/刷新/直控/添加设备/设备统计）在 ≤900px 全部隐藏；**布局切换器由「横屏1-6/竖屏1-6 十二项菜单」改为「卡片（宫格）/ 列表」两档**（借鉴 IPA 布局按钮：窄样式仅图标+▾，选中项切换 SVG 图标），列表视图 = 单列行式（48px 缩略图 + 设备信息行 + ⋯），卡片视图移动端固定 2 列大卡片。附带修复两个移动端布局错误：①`#workspace` 移动端改 `align-items:stretch`（原 flex-start 使 `#wall` 宽度收缩为内容宽 242px）；②移动端卡片改 `width:auto` 填满 grid 列（原 `min(var(--card-w),46vw)` 被 computeCardWBounds 在 390px 视口算成 115px 小卡片）。布局状态 localStorage `farm_layout` 持久化，PC 端恒为卡片宫格。`web/app.js` L1785-1880、`web/style.css` L306-352、`web/index.html` L20-31。验证：`test/verify-mobile-layout.mjs` 14 项全绿（header 精简/无溢出/宽度撑满/两档切换/列表行式/批量 checkbox）+ PC 端 verify-sync-ui 13/13 无回归。

#### 2.3c 今日沉淀的开发参考规则（思维逻辑，2026-08-11）

> 从今日「同步控制 + 直控模式 + 断连修复」全过程中提炼的可复用规则，作为后续开发的思考基准（与记忆库 project_memory 互补，文档侧留存一份便于反查）。

1. **模式状态与轮询刷新隔离**：模式专用连接/状态（`directRfbs`/`syncRfbs`）独立于设备轮询状态（`inst.rfb`）管理；所有 `refresh*`/`update*` 轮询逻辑必须感知当前模式，禁止"见 inst.rfb 为空就重启采样"（这是直控 5 秒断开的真凶之一）。
2. **退出 = 关闭全部底层连接**：任何模式/功能退出逻辑，clear 集合的同时必须真正 close 底层 WebSocket，否则残留会话堆积（`sessions=2`）→ 再次进入串流。
3. **同设备单会话（服务端兜底）**：设备端仅 1 条隧道 + 1 个 5901 连接，多会话共享会双重驱动协议状态机 → 串流。网关会话入口统一"顶掉旧会话 + 触发重建"，从架构层杜绝前端残留导致的协议错乱。
4. **noVNC 错误分级识别**：`Unexpected server message (type N)` = 协议串流根因（先查 `sessions=N` 日志，>1 即异常）；`Tried changing state of a disconnected RFB object` 只是 `_fail` 附带日志，不是独立错误，别被误导。
5. **noVNC canvas 等比但不居中**：`scaleViewport` 等比缩放后 canvas 可能窄于/矮于容器且钉左上角 → 需 CSS `left:50%; top:50%; translate(-50%,-50%)` 居中实现 contain；点击映射经 `getBoundingClientRect`（含 transform）计算不受影响。
6. **主控零跳动 = 不重建**：主控连接始终携带 `grp+broadcast`，勾选/取消同步不重建主控连接，杜绝画面跳动/重连抖动（比"动态切换广播开关"更优）。
7. **变化驱动签名**：轮询/布局重算用签名对比（设备集合+屏幕尺寸拼接），仅签名变化才重算，避免周期重排的额外性能开销。
8. **卡片高宽比用 padding-bottom**：CSS `aspect-ratio` 高度不贡献 grid 行高（Chromium 行为）→ 下一行覆盖上一行；必须 `height:0 + padding-bottom:%` + 内部 `absolute inset:0`。
9. **竞态两态按钮**：批量/同步/直控等切换按钮用"未激活⇄激活"两态（激活变色、图标不变），比"变 ✕ 取消"更高级、状态更直观。
10. **虚拟设备注入验证**：用 `MOCK_COUNT=30` 注入 mock 设备验证布局/比例/性能，不影响真实链路，置 0 即关闭。

#### 2.3d 卡片 ⋯ 菜单（参数快捷 + 常用能力）+ 端口全固定（2026-08-12；2026-08-13 重构）

> 用户确认的设计初衷："IPA 给前端开放可由前端配置和控制的东西"——前端应能便捷调整 IPA 上有实际意义的参数；**点击 ⋯ 后直接在一级菜单显示常用参数设置，重启服务不应外部实现，而是 IPA 本身暴露的能力**。菜单每一项数据源必须来自 IPA 上报（capMetadata/configSchema），点击即 invoke/setConfigs 直达设备。（2026-08-13 重构后改为前端自包含定义，见 §2.3e）两项落地：

**① 卡片 ⋯ 菜单 = 参数快捷段 + 常用能力段 + 更多参数入口（caps.js `QUICK_CONFIG_GROUPS` + `QUICK_ACTIONS`，2026-08-13 重构）**
- **参数快捷段**：一级菜单直达 6 项有意义参数（2 组，无需进二级配置面板）：
  - 画面与性能：`Scale` `FrameRateSpec` `PerformanceMode`
  - 输入与交互：`ClipboardEnabled` `ViewOnly` `Notifications`
- **常用能力段**：`QUICK_ACTIONS = ['service.restart']`——重启服务是 IPA 自身暴露的能力，点击 `doInvoke` 直达设备（invoke → TRGatewayClient.restartHandler → gWatchDog stop/start 重启），**非前端/网关外部实现**
- **更多参数入口**：`showConfigPanel(d)`（完整配置面板按 reload 分区）
- **已删除**：设备管理段（查看/控制、详情、编辑、测试在线、删除）与 menuCaps('tile') 低频管理段——一级菜单仅编排前端自包含定义的能力/参数（2026-08-13 重构后见 §2.3e）
- 交互：PC 鼠标悬停该项 → 展开该项控件 + 当前值；触屏点击标题锁定展开；保存即 `setConfigs` 同步到设备，成功后 toast + 收起（app.js `showTileMenu` 参数段 + `formatConfigVal`，caps.js `QUICK_CONFIG_GROUPS`，style.css `.qp-item/.qp-editor/.qp-save`）。
- 实现要点：参数行从前端 `CONFIG_DEFS` 取 schema（无则跳过）、从 `configs`（register 上报当前值）取当前值；hover 展开 + 点击锁定（`__qpManual`）避免触屏无 hover；`mouseleave` 仅未锁定时收起。
- 端口项（Port/HttpPort/GatewayPort）不在清单，configSchema 已删 → 菜单天然不显示。

**② 端口全固定不可调（5901/5801/18081/8080/18181）**
- 设备端：`trollvncserver.mm` daemon 忽略 defaults 的 Port/HttpPort（gPort=5901、gHttpPort=5801 硬编码）；`TRGatewayClient.mm` `_gatewayPort/_vncPort/_httpPort` 固定 18081/5901/5801（删 kGatewayPortKey/kPortKey 常量与 `_vncPort` 缓存）。
- 设置页：`Root.plist` 删除 GatewayPort/TCP Port/HTTP Port 三个设置项；`TVNCRootListController` `applyChanges` 删端口校验、`saveGateway` 固定写 18081；`gen-managed-plist.sh` 不再生成三个端口项。
- configSchema：`TRCapabilityRegistry.mm` 删除 `Port`/`HttpPort`/`GatewayPort` 三项注册与默认值；`searchGateway` 固定写 18081。
- App 端：`TVNCServiceCoordinator` 删 HttpPort 默认写入与 GatewayPort env 注入；`TVNCConnectViewController` QR/直连 URL 固定 5801；`TVNCGatewayClient.gatewayPort` 固定 8080（删 TVNCConsolePort 读取）。
- 网关：`server/index.js` 默认 8080/18081/18181 保留 env 覆盖（仅部署/测试隔离用，FARM_PORT 等）。
- **验证**：`node --check` 通过；`npm test` 全绿（含新增 QUICK_CONFIG_GROUPS 两组/精选 6 项/不含端口项 + QUICK_ACTIONS 断言，无回归）。
- **待打包**：设备端/App 端改动需重新打包 IPA 生效；前端改动经 serveStatic no-cache + app.js v30 即时生效。

#### 2.3e 上报去除与前端自包含定义（2026-08-13）

> 用户决策："去除上报更合适——IPA 有的能力前端直接编写实现调用，复用右侧按键（KEY_DEFS）的『本地定义 + 直接发送』模式；注册与静态契约元数据表是过度设计。" 配置表单定义与当前值除外（配置面板必需）。

**① register 上报精简**
- register 仅上报 deviceId/name/vncPort/httpPort/screen/configs；capabilities/capMetadata/configSchema 不再上报
- 网关不再存储/返回能力与配置 schema（/caps 仅返回 configs）；query 命令通道（按需查询）保留
- 设备端：`TRGatewayClient.mm` `_registerData` 删三字段、删 `_capabilities` 方法（executor 与注册表查询方法保留）
- 网关：`server/index.js` registerDevice 停存三字段、/caps 瘦身、TCP 解析删三字段

**② 前端自包含定义（复用 KEY_DEFS 模式）**
- `caps.js` 全改为自包含定义数组 + 传输封装，无任何元数据查询机制：
  - `KEY_DEFS`（12 键，RFB 直发 ks/code/ptr）
  - `ACT_DEFS`（动作区 5 项）/ `QUICK_ACTIONS`（service.restart）/ `BATCH_CAPS`（批量 32 项含 category）
  - `CONFIG_DEFS`（37 项配置表单定义，与设备端 _registerConfigSchemas 对齐）+ `CONFIG_BY_KEY`
  - `configSchemaByReload()`（无参静态分区）；已删 `deviceCaps`/`menuCaps`/`ACTION_CAPS`/`DEFAULT_CAPS`/`CAP_FALLBACK`
- `app.js` 渲染点直接用定义数组（renderCapOps 动作区/批量/卡片菜单/配置面板），按键区按 KEY_DEFS 无条件渲染
- 点击屏幕控制与右侧按键走 RFB 直连，不依赖上报（已验证）

**③ 新增能力/参数的协作方式**
- IPA 添加能力 → 注册表注册 executor（invoke 通道不变）+ 前端定义数组加一条适配（两端约定对齐，不做运行时发现）

**④ 验证与待办**
- 验证：`node --check` 通过；`npm test` 全绿（7 套件：smoke/tunnel/register/dedupe/caps/pending-replay/press）
- 待打包：设备端改动（TRGatewayClient.mm）需重新打包 IPA 生效；前端 app.js v31 即时生效

#### 2.3f IPA 打包部署（2026-08-13，Actions #116 · 全绿含 release）

- **提交链**：`6ad2d82`（Security 私有符号 extern C）→ `92bd45b`（CFDataCreate 显式转换）→ `84f8db2`（UITrackingRunLoopCommonModes 字面量）→ `6752d75`（release job 显式 `permissions: contents: write`，Git Data API 推送到远程 `181a617`）。
- **release 403 根因修复**：此前多次打包 release job 恒失败（`softprops/action-gh-release` "GitHub release failed with status: 403"）——`build.yml` release job 未声明 `permissions`，GITHUB_TOKEN 默认只读（Contents/Metadata/Packages 全 read）无法创建 Release。本次在 release job 加 `permissions: contents: write` 后 **release job 首次成功**（此前 #97-#105 release job 均因权限失败，不影响 artifact；#116 起 tag 由 Actions 自动创建）。
- **构建结果**：`push release` 触发，**build 矩阵 4 方案（default/rootless/roothide/bootstrap）+ release job 全部 success**（run 31694856913 / #116）。
- **产物**：`packages-bootstrap` artifact（id 9178954922）→ `dist/TrollVNC_0.0.1.tipa`（2,424,758 字节，覆盖 #105 包）。下载走 GCM token Bearer + curl（`Invoke-WebRequest` 大文件连接失败，curl 正常）。
- **本包含**：register 上报去除（仅 deviceId/name/vncPort/httpPort/screen/configs）+ 前端自包含定义落地（caps.js KEY_DEFS/ACT_DEFS/QUICK_ACTIONS/BATCH_CAPS/CONFIG_DEFS，无元数据查询）+ 端口全固定 + 卡片 ⋯ 菜单（参数快捷 6 项 + service.restart）+ 此前全部累积改动（屏幕变化 hash 门控 / 容器化 / ack 驱动重建 / XK_Home / WiFi FAB 等）。
- **待真机安装验证**：5801 出画面、App 三 Tab、卡片墙、批量操作、卡片 ⋯ 菜单 6 项参数保存、service.restart 重启；能力清单精简（BATCH_CAPS 32 / CONFIG_DEFS 37）按用户指示**实际安装后再定**。

#### 2.3g 电脑端卡片无画面根因修复（2026-08-13，待打包）

- **症状**：用户安装 #116 后，电脑端网关控制台卡片墙无画面（卡片显示"获取失败"/加载中）。
- **诊断链路**：
  1. 设备 `0DB353D0`（iPhone）已在线（新格式上报，无 capabilities）→ 非注册问题；
  2. `invoke screen.hash` **5/5 ack timeout**，`invoke clients.count` 同样超时；而 `screenshot`/`clipboard.get`/`service.state`（TRCapRouteNative 直接执行）全部成功 → 问题锁定在**走 `_rfbCommand`（TRCapabilityRegistry → 127.0.0.1:5901 管理连接）的能力**；
  3. 从 PC 直接探测 `10.0.0.125:5901`：TCP 握手完全正常（version→security→ServerInit 1172x2536），但发送 `cap.hello`（type 0x50）后**服务端无任何响应**（recv 超时）→ 5901 RFB 扩展消息通道失效。
- **根因**：`trollvncserver.mm` `tvExtReadMessage` 用 `rfbReadExact(cl, &hdr, sizeof(TVExtHeader))` **读 8 字节帧头（type+reserved+len）**，但 **libvncserver 在 `rfbProcessClientMessage` 中已先消费 1 字节 type**（存入 `message->type`），socket 上只剩 7 字节帧头（reserved+len）+ payload → **帧头错位 1 字节**，payloadLen 解析成巨大值（如 0x002E7B=11899）→ `rfbReadExact` 读 payload 永久阻塞 → 服务端永不回包 → 客户端 3s/5s 超时。该机制 8-12 才从 46752 文本命令迁到 5901 扩展（commit 5b8a473/30bd224），**从未真机验证过**（#97-#105 时代 46752 通道不经过 libvncserver 扩展）。
- **修复**：`tvExtReadMessage` 改为只读 **7 字节帧头**（3 reserved + 4 payloadLen），type 已由 libvncserver 消费（`message->type` 校验仍在 tvExtHandleMessage 入口）。commit `38c5ae8` → 推送 fork/release → 构建验证（bootstrap 编译 + 真机重装后需复测 screen.hash）。
- **影响面**：卡片墙 hash 门控（screen.hash → 变化才 screenshot）、clients.* 本地命令、screen.diff/waitStable 全部恢复；Native 能力不受影响（本就正常）。
- **注意**：`TVExtHeader` 结构体仍被 `tvExtWriteResponse` 使用（服务端→客户端写响应，写的是完整 8 字节头，客户端 `_rfbCommand` 按 8 字节读——方向相反不受影响）。

#### 2.3h 图标苹果化（2026-08-13，已完成）

- 右侧按键区 12 键（home/power/volup/voldn/mute/briup/bridn/keyboard/spotlight/snapshot/hwlock/releasekeys）改为**苹果 SF Symbols 风格内联 SVG**（`stroke=currentColor` 跟随主题色，`viewBox="0 0 24 24"`），渲染处 `(k.svg || escapeHtml(k.icon || '?'))`。
- 操作列按钮（同步/返回/适配/全屏/断开/退出）+ 移动端 opsMenu 同步改造。
- 修复 `updateSyncBtn` 用 `textContent = '🔗'` 清掉 SVG 的问题（改为仅切换 `sync-active` class）。

#### 2.3i 能力精简 + type.paste 跨设备粘贴（2026-08-13，已完成，纯前端无 IPA 改动）

- **背景**：用户审视 8 项能力（键盘锁/释放按键/文本输入/粘贴输入/获取剪贴板/设置剪贴板/屏幕快照）实用性。结论：`type.text`（屏幕直接打字，仅 ASCII 无场景）删；`clipboard.set`（并入 type.paste 内部流程）删；`screenshot` 按钮删（能力保留供卡片墙内部拉图）；`hwlock`/`releasekeys` 保留降级；`clipboard.get` 保留（远程读设备剪贴板窄场景）。
- **`ACT_DEFS` 精简为 2 项**：`type.paste`（粘贴输入，param: text）+ `clipboard.get`（获取剪贴板）。批量菜单 `BATCH_CAPS` 本就不含这些项，无需同步。
- **跨设备粘贴链路（用户确认场景"手机粘贴到被控手机"）**：控制端浏览器弹窗捕获**控制端**剪贴板（`paste` 事件 + `clipboardData.getData('text')`，http 明文下唯一可行，`navigator.clipboard.readText` 仅 HTTPS/localhost）→ `type.paste` 能力 → 设备端写剪贴板 + 模拟 Cmd+V 注入被控设备。
- **`promptParams(paramDefs, title)` 升级**：新增 title 参数（表单顶部显示能力名）、string 输入框**自动聚焦 + paste 事件捕获**（桌面 Ctrl+V / 移动端长按粘贴自动填入）、修复 `const firstInput = null` 恒真 bug（改 `let focused = false` 条件聚焦）。`doInvoke`/`doBatchInvoke` 传入 `meta.title`。
- 版本号 app.js v34→v35，`node --check` 全过，无 IPA 改动无需重打包。

#### 2.3j 屏幕手势跨设备粘贴（2026-08-13，已完成；server/index.js 改动需重启网关生效）

- **需求确认**：用户指出"操作被控屏幕时，识别到长按应获取控制端剪贴板；电脑端在被控屏幕执行 Ctrl+V 也应获取控制端剪贴板"。即：在 focus 大屏/直控卡片上**直接手势触发**跨设备粘贴，而非先点「粘贴输入」按钮再弹窗。
- **交互决策（AskUserQuestion 确认）**：①移动端长按 = 粘贴（放弃长按=右键，用户选"长按=粘贴即可"）；②电脑端 Ctrl+V = 拦截 + **尝试一次性读剪贴板**（`navigator.clipboard.readText()` HTTPS / `document.execCommand('paste')` Chrome 桌面），失败再提示手动粘贴。
- **实现（4 文件 + patch）**：
  - `server/index.js`：patch noVNC `gesturehandler.js` 的 `_longpressTimeout()`——长按不再产生右键，改 dispatch **`farmlongpress`** 自定义事件（detail 含坐标），该触摸进入 `_ignored` 列表（touchend 不再产生任何鼠标/手势事件）。已用临时脚本验证 patch 字符串精确命中（index 12363）。
  - `web/app.js`：新增跨设备粘贴条模块（`openPasteBar`/`closePasteBar`/`submitPaste`/`tryAutoReadPaste`/`bindLongPressPaste`）：
    - 移动端：focus 大屏 `stage` / 直控卡片 `tv` 监听 `farmlongpress` → 弹粘贴条（textarea 自动聚焦）→ 用户长按输入框弹系统粘贴菜单 → `paste` 事件捕获文本 → `invokeCap('type.paste', {text})` 注入被控设备（写剪贴板 + 模拟 Cmd+V）。
    - 电脑端：**capture 阶段** `document` keydown 拦截 Ctrl+V（focus 大屏激活、焦点不在页面输入框时）→ `preventDefault + stopPropagation`（阻断 noVNC canvas 键盘监听收到该键）→ 弹粘贴条 → 按序尝试自动读剪贴板 → 失败提示"再按一次 Ctrl+V 或长按输入框"。
    - `exitFocus`/`exitDirectMode` 关闭粘贴条；`bindLongPressPaste` 用 `dataset.pasteBound` 防重复绑定（直控卡片复用）。
  - `web/index.html`：底部浮动粘贴条 DOM（hint + textarea + 取消/粘贴按钮）；app.js 版本 v35→v36。
  - `web/style.css`：`.paste-bar` 系列样式（固定底部居中、移动端 ≤480px 换行铺满）。
- **技术要点**：noVNC `Keyboard` 监听挂在 **canvas**（`new Keyboard(this._canvas)`），粘贴条 textarea 是独立元素——聚焦期间按键不会发给设备，无需 ungrab；http 明文下移动端系统粘贴菜单（textarea 长按）是唯一可靠的剪贴板读取途径。
- **验证**：`node --check`（app.js/caps.js/server/index.js）全过；patch 命中验证通过。**server/index.js 改动需重启网关进程生效**（web 静态文件 no-cache 即时生效）。
- **线上问题修复（2026-08-13 二次）**：用户真机反馈"操作被控手机长按未触发剪贴板获取"。经 CDP 触摸自动化复现（`test/verify-longpress-paste.mjs`）定位根因——patch 的 `dispatchEvent(new CustomEvent('farmlongpress', {...}))` **默认 `bubbles:false`**，事件派发到 canvas 后不冒泡，监听在外层 `stage`/卡片 `.tv` 的 app.js 收不到（手动派发到 stage 本身可触发，故首次验证误判为通过）。修复：`CustomEvent` 加 **`bubbles:true`**（server/index.js +2 行），重启网关后回归全绿：长按 → farmlongpress 触发（lpCount=1）→ 跨设备粘贴条弹出。回归脚本 `test/verify-longpress-paste.mjs` 保留：进入 focus 大屏 → CDP 触摸长按 1300ms（> noVNC 1000ms 阈值）→ 断言 farmlongpress 冒泡计数 + 粘贴条可见。调试日志已清理。
- **【2026-08-13 三次：已回退】** 用户判定本实现"不成熟"并指出概念问题：被控设备输入框长按弹出的应是 **IPA 剪贴板**（`type.paste` 写入的内容），而手势劫持（控制端长按/Ctrl+V → 读控制端剪贴板）与"被控设备输入框长按 = 被控设备剪贴板"的语义冲突。已完整回退：① server/index.js 移除 farmlongpress patch（恢复仅 passive:false）；② web/app.js 删除粘贴条模块（openPasteBar/closePasteBar/submitPaste/tryAutoReadPaste/bindLongPressPaste）+ Ctrl+V capture 拦截 + 4 处绑定调用；③ web/index.html 删除粘贴条 DOM（版本 v36→v37）；④ web/style.css 删除 .paste-bar 样式；⑤ 删除回归脚本 verify-longpress-paste.mjs。**保留**：`type.paste`/`clipboard.get` 的 ACT_DEFS 按钮 + `promptParams` 升级（title + 自动聚焦 + paste 捕获，粘贴输入按钮的成熟功能）。网关已重启，验证 gesturehandler 返回无 farmlongpress、passive:false 保留。

#### 2.3k 输入/剪贴板四能力研究（2026-08-13）

用户实测：点击「粘贴输入」输入文字后，被控手机弹窗"xxx 想从 superphone 粘贴，你允许这样做吗"——**时序完美对齐"向手机输入框粘贴"**。这正是 iOS 14+ 对 UIPasteboard 读取的隐私确认弹窗，证明 `type.paste` 链路（写剪贴板 → Cmd+V → iOS 确认 → 粘贴）真实可用。四能力设备端实现（TRCapabilityRegistry.mm + ClipboardManager.mm）：

| 能力 | 路由 | 实现 | 支持字符 | 写设备剪贴板 | 模拟粘贴 | 系统弹窗 | 场景 |
|---|---|---|---|---|---|---|---|
| `type.text` 文本输入 | Touch(HID 键盘) | 逐字符 `keyPress` | **仅 ASCII**（`c<128` 才发，中文/emoji 跳过） | 否 | 否（逐字符敲键） | 无 | 向输入框输英文/数字/符号；无焦点要求时用 |
| `type.paste` 粘贴输入 | Touch(HID) | `setStringFromRemote` 写剪贴板 + 模拟 Cmd+V | **任意**（走剪贴板） | **是** | **是**（Cmd+V） | **会**（"想从 superphone 粘贴"） | 向**当前焦点输入框**粘贴任意文本（中英/emoji），用户确认弹窗后完成 |
| `clipboard.set` 设置剪贴板 | Native | `setStringFromRemote`（仅写不粘贴） | 任意 | **是** | 否 | 无 | 预设设备剪贴板内容，供设备用户自行系统粘贴/复用 |
| `clipboard.get` 获取剪贴板 | Native | `UIPasteboard.generalPasteboard.string` | 读取 | — | — | 可能（读剪贴板隐私提示） | 把被控设备当前剪贴板文本取回控制端查看/复用 |

**关键结论**：
1. `type.text` 与 `type.paste` 的区别 = 逐字符敲键（仅 ASCII）vs 写剪贴板+Cmd+V（任意字符）；`type.paste` 是 `clipboard.set` + 模拟粘贴的组合。
2. 被控设备输入框真正粘贴进的内容来自**被控设备剪贴板**（IPA 写入）——印证"被控设备输入框长按/粘贴 = IPA 传输的剪贴板"。
3. iOS 粘贴确认弹窗是**系统行为**（UIPasteboard 读取隐私），控制端无法绕过，属正常体验。
4. `clipboard.get` 用于"被控设备 → 控制端"方向；`type.paste`/`clipboard.set` 用于"控制端 → 被控设备"方向；`type.text` 仅 ASCII 场景极窄（屏幕直接打字更自然），**建议保持不占按钮**（设备端实现保留）。

#### 2.3l 统一浮层粘贴输入 v2（2026-08-13，代码已落地）

用户在 §2.3j 回退后重新收敛方案：**三端统一走"就地浮层粘贴"**——电脑端 Ctrl+V / 手机端长按被控屏幕 → 就地浮层（自动聚焦 input）→ 用户粘贴（Chromium 下尝试 `execCommand('paste')` 自动触发一次直达，失败则再按一次 Ctrl+V 或点系统粘贴按钮）→ paste 事件捕获控制端剪贴板文本 → `type.paste` 注入被控设备 → 自动关闭浮层。

落地文件（trollvnc-farm）：
- `server/index.js`：gesturehandler.js patch 重新启用——`_longpressTimeout` 派发 `farmlongpress`（**bubbles:true**，否则不冒泡到外层监听，此前回退 bug 根因）+ 触摸进 `_ignored` + `_state=GH_NOGESTURE`（不发右键）；**rfb.js 的 gesturehandler import 加 `?v=2` 版本参数**（ES module 内部 import 无版本号 → 浏览器尤其 iOS WKWebView 缓存旧版 → patch 不生效 → 手机端长按仍=右键返回，此为本次"长按触发返回"根因）；gesturehandler.js 响应头 `Cache-Control: no-store`。
- `web/app.js`：新增 `ensurePasteOverlay/showPasteOverlay/closePasteOverlay/submitPasteText`（就地浮层惰性注入 + 定位 + 聚焦 + paste 捕获 + `invokeCap type.paste` + 焦点归还 noVNC 画布）；document 级 `keydown` capture 拦截 Ctrl+V（focus 模式，`preventDefault+stopPropagation` 阻止 noVNC 把 Ctrl+V 发到设备，改为唤起浮层）；document 级 `farmlongpress` 监听（聚焦视图 → focus 设备；直控卡片 → 该卡片设备）。
- `web/index.html`：app.js `v=37→v=38`。
- `web/style.css`：`.paste-overlay` 就地浮层样式（fixed 定位、深色玻璃卡片、输入框聚焦即弹系统粘贴）。
- `test/caps-test.js`：修复过期断言（ACT_DEFS 精简为 2 项后测试未同步，`5 项`→`2 项` + id 列表同步），npm test 全绿。

**技术边界（用户已确认）**：
1. http 明文下浏览器禁止网页主动读剪贴板，唯一通道是"可编辑元素聚焦 + 用户真实粘贴"的 paste 事件；canvas 非可编辑元素无法捕获粘贴 → 必须经就地浮层中转。
2. 电脑端"两次 Ctrl+V"（第一次唤浮层、第二次粘贴）是 http 下的可靠路径；上 HTTPS 后可用 `navigator.clipboard.readText()` 一次直达（未实现，留待可选增强）。
3. 手机端长按被控屏幕 → 就地浮层聚焦 → iOS 弹系统粘贴按钮/键盘粘贴胶囊 → 点击 → 捕获 → 注入，与 §2.3j 的"浮层内再粘贴一次"一致。
4. 被控设备 OSK 弹了不拦截（控制端无法强制隐藏，注入不依赖它）；iOS 16+ 被控端粘贴有系统确认弹窗，15+ 无感（用户部署以 15+ 为主）。

#### 2.3m https 无感粘贴输入 v3（2026-08-14，改造中）

> 用户在 §2.3l 浮层方案实测后判定"弹窗 + 二次按键"严重偏离"无感粘贴"设计（电脑端 Ctrl+V 应一次直达、零弹窗；手机端长按应直连注入）。根因 = **控制台为 http 明文**：浏览器禁止网页主动读剪贴板（`navigator.clipboard.readText` 仅安全上下文可用），唯一通道是"可编辑元素聚焦 + 用户真实粘贴"的 paste 事件 → 必须浮层中转。用户拍板：**改 https（自签证书）治本**，https 下可在用户手势内 `navigator.clipboard.readText()` 一次直达。

**改造方案（网关 web 服务 8080 启用 https + 前端 readText 直达，保留浮层兜底）**：

| 端 | 改造项 | 状态 |
|---|---|---|
| 网关 | 新增 `scripts/gen-cert.mjs`：openssl 探测（where.exe + Git 常见路径）+ 自动生成自签证书（RSA2048 / 3650 天 / SAN 含本机全部 IPv4 + localhost），输出 `data/cert/cert.pem`+`key.pem` | ✅ 已验证（生成 1176/1704B，SAN=127.0.0.1+本机 IP） |
| 网关 | `server/index.js`：启动加载 TLS（缺失自动调生成脚本）；`http.createServer` → `https.createServer`；`FARM_TLS=0` 显式回退 http；WebSocketServer 挂同一 server 自动变 wss | ✅ 已验证（https://0.0.0.0:9443 启动 + curl -k /api/state 正常） |
| 测试 | 5 个 spawn 测试文件 env 加 `FARM_TLS:'0'`（smoke/tunnel/register/dedupe/pending-replay）保持 http 测试 | ✅ npm test 7 套件全绿 |
| 前端 | 电脑端 Ctrl+V：capture 拦截（现有）→ https 下 `await navigator.clipboard.readText()` 直接注入（keydown 原生手势）→ **零浮层一次直达**；失败回退浮层 | ✅ app.js v40 |
| 前端 | 手机端长按：gesturehandler patch 移入**原生 touchend**（`_touchEnd` ignored 分支，用户手势激活）→ `window.__farmPasteLongPress` → https 下 `readText()` **长按松手一次直达**（零浮层）；iOS 若拒绝/权限未授 → 自动兜底浮层「点击粘贴」 | ✅ app.js v41 + gesturehandler ?v=3（farmlongpress 已移除） |
| 前端 | 浮层保留作 readText 不可用（http/旧浏览器/权限拒）时的兜底（tap 失败自动切 input） | ✅ |
| App | `TVNCGatewayClient.m`：`apiURLWithPath` 改 https + 新增 `tlsTrustingSession`（NSURLSessionDelegate challenge 信任自签）；连接页 API 同步走 https | ✅ 已打包 #118 |
| App | `TVNCConsoleWebViewController.m`：URL `http://` → `https://` + `didReceiveAuthenticationChallenge` 信任自签 serverTrust；`Info.plist` ATS 升 `NSAllowsArbitraryLoads` | ✅ 已打包 #118 |

**网关重启 + 验证（2026-08-14，已生效）**：杀掉旧网关（PID 32732，旧代码 http 明文）→ `npm start` 新网关 → 日志确认 `https://0.0.0.0:8080 (self-signed cert)` + 同端口明文自动跳转；真实设备 `iPhone (0DB353D0)` @10.0.0.125 自动注册 + 隧道建立。验证项：① `curl -k https://127.0.0.1:8080/api/state` → 正常返回 JSON；② `curl -si http://127.0.0.1:8080/` → `301 Location: https://127.0.0.1:8080/`；③ gesturehandler.js 含 `__farmPasteLongPress`/`_farmLongPress`/`passive:false` 且 **`farmlongpress` 事件名零残留**（仅保留 _pushEvent 原生手势派发 1 处 dispatchEvent）；④ rfb.js import `gesturehandler.js?v=3`；⑤ app.js v41 含 `navigator.clipboard.readText`。真机长按动作（长按→原生 touchend→readText→type.paste 注入）需实测确认 iOS 手势激活。

**IPA 打包（2026-08-14，Actions #118 · 全绿含 release）**：提交 `e8cc2a6`（内容=本地 main `64b0e06`，https 适配 4 文件：TVNCGatewayClient.m / TVNCConsoleWebViewController.h/.m / Info.plist）→ build 矩阵 4 方案 + release job 全部 success → 产物 `packages-bootstrap`（artifact 9190153080）→ `dist/TrollVNC_0.0.1.tipa`（2,425,657B，已验证含 NSAllowsArbitraryLoads=True / bundle com.82flex.TrollVNCApp / minOS 14.0）。
> **网络绕行备忘**：本次 `github.com` 域名被当前网络阻断（api.github.com 正常），git fetch/push 均 TLS 失败，改用 **Git Data API** 完成推送——上传 4 个新 blob → `base_tree`(远端 release `9ea8d089`，内容≈本地 38c5ae8) + 4 条目构造新 tree（校验 `==` 本地 HEAD tree，`501af447` match=true）→ 创建 commit（parent=9ea8d089，fast-forward 无 force）→ PATCH `refs/heads/release`。远端 release/main 现为 CI 重建线（9ea8d089 系），本地 main 为独立线，网络恢复后需 fetch 远端保持同步。
| 设备端 5801 直连页 | 保持 http 不动（单设备直连场景，浮层 input 模式仍可用） | — |

**部署操作顺序**：
1. 重启网关（`cd trollvnc-farm && npm start`）→ 首次启动自动生成 `data/cert/` 自签证书，监听 https://:8080；
2. **浏览器直接输 `IP:8080` 或 `http://IP:8080` 即可自动 301 跳转 https**（同端口协议自适应：TLS ClientHello→https；明文→301，已验证 https/301/wss 三链路）——首次访问点「高级→继续前往」信任自签（或把 cert.pem 导入系统受信任根，一次到位）；
3. 电脑端 Ctrl+V 一次直达（无浮层）；手机端长按被控屏幕松手即注入（readText 失败自动兜底浮层）；
4. App 容器需重打包（`git push fork/release` 触发 Actions）：控制 Tab 自动加载 https + 自签信任，连接页 API 同步 https；
5. 回退：`FARM_TLS=0 npm start` 回 http（无跳转），浮层 input 模式兜底可用。

#### 2.3n 剪贴板双向同步（方案 B）+ Ctrl+V 可靠性修复（2026-08-14，前端已生效 / 设备端待打包）

> **背景**：2.3m 后用户实测 Ctrl+V"文字到剪贴板但不写入"，且 Ctrl+V 反复触发画面断开（1005）。深入排查（含网关字节流日志）：**前端 Ctrl+V 拦截只吞 V 键 keydown，Ctrl 键本身仍被 noVNC 注入设备** → 残留 LeftControl 修饰键把 type.paste 模拟的 Cmd+V 变成 Ctrl+Command+V 组合被 iOS 拒绝（写剪贴板成功=setStringFromRemote 可靠，粘贴失败=组合键被拒）。同时用户确认架构方向 **方案 B**：剪贴板同步（复制即同步）为基座，Ctrl+V 基于已同步剪贴板只触发粘贴动作，不再重复传文本；与"粘贴输入"解耦（同步=运弹药，粘贴=扣扳机）。

**最终架构（2026-08-14 用户拍板：剪贴板同步统一走协议通道，粘贴输入走能力原子通道）**：
```
剪贴板同步（控制端复制 → 设备剪贴板）＝ 协议通道：copy事件 → readClipboardText()
  → 遍历已连接会话（focus.rfb + directRfbs，_farmConnected）→ rfb.clipboardPasteFrom(text)
  → noVNC ExtendedClipboard Notify/Request/Provide（UTF-8 + deflate）→ 设备 setXCutTextUTF8 写剪贴板
  → 无任何连接会话时静默（不同步、不提示）；受设备 ClipboardEnabled 门控
剪贴板同步（设备复制 → 控制端剪贴板）＝ 协议通道：设备 ClipboardManager.onChange → Notify/Request/Provide
  → noVNC 'clipboard' 事件 → 前端 writeText 控制端剪贴板 + toast（最后变化者胜，1s 节流）
粘贴输入（Ctrl+V / 手机长按）＝ 能力原子通道：readClipboardText() → type.paste({text})
  → 设备端 setStringFromRemote + releaseEveryKeys + 模拟 Cmd+V（原子，避免协议异步无确认的"粘旧内容"竞态）
```

| 端 | 改造项 | 状态 |
|---|---|---|
| 设备端 | `type.paste` executor：`text` 参数改可选（`required:NO`）——有 text 先写剪贴板，**无 text 跳过写剪贴板仅触发粘贴动作**；模拟 Cmd+V 前先 `[hid releaseEveryKeys]` 清残留修饰键（修"Ctrl 残留 → Ctrl+Command+V 被 iOS 拒"根因） | ✅ 代码已改，**需重打 IPA** |
| 设备端 | `setupRfbCutTextHandlers`：`gScreen->enableExtendedClipboard = TRUE`（libvncserver 协商 Extended Clipboard caps 伪编码 0xC0A1E5CE，noVNC 检测后走 UTF-8 双向；`setXCutTextUTF8` 回调承接 Provide 解压数据）；开关关闭时 `= FALSE` | ✅ 代码已改，**需重打 IPA** |
| 设备端 | **移除 Latin-1 降级**（2026-08-14 用户决策）：`sendClipboardToClients` 删除 latin1 双轨与 `else if (latin1)` 兜底，只发 UTF-8（`rfbSendServerCutTextUTF8(..., NULL, 0)`）；`setXCutTextUTF8` 删除 UTF-8 解码失败后的 Latin-1 fallback（失败直接丢弃 + 日志） | ✅ 代码已改，**需重打 IPA** |
| 前端 | copy 同步改协议通道：`readClipboardText()` → 遍历 focus.rfb + directRfbs（`_farmConnected` 且 `ClipboardEnabled` 门控）→ `clipboardPasteFrom(text)`；**无会话静默**（移除 `syncClipboardToDevices` 能力群发与 toast） | ✅ app.js v46 即时生效 |
| 前端 | Ctrl+V 改原子：`readClipboardText()` → `submitPasteText`（`type.paste({text})` 带 text 一步同步+粘贴）；移除 clipboard.set 同步校验 | ✅ app.js v46 |
| 前端 | 手机长按（`__farmPasteLongPress`）：同 Ctrl+V → `submitPasteText`（原子）；http 明文仍浮层 input 兜底 | ✅ app.js v46 |
| 前端 | `submitPasteText`（浮层提交路径）保留：走 `type.paste({text})`（原子，兼容浮层） | ✅ |
| 前端 | **设备 → 控制端方向**：`createRfb` 监听 RFB `clipboard` 事件（设备剪贴板变化 → ClientCutText → 网关 FT_DATA 广播 wsSet → noVNC 触发；noVNC 内部过滤 viewOnly，控制/直控会话生效）→ 写控制端剪贴板（最后变化者胜）+ toast 标注来源设备，1s 节流 | ✅ app.js v45/v46 |
| 前端 | **移除手机端长按注入**（2026-08-14 用户决策）：剪贴板同步走"复制即同步/点卡片同步"，被控端系统粘贴为主路径；gesturehandler patch 保留 ignored 逻辑，长按被控画面无操作；`window.__farmPasteLongPress` 不再定义 | ✅ app.js v47 即时生效 |
| 前端 | **点卡片同步剪贴板**：`enterFocus`（点卡片=用户手势）内 `readClipboardText()` → 与已消费/待同步去重后记 `farmPendingClip` → RFB connect 就绪后 `clipboardPasteFrom` 写入设备（覆盖"App 外复制 → 切回控制台 → 点卡片"场景）；`copy` 事件监听保留（页面内复制即同步，已同步则更新 `farmLastClipText` 去重） | ✅ app.js v47 即时生效 |
| 前端 | **移除全部自动降级**（2026-08-14 用户决策）：① 删除就地粘贴浮层（`ensurePasteOverlay`/`showPasteOverlay`/`closePasteOverlay`/tap+input 双模式/execCommand 兜底）；② `readClipboardText` 改抛错（非 https/API 不可用/读取失败不再返回 null 静默）——Ctrl+V 明确报错 toast、copy/点卡片静默跳过（不阻断主操作，非降级路径）；③ `submitPasteText` 移除 closePasteOverlay 调用 | ✅ app.js v48 即时生效 |
| 前端 | **协议层降级（#6）处理**：所有设备统一安装新版 IPA（`enableExtendedClipboard`），**不做旧包兼容/检测/提示**（2026-08-14 用户决策）——copy 同步直接 `clipboardPasteFrom`；点卡片 pending 同步保留 500ms 延迟（caps 随画面帧到达的协议时序，非兼容措施），删除支持检测函数与"需更新设备端"提示 | ✅ app.js v50 即时生效 |
| 前端 | **控制台右侧动作区按钮移除**（2026-08-14 用户决策）：`ACT_DEFS` 清空为 `[]`——`type.paste`（粘贴输入）已被电脑 Ctrl+V 原子链路（`readClipboardText` → `type.paste({text})`）+ 手机端被控端系统粘贴覆盖；`clipboard.get`（获取剪贴板）已被设备→控制端 RFB clipboard 事件自动同步覆盖。**能力 executor 设备端保留**（Ctrl+V 依赖 `type.paste`）；`renderCapOps` 因 `ACT_DEFS.length === 0` 自动不渲染动作区分组，`doInvoke`/`promptParams` 仍被批量「调用能力」链路使用不受影响。`caps-test.js` 断言同步更新 | ✅ caps.js + index.html v51 即时生效 |

**关键决策记录**：① Ctrl+V/长按的"同步校验+粘贴"因协议通道**异步无确认**（clipboardPasteFrom 只发 Notify，前端拿不到设备写完时刻）改用 `type.paste({text})` 原子完成，避免竞态粘旧内容；② copy 同步范围 = 聚焦 + 直控全部；③ 无会话时 copy 静默（用户确认不提示）；④ `clipboard.set`/`clipboard.get` 能力保留（设备端注册 + 窄场景可用），前端同步链路不再调用；⑤ **手机端长按注入移除**：用户确认被控设备可实际接触、剪贴板同步后可直接在被控端系统粘贴，长按被控画面改无操作；点卡片（手势）读剪贴板待连接同步补齐"App 外复制→切回→点卡片"链路；⑥ Web 平台限制（iOS 13.4+ readText 需手势激活，`visibilitychange`/`pageshow` 非手势）→ 切回网页不能无点击同步，只能点卡片/长按等手势时读取；无点击同步需 IPA 原生层监听 UIPasteboard（后续可选增强）。

**技术边界（用户已确认方向）**：
1. https 下 `readText()` 必须处于**原生用户手势**激活内：电脑端 keydown 直达；手机端长按由 gesturehandler 在**原生 touchend**（`_touchEnd` ignored 分支）触发 `window.__farmPasteLongPress`，**长按松手即注入**（farmlongpress 为 JS 派发事件无激活，已废弃）；若 iOS 对 touchend 手势判定拒绝 readText（各版本行为差异），自动兜底弹浮层「点击粘贴」（click 激活最稳）。
2. 自签证书需各端一次性信任：电脑 Chrome/Edge 警告页「高级→继续前往」（或导入受信任根）；手机 Safari 类似；App 容器由原生 challenge 处理（无需用户操作）。
3. 证书 SAN 含本机全部 IPv4，内网 IP 直访仍出证书警告（自签非受信 CA，正常现象，继续即可）。
4. `FARM_TLS=0` 回退 http 后浮层 input 模式兜底仍可用，功能不失效，仅体验回落。

#### 2.3o 时序审查修复（2026-08-14，前端+网关即时生效 / 设备端待打包）

> **背景**：对设备注册到网关后的完整时序链路进行系统审查，发现 8 处问题。

| 端 | 修复项 | 状态 |
|---|---|---|
| 前端 | **syncRfbs 会话泄漏修复**：`exitSyncMode` 和 `toggleSync` 取消分支补 `closeRfb(syncRfbs.get(id))`——syncRfb 的 RFB 实例存在 syncRfbs Map 中（未赋给 inst.rfb），`stopWallRfb` 不会关闭它 → noVNC 不监听 container DOM 变更 → WS 残留为孤儿会话 → 占用 wsSet 槽位 → 影响 isFirstSession 判断 → 后续会话不触发 rfb.start 重建 → 黑屏。与 `exitDirectMode` 已有的 closeRfb 逻辑对齐 | ✅ app.js v52 即时生效 |
| 网关 | **wsSet 死会话清理修复**：`handleVncSocket` 多会话清理循环对 `readyState !== OPEN` 的 ws 从 `continue` 跳过改为 `delete + continue`——已关闭 ws 不从 wsSet 剔除导致 size 虚高 → isFirstSession 误判为 false → viewOnly 会话不触发 rfb.start 重建 → 黑屏 | ✅ index.js 即时生效 |
| 网关 | **restart 超时统一**：单台 8s → 15s、批量 8s → 15s——设备端重启到 VNC server 就绪可能 >8s，统一为 15s 与 sendDeviceCmd 默认 restart 超时一致 | ✅ index.js 即时生效 |
| 设备端 | **移除 type.text 注册**：HID keyPress 仅支持 ASCII（`c < 128`），中文/emoji 静默丢弃；前端 ACT_DEFS/BATCH_CAPS 均无调用入口，type.paste 是完整替代方案（支持中文/emoji） | ✅ 代码已改，**需重打 IPA** |
| 设备端 | **移除 clipboard.set 注册**：前端剪贴板同步已统一走 RFB 协议通道（clipboardPasteFrom → Extended Clipboard UTF-8），不再走能力通道；type.paste executor 内部已包含 setStringFromRemote 调用 | ✅ 代码已改，**需重打 IPA** |
| 设备端 | **gateway reload 机制已验证**：`NSUserDefaultsDidChangeNotification` → `_defaultsChanged` → `_needsReregister=YES` → worker 线程 select 超时（最多 5s）重发 register（含最新 configs）。机制完整，无需修复 | ✅ 已确认 |
| 记忆 | **项目记忆更新**：46752 端口 → 5901 RFB 扩展消息（0x50/0x80）；clipboard.set 已移除 | ✅ 已更新 |
| 配置 | **Root.plist 端口项确认**：未暴露 Port/HttpPort（仅有 BindHost 可调），端口完全由代码硬编码管理，无用户困惑风险 | ✅ 已确认 |

#### 2.3p 光标体验优化（2026-08-14，前端+网关即时生效）

> **背景**：ServerCursor 关闭时（默认 NO），noVNC 用 `RFB.cursors.dot` 兜底显示位置指示——原生 dot 是 **3×3 十字**（白底黑边中间透明，rfb.js L3400-3410），渲染到屏幕上呈 ✖️ 状。用户要求改为真正圆点，并确认触控/空闲自动隐藏与桌面端鼠标行为。

| 端 | 改造项 | 状态 |
|---|---|---|
| 网关 | **dot 光标 patch**：server/index.js 内存 patch 追加替换 `RFB.cursors.dot` 为 **7×7 圆点**（白色填充 + 黑色描边，热区 hotx/hoty = 中心 3,3，196 像素 RGBA）；验证脚本 `test/verify-dot-cursor.mjs`（正则匹配 + 替换生效 + 像素数 + 圆点形状绘制，全部 PASS） | ✅ index.js patch 即时生效，网关已重启验证 |
| 前端 | **空闲光标自动隐藏**：app.js v54，**仅手机端（≤900px）非 viewOnly 会话**挂起 mousemove/mousedown/touchstart/touchmove/wheel 监听（capture + passive）→ `cursorPoke`（显示 + 重置 1.5s 定时器）→ `cursorHide`（`rfb._canvas.style.cursor='none'` + `rfb._cursor._hideCursor()`）；断开时清理定时器 | ✅ app.js v54 即时生效 |
| 网关 | **rfb.js 加载版本号 `?v=2`**（app.js v55）：rfb.js 原无版本号 → 浏览器缓存 patch 前旧内容（3×3 十字）仍显示 ✖️（no-cache 只在重新请求时生效，已加载旧 JS 不更新）；加 `?v=2` 强制浏览器重新拉取 patch 后内容（同 gesturehandler.js?v=3 方案），验证 `?v=2` 下 200 + patch 内容正确 | ✅ app.js v55 即时生效 |
| 设备端 | **5801 直连页 dot 光标同步**：layout 打包 noVNC（`webclients/novnc/core/rfb.js` L3238）dot 改 **7×7 圆点**（与网关 server patch 完全一致，196 像素验证通过）；index.vnc L237 `showDotCursor=true` 已启用该光标 | ✅ 代码已改，**需重打 IPA** |

**行为确认（2026-08-14 用户拍板）**：电脑端鼠标移入画布 → 系统箭头**替换**为圆点（noVNC CSS `cursor: url(dataURI)` 原生机制，`useFallback=false` 路径），圆点借系统鼠标位置零偏差跟随；移出画布恢复箭头；**电脑端光标即鼠标，空闲不隐藏（常显）**。手机端（≤900px，`useFallback=true` 触屏浮层路径）圆点为 canvas 浮层跟随触控点，**空闲 1.5s 自动隐藏**。**不做桌面端"箭头 + 浮层圆点"双光标方案**（视觉歧义 + 浮层渲染延迟，桌面浏览器走替换路径是标准 VNC 行为）。ServerCursor 开启且推送真实光标时，`_shouldShowDotCursor` 检测非透明像素自动让位显示远程光标。

**✖️ 根因排查结论（2026-08-14，已定性）**：用户反馈"浏览器仍显示 ✖️"。逐层排查：① 服务器 rfb.js 内容 curl 验证含 7×7 圆点 ✓；② `https://10.0.0.111:8080/`（网关局域网 IP）index v56/app.js import rfb.js?v=2/返回 7×7 ✓；③ 设备端 ServerCursor=false（网关 API configs 确认）✓；④ 前端无 crosshair 覆盖 ✓；⑤ **浏览器运行时诊断（app.js 临时加 `[farm-cursor]` console log）**：`dot=7x7 | showDot=true | serverCursorPx=0 | canvasCursor=url(data:image/png;base64,SUhEUgAAAAcAAAAH...) 3 3, default`——IHDR 解码宽高 **7×7**，即 8080 控制台 noVNC 光标**已是圆点**。⑥ 用户确认大屏上仍 ✖️ → **定性：✖️ 不是 noVNC 光标，而是设备画面内容自带的 iOS 系统光标（ScreenCapturer 截屏捕获包含系统光标覆盖层，随画面推流），叠加显示在 noVNC 圆点下方**。诊断代码已移除（app.js v58），dot 光标 patch 本身已成功，5801 页面 dot patch 已同步（待重打 IPA）。设备端画面内 iOS 系统光标的处理属后续可选（ScreenCapturer 是否可排除光标覆盖层，待评估）。

#### 2.3r 右侧操作区精简：移除键盘锁/释放按键/适配画面（2026-08-14，前端即时生效）

> **背景**：用户审查三个右侧按钮的真实能力后决策：`hwlock`（键盘锁/解锁，RFB keysym 0x1008ff81 → HID ACLock 硬件键盘锁，iOS 外接键盘窄场景）、`releasekeys`（释放按键，0x1008ff82 → releaseEveryKeys 防粘键手动兜底）、`fit`（适配画面，纯前端本地操作）——其中**适配本已自动**（createRfb 统一 `rfb.scaleViewport=true` 等比 contain），按钮冗余。

| 端 | 改造项 | 状态 |
|---|---|---|
| 前端 | **KEY_DEFS 移除 hwlock/releasekeys 两键**（12→10 键）：右侧按键区不再显示；设备端 executor（hardwareLock/releaseEveryKeys）与 keysym 处理保留（type.paste 内部仍依赖 releaseEveryKeys） | ✅ caps.js + app.js v59 即时生效 |
| 前端 | **移除「适配画面」按钮**（PC focusOps + 移动端 opsMenu 两处）：适配为自动行为（`rfb.scaleViewport=true` 默认），`doOp` 删除 `case 'fit'` | ✅ index.html + app.js v59 即时生效 |
| 前端 | **caps.js import 加 `?v=2`** 版本号（同 rfb.js?v=2 缓存方案）；caps-test.js 断言 KEY_DEFS 12→10，全部 PASS | ✅ 即时生效 |
| 保留 | **BATCH_CAPS 中 hwlock/hwunlock/releasekeys 保留**（批量「调用能力」入口，用户未要求移除） | — |

**追加（2026-08-14 用户拍板：PC+移动端统一）——移除返回类按钮**：审查确认「返回设备墙」（btnBack → exitFocus → closeRfb=rfb.disconnect()）与「断开」（disc → disconnect + exitFocus）**完全等价**（均断连+回墙+恢复缩略图；架构硬约束"同设备仅 1 活跃会话"，不存在"返回但不断连"的中间态）。改动：index.html 移除 PC `btnBack` + 移动端 `opsExit`（「退出控制」）；app.js 移除两者绑定，移动端「断开」按钮 title 更新为"断开（退出控制并返回设备墙）"作为唯一出口；style.css 过时注释更新。app.js v59→v60 即时生效。

**追加（2026-08-14）——同步/断开按钮图标苹果化（三轮迭代）**：第一轮：「同步」改为循环图标、「断开」改为断开的锁链。第二轮（用户反馈"同步不够具体，锁链断开不够好看"后 AskUserQuestion 拍板）：「同步」由循环箭头改为 **两台设备同步**、「断开」改为 **完整锁链 + 断裂线 + 红色**（lucide link-2-off 结构：两条完整链环弧 `M10 13a5 5...`/`M14 11a5 5...` + 中间断裂斜线 `M9.5 14.5l5-5`，PC 按钮 `class="op danger"`、移动端 `class="danger"` 红色渲染）。第三轮（用户反馈"一个大手机一个小手机更好看"）：「同步」改为 **左大（主控）右小（被控）手机 + 双向箭头**（大手机 `rect x=2 y=3.5 w=9.5 h=17 rx=2`、小手机 `rect x=14 y=7 w=8 h=13 rx=1.8`，中间填充双向箭头 `M12.1 10.3l1.8 1.4-1.8 1.4` 朝右 + `M13.9 10.3l-1.8 1.4 1.8 1.4` 朝左）——尺寸对比直观体现主从同步关系。风格沿用 24×24 / stroke-width 1.8 / 圆角苹果风。验证：`node test/verify-icons.mjs` 全绿——SVG 配对 7/7、大+小手机+双向箭头命中、断裂线 2 处（PC+移动端）、danger 红色类 2 处、旧图标（等宽双机/循环箭头/broadcast/插头/旧断链/方块）全部清除。index.html 直接生效（no-cache 刷新即新）。

**追加（2026-08-14）——5801 直连页能力同步评估（仅分析，未执行）**：审查设备端 5801 直连页（webclients/index.vnc）与网关控制台差异，结论：
- **未同步**：5801 操作集仍为旧版 12 项（home/power/volup/voldn/mute/briup/bridn/kb/clip/fit/full/disc），caps.js 仅做标题覆盖；缺 snapshot/spotlight，多 clip/fit，顺序旧（power 在 home 下、mute 在 voldn 下、keyboard 末位）。
- **可复用当前逻辑**：① press.js attachPress（click/double/triple/long/down/up 全模式识别，index.vnc 为 `<script type="module">` 可直接 import）；② 音量/亮度/静音 down/up 分离式（按住连发）优于 5801 tap 式；③ snapshot/spotlight keysym 直发——已确认设备端 kbdEvent keysym 分发（0x1008ff80/0x1008ff1d）**连接无关全局生效**，5801 走 RFB 直发同样有效，无需设备端改动；④ keyboard 可升级为 XF86Keyboard keysym 直发（toggleOnScreenKeyboard 弹系统键盘）替代本地 rfb.focus()。
- **应保持旧实现**：① clip 剪贴板手动浮层——5801 为 HTTP 直连页（非 https），Clipboard API 必然被拒，手动浮层是直连场景唯一可用路径，必须保留（网关移除降级因 https+协议通道，场景不同）；② FAB 悬浮按钮/信号轮询/中央状态浮层/面板双击窗口（300ms 延迟收起）等特有交互保持；③ fit 冗余（scaleViewport=true 已自动）应移除，与网关对齐。
- **落地注意**：改动均在设备端 webclients（IPA 内置页面），需重打 IPA；需将 press.js 同步到 webclients 目录。
- **执行（2026-08-14 方案 A 轻量同步，已完成）**：① BAR_KEYS 重排对齐网关 KEY_DEFS 顺序（power/home/volup/mute/voldn/briup/bridn/snapshot/spotlight/kb/clip/full/disc），新增 snapshot/spotlight（tapKey 直发 0x1008ff80/0x1008ff1d，设备端仅 down 执行 snapshotPress/toggleSpotlight）；② 移除 fit 按钮与 dispatchOp case；③ 音量/亮度/静音 5 键升级为 down/up 分离式按压（attachKeyPressDownUp + DOWNUP_KEYS 映射，pointerdown 发 down、pointerup 发 up，按住连发对齐网关语义，跳过 click 防双触发）；④ home/power 保持 5801 attachKeyPress 旧交互（单击立即响应 + 双击自然识别 + 300ms 面板双击窗口，不套用网关 300ms 多击判定延迟）；⑤ **kb 键盘按钮修复（2026-08-14 用户反馈点击无反应）**：根因=旧实现 `rfb.focus()` 只 focus canvas（非输入元素），iOS 不弹软键盘；首版用隐藏 input + input 事件逐字符 keysym diff（noVNC keyInput 思路），**经查证 noVNC 官方 touch 键盘机制后发现缺陷**（官方用 Keyboard 实例绑定隐藏 input 抓 keydown，input 事件仅是 Android 补充——diff 方案漏 keydown 路径，回车不产生文本增量→无 input 事件→Enter 失效），**最终改为官方方案**：隐藏 input `#kbInput` + `Keyboard` 实例直接绑定抓 keydown/keyup（onkeyevent → rfb.sendKey），kb 键点击仅 focus input 唤起软键盘；⑥ clip 剪贴板手动浮层保留（HTTP 环境唯一粘贴路径）；⑦ webclients/caps.js CAP_META/DEFAULT_CAPS 同步（顺序对齐 + 增 snapshot/spotlight）；⑧ **控制端软键盘移植网关（2026-08-14）**：网关手机端原无文本输入路径（同 canvas.focus 问题，index.html 无隐藏输入、app.js 无 Keyboard/sendText）；新增隐藏 input `#kbdInput` + app.js `initTouchKeyboard()`（Keyboard 实例绑定，onkeyevent 直发当前 focus.rfb）+「键盘」能力键**按端分流**——触屏端（isMobile）点击=聚焦 kbdInput 唤起控制端软键盘输入，非触屏端保持 XF86Keyboard keysym 直发（设备端弹键盘），两端口同一套 Keyboard 官方方案；⑨ **5801 kb 按端分流对齐网关（2026-08-14）**：5801 全按钮再比对发现 kb 键 PC 端行为未对齐——PC 点击 kb 也聚焦隐藏 input（PC 无软键盘=无反应）；新增 `isTouch` 检测（ontouchstart/maxTouchPoints），触屏端聚焦 kbInput 弹控制端软键盘、非触屏端 `tapKey(0x1008ff2e,'XF86Keyboard')` 唤起设备端系统键盘，与网关一致。**验证**：`node test/verify-indexvnc.mjs` + `verify-kbd-port.mjs` + `caps-test.js` 全绿（5801 语法/结构/分流断言、网关 app.js 语法/移植断言、KEY_DEFS 契约），前端即时生效（app.js v62，硬刷新）；5801 侧需重打 IPA（与既有待打包批次合并）。

#### 2.3s 三端按钮对齐（图标/文案/收起菜单）+ 键盘输入源统一（2026-08-14，前端已生效 / 5801 侧待重打 IPA）

> **背景**：用户要求检查网关屏幕侧按钮与 5801 屏幕侧控制按钮对齐情况。核查结果：按键集合/顺序已对齐（10 键），主要未对齐项 = ①图标风格（网关 SF Symbols SVG vs 5801 emoji）、②文案（home/snapshot title）、③收起菜单按钮（5801 独有）、④键盘键触屏判定标准不一致（网关 `isMobile()`≤900px vs 5801 `isTouch` 触摸能力，iPad 横屏行为相反）。用户决策：#3 键盘键改为**统一无判定**——默认控制端键盘，点击切换。

| 端 | 改造项 | 状态 |
|---|---|---|
| 5801 | **移除面板「收起菜单」按钮**：`renderKeys` 删除 panel-close 追加 + CSS `#panel .panel-close` 删除（面板收起仍走操作后自动收起 / 点 FAB 收起，`dispatchOp case 'close'` 保留无引用） | ✅ 代码已改，**需重打 IPA** |
| 5801 | **图标对齐网关**：`BAR_KEYS` 13 键全部加 SF Symbols 风格内联 SVG（复制网关 KEY_DEFS + full/disc 用网关 index.html 同款 + clip 用 lucide 剪贴板），`renderKeys` 面板图标 `s1.innerHTML = k.svg || k.icon`；CSS 加 `#panel/#bar button svg { 20px }` | ✅ 代码已改，**需重打 IPA** |
| 5801 | **文案对齐网关**：BAR_KEYS 与 CAP_META 双处 home `'Home'`→`'Home 键'`、snapshot `'截屏'`→`'Home+Power截屏'`（其余键本就一致） | ✅ 代码已改，**需重打 IPA** |
| 两端 | **键盘键两态切换（统一，无 isTouch/isMobile 判定）**：默认=控制端键盘（触屏自动弹控制端软键盘），点击键盘键 → 切被控设备键盘（收起软键盘 + 发 XF86Keyboard），再点 → 切回控制端（触屏自动重新弹盘）。按钮 device 态高亮 `kbd-active` + title 标识（「键盘：被控设备」/「键盘：控制端」）；进入 focus/连接时重置为默认控制端 | ✅ 5801 需重打 IPA；网关 app.js v64 即时生效 |
| 网关 | `renderCapOps` keyboard 键移除 `isMobile()` 分流 → `toggleKbd()`；`isTouchable()` 仅用于触屏自动弹软键盘（PC 无系统软键盘）；enterFocus/exitFocus 重置 kbdMode/kbdBtns；style.css 加 `.kbd-active` 主题色高亮 | ✅ app.js v64 即时生效 |
| 测试 | `verify-indexvnc.mjs` / `verify-kbd-port.mjs` 断言重写（两态切换、isTouch 仅自动弹盘、kbdMode/toggleKbd/kbd-active/svg/panel-close 移除等），`caps-test.js` 无需改（KEY_DEFS 10 键契约不变） | ✅ 全绿 |

**验证**：`node --check`（app.js/caps.js 两端 + index.vnc 提取脚本）全过；`verify-indexvnc.mjs` 34 项 + `verify-kbd-port.mjs` 17 项全绿；**`npm test` 7 套件全绿**（smoke/tunnel/register/dedupe/caps/pending-replay/press 无回归）。
**注意**：①触屏端进入控制自动弹控制端软键盘（默认输入源）；软键盘被点击画面收起后，切 device 再切回即可重新唤起；②5801 clip 剪贴板手动浮层保留（HTTP 直连唯一粘贴路径，属刻意差异）；③网关「同步」按钮为多设备群控功能，5801 单设备直连无此概念，属刻意差异。

#### 2.3t 键盘按钮对齐上游原生 noVNC「Show Keyboard」语义（2026-08-14 终版，网关 app.js v86 即时生效 / 5801 侧待重打 IPA）

> **演进回顾**：用户最初反馈手机端网页控制端点击「键盘」键时本机（控制端）键盘与被控设备键盘**同时调起**。先后迭代：①「隐藏被控键盘」开关（红色背景）→ ②「输入源切换」两态（无颜色=被控原生 / 蓝色=控制端，HID attach hack 显式隐藏被控软键盘）→ ③ **研究上游原生实现后用户拍板：完全对齐原生**——上游 noVNC 的键盘按钮语义就是 **"Show Keyboard"（显示/隐藏控制端软键盘）**，聚焦/失焦隐藏 input，无任何"输入源切换"概念；被控端键盘完全由被控端系统管理（点被控画面输入框才弹）。原生的控制端打字链路 = Keyboard 实例捕获软键盘按键 → sendKey(keysym) → 设备端 kbdEvent → keysymToString 映射 → STHIDEventGenerator 注入 HID；剪贴板 = noVNC 面板 textarea → clipboardPasteFrom → setXCutTextUTF8 写 UIPasteboard（不自动粘贴）。据此判定 **HID attach hack（自定义 keysym 0x1008ff83/84 + setHardwareKeyboardAttached + kIOHIDEventFieldKeyboardAttached）为过度设计，全部回滚删除**；「输入源切换」为对键盘按钮能力的理解偏差（控制端键盘天然经 RFB 注入被控端，无需切换）。上游源码已拉取至 `TrollVNC-upstream/TrollVNC-main`（独立目录供后续对照）。

| 端 | 改造项 | 状态 |
|---|---|---|
| 设备端 | 删除 HID attach hack 全部逻辑：① `trollvncserver.mm` kbdEvent 删 `0x1008ff83` XF86KeyboardHide / `0x1008ff84` XF86KeyboardShow case；② `STHIDEventGenerator` 删 `setHardwareKeyboardAttached:`（.m/.h）；③ `IOKitSPI.h` 删 `kIOHIDEventFieldKeyboardAttached`；④ `TRCapabilityRegistry` 删 `keyboard.set` 能力（attach hack 载体，AI/脚本弹被控键盘仍可用 `keyboard` toggle 能力） | ⏳ 代码已改，待重打 IPA |
| 网关 | 键盘键回归原生语义 = **显示/隐藏控制端软键盘**（toggleKbd = kbdSoft 时 blurKbInput，否则 focusKbInput，聚焦/失焦弹收 iOS 软键盘）；删 `kbdControl` 两态状态机、`sendXf86KeyboardHide/Show` 协议函数、pagehide attach 恢复；`updateKbdBtns` 改为 `.kbd-on` 高亮（原生 selected 风格）；`blurKbdInput` iOS readonly+blur 可靠收起保留；connect/enterFocus/exitFocus 复位 kbdSoft | ✅ app.js v86 + style.css 即时生效 |
| 5801 | index.vnc 同步回归原生 toggle：删 `kbdControl` 两态、tapKey(0x1008ff83/84)、内嵌 `.kbd-control` CSS、pagehide 恢复；改用 `.kbd-on` 高亮 | ⏳ 代码已改，待重打 IPA |

**验证**：`node --check` 两端语法通过；`verify-kbd-port.mjs` / `verify-indexvnc.mjs` 断言更新为原生语义全绿（无 kbdControl、无 attach 协议函数/keysym、kbd-on 高亮、toggle=kbdSoft blur/focus）；`npm test` 7 套件全绿无回归。

**IPA 打包（2026-08-14，Actions #125 · 全绿含 release）**：提交 `990db0b`（release 分支）→ run `31806027074` success → `packages-bootstrap`（artifact 9221268361）→ `dist/TrollVNC_0.0.1.tipa`（2,431,225B）。**已验证**：包内 index.vnc 无 kbdControl / 0x1008ff83-84 / .kbd-control，含 .kbd-on + 原生 toggle；三个二进制均无 `setHardwareKeyboardAttached` selector。

**实机验证项（安装新 IPA 后）**：① 点「键盘」键仅弹起控制端软键盘，打字直达被控端聚焦输入框（iOS→iOS 场景被控端输入框聚焦时可能同时弹被控键盘——原生行为，用户手动收起）；② 再点「键盘」键可靠收起控制端软键盘（readonly+blur trick）；③ 剪贴板面板 textarea 粘贴流程正常。

**追加：手机端剪贴板方案审查收敛（2026-08-14，网关 app.js v88 即时生效）**：用户实测质疑后审查，**纠正两处错误认知**：① 我声称"长按 kbdInput 弹控制端系统菜单"错误——kbdInput 是 `left:-9999px;pointer-events:none` 隐藏元素不可交互，长按画面会经 rfb.js patch 转发被控设备、弹的是**被控端**菜单；② 我声称"iOS 键盘上方自动出现粘贴按钮"错误——用户实测无此按钮（QuickType 栏不提供）。**确认设备→控制端剪贴板同步为真实实现非空壳**：设备端复制 → ClipboardManager Darwin 通知 → `sendClipboardToClients` → `rfbSendServerCutTextUTF8`（Extended Clipboard Provide UTF-8）→ 网关透传 → noVNC `_handleServerCutText` Provide 解析 → `clipboard` 事件 → 控制端 `navigator.clipboard.writeText` 写系统剪贴板 + toast（PC/手机/iOS 均可，**写入不弹横幅**；iOS 读取才弹）。**用户拍板删除**：①「粘贴到设备」弹窗（openClipboardSyncModal + 操作菜单按钮）——iOS 不提供正向粘贴；② 点卡片作为控制端→设备写剪贴板事件（隐式同步易塞入控制端旧文本）；③ 同类隐式同步 `trySyncClipboardOnResume`（切前台）与 `farmPendingClip/consumePendingClip` 体系、`isIOSDevice` 死函数；④ kbdInput paste 事件监听（iOS 正向粘贴入口，实测键盘无粘贴按钮）。**保留**：copy 事件（控制端主动复制 → 协议通道写设备剪贴板，farmLastClipText 去重）、PC Ctrl+V（type.paste 原子粘贴）、设备→控制端反向同步（rfb clipboard 事件 → writeText）。**当前剪贴板体系**：控制→设备=copy 事件（协议通道 UTF-8）+ PC Ctrl+V（type.paste 原子）；设备→控制=自动写控制端系统剪贴板（双向真实）。验证：`node --check` + `verify-kbd-port.mjs` + `npm test` 全绿。

**追加：按压语义统一为 5801 方案（2026-08-14，用户拍板）**：单击/双击/三击/长按的识别统一采用 5801 简化模型——**单击立即发送（无 300ms 判定延迟）、长按 800ms 明确识别、双击/三击=自然连点（每次点击独立立即发送，iOS 系统层识别双击/三击）**。网关 KEY_DEFS 的 home/power 移除 `double/triple` 声明（仅保留 click/long），`press.js` PressRecognizer 作为通用识别器保留完整四态能力（任一键声明 double/triple 即可启用窗口判定，供未来能力扩展）；网关与 5801 两端按键行为完全一致。批量「调用能力」入口的 `home.double/home.long/power.double/power.triple/power.long` 显式能力保留不受影响。验证：`node --check` + `npm test` 7 套件全绿（caps-test/press-test 无回归）；caps.js 版本 v3→v4（KEY_DEFS 变更强制刷新），app.js v64。

**追加：键盘按钮改为「隐藏键盘」开关（2026-08-14，用户拍板）**：键盘按钮语义从"输入源切换"改为**针对被控设备的「隐藏键盘」开关**——默认=隐藏键盘（**红色背景** `kbd-hidden`，被控设备键盘不显示，控制端输入可用）；点击=取消隐藏（背景恢复正常，发 XF86Keyboard 弹出被控设备键盘，收起控制端软键盘）；再点=再次隐藏（再发一次 XF86Keyboard，设备端 toggleOnScreenKeyboard 收起，触屏重弹控制端软键盘，背景恢复红色）。状态变量 `kbdHidden`（true=隐藏默认红），进入 focus/连接时重置为 true；`kbd-active` 类更名 `kbd-hidden`（红 #ff3b30）。网关 app.js v65 即时生效；5801 需重打 IPA。验证：`node --check` + `verify-indexvnc.mjs`/`verify-kbd-port.mjs` 全绿 + `npm test` 7 套件全绿。

**追加：Ctrl+V 粘贴不生效根因修复（2026-08-14，设备端代码已改 / 待重打 IPA）**：用户实测 Ctrl+V「提示已粘贴但无效果」——**文字已进设备剪贴板（setStringFromRemote 生效）但粘不进聚焦输入框**。逐层排查（对比「粘贴输入按钮」时代实测有效）定位根因 = **§2.3n 改 executor 为 `releaseEveryKeys + keyDown COMMAND + keyDown v + keyUp v + keyUp COMMAND` 后，四个 IOHID 事件时间戳几乎相同**（`IOHIDEventCreateKeyboardEvent` 用 `mach_absolute_time` 连续创建），iOS 判定"同时按下/无效按键"，**不识别 Cmd+V 粘贴组合键**；按钮时代实现为 `COMMAND↓ → v↓ → COMMAND↑`（**v 不抬起**，组合持续被 iOS 识别）故有效。**修复**：`TRCapabilityRegistry.mm` type.paste executor 改为**异步注入 + 事件间隔**——`dispatch_async(后台)` 内 `usleep(150ms)`（等 noVNC 注入的 Ctrl down 到达设备，降低清键竞态）→ `releaseEveryKeys`（清 LeftControl 残留，保留 §2.3n 修复）→ `COMMAND↓ → 120ms → v↓ → 90ms → v↑ → 40ms → COMMAND↑`（模拟真实按键时序，iOS 识别组合键）；`STHIDEventGenerator.mm` 同步加固 `_activeKeyCodes` 线程安全（`@synchronized` + releaseEveryKeys 快照后清空，防后台队列注入与 RFB 键盘事件并发崩溃）。前端 Ctrl+V 拦截逻辑无需改（通道与按钮时代一致）。**需重打 IPA + 真机验证**（重点：Ctrl+V 是否一次直达、无确认弹窗时可粘贴；按钮弹窗路径回归）。

**追加：IPA 打包 CI 构建修复（2026-08-14）**：打包过程中 CI 编译失败——`src/trollvncserver.mm:4487:90: error: no member named 'enableExtendedClipboard' in '_rfbScreenInfo'`。**真正根因**：`include/rfb/rfb.h` 中 `setXCutTextUTF8`（L375）是 `_rfbScreenInfo`（L201-379）的屏幕级字段，而 `enableExtendedClipboard`（L700）是 **`_rfbClientRec`（L429-718）的 per-client 字段**——源码误用 `gScreen->enableExtendedClipboard` 导致 `no member` 编译错误（同宏块的 setXCutTextUTF8 因结构体正确而未报错，故宏排查为误判方向）。**修复**：① `setupRfbCutTextHandlers` 删除对 `gScreen->enableExtendedClipboard` 的赋值；② 在 `newClientHook`（L3941）中对每个新客户端 `cl->enableExtendedClipboard = TRUE`（gClipboardEnabled 时），libvncserver 据此协商 extended caps 伪编码（0xC0A1E5CE）。中间尝试的 Makefile `-DLIBVNCSERVER_HAVE_LIBZ`（CFLAGS/CCFLAGS 双处）与源码 `#ifndef` 兜底定义均属误判方向（宏本就由 rfbconfig.h L79 定义，非编译失败原因），保留源码兜底定义无副作用。**release 分支 rebase 冲突处理**：release 独有 2 个 commit（f6c1132 批量按钮样式 / 7b0ee2d wifi FAB 图标，均针对 `TVNCControllerViewController.m`/`TVNCViewerViewController.m`）——这两个文件已在 main b49564c「SuperPhone App 重构」中删除，App 控制台已容器化为 WKWebView 承载，改动无落点 → `git rebase --skip` 丢弃，release 已同步 main。**打包结果**：commit f98940a push fork/release → Actions run 123（首跑因 runner 下载 setup-xcode 网络 500 失败，rerun-failed-jobs 后全绿 success，含 release job）→ 下载 `packages-bootstrap` artifact → `TrollVNC_0.0.1.tipa`（2.4MB）验证通过：3 个二进制（TrollVNC/trollvncserver/trollvncmanager）均 arm64 Mach-O + LC_CODE_SIGNATURE 完整、Info.plist 正常（bundle=com.82flex.TrollVNCApp v0.0.1 minOS 14.0）。已复制至 `TrollVNC_0.0.1.tipa`（项目根）。**真机验证清单**：① Ctrl+V 一次直达粘贴（重点回归，异步注入时序修复）；② 粘贴输入按钮弹窗路径回归；③ 5801 控制台键盘「隐藏键盘」红色开关 + 图标/文案对齐 + panel-close 移除；④ 剪贴板 UTF-8 中文双向同步；⑤ 网关 8080 与 5801 按钮对齐（10 键/按压语义/键盘键）。

#### 2.3u 设备→控制剪贴板崩溃根因与协议修复（2026-08-14，设备端代码已改 / 待重打 IPA）

> **背景**：用户实测被控设备复制 → 控制台提示后，在电脑上粘贴不是复制内容 / "什么也没发"。随后控制台抛出 `Failed while connected: Unsupported encoding (encoding: 268435457)` 及调用栈（`_handleDataRect@rfb.js:2979` → `_handleRect` → `_framebufferUpdate` → `_normalMsg`）。**"什么也没发"的真相**：设备复制瞬间剪贴板以 rect 发送 → noVNC 不识编码立即 `_fail` 断连 → toast 永远到不了（连接先崩）。

**根因定性**：`268435457 = 0x10000001 = (1<<28)|1 = rfbExtendedClipboard_Provide | rfbExtendedClipboard_Text`。**两套不兼容的 Extended Clipboard 传输**：
- **noVNC（TigerVNC 风格）**：客户端在 SetEncodings 请求伪编码 `0xC0A1E5CE`（encodings.js），剪贴板走**负长度 ServerCutText 消息**（`_handleServerCutText` 解析 flags/actions：Caps/Request/Peek/Notify/Provide，Provide 分支 inflate zlib → `clipboard` 事件）；
- **旧版 libvncserver（设备预编译库 `lib/libvncserver.a`）**：`rfbSendServerCutTextUTF8` 把剪贴板作为 **framebufferUpdate rect**（rect 头 encoding 字段 = flags 组合值 `0x10000001`）发送——noVNC 的 `_handleDataRect` 只认 `this._decoders[encoding]`（图像编码 + 标准伪编码），`0x10000001` 无解码器 → `_fail("Unsupported encoding")` 断连。
- 已对照 **libvncserver master 源码**（GitHub API 拉取 `src/libvncserver/rfbserver.c` 解码确认）：上游现行为 = `rfbSendExtendedServerCutTextData` 发送 `[type=3][pad×3][-(4+zlibSize)][flags=0x10000001][zlib( [len BE] + data )]` 负长度 ServerCutText（TigerVNC 兼容，noVNC 原生解析）。设备预编译库为旧版 rect 实现。

| 端 | 修复项 | 状态 |
|---|---|---|
| 设备端 | **`sendClipboardToClients` 不再调用旧库 `rfbSendServerCutTextUTF8`**：新增 `sendExtendedClipboardProvideToClients(utf8, utf8Len)` 自实现**负长度 ServerCutText Provide**（字节格式与 libvncserver master 完全一致）：zlib(`compress`+`Z_DEFAULT_COMPRESSION`) 压缩 `[utf8Len(4BE)]+utf8` → 组装 `[0x03][0,0,0][-(4+zlibLen) BE][0x10000001 BE][zlibData]` → 逐客户端（`enableExtendedClipboard`）`pthread_mutex_lock(&cl->sendMutex)` + `rfbWriteExact` 发送（与 libvncserver `LOCK(sendMutex)` 模式一致），失败 `rfbCloseClient`。中文/emoji UTF-8 无损 | ✅ 代码已改，**需重打 IPA** |
| 前端 | 无需改动：noVNC `_handleServerCutText` 原生解析负长度 Provide（gateway 与 5801 两套 noVNC 均原生支持，**无需任何 patch**） | — |

**验证要点**：① 设备复制 → 控制台 toast「已写入控制端剪贴板」且连接不断；② 控制台粘贴 = 设备复制内容（中文/emoji 无损）；③ PC/手机/iOS 控制端均可（writeText 不弹横幅）；④ Ctrl+V（能力通道 type.paste）与 copy 同步（协议通道）回归不受影响；⑤ 网关 `sessions=N` 无堆积、无 "Unexpected server message"。

**根因排查方法论**（供后续）：错误栈 `_handleDataRect` = rect 路径（FramebufferUpdate 内），`_handleServerCutText` = 消息路径（ServerCutText type=3）；编码值 `268435457` 与 rfbproto.h 常量表（L532 `rfbEncodingExtendedClipboard 0xC0A1E5CE` / L1453-1463 flags）比对即得"flags 当 encoding"结论；对照上游源码确认标准负长度格式后直接设备端自实现，避免前端 patch 非标准 rect 路径（不确定性 + 双端 noVNC 都要改）。

**追加：移动端 FAB 菜单「粘贴」按钮（2026-08-14，网关 app.js v90 即时生效）**：用户确认恢复显式一键粘贴入口（此前弹窗/隐式同步因"多步/易塞旧文本"被删）。**时序可行性结论：一个点击可走完完整链路**——① 点 FAB 展开菜单 → ② 点「粘贴」（原生用户手势，`readClipboardText()` 在 click 内**同步发起**，iOS WebKit 激活窗口有效）→ ③ iOS 控制端弹一次系统「允许粘贴」横幅（iOS 隐私，无法绕过）→ ④ `type.paste({text})` 能力通道（网关隧道 → 设备端写剪贴板 + releaseEveryKeys + 异步模拟 Cmd+V）→ ⑤ 被控端再弹一次「想从 superphone 粘贴」隐私确认（iOS 14+ UIPasteboard 读取确认）→ ⑥ 用户确认落入被控端聚焦输入框。**两次系统确认（控制端读 + 被控端粘）为 iOS 隐私设计使然，非可绕过项**。实现：`renderCapOps` 仅 `opsMenu`（移动端 FAB）追加「粘贴」按钮（clipboard-paste SVG，与 KEY_DEFS 图标风格一致），`pasteToFocusedDevice()` 与电脑 Ctrl+V 同链路（readClipboardText → submitPasteText，非 https/被拒/空剪贴板均明确 toast，不降级弹浮层）；电脑端已有 Ctrl+V 不重复加按钮。**通道对照**：上游（OwnGoalStudio/TrollVNC）无能力体系，粘贴纯协议通道（noVNC clipboardPasteFrom → setXCutTextUTF8 → UIPasteboard，**只写不粘**）；我们的 type.paste 为 fork 增强走**能力通道**（网关 invoke → 设备 executor 自动模拟 Cmd+V，补上上游缺失的"自动粘贴"）。验证：`node --check` + `npm test` 7 套件全绿无回归；index.html `?v=89→90`。

**追加：控制端→被控端剪贴板自动同步（2026-08-14，IPA 原生监听 + RFB 协议复用，App 待重打 IPA / 网关前端 v91）**：用户拍板最终架构——**双向都复用 RFB Extended Clipboard 协议通道、自动双向同步、粘贴原生无感**（否决"设备端上报网关"与"桥按钮"两案：上报-路由三端改动+防循环复杂，桥按钮仍需用户动作）。链路：控制端设备（装 IPA）复制 → **App 原生层**（`TVNCConsoleWebViewController` 常开监听：Darwin 通知 `com.apple.pasteboard.notify.changed` + changeCount 去重 + `clipboardLastPushed` 回显抑制 + 回前台补发覆盖"其他 App 复制→切回"）→ `evaluateJavaScript` 推 `window.__farmNativeClipboard(text)` → Web 层 `farmPushClipboardToSessions`（复用 copy 事件发送路径：focus.rfb + directRfbs + ClipboardEnabled 门控 + `farmLastClipText` 去重）→ `rfb.clipboardPasteFrom`（ClientCutText 协议通道，UTF-8）→ 被控端 `setXCutTextUTF8` 写剪贴板 → 被控端用户原生长按粘贴。**防循环双层**：原生 `clipboardLastPushed` 同文本跳过（RFB writeText 写入回显）+ Web `farmLastClipText` 同文本跳过（RFB 'clipboard' 事件 writeText 前同步标记"刚来自设备"）。**角色说明**：App 不区分控制端/被控端——同一 IPA 同时具备 server 面（被控端：trollvncserver ClipboardManager，已有）与 App 控制台面（控制端：本监听），两面向互不冲突，关系由网关会话协调；被控端已有 `gClipboardSuppressSend` 抑制写回。**边界**：iOS 16+ 原生读剪贴板受「粘贴权限」约束（首次/按系统设置弹提示）；控制端为浏览器（无 IPA）时走 Web 通道（copy 事件 / Ctrl+V / v90 粘贴按钮），双通道并存。

**追加：设备→控制端剪贴板写入修复（2026-08-14，IPA 原生桥写剪贴板，网关 v92 / App 待重打 IPA）**：实测发现被控端复制 → 控制端剪贴板写入**电脑 OK、iOS 失败**（toast「剪贴板已同步但写入控制端失败」，手机 Safari 与 IPA 容器均如此）。**根因** = iOS WebKit 在**非用户手势**下 `navigator.clipboard.writeText` 被拒（`NotAllowedError`——RFB clipboard 事件为异步网络数据到达、非手势；手机 Safari 与 WKWebView 同受限，**电脑 Chrome 桌面允许非手势写入**故成功；与 secure context/网页身份无关）。**修复**：容器模式（`container=ipa`）优先走**原生桥**——`TVNCConsoleWebViewController` 加 `WKScriptMessageHandler`（`farmBridge`）收 `{type:'writeClipboard'}` → 原生 `[UIPasteboard].string = text`（无手势/安全上下文限制，必成功；写入前同步 `clipboardLastCount`/`clipboardLastPushed` 抑制本机监听回显推送）；无桥环境（浏览器）`writeText` 尽力而为 + 明确 toast「iOS 非手势限制，建议使用 SuperPhone App 内控制台」。cleanup `removeScriptMessageHandlerForName` 防 WKScriptMessageHandler 循环引用泄漏。**边界**：手机浏览器（无 IPA 桥）非手势 writeText 被拒为 iOS 平台限制，无法完全绕过——正确路径是 IPA 容器。

**追加：剪贴板同步改"来源排除"（2026-08-14，用户思路「从谁复制的不推送给谁」，网关 v94 即时生效）**：用户质疑 v93 全局同文本去重的必要性——其更简单的思路是"从谁复制的不推送给谁"。**两层结论**：① **设备端抑制必须保留**——RFB ClientCutText 协议**不携带来源设备**，被控端收到远程写入后本机 Darwin 通知触发时无法辨别"变化是远程写入还是用户本机复制"，只能靠 `lastSetValue` 文本对比（echo 判断）；且多设备群控场景 R∘B1→B2 转发后 B2 若抑制掉会把同文本发回 B1 → 环形。故设备端抑制是协议无来源信息下的必然。② **Web 侧采纳用户思路**：v93 全局 `farmLastClipText` 同文本去重会**误伤多设备**（B1 复制的 X 同步到控制端后，全局去重导致 X 不再同步给 B2）；改为 **`farmClipSource = { text, deviceId }` 来源记录 + `farmPushClipboardToSessions(txt, excludeDeviceId)` 排除参数**——RFB 收到某设备文本记录来源；控制端剪贴板变化（`__farmNativeClipboard` 原生监听、copy 事件、点卡片同步三路）推同文本时**仅排除来源设备、仍发给其他设备**。copy 事件分支补 `isFromDevice` 判断（全选再复制刚同步来的内容也不回发来源设备）。验证：`node --check` 通过 + `npm test` 7 套件全绿；index.html `?v=93→94`。

**追加：控制 Tab 去原生上标题 + H5 容器化限制（2026-08-15，IPA 需重打 / 网关 v95 即时生效）**：用户反馈 IPA 控制面板内 webView 顶部被容器上标题（UINavigationBar）遮挡。**澄清概念**：底部 TabBar（连接/控制/设置）保留用于切换；被遮挡的是「控制」Tab 顶部 UINavigationBar（opaque background 悬浮在 webView 之上），去除后由 H5 自带 header（群控台标题 + 布局切换 + 批量操作）充当标题栏——即"网页顶部与标题集成"的最优方式（WebKit 上 DOM Fullscreen API 仅支持 video，H5 无法系统级全屏，无需走此路）。**原生改动**（TrollVNC 需重打 IPA）：① `TRMainTabBarController` 对 controllerNav `setNavigationBarHidden:YES`（仅控制 Tab，连接/设置导航栏不变，底部 TabBar 保留）；② `TVNCConsoleWebViewController` override `preferredStatusBarStyle` 返回 `UIStatusBarStyleLightContent`（隐藏导航栏后 webView 顶到状态栏，深色 H5 背景需浅色文字）+ viewWillAppear `setNeedsStatusBarAppearanceUpdate` 刷新；③ `webView.scrollView.bounces = NO` 禁页面级橡皮筋。**Web 改动**（trollvnc-farm 即时生效）：① viewport 加 `maximum-scale=1, user-scalable=no` 禁双指/双击缩放；② `html/body overflow-x:hidden` 禁横向滚动（元素超宽不撑出左右拖动）+ `body overscroll-behavior:none` 阻断滚动链式传导；③ `header` 顶 padding 加 `env(safe-area-inset-top)`、`main` 底 padding 加 `env(safe-area-inset-bottom)`（IPA 隐藏导航栏后顶到刘海，桌面端恒 0 无影响）。**未做**：进入控制画面不隐藏底部 TabBar/状态栏（用户确认需保留 Tab 切换入口）；点击卡片最大化 = H5 focus-panel `position:fixed; inset:0`（webView 全区域内全屏，无原生导航栏遮挡）。验证：`node --check` + `npm test` 7 套件全绿；index.html `?v=94→95`。

**追加：三 Tab 顶部导航栏统一处理（2026-08-15，IPA 需重打）**：用户确认诉求 = 去掉各页**顶部**的 UINavigationBar（"顶部标签"），底部 TabBar 保留。逐页评估：① 连接页（TVNCConnectViewController）——无 push 依赖（仅 present 弹窗），scroll 顶部已对齐 `safeAreaLayoutGuide.topAnchor`，隐藏后自动顶到状态栏下方，**直接隐藏**；② 设置页（TVNCRootListController，PSListController）——**子菜单靠 pushViewController 进入**（网关配置/连接/安全等子页），根页直接隐藏会让子页无返回按钮，故采用 **UINavigationControllerDelegate**（`willShowViewController:`）：根页（self）隐藏、push 进入的子页临时显示（承载返回按钮）、退回根页再隐藏；`settingsNav.delegate = settings`（PSListController 头文件与现有代码均无 delegate 占用，安全）。三 Tab 状态栏文字：连接页浅色背景默认深色文字、控制页深色 H5 背景 override 浅色文字、设置页默认，均经 UINavigationController 沿 VC 链正确转发。

**追加：设置页配置项审核 + 中文化（2026-08-15，IPA 需重打）**：用户质疑"设置页是一整页，为何有返回之说"，并怀疑配置项在开发中丢失。**审核结论**：① OrientationPadFix（方向偏移）与 ModifierMap（修饰键映射）均在设置面板（42 项），未丢失；"返回之说"来自这两个 `PSLinkListCell` 点击 push 子选择页（TVNCListItemsController）；② 上游有而我们删的 9 项全部有据：`Enabled`（App 启动即服务）/`Port`+`HttpPort`（端口硬编码 5801/5901）/`ReverseMode`+`ReverseSocket`+`ReverseRepeaterID`（反向连接废弃）/`SingleNotifEnabled`+`ClientNotifsEnabled`（Phase 8.2 合并为 Notifications 枚举，服务端仍读底层 key，TRCapabilityRegistry 写枚举映射回底层——功能保留 UI 收敛）；③ **DesktopName 确认"桌面名=设备名"**：TRGatewayClient `_deviceName` 已自动同步 UIDevice name → DesktopName（VNC 桌面名/Bonjour 服务名一致），无需配置项；④ **OrientationPadFix 非旧实现**：上游 TrollVNC-main 完整同款实现（trollvncserver.mm/ScreenCapturer/STHIDEventGenerator/gen-managed-plist/README 均一致），`gOrientationFixQuad` 在 4 处运行路径真实生效（服务器旋转合成、rotateOrientation、截图/触控坐标宽高交换），删除会使画面/触控错乱。**本次改造（用户拍板仅"中文化+说明"）**：Root.plist 全部英文 label/placeholder/staticTextMessage 中文化（32 处：绑定地址/自动发现/完全访问密码/只读密码/只读模式/剪贴板同步/帧率/同步界面方向/方向偏移/服务器端光标/非阻塞交换/自然滚动方向/修饰键映射/辅助触控自动激活/滚轮调节/HTTP 文档目录/SSL 证书文件/SSL 私钥文件/生成证书/记录键盘事件/查看日志/重置默认设置等）；8 组全部补充 footerText 说明（画面与性能/输入/关于与诊断 新增，进阶/高级 扩展列举各滑条含义）；slider 项（Scale/DeferWindowSec/MaxInflight/TileSize/FullscreenThresholdPercent/MaxRects/WheelStepPx/KeepAliveSec）无 label 为刻意（TVNCSliderCell 无文字位，slider 从左边铺满），含义由组 footerText 说明。**未做**（用户未选）：分组重构/PerformanceMode 枚举联动/诊断区补 UDID·版本·IP/去 push 子页。

#### 2.3q 设置页生效机制审查 + Apply 按钮移除（2026-08-14，设备端代码已改 / 待重打 IPA）

> **背景**：审查设置页「搜索网关」点击时序与右上角 Apply 按钮的实际意义，梳理设置操作生效机制。

**搜索网关时序（确认能写入配置，且无需重启/Apply）**：点击「搜索网关」→ `searchGateway`（TVNCRootListController L596）→ NSNetServiceBrowser 搜索 `_superphone-farm._tcp` local. 域 → 1.5s 后 `presentFoundGateways` 过滤 IPv4 弹「选择网关」→ 选中 → `saveGateway:port:`（L669）**写 NSUserDefaults：GatewayHost=选中 IP + GatewayPort=18081（固定）** + synchronize + reloadSpecifiers → defaults 变更通知 → TRGatewayClient `_defaultsChanged`（TRGatewayClient.mm L300）→ `_needsReregister=YES` → worker 线程（≤5s）重发 register（含最新 configs）→ 生效。另 `settings.searchGateway` 能力（无 UI invoke）经 TRSearchGatewaySync 取首个可用网关自动写 defaults，落盘逻辑一致。

**Apply 按钮真相**：它不是"提交配置"（配置在 cell 变更时即写入 defaults），而是 **「重启服务」按钮**（校验 BindHost → 确认弹窗 → TVNCRestartVNCService 杀 trollvncserver 由 launchd 拉起）。其唯一意义 = 让 restart 级配置生效（Root.plist 暴露 9 项：BindHost/FullPassword/ViewOnlyPassword/TileSize/MaxRects/AsyncSwap/HttpDir/SslCertFile/SslKeyFile）。

**生效机制全貌（关键事实：设备端 daemon 只在启动时读取配置到 C 全局变量，运行中仅 TRGatewayClient 监听 defaults）**：
| 级别 | 配置 | 设置页（App 内） | 控制端 Web 面板（setConfig） |
|---|---|---|---|
| gateway | GatewayHost/GatewayToken/BonjourEnabled | ✅ 自动重注册生效 | ✅ 自动生效 |
| hot | Scale/ServerCursor/FrameRateSpec/OrientationSync/DeferWindowSec/MaxInflight/FullscreenThresholdPercent/ModifierMap/WheelStepPx/WheelTuning 等 | ❌ 需重启 | ✅ 即时（tvReloadConfigForKey 更新 C 全局变量） |
| restart | 上述 9 项 | ❌ 需重启 | ✅ 自动 watchdog restart |
| instant | ViewOnly/ClipboardEnabled/NaturalScroll/AutoAssistEnabled/ThumbInterval/Notifications/KeyLogging | ❌ 需重启 | ⚠️ 无热路径（标注"立即生效"但服务端无 tvReloadConfigForKey 分支，实际需重启） |

**改动（用户拍板：删除 Apply + 配置变更自动重启）**：
| 端 | 改造项 | 状态 |
|---|---|---|
| App | **删除右上角 Apply 按钮**（viewDidLoad 移除 applyItem，删除 applyChanges 方法） | ✅ 代码已改，**需重打 IPA** |
| App | **restart 级配置变更自动重启**：覆写 `setPreferenceValue:specifier:`（super 写 defaults 后拦截）→ `_restartRequiredKeys`（9 键与 CONFIG_DEFS reload=restart 对齐）→ `_scheduleRestartConfirm`（400ms 防抖合并多次修改）→ `_maybeConfirmRestart`（BindHost 校验沿用原逻辑 + 确认弹窗复用原 Localizable key + TVNCRestartVNCService；restartConfirmVisible 防重复弹窗；Managed 配置下跳过） | ✅ 代码已改，**需重打 IPA** |
| App | **生成证书后重启入口**：`_reallyGenerateKeys` 成功 alert 的 OK 按钮改为「重启服务」（destructive → TVNCRestartVNCService），保留「导出证书…」 | ✅ 代码已改，**需重打 IPA** |
| App | **重置默认值后重启确认**：`_reallyResetDefaults` 末尾 `_scheduleRestartConfirm`（重置值也需重启才在全局变量生效） | ✅ 代码已改，**需重打 IPA** |

**遗留提示**：instant 级 7 项在服务端无热更新路径（tvReloadConfigForKey 未覆盖），前端标"立即生效"与实际不符——如需真正即时生效，需在 tvReloadConfigForKey 补这些 key 的全局变量更新分支（待用户确认后另行处理）。

#### 2.4 5801 网页黑屏问题（历史修复，代码已落地）
- `layout/usr/share/trollvnc/webclients/index.vnc`（+580 行）当前**只 `import RFB`**（行 211，无静态 caps import），caps.js 改为动态 `import('./caps.js')` 可选增强（行 558，失败不影响连接）。
- 这解决了「caps.js 静态导入 404 → 模块脚本死亡 → 不连接 → 黑屏」根因。**但真机是否出画面仍需验证**。

### 2.5 网关前端（Phase 4.7 / 10.2-10.3 / 11.5）
- `web/app.js`（+804 行）：垂直列表、**批量操作栏（selectedDevices Set + updateBatchBar + showBatchMenu 三分组）**、配置面板（按 reload 四区分区）、卡片墙完整 RFB（v2.3 弃用 invoke screenshot 轮询）。
- `web/caps.js`（+192 行，2026-08-13 自包含定义）：`KEY_DEFS`（12 键）/ `ACT_DEFS` / `QUICK_ACTIONS` / `BATCH_CAPS`（32 项含 category）/ `CONFIG_DEFS`（37 项）+ `CONFIG_BY_KEY` / `configSchemaByReload()` / invoke/set/batch 封装（已删 `DEFAULT_CAPS`/`CAP_FALLBACK`/`deviceCaps()`，见 §2.3e）。
- `web/index.html`：布局切换器（12 项下拉）、墙缩放、批量按钮、focus 面板 + 操作列 + FAB。
- `web/app.js` `renderCapOps`：改用 **DocumentFragment 批量渲染**（106 个能力按钮 + 分组标题一次性插入），消除逐元素 append 的 forced reflow（此前点击卡片触发 68ms 卡顿），app.js 版本已 bump v18。
- **Web 卡片墙画面获取改为 hash 门控 + 变化拉图（2026-08-10）**：`startWallRfb` 由"每 ThumbInterval 秒定时全量截图"升级为——先 `invoke screen.hash`（设备端 0.3ms 级轻量 pHash，CPU<1%），与上次 hash 相同则跳过（卡片保持缓存帧，静止时零图片流量），**仅画面变化才 `invoke screenshot` 拉新帧**；安装本 IPA 的设备均注册 `screen.hash/screenshot`，**hash 失败显式报错、无截图回退**；卡片墙不建 RFB 持久连接（修正注释与实现脱节）。app.js 版本 v18→v20。设备端/网关零改动。网关已杀净旧进程（PID 40392）重启，设备自动重新注册上线。

### 2.6 清理类（Phase 6 / 2）
- **反向连接残留**：trollvncserver.mm / TVNCConnectViewController.m / TRCapabilityRegistry.mm 中 `ReverseMode|ReverseSocket|ReverseRepeaterID|gRepeater|isRepeaterEnabled` **零匹配**（已彻底清理）。
- **营销入口**：`havoc|OwnGoalStudio|View Source Code|Made with|付费作品|GitHubReleaseUpdater` 在 app/prefs/src **零匹配**（GitHubReleaseUpdater 文件已删除）。
- CI `build.yml`：已移除 reverse_mode/reverse_socket/reverse_repeater_id 三个 input。

---

## 3. 文档口径 vs 代码实测的差异（重要）

| 项 | 文档口径 | 代码实测 | 结论/建议 |
|---|---|---|---|
| 能力数量 | 97 控制 + 38 配置 | 直接计数 75 `_registerControl:` + 45 `_registerConfig:`（循环数组展开更多） | 需运行期 `query caps` 确认最终总数，**文档数字需校准** |
| Phase 11.1 元数据 | Batch 6a 未实现 | menu/scenes/batch 字段+默认映射**已实现**（screen.* 标 internal/[ai]） | 文档过时，实际已落地 |
| screen.* 能力 | Batch 6b/6c 未实现 | **已实现并注册**（screen.hash/diff/waitStable/subscribe，46752 + TRScreenHasher） | 文档过时，实际已落地（需真机验证） |
| macro.run / app.* | Batch 6a/6b 规划 | **未实现**（grep macro.run/app.list/TRAppManager 零匹配） | 待实现，保持规划 |
| caps-test.js | 「npm test 全绿」 | **引用已改名的 `CAP_META`（现为 `CAP_FALLBACK`），import 即失败**；且断言字段 op/label 与新结构不符 | **测试与实现断链，npm test 必挂**，需修复测试 |
| 注册通道 | 「单通道 TCP JSON」 | 已确认无 `/ws/register`；register-test 断言拒绝 | ✅ 一致 |
| farm 模块 | — | ESM（`import` + package.json type:module）| ✅ 一致，非风险 |
| 5801 黑屏 | — | index.vnc 已移除静态 caps import | 大概率已解决，需真机打开验证 |
| UI Phase 12 | M20 已完成 | TRMainTabBarController + 卡片墙 + 批量 + 配置面板存在 | 未 build，需真机验证 |
| 隧道 | Phase 7 已完成 | farm tunnelServer + TRTunnelClient 存在 | 未真机联调，需验证跨网络透传 |
| 卡片墙方案 | v2.2 判定 IPA 端 TVNCFetchThumbnailViaRFB（libvncclient 首帧）"符合新方向" | 与最新约束冲突：已改为 **TRWallTileWebView + WallTileWeb.html（WKWebView + noVNC 完整 RFB viewOnly）**，libvncclient 首帧抓取已移除（§2.7） | ✅ 已修正，需真机验证 |
| 里程碑 M15 反向清理 | ✅ | 代码零匹配 | ✅ 已可信（未编译验证） |

---

## 4. 未完成 / 未验证

- **未提交**：trollvnc-farm、根文档仍有未提交改动（farm 仅 1 条 auto commit）；TrollVNC 子模块已提交（commit `2a4df98`，2026-08-11）。
- **已构建（2026-08-11）**：`dist/TrollVNC_0.0.1.tipa`（2.36MB，Actions #97，commit `2a4df98` ack 驱动 + 卡片墙改造），4 scheme 全部构建成功，packages-bootstrap artifact 已下载解压。
- **未跑测试**：本沙箱 node/npm 因 `EPERM: lstat C:\Users\Administrator` 无法运行 farm 测试（环境限制，非代码问题）；且 **caps-test.js 本身断链（§3）**，需在正常环境修复后跑 `npm test`。
- **真机联调**：M2 验收 + M14-M20 实现 + 全链路验证（5801 出画面、App 三 Tab、能力按钮渲染、隧道透传、卡片墙）均需实体 iOS 设备。
- **Batch 6a/6b 剩余**：macro.run / app.list / app.current / app.open / app.icon **未实现**（screen.* 已提前落地）。
- **前端并行**：三端菜单分层（primary/secondary/internal）+ 批量过滤（scenes/batch）+ AI 通道的前端渲染**未完成**（元数据已有，前端未消费）。
- **端到端真实输入验证（2026-08-11 待办）**：同步推流 / 直控的**真实触摸输入端到端验证**未做——需第二台真实设备（验证同步广播到多设备）或用户确认可向在线 iPhone 发送测试输入（直控点击落点正确性）。当前仅完成连接保持、协议无串流、UI 交互自动化验证（verify-sync-ui.mjs 13/13）。

---

## 5. 风险与已知边界

1. **构建风险**：新增文件（TRCapabilityRegistry/TRTunnelClient/TRScreenHasher 走 Makefile：trollvncmanager_FILES/trollvncserver_FILES 已加；TRMainTabBarController/TVNCDeviceListCell/ViewerWeb.html 走 pbxproj 已加）——需 CI 编译确认。
2. **bootstrap scheme**：此前多次 bootstrap 因 clang modules / import 顺序失败；本次大改需重点验证 bootstrap 编译。
3. **能力真实可用性**：文档标 ✅ 不代表全部经真机验证（尤其 HID 注入、截图、剪贴板、触摸、screen.*）。
4. **隧道安全**：18181 隧道无鉴权设计（内网自用），反向模式已废弃，符合既定边界。
5. **5801 端口语义**：5801 仅发网页，画面走 5901 WebSocket；若真机仍黑，按「连接/首帧诊断」区分锁屏黑帧 vs 5901 未监听。
6. **caps-test.js 断链**：若直接跑 `npm test` 会在 caps-test 处失败，需先修复（对齐 CAP_FALLBACK 与断言字段）。
7. **Node 版本**：smoke.js 用 `import.meta.dirname`（Node 20.11+）；本机 node v22.14.0 满足，软路由部署时需注意。

---

## 6. 建议的下一步（按序）

1. **修复 caps-test.js**：将 `CAP_META` → `CAP_FALLBACK`，断言对齐新结构（id/title/icon/category/route/params），补齐缺失导出校验。
2. **跑 farm 测试**：在正常环境 `cd trollvnc-farm && npm test`（smoke/register/dedupe/caps），确认注册/去重/单通道/隧道框架全绿。
3. **构建验证**：✅ **已完成**（2026-08-11，Actions #97，commit `2a4df98`，4 scheme 全绿，`dist/TrollVNC_0.0.1.tipa` 2.36MB）。
4. **真机冒烟**：安装新 tipa → 验证 5801 打开出画面、App 三 Tab、能力按钮按清单渲染、网关控制台卡片墙、批量操作；重点验证 ack 驱动下接管/重开场景"有时没画面"问题是否消除。
5. **再纵深**：实现 Batch 6a/6b（macro.run / app.*），以及三端菜单分层前端渲染。
6. **文档校准**：能力数量（97+38 → 运行期实测值）、Phase 11 状态（screen.* 已落地）同步回「能力追踪清单.md」与「06-构建交付与里程碑.md」。

---

## 7. 附：文档体系索引

| 文档 | 内容 | 状态 |
|---|---|---|
| 00-当前进度基线 | 本文档 | 持续更新 |
| 01-愿景与设计基线 | 核心原则/端口/连接方向/UI 定位 | 已提炼 |
| 02-能力即服务架构 | 能力注册/命令 v2/配置上报/热重载/REST/前端渲染 | 大量 ✅ |
| 03-隧道与连接架构 | 18181 隧道/三端接入/命令复用 | ✅ 已完成 |
| 04-UI交互规范 | 三端 UI 规范（垂直列表/卡片墙/悬浮菜单） | ✅ 已完成 |
| 05-能力扩展规划 | Phase 11 场景化分层 + app.* / screen.* | **部分已提前落地**（screen.* ✅，macro/app 待做） |
| 06-构建交付与里程碑 | M1-M20 里程碑 | 几乎全 ✅，卡点=真机 |
| 能力追踪清单 | 逐能力状态（97 控制 + 38 配置） | **数字待校准** |
| 99-历史归档 | 历史上下文归档（v1.8 反推） | — |
| IPA改造计划 | 主计划聚合（v2.4） | 已重写 |
| 开发宪法 | 能力模型/铁律/协议（v1.8） | 已更新 |

---

## 8. Web 容器化改造（Phase 13，2026-08-11 启动 · 进行中）

> **决策（用户确认 2026-08-11）**：IPA 控制端**整 Tab 替换为 WKWebView 容器**加载网关 H5 手机控制台；原生设备墙（TVNCControllerViewController）与大屏（TVNCViewerViewController）**直接替换并删除**。所有交互组件统一由网页端（H5）承担——"本质与操作网页端一致，什么能力都便于后续开发"。改 Web 端即生效（serveStatic no-cache + app.js?v=N），App 容器自动同步；仅容器 VC 本身需重打包 1 次。

### 8.1 目标架构

```
iPhone App ── Tab2 控制页 = WKWebView 容器
   └─ http://{GatewayHost}:{TVNCConsolePort}/?token={GatewayToken}&selfId={DeviceUUID}&container=ipa
        └─ H5 手机控制台（设备墙/聚焦大屏/批量操作/FAB 能力菜单/布局切换 全由 H5 渲染）
网关 trollvnc-farm ── web/ + /api/* + /ws/vnc/*（静态不鉴权、API 走 ?token=）
设备端 ── RFB 5901 + 46752 命令通道 + 能力注册表 + screen.*
```

### 8.2 改造点与状态

| 端 | 改造项 | 状态 |
|---|---|---|
| Web | `?selfId=` 自身设备过滤（refreshDevices 过滤，pollWallChanged 经 wallInstances 天然跳过） | ✅ 已落地 |
| Web | `?container=ipa` 容器标记（body[data-container] + window.__FARM_CONTAINER，CSS/JS 分支用） | ✅ 已落地 |
| Web | 容器模式全屏按钮处理（WKWebView 无 requestFullscreen） | ⏳ Phase 1.5 |
| Web | 容器加载失败重试页 / 断网引导 | ⏳ Phase 1.5 |
| IPA | 新增 `TVNCConsoleWebViewController`（WKWebView 容器：URL 构造 token/selfId/container、未配置引导+3s 自动轮询、加载失败重试、网关变更幂等重载、cleanup 释放） | ✅ 已落地（Phase 2，待构建验证） |
| IPA | Info.plist 加 `NSAppTransportSecurity`→`NSAllowsLocalNetworking`（局域网明文 HTTP；真机验证仍被拦再升 NSAllowsArbitraryLoads） | ✅ 已落地 |
| IPA | token/selfId 注入：URL query 构造（TVNCGatewayClient 读 GatewayHost/GatewayToken/TVNCConsolePort + TVNCReadSelfDeviceId） | ✅ 已落地 |
| IPA | TRMainTabBarController Tab2 换容器；**删除** TVNCControllerViewController / TVNCViewerViewController / TVNCDeviceListCell / ViewerWeb.html（TVNCAppStore 被连接页使用**保留**；pbxproj 四步齐全） | ✅ 已落地 |
| IPA | 可选 JS bridge（`webkit.messageHandlers.console`）：全屏接管 / 返回 App / 触感反馈 | ⏳ 按需 |
| 网关 | 基本零改动（静态不鉴权 + API ?token= 已支持）；可选新增 /health | ⏳ 按需 |

### 8.3 难点与风险备忘

1. **ATS 明文 HTTP**：Info.plist 现无 NSAppTransportSecurity；URLSession 访问裸 IP 已豁免（API 一直正常），但 **WKWebView 加载 http:// 页面需真机验证**，预备 `NSAllowsLocalNetworking`，兜底 `NSAllowsArbitraryLoads`。
2. **H5 全屏 API**：iOS WKWebView 不支持 `Element.requestFullscreen`（app.js L1278 分支会静默失败）——容器模式需隐藏或 JS bridge 接管。
3. **自身设备过滤**：H5 原生无排除自身逻辑，靠 `?selfId=` 传入 DeviceUUID（与原生 handleDevices 的 UUID 权威去重一致）。
4. **性能**：H5 卡片墙默认 hash 门控 + 截图轮询（非持久 RFB），与原生同模型，容器化不加重。
5. **iOS 14 兼容**：H5 用 ES module / optional chaining，Safari 14 支持，真机验证。
6. **WebContent 进程**：卡片墙截图轮询 + 大屏 noVNC 同进程，真机验证 30 台场景。

### 8.4 Phase 1 完成记录（2026-08-11）

- `web/app.js`：URL 参数区新增容器参数解析（`SELF_ID` / `IS_IPA_CONTAINER` / body data 属性 / window 暴露 / console.info）；`refreshDevices` 按 `?selfId=` 过滤自身设备（String 严格比较，普通访问不受影响）。
- `web/index.html`：app.js 版本 `v=26→v=27`（serveStatic no-cache + 版本号双保险，WKWebView 容器强制刷新）。
- 验证：`test/verify-container-mode.mjs` **9/9 PASS**（普通访问含自身 / 容器访问过滤自身 31→30 台 / 仅容器标记不过滤 / body+window 标记正确）；`test/verify-mobile-layout.mjs` **14/14 无回归**；`node --check` 通过。
- 遗留：容器模式全屏按钮处理、加载失败重试页 → Phase 1.5（Web 继续迭代）。

### 8.5 Phase 2 完成记录（2026-08-11 · IPA 容器化，待构建验证）

- **新增 `TVNCConsoleWebViewController.h/.m`**（控制 Tab 容器，替换原生设备墙）：
  - URL 构造 `http://{GatewayHost}:{GatewayPort}/?container=ipa&token={Token}&selfId={DeviceUUID}`（读 `TVNCGatewayClient` 配置键 + `TVNCReadSelfDeviceId`，percent-encoding）；
  - 未配置网关 → 居中引导「去设置」+ 3s 定时轮询，配置出现自动加载；加载失败 → 失败引导 + 重试按钮（`didFailProvisionalNavigation` / `didFailNavigation`，忽略 `NSURLErrorCancelled`）；
  - 幂等重载：`loadedURL` 对比 + `webView.URL` 判断，网关配置变更（切 Tab 回）自动重载；`cleanup` 幂等释放（stopLoading + 清空页面 + 停轮询）。
- **TRMainTabBarController Tab2**：`TVNCControllerViewController` → `TVNCConsoleWebViewController`（.h/.m 注释同步）。
- **Info.plist**：新增 `NSAppTransportSecurity → NSAllowsLocalNetworking=true`（局域网明文 HTTP；裸 IP 本豁免 ATS，真机若仍拦再升 NSAllowsArbitraryLoads）。
- **删除原生实现（7 文件）**：`TVNCControllerViewController.h/.m`、`TVNCViewerViewController.h/.m`、`TVNCDeviceListCell.h/.m`、`ViewerWeb.html`。**TVNCAppStore 保留**（连接页 Hero 卡在用）；TVNCGatewayClient 保留。
- **pbxproj 四步**：容器 BuildFile(AA..C3) + FileReference(AA..C1/C2) + Group children + 主 target Sources 已注册；被删 4 类引用（BuildFile/FileReference/Group/Sources/Resources 双 target）全部移除。`scripts/check-pbxproj.py` 静态校验 **ALL OK**（括号平衡 / 无孤儿引用 / 删除零残留 / 容器四步齐全）。`build.yml` 诊断 grep 标签同步为容器类。
- **验证方式**：Windows 无法本地编译 iOS → 推送 fork/release 触发 Actions（bootstrap scheme 编译 app 子工程，可抓出 pbxproj/编译错误，吸取 #101 教训）。真机验证要点：①控制 Tab 加载网关 H5（含自身设备不显示）；②未配置网关显示引导；③断开网关显示重试；④iOS14 兼容；⑤ATS 是否拦截（备选 NSAllowsArbitraryLoads）。

### 8.6 设备墙遗留链路清理（2026-08-11 · 方案 A：H5 hash 门控唯一拉图）

> **决策（用户确认）**："控制端也用网页端了，变化拉图是网页基本能力"——`screen_changed` 上报 → 网关记录 → `changedSince` 增量拉图这条为**原生设备墙**设计的链路整体裁掉，H5 的 hash 门控（`screen.hash` 变化才 `screenshot`）恢复为唯一拉图机制，静止零图片流量目标达成。

| 端 | 清理项 | 状态 |
|---|---|---|
| Web | 删 `pollWallChanged`/`fetchWallShot`/`startWallChangedPoller`/`stopWallChangedPollerIfIdle`/`wallChangedTimer`/`suppressHash`（Phase C 区块整体删除）；hash tick 恢复唯一拉图 | ✅ app.js v28 |
| 网关 | 删 `screen_changed` 消息处理（`screenChangedAt` 不再产生）；devices.json 清残留字段 | ✅ 已重启生效 |
| IPA | `TVNCGatewayClient` 删 6 个死方法（`fetchChangedDevicesSince`/`invokeCap`/`pingDevice`/`fetchCapsForDevice`/`fetchConfigSchemaForDevice`/`setConfig`）+ 私有 `fetchDeviceCapsPath`，仅保留 `fetchDevicesWithCompletion` + 3 配置属性 | ✅ |
| IPA | `TVNCAppStore` 公开 API 瘦身：`deviceDirectory`/`lastDirectoryFetchedAt`/`refreshDeviceDirectory`/`isRegistered` 移出 .h（内部逻辑保留，Registered 判定是连接页状态机核心） | ✅ |
| IPA | 删死文件 `TVNCViewController.h/.m`（旧启动弹窗，无实例化点）+ `TVNCFormatLastSeen`（服务设备墙卡片，零调用）+ Main.storyboard `customClass` 清理 + pbxproj 双 target 引用移除 | ✅ |
| 设备端 | `TRGatewayClient.mm` 删 `screen_changed` 上报链：`kLocalScreenCtlPort` 常量 / `_connectScreenSubscribe` 订阅 / `screen.event` 解析分支 / `_reportScreenChanged` 上报方法（9 处，含 subFd 全部引用） | ✅ 待打包 |
| 设备端 | 删 `screen.subscribe` 全部残留：trollvncserver 46752 命令分支 + `tvCtlBroadcastToSubscribers` + TRCapabilityRegistry 注册 + TRScreenHasher 订阅实现（6 ivar/init/方法/h 声明）；tvCtl 基础设施（gTvCtlSubscribers/tvCtlAddSubscriber/tvCtlBroadcastChanged）与 clients.subscribe 保留 | ✅ 待打包 |

**验证**：`node --check` 全过；`scripts/check-pbxproj.py` ALL OK（143 id）；`verify-container-mode.mjs` 9/9；`verify-mobile-layout.mjs` 14/14 无回归；网关已重启（8080/18081/18181 监听）。
**遗留（Phase 1.5 可选）**：`MOCK_COUNT=30` 无条件注入 mock 设备 → 改 URL 参数运行时开关；`GET /api/devices/:id/caps` H5 无消费者（保留作对外契约）；`caps.js` 头部注释与实现不符（`deviceCaps` 实从设备对象读）待修正。
**注意**：设备端上报链删除需重打包 IPA 生效；App 侧死代码清理同样并入下一次打包。

### 8.7 全链路冗余复核（2026-08-11 · 确认+修复+待清理清单）

> 用户要求确认"时序之外无重复冗余实现"。三端全量扫描后结论：**核心链路已干净**，但发现 1 处构建隐患（已修）与若干可选清理项（待决策）。

**已修复（构建隐患，必须）**：
- **pbxproj 设备 target 残留 TVNCViewController 孤儿引用 3 处**（Group children L244-245 + Sources L425）——上一轮并行编辑丢失更新遗留，缺 FileReference 定义会导致 bootstrap 编译失败（#101 同类）。已删除；**自检脚本升级**：`check-pbxproj.py` 的 defined 集合改为"所有 section 行首 ID"，孤儿检查只看非行首引用（此前把引用行误算为定义导致漏检），重跑 **ALL OK（140 定义、零孤儿）**。

**确认干净（时序链路无重复）**：
- 画面获取：唯一 = H5 hash 门控（screen.hash → 变化才 screenshot）；增量通道/事件上报链已整体删除
- 命令下发：唯一入口 = `sendDeviceCmd`（隧道 FT_CMD 优先、注册通道回退，属宪法 7.4 回退设计非冗余）；REST 专用端点（invoke/configs/restart/ping）为 H5 主用
- 设置页：唯一在用 = prefs/PSListController 体系（Root.plist）；能力执行：唯一 = TRCapabilityRegistry executor（route 仅元数据）

**待决策清理项（可选）**：
| 项 | 现状 | 建议 |
|---|---|---|
| ~~iOS 死类 4 个~~ | **已执行**：删 `TVNCSettingsViewController.h/.m` `TVNCSettingFormController.h/.m` `DpkgVersion.h/.m`（app 版）+ 4 类 pbxproj 全部引用（BuildFile/FileReference/Group/Sources 双 target）；`TaskProcess+ObjC.swift` **仅移除 pbxproj 引用、文件保留**（src symlink 被 Makefile L105 编译引用，删文件会悬空破坏 trollvncmanager 构建）；prefs 版 DpkgVersion 悬空 symlink 已删；TRTask.m/TRCapabilityRegistry.mm 过时注释已更新 | ✅ |
| ~~设备端 `screen.subscribe` 残留~~ | **已执行**：trollvncserver 命令分支 + `tvCtlBroadcastToSubscribers` + TRCapabilityRegistry 注册（含 `_inferCategoryForCapId` 前缀）+ TRScreenHasher 订阅实现（h 常量/typedef/方法声明、mm 6 ivar/init/方法）全删；tvCtl 基础设施与 clients.subscribe 保留 | ✅ 待打包 |
| ~~网关 `POST /api/devices/:id/command`~~ | **已执行**：端点删除；register-test.js 4 处调用改造（invoke home 验证 ack / configs set / 超时与离线 504 用 invoke） | ✅ |
| mDNS/manual 设备支持 | 可见但不可控（前端 register 硬门槛）；mDNS 发布 `_superphone-farm` 供 App 搜索网关有用 | 保留（设计取舍） |
| `/ws/control/:id` + `GET /api/devices/:id/caps` | 仓库内无消费者，属 AI 工具对外契约 | 保留（契约） |

**验证（三项清理执行后）**：`check-pbxproj.py` ALL OK（127 id、零孤儿）；`node --check` server/index.js + register-test.js 通过；**`npm test` 全绿**（smoke/tunnel/register/dedupe/caps/pending-replay 6 个套件）；`verify-rfb-fix.js` 7/7、`verify-bidirectional.js` 6/6。另修复 tunnel-test.js 既有失败：`viewOnly 上行转发` 断言须在 ctrl 加入前执行（同设备单活跃会话机制会顶掉 viewOnly 4001）+ ctrl 验证前清空旧 FT_DATA 帧（nextFrame 不消费匹配帧）。
**注意**：`TaskProcess+ObjC.swift` app 版保留（src symlink 依赖）；`screen.subscribe` 删除需重打包 IPA 生效；tunnel-test 的 viewOnly 断言调整源于既有测试缺陷（与本次清理无因果，验证时定位修复）。
