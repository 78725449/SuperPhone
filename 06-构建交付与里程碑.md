# 06 - 构建交付与里程碑

> **来源**：IPA改造计划.md 行 1827-1912（## 3. 构建与交付路径 + ## 4. 明确不做 + ## 5. 风险与已知边界 + ## 6. 里程碑）
> **关注点**：构建与交付路径、明确不做清单、风险与已知边界、里程碑总表
> **索引**：[返回主文档](IPA改造计划.md)

---

## 3. 构建与交付路径

```
改源码（TrollVNC 仓库） → push 到 fork(li78725449-ship-it/TrollVNC)
  → GitHub Actions "Build TrollVNC" → 下载 packages-bootstrap 产物
  → 得到 TrollVNC_<ver>.tipa → TrollStore 安装 → 真机按 Phase 验收
```
- 每次改动以独立 commit 推进，便于回滚
- UI 文案走 zh-Hans
- 服务器侧改动走 `trollvnc-farm` 仓库，本地 `npm test` + 冒烟测试验证后再上软路由

---

## 4. 明确不做（内网自用边界）

| 项 | 说明 |
|---|---|
| 注册鉴权 | 内网可信，不设设备鉴权；网关侧 FARM_TOKEN 兜底 |
| TLS / 公网加固 | frp 只做传输，不额外加固 |
| 多租户 / 审计 / 计费 | 不做商业化 |
| 安装 App | 远程装 App 不在范围内（如需另立项目） |
| Managed.plist 远程下发 | Managed.plist 保持静态预置；配置通过命令通道 set 动态调整 |

> **注意**：以下能力已在 Phase 4 实现，不再属于"不做"范围：
> - ~~命令通道下发~~ → Phase 4.2 已实现（set/invoke/restart）
> - ~~远程配置下发~~ → Phase 4.3 已实现（configs API）
> - ~~远程启停服务~~ → Phase 4.2 已实现（restart 命令）

---

## 5. 风险与已知边界

| 风险 | 应对 |
|---|---|
| iOS 后台挂起 | 真机实测存活时长；沿用预启动逻辑；文档写明"重启后可能需打开一次" |
| mDNS 不可达 | 初始化支持手动输入网关 IP 兜底 |
| 私有 API 随 iOS 升级失效 | 锁定 TrollStore 支持范围，升级前灰度 |
| 网关与 IPA 协议不一致 | Phase 0 先定契约，两边各自自测后再联调 |
| 远程 set 后本地 UI 不同步 | 4.4 热重载监听 NSUserDefaults 变更，同步 UI 显隐 |
| 批量操作影响多台设备 | restart/stop 类二次确认；invoke 类按需确认 |
| **v1.6 新增：shake 空实现 BUG** | `STHIDEventGenerator.shakeIt` 方法体为空，Batch 0 修复（实现加速度计注入或移除） |
| **v1.6 新增：screenshot 空壳（已修复）** | 注册返回 code:99，Batch 0 已修复（调用 ScreenCapturer `captureSingleFrameImage` 真正实现） |
| **v1.6 新增：clipboard.paste 合并影响** | 合并为 clipboard.get 后，已调用 paste 的客户端需改用 get；ACK 返回格式变化 |
| **v1.6 新增：restart 统一入口兼容** | 统一为 service.restart 后，网关 cmd `restart` 保留兼容（双入口均可调用） |

> **v1.6 移除**：原"反向模式与 HTTP 互斥"风险已随反向模式废弃而移除。

---

## 6. 里程碑

> **v1.6 新增**：能力开发编排批次（Batch 0-5），详见 [能力追踪清单.md](能力追踪清单.md) 第 0.3 节。里程碑与批次对应关系如下。

| 里程碑 | 内容 | 状态 | 对应批次 |
|---|---|---|---|
| M1 | Phase 0 完成（网关注册/心跳 + mDNS + 冒烟测试） | ✅ 已完成 | — |
| M2 | Phase 1 代码完成（UUID/初始化/注册心跳/命令通道v2/restartHandler） | ✅ 代码完成；待真机联调 | — |
| M3 | Phase 2 UI 重构完成（模式化分组 + 网关配置 + 去营销） | ✅ 已完成 | — |
| M4 | Phase 3 web 兜底体验完成（自动连接 + 悬浮按钮 + 双端适配） | ✅ 已完成 | — |
| M5 | Phase 3.5 三端布局按钮归位 | ✅ 已完成 | — |
| M6 | Phase 4.1 能力注册框架（13 控制能力 + 34 配置 schema） | ✅ 已完成 | — |
| M7 | Phase 4.2 命令通道 v2（query/set/invoke/restart） | ✅ 已完成 | — |
| M8 | Phase 4.3 配置全量上报（34 字段 + 密码安全） | ✅ 已完成 | — |
| M9 | Phase 4.6 网关 REST API（单台 + 批量） | ✅ 已完成 | — |
| M10 | Phase 4.7 前端自动渲染（元数据驱动 + 配置面板分区） | ✅ 已完成 | — |
| **M10.5** | **P0 修复空壳/BUG/合并**（shake 移除/screenshot 修复/clipboard.get 合并/service.restart 统一注册） | ✅ 已完成 | **Batch 0** |
| M11 | Phase 4.4 配置热重载（tvReloadConfigForKey + g* 变量更新 + framebuffer 重建 + Watchdog/HID 属性设置） | ✅ 已完成 | — |
| **M11.5** | **P1 即用即注册**（59 项能力 + 4 项配置注册到 TRCapabilityRegistry） | ✅ 已完成 | **Batch 1** |
| M12 | Phase 4.5 本地端口代理（clients.* 能力暴露 + localcmd 桥接器 + subscribe 推送） | ✅ 已完成 | **Batch 2** |
| **M12.5** | **P3 真正实现**（type.paste/blocked.list 控制端口扩展） | ✅ 已完成 | **Batch 3** |
| **M12.6** | **P4 补声明注册**（touchDownAtPoints 等 .h 声明 + 4 项能力注册） | ✅ 已完成 | **Batch 4** |
| M13 | Phase 4.8 AI 工具 WS 接口（流式控制） | ✅ 已完成 | — |
| **M13.5** | **P5 扩展增强**（系统查询/ScreenCapturer 扩展/网关客户端 14 项） | ✅ 已完成 | **Batch 5** |
| M14 | Phase 5 IPA 控制端面板改造（libvncclient → WebSocket + noVNC） | ✅ 已完成（v1.8 隧道接入已补齐） | — |
| **M15** | **Phase 6 反向连接与营销彻底清理**（删除 trollvncserver.mm 反向连接残留 + TVNCRootListController.m support/source 方法 + 本地化字符串） | ✅ 已完成 | — |
| **M16** | **Phase 7 18181 隧道落地**（TRTunnelClient + 网关 tunnelServer + 三端接入 + 命令通道复用隧道） | ✅ 已完成 | — |
| **M17** | **Phase 8 配置 schema 补齐与枚举合并收尾**（6 项缺失 schema + Notifications/PerformanceMode 合并） | ✅ 已完成 | — |
| **M18** | **Phase 9 三端 UI 规范统一**（移动网页 4 列网格改垂直列表 + 卡片墙改 invoke screenshot API） | ✅ 已完成 | — |
| **M19** | **Phase 11.5 IPA 首页连接页改造**（Hero 卡 3 行 + 扫描直连 + 客户端卡右滑冻结/断开） | ✅ 已完成 | — |
| **M20** | **Phase 12 IPA 控制端 UI 重构**（TabBarController + 导航栏 + 双视图 + 批量配置 + ⚙配置入口 + 12.1 卡片墙恢复完整 RFB） | ✅ 已完成（v2.3） | — |
| **最终卡点** | 真机联调（M2 验收 + M15-M20 实现 + 全链路验证） | 需实体 iOS 设备 | — |

> **批次依赖**：Batch 0（修复）→ Batch 1（即用注册，依赖修复完成）→ Batch 2（桥接，依赖注册表）→ Batch 3（实现，依赖桥接）→ Batch 4（补声明，独立于 2/3）→ Batch 5（扩展，依赖 Batch 1）。

---
