# 04 - UI 交互规范

> **来源**：IPA改造计划.md 行 216-268（0.4）+ 行 610-643（Phase 5）+ 行 792-836（Phase 9）+ 行 1913-2145（Phase 11.5/12）
> **关注点**：UI 界面与交互规范、IPA 控制端面板改造（noVNC）、三端 UI 规范统一、IPA 首页改造、IPA 控制端 UI 重构
> **索引**：[返回主文档](IPA改造计划.md)

---

### 0.4 UI 界面与交互规范（2026-08-11 容器化后定版）

> **核心原则**：能力菜单统一为**纵向列表分组菜单**（按 category 分组），差异仅在于触发方式和图标/名称的显示形式。
> **架构变化（2026-08-11）**：IPA 控制端**整 Tab 替换为 WKWebView 容器**（`TVNCConsoleWebViewController`）加载网关 H5 手机控制台——原生设备墙（TVNCControllerViewController）与大屏（TVNCViewerViewController）**已删除**，所有交互组件统一由网页端（H5）承担；改 Web 端即生效（serveStatic no-cache + app.js?v=N），App 容器自动同步。
> **能力与菜单统一（2026-08-12，07 §3/§4）**：控制台操作菜单收敛为**按键区（KEY_DEFS 12 按键对象 + PressRecognizer 按压识别）+ 动作区（5 项）**，不再全量渲染能力；**触控从菜单移除改画布直操**；管理/查询类（secondary）移入卡片 ⋯ 菜单；**配置入口移至卡片 ⋯ 菜单设备管理段（⚙ 设备配置）**，控制台菜单无配置按钮；批量菜单过滤触控/截图/文本/剪贴板。

#### 0.4.1 界面定位

| 界面 | 定位 | 卡片墙 | 大屏操控 |
|---|---|---|---|
| PC web 控制台 | 多设备墙 + 大屏操控 | **H5 hash 门控变化拉图**（screen.hash 比对，变化才 screenshot，静止零请求） | 屏幕右侧**纵向分组菜单图标列** |
| 手机 web 控制台 | 多设备墙 + 大屏操控 | 同上 | **悬浮按钮** → 纵向列表能力分组菜单 |
| IPA 控制端（控制 Tab） | **WKWebView 容器加载网关 H5 手机控制台** | 同上（网页端承担） | 同上（网页端承担） |
| 5801 电脑访问 | 单设备直连（无卡片墙） | — | **右侧操作栏**（静态按键栏，按钮抽为 keyDefs 常量渲染 + 按压识别长按/双击，JS 贴画布右缘；caps.js 增强标题） |
| 5801 手机访问 | 单设备直连（无卡片墙） | — | 全屏 + **悬浮按钮** → 按键操作面板（同上 keyDefs 渲染） |

#### 0.4.2 卡片墙交互（点击卡片前）

| 界面 | 卡片操作入口 | 菜单样式 |
|---|---|---|
| PC web 控制台 | 卡片右下角 `⋯`（或右键） | **纵向列表分组菜单**（能力按 category 分组 + 设备管理操作） |
| 手机 web 控制台 | 卡片 `⋯` | 同上 |
| IPA 控制端 | 网页端承担（容器内即手机 web） | 同上 |

> **关键规则**：卡片墙菜单 = **能力分组（menuCaps('tile')：primary 按键/动作 + secondary 管理查询，排除 internal）+ 设备管理段（查看/控制、详情、编辑、测试在线、⚙ 设备配置、删除）**，能力按 category 分组排列；**⚙ 设备配置为唯一配置入口**（07 §4.2/§7，控制台菜单无配置按钮）。

#### 0.4.3 大屏操控交互（点击卡片后）

| 界面 | 触发方式 | 菜单样式 | 配置入口 |
|---|---|---|---|
| PC web 控制台 | 屏幕右侧固定 | **按键区（KEY_DEFS 12 按键对象+按压识别）+ 动作区（5 项）**（仅图标，无分组标题） | 无（在卡片 ⋯ 菜单设备管理段） |
| 手机 web 控制台 | **悬浮按钮** → 点击展开 | 同上按键区+动作区（图标+名称） | 无（在卡片 ⋯ 菜单设备管理段） |
| IPA 控制端 | 网页端承担（容器内即手机 web） | 同上 | 同上 |
| 5801 电脑访问 | 固定显示 | **右侧操作栏**（keyDefs 静态按键：Home/电源/音量/亮度/键盘/剪贴板/适配/全屏/断开，按压识别长按/双击） | 剪贴板手动浮层（HTTP 降级） |
| 5801 手机访问 | **悬浮按钮** → 点击展开 | 同上按键 + 收起/断开 | 同上 |

> **关键规则**：
> - PC web 大屏：**按键区+动作区图标列**（仅图标，07 §4.1）；5801 电脑：**keyDefs 静态按键操作栏**（非分组渲染）
> - 手机 web / IPA 控制端 / 5801 手机：**悬浮按钮** → **纵向列表菜单**（图标+名称）
> - **配置入口统一在卡片 ⋯ 菜单设备管理段（⚙ 设备配置）**，控制台操作菜单无配置按钮（07 §4.2/§7）
> - 点击悬浮按钮展开/收起

#### 0.4.4 卡片墙帧获取（2026-08-11 定版）

- ~~所有控制台卡片墙建立完整 RFB 连接（viewOnly 实时渲染）~~（v2.3 旧设计，已弃用）
- **当前唯一机制：H5 hash 门控变化拉图**——每设备 `invoke screen.hash`（0.3ms 级轻量 pHash）与 `lastHash` 比对，**变化才 `invoke screenshot` 拉帧渲染**；静止时零图片流量
- **双速退避**：变化 1s 快检 / 静止 base×1.5^silent 至 15s 封顶（base=ThumbInterval 默认 5s）
- IPA 端原生卡片墙（TRWallTileWebView / 原生 hash 轮询）**已整体删除**，由控制 Tab 容器加载网页端承担（Jetsam 闪退根治）
- 点击卡片进入大屏操控时切换为可操控 RFB 连接（通过隧道，ctrl 会话）
- 离线检测：RFB 连接断开/失败标记卡片"不可达"

#### 0.4.5 批量操作入口

- 网关设备墙顶部「批量操作」栏 + PC 端「直控」按钮
- 勾选多台设备后显示批量能力按钮 + 批量配置按钮 + 批量重启（二次确认）
- **三端分工**：电脑端 = 调用能力 + 调整配置 + 批量重启；手机端 / IPA（容器内即手机 web）= 仅调整配置
- restart/stop 类操作需二次确认弹窗
- **实现状态**：批量能力列表按 `menuCaps('batch')` 过滤渲染——**按键类 + 管理类（已过滤触控/触控笔/截图/文本/剪贴板，07 §4.3）**；端侧差异（手机端仅调整配置）见文末 §待办

---

## Phase 5：IPA 控制端面板改造（libvncclient → WebSocket + noVNC）⚠️ 已废弃（2026-08-11 容器化替代）

> **废弃说明**：原生 Viewer（`TVNCViewerViewController`）与 Controller（`TVNCControllerViewController`）**已整体删除**（2026-08-11），大屏操控由控制 Tab `TVNCConsoleWebViewController`（WKWebView 容器加载网关 H5）承担。本节保留仅作历史参考。

**目标**：将 IPA 控制端面板从 libvncclient 改为 WebSocket + noVNC，与网关/Web 统一技术栈。

> 用户确认：独立为 Phase 5，不纳入 Phase 4 范围。
> **v1.8 修正后状态**：v1.7 代码骨架已完成，v1.8 Phase 7 补齐隧道接入，Phase 9 补齐卡片墙 invoke screenshot。

### 5.1 Viewer 改造 ✅ 已完成

**文件**：`app/TrollVNC/TrollVNC/TVNCViewerViewController.m`

- 移除 libvncclient 依赖（含 SASL 链接桩）✅ 已完成
- 改用 WebSocket 连接网关 `/ws/vnc/:id` 或直连设备 5801 ✅ 已完成（Phase 7 补齐隧道模式：useGatewayTunnel 属性，跨网络走网关 8080 桥接，局域网保留直连 5801）
- noVNC 前端复用网关 web 已有的 `core/rfb.js` ✅ 已完成
- 容器法旋转（`canvasContainer`）**已移除**（与 Phase 3.5 一致，不做画面旋转）✅ 已完成

### 5.2 Controller 改造 ✅ 已完成

**文件**：`app/TrollVNC/TrollVNC/TVNCControllerViewController.m`

- 设备列表拉取走网关 `/api/devices`（已有）✅ 已完成
- 缩略图获取改用 WebSocket + RFB 第一帧捕获（替代 libvncclient）✅ 已改为 invoke screenshot API
- 设备列表过滤自身 deviceId ✅ 已完成
- **跨网络隧道模式接入** ✅ 已完成（Phase 7：按设备来源 register/mdns/manual 自动判断 useGatewayTunnel）

> **v1.8 修正后状态**：web 端卡片墙 invoke screenshot 改造已完成（Phase 9）。

### 5.3 能力操作集成

- 控制端面板的操作按钮复用 Phase 4.7 的元数据驱动渲染 ✅ 已完成
- 横竖屏切换保持（Phase 3.5 已实现）✅ 已完成

---

## Phase 9：三端 UI 规范统一（v1.8 新增）✅ 已完成（2026-08-11 修订）

> **2026-08-11 修订**：9.2「卡片墙 invoke screenshot API」已演进为 **hash 门控变化拉图**（见 0.4.4）；9.3「IPA opsMenu」随原生控制器删除而不再适用（容器内即手机 web）。

**目标**：移动网页 4 列网格改垂直列表；卡片墙改用 invoke screenshot API。
**前置**：Phase 7 完成（卡片墙走隧道后才能用 invoke screenshot）。
**反推依据**：见 0.B 表 D。

### 9.1 网关移动端 opsMenu 改造

**文件**：`trollvnc-farm/web/style.css` + `trollvnc-farm/web/index.html`

| # | 改动 | 说明 |
|---|---|---|
| 9.1.1 | style.css 行 179 `.ops-menu-grid` 改为 `display: flex; flex-direction: column; gap: 2px` | 与 5801 直连 `#panel` 一致 |
| 9.1.2 | `.ops-menu-grid button` 样式调整 | 横向布局：图标 + 名称（左对齐），min-height: 44px |
| 9.1.3 | index.html 行 83-84 验证 | 确保 `opsMenuCap` 与底部配置按钮在垂直列表中正确排列 |
| 9.1.4 | 移动端 FAB 行为 | 点击展开垂直列表，再次点击收起（沿用现有 JS 逻辑） |

### 9.2 卡片墙改用 invoke screenshot API

**文件**：`trollvnc-farm/web/app.js`

| # | 改动 | 说明 |
|---|---|---|
| 9.2.1 | 删除 `createWallTile` 中 `createRfb` 调用 | 行 112 `inst.rfb = createRfb(tv, d, { viewOnly: true })` |
| 9.2.2 | 改为定时调用 `invokeCap` 触发 `screenshot` 能力 | 复用 Phase 4.6 网关 invoke API，返回 base64 JPEG 后填入 `tv` 容器 |
| 9.2.3 | 帧率控制 | 卡片墙默认 1 帧/5 秒（可配置），focus 视图保持完整 RFB |
| 9.2.4 | 离线检测 | screenshot 连续失败 3 次标记卡片"跨网络不可达"，但保留局域网直连提示 |
| 9.2.5 | 防抖与串行化 | 与 IPA 控制端 Controller 一致：串行队列 + 8 秒去重 |

### 9.3 IPA 控制端 opsMenu 复用规范

**文件**：`TrollVNC/app/TrollVNC/TrollVNC/TVNCControllerViewController.m`

| # | 改动 | 说明 |
|---|---|---|
| 9.3.1 | 检查现有 opsMenu 实现 | 若已是垂直列表（与 5801 直连一致）则无需改动；否则同步改造 |
| 9.3.2 | FAB 拖动行为 | 沿用 Phase 3 FAB 实现（已完整） |

**验收**：
- 网关移动端 opsMenu 显示为垂直列表（图标 + 名称），不再是 4 列网格
- 卡片墙显示截图帧而非完整 RFB 连接，性能显著改善
- 三端 UI 规范统一：PC 网页图标列 / 移动网页垂直列表 / IPA 控制端垂直列表

---

## Phase 11.5：IPA 首页（连接页）改造计划（v2.1 定版，2026-08-09）

> **状态**：✅ 已完成（v2.2 标记，2026-08-09）
>
> **背景**：当前 IPA 首页（TVNCConnectViewController）已具备三卡片结构（Hero + 扫描直连 + 客户端），但信息层级混乱、UDID 无意义、状态显示弱、客户端卡无右滑操作。经用户与助手多轮讨论，定版如下。

### 11.5.1 整体布局（三卡片垂直滚动）

```
TVNCConnectViewController（UIScrollView + UIStackView 垂直布局）
├─ 1. 状态卡片（TVNCGradientCard 渐变紫色）
├─ 2. 扫描直连卡（白色卡，QR 码 + URL）
└─ 3. 客户端卡（白色卡，计数 + 全部断开 + 客户端列表）
```

### 11.5.2 状态卡片（精简为 3 行直观信息）

| 行 | 内容 | 说明 |
|---|---|---|
| 第一行 | 设备名（UIDevice.name）+ 🟢已连接/🔴未连接 | 设备名来自"设置→通用→关于本机→名称"；右侧状态点+文字 |
| 第二行 | 连接状态文字 | 已连接："已注册到网关，隧道已建立" / 未连接："未注册到网关" |
| 第三行 | 服务状态文字 | 已连接："VNC 服务运行中 · N 个客户端在线" / 未连接："VNC 服务未运行 · 请前往设置配置网关" |

**删除项**：
- ~~UDID 显示~~ → 移到设置页诊断区
- ~~右上角状态胶囊~~ → 与状态卡片重复，统一在状态卡片显示

### 11.5.3 扫描直连卡（保持现状）

- 标题"扫描直连"（已从"扫码直连"修正）
- QR 码（110×110，http://IP:HttpPort）
- URL 文字
- 提示"内网扫码即连"

### 11.5.4 客户端卡（增强 + 右滑操作）

**布局**：
- 标题"客户端" + 在线/冻结计数 + "全部断开"按钮
- 客户端列表（UIScrollView 自然滚动，无折叠，无"查看全部"按钮）

**客户端显示**：
- 在线客户端：正常显示，🟢在线点
- 冻结客户端：灰色卡片，🟠冻结点，不清除

**右滑操作（互斥逻辑）**：

| 客户端状态 | 右滑按钮 | 行为 |
|---|---|---|
| 🟢 在线 | 冻结🟠 + 断开🔴 | 冻结→灰色卡片 / 断开→卡片自动清除 |
| 🟠 冻结 | 仅解冻🟢 | 解冻→释放卡片（客户端下次自动注册） |

**状态机**：
```
在线 🟢 ──右滑──→ [冻结🟠] [断开🔴]
                     │        │
                     ↓        ↓
                  灰色卡片   清除
                     │
                  右滑──→ [解冻🟢] → 释放卡片
```

**底层能力**：
- 冻结：clients.freeze
- 解冻：clients.unfreeze（=释放，解除限制）
- 断开：clients.disconnect

### 11.5.5 涉及文件

| 文件 | 改动 |
|---|---|
| `TVNCConnectViewController.m` | 状态卡片精简（删 UDID、改 3 行状态）；客户端卡加右滑操作（UISwipeActionsConfiguration）；删除右上角状态胶囊 |
| `TVNCConnectViewController.h` | 接口调整（如需） |

### 11.5.6 不改动的部分

- 整体 ScrollView + StackView 垂直布局（已合理）
- TVNCGradientCard 渐变样式（视觉好）
- TVNCClientListController 内嵌（已合理）
- 扫描直连卡布局（已合理）

---

## Phase 12：IPA 控制端 UI 重构（v2.1 新增，2026-08-09）⚠️ 已归档（2026-08-11 容器化替代）

> **归档说明**：12.1-12.10 的原生实现（TabBar 导航 / 双视图 / 批量流程 / FAB 菜单等）已随容器化**整体删除**，由控制 Tab WKWebView 容器加载网关 H5 承担。其设计约束（位置稳定 / 布局两档 / 批量流程 / 批量过滤规则）已迁移至 0.4 与 §13 的 web 端规范，见 12.11。

> **背景**：Phase 10 完成后，核查发现 IPA 控制端 UI 仍沿用上游遗留结构（设置页为主入口 + 控制端为次要入口），与新愿景"装一次初始化一次，之后零操作"不符。
>
> **设计讨论成果**：用户与助手经多轮讨论，确定了 IPA 控制端 UI 的全新设计方向，本节为待实现任务清单。
>
> **v2.3 修正**：v2.2 曾因误读 v1.8.3 愿景将 12.1 标记为"放弃"，现确认 v1.8.3"卡片墙只获取帧"为**已弃用的旧设计**，12.1"卡片墙恢复完整 RFB 连接"才是新方向。v2.3 已执行 web 端改动（startWallThumbnail → createRfb viewOnly），Phase 12 全部完成。

### 12.1 帧获取机制修正（P0）✅ 已完成（v2.3 执行）

**问题**：Phase 9/10 将卡片墙改为 invoke screenshot API 轮询，但用户本意是"控制前端渲染帧率"，不是"不建立完整 RFB 连接"。

**修正方案**：
- 卡片墙恢复用完整 RFB 连接（旧 createRfb 方法，viewOnly 只读）
- 帧率阈值（ThumbInterval）改为可选的 RFB 帧渲染频率控制（可有可无，默认不限制）
- invoke screenshot 仅保留为大屏操控时的单帧截图能力

**执行结果**（v2.3）：
- ✅ IPA 端已用 `TVNCFetchThumbnailViaRFB`（RFB 首帧获取，libvncclient），符合新方向
- ✅ web 端已改回 `startWallRfb`（createRfb viewOnly 持久连接），删除 `startWallThumbnail`/`refreshThumbInterval`/`stopWallThumbnail` 三个旧函数

**涉及文件**：
- `trollvnc-farm/web/app.js`：startWallThumbnail → startWallRfb（createRfb viewOnly），删除 screenshot 轮询三函数
- ~~`TrollVNC/app/TrollVNC/TrollVNC/TVNCControllerViewController.m`~~：✅ 已是 RFB 首帧获取，无需改动

### 12.2 TabBarController 导航重构（P0）

**问题**：当前设置页（TVNCRootListController）是主入口，控制端（TVNCControllerViewController）是次要 push 入口，本末倒置。

**方案**：
- 新增 `UITabBarController` 作为根控制器
- Tab 1 "控制"：TVNCControllerViewController（卡片墙，升为主入口）
- Tab 2 "设置"：TVNCRootListController（配置，降为次要入口）
- AppDelegate/SceneDelegate 改根控制器为 TabBarController

**涉及文件**：
- 新建 `TRMainTabBarController.h/.m`
- `AppDelegate.m` 或 `SceneDelegate.m`（改根控制器）

### 12.3 顶部导航栏设计（P0）

**布局**（位置稳定原则——模式切换只变内容不移动）：

```
[全选☑] 设备墙              [批量操作] [宫格▾]
```

| 元素 | 默认状态 | 批量模式 | 位置 |
|---|---|---|---|
| 全选 | 隐藏（占位） | 紫色☑ | 最前 |
| 标题 | "设备墙" | "已选 N 台" + 提示 | 紧随全选 |
| 批量操作 | "批量操作"（紫底白字） | "取消"（白底灰字） | 右侧固定 |
| 布局切换 | 宫格图标+▾ | 宫格图标+▾ | 最右固定 |

**关键规则**：所有右侧操作组件位置固定，模式切换只变内容不移动。

**涉及文件**：
- `TVNCControllerViewController.m`（重写 navigationItem）

### 12.4 chips 标签去除 + 布局切换器（P0）

**问题**：当前顶部 chips "全部/直连/中继"中"中继"已废弃，"直连"语义错误。

**方案**：
- 去除 chips 标签（无需过滤）
- 布局切换器：44px 窄按钮，仅图标+▾
- 下拉菜单 2 个选项：宫格 / 列表
- 宫格选中后默认 2 列显示，双指捏合调整列数
- 列表固定单列行布局

**涉及文件**：
- `TVNCControllerViewController.m`（删除 chips UI + 新增布局切换器）

### 12.5 双视图实现（P0）

**视图 1：宫格列表（默认）**
- 完整 RFB 连接显示画面
- 默认 2 列，双指捏合调整列数（2/3/4/6）
- 卡片底部栏：设备名 + ⋯按钮
- 点击画面 → 进入大屏操控
- 点击右下角⋯ → 单台设备调整设置（actionSheet 能力分组）
- 长按卡片 → 进入多选模式

**视图 2：详情列表**
- 每设备一行，左侧小画面缩略图（40×40），右侧设备信息
- 设备信息：IP + iOS 版本 + 在线状态
- 任务状态字段（AI 任务预留）：空闲 / 任务中 / 排队 / 失败
- 离线设备显示"最后在线时间"
- 点击行 → 进入大屏操控
- 点击⋯ → 单台设备设置
- 长按行 → 多选模式

**涉及文件**：
- `TVNCControllerViewController.m`（双视图 UICollectionView 布局切换）
- 新建 `TVNCDeviceListCell.h/.m`（详情列表 cell）

### 12.6 批量配置流程（P1）

**流程**（按钮触发，非自动显示）：
1. 浏览模式（默认）：顶部右侧"批量操作"按钮
2. 点击"批量操作" → 进入多选模式（全选☑出现 + 标题变"已选 N 台" + 卡片出现 checkbox）
3. 勾选设备后，底部出现"调整配置（N 台）"按钮
4. 点击底部按钮 → 弹出配置表单（按 reload 分区显示）
5. 下发后自动退出多选模式回到浏览

**三端批量能力分工**：
- 电脑端控制台：调用能力 + 调整配置 + 批量重启
- 手机端控制台：仅调整配置
- IPA 控制端：仅调整配置

**涉及文件**：
- `TVNCControllerViewController.m`（批量模式状态机 + 底部按钮 + 配置表单）

### 12.7 tagLabel 删除（P1）

**问题**：当前卡片 `tagLabel.text = @"直连"` 写死，语义错误（所有设备都走网关）。

**方案**：删除 tagLabel。

**涉及文件**：
- `TVNCControllerViewController.m`（删除 tagLabel 相关代码）

### 12.8 大屏⚙菜单补齐配置入口（P1）

**问题**：当前 ⚙ 菜单只有能力分组 + 结束控制，无配置入口，违背愿景"列表底部⚙按钮"。

**方案**：在 `rebuildGearMenuWithCaps` 末尾"结束控制"前插入"设备配置"UIAction，push TVNCSettingsViewController。

**涉及文件**：
- `TVNCViewerViewController.m`（rebuildGearMenuWithCaps 新增配置项 + showConfigPanel 方法）

### 12.9 任务状态字段预留（P2）

**目标**：为后期 AI 任务拓展预留。

**字段**：`空闲` / `任务中` / `排队` / `失败`

**实现**：详情列表 cell 右侧显示任务状态，当前全部显示"空闲"，AI 任务接入后动态更新。

### 12.10 帧率阈值可选配置（P2）

**目标**：控制前端 RFB 渲染频率（可有可无）。

**方案**：ThumbInterval 配置项保留，作为 RFB 帧渲染频率控制。默认不限制（实时渲染），配置后按阈值渲染。

**涉及文件**：
- `trollvnc-farm/web/app.js`（RFB 帧渲染节流）
- `TVNCControllerViewController.m`（IPA 帧渲染节流）

### 12.11 容器化取代（2026-08-11 · 替代 Phase 5/12 全部原生实现）

**方案**：IPA 控制端**整 Tab 替换为 WKWebView 容器**加载网关 H5 手机控制台——「本质与操作网页端一致，什么能力都便于后续开发」。

| 项 | 内容 |
|---|---|
| 新增 | `TVNCConsoleWebViewController.h/.m`：URL 构造（`?container=ipa&token={Token}&selfId={DeviceUUID}`）、未配置引导 + 3s 自动轮询、加载失败重试、网关变更幂等重载、cleanup 释放 |
| 配置 | `Info.plist` 加 `NSAppTransportSecurity → NSAllowsLocalNetworking`（局域网明文 HTTP） |
| 替换 | `TRMainTabBarController` Tab2「控制」= 容器 VC |
| 删除 | `TVNCControllerViewController.h/.m`、`TVNCViewerViewController.h/.m`、`TVNCDeviceListCell.h/.m`、`ViewerWeb.html`、`TRWallTileWebView.h/.m`、`WallTileWeb.html`（原生卡片墙 / 大屏 / RFB 缩略图全删） |
| 保留 | 连接页（TVNCConnectViewController）/ 设置页（prefs）/ 客户端列表（TVNCClientListController）/ TVNCAppStore / TVNCGatewayClient |
| 收益 | 改 Web 端即生效（serveStatic no-cache + app.js?v=N），App 容器自动同步；后续能力开发只改网页端 |

**涉及文件**：
- `app/TrollVNC/TrollVNC/TVNCConsoleWebViewController.h/.m`（新增，pbxproj 四步齐全）
- `app/TrollVNC/TrollVNC/TRMainTabBarController.m`（Tab2 换容器）
- `app/TrollVNC/TrollVNC/Info.plist`（ATS）

---

## 待办（文档声称 vs 当前实现差异）

| 项 | 设计规则 | 当前实现 | 状态 |
|---|---|---|---|
| 批量调用能力过滤 | 仅显示硬件按键、系统与设备、服务管理类（过滤触控类/触控笔类/截图类） | [caps.js `menuCaps`](trollvnc-farm/web/caps.js#L204-L221) 的 `batch` 场景过滤（internal/触控/触控笔/截图/文本/剪贴板） | ✅ 已完成（2026-08-12，07 §4.3） |
| 手机端批量菜单 | 手机端 / IPA 仅「调整配置」，无「调用能力」按钮 | `showBatchMenu` 未区分 PC/手机，手机端同样显示调用能力 | ⏳ 待补 |

---

## Phase 13：卡片墙增强 + 同步控制 + 直控模式（PC web 控制台，2026-08-11）✅ 已完成

> **背景**：PC 控制台卡片墙经多轮迭代形成三块能力——尺寸阈值动态化、同步控制（群控）、直控模式（免大屏直达控制）。本节沉淀交互规范与约束，供将来反查追溯。

### 13.1 卡片墙尺寸模型

- **阈值动态化**：`computeCardWBounds()` 按容器宽度反推——每行 5 列取最大 px（动态）、固定最小 110px（约 15 列）、固定默认 175px（约 10 列），滑块范围实时自适应。
- **比例自适应**：`--tile-pb`（padding-bottom 百分比）驱动卡片高宽比；默认按**多数设备屏幕比例**分组取值（0.01 精度），横/竖屏手动切换覆盖。
- **变化驱动**：`devicesSignature()`（在线设备 id + 屏幕尺寸）变化才重算比例，避免每次轮询重排。
- **虚拟预览设备**：`MOCK_COUNT=30` 注入 `.tile-mock`（SVG 等比占位），置 0 关闭；虚拟设备仅预览布局，不可控制/批量/同步。

### 13.2 同步控制（群控广播）

- 入口：聚焦大屏操作列顶部「🔗 同步」按钮；**两态**：激活变红（`sync-active`），图标恒为 🔗（不变 ✕）。
- 点击按钮进入同步选择模式：点卡片即切换同步（无复选框），选中态 = 边框高亮 + 中央「同步中」徽标（不遮挡推流、不可触控）。
- 同步设备建立 `grp=wallSession&viewOnly` RFB 订阅：卡片实时推流（显示同步执行情况）+ 作为广播接收成员；**主控始终携带 `grp+broadcast`** → 勾选/取消同步不重建主控连接，主控画面零跳动。
- 仅聚焦状态下可用；主控设备本身不可同步；离线设备不可同步（提示）。
- 退出聚焦/退出同步模式：关闭全部同步 RFB 订阅 → 恢复截图轮询。

**PC 大屏操作列（2026-08-12 能力与菜单统一修订）**：
- 顶部按钮顺序：`← 返回` → `🔗 同步` → 能力图标列 → `hr` → 适配/全屏/断开；**返回按钮由头部移入操作列顶部**。
- 能力图标列 = **按键区（KEY_DEFS 12 按键对象，挂 PressRecognizer 按压识别）+ 动作区（5 项）**（renderCapOps + menuCaps('console')，07 §4.1）；适配/全屏/断开为控制台本地操作，由静态按钮提供。
- 头部紧凑：`padding:3px 10px; font-size:13px`，仅状态圆点 + 标题（无返回按钮）；`prefitFocusPanel` 头部高度按 26px 计算。
- 操作列**去除分组标题**（`.focus-ops-cap .cap-group-title { display:none }`，移动端悬浮菜单标题保留）；能力按钮仅图标（`.cap-name` 隐藏）。
- 操作列滚动条主题色：Firefox `scrollbar-color: var(--line)`；WebKit 6px 圆角 + 悬停 accent（不再白色）。

### 13.3 直控模式（免大屏直达控制）

**入口与两态**：批量操作按钮**左侧**「直控」按钮；激活 = 主题蓝高亮（`direct-active`）+ 显示设备数 `直控 (N)`；再次点击退出恢复原色。

**行为**：
- 进入 = 为**全部在线真实设备**（`source=register`，排除虚拟/离线）建立**非 ctrl 可输入 RFB**（互不抢占、输入直达设备 5901）；新上线设备自动补建。
- 直控期间：悬停放大、悬停引导提示「点击进入控制」失效；点击卡片**直达 RFB 控制**（canvas 输入由 noVNC 处理），不聚焦、无引导。
- 退出 = 关闭全部直控 RFB（**必须真正 close WS**，防残留）→ 恢复截图轮询（变化帧采样）。

**互斥规则**：
- 直控时聚焦与悬停效果失效；
- 同步仅在非直控、聚焦后才有；
- 批量操作不受影响。

**画面适配**：直控/同步推流 canvas 由 noVNC `scaleViewport` 等比缩放，**居中**放置（`left:50%; top:50%; translate(-50%,-50%)`），与截图缩略图 `object-fit:contain` 的显示位置一致（模式切换无跳变，四周留黑边）。

**断开处理**：单设备直控断开（离线/隧道断/服务端断开）→ 清理该设备直控标记 → 恢复截图轮询 → toast 显示断开码（4001/4002/4005 等）便于诊断。

**服务端硬约束（同设备单会话）**：设备端仅 1 条隧道 + 1 个 5901 连接，**同设备同时只允许 1 个活跃会话**——`handleVncSocket` 新会话建立时顶掉同设备其他非 ctrl 会话（4001），随后按"首会话"触发 5901 重建保证干净握手。多会话共享 5901 会导致协议串流（noVNC `Unexpected server message (type N)` 断开）。

### 13.4 手机端 H5 布局规范（对齐 IPA 控制端，2026-08-11）

> **原则**：手机端 H5 顶部导航与 IPA 控制端保持一致——**仅「批量操作」+「布局切换」两个操作组件**；PC 端继承的不适合触屏操作的组件在移动端全部隐藏。

**顶部导航（≤900px）**：
- 仅保留：`布局切换`（窄样式图标按钮 + ▾）+ `批量操作`（两态：进入多选→显示选中数/取消）。
- 隐藏：卡片滑杆（.zoom）、刷新、直控、+添加设备、设备统计（.meta）。

**布局切换器**：
- 两档菜单：`卡片`（宫格）/ `列表`（单列行），借鉴 IPA 布局按钮（窄样式仅图标+▾）。
- 按钮图标随选中项切换：grid=四宫格 / list=三横线。
- 布局状态 localStorage `farm_layout` 持久化；**PC 端（≥901px）恒为卡片宫格**（布局按钮 PC 端隐藏）。

**两视图**：
- 卡片视图：grid 固定 2 列，卡片填满列宽（`width:auto`，grid item stretch）；`--tile-pb` 比例自适应继续生效。
- 列表视图：单列行式，固定行高 64px = 48px 缩略图（.tv）+ 设备信息行（.tile-bar：状态点+名称+⋯+状态文字）+ 批量 checkbox（左上角）。

**移动端布局错误规避（已修复的坑）**：
- `#workspace` 移动端必须 `align-items:stretch`——`flex-start` 在 column 布局下使 `#wall` 宽度收缩为内容宽（390px 视口实测 242px），卡片被压小。
- 移动端卡片宽禁止沿用 PC 端 `--card-w`（滑杆值）——`computeCardWBounds` 在 390px 视口会算出 115px 小卡片；应 `width:auto` 填满 grid 列。

**涉及文件**：
- `trollvnc-farm/web/index.html`：布局菜单改 2 项、`#layoutIcon` 动态 SVG
- `trollvnc-farm/web/app.js`：`applyLayout('grid'|'list')`、`LAYOUT_ICONS`、PC 端恒 grid（L1785-1880）
- `trollvnc-farm/web/style.css`：移动端 header 精简、`#wall.wall-list` 行式、`#workspace` stretch、卡片 `width:auto`（L306-352）
- `trollvnc-farm/test/verify-mobile-layout.mjs`：移动端 14 项 UI 验证

**涉及文件**：
- `trollvnc-farm/web/app.js`：`startDirectRfb/toggleDirectMode/exitDirectMode/updateDirectBtn`（L1009-1107）、`toggleSyncMode/toggleSync/setSyncBadge`（L876-986）、`computeCardWBounds/majorityTilePb/applyAutoTileRatio/devicesSignature`
- `trollvnc-farm/web/style.css`：`.direct-bar/.direct-active`、`.sync-btn.sync-active`、`.tile canvas` 居中、`#wall` 滚动条隐藏
- `trollvnc-farm/server/index.js`：`handleVncSocket` 同设备会话唯一化（L331-343）
- `trollvnc-farm/test/verify-sync-ui.mjs`：13 项 UI 回归验证（卡片渲染/批量两态/直控激活/聚焦/同步红态/离线提示/返回墙）

---
