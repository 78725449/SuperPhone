# 设备操作 Agent 时序设计（实现意图完整记录 v2）

> 提炼自 2026-08-16 DSH 集成设计讨论。v2 重写：补入设计**灵魂**（哲学叙事）与完整细节。
> 本文档回答一个核心问题：**"一次任务如何变成全群的能力"** —— 项目方的完整实现意图。
>
> ---
>
> ## ★ 文档定位（2026-09-18 入库时确立）
>
> **本文是 AI 操作层的【主干设计】** —— 下列内容**以本文为准**：
> **角色分层**（人/群控/导演/演员/引擎）· **完整时序 A→E** · **资产结构**（skill 卡/动作库/trace/汇报）·
> **关键机制 12 条** · **结构契约表** · **引擎适配现状（§十一）**。
>
> **与《架构总纲-AI操作层-2026-09-17.md》的关系**：那一份是本文主干下的**【感知与执行层细化】**
> （四级验证 / 页面表 / 等待策略 / 工具面现状 / 真机坑清单）。**★ 冲突时以本文为准**（主干优先）；
> 总纲只做补充，**不得改本文的结论**。
>
> **★ 入库缘由（必须记住的教训）**：本文原存于仓库外（`D:\编程项目\设备操作Agent时序设计.md`），
> **长期处于"无人知晓"状态** —— 导致 2026-09-17 的后续设计（总纲）在不知情的情况下**重复造轮子**
> （例："锚点图从哪来"在本文 §11.5 早已定稿，而总纲作者 2026-09-18 仍在"发现"这个问题）。
> **纪律：设计文档必须住在仓库里** ✗ 仓库外 = 会丢 = 会重复劳动。

---

## 一、设计哲学（灵魂，10 条）

1. **任务即技能建设（核心叙事）**
   "派发一次任务，子代理就为此任务以编写自动化脚本为目标执行，驱动 UI 视觉代理解决问题，解决以后方案就定了" —— 每次任务不只是完成任务，而是开采一座技能矿：**产出的核心资产是可复用的自动化脚本/skill，不是"这次做完了"**。

2. **踩坑经验优先于智能（运行哲学）**
   "沉淀的是 skill：具备解决踩坑经验的 skill；后续再执行时踩坑可以第一时间查历史对照解决方案" —— 运行时**第一步是检索经验**（纯检索、零 LLM），命中→套解法→过闸门；只有历史没有答案时才动用模型。

3. **能力稀缺观（触发哲学）**
   "100 胜老方案跑不通的情况下才会触发子代理接入派发新的试错方案；没有实现时探索和执行失败介入才是能力" —— 智能是稀缺资源，按触发条件分配：**高胜率方案失效才升级、无方案才探索**；其余时间走确定性路径。

4. **子代理是导演不是演员（角色哲学）**
   完整角色切换链："子代理派发识别 UI 的子 agent 跑一遍（探索）" → "跑不通的我们子代理再去掉 UI 执行层代理去调试（下潜）"。子代理负责：派活→监督→剥离视觉层亲自调试→沉淀→广播，**不亲自逐步操作**。

5. **民主化复用（社区哲学）**
   "解决以后方案就定了，全域广播此任务能力，其他的子 agent 也能复用" —— 一次突破全群受益；广播是**系统级机制**，不是可选功能。

6. **动作数据库（资产哲学）**
   "历史成功或者失败方法都是分类外挂的，动作区分开，相当于是动作数据库" —— 成败经验是**独立于流程的资产**（全局一份、分类外挂）：一处改进，所有引用它的 skill 自动受益。

7. **演进账本（完整性哲学）**
   "方式更新等演进历史也要在 skill 中做成表在 skill 里沉淀" —— 每个资产自带**完整族谱**（创建/修复/广播/回滚每一笔都有账），审计、回滚、固化判定全部有据可查。

8. **时效性排序（界面敏感哲学）**
   "最新跑通，权重到顶，然后是历史跑通次数排列权重" —— 界面会漂移（改版/反爬/布局变化），**"最近验证过"的价值高于"很久以前验证过很多次"**；失败时间同样对称计数。

9. **动态演化（在线学习哲学）**
   "第一次执行时动态编写脚本就好了？根据执行结果调整权重以及降级链" —— 组合脚本**按需现场生成**（无预编译缓存）；权重、触发阈值都**随执行结果自我调节**；链结构固定、参数自适应。

10. **中间产物即规则（契约哲学）**
    "子代理的规则实际上就是驱动视觉 ui 模型沉淀 skill 出来中间的步骤产物" —— 子代理的规则围绕**中间产物流**（步骤/修复/验证事件）定义；中间产物先定下来，规则、沉淀、审计都服从同一份事实。

---

## 二、系统叙事（一句话故事）

> 群控会话一句话下任务 → 每个设备子代理领到任务包 → 先查历史（skill 命中即回放；未命中派发视觉代理探索）→ 踩坑先查经验对照、查不到且高胜率方案全败才动用智能修复 → 走通的路径提炼成意图脚本、坑沉淀成失败经验、变更记进演进账本 → 闸门验证 → 灰度 → 全域广播 → 其他设备子代理从此直接复用。
>
> **每一个任务都在为全群开采一座技能矿；智能只在矿脉断层处出现。**

---

## 三、角色与分层

| 角色 | 是谁 | 职责 |
|---|---|---|
| 人 | 需求方 | 群控对话下指令、@设备、验收结果 |
| 群控 agent | DSH 主会话 | 理解需求、枚举设备（kind×group）、定位/派生子代理、下发任务包、合并各设备汇报 |
| **设备子代理（导演）** | 每设备一个 DSH 会话（可开窗口） | 任务理解、查资产、派发视觉代理、监督、剥离视觉层下潜调试、沉淀、转正申请、汇报 |
| UI 视觉代理（演员） | 被派发的执行单元 | 观察（截图/树）→ 模型决策 → 动作，循环直到达成；UI 专项模型驱动 |
| 引擎（L0/L1/L2） | DSH 之外的运行时 | 确定性回放、技能命中、组合脚本执行、约束护栏 |

**子代理是导演**：它不亲自逐步操作设备；它编排"哪个演员上场"（视觉代理）、"何时换角"（剥离视觉层自己调试）、"何时收工"（沉淀）、"何时报功"（转正申请）。

---

## 四、完整时序（A→E，详细展开）

### 阶段 A · 群控编排

```
1. 人（群控对话）："@所有手机，把这些视频发到抖音，各账号单独处理"
2. 群控 agent：提取三要素
     goal（达成什么）· 目标设备（kind×group 过滤）· 成功判定（可断言状态）
3. 群控 agent：为每个目标设备定位已有子代理 / 派生子代理（每设备独立会话 —— 失败不连坐、可开窗口观察）
4. 群控 agent：下发任务包 { goal, 上下文, 成功判定 }
→ 各设备子代理并行进入阶段 B
```

- 边界：设备离线 / 缺能力 → 该设备直接阻塞，不硬试、不拖累其他设备
- 交互落点：导航页面的群控经 agent 编排后，向各子代理窗口下发会话（用户可见：左侧分组、子代理窗口）

### 阶段 B · 设备执行

```
B0 查资产（零成本决策点）：
   query_skill(goal, platform)
   ├─ 命中 → B1 回放      （历史已验证的路径，直接走）
   └─ 未命中 → B2 探索    （第一次，需要人演）

B1 回放（有 skill，确定性路线）：
   1. 按 skill.success_path 逐条意图步骤执行（只用意图动词）
   2. 每步 query_action 取该动作最高权重方案 → 直接执行（零模型）
   3. 每步后按 expect 断言校验 —— 断言不过 = 该步失败 → 阶段 C
   4. 全部通过 → 完成 → 阶段 D

B2 探索（无 skill，试探路线）：
   1. 子代理派发 UI 视觉代理：session.observe（截图/树）→ 模型决策 → session.execute → 循环
   2. 每步记 trace（动作/结果/截图 —— 沉淀的唯一原料，漏一步少一块拼图）
   3. 达成后把实际步骤提炼成意图序列（只许意图动词，跨设备可复用）
   4. 产出：候选 skill 卡（draft）+ 路径每个动作的方案（动作库，试用期）
```

### 阶段 C · 降级链（失败时，顺序固定）

失败定义：某一步 expect 断言不过。**智能消耗逐级递增，逐级才动用**：

```
① 次优方案（确定性）：本动作 plans 内降级到下一权重方案重试
② 组合脚本（枚举）：历史全部成功方案按权重序现场编译成回放脚本，
     逐一执行 + expect，命中即停
     —— 串行 + 失败退避 + 密度限额（多账号场景：风控是真实成本，宁可慢不可错）
③ 介入调试（智能）：子代理剥离视觉层，携带每方案失败诊断
     （截图 diff / 异常 / 断言差异）下潜 DOM/CDP 层定位，产出替代方案
     —— 触发条件严格：100 胜老方案跑不通 / 组合脚本全败，才到此步
④ 阻塞：连续修复失败 ≥2 次 → 停止，发阻塞汇报（禁止无限重试）
```

### 阶段 D · 沉淀（执行完成必做，不做 = 任务白做）

```
1. skill 卡写/更新：
   - success_path：意图动词序列（设备做法一律不进 skill）
   - verification：可重放断言（B 阶段的 expect 即原料）
   - failure_patterns[platform]：本次踩的坑（symptom / root_cause / fix / hits+1）
       按平台分桶 —— 浏览器踩的坑不套手机
   - evolution 账本追加一行 { rev, type: create|fix|broadcast|regress,
        source: task#…, gate, status }
2. 动作库更新：每步方案 wins++ / fails++，重算 weight（排序规则见七-8），刷新时间戳
3. 固化判定：
   - 单次成功 → 保持 draft（不造一次性资产）
   - 同一目标重复 ≥2 次 → 转正申请（阶段 E）
```

### 阶段 E · 转正申请（让资产服务全群）

```
1. 闸门：目标设备重放 success_path，全部 expect 通过 —— 不过闸门不申请
2. 灰度：推 1 台设备重放验证，通过
3. 上报 promote_request —— 子代理只申请，全群广播由上层执行
→ 广播后：其他设备子代理 query_skill 命中即回放，权重持续累计（闭环）
```

---

## 五、每次执行汇报（四态，证据驱动）

汇报是 trace 的人读投影：从事件聚合、不重写事实；**只在三个时刻发**（完成/阻塞/转正申请）；执行细节留在子代理会话窗口，群控需要时可见。

- `success`：全过、零降级 → 结果 + 资产更新，一句话
- `success_with_repair`：中途降级但成功 → **repairs 段是主角**（新坑已沉淀，最有价值的汇报）
- `partial`：部分子目标完成 → 完成/未完成清单 + 继续/停止建议
- `blocked`：连续修复失败 → **诊断包快速直达**（设备/任务/失败 intent/已试方案/建议）—— 唯一要响亮汇报的状态

成功判定永远来自断言（assert + passed + evidence），不自我宣布。

---

## 六、结构契约（已约束）

| 结构 | 约束内容 |
|---|---|
| 任务包 | { goal, 上下文, 成功判定 } |
| skill 卡 | id / platforms / status / preconditions / success_path[意图] / verification / failure_patterns[platform] / evolution[] |
| 动作库条目 | key = intent:target@platform；plans[] { method, 参数, stats{wins,fails}, avgMs, weight, last_success, last_fail, probation } |
| trace 事件 | step / tool_call·tool_result / repair / verification（NDJSON，逐行） |
| 汇报 | 四态 JSON（见五） |
| 意图动词表 | navigate / open / find_and_click / input_text / scroll / back / home / wait_for / expect |

---

## 七、关键机制详解

1. **沉淀物 = skill**：success_path（骨架）+ verification（验收）+ **failure_patterns（踩坑经验：现象→根因→解法，这是 skill 的灵魂）**
2. **踩坑检索优先**：失败 → 提取错误特征（截图 diff/日志/DOM 缺失/屏幕哈希）→ 检索 failure_patterns（同平台优先）→ 命中套 fix 过闸门（零新增 LLM 调用）→ 未命中才走降级链
3. **skill 更新 = 双通道**：快通道（fix 成功即追加候选，仅本 worker 可见）/ 慢通道（同坑重复 ≥2 次升级广播）；版本化 + 回滚；更新唯一入口 = 真实失败
4. **跨设备统一**：意图级 skill + 动作解析器；观察面 dom→a11y→uia→visual 分层；经验按 platform 分桶
5. **动作数据库**：经验与流程分离，全局一份、分类外挂、众筹共享
6. **中间产物先定**：四类事件是规则/沉淀/审计的统一事实源；evolution 账本在 skill 内
7. **多方案 + 权重**：一动作多方案，成败统计驱动
8. **权重排序**：试用期排后 → 最近成功最新优先 → 最近失败对称降权/连续失败摘牌 → wins 降序 → avgMs 升序；复合 weight÷avgMs
9. **介入触发**：连续失败 ≥N 或 plans 全败；构造上禁止主动接管；每小时限额；频繁触发 = 报警信号
10. **组合脚本**：动态现场编译（无预编译缓存）、命中即停、串行+退避+密度限额
11. **终态 = 函数 + 档案**：成功方案固化可执行函数（内嵌验证+结构化抛错）+ 档案（统计/版本/evolution）；泛化合并，不每成功造一个
12. **元技能三层**：元技能（教建技能）→ 业务 skill 实例（被建资产）→ 动作库（共享经验）

---

## 八、界面与交互（用户设想形态）

1. 导航入口：侧边栏新增"云手机""指纹浏览器"
2. 群控页：@全部设备 → 广播任务
3. 每设备独立子代理会话窗口（可打开观察执行过程）
4. 左侧分组：设备会话按"浏览器/云手机"分组展示（对齐现有分组能力，任务/定时区域扩展）
5. 右侧浮窗：设备实时画面（shell.overlay 加法座）

## 九、交付物与边界

```
DSH 插件（编排面）：设备注册表 + 任务级工具桥 + 画面浮窗 + 群控入口
元技能 dsh-device-skill：设备子代理执行手册（SKILL.md + evals，开发中）
引擎接口：devices 注册 / submitTask 任务级 / trace 流回流（待定稿）
设备层：browser adapter（VirtualBrowser 已有）· phone adapter（蓝图）· cloudpc adapter（未来）
```

边界：DSH 只做任务级编排（禁止逐步动作暴露给 DSH 模型）；skill/动作库/权重属于引擎域（独立于 DSH 会话）；长任务状态活在引擎 checkpoint。

## 十、已落地 & 待办

**已落地**：`dsh-device-skill/SKILL.md`（执行手册 + 附录 A-E）、`evals/evals.json`（3 用例）、`设备操作Agent时序设计.md`（本文档 v2）、anthropics/skills 已装 DSH。

**待办**：① dsh-device-skill dry-run 对照测试迭代 ② 引擎任务接口定稿 ③ DSH 插件原型（注册表+工具桥+浮窗）④ 云手机 adapter 实现、云电脑预留。

---

## 十一、引擎适配现状（两系统能力映射 · 2026-08-28 审视定稿）

### 11.1 执行层能力对照（与设计映射）

| 设计需求 | VirtualBrowser（browser，已有） | SuperPhone（phone，已有/缺口） |
|---|---|---|
| 结构化观察 | `getDOMSnapshot`（DOM）✅ | **无 a11y 树**（TrollStore 无注入能力，见 11.4 ③）⚠️ |
| 视觉兜底 | `screenshot` ✅ | 截图（RFB 帧）✅ |
| navigate | `launchBrowser` + CDP ✅ | `app.open` ✅ |
| find_and_click | DOM 选择器 + CDP ✅ | `touch.tap`（坐标 0-1）+ **视觉定位（待建，11.4）** |
| input_text | CDP ✅ | `type.paste` ✅ |
| scroll | CDP ✅ | `touch.swipe` ✅ |
| wait_for / expect | DOM 轮询 ✅ | **`screen.hash/diff/waitStable`（pHash，原生）✅** |
| 探索循环 | MCP `session.observe/execute` + `recordSteps` ✅ | 控制端逐条 invoke（trace 记在子代理侧）✅ |
| 群控编排 | `/api/tasks` + `groups` ✅ | farm `batch/*` ✅ |

**结论**：两系统执行原语已齐，缺的只是"手机端视图定位层"（11.4）与"手机端脚本执行器"（11.3）。

### 11.2 执行层架构决策：指令模式 vs 脚本模式（WDA 范式）→ 混合

| 模式 | 何时用 | 执行形态 | 理由 |
|---|---|---|---|
| **指令模式**（逐条 invoke） | **B2 探索** | 控制端在环：下发 tap → 回截图 → 再决策 | 每步需观察+决策，必须人在环 |
| **脚本模式**（WDA 范式） | **B1 回放 / C2 组合脚本** | 下发 steps[] → **手机连续执行** → 回传结果 | 路径确定，无中间决策 |

**决策：混合。** 探索走指令（视觉代理逐步看），回放走脚本（确定性批量）。脚本模式收益：① 断链不丢任务（脚本已在手机，结果缓存+重连补齐回传，对齐 farm 心跳/退避哲学）；② 进程内零网络往返（每步直接调注册表 IOHID）；③ 原子回传（{每步结果, trace, 断言}，一步失败整体中止）；④ 风控护栏（退避/密度限额）可在执行器内自控。

> 与 WDA 的差别：WDA 用 XCUITest（TrollStore 装不了），我们用已有 IOHID 注入链路 —— 底层更硬。

### 11.3 `script.exec` 契约草案（SuperPhone 新增 B 类能力，三处补齐）

```json
{ "steps": [
    { "op": "find_and_click", "target": {"type":"ocr","text":"发布"} , "expect": {"hashDiff": true, "timeoutMs": 5000} },
    { "op": "input_text", "text": "标题" },
    { "op": "wait_for", "expect": {"hashDiff": true, "timeoutMs": 10000} },
    { "op": "expect", "assert": "出现'发布成功'" }
] }
```

- 执行器 = 解析 steps → 进程内逐条调注册表 → 聚合 `{每步结果, trace, 最终断言}`
- `op` 含现有控制能力（touch.*/type.paste/app.open…）+ 视图定位（11.4）+ 等待断言（screen.hash）
- **决策永远在 DSH 子代理**（编译意图序列→steps），执行在手机 —— 边界不变
- 回放脚本内容（find_and_click 怎么解析成坐标）由子代理按动作库方案编译，非手机自思考

### 11.4 视图定位三件套（vision.*，SuperPhone 待建）

| 能力 | 实现 | 用途 | 状态 |
|---|---|---|---|
| `vision.ocr` | **Vision framework VNRecognizeTextRequest**（系统框架，中文离线，零集成） | 文本清单 → 喂 find_text / 云端语义 | ✅ 首选（价值/成本最高） |
| `vision.find_text` | OCR + 匹配 → bbox 中心 | 文本类目标定位 | ✅ 首选 |
| `vision.find_image` | **自实现归一化互相关**（Accelerate vDSP）+ 降采样金字塔 + ROI 约束；**不引 OpenCV**（体积/编译不值） | 纯图标目标（锚点图匹配） | ⚠️ 次选 |
| `vision.tree` | a11y 树：root daemon dlopen AXRuntime 走 SpringBoard AX 服务（**实验线**） | 结构化元素 | ❌ 研究项，不阻塞主线 |

**`vision.find_image` 原生实现（2026-08-28 定稿，零外部依赖）**：

- **技术选型**：全系统框架 —— vImage（降采样金字塔/灰度化）+ vDSP（互相关/归一化/求峰）；BNNS（模板=卷积核，可选替代路径）；Metal（GPU 计算着色器，大规模可选）。**无 OpenCV**（体积 50~100MB + Theos 编译复杂，只为滑窗匹配不值）；iOS 无公开 matchTemplate API，自实现是正路
- **流程**：① vImage 转灰度 + 降采样 8×（1170×2532→146×317，粗匹配≈4.5 万像素）② 模板翻转（vDSP_conv 方向问题）③ `vDSP_conv` 输出相关图 ④ **NCCC 归一化**（局部均值/方差，亮度/反色变化不失效）⑤ `vDSP_maxvi` 找峰 → 映射回原分辨率 ⑥ 粗定位 ±50px ROI 原尺寸精匹配确认
- **性能**：粗匹配微秒级 + ROI 精匹配毫秒级，全流程 **<20ms**；Accelerate/CoreGraphics 在 daemon 进程直接可用（跑在 `trollvncserver`，截图帧缓冲在手零拷贝）

**控件树诚实结论**：TrollStore 只能装签名 IPA、无注入能力 → 拿第三方 App 的 a11y 树基本不可行；唯一可试暗线是 root daemon 走私有 AX 框架（实验性、iOS 版本敏感）。**务实替代：OCR+模板匹配组"伪树"**（文本/图标 = 元素+坐标），业界云真机同样如此 —— "树优先"哲学的手机版现实：**树上不了，OCR 就是半棵树**。

- 所有 vision.* 放 `trollvncserver` 进程（截图帧缓冲在手，零拷贝）
- 性能：OCR 全屏 200~500ms（回放每步一次可接受）；find_image 金字塔+ROI 后十毫秒级
- 实施顺序：**① OCR → ② 模板匹配 → ③ 树（实验）**

### 11.5 锚点图生命周期闭环（find_image 的锚点从哪来、怎么死）

```
B2 探索：视觉代理决定点(x,y) → ① 裁剪周边小块=锚点图草稿 → ② tap
         → ③ expect 校验 → ④ 通过才存进动作库函数档案 anchor 字段
         （失败不存；单次成功留候选，重复≥2 转正广播 —— 与 skill 卡同规则）
B1 回放：手机端 find_image(anchor) 匹配 → tap
换皮失效：匹配不到 → 降级链（组合脚本 → 云端识图兜底 → 重新定位）
         → 重新裁剪 → 更新档案 → 记 failure_patterns —— 又一次踩坑-修复-沉淀
```

锚点类型分工：有文字→文本锚（OCR，改版文字还在就找到）；纯图标→锚点图（模板匹配）；双保险（先 OCR 后图匹配，再云端兜底）—— 与动作库"多方案+权重"同构。

### 11.6 引擎侧缺口清单（实现前要补的）

1. **SuperPhone 新增 `vision.*` 三件套**（OCR 先行）—— B 类能力三处补齐
2. **SuperPhone 新增 `script.exec` 脚本执行器**（steps 解析 + 聚合回传 + 断链缓存）
3. **动作库函数档案加 `anchor` 字段**（锚点图/文本锚，跨两系统通用契约）
4. VirtualBrowser 侧可选：MCP `session.recordSteps` 记录正式化为 trace 事件回传（已具备雏形）