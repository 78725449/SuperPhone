# WiFi 订阅模型对齐方案（2026-09-06）

> 状态：**待用户审阅定案**。定案后实施，一个 commit + 56 轮 CI + 部署。
> 此文档是本次改动的唯一执行依据。改动前先审阅，确认后按方案逐条实施。

---

## 一、问题定义：wifi 显示不是“事件”，是“订阅”

当前代码把 wifi 状态栏当作“收到 evt 就显示、未收到就未连接”的事件驱动，但用户定义的语义是**订阅模型**——与 GPS 定位同级：

| 维度 | GPS 定位订阅 | WiFi 连接订阅 | 对齐 |
|---|---|---|---|
| 能力是什么 | locationd 持续广播当前位置 | configd 记录当前连接 AP（SSID/BSSID） | 同源：系统级常驻数据 |
| 从哪来 | 系统定位服务，常驻 | 系统配置服务（SCDynamicStore），常驻 | 同源 |
| 怎么来 | `startUpdatingLocation` 一次订阅 → 回调 | `SCDynamicStoreSetNotificationKeys` 一次订阅 → SDS 回调 → UDS 推 App | 同构：一次订阅，变化推送 |
| 什么时候变 | 注入坐标变化 / 真实 GPS 变化 | BSSID 变化（设备漫游/软路由切换） | 同构：变化才推，不变稳定 |
| 没变时能持续显示吗 | ✅ 蓝点停在最后 fix 上 | ✅ 状态栏停在最后推送的 SSID/BSSID 上 | **应同构，但当前当事件处理** |
| 与状态无关 | ✅ 播放/停止/驻留同一蓝点 | ✅ 统一渲染器已实现 | ✅ |
| 变化时触发反查 | —（GPS 无需反查） | BSSID 变化 → wloc 反查 → 更新水滴 | ✅ 独立逻辑，与显示分离 |

**当前缺口**：`_renderWifiStatusBar` 把 `lastWifiBSSID == nil` 统一显示为“WiFi: 未连接”——但 nil 有两种不同含义，混为一谈：
- ① UDS 未连接（App 没连上 daemon）→ 应显示“未连接”（诚实降级）
- ② UDS 已连接但首个 evt 还没到达 → 应显示“同步中…”（与 GPS 定位“等待首个 fix”同态）

---

## 二、修复方案

### 方案 A：`_renderWifiStatusBar` 状态区分（App 侧，~5 行）

```objc
- (void)_renderWifiStatusBar {
    if (!self.lastWifiBSSID) {
        // 订阅模型的三态区分（2026-09-06 架构对齐）：
        // ① UDS 断线 = 定位服务的"定位服务不可用"，诚实显示未连接
        // ② 已连等待首次数据 = 定位的"等待首个 fix"，显示同步中
        self.wifiDiagLabel.text = (self.simUDSFD < 0)
            ? @"WiFi: 未连接"
            : @"WiFi: 同步中…";
        return;
    }
    // ... 正常三态渲染（反查中/定位坐标/反查失败，不变）
}
```

**语义变化**：打开 App 时，如果 UDS 通但 evt 还没到，用户看到的是“同步中…”而不是“未连接”——“同步中”是订阅子系统的正常初始化状态，不和“当前连接丢失”混淆。

### 方案 B：全状态同步原语（daemon 侧，一次性替换所有恢复点，~15 行）

```objc
/// 全状态同步推送（2026-09-06 架构收敛）：执行态回执 + 当前连接状态同时推送——
/// 推 state：mode/seq/position（App 对齐 locating 与 UI）
/// 推 evt=bssid：当前 SSID/BSSID（App 渲染状态栏与水判）
/// 所有恢复点统一走此入口，保证 wifi 显示与执行态同步，不再有"某条路径漏推"的缺口
+ (void)pushFullStateSync {
    SimLocationController *sc = [SimLocationController sharedController];
    [SimLocationController pushSimStateToApp];
    [sc _handleWifiStoreChanged];
}
```

**替换所有恢复点**（当前零散推送）：

| 恢复点 | 当前代码 | 替换为 |
|---|---|---|
| **UDS accept** | `[self pushSimStateToApp]; [[self sharedController] _handleWifiStoreChanged];` | `pushFullStateSync` |
| **UDS 命令处理尾部**（play/stop/anchor/query） | `[self pushSimStateToApp]; [[self sharedController] _handleWifiStoreChanged];` | `pushFullStateSync` |
| **播完复位**（`_trackTick` 播完分支） | `[SimLocationController pushSimStateToApp];`（**缺推 bssid**） | `pushFullStateSync` |
| **空轨迹**（`_startTrack` 空分支） | `[SimLocationController pushSimStateToApp];`（**缺推 bssid**） | `pushFullStateSync` |

**零变动**：心跳（`_wifiSubscribeHeartbeatTick` 已独立推 bssid）、daemon 启动（App 未连时推空管道，无影响）、`_forceStopOnStartup` 恢复后（App 未连，推空管道——UDS accept 会补推）。

---

## 三、同步清理的冗余

| 冗余 | 清理方式 | 原因 |
|---|---|---|
| `pushSimStateToApp` + `_handleWifiStoreChanged` 零散重复 | 统一为 `pushFullStateSync` | 架构对齐，消除重复代码 |
| 播完复位/空轨迹只推 state 不推 bssid | 统一为 `pushFullStateSync` | 订阅模型缺口修复 |
| `_renderWifiStatusBar` 的"未连接"单一态 | 改为三态区分 | 订阅模型语义词面对齐 |

**此前已清理的旧冗余**（本轮 Q1-Q7 + F 系 + 崩溃修复历史中已删除，不再重复动手）：
`_injectSimulationForCurrentLocation`、`_lastParamsSig`/`_paramsSignature`、`_reloadDebounce`、`scheduleReloadFromPrefs`/`reloadFromPrefs`/`applyFromPrefs`、`writeMobilePrefsUsingBlock`、`_teardownWifiStore`、`_refreshWifiAnnoFromCurrentConnection`、`_updateWifiStatusBar`、`commitSimPrefs`、`currentNetworkInfo`/CNCopy、`_wifiTargetBSSID`。

---

## 四、首锚点创建时的完整调用链（含 AP 反查+下发策略）

```
用户操作（搜索/点击地图）→ handleTap / applySearchResult
  → commitAnchor → UDS {"cmd":"anchor", lat, lon}

daemon _simUDSHandleCommand anchor 分支
  → _stopTrack（停播放 tick，防残留）
  → _startAnchorAtLat:lat:lon:acc:
      ├── 设状态变量（_anchorLat/_currentLat/_currentMode=anchor …）
      ├── _injectGpsForCurrentLocation      ← ① GPS 注入（v4 模型：clear→append→flush→**start**）
      ├── setSystemLocationServices:YES     ← ② 开定位（先注入后开，确保第一秒即模拟值）
      ├── _handleAPSwitchForCurrentLocationForced:YES  ← ③ AP 反查+下发
      │     └── _lastWifiTileKey = 0 → 新建基线（F6 首拍抑制：不下发）
      │     └── _lastWifiTileKey != 0 → 正常反查
      │           └── tile 查询（LRU）→ 回调 → _handleAPList → 最近 AP → 下发软路由
      ├── _startMicroWanderWithCenter        ← ④ 微动游走
      └── TVLog("anchor set")
  → _persistState（mode=anchor + 位置 + seq）
  → pushFullStateSync（state + bssid 回执，由本方案统一）

App 收到回执
  → handleSimStateLine: state 行 → locating 对齐 → _renderWifiStatusBar
  → handleSimStateLine: evt=bssid 行 → 渲染 AP + 变化才反查 wloc → 水滴
```

**策略说明**：
- `_handleAPSwitchForCurrentLocationForced:YES` 强制穿透 F6 首拍抑制（显式位置命令必须 AP 跟随）
- 反查 LRU 缓存 32 瓦片，同瓦片零网络开销
- 下发 fire-and-forget（软路由改 macaddr，SSID 不变 = 无感漫游）

---

## 五、路线创建（+ 计划生成）时的完整调用链

```
用户添加第二个锚点 → handleTap → runEdit → commitItinerary
  → syncChainAndGenerate
      ├── holdAtCurrentPosition（UDS anchor 当前位置，驻留不切路由器）
      ├── generateSegment 每段（MKDirections 异步算路 → 回调）
      └── finishChainSync
            ├── 拼接全局 seq（0..N-1）→ joined
            ├── trackVersion++
            ├── writeTrackFile(joined) ← 写队列（串行异步）
            │     ├── _generateBSSIDPlanForPoints(joined)
            │     │     └── 每代表点：queryBssidsForCoordinate（LRU）→ 失败 → queryNearestBssids（螺旋）
            │     │     └── 结果 → 段合并（fromSeq/toSeq/bssid，mutable 字典）
            │     ├── 原子写文件（tmp + rename）
            │     └── if locating → dispatch_async(main) → UDS play（续播）
            └── runPendingEdit（链式编辑）

播放时的下发链：
daemon _startTrack
  → _updateCurrentFromPoint(points[startIdx])  ← 更新当前位置
  → _dispatchBSSIDForSeq(_currentSeq)           ← 计划段下发（首点即走计划，W-B 统一）
  → _injectGpsForCurrentLocation                ← GPS 注入
  → _trackTick 每秒：
      → _updateCurrentFromPoint → _injectGpsForCurrentLocation
      → _dispatchBSSIDForSeq(_currentSeq)       ← 与首点同一语义（段 BSSID 不同才下发）
```

**策略说明**：
- `_generateBSSIDPlanForPoints`：两级反查（单瓦片 LRU → 失败螺旋邻近回退），C6 无失败分支
- `_dispatchBSSIDForSeq`：计划段比对（`sLastSentBSSID` 段内零下发，同 BSSID 不重复 reload）
- 崩溃修复：字典用 `mutable` 创建（54 轮，已实机验证不崩）

---

## 六、恢复路径全覆盖检查（wifi 显示同步的完整性）

| 恢复路径 | 当前推 bssid | 推 bssid 时机 | 修复后（`pushFullStateSync`） |
|---|---|---|---|
| UDS accept（App 连接） | ✅ | accept handler 尾部 | ✅ 不变 |
| UDS 命令处理完毕（play/stop/anchor/query） | ✅ | 命令 handler 尾部 | ✅ 不变 |
| 播完复位（_trackTick finish） | ❌ **缺** | 只有 pushState | ✅ 补上 |
| 空轨迹（_startTrack empty） | ❌ **缺** | 只有 pushState | ✅ 补上 |
| daemon 启动（BSSID 订阅初始推） | ✅ | _startBSSIDChangeMonitor | ✅ 独立路径，不变 |
| 心跳（15s 订阅活性，F7） | ✅ | _wifiSubscribeHeartbeatTick | ✅ 独立路径，不变 |
| daemon 恢复（forceStopOnStartup） | — | App 未连时推空管道 | ✅ 不影响（accept 补推） |

**结论**：两个缺口（播完复位、空轨迹），方案 B 统一修复后全覆盖。

---

## 七、改动范围

| 文件 | 行数 | 改动 |
|---|---|---|
| `TRMapPickerViewController.m` | ~5 行 | `_renderWifiStatusBar` 三态区分（UDS 断线/同步中/当前AP） |
| `SimLocationController.mm` | ~15 行 | 新方法 `pushFullStateSync` + 4 个调用点替换 |
| `整改方案-WiFi订阅模型对齐-2026-09-06.md` | — | 本方案文档（已入库） |

**不变**：架构总纲、说明文档、网关、caps.js、Makefile、pbxproj、CI 流程。

---

## 八、验证清单（定案后你重跑）

| # | 场景 | 操作 | 预期 |
|---|---|---|---|
| 1 | 打开 App（daemon 已运行） | 杀 App 重开 → 打开 | 状态栏显示“当前AP：时来运转，20:58:…”（非“未连接”） |
| 2 | 杀掉 App 重开（恢复流程） | 双击 Home 杀 App → 重开 | 同上，恢复后 1 秒内对齐 |
| 3 | 点播放/停止/设锚点 | 播放 → 停止 → 设锚点 | 状态栏不变（AP 没变）或 BSSID 变化后更新 |
| 4 | 杀掉 daemon（killall） | 模拟 daemon 崩溃 | 状态栏变为“未连接”（UDS 断线，诚实降级） |
| 5 | daemon 恢复后 | 等 daemon 重启完成 | 状态栏恢复“当前AP：…”（UDS 重连 → accept 推） |
| 6 | 软路由切换生效 | 播放中观察 | 状态栏 BSSID 更新 + 水滴移动（删旧建新） |
| 7 | 空轨迹点播放 | 无锚点状态点播放 | 不崩（mutable 字典修复已实机验证） |
| 8 | 创建首锚点 | 搜索南京→选中→创建 | 蓝点南京不动、状态栏“当前AP：…”、水滴出现（wloc 反查成功时） |
| 9 | 编辑路线 | 添加第二个锚点 → 等待 | 生成中不崩、生成后状态栏不变、播放续播正常 |