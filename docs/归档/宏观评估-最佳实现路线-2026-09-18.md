# 宏观评估：怎么实现才是最好的（2026-09-18）

> 用户问："综合评估一下，站在宏观角度看看要怎么实现才是最好的。"
>
> 本文把本会话积累的全部事实、你的三条目标（**短期驱动 iOS → 中期网关做基座 → 长期通用 MCP 服务器**）
> 放在一起做取舍，**给出明确推荐而非罗列选项**。

---

## 一、结论先行

> **两条腿走，重心在第二条。**
>
> **腿 1（1–2 天）**：给 Mobile-Agent 写 `IosTools` —— **低成本试水**
> **腿 2（正题）**：**网关做成 MCP 服务器** —— **这才是能承载我们 57 个能力的地方**
> **腿 3（后话）**：技能层（零模型回放）—— 架构总纲的核心，但**建立在前两条之上**

**★ 一句话**：
> **`IosTools` 是"能用"，MCP 服务器才是"发挥优势"。**
> **因为 Mobile-Agent 的动作空间本质上就是为 adb 设计的 11 个动作 —— 它承载不了我们的 57 个能力。**

---

## 二、为什么不是"只做 `IosTools`" —— 一个不可回避的事实

### 2.1 `IosTools` 的天花板

`IosTools` 与 `AdbTools` 同接口 → **只有 9 个方法**：

```python
get_screenshot · click · long_press · slide · back · home · type
get_package_name · open_app
```

**★ 我们最值钱的东西【一个都进不去】** ✗：

| 我们的独有能力 | 能否通过 `IosTools` 暴露 | 为什么 |
|---|---|---|
| **`vision.ocr`**（屏幕文字 + 坐标）| ❌ | `AdbTools` 接口里没有"感知文字"这个动作 |
| **`vision.find_text`**（按文字定位）| ❌ | 同上 |
| **`screen.hash` / `waitStable`**（零模型校验）| ❌ | 接口里没有"校验"这个动作 |
| **`keyboard`**（HID 键盘，比 WDA 更正统）| ⚠️ 只能塞进 `type()` 内部 | 方法名固定，无法单独暴露 |
| **`type.delete`**（删除键）| ❌ | 没有对应方法 |
| **`touch.doubleTap` / `pinch` / `taps`** | ❌ | 同上 |
| **`app.list` 的显示名** | ⚠️ 只能喂给 `get_package_name()` | —— |
| **`script.exec`**（技能脚本执行器）| ❌ | 同上 |

**→ 9 个方法里能塞的只有 4–5 个能力，其余 50 多个【永远出不去】** ✗✓✓

### 2.2 更要紧的是：**Mobile-Agent 的动作空间是"adb 的形状"**

我读过它的 SYSTEM_PROMPT（`mobile_use/utils.py:300-320`），动作枚举是：
```
key · click · long_press · swipe · type · system_button · open · wait · answer · interact · terminate
```
**★ 这 11 个动作里的每一个，都能一对一映射到一条 adb 命令** ✗：
- `click` → `input tap` · `swipe` → `input swipe` · `system_button{Back}` → `keyevent 4`
- **`system_button` 的 `Menu` 在 PC/Web 端【根本不存在】** ✗（子代理实测 ✓）
- **`type` 在 Android 上要靠装 ADB Keyboard IME** ✗

**★★ 即：这个动作空间不是"通用 GUI 抽象"，而是"Android 能力的一份投影"** ✗
**★★★ 换到 iOS 上，它天然表达不了 iOS 特有的东西**（没有返回键、HID 键盘、OCR 定位…）✗

**★★★★ 所以 `IosTools` 的价值是"让 Mobile-Agent 能跑 iOS"，不是"让 iOS 发挥所长"** ✓

---

## 三、为什么 MCP 才是正题

### 3.1 因为 MCP 是**工具发现驱动**的，不是**动作枚举驱动**的

| | Mobile-Agent 动作空间 | **MCP** |
|---|---|---|
| 能力怎么暴露 | **枚举写死在提示词里**（11 个）✗ | **`tools/list` 动态返回** ✓ |
| 加一个能力 | **改提示词 + 改分发 if/elif** ✗ | **注册一个工具** ✓ |
| 上限 | **受枚举约束**（11 个）✗ | **无上限**（57 个随意）✓ |
| 客户端 | **只有它自己** ✗ | **任何 MCP 客户端**（Claude / Cursor / 自己写的）✓ |

**★★ 结论**：**只有 MCP 这种"工具发现"协议，才能承载"57 个能力"这种规模** ✓✓✓

### 3.2 而且这正是你自己说的目标

> "后续我们将其作为一个 MCP 服务器，用来控制我们的手机、电脑等等通用场景"

**★ 这条与宏观判断完全一致** ✓ —— **不是我在另起炉灶，而是你早已看准了** ✓

### 3.3 MCP 还顺手解决了三个悬而未决的问题

| 问题 | MCP 怎么解 |
|---|---|
| **"注册了但 0x50/5802 没实现 → 调用才炸"** | **能力表驱动 `tools/list`**：没实现的**根本不出现**在工具列表里 ✓（§17.8 N1）|
| **"注册表+0x50+5802 三处补齐"** | **一份声明式 schema 生成工具面**：加能力只改一个文件 ✓（§17.8 N7）|
| **模型不知道设备会什么** | **`tools/list` 就是能力清单**；不支持的动作返回结构化错误 + `alternative` + `hint` ✓（§17.8 N2）|

---

## 四、推荐路线（分三层，有明确先后）

```
┌─ 腿 1（1–2 天）─────────────────────────────────────────────┐
│ 给 Mobile-Agent 写 IosTools（2 新文件 + 3 行）              │
│ 目的：① 立刻有一个成熟 Agent 循环在驱动我们                  │
│       ② 顺手验收 9 个能力的真机正确性（未调用=未验收）        │
│       ③ 只增不改，有可能提 PR 回上游                          │
│ 验收：官方脚本原样跑通"打开抖音看首页"                        │
└──────────────────────────────────────────────────────────────┘
                          ↓ 借鉴它的循环，但不依赖它
┌─ 腿 2（正题）───────────────────────────────────────────────┐
│ 网关做成 MCP 服务器：57 能力 → 57 个 MCP 工具                │
│ 关键：声明式能力表（mode / risk / verify / backends /        │
│       unsupported+alternative+hint）                          │
│ 验收：任何 MCP 客户端 tools/list 能看到 57 个工具，           │
│       且调不通的能力【不出现】或返回带替代路径的错误          │
└──────────────────────────────────────────────────────────────┘
                          ↓ 有了工具面与 trace，才谈得上沉淀
┌─ 腿 3（后话）───────────────────────────────────────────────┐
│ 技能层（零模型回放）：做法表 + 页面表 + 技能卡 + evolution 账本 │
│ 依据：架构总纲 §6/§7；参考 AgentProg 的结构剪枝 +              │
│       grab_similar_nodes 的历史代码召回                       │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、★ 关键的架构判断：**平行，而不是跟随**

| 策略 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **跟随** | 改 Mobile-Agent 加 iOS 平台 | 蹭成熟框架 ✓ 可提 PR ✓ | **受限于它的 11 动作 / 9 方法** ✗ **50+ 能力出不去** ✗ |
| **平行** | **网关做 MCP 服务器，它作为客户端** | **57 能力全出** ✓ **通用** ✓ **契约我们定** ✓ | 要写 MCP 服务器 ✗ |

**★ 推荐：两条都做，重心在平行** ✓

**★★ 为什么"跟随"仍值得做（腿 1）**：
1. **它给了我们一个【现成的成熟 Agent 循环】** —— 而我们自己那个还没写好
   （`scripts/actor-run.py` 是临时脚本，架构总纲说的执行器尚未落地）
2. **它顺便验收 9 个能力**（本项目血泪：**未调用过的能力 = 未验收**）
3. **只增不改 → 有可能贡献回上游**（对项目声誉与生态都有益）
4. **成本极低**（2 文件 + 3 行），**即使失败也不亏**

**★★★ 但绝不能把"跟随"当主线** ✗ —— 因为**它承载不了我们的优势**（§二已论证）

---

## 六、★ 一个必须现在就想清楚的问题：**MCP 工具面的"能力粒度"**

**MCP 工具一旦发出去，客户端会照着它调** ✗ → **粒度定错，后面很难改** ✗

三个候选粒度：

| 粒度 | 例子 | 优点 | 缺点 |
|---|---|---|---|
| **A. 一能力一工具** | `touch_tap` / `type_paste` / `vision_ocr`…（57 个）| **最忠实** ✓ | **工具太多，模型选择困难** ✗ |
| **B. 按域聚合** | `touch(action, x, y)` / `type(action, text)` / `vision(op)` | 工具少 ✓ | **参数变复杂，模型容易传错** ✗ |
| **C. 混合（推荐）** ✓ | **高频细粒度**（tap/swipe/type/back/home/screenshot/ocr/find_text）+ **低频聚合**（`device_ctl(op)` 管电源/音量/亮度）| **兼顾** ✓ | 需要一次判断 ✗ |

**★ 建议采用 C**，并且**用 `mode` 元数据区分**（§17.8 N7）：
- `mode: complete` —— 动作本身即完成（`tap` / `type` / `home`）
- `mode: intent_only` —— 只拉起界面不算完成（`app.open`）
- `mode: bootstrap` —— 用于初始化

**★★ 而这个粒度决策，最好【在腿 1 跑过之后】再做** ✓
—— 因为腿 1 会告诉我们"模型实际会怎么用这些动作"（真实证据 > 拍脑袋）

---

## 七、风险与代价（诚实列出）

| 风险 | 说明 | 缓解 |
|---|---|---|
| **腿 1 的 PR 可能不被接受** | 上游可能不想要"依赖第三方网关"的平台实现 | 只增不改、代码整洁、**明确写成可选后端** ✓ |
| **腿 2 的 MCP 服务器要改网关** | 网关是**正在服役**的（设备在用）| 端口固定不动、**新端点纯加法**、改完跑 `npm test` + `verify-iframe-chain.py` ✓ |
| **坐标空间歧义** | mobilerun 踩过（截图物理像素 vs 输入 points）| **MCP 工具的 schema 里写死 `coordinate_space` + `screen{w,h}`** ✓ |
| **iOS 能力缺口** | 无杀进程 / 装卸 IPA / 查前台 bundleId | 按 §17.8 N2 **如实返回 `unsupported` + `alternative` + `hint`** ✓ |
| **"记了但看不到"类问题** | 本会话刚踩过（AGENTS.md 超预算被截断）| **新结论一律进架构总纲 §17.8 / 独立报告，AGENTS.md 只留索引** ✓ |

---

## 八、我现在建议的下一步（按顺序，每步有验收）

| # | 做什么 | 验收标准 | 成本 |
|---|---|---|---|
| **1** | 写 `IosTools` + `device_tools.py` + 入口改 3 行 | **官方脚本原样跑通一个真机任务** | 1 天 |
| **2** | 在**真机**跑通"打开抖音看首页"和"搜索并看 3 个结果" | 两任务都完成，且**设备不被回收** | 半天 |
| **3** | 把腿 1 的实测结论回灌到设计（能力粒度、缺口清单）| 产出一份"实测发现" | 半天 |
| **4** | **开始做 MCP 服务器**（先 10 个核心工具，验证协议）| **`tools/list` 可见、`tools/call` 可执行** | 2–3 天 |
| **5** | 扩到 57 能力 + 声明式能力表 | 未实现的能力**不出现**或返回带替代路径的错误 | 2–3 天 |

**★ 关键**：**第 1、2 步只是"试水与验收"，别停在它上面** ✗
**★★ 真正的主线是第 4、5 步** ✓✓✓

---

## 九、一句话总结

> **Mobile-Agent 是一条"别人修好的路"，但它只通向 Android 的能力形状 —— 我们开上去能走，但运不了自己的货。**
>
> **所以最好的做法是：把车开上去验收一遍（`IosTools`，1–2 天），
> 然后修自己的路（MCP 服务器），把 57 个能力全部运出去。**
>
> **而你今天说的"MCP 服务器"这个判断，正是这条主线的名字。**
