# 整改方案：定位时序与 WiFi 链修复（2026-09-05 · v2 修订版）

> 状态：**待用户定案**（定案前不动任何代码）。
> 本文档是唯一执行依据：每个问题 = 现象 / 根因调用链（行号实证）/ 契约违背点 / 修复设计 / 不被治坏保证。
> 行号基准：`d553ff6`（第四十五轮部署版）。App = `TRMapPickerViewController.m`（2549 行），daemon = `SimLocationController.mm`（951 行）。
> **v2 修订**（用户质询驱动）：①"plist 镜像"推翻 → mode 入 simstate.json（Q1）；②新增 Q2（`_startAnchor` 坐标覆写 P0，v1 审计遗漏，"反复暴露真实位置"的第一根因）；③CNCopy 从 App 整体删除，对账改 UDS query 权威通道（Q6）；④新增 C11 契约（持久化驱动状态源方法论）。

---

## 〇、契约基准（修复方案的合法性来源）

| # | 契约 | 出处 |
|---|---|---|
| C1 | 命令唯一通道 = UDS；plist SimLocation* **命令与读取**全部退役 | 2026-09-05 权威语义（v2 收紧：读端一并退） |
| C2 | 当前位置唯一真相 = daemon（_currentLat/simstate）；命令可携带位置，前端不维护不回写 | 定位播放状态建模·职责铁律 |
| C3 | 双订阅对称架构：GPS 侧 locationd 流 ‖ WiFi 侧 SCDynamicStore 流 → evt 推 App；禁止退化 CNCopy 轮询；**WiFi 链触发与显示均不依赖定位授权** | 定位播放状态建模（v2：显示端补齐） |
| C4 | 变化才查、比对零开销（BSSID 比对/瓦片比对纯本地） | WiFi 反查触发策略 |
| C5 | 无感漫游：下发只改目标 BSSID、fire-and-forget、常驻订阅验证 | c319415 定案 |
| C6 | 计划固化于轨迹文件（创建轨迹时同步计算）、播放中零反查、daemon 按 seq 段下发；**瓦片反查无失败分支（空洞自动邻近回退）** | 1b8211d 定案 + App L2279 注释 |
| C7 | 接力纪律：从当前位置继续（seq 精确→几何兜底），禁回起点 | 定位播放状态建模 |
| C8 | 单一写入者：simloc.json=App 独写、simstate.json=daemon 独写、mobile plist 仅存非命令配置（SimRouterHTTP 等） | 数据契约（v2：plist Sim* 键全退场） |
| C9 | 不被治坏：GPS 注入链、微动、接力语义、统一「当前AP」语义、无感漫游 CGI——零触碰 | 工程治理决策原则 |
| C10 | 启动契约：daemon 重启=先关定位→注入上次位置→开定位→**按持久化模式恢复（itinerary 放行恢复播放）**；App 重启=停止态+UDS 对齐 | 2026-09-05 启动契约（用户定义） |
| C11 | **持久化驱动状态源**：先确定哪些状态必须持久化（活过重启），再基于持久化设计状态源；不发明新持久化文件/新通道，优先补齐既有机制 | 2026-08-30 用户方法论 |

---

## 一、问题清单（Q1–Q10，每个已查清根因调用链）

### Q1 双真相源：plist reload 状态机与 UDS 状态机并存（时序瘫痪的结构根因）

**现象**：播放/编辑中 daemon 被拉回 off；"回出发点"复活；行为随 plist 陈旧值漂移。

**根因调用链**：
```
① UDS 迁移后 App 不再写 plist Sim* 键，但 daemon 播完复位仍写 plist mode=off（L592-596）
   → plist mode 与 daemon 执行态永久脱节
② 任何编辑 → writeTrackFile → notify_post(prefs-changed)（L2296）
③ manager 订阅 prefs-changed → scheduleReloadFromPrefs（trollvncmanager.mm L652）
④ scheduleReloadFromPrefs 读 plist mode（L155）≠ daemon _currentMode
   → 误判"模式切换" → applyFromPrefs 按 plist 旧值执行 → 正在播放的 daemon 被打断
```

**契约违背**：C1（读端未随写端退役）、C8、C10。

**修复设计（v2：单状态机 + mode 入 simstate，依 C1/C8/C10/C11）**：
1. **simstate.json 增加 `mode` 字段**（`off|anchor|itinerary`，顶层）：`_persistState` 写入（每次注入已落盘 → mode 恒新鲜）；stop 分支/播完复位/forceOffAndReload 处显式补写一次（防 ≤1s 空窗误恢复）。
2. UDS 命令处理保持**直调执行分支**（play→`_stopAnchor`+`_startTrack`；stop→off 内联分支；anchor→Q2 修复后的 `_startAnchor`，坐标参数传入）——此代码已被 CI 验证，补齐：各分支执行后 `_persistState` 一次 + 既有 push state/bssid。
3. **整套退役**（输入源已不存在，机器连带退役）：`reloadFromPrefs`、`scheduleReloadFromPrefs`、`_reloadDebounce`+`kSimReloadMergeInterval`、`applyFromPrefs`、`_paramsSignature`；`_readPref/_readDouble` 仅保留 SimRouterHTTP 配置读取用途。
4. **删除** manager prefs-changed 处理器里的 `scheduleReloadFromPrefs` 桥（trollvncmanager.mm L652 一行；处理器其余职责——网关配置/watchdog 热调——不动）。
5. **删除** App `commitSimPrefs`（L1912-1917，零调用者）；播完复位的 plist 写块（L592-596）删除（simstate 已覆盖）。
6. `forceOffAndReload`（升级通道）：写 simstate mode=off + 执行 stop 路径，不再写 plist。
7. 启动恢复（`_forceStopOnStartup` 内）：注入上次位置+开定位（既有）后，读 simstate mode——`itinerary` → `_startTrack`（C7 续播机器现成）；`anchor|off` → `_startMicroWanderWithCenter(当前位置)`（既有）。

**依据**：C11——缺失的持久化状态只有 mode，家在已存在的 simstate.json；恢复走同一台已验证状态机；plist 写读回环消失。

**不被治坏**：GPS 注入链/微动/接力零触碰；非 Sim 配置（SimRouterHTTP/watchdog/网关）通道不动。

---

### Q2 【P0·v2 新增】UDS anchor 命令坐标被 `_startAnchor` 用 plist 陈旧值覆写（"反复暴露真实位置"的第一根因）

**现象**：创建锚点后不注入/蓝点回真实位置/跳回旧锚点；播放中编辑（hold 走同路径）注入中断；时序全面瘫痪。

**根因调用链**（逐行实证）：
```
UDS anchor{lat,lon} → _simUDSHandleCommand L879-881：_currentLat/_currentLon = 命令坐标 ✓
                    → [selfer _startAnchor]（L883）
_startAnchor L244-246：double lat = [self _readDouble:@"SimLocationLat" def:0.0]   ← 读 plist！
              L249-259：_anchorLat=plist值；_currentLat=plist值                    ← 命令坐标被扔掉
              L262：注入 plist 值
干净基线（plist 无 Sim 键）：lat=lon=0 → L262 注入被 _injectGpsForCurrentLocation 的 0,0 守卫跳过
                            → 注入永不落地；但 L265-267 无条件打开系统定位
                            → locationd 无模拟会话 → 广播真实 GPS = 蓝点回真实位置
plist 有残留（旧播完终点）：注入旧坐标 = "回出发点"复活
```

**定性**：UDS 迁移（818df08）改了命令入口（坐标进 JSON），没改 `_startAnchor` 消费端（仍读 plist）——半迁移 bug。commitAnchor 退役 plist 写入后，plist 里的坐标必然陈旧，此 bug 从"潜伏"变"必现"。

**契约违背**：C2（命令携带的位置被丢弃）、C1（读已退役的命令键）。

**修复设计**：`_startAnchor` 改签名 `_startAnchorAtLat:lon:acc:`（坐标由调用方传入：UDS anchor 命令传 JSON 坐标）；删除方法内全部 plist 读取。L878 的 `lat!=0||lon!=0` 非法命令守卫保留。

**不被治坏**：注入/微动/anchor 平移逻辑零改动——只换坐标来源（参数化）。

---

### Q3 编辑重算打断播放：writeTrackFile 后无重播指令

**根因调用链**：`writeTrackFile` L2294-2297 定位中仅 `notify_post(prefs-changed)`——UDS 迁移后该 notify 的播放恢复语义无载体（Q1 修复后彻底无消费者）。

**契约违背**：C7 + C1。

**修复设计**：写盘成功块内 `if (locating)` 分支的 notify_post 替换为：回主队列 `sendSimCommand:@{@"cmd":@"play"}` → daemon `_startTrack` 读新轨迹（版本不一致）→ 几何兜底从当前位置（hold 驻留点）接力 → 无缝续播。UDS 断线时命令自动进 pendingSimCommand 重发（既有送达保证）。

---

### Q4 空轨迹播放状态自谎

**现状**：`_startTrack` 空分支（L512-517）只停播放不改 `_currentMode`、不落盘。

**修复设计**：空分支补 `_currentMode = @"off"` + `_persistState` + push（状态不自谎；配合 Q1 的 mode 恢复语义）。

---

### Q5 计划生成未兑现"无失败分支"契约（螺旋缺失 + 最近 AP 假算）

**现象**：计划里出现 `bssid:""` 空段 → 对应 seq 区间软路由不切换，AP 与模拟位置不自洽；`aps[0]` 假设排序无证据（TRWpsTile 全文无 sort；daemon 同场景都是 haversine 遍历取最近 L364-367），"兜底遍历"在死分支里（L2325-2331：bssid 已被 aps[0] 赋值，永远非 nil）。

**根因**：`_generateBSSIDPlanForPoints` L2323 只调单瓦片查询；注释 L2279-2280 声称的"螺旋邻近回退"从未实现——**注释与实现矛盾**（AGENTS.md 注释纪律）。

**契约违背**：C6（"无失败分支"是用户定案设计，实现欠账）。

**修复设计（与 daemon 反查同构，环环相扣）**：
1. 代表点反查两级结构，与 daemon `_handleAPSwitchForCurrentLocation`（L344 单查 → L349 螺旋）同构：单瓦片查询（LRU/inflight 既有复用）→ 失败（error 或空）→ `queryNearestBssidsForCoordinate` 邻近螺旋 → 回调取 **haversine 真实最近** AP。信号量 signal 只在最终回调，每轮恰好 1signal+1wait 配对不变。
2. 极端数据沙漠（螺旋 24 瓦片全空）才产出空段——daemon `_dispatchBSSIDForSeq` 空 bssid 跳过（L624）保留为纵深防线（不赌它被触发）；计划尾部 TVLog 汇总段数，空洞可观测。
3. **不加**超时/预算硬编码——有界性由既有机制保证（单请求 15s 必回调 + 代表点 ≤65 + 跨瓦片 LRU 消解请求量）。

---

### Q6 App WiFi 显示链依赖定位授权 + CNCopy 残余角色重裁决（"从来无水滴/获取中"的架构病根）

**现象**：wifi 状态栏从不显示当前连接 BSSID；地图无水滴。

**根因调用链**（四个死法同一根因）：
```
_updateWifiStatusBar（L477-491）→ currentNetworkInfo = CNCopyCurrentNetworkInfo
→ 需系统定位开启才返回数据
① App 启动 readCurrentStatus L590 调它——daemon 启动契约刚关定位 → 空
② daemon 无持久位置时定位不开 → 永空
③ state 回执触发刷新（L1236）落在"先关再开"抖动窗口 → 空，evt=bssid 只在变化时推 → 无下一次
④ evt=bssid 反查成功画水滴后，回调 L552 再调 _updateWifiStatusBar → CNCopy 空 → 覆盖回"获取中"
```

**契约违背**：C3（触发端不依赖定位授权做到了，显示端没做到——链路两半语义不一致）。

**修复设计（v2：evt 全驱动 + CNCopy 从 App 整体删除）**：

daemon 侧（四点，全部复用既有代码）：
1. evt=bssid 载荷扩展 `{"evt":"bssid","bssid":"..","ssid":".."}`——SSID 在已读的 AirPort 字典里（TRWifiKnownNetworks 同源），同一事件白带一个字段；
2. `_handleWifiStoreChanged` 空 BSSID 不再 return，推 `bssid:""`（断连是真实物理事件，App 需要它撤水滴）；
3. UDS accept 时随 state 补推一条当前 bssid（A1 逻辑延伸到连接时点）；
4. **新增 UDS `{"cmd":"query"}`**：daemon 同步读当前连接（`_handleWifiStoreChanged` 的既有直读路径，无定位依赖）+ push state → evt 回推。即"点一下立即拿权威当前值"。

App 侧：
1. `_updateWifiStatusBar` 改名降级 `_renderWifiStatusBar`：**纯渲染器**——只从内存缓存（lastWifiBSSID/lastWifiSSID）渲染文本，不调任何系统 API、不发起反查；
2. evt=bssid 分支成为唯一驱动：更新缓存 → 渲染 → BSSID 变化才 `++seq` 发 wloc 反查 → 回调画水滴（删旧建新）→ 渲染位置；
3. **删除** `_refreshWifiAnnoFromCurrentConnection` 整个方法及调用点（CNCopy 轮询血统残存）——播放/停止切换连接没变，只需重渲染不需重查询（C4 语义边界）；
4. **删除** `currentNetworkInfo` helper 与 CaptiveNetwork import——CNCopy 在 App 零残留；
5. `wifiDiagTapped` → 发 UDS query 命令（UDS 断线则立即触发重连，状态栏诚实显示未连接）——**对账的重新定义**：对的账 = App 内存缓存 vs configd 真相（缓存可能因 SDS 初始化失败/断线/时序而错）；对账动作 = 经权威通道强制重取并修正缓存，而非 App 自己读系统（那会重新引入定位授权依赖）；
6. seq 竞态消解：查询入口只剩 evt（UDS 主队列串行保序）+ 手动 query。

**依据**：C3 完全体——WiFi 链从触发到显示全程定位无关；单一数据源原则完全体（App 零直接系统读取）；"获取中"卡死从架构上铲除。

**不被治坏**：统一「当前AP」文案语义不变；水滴"删旧建新"不变；wloc 反查/质心标注不变。

---

### Q7 状态栏文案振荡

**现象**：反查失败区"反查中…/反查失败"振荡无稳态。

**修复设计**：反查失败稳态文案 `当前AP：%@，%@ · 位置反查失败（本区域无数据）`；断连态 `未连接 WiFi`（随 Q6 的 evt bssid:""）。渲染器唯一出口后无重复刷新源。

---

### Q8 坐标语义未裁决（gspe85 吃 GCJ 还是 WGS）——独立实验，不阻塞修复

**现状**：计划生成与 daemon 反查同用轨迹文件系数值，互相自洽（Q1-Q7 不依赖此项）；但 AP 与模拟位置的绝对自洽取决于 gspe85 的坐标语义。从未实测。

**修复设计**：真机对照实验（不进本轮 CI）：同一坐标分别以原值与 `gcj02ToWgs84` 转换值请求瓦片，对比返回 AP 集合 + wloc 反查验证 BSSID 物理位置落在哪侧。定案后若需转换，改动点仅 `_generateBSSIDPlanForPoints` 与 `_handleAPSwitchForCurrentLocation` 入参各一行。

---

### Q9 注释与实现矛盾（触碰点随实施审计）

- App L2279-2280（螺旋注释撒谎，Q5 修复后为真）；daemon L95-98（启动契约注释与 C10 相反，Q1 修复后为真）；
- 本次全部触碰文件按双重真相源法（当前实现 + 沟通历史用户意图，9-04 先例）随实施审计。

---

### Q10 文档欠账（三十六轮以来架构级变更零同步）

1. `说明文档.md`：
   - §4.11 wifi 模拟链重写：计划模型（C6 两级反查）/evt 协议（bssid+ssid、空推、query 命令）/无感漫游（C5）/渲染与查询分离（C3 完全体）；
   - 新增"UDS 权威通道与 simstate 执行态"节：C1 完整语义 + **plist 角色终局**（Sim* 键零读零写退役，plist 仅存非命令配置）+ reload 状态机退役裁决；
   - 启动契约节对齐 C10（simstate mode 恢复/放行）；
   - 双订阅对称架构节（C3 完整版）。
2. `CodeWiki.md`：SimLocationController 职责（+simstate mode/−reload 链）、TRWpsTile 消费方（+App 计划生成）核对。
3. OpenViking 记忆沉淀（实施完成后一次写入）：plist Sim* 终局、simstate=唯一持久化状态源（C11 落地实例）、scheduleReload 退役裁决（推翻 9-04 "C2 observe only"——UDS 迁移后前提已变）、"命令通道迁移必须同步退役读端与消费端"方法论（Q1+Q2 双实锤）、"未实测假设显式标注"再确认。

---

## 二、修复后全链交互矩阵（集成性验证）

| # | 场景 | 修复后时序 | 契约 |
|---|---|---|---|
| 1 | 创建首锚点（干净基线） | UDS anchor{坐标}→Q2 修复：坐标直达注入→注入落地→开定位→微动驻留→simstate{mode:anchor} | C2/C1 ✓ 真实位置零暴露 |
| 2 | 播放中编辑锚点 | hold(UDS anchor，Q2 修复坐标生效)→驻留微动→算路→写盘(计划含螺旋)→**UDS play**→新轨迹从当前位置接力续播 | C7/C1 ✓ 无 notify 无 reload 无掐断 |
| 3 | 点播放/停止 | UDS→直调分支→simstate mode→push | C1/C2 ✓ |
| 4 | 轨迹播完 | finish 单方复位（不动）→simstate off→push | C2 ✓ |
| 5 | daemon 崩溃重启 | simstate 恢复注入+开定位→按 mode 恢复（itinerary 放行续播）→accept 推 state+bssid | C10/C11 ✓ |
| 6 | App 崩溃重启 | 不写键→UDS 重连→接收 state+bssid→UI 对齐 | C2/C10 ✓ |
| 7 | 软路由切换生效 | SDS 变化→evt{bssid,ssid}→渲染+变化才反查→水滴删旧建新 | C3/C4/C5 ✓ 定位无关 |
| 8 | WiFi 瞬断/恢复 | evt bssid:""→撤水滴+"未连接"→重连 evt 恢复 | 物理诚实 ✓ |
| 9 | 用户点诊断条 | UDS query→daemon 同步读 configd→evt 回推权威值→缓存修正+渲染 | 单一数据源 ✓ |
| 10 | 数据沙漠区造轨迹 | 单查→螺旋邻近→极端全空才空段（daemon 跳过防御+日志可观测） | C6 ✓ 不赌 |
| 11 | UDS 断线 | 命令缓存重发（既有）→显示最后已知值+evt 停更+点条触发重连 | 诚实降级 ✓ |
| 12 | 空轨迹播放 | 空分支归 off+落盘+push | 状态不自谎 ✓ |
| 13 | GPS 显示/微动/接力/CGI/统一AP | **零改动** | C9 不被治坏 ✓ |

---

## 三、触碰面清单（取舍指标）

| 文件 | 改动 |
|---|---|
| `SimLocationController.mm` | Q1：UDS 分支补 _persistState+mode；删 reload/applyFromPrefs/scheduleReload/_paramsSignature/_reloadDebounce（~90 行）；启动恢复读 mode；forceOffAndReload 改 simstate；播完复位删 plist 写块。Q2：_startAnchor 参数化删 plist 读。Q4：空轨迹分支补齐。Q6：evt 载荷+ssid、空推、accept 补推、query 命令（~15 行） |
| `TRMapPickerViewController.m` | Q6：_renderWifiStatusBar 拆分、evt 分支唯一驱动、删 _refreshWifiAnnoFromCurrentConnection+currentNetworkInfo+CaptiveNetwork import（~60 行）、wifiDiagTapped→query。Q5：计划生成两级反查+真最近 AP（~30 行）。Q3：writeTrackFile play 重发（~5 行）。Q1：删 commitSimPrefs（6 行）。Q9：触点注释审计 |
| `trollvncmanager.mm` | Q1：删 prefs-changed → scheduleReload 桥（1 行） |
| `说明文档.md` / `CodeWiki.md` | Q10 清单 |
| **零触碰** | 网关（trollvnc-farm）、5801 直连页、caps.js、SimLocationManager 注入原语、SimRouteCalculator、RegionSimulator、无感漫游 CGI、非 Sim 配置通道 |

---

## 四、执行编排（定案后一次跑完）

1. **方案本文档入库**（docs commit）；
2. **代码实施**（依赖序）：Q2（坐标参数化，第一根因）→ Q1（状态机收敛+mode 持久化）→ Q4 → Q3（play 重发）→ Q5（计划螺旋）→ Q6→Q7（显示链重建）→ Q9（注释随触点）——一个 commit 系；
3. **文档同步**：Q10 与对应代码同 commit；
4. **一轮 CI** → 部署；
5. **真机验证清单**（用户操作，我拉 5902 日志逐帧对）：
   - ① 干净基线创建首锚点：注入立即落地（5902 见 inject 行）、蓝点到锚点、**不出现真实位置/null island**（Q2 验收）；
   - ② 播放中编辑锚点：全程蓝点不回真实位置、编辑后无缝续播不回起点（Q2+Q3 验收）；
   - ③ 播放/停止/播完三态：simstate mode 与 UI 一致；杀 App 重开自动对齐；杀 daemon 重启自动恢复续播（Q1 验收）；
   - ④ **定位权限关/飞行模式组合**下：wifi 状态栏仍显示当前连接 BSSID（Q6 核心验收）；
   - ⑤ 点 wifi 诊断条：状态栏立即刷新为权威当前值（query 验收）；
   - ⑥ 软路由切换后：BSSID 更新 + 水滴删旧建新（Q6 验收）；
   - ⑦ 数据密集区新建轨迹：计划 TVLog 段数无空洞（Q5 验收）；
6. **独立实验**：Q8 坐标裁决（双请求对照脚本走 diag 通道）；
7. **记忆沉淀**：Q10-3。

---

## 五、撤销记录（沟通中被纠正的方案演进，留痕防回潮）

| 版本 | 撤销内容 | 撤销理由 | 正确定案 |
|---|---|---|---|
| v1 | 计划生成不加螺旋、空洞段"诚实跳过" | 顺着实现缺陷合理化设计欠账——C6 明确"无失败分支" | Q5：接入与 daemon 同构的两级反查 |
| v1 | 信号量 20s 超时 + 全程 10s 预算 | 硬编码赌注，不信任既有原语 | 有界性由机制保证（Q5-3） |
| v1 | "UDS 在线才跳过 plist reload"守卫 | 补丁式状态判断 | Q1：单状态机，读端整套退役 |
| v1 | plist 镜像（mode 回写 plist 供恢复读取） | **用户质询"读什么镜像"触发重推**：写读回环；违背 C11 持久化驱动状态源——缺的持久化状态只有 mode，家在已存在的 simstate.json | Q1：simstate.json + mode 字段 |
| v1 | CNCopy 保留"手动对账"角色 | **用户质询"对什么账/需要什么能力"触发重推**：对账走 App 自读会重新引入定位授权依赖（C3 病根回潮） | Q6：CNCopy 整体删除；对账 = UDS query → daemon configd 直读 → evt 回推 |
| v1 审计 | （遗漏）`_startAnchor` plist 坐标覆写 | v1 审计时未逐一核对 plist 读取者 | Q2：P0 第一根因，参数化修复 |
