# 网关做「全平台设备基座」——方案评估与路线（2026-09-18）

> **缘起**：用户提出——细致了解 Mobile-Agent-v3.5，参考别家的 WDA 能力对它做集成，
> **"以 WDA 同样的基类，基于我们的 noVNC 作为 WDA 的分支进行集成"**，
> 从而网关成为全平台基座（后续还能扩 Android）。
>
> 本文基于**两份源码级调研**（笔记在 `_research/_notes/`）：
> - `MobileAgent-设备抽象层.md`（56KB / 511 行，MobileAgent v3.5 全量设备线勘察）
> - `WDA与iOS接入路线调研.md`（51KB / 385 行，mobile-use / mobilerun / mobile-harness 的 iOS 方案）
>
> **证据等级**：全部结论带 `文件:行号`；区分【事实】/【推断】/【未找到】。

---

## 一、结论先行：用户设想的三处修正

| # | 设想 | 事实 | **修正** |
|---|---|---|---|
| **1** | 参考 Mobile-Agent 的架构 | **v3.5 的手机框架只有 1137 行（`mobile_use/` 3 文件），是【薄脚本】**；`AdbTools`(`mobile_use/utils.py:70`) 是**无基类的裸类**，动作分发是 `run_gui_owl_1_5_for_mobile.py:209-272` 的 **if/elif 硬编码**。PC 的 `ComputerTools`、Web 的 `PlaywrightComputer` **同样无基类**，**三端不共享任何抽象**（唯一 `abc.ABC` 命中是 **LLM 封装**，还被抄了两份）| **它的"架构"要参考 `android_world_v3.5/`（42434 行）**，但那部分是**评测框架**；产品向只借**动作协议与坐标制** |
| **2** | **"以 WDA 同样的基类"** | ⚠️ **Mobile-Agent 里【不存在】这个基类**——仓库 **iOS 相关代码 0 命中**（grep `ios/wda/idb/xctest/xcrun/iphone` 全空），`README.md:46` 明写 "only Android OS support… iOS do not support" ✗ | **真正的"同基类"在 `mobilerun-core`**：`VisualRemoteDriver`(`driver/visual_remote.py:37-260`) —— **它本身就是"HTTP 契约驱动设备"** ✓✓✓ |
| **3** | noVNC 作为 **"WDA 的分支"** | ⚠️ **WDA 是设备上运行的 XCTest runner（HTTP 元素级服务）**，需 **Mac + `xcodebuild test` 现场编译签名**（mobile-use `wda_lifecycle.py:240-250`）+ **`iproxy` 转发 8100**；我们的 noVNC 是 **VNC 字节流 + RFB→IOHID 注入，零设备端进程、无 Mac** ✗ **抽象层级与部署形态都不同** | **不"继承 WDA"，而是"实现同一套【动作语义契约】"** ✓ |
| **4** | 网关 → 全平台基座 | **方向完全正确** ✓ | ✓ 但**基座契约应由我们定**，各框架通过**适配层**接进来（我们提供端点，它们写薄适配）|

**★ 一句话**：**"基座"这个想法对，但"基座"不该是 WDA，而应是【一份 HTTP 动作契约】——
恰好 `mobilerun-core` 的 `VisualRemoteDriver` 就是这个形态，且我们能零改动被它驱动。**

---

## 二、★★★ 决定性发现：三条现成路径（按成本排序）

### 路径 A：零改动被 mobilerun 驱动（**成本最低，建议先做**）

**`mobilerun-core` 的 `VisualRemoteDriver`**（`driver/visual_remote.py:37-260`）——**只要求网关提供 3 个端点**：

```
GET  /devices                                  → 设备列表
GET  /devices/{id}/screenshot                  → 截图
POST /devices/{id}/actions                     → 动作：
       tap / swipe / type_text / press_button / open_app
       body 里带 coordinate_space + screen{w, h}
```

> **「就能被 mobilerun 直接驱动，一行 Python 都不用改」** —— 这是**成本最低的可行性验证路径** ✓

**我们已有的对应**：设备列表 ✓ / 截图 ✓（`screenshot` 能力）/ 动作 ✓（57 能力）——
**→ 只需在网关上做一层"契约适配路由"**，不碰设备端 ✓

### 路径 B：一文件适配 Mobile-Agent（**成本也很低**）

`MobileAgent-设备抽象层.md` 的结论：
> **推荐走范围 A = 1 个新文件**（仿 `AdbTools` 的 11 方法类）+ **改 `run_gui_owl_1_5_for_mobile.py:23/148` 两行**

**★★ 而且坐标【天然对齐】**：
```
750×1334 走 qwen-vl(0-999) → abs_origin：x_orig = x_model / 999 * 750
而我们的 superphone_tap 接受 0-1 归一化
→ 两者只差 ÷999，无需屏幕换算 ✓
```

### 路径 C：写 `AsyncEnv` 实现（**不推荐** ✗）

`AsyncEnv`(`android_world/env/interface.py:73`) 有 13 个抽象成员，**但它被约 250 处 `task_evals` 判定器
当作"设备数据访问口"**（`initialize_task`/`is_successful`/`tear_down`、SQLite/文件/剪贴板读写）
→ **接新平台 = 同时满足 250 处假设** ✗
> **官方唯一干净的平台注入点是 `env_launcher.py:31-38` 那个 8 行的 `_get_env`** ✓

**→ 结论：不该把 SuperPhone 做成 `AsyncEnv`** ✗

---

## 三、★★★ 一条"通用钥匙"：把 OCR 伪装成 accessibility 树

**事实**（`WDA与iOS接入路线调研.md`）：
> **三家 iOS 定位 100% 依赖 accessibility 树，全仓库 OCR/vision 零命中**
> （mobile-use `README.md:36` 自认在游戏/Canvas 上失效）

**→ 我们的 `vision.ocr` / `vision.find_text` 是它们【没有的能力】** ✓✓✓

**★ 而且接入方式极妙**：
> 只要把 OCR 结果**伪装成 a11y 树的形状**（字段名严格照
> `mobilerun-core` 的 `_a11y.py:16-28` 白名单 + `ios_controller.py:230-246`），
> 它们的 **`find_nodes` / `tap_text` / `find_element` / `tap_element` 全部无需理解 OCR 即可跑通** ✓

**→ 这是把 57 个能力接进现成抽象的【通用钥匙】** ✓
（我们只有 visual 通道这件事，从"劣势"变成"独有能力"）

---

## 四、必须显式处理的三个坑（调研实测，别家踩过）

| # | 坑 | 实证 | 我们的对策 |
|---|---|---|---|
| **1** | **坐标空间 ≠ 截图像素** | mobilerun `ios_provider.py:71-92` 直写「**iOS 截图是物理像素、taps/bounds 是 points**」；无契约时**直接拒绝坐标类动作**（`actions.py:120-139` 报 "Coordinate actions are unavailable"）；测试钉死：points 440×956 / 物理 1320×2868 / display 943×2048，`convert_point(471,1024)==(220,478)` | **契约里必须显式声明 `coordinate_space` + `screen{w,h}`**（我们的截图 750×1334 是物理像素、输入是 0-1 归一化 → 要写清）|
| **2** | **`system_button` 是【模型动作语汇】不是设备能力清单** | Android→`keyevent 4` / `am start HOME`；**PC 全目录 `system_button` 0 命中**；**Web 0 命中**；**`Menu` 三端全无实现** | **iOS 侧在适配层翻译**：`Home`→`home`、`Enter`→HID Return、`Back`→左缘滑动手势、`Menu`→**声明不支持**。**不在设备端伪造 keyevent**（与"能力层唯一地基 = RFB→IOHID"红线一致 ✓）|
| **3** | **能力缺口要如实降级** | 对方 `app_stop` 官方降级建议就是 `key('home')`（`framework.py:492-497`）；**前台 App bundleId 缺失会导致 `current_app_id`/`assert_on`/`wait_for_app` 失效** ✗ | 我们**无「杀进程 / 装卸载 IPA / 查前台 App bundleId」** → 按 §17.8 的 **N2（`@unsupported_on` 式结构化错误 + alternative + hint）** 如实返回 ✓；反过来 **`clipboard.get` 我们有**（对方仅云设备独有）✓ |

---

## 五、方案：网关 = 全平台基座（三层契约）

```
┌──────────────────────────────────────────────────────────────┐
│ 框架层（谁在驱动）                                             │
│   Mobile-Agent v3.5  ·  mobilerun  ·  mobile-use  ·  未来别的  │
│        ↑ 各写各的薄适配（1 个文件 / 0 行）                       │
├──────────────────────────────────────────────────────────────┤
│ ★ 契约层（我们定的基座，全平台统一）                             │
│   · 设备发现      GET /devices                                │
│   · 感知          GET /devices/{id}/screenshot                │
│                   GET /devices/{id}/ui  ← ★ OCR 伪装成 a11y 树 │
│   · 动作          POST /devices/{id}/actions                  │
│                    { action, coordinate?, coordinate_space?,   │
│                      screen{w,h}, text?, button? }             │
│   · 能力内省      GET /devices/{id}/capabilities ← ★ 声明式清单  │
│                    （含 mode/risk/verify/backends/unsupported） │
├──────────────────────────────────────────────────────────────┤
│ 后端层（一台设备一个后端，可插拔）                               │
│   iOS-VNC 后端（现有 57 能力）·  Android 后端（未来）· PC（未来）│
└──────────────────────────────────────────────────────────────┘
```

**★ 三条关键设计**（都来自调研，不是发明）：
1. **契约采用 `VisualRemoteDriver` 的形状**（`mobilerun-core/driver/visual_remote.py`）→ **立刻能被 mobilerun 零改动驱动** ✓
2. **动作语义采用 Mobile-Agent 的 `enum`**（11 个动作 + 0-999 坐标）→ **坐标天然对齐，1 个文件接入** ✓
3. **能力内省采用 §17.8 N1/N2/N7 的声明式清单**（`mode`/`risk`/`verify`/`backends`/`unsupported`）
   → **一次替掉我们"注册表+0x50+5802 三处补齐"和"注册了没实现"两个坑** ✓

---

## 六、落地顺序（建议）

| 阶段 | 做什么 | 验收标准 | 成本 |
|---|---|---|---|
| **P0** | **网关加 3 个契约端点**（`/devices`、`/screenshot`、`/actions`），采用 `VisualRemoteDriver` 形状 | **mobilerun 零改动能驱动我们跑通一个任务** | 小-中 |
| **P0** | **`/ui` 端点：OCR → a11y 树形状**（字段名照 `_a11y.py:16-28` 白名单） | mobilerun 的 `tap_text` 能用文字点到目标 | 中 |
| **P1** | **`/capabilities` 声明式能力清单**（含 `unsupported` + `alternative` + `hint`） | 模型永远拿不到"设备做不到的动作" | 中 |
| **P1** | **Mobile-Agent 适配**（1 个新文件，仿 `AdbTools`） | 官方 `mobile_use` 脚本能驱动我们 | 小 |
| **P2** | **动作空间换成 App 真实能力**（`keyboard` HID / `type.delete` / **双击 Home** / `find_text`） | 见《参考项目调研》P0 | 小 |
| **P2** | **Android 后端**（同一个契约的第二实现） | 换后端不改契约 | 大 |

> **★ 与既有计划的关系**：本方案的 P0/P1 **与 §17.8 的 N1/N2/N7 是同一件事**——
> 只是现在有了**明确的外部验收标准**（"能被 mobilerun 零改动驱动"），
> 而不是我们自说自话地实现一份抽象 ✓

---

## 七、对用户六点设想的逐条回答

| 你的设想 | 结论 |
|---|---|
| ① 细致了解 Mobile-Agent-v3.5 | ✅ **已做**（56KB 勘察笔记）；关键事实：**它的手机框架是 1137 行薄脚本，抽象只在 LLM 层，三端不共享设备抽象** |
| ② 参考有 WDA 能力的项目对它集成 | ✅ 已查：mobile-use 真机走 **WDA**（需 Mac + xcodebuild + iproxy）；mobilerun 走**自家 App 的私有 HTTP**（6643）；**两者都要在设备上装东西 + 都要 Mac** ✗ |
| ③ 以 WDA 同样基类，noVNC 作其分支 | ⚠️ **修正**：Mobile-Agent 没有这个基类 ✗；**真正的"同基类"是 `mobilerun-core` 的 `VisualRemoteDriver`** ✓ —— **我们应实现【它】，而不是"继承 WDA"** |
| ④ 这样就有全平台基座 | ✅ **方向对** ✓ —— 但**基座契约由我们定**，框架写薄适配 |
| ⑤ 集成进网关，不只 iOS | ✅ **正确** ✓ —— **网关加"契约层 + 可插拔后端层"** 即是 |
| ⑥ 以后扩 Android | ✅ **正确** ✓ —— **同一契约的第二后端**；而且调研显示 **Android 侧反而是最成熟的**（大家默认支持）|

---

## 八、待决策

1. **是否先做 P0 第一条**（网关加 3 个 `VisualRemoteDriver` 形状的端点）——
   **理由**：**这是唯一"零改动就能被现成框架驱动"的路径**，能最快验证"基座"这个概念成不成立 ✓
2. **是否需要我先在真机上验证"OCR 伪装成 a11y 树"这条钥匙**（`/ui` 端点）——
   它是"把 57 能力接进现成抽象"的关键，值得先做一个小实验 ✓
