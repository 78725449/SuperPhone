# 三个重点项目深读汇总 —— 面向「通用 MCP 服务器」目标

> 承接《参考项目调研-共同优点-2026-09-18.md》。
> 本文基于对三个项目的**源码级深读**（笔记见 `_research/_notes/`）：
> - **PhoneHarness** `main@1cdc0d9` 【深读】—— mixed-action orchestration harness
> - **mobile-harness**（droidrun）`main@e0e7310` 【深读】—— 它指定的控制库 `mobilerun-core 1.6.2` 也已下源码逐文件读
> - **AgentProg** `main@ef5e299` 【深读】—— Program-Guided Context Management
>
> **用户目标**：把 SuperPhone 做成一个**通用的 MCP 服务器**，用来控制手机、电脑等通用场景。

---

## 一、三个项目各自的定位（先纠正两个认知）

| 项目 | 我们原以为 | **实际是什么** |
|---|---|---|
| **PhoneHarness** | 一个 MCP 编排框架 | **公开仓里【没有真 MCP 客户端】**（全仓 grep `mcp` 仅 28 处）。它做的是"**读配置 JSON → 造工具实例 → 子进程/HTTP 调用**"。真 MCP server 在宿主侧，设备经 `mcp_proxy.py` 间接调 |
| **mobile-harness** | 一个控制库 | **它不是库，是纯 Markdown 说明书**（37 个文件全是 .md/.json/.png，零 Python）。真正的控制库是独立 PyPI 包 **`mobilerun-core`**；三层分家：mobile-harness=说明书 / mobilerun-core=控制库 / mobilerun=带 LLM 的 agent |
| **AgentProg** | 一个上下文管理库 | **一套"程序引导"的完整方法论 + 可移植的三段纯代码**（`compile_workflow()` / `show_python_context()` / `grab_similar_nodes()`）|

**★ 这两条纠正很重要**：
- **想抄 PhoneHarness 的"MCP 实现"会扑空** ✗ —— 它给的是**工具面的组织方式**，真 MCP 协议要我们自己按规范实现
- **想 `pip install mobile-harness` 也不行** ✗ —— 要装的是 `mobilerun-core`

---

## 二、★★★ 三条最有价值的做法（直接对着我们的坑）

### ① 能力必须是一等公民：可内省 + 不支持时给替代路径

**mobile-harness / mobilerun-core 的做法**（【深读】原文级）：
```python
device.capabilities["actions"]        # 能力清单
device.supports(action)               # 查询
@unsupported_on(backend, reason, alternative, hint)   # 装饰器
```
- **不支持的方法体【永不执行】**，直接抛结构化异常 `UnsupportedOperation.as_dict()`
  含 **`verb` / `backend` / `reason` / `alternative` / `hint`** 五个字段
- **`is_supported()` / `describe()` 可【不调用即内省】** ★★★
- 范例（原文）：iOS 的 `app_stop` → `"ios-portal does not expose a force-stop verb"` + hint `"Use device.key('home')"`

**PhoneHarness 的做法**：
- **一份声明式 schema 文件 + 一个加载器**驱动整个工具面 —— `tools/list` 与分派**都从它生成**，
  **加能力只改一个文件** ✓
- 每个工具带三个**元数据字段**：
  - **`mode`**：`complete` / `intent_only` / `bootstrap`（★ 显式声明"完成语义"）
  - **`risk`**：`SAFE_COMPLETE` / `CONFIRM_FIRST` / `NEVER_AUTO` 三级
  - **`verify`**：**一条【非模型原语】的校验**（对齐我们 0 模型路径）
- **统一返回契约**：`ToolResult{ok, output, summary, exit_code, checks, artifact_paths, image_base64}`

**👉 这直接替掉我们两个血泪坑**：
| 我们的坑 | 被这套怎么解掉 |
|---|---|
| **"注册表 + 0x50 + 5802 三处补齐"**（漏一处就返回「未知操作」）| **声明式 schema 一份 → 加载器生成工具面**，加能力只改一个文件 |
| **"注册了但 0x50 没实现 / executor 从未被调用过"**（调用即崩）| **`is_supported()` 不调用即内省** + 不支持时给 `alternative`+`hint` |
| **"ack ok=true 但任务没完成 / 进程已崩"** | **每个工具带 `verify`（非模型原语校验）+ `mode`（完成语义）** |

### ② 必须区分「没有」与「读不到」

**mobile-harness / mobilerun-core**（【深读】原文级）：
- **`UiState.dead_tree`** —— 显式可用性属性（树到底有没有拿到）
- **字段缺失【永不算 hidden】**，注释原文：*"A missing flag is not proof of visibility"*
- **截图用魔数兜底**（PNG/JPEG 头）而**不信 `Content-Type`**
- **视口停摆判定用【容差】而非相等** —— 真机证据：iOS 设置页的 uptime **每秒翻秒**，
  精确比较会**永久失效** ★★★

**PhoneHarness**：
- **语义后验保守否决** —— `final_text` 命中"黑屏/崩溃"等信号 → **把 success 改回 False** ★

**👉 我们的对应问题**：`vision.ocr` 返回空数组时，**我们分不清"屏幕上真的没字"和"这一帧读不到"** ✗
→ 这条在**没有无障碍树**的 iOS 场景下**最值钱**（我们本来就只能靠截屏 + OCR）

### ③ 控制流必须由【程序】保证，模型只填单步

**AgentProg 的做法**（【深读】原文级）：
- **LLM 先生成一份 DSL 程序 STP**（`.ap` 文件，`generate_workflow.py`）
- **`compile_workflow()` 把它机械改写成可执行 Python 骨架**
  （每行包成 `workflow(..., LOOP_FOR/CONDITION/SEQUENTIAL)`）
- **控制流真跑**；**每个节点"这步怎么做"才交给模型现场生成一步代码**
- **禁止模型自己写 `for` / `if`** ★★★
- 双模式交替：`Code Generation`（出一步 Python，如 `mobile.click("'+' button")`）
  ↔ `Workflow Status Update`（**只单选一个操作** `hold/continue/break/return`）
- 护栏：`MAX_RETRY_TIME / MAX_LOOP_TIME = 100`

**PhoneHarness 的同类做法**：
- **`skills/routing.yaml` 是启动即注入的路由卡**（**早于任何 `load_skill`**）
- 把任务分四类：**`COMPLETE` / `COMPLETE via load_skill` / `INTENT_ONLY` / `BOOTSTRAP`**
- **显式标注"完成语义"**（例：`send_email` **只开写信界面、不算完成**）★★★
- **系统提示按此硬编选择顺序**，GUI 标为**最慢兜底（2-5 分钟）**

**👉 我们的现状**：**循环把控制流也交给模型了** ✗ —— 这正是它"跑到一半忘了在干嘛"、"重复动作到步数上限"的根源之一。

---

## 三、★★★ 上下文管理：靠结构剪枝，不靠 LLM 摘要

**AgentProg 的做法**（【深读】）：
```
上下文 = show_python_context()      # 沿 parent 链【只拼当前执行路径】
       + 完整 STP 程序全文
       + summarize_variables()      # 变量快照
       + belief_state               # 信念表
```
- **纯代码，无 LLM 摘要、无 token 阈值触发** ★★★
- **没走到的分支 / 跑完的循环轮次【结构上就不存在】→ 循环 100 轮上下文不增长** ★★★
- **进度 = 程序计数器 + 变量**（不是自由文本）
- **`show_python_context()` 约 10 行纯代码**
- 信念层铁律：**"不能判定真假的条目必须原样保留"**；
  **发现环境被打乱必须立即 `hold` 停下先修，禁止"回头再修"**

**对照我们**：我们现在是 **"最近 4 张截图 + 文字摘要"**（照抄 MobileAgent 的 `history_n=4`）✗
**→ 换成 AgentProg 式后，"跑到一半忘了在干嘛"会变成显式的一行 `# <-- current step`** ★

---

## 四、失败处理的精细化（我们全都没做）

| 做法 | 谁 | 说明 |
|---|---|---|
| **只对【读】做退避重试，【写】动作不重试** | mobile-harness | 传输层 (0.5, 1.5)s 退避**仅用于读** ★★★ |
| **重复动作熔断** | PhoneHarness | **重复动作 ≥6 次即熔断**；flat 模式下"同一 turn 既截图又动作"**直接 BLOCKED** |
| **危险动作硬禁** | mobile-harness | **`key("power")` 硬禁** —— 在亮屏设备上等于**锁屏困死整个 run** ★ |
| **卡死用 home 解** | mobile-harness | `ui_with_recovery()` 超时后 `key("home")` 再读一次，解序列化器卡死 |
| **滑动卡住先降级一次再早退** | mobile-harness | `scroll_until` 卡住 → 降级成更慢更大的滑动一次 → 仍不动即提前返回 |
| **HITL 只 gate 不可逆动作** | mobile-harness | 判据原文：**"不可逆才 gate"**（它只 gate install/uninstall）|
| **反转矩形/零宽高拒点** | mobile-harness | 注释原文：*"a touch that can't land would otherwise be a silent no-op (the worst failure mode: it **looks** like the tap worked)"* ★★★ |

**👉 最后一条尤其戳中我们**：我们的 `touch.tap` **只要发出去就 ack ok=true** ✗ ——
而 mobile-harness 的立场是：**点不到就必须拒绝，不能让它"看起来成功了"**。

---

## 五、★★★ 对「通用 MCP 服务器」的具体设计建议

### 5.1 工具面：一份声明式 schema 驱动（抄 PhoneHarness）

```
device-capabilities.yaml          ← 唯一真相源，加能力只改这里
  - id: touch.tap
    title: 点击
    input: {x: number, y: number}
    mode: complete                ← complete / intent_only / bootstrap
    risk: SAFE_COMPLETE           ← SAFE_COMPLETE / CONFIRM_FIRST / NEVER_AUTO
    verify: {kind: hash_changed}  ← ★ 一条【非模型原语】校验
    backends: [ios-trollvnc]      ← ★ 哪些后端支持
  - id: app.stop
    backends: []                  ← iOS 不支持
    unsupported:                  ← ★ 不支持时给替代路径
      reason: "TrollVNC 未暴露强杀 App"
      alternative: key("home")
      hint: "iOS 用 home 退回桌面即可"
```
**加载器**据此生成：`tools/list`（MCP 协议）+ 分派 + 能力内省 `is_supported()` **不调用即知**

**收益**：① 消灭"三处补齐"；② 消灭"注册了没实现"；③ 模型**永远不会发出设备做不到的动作**

### 5.2 分层与角色：抄 PhoneHarness 的双模型分工

**PhoneHarness 的三层物理角色**（【深读】）：
```
设备内  python3 -m phoneharness server (:8920)   ← 设备侧执行器（纯 stdlib）
宿主    gui_proxy.py (:8919, 转 adb shell)       ← GUI 通道代理
宿主    mcp_proxy.py (:8921, 宿主 shell 代理)     ← CLI 通道代理
模型    编排模型 + gui_model 双分工（_resolve_gui_backend 强制显式、无隐式回退）
```
**★ 它还有个做法（`run_seed_gui_subtask`：主模型只发目标，视觉操作整体外包给 `gui_model`）**：

**⚠️ 初稿曾写"这正好解掉我们的硬前提（演员没眼睛）"—— 复核架构总纲第五章后判定该表述不准确，已修正如下：**

> **第五章已论证过"GUI 工作模型"是伪需求** ✗ —— 理由是**它的职责（看图→出坐标）设备端已用零模型原语做完了**
> （`vision.find_text` 文字→坐标、`vision.find_image` 图标→坐标，零模型 <1s），
> 而它要解决的"一半以上可点击目标没有文字"的正解是**「图标模板库 + find_image」**。
>
> **PhoneHarness 之所以上模型，是因为它的设备层只有 `adb screencap` + `uiautomator`，没有"文字→坐标"的确定性原语** ——
> **是条件逼出来的，不是更优解。** 即：**第五章的判断更优，我们不应对标它的双模型。**

**★ 但它暴露了一个我们【真实的空档】**：
第五章删"GUI 工作模型"的**前提是设备端视觉原语可用**。现状是 ——
**`vision.find_text` ✅ 可用，但 `vision.find_image` ❌ 缺模板来源、加了也用不了**（图标模板库尚未建立）。
→ 于是存在「'看图→出坐标'设备端答不了、而编排模型又没有视觉」的**空档**。

**★★ 空档的三条填法（按本项目哲学排序）**：
1. **优先建图标模板库**（架构总纲 §13.2 既定路线）—— **正解，最符合"零模型"哲学** ✓
2. **临时用纯视觉模型补位**（PhoneHarness 思路）—— **只可是临时救急，模板库可用后必须撤下**，否则就是伪需求回归 ✗
3. ★ **导向"少走视觉"**（公理一：**GUI 是最后兜底，不是默认路径**）—— **视觉占比本就该很小，这个空档因此不该致命** ✓

**→ 结论**：**硬前提的真正解法是「少走视觉 + 建模板库」，不是引入模型。**（已同步补注到架构总纲 §5 与 §17.8.2）

### 5.3 权限在代码层，不是 prompt

**PhoneHarness 的硬门**（【深读】）：
- `mcp_proxy.py` **必须传 `--allow-root`**，否则**启动即报错**
- **cwd 必须落在允许根下**，否则 **403**
- **默认只绑 `127.0.0.1`**
- 而 skill YAML 里的 `allowed_tools` **只注入 prompt、不强制** —— **它明确区分了"提示"与"强制"**

**👉 我们的对应**：当前靠"约定"而非代码 ✗ → 应把**禁用清单**（如 `power`）做成代码层硬门

### 5.4 技能沉淀：三个项目【都没做】，这是我们的机会

| 项目 | 跨任务技能沉淀 |
|---|---|
| **AgentProg** | ❌ `cache_mode` **默认关闭**，且只比连续两次、只在进程内 |
| **PhoneHarness** | ❌ 只有**人工维护**的 `skills/*.yaml`，无自动沉淀 |
| **mobile-harness** | ❌ 无 |

**★ 但 AgentProg 留下了一个现成的实现**（【深读】）：
> **`grab_similar_nodes()` —— 按 (文件名, 行号) 精确召回同一程序步的历史代码**

**→ 这就是现成的"第二次执行查表"** ★★★
**→ 与主干设计的「技能卡」位置完全一致** —— **我们做出来就是超前**

### 5.5 三段可直接移植的纯代码（AgentProg）

| 函数 | 作用 | 成本 |
|---|---|---|
| **`compile_workflow()`** | 把 DSL 程序改写成可执行骨架（控制流真跑）| 移植价值高 |
| **`show_python_context()`** | 只拼当前执行路径（**约 10 行**，无模型调用）| **立即可用** |
| **`grab_similar_nodes()`** | 按 (文件名,行号) 召回历史代码 | **技能卡的关键** |

---

## 六、把三个项目的启示合并成一句话

> **PhoneHarness 告诉我们【工具面怎么组织】**（声明式 schema + 元数据 + 代码层权限）
> —— 但它的**双模型/视觉外包我们不采纳**（架构总纲 §5 已论证该层是伪需求；它上模型是条件所迫，见 §5.2 修正）
> **mobile-harness 告诉我们【能力怎么建模】**（可内省 + 不支持给替代路径 + 区分"没有"与"读不到"）
> **AgentProg 告诉我们【上下文怎么管】**（控制流由程序保证 + 结构剪枝 + 程序计数器 + 历史代码召回）
>
> **三者拼起来，正好是一套完整的「通用设备 Agent 服务器」骨架** ——
> **而且它们三家【都没有做跨任务技能沉淀】，那恰恰是我们主干设计里最核心的一块。**

---

## 七、与我方现状的对接（下一步可选动作）

| 优先级 | 动作 | 依据 | 成本 |
|---|---|---|---|
| **P0** | **建 `device-capabilities` 声明式清单**（含 mode/risk/verify/backends/unsupported）驱动工具面 | §5.1 | 中 |
| **P0** | **动作空间换成 App 真实能力**（keyboard HID / type.delete / 双击Home / find_text / script.exec）| 前一份报告 P0 | 小 |
| **P0** | **每步加 `verify`（`screen.hash` 零模型判定）** + **重复动作熔断（≥6）** | §4 | 小 |
| **P1** | **区分"没有"与"读不到"**（OCR 空 → 是没字还是没读到）| §2② | 小 |
| **P1** | **写动作不重试 / 危险动作硬禁（power）** | §4 | 小 |
| **P1** | **建图标模板库**（让 `vision.find_image` 真正可用）—— 这是"看图→出坐标"的正解，**不是**引入视觉模型 | §5 边界补注 + §13.2 | 中 |
| **P2** | **AgentProg 式上下文**（程序全文 + 当前路径 + 变量快照）| §3 | 中 |
| **P2** | **技能沉淀**（`grab_similar_nodes` 思路 + 我们的 `script.exec`）| §5.4 | 中 |
