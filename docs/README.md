# 文档体系索引（2026-08-16 最终版）

> **分层**：规则（开发宪法，根目录）→ 架构（01/02）→ 状态（00）→ 规划（03）。
> **维护纪律**：改代码必须同步架构/状态文档；**文档只记录最终架构实际存在的机制**——已废弃/纠错前的概念（能力即服务、能力里程碑、能力追踪清单、历史推进记录、远期设想等）一律不收录、不维护，历史可在 git 中追溯；`00-当前进度基线.md` 是唯一"当前事实"入口。

## 活跃文档

| 文件 | 角色 | 读前须知 |
|---|---|---|
| [00-当前进度基线.md](00-当前进度基线.md) | **当前状态（living）** | §0 现状速览为准；2026-08-16 已精简（历史记录删除） |
| [01-架构-能力与隧道.md](01-架构-能力与隧道.md) | 架构：设备端命令接口/隧道/前端契约 | 协议条文以开发宪法为准 |
| [02-架构-UI与菜单.md](02-架构-UI与菜单.md) | 架构：控制台 UI/菜单契约 | 交互与清单以 `trollvnc-farm/web/` 代码为准 |
| [03-规划-构建与边界.md](03-规划-构建与边界.md) | 规划：构建交付/明确不做/剩余卡点 | 2026-08-16 简化（里程碑/扩展规划已删） |

## 单一真相源映射（改代码 → 同步哪里）

| 改动 | 唯一真相源（先改这里） | 需同步的文档 |
|---|---|---|
| 端口 / 协议 / 命令字 / REST 端点 | 开发宪法 2.7 / 7.x | 00 §0 速览（如涉及） |
| 前端契约（KEY_DEFS / BATCH_CAPS / CONFIG_DEFS / API 封装） | `trollvnc-farm/web/caps.js` | 02 §3 语义节（清单文档不复制） |
| 设备端命令注册表 | `TrollVNC/src/TRCapabilityRegistry.mm` | 01 §3（仅结构变化时） |
| 构建 / CI / 产物 | `.github/workflows/build.yml` | 03 §1 |
| UI 交互行为 | `trollvnc-farm/web/`（app.js/index.html/style.css/press.js） | 02 §1-2 |
| 现状事实（端口/端点/产物/状态） | 代码 + 00 §0 | 00 优先，其余引用 |

> 规则：**文档不复制代码清单**——列表类信息（配置项、能力项、按键表）只存在于代码真相源，文档只写语义与指针。

## 根目录

- `../开发宪法.md` — 规则层（v2.3，2026-08-16：过时条款清理 + 远期设计清除 + 单一真相源治理）
- 全局规则：`~/.zcode/AGENTS.md`（用户级，所有工作区生效，先于工作区文档加载）
