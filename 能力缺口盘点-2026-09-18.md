# 能力缺口盘点（2026-09-18）

> 用户三问：
> ① **"我们应该看我们欠缺什么能力才对吧？"**
> ② **"我们既然已经全部跑通了，证明我们当前的实现是正确的路径，我们对原项目是怎么改造的？"**
> ③ **"我们 app 自身各个动作的原语它知道了吗？"**
>
> 数据来源：`scripts/inventory-capabilities.py`（读设备端注册表 + 对照各框架接口，只读）

---

## 〇、一句话结论

> **接入这条路是对的，但它是一条【窄路】——**
> **我们把 55 个能力的车，开上了只能过 8 个能力的桥。**
> **下一步不是继续在这条桥上跑，而是修我们自己的路（MCP）。**

---

## 一、问题③的答案：**它不知道，而且差得很远** ✗

### 1.1 盘点结果（实测）

```
设备端注册能力总数：55 个
Mobile-Agent 用到的：8 个（通过 AdbTools 那 9 个方法的接口）
它【完全不知道】的：32+ 个
```

### 1.2 它不知道什么（按价值排序）

| 类别 | 它不知道的能力 | 为什么可惜 |
|---|---|---|
| **感知/视觉** | `vision.ocr` · `vision.find_text` · `vision.find_image` | ★ **三家参考项目的 iOS 定位 100% 依赖 a11y 树、OCR 零命中** —— 这是我们的独有优势 |
| **零模型校验** | `screen.hash` · `screen.snapshot` | ★ 判"这步成没成"的零模型原语（对齐架构总纲"验证必须由非模型原语承担"）|
| **输入（比 adb 正统）** | `keyboard`(HID) · `type.delete` | ★ 比 WDA 的输入法注入更干净；`type.delete` 是"删除"（`type.paste` 只能追加）|
| **手势（它压根没有）** | `touch.doubleTap` · `pinch` · `taps` · `twoFingerTap` · `threeFingerTap` · `curveSwipe` | 双击 Home 切后台、缩放、连点 |
| **低层触控原语** | `touch.down/up/event/eventStream/downMulti/upMulti/reset/…` | 精确控制 |
| **硬件/系统** | `power` · `volup` · `voldn` · `mute` · `bridn` · `briup` | 锁屏 / 音量 / 亮度 / 静音 |
| **剪贴板** | `clipboard.get` | 读回输入内容做确认 |
| **执行器** | `script.exec` | ★ **技能卡回放的执行器**（我们架构的核心资产）|
| **数据/设备** | `data.read` · `device.rename` | 维护类 |

### 1.3 根因

**Mobile-Agent 的动作空间是【枚举写死在 SYSTEM_PROMPT 里的 11 个 adb 风格动作】** ✗
（`mobile_use/utils.py:300-320`）

**那份 schema 里根本没有 `keyboard` / `find_text` / `screen.hash` 这些词** ✗
→ **模型不可能调用它没听说过的动作** ✗

**★ 且 `system_button` 的 `Menu` 在 PC/Web 端【根本不存在】**（子代理实测）
→ **连它自己的三端都没对齐，更没有余量装我们的能力** ✗

---

## 二、问题①的答案：缺的是【三类】，不是一类

### A 类：设备端**真缺**的能力（要从 TrollVNC 补，2–3 个）

| 缺什么 | 后果 | 难度 |
|---|---|---|
| **查前台 App bundleId** | ★ **最要紧** —— 多框架靠它做"我现在在哪个 App"的判定；`current_app_id` / `assert_on` / `wait_for_app` 全依赖它 | 中 |
| **`app.list` 包含系统 App** | `TRCapabilityRegistry.mm:939` 有 `if (type == "System") continue;` → **「打开设置」「打开相机」解析不到 bundleId**（实测已暴露：候选为空）| **小** |
| **App 中文名规范** | 实测「微信」未命中（官方表里是 Android 的 `com.tencent.mm`）→ 需确认 `app.list` 返回的 `name` 实际形态，再决定补别名还是改设备端 | 小 |

### B 类：我们**有**但框架**用不到**的能力（32+ 个）

**根因不是我们缺，是它【看不见】** ✗
- 不改它的提示词 → 永远用不上
- 而**改它的提示词 = 改法** ✗ → PR 会被拒
- **→ 正解是 MCP**：工具由 `tools/list` **动态发现**，不受枚举约束 ✓

### C 类：我们**不该补**的能力（判断后主动放弃）

| 不该补 | 理由 |
|---|---|
| `install` / `uninstall` IPA | 风险高；与"内网自用"目标无关 |
| `grant_permission` | 同上 |
| `terminate_app`（强杀进程）| **官方降级建议就是 `key('home')`** —— 我们已有 `home` ✓ |

> **★ 这一类的价值在于"明确不做"** —— 否则"缺口清单"会无限膨胀。

---

## 三、问题②的答案：改造清单（**只加不改**）

```
Mobile-Agent v3.5（官方）
├── mobile_use/ios_tools.py          ★ 新增 232 行 —— IosTools（9 方法 + 运行时注入官方别名表）
├── mobile_use/device_tools.py       ★ 新增  28 行 —— create_device_tools 工厂
├── mobile_use/run_gui_owl_1_5_for_mobile.py   ~ 改 3 处
│     · import 工厂（+1 行）
│     · 加 --platform / --gateway（+2 参数）；--adb_path 改为非必需
│     · 实例化换成工厂调用（变量名 adb_tools 保留，避免动 12 处）
├── mobile_use/utils.py              ~ 改 1 处（修上游 file:// bug，+6 / −1）
└── AdbTools / handle_open_action / build_messages / parse_action /
    rescale_coordinates / annotate_screenshot / packages.py /
    时序 / 动作协议                    —— 完全未动 ✓
```

**★ 三条纪律**：
1. **不加抽象基类** ✗ —— Python 鸭子类型本就不需要；加基类 = 动它的架构
2. **不改 `AdbTools`** ✓ —— 官方代码零风险；PR 只增不改，接受度最高
3. **用【运行时注入字典】解决依赖** ✓ —— 而不是改 `handle_open_action`

**★★ 唯一改了官方既有代码的地方**：`utils.py:image_to_base64`（修 `file://` 前缀）
**—— 那是上游真 bug**：
```python
build_messages() 产出 {"image": "file://" + path}     # utils.py:401/407/415/422
image_to_base64() 直接 Image.open(path)               # utils.py:436 → OSError: [Errno 22]
→ 官方 GUIOwlWrapper（OpenAI 兼容路径）【从来没有被跑通过】
```

---

## 四、★★ 对"已经跑通 = 路径正确"的精确补充

**✓ 跑通证明的是**：**"接口同构 + 运行时注入"这条【接入】路径正确**
- 9 个方法与 `AdbTools` 签名完全一致 → 主循环零感知
- 官方 `handle_open_action` 两条路径都命中 iOS bundleId → **"无缝"成立**
- 真机任务两次跑通（`open` / `click` / `type` 都验证过）

**✗ 它【没有】证明**：**我们的 55 个能力能被用上**
- **恰恰相反**：实测只有 **8 个**能被用上
- **剩下 47 个**（含 `vision.ocr` 这个独有优势）**全都闲置**

**★★★ 所以准确表述**：
> **接入路径正确 ✓，但它是一条窄路（9 方法 vs 55 能力）。**
> **下一步要做的是【把桥拓宽或另修一条路】，而那条路叫 MCP。**

---

## 五、待办（按优先级）

| # | 做什么 | 类别 | 依据 |
|---|---|---|---|
| **1** | **补「查前台 App bundleId」能力** | A | 多框架硬依赖；接任何框架都绕不开 |
| **2** | **`app.list` 包含系统 App**（去掉 `System` 过滤或加白名单）| A | 实测「设置」「相机」解析失败 |
| **3** | **确认 `app.list` 的 `name` 形态**，补常用 App 别名 | A | 实测「微信」未命中 |
| **4** | **做 MCP 服务器**（先 10 个核心工具）| B | **这是唯一能让 47 个闲置能力出场的路** |
| **5** | 扩到 55 能力 + 声明式能力表（`mode`/`risk`/`verify`/`backends`/`unsupported`）| B | §17.8 N1/N2/N7 |
| **6** | 跑一个需要 `swipe` 与 `back()` 的任务 | 验证 | 补全 9 方法最后两个未验证项 |
| **✗** | ~~install/uninstall/grant_permission/terminate_app~~ | C | **明确不做** |
