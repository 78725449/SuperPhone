# 能力与菜单统一 · Phase 1「菜单规范化」实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 按 07-能力与菜单统一设计 §3/§4/§7 落地：按键对象（keyDefs）+ 按压识别（PressRecognizer）+ 四处菜单收敛 + 配置入口入卡片菜单 + 设置项动作暴露。Phase 2（5901 端口收敛 + AI 能力模型，§5/§6）另立计划。

**架构：** 设备端 TRCapabilityRegistry 标 internal（数据源）；前端 caps.js 提供 keyDefs 常量 + menuCaps(scope) 过滤 + PressRecognizer 按压识别组件；renderCapOps/showTileMenu/showBatchMenu 三处只传 scope；5801 按钮抽 keyDefs 常量渲染。

**技术栈：** ObjC++（设备端）、ES Module（web/caps.js、web/press.js）、原生 JS（index.vnc）、Node 单测（test/caps-test.js 模式）。

**前置：** 已批准 07-能力与菜单统一设计.md。测试命令：`cd trollvnc-farm && npm test`（含 caps-test.js）。设备端编译：TrollVNC 的 make/build（CI 验证）。

---

## 文件结构

- `TrollVNC/src/TRCapabilityRegistry.mm` — 修改：internal 标记（31 项原语+AI 专用+手势类）；新增 settings.generateKeys/searchGateway 动作项注册
- `trollvnc-farm/web/caps.js` — 修改：新增 `KEY_DEFS` 常量、`menuCaps(device, scope)`、`actionCaps(device)`（动作区 5 项）
- `trollvnc-farm/web/press.js` — 创建：`PressRecognizer` 按压识别器（纯逻辑，可单测）
- `trollvnc-farm/web/app.js` — 修改：renderCapOps（控制台菜单=按键区+动作区）、showTileMenu（卡片=4.2+配置入口）、showBatchMenu（过滤）、移除 renderCapOps 尾部 cfgBtn
- `trollvnc-farm/test/caps-test.js` — 修改：menuCaps/KEY_DEFS 测试
- `trollvnc-farm/test/press-test.js` — 创建：PressRecognizer 时序识别测试
- `TrollVNC/layout/usr/share/trollvnc/webclients/index.vnc` — 修改：按钮抽 keyDefs 常量渲染 + tapKey 按压识别
- 设计文档 02/04/能力追踪清单 — 修改：同步

---

### 任务 1：设备端 internal 标记（数据源）

**文件：** 修改 `TrollVNC/src/TRCapabilityRegistry.mm`

**目标：** 按 07 §4.5 清单，将 AI 原语/手势类能力标 `menuLevel=internal`（invoke 仍可用），使前端可据此过滤。

- [ ] **步骤 1：hidNoParam 数组加 menu 标记**

`src/TRCapabilityRegistry.mm` L167-192 数组内，为 14 项 down/up 条目加 `@"menu":@"internal"`：

```objc
        @{@"id":@"home.down",   @"title":@"Home按下",   @"icon":@"🏠",  @"sel":NSStringFromSelector(@selector(menuDown)), @"menu":@"internal"},
        @{@"id":@"home.up",     @"title":@"Home抬起",   @"icon":@"🏠",  @"sel":NSStringFromSelector(@selector(menuUp)), @"menu":@"internal"},
        // …power.down/up、volup.down/up、voldn.down/up、mute.down/up、briup.down/up、bridn.down/up 同理
```

循环内（L196）注册后应用：

```objc
        TRControlCap *cap = [self _registerControl:capId title:item[@"title"] icon:item[@"icon"] route:TRCapRouteHID params:@[] executor:…];
        if (item[@"menu"]) cap.menuLevel = item[@"menu"];
```

- [ ] **步骤 2：consumer/hid/key 参数化原语标 internal**

`consumer.press/down/up`（L206-218）、`hid.press/down/up`（L220-234）注册后设 `menuLevel=@"internal"`；`key.down`/`key.up`（L236-249）同理。方式：注册返回值赋给局部变量后设置。

- [ ] **步骤 3：触控类标 internal**

`touch.down/up`、`downMulti/upMulti`、`downMultiAt/upMultiAt`、`event`、`eventStream`、`taps`、`reset`（L366-500）以及手势类 `touch.doubleTap/twoFingerTap/threeFingerTap`、`curveSwipe`、`pinch`（L310-364）注册后设 `menuLevel=@"internal"`。批量注册（tapIds 数组）循环内加：

```objc
        TRControlCap *cap = [self _registerControl:tapIds[i] title:tapTitles[i] icon:tapIcons[i] route:TRCapRouteTouch params:@[…] executor:…];
        cap.menuLevel = @"internal";
```

- [ ] **步骤 4：stylus.* 与 screen.capture.* 标 internal**

`stylus.tap/down/move/up`（L562-588，循环内）与 `screen.capture.start/stop`（L905-914）注册后设 `menuLevel=@"internal"`。

- [ ] **步骤 5：编译验证**

运行：TrollVNC 目录 `make`（或按 .github/workflows/build.yml 编译路径）
预期：编译通过，无警告级错误

- [ ] **步骤 6：Commit**

```bash
git add TrollVNC/src/TRCapabilityRegistry.mm
git commit -m "feat(caps): mark AI primitives/gestures internal (menuLevel) per 07 design"
```

---

### 任务 2：设置项动作项注册

**文件：** 修改 `TrollVNC/src/TRCapabilityRegistry.mm`；参考 `app/TrollVNC/TrollVNC/TVNCRootListController.m`

**目标：** Root.plist 动作项（PSButtonCell：searchGateway、generateKeys）暴露为可 invoke 能力（07 §7）。

- [ ] **步骤 1：新增 `_registerSettingsActions` 注册块**

在 `_registerNativeCapabilities`（或新方法）内注册：

```objc
- (void)_registerSettingsActions {
    // settings.generateKeys：生成自签 CA + SSL 证书（复用 TVNCRootListController generateKeys 的 ZTSelfSignedCertificate 逻辑）
    [self _registerControl:@"settings.generateKeys" title:@"生成证书" icon:@"🔐" route:TRCapRouteNative params:@[]
        executor:^NSDictionary *(NSDictionary *p, NSError **e) {
            ZTSelfSignedCertificate *ca = [ZTSelfSignedCertificate generateWithCommonName:@"TrollVNC"];
            if (!ca) { *e = …; return nil; }
            // 写回 defaults：SslCertFile/SslKeyFile（与 TVNCRootListController generateKeys 落盘路径一致）
            return @{@"ok":@YES, @"cert": ca.certPath ?: @""};
        }];
    // settings.searchGateway：触发局域网网关搜索（复用 connect 页 searchGateway 逻辑，若跨进程需经 TRGatewayClient 通知）
    [self _registerControl:@"settings.searchGateway" title:@"搜索网关" icon:@"🔍" route:TRCapRouteNative params:@[]
        executor:^NSDictionary *(NSDictionary *p, NSError **e) {
            // 触发 TRGatewayClient 的网关搜索/上报（实现时对齐现有 searchGateway 动作）
            return @{@"ok":@YES};
        }];
}
```

并在注册入口调用。`category` 推断为 native → secondary（管理类，进卡片菜单）。

- [ ] **步骤 2：核对 searchGateway 现有实现并复用**

运行：`Select-String -Path TrollVNC/app/TrollVNC/TrollVNC/*.m -Pattern "searchGateway"`
预期：找到实现位置（预期 TVNCConnectViewController 或 TRGatewayClient），在 executor 内调用同一逻辑（跨模块用注入/通知）

- [ ] **步骤 3：编译验证**

运行：`make`
预期：编译通过

- [ ] **步骤 4：Commit**

```bash
git add TrollVNC/src/TRCapabilityRegistry.mm
git commit -m "feat(caps): expose settings.generateKeys/searchGateway as invoke capabilities"
```

---

### 任务 3：caps.js 新增 KEY_DEFS 常量与 menuCaps/actionCaps

**文件：** 修改 `trollvnc-farm/web/caps.js`；测试 `trollvnc-farm/test/caps-test.js`

**目标：** 前端统一菜单数据源：按键对象清单 + 场景过滤函数（07 §3.1/§3.3）。

- [ ] **步骤 1：新增 KEY_DEFS（12 个按键对象）**

`caps.js` 追加：

```js
/** 按键对象清单（07 §3.1/§4.1）：events 引用设备端能力 id，前端按按压模式翻译 */
export const KEY_DEFS = [
  { key: 'home',    title: 'Home 键',  icon: '🏠', events: { click: 'home',           double: 'home.double',   long: 'home.long' } },
  { key: 'power',   title: '电源',     icon: '⏻',  events: { click: 'power',          double: 'power.double',  triple: 'power.triple', long: 'power.long' } },
  { key: 'volup',   title: '音量 +',   icon: '🔊', events: { click: 'volup',          down: 'volup.down',      up: 'volup.up' } },
  { key: 'voldn',   title: '音量 −',   icon: '🔉', events: { click: 'voldn',          down: 'voldn.down',      up: 'voldn.up' } },
  { key: 'mute',    title: '静音',     icon: '🔇', events: { click: 'mute',           down: 'mute.down',       up: 'mute.up' } },
  { key: 'briup',   title: '亮度 +',   icon: '☀️', events: { click: 'briup',          down: 'briup.down',      up: 'briup.up' } },
  { key: 'bridn',   title: '亮度 −',   icon: '🌙', events: { click: 'bridn',          down: 'bridn.down',      up: 'bridn.up' } },
  { key: 'keyboard',title: '键盘',     icon: '⌨️', events: { click: 'keyboard' } },
  { key: 'spotlight',title: '搜索',    icon: '🔍', events: { click: 'spotlight' } },
  { key: 'snapshot',title: 'Home+Power截屏', icon: '📸', events: { click: 'snapshot' } },
  { key: 'hwlock',  title: '键盘锁/解锁', icon: '🔒', events: { click: 'hwlock' } },
  { key: 'releasekeys', title: '释放按键', icon: '🙊', events: { click: 'releasekeys' } },
];

/** 动作区固定能力 id（07 §4.1，从设备 capMetadata 取元数据渲染） */
export const ACTION_CAPS = ['type.text', 'type.paste', 'clipboard.get', 'clipboard.set', 'screenshot'];
```

- [ ] **步骤 2：新增 menuCaps(device, scope)**

```js
/**
 * 按场景过滤设备能力（07 §3.3/§4）
 * @param device 设备对象
 * @param scope 'console' 控制台侧边栏/悬浮 | 'tile' 卡片菜单 | 'batch' 批量
 * @returns 过滤后的能力元数据数组
 */
export function menuCaps(device, scope) {
  const caps = deviceCaps(device);
  const byId = new Map(caps.filter((c) => c && c.id).map((c) => [c.id, c]));
  if (scope === 'console') {
    // 按键区：KEY_DEFS 的 events 引用的能力（全部存在才渲染）；动作区：ACTION_CAPS
    const ids = [...KEY_DEFS.flatMap((k) => Object.values(k.events)), ...ACTION_CAPS];
    return ids.map((id) => byId.get(id)).filter(Boolean);
  }
  if (scope === 'tile') {
    return caps.filter((c) => c && c.menu !== 'internal');
  }
  if (scope === 'batch') {
    return caps.filter((c) =>
      c && c.menu !== 'internal' && c.category !== 'touch' && c.category !== 'stylus'
      && !/screenshot|screen\.|clipboard|type\./i.test(c.id));
  }
  return caps;
}
```

- [ ] **步骤 3：caps-test.js 追加测试**

```js
import { KEY_DEFS, menuCaps, ACTION_CAPS } from '../web/caps.js';

// KEY_DEFS 12 个按键对象，events 覆盖 click，含 double/long 的按需启用
check('KEY_DEFS 12 个按键对象', KEY_DEFS.length === 12);
check('KEY_DEFS 全部含 click 事件', KEY_DEFS.every((k) => k.events && k.events.click));
check('home 支持 double/long', KEY_DEFS.find((k) => k.key === 'home').events.double === 'home.double'
  && KEY_DEFS.find((k) => k.key === 'home').events.long === 'home.long');
check('volup 支持 down/up', KEY_DEFS.find((k) => k.key === 'volup').events.down === 'volup.down'
  && KEY_DEFS.find((k) => k.key === 'volup').events.up === 'volup.up');
check('ACTION_CAPS 5 项', ACTION_CAPS.length === 5);

// menuCaps 过滤
const fullCaps = [
  { id: 'home', title: 'Home', icon: '🏠', category: 'hid', menu: 'primary' },
  { id: 'home.down', title: 'Home按下', icon: '🏠', category: 'hid', menu: 'internal' },
  { id: 'volup.down', title: '音量+按下', icon: '🔊', category: 'hid', menu: 'internal' },
  { id: 'touch.tap', title: '点击', icon: '👆', category: 'touch', menu: 'primary' },
  { id: 'type.text', title: '文本输入', icon: '⌨', category: 'touch', menu: 'primary' },
  { id: 'screenshot', title: '截屏', icon: '📷', category: 'native', menu: 'primary' },
  { id: 'service.restart', title: '重启服务', icon: '🔄', category: 'native', menu: 'secondary' },
];
const dev = { capMetadata: fullCaps };
check('console 场景不含 internal 原语', !ids(menuCaps(dev, 'console')).some((c) => c.endsWith('.down') || c.endsWith('.up')));
check('console 场景含动作区与按键', ['home', 'type.text', 'screenshot'].every((c) => ids(menuCaps(dev, 'console')).includes(c)));
check('tile 场景排除 internal 保留 secondary', menuCaps(dev, 'tile').some((c) => c.id === 'service.restart')
  && !menuCaps(dev, 'tile').some((c) => c.id === 'home.down'));
check('batch 场景排除 touch/截图', !menuCaps(dev, 'batch').some((c) => c.category === 'touch' || /screenshot/.test(c.id)));
```

- [ ] **步骤 4：运行测试验证**

运行：`cd trollvnc-farm && node test/caps-test.js`
预期：ALL CAPS TESTS PASSED

- [ ] **步骤 5：Commit**

```bash
git add trollvnc-farm/web/caps.js trollvnc-farm/test/caps-test.js
git commit -m "feat(web): KEY_DEFS + menuCaps(scope) filtering per 07 design"
```

---

### 任务 4：PressRecognizer 按压识别器

**文件：** 创建 `trollvnc-farm/web/press.js`；测试 `trollvnc-farm/test/press-test.js`

**目标：** 多击/长按/按下抬起识别，keyDefs.events 声明式驱动（07 §3.2）。

- [ ] **步骤 1：创建 press.js**

```js
// PressRecognizer：按键对象按压模式识别（07 §3.2）
// 用法：attach(element, keyDef, { invoke: async (capId, extra) => {} })
// 事件：pointerdown/pointerup，双击/三击窗口 300ms，长按阈值 800ms

const DOUBLE_MS = 300;
const LONG_MS = 800;

export function attachPress(element, keyDef, opts = {}) {
  const events = keyDef.events || {};
  let downAt = 0, isLong = false, longTimer = null, pressCount = 0, clickTimer = null;

  const fire = (name, extra) => {
    const capId = events[name];
    if (capId && opts.invoke) opts.invoke(capId, extra);
  };

  const onUp = () => {
    clearTimeout(longTimer);
    if (isLong) { isLong = false; return; }        // 长按已触发，抬起不再算点击
    pressCount += 1;
    // 有 double/triple 才启用多击窗口
    if (events.double || events.triple) {
      clearTimeout(clickTimer);
      const need = events.triple ? 3 : 2;
      clickTimer = setTimeout(() => {
        if (pressCount >= need) fire(events.triple ? 'triple' : 'double');
        else fire('click');
        pressCount = 0;
      }, DOUBLE_MS);
    } else {
      fire('click');                                // 无多击：立即执行，零延迟
    }
  };

  const onDown = (e) => {
    e.preventDefault();
    downAt = Date.now(); isLong = false; pressCount = 0;
    clearTimeout(clickTimer);
    fire('down');                                    // down 类按键按下立即触发
    if (events.long) {
      longTimer = setTimeout(() => { isLong = true; fire('long'); }, LONG_MS);
    }
  };

  element.addEventListener('pointerdown', onDown);
  element.addEventListener('pointerup', onUp);
  return () => { element.removeEventListener('pointerdown', onDown); element.removeEventListener('pointerup', onUp); };
}
```

- [ ] **步骤 2：创建 press-test.js（时序模拟）**

```js
// PressRecognizer 时序单测（模拟 pointer 事件）
import { attachPress } from '../web/press.js';

let failures = 0;
function check(name, cond) { console.log(`${cond ? 'PASS' : 'FAIL'}  ${name}`); if (!cond) failures++; }

function makeEl() {
  const el = new EventTarget();
  el.addEventListener = el.addEventListener.bind(el);
  el.removeEventListener = el.removeEventListener.bind(el);
  el.dispatchEvent = el.dispatchEvent.bind(el);
  return el;
}
function press(el, t) { return new Promise((res) => { setTimeout(() => { el.dispatchEvent(new Event('pointerdown')); }, t); setTimeout(() => { el.dispatchEvent(new Event('pointerup')); res(); }, t + 60); }); }

(async () => {
  const home = { events: { click: 'home', double: 'home.double', long: 'home.long' } };
  const volup = { events: { click: 'volup', down: 'volup.down', up: 'volup.up' } };
  const calls = [];
  const el = makeEl();
  attachPress(el, volup, { invoke: (id) => calls.push(id) });

  await press(el, 0);                                // 单击 → 立即 volup
  check('volup 单击立即执行（无多击延迟）', calls[0] === 'volup' && calls.includes('volup.down') && calls.includes('volup.up'));

  calls.length = 0;
  const el2 = makeEl();
  attachPress(el2, home, { invoke: (id) => calls.push(id) });
  await press(el2, 0); await press(el2, 120);        // 两次快速点击 → double
  await new Promise((r) => setTimeout(r, 400));
  check('home 双击识别为 home.double', calls.includes('home.double') && !calls.includes('home'));

  calls.length = 0;
  const el3 = makeEl();
  attachPress(el3, home, { invoke: (id) => calls.push(id) });
  await press(el3, 0);                               // 单次点击 → 窗口超时后 click
  await new Promise((r) => setTimeout(r, 400));
  check('home 单击窗口超时执行 click', calls.includes('home'));

  calls.length = 0;
  const el4 = makeEl();
  attachPress(el4, home, { invoke: (id) => calls.push(id) });
  setTimeout(() => el4.dispatchEvent(new Event('pointerdown')), 0);
  setTimeout(() => el4.dispatchEvent(new Event('pointerup')), 900);  // 按住 900ms → long
  await new Promise((r) => setTimeout(r, 1100));
  check('home 长按识别为 home.long', calls.includes('home.long'));

  console.log(failures === 0 ? '\nALL PRESS TESTS PASSED' : `\n${failures} TEST(S) FAILED`);
  process.exit(failures === 0 ? 0 : 1);
})();
```

- [ ] **步骤 3：运行测试验证**

运行：`cd trollvnc-farm && node test/press-test.js`
预期：ALL PRESS TESTS PASSED

- [ ] **步骤 4：加入 npm test 并全量验证**

修改 `package.json` scripts.test 追加 `&& node test/press-test.js`；运行 `npm test`
预期：全部 PASS

- [ ] **步骤 5：Commit**

```bash
git add trollvnc-farm/web/press.js trollvnc-farm/test/press-test.js trollvnc-farm/package.json
git commit -m "feat(web): PressRecognizer 按压识别器（多击/长按/按下抬起，keyDefs 声明式）"
```

---

### 任务 5：控制台菜单改造（renderCapOps → 按键区+动作区）

**文件：** 修改 `trollvnc-farm/web/app.js`

**目标：** PC 侧边栏（focusOpsCap）与手机悬浮（opsMenuCap）共用新渲染：按键区（KEY_DEFS + attachPress）+ 动作区（ACTION_CAPS）。移除旧「⚙ 设备配置」尾部按钮（移入卡片菜单）。

- [ ] **步骤 1：改造 renderCapOps**

`app.js` L687-732 替换为：

```js
import { KEY_DEFS, ACTION_CAPS, menuCaps, deviceCaps } from './caps.js';
import { attachPress } from './press.js';

/**
 * 渲染控制台操作菜单（07 §4.1）：按键区（KEY_DEFS 按键对象+按压识别）+ 动作区（ACTION_CAPS）
 * @param {HTMLElement} container 容器（focusOpsCap / opsMenuCap）
 * @param {object} device 当前聚焦设备
 */
function renderCapOps(container, device) {
  if (!container) return;
  container.innerHTML = '';
  const frag = document.createDocumentFragment();
  const caps = menuCaps(device, 'console');
  const byId = new Map(caps.map((c) => [c.id, c]));

  // 按键区：分组标题 + 按键对象按钮（按压识别）
  const keyTitle = document.createElement('div');
  keyTitle.className = 'cap-group-title';
  keyTitle.textContent = '按键';
  frag.appendChild(keyTitle);
  for (const k of KEY_DEFS) {
    const meta = byId.get(k.events.click) || byId.get(k.events.down);
    if (!meta) continue; // 设备不支持该按键能力则跳过
    const b = document.createElement('button');
    b.type = 'button';
    b.className = 'op key-op';
    b.title = k.title;
    b.innerHTML = `<span class="cap-icon">${escapeHtml(k.icon || meta.icon || '?')}</span><span class="cap-name">${escapeHtml(k.title)}</span>`;
    attachPress(b, k, { invoke: (capId) => doInvoke(byId.get(capId) || { id: capId, title: capId, params: [] }) });
    frag.appendChild(b);
  }

  // 动作区：ACTION_CAPS 单次点击执行
  const actMeta = ACTION_CAPS.map((id) => byId.get(id)).filter(Boolean);
  if (actMeta.length > 0) {
    const actTitle = document.createElement('div');
    actTitle.className = 'cap-group-title';
    actTitle.textContent = '动作';
    frag.appendChild(actTitle);
    for (const meta of actMeta) {
      const b = document.createElement('button');
      b.type = 'button'; b.className = 'op';
      b.dataset.cap = meta.id; b.title = meta.title;
      b.innerHTML = `<span class="cap-icon">${escapeHtml(meta.icon || '?')}</span><span class="cap-name">${escapeHtml(meta.title || meta.id)}</span>`;
      b.addEventListener('click', () => doInvoke(meta));
      frag.appendChild(b);
    }
  }
  container.appendChild(frag);
}
```

- [ ] **步骤 2：更新调用点**

`app.js` L854-855 传 device 对象（当前已传 `d`）：

```js
renderCapOps($('focusOpsCap'), d);
renderCapOps($('opsMenuCap'), d);
```

确认 `d` 为聚焦设备对象（含 capMetadata），且 `focus.capMetadata = deviceCaps(d)`（L863）不受影响。

- [ ] **步骤 3：运行前端测试 + 手工清单**

运行：`cd trollvnc-farm && npm test`
预期：全部 PASS（caps/press 新增断言）
手工：`npm start` 后打开控制台聚焦设备——侧边栏仅按键区+动作区，无成对原语/触控/管理类

- [ ] **步骤 4：Commit**

```bash
git add trollvnc-farm/web/app.js
git commit -m "feat(web): 控制台菜单改 KEY_DEFS 按键区+ACTION_CAPS 动作区（按压识别）"
```

---

### 任务 6：卡片菜单改造（showTileMenu）

**文件：** 修改 `trollvnc-farm/web/app.js`

**目标：** 卡片「...」菜单 = menuCaps('tile') 分组 + 设备管理段 +「⚙ 设备配置」入口（07 §4.2）。

- [ ] **步骤 1：改造 showTileMenu 能力段**

`app.js` L1485 的 `groupByCategory(deviceCaps(d))` 改为 `groupByCategory(menuCaps(d, 'tile'))`；import menuCaps。能力按钮渲染沿用（保留 cap-name）。

- [ ] **步骤 2：设备管理段加配置入口**

L1518 的 `[['view','查看/控制'],…]` 数组追加 `['config', '⚙ 设备配置']`；`m.onclick`（L1535-1556）加分支：

```js
else if (a === 'config') { showConfigPanel(); }
```

- [ ] **步骤 3：验证 + Commit**

运行：`npm test`；手工打开卡片「...」菜单确认含配置入口
```bash
git add trollvnc-farm/web/app.js
git commit -m "feat(web): 卡片菜单按 menuCaps(tile) + 设备管理段加配置入口"
```

---

### 任务 7：批量菜单过滤（showBatchMenu）

**文件：** 修改 `trollvnc-farm/web/app.js`

**目标：** 批量能力 = menuCaps('batch')（07 §4.3，顺带落地「触控/截图类过滤」历史待办）。

- [ ] **步骤 1：改 deviceCaps → menuCaps('batch')**

`app.js` L501 的 `for (const meta of deviceCaps(dev))` 改为 `for (const meta of menuCaps(dev, 'batch'))`。

- [ ] **步骤 2：验证 + Commit**

运行：`npm test`；手工勾选多台设备验证批量菜单无触控/截图类
```bash
git add trollvnc-farm/web/app.js
git commit -m "feat(web): 批量菜单按 menuCaps(batch) 过滤触控/截图类"
```

---

### 任务 8：5801 按钮 keyDefs 化 + 按压识别

**文件：** 修改 `TrollVNC/layout/usr/share/trollvnc/webclients/index.vnc`

**目标：** 按钮定义抽为常量渲染（消除 #bar/#panel 双份 HTML），tapKey 加按压识别（长按/双击），可选 caps.js 增强（07 §4.4）。

- [ ] **步骤 1：定义按键常量与渲染函数**

在 `index.vnc` 脚本区新增（替换 L193-205 与 L216-229 的静态 HTML 为容器 + 渲染）：

```js
// 按键清单（07 §4.4）：op → { label, icon, ks(keysym) 或 fn(本地操作) }
var BAR_KEYS = [
  { op: 'home',   label: 'Home',  icon: '🏠', ks: 0xff50 },
  { op: 'power',  label: '电源',  icon: '⏻',  act: 'power' },
  { op: 'volup',  label: 'Vol+',  icon: '🔊', ks: 0x1008ff13 },
  { op: 'voldn',  label: 'Vol−',  icon: '🔉', ks: 0x1008ff11 },
  { op: 'mute',   label: '静音',  icon: '🔇', ks: 0x1008ff12 },
  { op: 'briup',  label: '亮+',   icon: '☀️', ks: 0x1008ff03 },
  { op: 'bridn',  label: '亮−',   icon: '🌙', ks: 0x1008ff02 },
  { op: 'kb',     label: '键盘',  icon: '⌨️', act: 'kb' },
  { op: 'clip',   label: '剪贴板',icon: '📋', act: 'clip' },
  { op: 'fit',    label: '适配',  icon: '⛶',  act: 'fit' },
  { op: 'full',   label: '全屏',  icon: '⤢',  act: 'full' },
  { op: 'disc',   label: '断开',  icon: '✕',  act: 'disc' },
];
function renderKeys(containerId, keys) {
  var c = document.getElementById(containerId); if (!c) return;
  c.innerHTML = '';
  keys.forEach(function (k) {
    var b = document.createElement('button');
    b.setAttribute('data-op', k.op);
    b.title = k.label;
    if (k.icon) b.innerHTML = '<span>' + k.icon + '</span>' + (containerId === 'panel' ? '<span>' + k.label + '</span>' : '');
    else b.textContent = k.label;
    c.appendChild(b);
  });
}
renderKeys('bar', BAR_KEYS); renderKeys('panel', BAR_KEYS);
```

`#bar`/`#panel` 的 HTML 改为仅保留空容器 `#bar`、`#panel`（panel 内保留「收起菜单」按钮由 renderKeys 后追加或单独渲染）。

- [ ] **步骤 2：tapKey 加按压识别（长按/双击）**

改造 `dispatchOp` 的按键分支：`home`/`power` 支持长按（keysym down 保持 800ms 再 up，或 act 触发 powerLong）；双击（两次 300ms 内 → 触发 menuDoublePress/powerDoublePress，需设备端 kbdAddEvent 支持对应 keysym——见步骤 3）。

```js
function tapKeyHold(ks, code, holdMs) { rfb.sendKey(ks, code, true); setTimeout(function () { rfb.sendKey(ks, code, false); }, holdMs || 60); }
```

`home`/`power` 按钮在 pointerdown 时启动长按计时（800ms 后调用 `tapKeyHold(ks, code, 60)` 或触发对应双击能力），pointerup 提前则按单击/双击窗口判定。实现时复用 L371 `tapKey` 与现有 `rfb.sendKey`。

- [ ] **步骤 3：设备端 kbdAddEvent 扩展组合 keysym（如需要）**

若长按/双击需设备端状态机（menuDoublePress 等），在 `TrollVNC/src/trollvncserver.mm` 的 `kbdAddEvent` 增加私有 keysym（0x1008ff 段未占用值）映射：

```c
    case 0x1008ffB0UL: // 私有：Home 双击（menuDoublePress）
        if (down) [[STHIDEventGenerator sharedGenerator] menuDoublePress];
        return;
```

（仅当步骤 2 的前端时序方案不足以表达时才需要；优先前端 keysym 时序。）

- [ ] **步骤 4：验证**

设备端编译 `make` 通过；5801 页面（设备本地 http://<ip>:5801）手工验证：两面板按钮渲染一致、长按 Home 生效、可选 caps 增强不破坏连接

- [ ] **步骤 5：Commit**

```bash
git add TrollVNC/layout/usr/share/trollvnc/webclients/index.vnc
git commit -m "feat(5801): 按钮抽常量渲染 + tapKey 按压识别（长按/双击）"
```

---

### 任务 9：文档同步与全量验证

**文件：** 修改 `02-能力即服务架构.md`、`04-UI交互规范.md`、`能力追踪清单.md`

- [ ] **步骤 1：同步 02/04/能力追踪清单**

按 07 文档更新：能力分层（primary/secondary/internal）、四菜单清单、internal 清单、配置入口位置。

- [ ] **步骤 2：全量测试**

运行：`cd trollvnc-farm && npm test`；TrollVNC `make` 编译
预期：全部 PASS / 编译通过

- [ ] **步骤 3：Commit**

```bash
git add "02-能力即服务架构.md" "04-UI交互规范.md" "能力追踪清单.md"
git commit -m "docs: 同步能力分层与菜单清单到 02/04/能力追踪清单"
```

---

## 自检

- **规格覆盖度**：07 §3（KEY_DEFS 任务3、PressRecognizer 任务4）、§4.1（任务5）、§4.2（任务6）、§4.3（任务7）、§4.4（任务8）、§4.5（任务1）、§7（任务2、配置入口任务6）。§5/§6 端口收敛 → Phase 2 计划。
- **占位符**：任务 8 步骤 3 标「如需要」——条件性步骤，已给出具体 keysym 方案，可执行。
- **类型一致性**：`menuCaps(device, scope)` 三处调用一致；`KEY_DEFS.events` 与设备端能力 id 对齐（home.double/volup.down 等均已在注册表存在）；`attachPress(el, keyDef, {invoke})` 签名一致。
