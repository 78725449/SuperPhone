# 实施设计：执行预测 · 蜂群资产 · 步骤级置信度（2026-09-19）

> **本文档地位**：**《架构定案-手机为自主主体-2026-09-19.md》的实施细则**。
> 定案文档回答"架构长什么样"，本文档回答"**具体怎么造**"。
>
> **一句话**：**设备端把每次执行变成一次「带置信度的预测 → 执行 → 误差检测 → 纠错 → 沉淀」的闭环，
> 沉淀出的资产按四层组织，蜂群按三个方向合并。**
>
> **★ 已有 vs 缺**（诚实盘点，贯穿全文标注）：
> **已有**：`script.exec`（意图层执行器，含 `find_and_click`/`wait_for`/`expect`/trace/failOp）· 64 个能力 ·
> `vision.ocr`/`find_text`/`find_image` · `screen.hash`/`diff`/`waitStable` · 群控 + 隧道 + 控制权状态机
> **缺**：① 技能库 ② 页面表 ③ 图标模板库 ④ `vision.layout`（坐标聚类）⑤ trace 补"用了哪档策略"字段
> ⑥ 步骤级置信度 ⑦ 回传通道 ⑧ MCP 层 ⑨ 中心同步 ⑩ `ui.dump`（可行性未证实）

---

## 一、总览：各部分的关系

```
                         ┌──────────────────────────────────┐
                         │ ① 命令下达者（MCP 客户端）        │
                         │   控制台 / 脚本 / 定时器           │
                         └───────────────┬──────────────────┘
                                         │ 结构化命令
                                         ↓
┌────────────────────────────────────────────────────────────────────────┐
│ ② 手机 = 自主主体                                                       │
│                                                                        │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │ ★ 执行预测（§四）                                             │    │
│   │   查技能库 → 预测【链路 + 每步置信度】→ 按阈值决定策略          │    │
│   └───────────────┬──────────────────────────────────────────────┘    │
│                   ↓                                                    │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │ script.exec 零模型执行（§四.3）                               │    │
│   │   每步：动作 → 验证 → 记 trace（★ 含"用了哪档策略"）           │    │
│   └───────────────┬──────────────────────────────────────────────┘    │
│                   ↓ 成功 / 失败                                        │
│   ┌──────────────────┐        ┌────────────────────────────────┐     │
│   │ 成功 → 沉淀       │        │ 失败 → 打回传包（§四.5）        │     │
│   │ · 步骤级统计      │        │   predicted / actual /          │     │
│   │ · 策略命中记录    │        │   evidence / context            │     │
│   │ · 新坑            │        └───────────┬────────────────────┘     │
│   └────────┬─────────┘                    ↓                          │
│            │                    ┌──────────────────────┐              │
│            │                    │ ③ AI 服务 纠错        │              │
│            │                    │ 新序列/参数修正/放弃   │              │
│            │                    └──────────┬───────────┘              │
│            │                               │ 熔断 ≤3 次              │
│            │                               ↓ 重跑                    │
│   ┌────────┴───────────────────────────────┴──────────────────┐      │
│   │ 资产库（§二、§三）                                          │      │
│   │  L0 图标素材库 · L1 页面表 · L2 技能卡 · L3 任务卡          │      │
│   │  ★ 每个"定位"步骤带【步骤级置信度】（§五）                   │      │
│   └────────────────────────────┬──────────────────────────────┘      │
└────────────────────────────────┼───────────────────────────────────────┘
                                 │ 上报
                                 ↓
                    ┌────────────────────────────┐
                    │ 中心：合并（§六）→ 下发全群  │
                    └────────────────────────────┘
```

---

## 一.5 ★★★★★ 「AI 参与」≠「AI 规划」—— 三层分工与命令粒度契约

> **用户 2026-09-19 纠正**："可是第一次也是经过 AI 给手机下达的任务啊，你是不是理解错了"
> **→ 纠正成立。** 初稿说"预置卡能做到 → 零 AI"表述不准确：那是"**手机侧不请求 AI 服务**"，
> **但【客户端侧的 AI】一直都在**。

### 1.5.1 三层分工（必须分清）

```
① ★ 客户端 AI（永远在，不可省）
     作用：人话 → 结构化【意图命令】
     例：  "打开抖音搜南京美食" → open_app{name:"抖音"} + search_in_app{app,q}
     ★ 为什么不可省：手机做不了"语义理解"（那是 LLM 的事）

② ★ 手机预置卡 / 内置流程（零 AI 调用）
     作用：意图命令 → 【步骤序列】
     例：  open_app{name:"抖音"} → [app.list, match, app.open, wait, verify]
     ★ 这就是"手机自己展开"，不需要请求任何 AI 服务

③ ★ 手机请求 AI 服务（按需）
     作用：② 展不开时，请 AI 规划 / 判断 / 恢复
     例：  "把这条视频加入收藏"（收藏按钮是图标，预置卡做不到）
```

**★ 准确表述**（替换一切"零 AI"的说法）：
> **"第一次执行【手机侧】不用请求 AI 服务；但【客户端侧】的 AI 一直在。"**

### 1.5.2 ★★★ 命令粒度契约（客户端只能下【意图级】命令）

```
✅ 正确（意图级，手机自己展开）：
   open_app{name:"抖音"}
   search_in_app{app:"抖音", query:"南京美食"}
   do_skill{intent:"打开抖音搜南京美食看3个作品"}

❌ 错误（原子级，客户端越权）：
   touch.tap{x:0.906, y:0.049}

❌ 错误（步骤序列，绕过手机自主）：
   [app.list, match, app.open, wait_for, expect]      ← 这是【手机内部】的事
```

**★ 为什么这条契约至关重要**：
```
· 若客户端直接给【步骤序列】→ 手机退化成执行器，技能库/页面表/置信度全部被绕过
· 而【客户端 AI 并不知道】：
    - 这个 App 是哪个版本、该用哪档策略
    - 本机有没有这张卡、置信度多少、n 够不够
    - 上次在哪一步失败过、有什么坑
  → ★★★★ 只有【手机】知道这些 —— 所以展开必须在手机
```

### 1.5.3 ★ MCP 工具面的硬约束（由此推出）

```
✅ 暴露给客户端：
   · 原子能力（touch.tap / app.open / vision.find_text …）—— 供 agent 自己编排（备选路径）
   · ★ 意图命令（open_app / search_in_app / do_skill / list_skills）—— 主路径

❌ 不暴露（或默认关闭 + 明确标注）：
   · ★ script.exec（执行任意步骤序列）—— 暴露了客户端就会绕过手机自己编排 ✗
     ★ 它是【手机内部的执行器】，不是对外工具
   · 例外：调试用途的 do_steps{...}，默认关闭
```

> **★ 这条改掉了初稿的一个想反的地方**：初稿打算把 `script.exec` 当工具透出去 ——
> 那样客户端会自己拼 steps，手机的内置智能（技能库/页面表/置信度）就全被绕过了。

---

## 一.6 ★★★★★ 端到端实例：值守类任务（用户 2026-09-19 举例）

> **用户原话**："从手机上暴露的 mcp 服务的端口，对手机下发任务：打开微信，按照'xx'人设对该应用的消息进行消息值守回复，
> 这时是 ai 接收，同时发起消息时也携带了这个设备的基本信息，而 ai 会查其他想要了解的信息的，对吗？"
>
> **→ 大方向对，但暴露了三处必须补的设计。**

### 1.6.1 关键点①：「值守」不是一次 MCP 调用，而是一次【会话建立】

**MCP 是请求-响应协议，没有"长驻任务"的概念** —— 所以长驻必须在手机侧落地：

| 方案 | 做法 | 判断 |
|---|---|---|
| A：客户端轮询 | AI 定期调 `check_messages()` | ✗ 客户端必须一直在线，频率难定 |
| **B：★ 手机侧常驻** | 手机收到任务后**自己常驻值守** | **✓✓✓ 正是"手机=自主主体"** |
| C：手机主动 push | 用 MCP notifications | ⚠️ 需客户端保持连接，不能离线值守 |

**→ 采用 B**：
```
客户端：tools/call start_duty {app, persona, scope, reply_policy}
   ↓ ★ 手机在本地【建立并持久化一个 duty 对象】
★ 客户端【可以离开】✓
   ↓
手机常驻值守：
  · 有新消息 + 有现成技能卡 → ★ 手机自己回（手机侧零 AI）
  · 有新消息 + 没把握       → ★ 手机【主动请求 AI 服务】
```
**★ 这呼应 §1.5**：客户端 AI 在【下发时】参与一次；手机在【值守中】按需请求 AI —— **此时是【手机主动发起】**。

### 1.6.2 ★ 关键点②：设备信息必须【双向】携带

```
方向1  客户端 → 手机：任务上下文（我是谁 / 要什么 / 人设是什么）
方向2  ★ 手机 → AI 服务：设备上下文 ← ★★★ 这个更关键
```

**手机请求 AI 时必须带**：

| 信息 | 为什么关键 |
|---|---|
| device_id / 名称 | 22 台设备，AI 要知道是哪台 |
| **★ 当前登录的账号** | **★★★ 对"人设"是决定性的** —— 同人设在不同账号上表现不同 |
| App 版本 | **决定用哪张技能卡 / 哪个策略档** |
| 屏幕尺寸 | 决定坐标空间 |
| **★ 本机可用资源清单** | 告诉 AI"你能查什么" |

### 1.6.3 ★ 关键点③：AI 查信息用 resources，"能查什么"由手机声明

**已有资源设计**：`skill://`（技能卡）· `page://`（页面表 L1）· `icon://`（图标素材 L0）· `pitfalls://`（坑）

**这个例子暴露要新增的**：
```
· ★ device://info      设备基本信息（id/名称/型号/屏幕/系统/App版本/★账号）
· ★ device://stats     步骤级置信度统计（§五）
· ★ duty://<id>        值守会话状态与日志 ← ★ "值守"要落地成资源
· ★ app://<bundleId>   App 知识（版本/界面特征/页面清单）
· ★ persona://<名字>    人设 ← ★ 见下 1.6.5
```

### 1.6.4 ★★ 完整流程（把这个例子走一遍）

```
【① 下发任务】
客户端 AI                        手机（MCP 服务端）
   ├─ tools/list ──────────────────→│  看有哪些工具
   ├─ resources/list ──────────────→│  ★ 看能查哪些信息
   ├─ tools/call start_duty {       │
   │     app:"微信", persona:"xx人设", scope:"私聊未读", reply_policy:{...}
   │   } ─────────────────────────→ │
   │                                ├─ ★ 落地 duty 对象（持久化）
   │← {"duty_id":"…", "ok":true} ───┤
★ 客户端【可以离开】✓                │

【② 值守中（手机自主）】
                                    ├─ 定期 vision.ocr / 监听消息
                                    ├─ 有消息 → 查技能库
                                    │    ├─ 命中   → ★ 手机自己回（零 AI）
                                    │    └─ 没把握 → ★ 请求 AI（带设备上下文）
                                    │              AI 可能先 resources/read：
                                    │                device://info · page://wechat.chat
                                    │                skill://reply_to_message · pitfalls://wechat
                                    │              然后给"怎么回"
                                    ├─ 执行回复
                                    └─ ★ 沉淀技能卡 + 统计

【③ 客户端查值守状态】
   ├─ tools/call duty_status {id} ─→│
   └─ resources/read duty://<id>/log│  ★ 值守期间的记录
```

### 1.6.5 ★ 「值守会话（duty）」的生命周期（本文件新增设计）

**它必须是一个持久化对象**：
```
id · 创建者（哪个客户端）· app · persona · scope · reply_policy
状态：running / paused / stopped
★ 运行记录：处理多少条 / 回了几条 / 多少走 AI / 多少零 AI / 平均耗时
★ 统计：供蜂群沉淀（该 app 的值守成功率、典型回复耗时、常见坑）
```

**★ "人设"怎么落地是个真问题**（三种方案）：
```
① 作为 prompt 文本传给手机 → 手机转发给 AI 服务 —— 简单，但每次都要传、无法沉淀 ✗
② 作为【资源】存在手机上（persona://xx）—— 更好，客户端只给名字 ✓
③ ★★★ 作为【技能卡的一种】（"回复风格卡"）—— 最好 ✓✓✓
   · 能被蜂群同步（一次定义，全群可用）
   · 能带环境指纹（不同账号/App 版本的人设差异）
   · 能沉淀统计（这个人设的回复被接受率）
```

### 1.6.6 ★ 这个例子对 §八（MCP 工具面）的增量

```
Tools 新增：
  · start_duty / duty_status / stop_duty    ← ★ 值守三件套
Resources 新增：
  · device://info · device://stats · duty://<id> · app://<bundleId> · persona://<名字>
```

### 1.6.7 ★★★★★ 技能卡分【两类】（用户 2026-09-19 关键纠正）

> **用户原话**："可是我们回复消息这个事情本质上每次是要根据不同的聊天上下文进行回复的哦"
> **→ 纠正成立。** 初稿把"技能卡=固定步骤序列"当成了唯一形态，**混淆了「操作骨架」与「回复内容」**。

**★ 两者是不同的东西**：

| | 能否固化 | 说明 |
|---|---|---|
| **① 操作骨架** | **✓ 完全可以** | 打开微信 → 找未读 → 点进 → 读上下文 → 找输入框 → 输入 → 发送 → 返回。**每次完全一样** |
| **② 回复内容** | **✗ 不可能** | 要根据上下文 + 人设生成"该说什么"。**每次必须 AI** |

**→ 所以技能卡必须分两类**：

```
【类型 A：纯确定性卡】
   steps 全是设备原语，手机侧零 AI
   例：打开 App / 搜索 / 翻页 / 截图 / 定位
   ★ 首次之后完全零 AI（2–5 秒）

★★ 【类型 B：带 AI 调用点的「生成型卡」】
   骨架确定性，但有一处（或多处）标了 `op: generate`
   例：回复消息 / 写评论 / 按人设发言 / 判断内容符不符合要求
```

**★ 生成型卡的样子（以"回复微信未读"为例）**：
```yaml
id: reply_wechat_unread
steps:
  - op: open, bundleId: "com.tencent.xinWeChat"
  - op: locate, element: "wechat.chatlist.first_unread"        # 确定性
  - op: tap, from: "{{located}}"
  - op: read_context                                           # ★ 确定性采集
      args: {scope: "current_chat", screens: 3}                # ★ "读几屏"是可沉淀的经验
      → 产出 {{context_full}}
  - op: generate                                               # ★★★ 只有这里要 AI
      args: {template: "persona_reply",                        # ★ 引用 persona 卡（§1.6.5）
             input: {context: "{{context_full}}", persona: "{{persona}}"}}
      → 产出 {{reply_text}}
  - op: locate, element: "wechat.chat.input_box"
  - op: input_text, text: "{{reply_text}}"
  - op: locate, element: "wechat.chat.send_button"
  - op: tap, from: "{{located}}"
  - op: expect, assert: {text_contains: "{{reply_text}}"}       # ★ 零模型验证
```
**★★★ 关键：10 步里只有 1 步要 AI。**

**★ 而"快"的账要分卡类型算**：

| 卡类型 | 时间 | 手机侧 AI |
|---|---|---|
| **A 纯确定性** | **2–5 秒** | **零** |
| **B 生成型** | **3–12 秒** | **一次** |

```
❌ GUI agent 模式（每步问 AI）：8 步 × (5-20s + 往返) ≈ 40–160 秒
✓ 生成型卡：确定性步骤 ~1–2s + ★ 一次 AI 调用 2–10s ≈ ★ 3–12 秒
→ 把 8 步里【7 步】的 AI 需求消掉，只剩【本来就该每次不同】的那 1 步
```

**★★ 生成型卡里【仍然可沉淀】的部分**：
```
· 骨架固化      → 每台手机"怎么操作微信"完全一样，蜂群可同步
· 定位固化      → 找输入框/发送按钮/判断未读 —— 零 AI
· ★ 上下文采集固化 → 怎么读聊天记录（OCR 哪块、读几屏、什么顺序）零 AI
   ★ 而"读几屏够"本身是可沉淀的经验 —— 单台不知道 3 屏够不够，
     22 台的数据能算出"读 3 屏覆盖 95% 的对话"
· ★ AI 调用本身的经验也能沉淀：
   什么上下文必要 / prompt 模板怎么组织 / ★ 生成的回复怎么【零模型验证】
   （太短？有违规词？格式不对？）/ 生成失败怎么办（重试/兜底话术/上报）/
   ★ 统计（人设的回复被接受率、平均生成耗时）
```

**★ 顺带解决：聊天上下文怎么拿**
```
方法1  vision.ocr 读屏幕      ✓ 零模型通用；✗ 只看当前屏
方法2  向上滑动 + 多屏 OCR     ✓ 能拿更多历史；✗ 慢、有边界
方法3  剪贴板「复制聊天记录」   ⚠️ 依赖 App 支持
→ ★ 采用 1+2 组合：读最近 N 屏（N 是卡片参数，且【N 本身是可沉淀经验】）
```



---

## 二、★★★★ 资产体系：四层（这是最重要的结构）

**核心原则：上层【引用】下层，不内嵌。**
> 一个元素学好了，**所有引用它的技能卡都受益**；反之若内嵌，改一处要改十张卡。

| 层 | 资产 | 粒度 | 复用范围 | 来源 | 现状 |
|---|---|---|---|---|---|
| **L0** | **图标素材库** | 元素 | ★ **跨 App / 跨页面 / 跨任务** | 人工截取 / 自动采集（模板匹配命中时回存） | **✗ 缺**（机制有，素材无）|
| **L1** | **页面表（元素表）** | 元素×页面 | **跨任务**（同页面的所有任务共用）| ★ 每次执行的**顺手全页采集**（零成本 §七.1）| **✗ 缺** |
| **L2** | **技能卡（流程）** | 任务 | 特定任务（参数化后可复用）| 首次执行的规划 + 每次 trace | **✗ 缺**（`script.exec` 的 steps 是雏形）|
| **L3** | **任务卡（复合）** | 复合任务 | 特定复合任务 | 多个 L2 的组合 | **✗ 缺** |

**★ 分层的判断依据**（回答"抖音图标长什么样""首页点搜索框点哪里"属于哪层）：
```
"抖音图标长什么样"      → L0（图标素材库）—— 跨 App 跨页面通用
"QQ 图标长什么样"        → L0
"首页点搜索框点哪里"      → L1（页面表）—— 同页面跨任务通用
"搜南京美食的流程"        → L2（技能卡）—— ★ 引用 L1 的"搜索框"元素，【不内嵌坐标】
```

### 2.1 ★★★ L2 分两类：预置卡 vs 学到的卡

**这是一个在初稿里被漏掉、但决定"第一次执行快不快"的设计。**

| 类别 | 来源 | 需要 AI 吗 | 例子 |
|---|---|---|---|
| **★ 预置卡 A 类**（纯确定性）| 手工编写，参数化通用模板 | **✗ 不需要** | `open_app` · `search_in_app` · `back_to_home` · `tap_text` · `swipe_find` · `list_scroll` |
| **★ 预置卡 B 类**（生成型，含 `op: generate`）| 手工编写骨架 + 引用 `persona://` | **✓ 每次一处** | `reply_message` · `post_comment`（见 §1.6.7）|
| **学到的卡** | 首次执行时 AI 规划 / 内置流程沉淀 | 首次需要 | 某 App 特有的复合流程 |

**预置卡清单（首版建议 6 张，全部 A 类纯确定性、手机侧零 AI）**：
```
open_app       {name}    → app.list → match(精确/别名/模糊) → app.open → wait_stable → verify
search_in_app  {app, q}  → open_app → find_text("搜索") → type_paste → wait → verify
back_to_home   {}        → home
tap_text       {text}    → vision.find_text → touch.tap → verify(hash_diff | text)
swipe_find     {text}    → swipe + vision.ocr 循环 N 次 → 命中则 tap
list_scroll    {n}       → swipe × n + vision.layout 采集
```

**★★★ 有了这 6 张 A 类预置卡，「打开抖音」「搜南京美食」这类【第一次执行也不用 AI】** ✓✓✓
**★★ 而 B 类预置卡（生成型）首批建议 1–2 张**：`reply_message{app, persona, target, screens}` ——
**骨架 10 步、只有 1 步 `generate`**（§1.6.7 已给骨架）；**它是"消息值守"的落点**。

### 2.2 ★★★★ 关键区分：「首次执行」≠「首次学」

```
【首次执行】= 预置卡 / 内置流程能做到 → ★ 手机侧零 AI 调用，秒级
              （★ 客户端的"人话→意图命令"那道 AI 仍然在 — 见 §1.5）
              ★★ 例外：命中【B 类生成型卡】时，骨架零 AI，但那一处 `generate` 仍要 AI（§1.6.7）
【首次学】  = 预置卡 / 内置流程都做不到 → 请求 AI → 沉淀新卡
```

**→ AI 真正被用到的场景比直觉少得多**：

| 何时才真的需要 AI | 例子 |
|---|---|
| ★ **B 类卡的 `generate` 步骤** | **"按 xx 人设回复这条消息"** —— **本来就该每次不同**（§1.6.7）|
| 特殊 App 操作（按钮无文字）| "把这条视频加入收藏"（收藏按钮是图标）|
| 需要语义判断 | "找点赞过 1000 的视频"（要读懂数字含义）|
| 卡住时恢复 | 找不到搜索框 → 替代路径 |
| 全新意图 | 预置卡里没有的操作序列 |

**★ 注意区分**：前两类是【内容生成】（必须 AI，无法固化）；后三类是【操作规划】（首次需要，之后可固化）。

### 2.3 ★★★ 调用视觉模型能产出什么可复用的

**核心认知**：
```
❌ 模型的【输出本身】（那段文本、那份 steps）→ 一次性的
✓ 模型输出【被执行并验证成功后】沉淀下来的东西 → 那才是资产
→ ★ 模型是"一次性催化剂"，资产是它催化出的产物
```

**★★★ 最值钱的一条转化：把"需要模型判断的事"变成"查表的事"**
```
第一次：模型看一眼 → "这是搜索结果页"           ← 花钱、慢
★ 沉淀：页面签名 = {综合, 视频, 用户, 商品…}     ← 特征文字集合（L1）
第二次：vision.ocr → 特征命中 → 零模型判定 ✓✓✓
★★ 模型【只付一次学费】，之后永久零模型
```

**完整清单（按调用时刻）**：

| 调用时刻 | 模型输出 | ★ 可复用资产 | 归属 |
|---|---|---|---|
| **规划 Plan** | steps 草稿 | **技能卡（意图→流程）** | **L2** |
| | | 每步的目标描述 | L2→L1 |
| **判断 Judge** | 语义结论 | **★★★ 页面签名（特征文字集合）** | **L1** ← 最值钱 |
| | | 判定规则 `verify` | L2 |
| | | 元素 role | L1 |
| **恢复 Recover** | 替代方案 | **★★★ 降级链 `fallbacks`（踩坑经验）** | L2 |
| | | 新坑 `pitfalls` | L2 |
| | | 新策略档 | L1 |
| **任何时刻** | — | 元素采样（顺手全页采集，零成本）| L1 |
| **任何时刻** | — | 图标裁剪图（匹配命中时回存）| **L0** |

**★ 推论**：**模型调用本身不产出资产；是【调用 + 执行 + 验证成功】才产出资产**。
**而【验证失败】那次也不浪费 —— 它产出【坑】** ✓（这就是"每次执行都有学习目标"的依据）


**★★ 引用关系示例**：
```yaml
# L2 技能卡
steps:
  - op: locate
    element: "douyin.home.search"      # ★ 引用 L1 的元素，不是坐标
  - op: tap, from: "{{located}}"

# L1 页面表
douyin.home.search:
  page: "douyin.home"
  strategies:
    - {kind: cached_point, x: 0.906, y: 0.049, confidence: {...}}
    - {kind: text, value: "搜索", confidence: {...}}
    - {kind: image, template: "douyin_search_icon", confidence: {...}}   # ★ 引用 L0

# L0 图标素材库
douyin_search_icon:
  crop: "<base64 png>", sha256: "...", size: "16x16"
```

---

## 三、技能卡 schema（L2）

### 3.1 文件形态：`SKILL.md` + YAML frontmatter

**对齐 MCP 的 [SEP-2640 Skills over MCP](https://github.com/github/github-mcp-server/pull/3046)**（`skill://` resource + `skills/list`/`skills/get`），
好处：未来能与生态互通；`frontmatter` 元数据可被协议直接返回，不在代码里重复。

```markdown
---
id: search_in_app
kind: intent                      # intent | task
intent: {type: search_in_app, params: {app: "{{app}}", query: "{{query}}"}}

# ===== 环境指纹（合并的维度，§六）=====
env_fingerprint:
  app: com.ss.iphone.ugc.Aweme
  app_version_range: ">=26.0.0 <27.0.0"      # ★ 范围，不是一版本一卡
  screen: 750x1334
  lang: zh-Hans
  account_state: logged_in                    # ★ 账号状态会影响界面

# ===== 步骤（★ 每步引用 L1 元素，不内嵌坐标）=====
steps:
  - op: open, bundleId: "{{app_bundle}}"
    critical: true                            # ★ 失败即中止并回传（§四.4）
    verify: {kind: screen_stable}

  - op: locate, element: "douyin.home.search"
    critical: true
    # ★★★ 步骤级置信度（§五）—— 决定走哪档、要不要验证
    confidence:
      "26.*": {ok: 47, fail: 2,  rate: 0.96, n: 49}
      "27.*": {ok: 2,  fail: 15, rate: 0.12, n: 17}
    policy: {skip_verify_above: 0.95, skip_first_strategy_below: 0.70, abort_below: 0.30}

  - op: tap, from: "{{located}}"
    critical: true
    verify: {kind: hash_diff}

  - op: input_text, text: "{{query}}"
    critical: true

  - op: wait_for, expect: {text: "{{query}}"}, timeoutMs: 5000
    critical: false                           # ★ 非关键：失败记录但继续

  - op: expect, assert: {text: "综合"}         # 验证：搜索结果页特征
    critical: true

# ===== 降级链（★ "踩坑经验"的载体）=====
fallbacks:
  - when: "locate 的策略链全不中"
    steps: [{op: home}, {op: swipe, dir: left, times: 2}, {op: locate, element: "...", retry: 3}]
  - when: "还是不中"
    return: {unsupported: "未找到搜索入口", hint: "当前页面：{{page_signature}}"}

# ===== 版本差量覆盖（★ AI 自愈产生）=====
version_overrides:
  ">=27.0.0":
    steps[1].strategies: [text, image]         # ★ 27.0 起坐标不可用，重排

# ===== 统计（同步用）=====
stats: {success: 141, fail: 6, devices: 6, versions: ["26.9.0","26.9.1","27.0.0"]}
---

## 流程说明（人可读）
1. 打开抖音 → 2. 定位搜索入口 → 3. 点击 → 4. 输入关键词 → 5. 等结果 → 6. 验证

## 踩到的坑（★ 负样本，复用价值最高）
- 搜索结果第一屏可能全是广告 → 需下滑
- 点赞数显示为 "1.2万" 不是数字 → OCR 后需转换
- 标题会截断成 "..." 结尾
- 中文输入若没等键盘弹完就输入会丢字
```

---

## 四、执行预测机制（核心闭环）

### 4.1 为什么叫"执行预测"

```
★ 技能卡 = 【先验】（历史经验）
★★ 执行   = 【用先验预测未来】
★★★ 验证   = 【比对预测与实际】      ← screen.hash / expect / vision.* 是【误差检测器】
★★★★ 偏差 = 【预测误差】→ 回传 AI 纠错
```

**这是控制论的「预测-校正回路」。而它让 AI 的角色精确化：不是逐步决策者，而是纠错者。**

### 4.2 完整回路（10 步）

```
① 收到命令（结构化）     search_in_app{app:"抖音", query:"南京美食"}
        ↓
② ★ 技能库查表 → 预测【链路 + 每步置信度】
     ★★★ 三级查找（这一步决定"第一次执行要不要叫 AI"）：
       a. 查【学到的卡】    命中 → 展开 steps + 参数替换 + 取 version_overrides + 按置信度定策略
       b. 查【预置卡】      命中 → ★ 同样展开（预置卡是参数化的通用模板，见 §2.1）
       c. 走【内置确定性流程】（零 AI，硬编码在设备端的兜底路径）
       d. ★ 以上都不行 → 才请求 AI 规划  ← 这才是真正的"第一次学"
     ★★ 关键：「首次执行」≠「首次学」——
        a/b/c 能覆盖的意图【手机侧零 AI 调用且秒级】；只有 d 才付一次学费
        （★ 客户端侧的"人话→意图命令"AI 始终存在 — 见 §1.5）
        ↓
③ 【可选】预演批准 —— 把预测链路给 AI 看一眼（★ 高风险操作必做：下单/发消息/关注）
        ↓
④ 手机零模型执行（script.exec）
    逐步：动作 → 【按置信度决定是否验证】→ 记 trace
    · critical 步失败 → 立即中止，跳 ⑥
    · 非 critical 失败 → 记录，继续
        ↓
⑤ 成功 → 回结果 + 更新【步骤级统计】+ 记录【策略命中】
        ↓
⑥ ★ 失败 → ★ 先走本地 fallbacks 降级链（★ 不叫 AI，因为 AI 贵）
        ↓ 降级链也不行
   打包【回传包】→ 回传 AI（§4.5）
        ↓
⑦ AI 纠错 → 输出【新序列 / 参数修正 / 放弃】
        ↓
⑧ 手机按纠错结果重跑（★ 熔断：同一任务 ≤3 次）
        ↓
⑨ ★ 无论成败都沉淀（§2.3 的资产化）：
     · 成功且走新路径 → 更新卡片 / 加降级链
     · ★ 模型判断结论  → 转成【页面签名】（判断→查表，§2.3）
     · 失败找到原因   → 新增坑记录
     · 环境瞬时抖动   → 只上报统计增量
     · ★ 顺手元素采样 → 更新 L1 页面表 / 回存 L0 图标
        ↓
⑩ 中心合并（§六）→ 下发全群
```

### 4.3 ★ 执行时【必须记录】的字段（`script.exec` 要补）

```objc
// 现状：trace 里已记 {index, op, ok, detail, x, y} + allOk + failOp
// ★★ 必须补的字段：

每步新增：
  "strategy": "cached_point" | "text" | "image" | "relative" | null
      // ★★★ 定位用了策略链的哪一档 —— 这是【中心重排策略链的唯一数据来源】
  "baseHash": "..."     // 动作前的基线（已有 baseHash 变量，需入 trace）
  "afterHash": "..."    // 动作后（判断 hash_diff）
  "vars": {"{{query}}": "南京美食", "{{located}}": {"x":0.906,"y":0.049}}
      // ★ 变量现场（纠错时 AI 需要知道"它当时拿到了什么"）
  "ms": 234             // 每步耗时（判断"慢得异常"）
  "pageSignature": "bottom-tabs(4):首页|朋友|消息|我"
      // ★ 页面上下文（采集 L1 页面表用）
```

### 4.4 ★ critical 标记（决定【何时】回传）

```
不是"整条链跑完才回传" ✗  也不是"每步失败都立即回传" ✗（会碎）
★ 而是：每步标性质
   {op: wait_for, critical: false}  → 可跳过，记录后继续（减少 AI 调用）
   {op: locate,   critical: true }  → 必须成功，失败【立即中止并回传】
   
好处：
  · critical 失败时后面的必然也错 → 早回传省时间 ✓
  · 非 critical 失败不影响主流程 → 不打断 ✓
  · trace 里记下"跳过过哪些" → 这也是经验 ✓
```

### 4.5 ★★ 回传包 schema（"所有已执行的以结果形式回传"的精确形态）

**关键：必须是【三件】—— 只有"失败了"AI 无从下手；只有纯文本 trace，AI 要"读日志猜屏幕"。**

```json
{
  "kind": "prediction_failure",
  "predicted": {                       // ① 预测了什么（AI 才能对比）
    "intent": "search_in_app",
    "params": {"app": "抖音", "query": "南京美食"},
    "steps": [ {"i":0,"op":"open","expect":"抖音启动需 1-2s"}, ... ]
  },
  "actual": {                          // ② 实际发生了什么
    "trace": [ {"i":0,"op":"open","ok":true,"ms":1200},
               {"i":1,"op":"locate","ok":false,"strategy":"cached_point",
                "ms":800,"detail":"hash_diff 未变（点了没反应）"} ],
    "failed_at": 1,                    // ★ 偏差点
    "vars": {"{{located}}": null}      // ★ 变量现场
  },
  "evidence": {                        // ③ ★★ 失败点的屏幕证据（让 AI 能"看"）
    "ocr_texts": [ {text,x,y,cx,cy,confidence}, ... ],   // ★ vision.ocr 全屏
    "screen_hash": "b814ce09ac",
    "screenshot_b64": "..."            // 可选
  },
  "context": {                         // ④ 上下文
    "env_fingerprint": {"app_version":"27.0.0","screen":"750x1334","account_state":"logged_in"},
    "card_version": 3,
    "device": "553A6EA8-…",
    "attempt": 1                       // ★ 第几次纠错（熔断用）
  }
}
```

### 4.6 ★ 纠错契约 + 熔断

```
【纠错的三种输出】
  a. ★ 新的步骤序列       → 手机重跑（主）
  b. 参数修正（改某一步）  → 更小的改动（辅）
  c. 放弃 + hint          → 兜底（如实降级，不硬撑）

【★ 熔断（必须有，否则无限循环）】
  · 同一 intent 单次任务内纠错上限：★ 3 次
  · 或"连续两次失败在同一步" → 不再纠错，直接返 unsupported + hint
  ★ 对应 AGENTS.md 已有纪律「重复动作熔断（≥6）」

【★ 成本意识】
  每次纠错 = 一次 AI 调用（贵）→ ★ 纠错前【先试本地降级链 fallbacks】✓
  降级链都不行，才叫 AI。
```

### 4.7 预演批准（建议加，高风险场景必做）

```
收到命令 → 技能卡预测出链路
     ↓
★ 把【预测的链路】给 AI 看一眼（一次调用）：
   "我打算这么做：①打开抖音 ②点右上搜索 ③输入'南京美食' ④等结果 ⑤找视频
    这张卡在 26.9.0 上成功率 94%。执行吗？"
     ↓
AI 一次批准（或修正）→ 手机零模型执行整条链
```

**三个好处**：① AI 参与从"每步"挪到"任务前一次"，依然快 ✓
② **高风险操作（下单/发消息/关注）的必要审批点** ✓
③ AI 的"批准/否决"本身是**卡片质量的反馈信号** ✓

---

## 五、★★★★★ 步骤级置信度（本设计的量化基础）

### 5.1 为什么不能是「卡片级」

```
技能卡 "search_in_app" 统计 = {success: 141, fail: 6} → 95.9%
★ 这个数字【掩盖了全部有用信息】✗：
  步骤① 打开 App   141/141  ← 稳
  步骤② 找搜索框   138/141  ← ★ 3 次失败（版本变了？）
  步骤④ 等结果     135/141  ← ★ 6 次超时
→ 95.9% 对执行【没有指导意义】✓✓✓
```

### 5.2 统计单元：四维交叉

```
{ 意图 · 步骤序号 · App+版本 · 策略档 } → { ok, fail, 平均耗时 }

例：
  search_in_app / step1 / com.ss.iphone.ugc.Aweme@26.9.1 / cached_point → 96% (47/49)
  search_in_app / step1 / com.ss.iphone.ugc.Aweme@27.0.0 / cached_point → 12% (2/17)  ★ 崩了
  search_in_app / step1 / com.ss.iphone.ugc.Aweme@27.0.0 / text        → 99% (16/16) ★ 靠它
```

### 5.3 ★ 层级回退（防维度爆炸 —— 必须）

```
22 台 × 3 版本 × 5 步骤 × 3 策略 = 990 个格子
★ 若总共只跑 200 次 → 大部分格子 0-1 样本 → 统计毫无意义 ✗

★ 回退链（每级带样本数 n，调用方知道这个置信度有多可信）：
  ① {意图, 步骤, App, 版本, 策略}   ← 最细，n ≥ N 时用
  ② {意图, 步骤, App, 版本}          ← 省策略
  ③ {意图, 步骤, App}                ← 省版本
  ④ {意图, App}                      ← 省步骤
  ⑤ {意图}                           ← 全局兜底
```

### 5.4 执行策略阈值（置信度 → 实时决策）

```
高置信度（>95% 且 n 充足）→ ★ 直接执行，【不验证】（省时间）
中置信度（70-95%）        → 执行 + 验证
低置信度（<70%）          → ★ 跳过第一档，直接从备选策略开始（不浪费时间试错的）
极低（<30%）              → ★ 不执行，直接上报 / 请求 AI，或走 fallbacks
```

### 5.5 该收哪些维度（克制原则）

| 维度 | 该不该进 | 理由 |
|---|---|---|
| 步骤序号 | **★★★ 必须** | §5.1 已论证 |
| App + 版本 | **★★★ 必须** | 用户指出的核心 |
| 策略档 | **★★★ 必须** | 决定"该用哪档" |
| 账号状态 | **★★ 应该** | 界面可能完全不同 |
| 屏幕尺寸/机型 | ⚠️ 可选 | 22 台可能同型号 |
| 语言/地区 | ⚠️ 可选 | 通常固定 |
| 网络环境 | ⚠️ 谨慎 | 是"环境"不是"资产" |
| 前前置页面 | ⚠️ 谨慎 | 同页面不同入口可能不同 |
| 时间/负载 | ✗ 不进 | 噪声大，收益低 |

**★ 原则：只收【会导致界面/行为不同】的维度，不收【环境噪声】。**

### 5.6 ★ 副产品：版本回归预警

```
中心侧看板：
  search_in_app / step1(找搜索框) / 抖音
    26.9.x：  缓存坐标 96%   文字 99%
    26.9.1：  缓存坐标 97%   文字 99%
    ★ 27.0.0：缓存坐标 12% ✗ 文字 99% ✓

自动动作：
  ① ★ 给 27.0.0 的策略链【重排】：文字提到第一档
  ② 给 26.x 维持原样（缓存坐标仍最快）
  ③ 生成"版本回归预警"
  ④ ★ 下发全群 → 其他 21 台不用再踩
★ 全程【不需要人工干预、不需要重新学整张卡】
```

---

## 六、资产合并规则（中心侧）

### 6.1 三个合并维度

```
① 版本维度：26.9.0 / 26.9.1 / 27.0.0
② 设备维度：6s(750×1334) / 12(1170×2532) / 不同语言
③ 来源维度：哪台手机、哪个账号
```

### 6.2 ★★ 关键洞察：因为我们固化的是「意图级动作」而非像素坐标，版本影响面被压到最小

| 资产类型 | 版本变化时 | 为什么 |
|---|---|---|
| 页面特征签名 | **通常不变** ✓ | 签名抓的是**多特征组合**（"底部4个tab + 顶部搜索图标"），比单元素稳 |
| 动作序列 | **通常不变** ✓ | 因为是"点搜索/输入/等待"，**不是坐标** |
| 判定规则 | **不变** ✓ | 判"屏幕文字含 XXX"，与布局无关 |
| **定位策略链** | **★ 部分失效** ✗ | **只有"缓存坐标"那档会失效**，文字/图标档通常还活着 |
| 坑 | 可能新增 | 新版本可能引入新坑 |

**→ 合并工作量集中在【一档：定位策略里的缓存坐标】。**

### 6.3 ★ 合并算法（按资产类型分别处理）

```
【页面签名】—— 求交集
  多版本都成立的特征 → ★ 提升为"通用签名"（变更稳）
  版本独有的特征     → 变成"版本判定条件"

【定位策略链】—— 按成功统计重排 + 标版本范围 + 层级回退
  ★ 多版本/多设备都成功的策略 → 提升优先级
  只在某版本成立的 → 标 version_range
  ★ 效果：可能从"坐标优先"重排为"文字优先"

【动作序列】—— 找差异，多数不改（只有 UI 大改才需新序列）

【坑】—— ★ 追加，不替换（关键！）
  新坑带版本范围（"26.x 有，27.x 已修复"）
  ★ 坑是【累积的历史】，不是当前状态

【步骤级置信度】—— 按 {步骤, App, 版本, 策略} 累加 + 层级回退（§5.3）
```

### 6.4 合并实例（真实感）

```
手机A（26.9.0）成功： strategies = [cached_point(0.906,0.049), text="搜索"]
手机B（26.9.1）成功： strategies = [cached_point(0.910,0.051), text="搜索"]   ← 偏 0.004
手机C（27.0.0）缓存失败、文字成功： strategies = [text="搜索", image=模板]

【中心合并结果】
search_in_app / step1:
  strategies:
    - {kind: text, value: "搜索"}                            # ★ 三版本全成功 → 提到第一
    - {kind: cached_point, x: 0.908, y: 0.050,
       version_range: ">=26.0 <27.0"}                        # 带版本范围的加速缓存
    - {kind: image, template: "douyin_search_icon"}          # 兜底
  version_overrides:
    ">=26.0 <27.0": {cached_point: {x:0.908, y:0.050}}
    ">=27.0":       {cached_point: null}                     # ★ 自动发现 27 起不可用
  stats: {success:141, fail:6, devices:3, versions:["26.9.0","26.9.1","27.0.0"]}
```

**★★ 这里发生了【质变】** ✓✓✓：
> 单台手机学到的只是"我的坐标是多少"；
> 合并后中心发现【文字定位在所有版本都成功】→ 提为第一档，把坐标降级为"版本限定的加速缓存"。
> **★ 这是任何单台设备【不可能】得出的结论。**
> **→ 蜂群沉淀的价值不是"复制经验"，而是【跨版本/跨设备交叉验证 → 提炼出单台得不出的更稳定抽象】。**

### 6.5 ★★ 避免同步的复杂度：中心当【客户端拉取】，手机不主动 push

**MCP 是客户端拉（pull）协议，服务端不能主动推** ✓

```
【推荐：中心当 MCP 客户端去拉】
  中心定期：
    for 每台手机：
      连它的 MCP → skills/list  → 拿全部技能 + 元数据 + 统计
                 → skills/get   → 拿需要的正文
    → 合并 / 去重(sha256) / 冲突检测 / 灰度判定
    for 达标的新技能：
      连目标手机的 MCP → tools/call skill_upsert {skill}    ★ 下发

★ 优点：手机侧只需提供 3 个工具（skills/list · skills/get · skill_upsert），
        不需要"主动上报"逻辑；天然幂等（sha256 去重）；天然可审计；无并发写问题 ✓✓✓
★ 可选混合：手机在成功执行时顺手 push 一次轻量 stats（只有计数和 trace）
```

### 6.6 三个必备护栏（否则"集群复用"变成"集群一起坏"）

| # | 护栏 | 防什么 |
|---|---|---|
| **①** | **环境指纹过滤** | 6s 上学的卡被下到 12 上 → 必然失败 |
| **②** | **验证计数 + 来源可信度** | 单台一次成功被当通用 → 优先用高计数高成功率 |
| **③** | **★ 灰度发布（带版本可回滚）** | **一条坏经验同步到 22 台 = 22 台一起坏** |

**风险表**：

| 风险 | 护栏 |
|---|---|
| 失败放大 | 灰度 + 回滚 |
| 行为同质化 | 做法表保留多条备选，运行时随机/轮换（防平台风控识别）|
| 技能库并发写 | 中心侧串行化 + 幂等（按 `{意图,环境,设备}` 去重）|
| 污染传播 | 环境指纹过滤 + 来源标注 |
| 界面漂移 | `expect` 断言失败即标记该卡失效，回落重学 |

---

## 七、设备端能力补齐清单（按优先级）

### 7.1 ★★★ `vision.layout`（坐标聚类）—— 已勘察，可直接实施

**为什么做**（三问检验已过）：
```
① 消费者 = 机器（定位逻辑 / 页面表采集）✓
② vision.ocr 只给散文字，回答不了"右上角的搜索"✓ 需新原语
③ 纯坐标计算，零模型零网络 → 0 模型路径里活着 ✓
```

**它能提升什么**：
```
❌ 现在：find_text("搜索") → 37 个文字里找 → 多个"搜索"时有歧义
✅ 有 layout：
   ★ "右上角的搜索"   → row=0 且 rowIndex=rowCount-1 ✓ 唯一
   ★ "第 3 行的按钮"  → elements 里 row==3 ✓
   ★ "这行最右边的"   → 按 row 分组取 cx 最大 ✓    ← dsh-ios 验证过的核心手法
   ★ 页面签名         → "底部有 首页/朋友/消息/我" = 抖音首页 ✓ 判"我在哪"
```

**接口设计**（职责单一：只做结构解析，不做查询）：
```json
// 输入
{ "region": {...}, "minConfidence": 0.5, "yTolRatio": 0.012, "xGapRatio": 0.06 }

// 输出
{
  "ok": true, "width":750, "height":1334, "durationMs":234, "count":34,
  "elements": [
    { "text":"抖音", "x":0.346,"y":0.538,"w":0.072,"h":0.021,"cx":0.382,"cy":0.549,
      "confidence":1.0,
      "row":5, "col":1, "rowIndex":1, "rowCount":4,       // ★ 结构字段
      "role":"label",                                      // ★ 启发式推断
      "clickPad":{"x":0.020,"y":0.008}                     // ★ 外扩量（补 bbox≠按钮）
    }
  ],
  "rows": [ {"row":5,"cy":0.549,"count":4,
             "texts":["今日头条","抖音","抖音火山版","抖音极速版"],
             "cxFirst":0.152,"cxLast":0.847} ],
  "signatureKeys": ["首页","朋友","消息","我"],
  "signatureHint": "bottom-tabs(4); top-search-icon"
}
```

**★ 实测依据**（真机跑出来的）：
```
· vision.ocr 只返回 8 个字段 {text,x,y,w,h,cx,cy,confidence}，位置是归一化 0-1
· ★ 置信度能有效区分正确(1.0)与误识(0.3) → minConfidence 默认 0.5 过滤 0.3 那档
  （★ 这与 dsh-ios 那条"别提高 min_confidence"的建议相反；那条可能针对模拟器或别的后端）
· 主屏实测 37 个文字块；正确的中文标签全是 1.0，误识（'片。'/'KOWHID'/'留wly'）全是 0.3
```

**要补的 6 处**（AGENTS.md「三处补齐」铁律）：

| # | 文件 | 位置 | 改什么 |
|---|---|---|---|
| ① | `TRVisionEngine.mm` | `findImageWithTemplateBase64:` 后（L326 附近）| 加聚类算法（纯计算）|
| ② | `trollvncserver.mm` | L3702 后 | 加 `tvExtHandleVisionLayout` 声明 |
| ③ | `trollvncserver.mm` | L3830 后 | **0x50 分派**加分支 |
| ④ | `trollvncserver.mm` | L5643 后 | 加实现 |
| ⑤ | `trollvncserver.mm` | L4238 后 | **5802 分派**加分支 |
| ⑥ | `TRCapabilityRegistry.mm` | L1329 后 | **注册能力**（走 `_rfbCommand`，模式照 `vision.ocr`）|

**★ 全是【新增】，无一行"改"** ✓

### 7.2 `script.exec` 的 trace 补字段（§4.3）

**`strategy` 字段是【最高优先级】** —— §六的"策略链重排"完全依赖它。
现状：trace 里已有 `{index, op, ok, detail, x, y}` + `allOk` + `failOp` + `baseHash` 变量，**只差把它入 trace**。

### 7.3 图标模板库（L0）

**机制已有（`vision.find_image` + `TRVisionEngine.findImageWithTemplateBase64:`），缺素材。**
来源：① 人工截取常用图标 ② **★ 模板匹配命中后自动回存**（自举）

### 7.4 `ui.dump`（a11y 树）—— ⚠️ 可行性未证实，降级为可选

**2026-09-19 真机实测**：
```
✓ 框架全在：AXRuntime · AccessibilityUI · AccessibilityUIService ·
            AccessibilityUIUtilities · AccessibilityFocusEngine · UIAccessibility ·
            AXContainerServices，以及大量 .axbundle/.axuiservice
✓ 有 2 个 App 链接了 Accessibility（discover.app / MobileNotes.app）—— 但那是【读自己】
✗ AXRuntime 磁盘上是空壳（二进制在 dyld 共享缓存里）
✗ 没有对外的 a11y XPC 服务（只有 AXSettingsShortcuts / HearingAidsTapToRadar 两个设置类 appex）
✗ 设备上【没有 substrate / hooker】（无注入框架）
✗ 而"注入 dylib 到目标 App"这条捷径【被项目红线否掉】（"不越狱不注入 dylib"）

★ 结论：地基全在，但"跨进程读任意 App 的 UI 树"【系统没提供对外服务】。
  要定论只能写个最小 ui.dump 上真机跑一次（未调用过的能力 = 未验收）。
★ 而更重要的是：★ 我们【不需要它】——
  图标类控件的正解是【图标模板库】（确定性匹配），比 a11y/OmniParser 更可靠。
```

### 7.5 ★ 关于 OmniParser（视觉→结构化元素）的判断

> **★★★ 2026-09-19 更新：本条原判「基本用不到」已被【真机实测】修正** ✗ ——
> **原判三条理由逐条见下（保留原文，便于对照）；实测结论见 §7.5.1。**
> **新定位：★「离线资产解析器」，而不是「实时感知层」。**

**原判：基本用不到** ✗，三条理由：
```
① 我们已有 OCR 的结构化输出（text + 归一化 bbox + 中心点 + 置信度）
   —— 差【role】✗，而 role 恰恰是"靠猜最不可靠"的部分
② OmniParser 解决"没有 a11y 怎么办"，而我们的答案是【根本不需要"理解"】——
   我们的定位靠【找文字/找模板/缓存】，不靠"模型理解 UI"
③ ★ dsh-ios 的一手纪律否掉了这条路的可靠性：
   "Icon-only controls (a heart, a bookmark) carry no OCR text by definition"
   "Every tap on a real device has real consequences... NEVER tap an unidentified
    control to find out what it does"
   → YOLO 能【检测到】有图标，但【不知道是什么】，除非 caption 猜（常错）
```

#### 7.5.1 ★★★ 真机实测（2026-09-19）——三条理由的修正

**做法**：下载 OmniParser V2 的 `icon_detect`（YOLOv8-Nano · 38.74MB · 单类别 `{0:'icon'}`）
与 `icon_caption`（Florence-2 基座微调 · 1033MB），在**我们自己的真实 iOS 截图**上实测
（设置 App 12 张 + 抖音 26 张 + 后补的跨视频采样 8 张）。判据事先定好，避免事后找理由。

```
① 原判："已有 OCR，差 role，而 role 靠猜最不可靠"
   ★ 修正：★ 部分成立，但漏了关键一半 ✗ ——
   OCR 能给【文字类】元素，而【图标类】元素它【完全给不了】✗。
   实测：完整管线产出 462 个元素中 ★ 200 个是「纯图标元素」(43%) ✓
   —— 那正是 OCR 永远抓不到的那一半，也正是二期死结所在。

② 原判："我们不需要'理解'UI，定位靠找文字/找模板/缓存"
   ★ 修正：★ 目标不变，但它提供了【造模板的原材料】✓ ——
   "找模板"需要模板库，而模板库需要【裁剪依据】✗ —— 光有坐标点不知道裁哪里 ✗。
   实测：元素表给出 bbox → 裁剪依据有了 ✓（已裁出 859 张图标候选）

③ 原判："dsh-ios 纪律否掉其可靠性；YOLO 只知有图标、不知是什么"
   ★ 修正：★ 前半句被实测推翻，后半句成立但可解 ✓ ——
   · 实测：覆盖率 100%（已知可点击元素 29/29 全被覆盖）· 漏检 0 ✓
   · 与我的 OpenCV 轮廓法【两个完全独立的方法】在同一位置吻合（IoU 0.539）✓
   · 宽高比分析：近方形（图标型）占 73.4%（设置）/ 55.2%（抖音）✓
   · "不知是什么"→ 用 icon_caption 解：200 个图标全命名成功，
     ★ 但关键是【类内一致率 0.86】（全局才 1.3%）→ 先聚类再取众数即可用 ✓
```

**★ 由此确立的新定位与用法**：
```
· 定位：★ 离线资产解析器 ✗（不是实时感知层 ✗）
  —— 在"事后再跑"的前提下，速度不再是硬伤：实测 1.1~1.5 秒/帧，离线可接受 ✓
  —— 且可【反复重跑历史截图】✓（这正是离线解析该有的性质）

· 它补的是：★★ 纯图标元素（OCR 给不了的那一半）✓
  —— 二期死结"抖音首页结构是图标、OCR 抓不到、verify 仅 2/5"有了直接对策

· 分工（不重复劳动）：
  · OCR 我们【用 Apple Vision 系统级】✓（比 PaddleOCR 更懂 iOS 界面）
  · ★ 离线解析用电脑侧 RapidOCR ✓（因为设备端 OCR 要当场取，历史截图补不了）
  · icon_detect 取 bbox + interactivity · icon_caption 取语义 · 聚类做骨架

· ★★ L0 图标库的正确建法（实测确立）：
  ① icon_detect 检出可交互元素 → ② 取纯图标元素（content 为空）
  ③ ★ 按【视觉特征聚类】（pHash）—— 不靠 caption ✗
  ④ ★ 每类取【caption 众数】当名字（类内一致率 0.86 ✓ 可信）
  ⑤ 一致率低的类 → 标记「待人工确认」
  → 每类的 purity 就是【命名可信度】：高=自动可用，低=人工过一眼

· ★★★ 采样协议（本轮最重要的方法论发现，见 §7.5.2）
```

#### 7.5.2 ★★★ `stability` 的语义【由采样协议决定】

```
★ 同一批抖音首页截图，两种采样方式得到完全不同的 stability：
    连拍（间隔 1 秒）  → 32 元素里 ★ 31 个"稳定" ✗（含视频标题/字幕 —— 内容是假的稳定）
    跨内容（帧间换视频）→ 60 元素里 ★ 11 个稳定  ✓（只剩顶栏/底部 tab/右侧操作列）
    ★ 相差 3 倍 ✗

★★ 根因：连拍 8 帧全在同一条视频上 → 测的是"瞬时稳定"✗，不是页面资产需要的
   "跨内容变化的稳定"✗

★★★ 跨内容采样后得到的 11 个稳定元素【正是抖音页面的真实骨架】：
   底部 tab（首页/精选/消息/我）· 底部中间 + 按钮 · 右上角搜索 · 右侧操作列（分享）
   → ★ 这恰恰是二期【需要却拿不到】的抖音页面签名 ✓
   → ★ 二期判"抖音页面签名不可靠"，根因是【采样方式】而非"OCR 抖动"✗

★★★★ 纪律：★ 任何"稳定性 / 置信度 / 支持度"指标，都必须【同时声明它的采样协议】✗
   否则同一个数字在不同协议下含义完全不同，会被误读 ✗
```

**★ 诚实留白（未解）**：
```
· 仍有"假稳定"：连续 8 个视频恰好是同一作者时，作者名与标题也会 8/8 稳定 ✗
  → 需跨【不同作者】采样
· 名字全是英文，与中文 UI 有落差（需映射或中文化）
· 只验了两类界面（设置列表 / 抖音信息流）；键盘页、权限页、弹窗有样本但未处理
· 元素粒度偏细（≈YOLO 框数）；已用【分层】缓解（区域 ⊃ 元素，不合并）
· 聚类阈值未调优（有同类被拆成两簇的情况）
```

**★ 但两块思想值得吸收**：
```
· ★ Set-of-Mark 编号 —— 给 AI 一份"编号元素表"，它只需说"点 5 号"而不是输出坐标
  （AI 最容易错的环节就是输出坐标）—— 我们已有类似的 annotate_screenshot
· "把 UI 转成结构化元素表"这个形态 —— 我们的 vision.ocr + vision.layout 正是它
  （★ 实测后这条更成立了：元素表已能产出，见 §7.5.1）
```

### 7.6 ★ 顺带：`dsh-ios` 的一手纪律（真机踩出来的，直接用）

```
· "Never compare screenshots or count pixels to decide whether something happened.
   Read the text back."                          → 优先读文字，不是比像素
· "Chinese/Japanese labels commonly read at confidence 0.3–0.6. Do NOT raise
   min_confidence to 0.8"                        → ⚠️ 与我们实测相反（见 §7.1），以实测为准
· "ios_sim_tap_row at a RELATIVE position (x/y as fractions of that row's frame —
   a right-side action button is often near x=0.9). Never hand-compute absolute
   screen coordinates from a remembered tree: the tool RE-LOCATES the row in a FRESH read"
                                                  → ★★★ 正是我们策略链该学的
· "expect_count={key,delta} (+1 or -1) ... This is the ONLY reliable confirmation
   a list app offers."                            → ★★★ 列表类 App 的验证原语
                                                  （我们 vision.ocr 能读计数 ✓）
· "Every WebDriverAgent action costs ~0.5 s and WDA serves requests SERIALLY"
                                                  → 教训：串行服务要控调用次数
```

---

## 八、MCP 协议层设计

### 8.1 三种原语（三控制面）

| 原语 | **谁控制** | 有副作用 | 用途 |
|---|---|---|---|
| **Tools** | **★ 模型控制** | **✓ 唯一改变状态** | 动作 |
| **Resources** | **★ 应用控制** | ✗ 只读 | 数据/上下文 |
| **Prompts** | **★ 用户控制** | ✗ 无状态 | 工作流模板 |

**★★ 决定性机制点**：**Resources 是"应用控制"的 —— 模型【不能主动读】**
**→ 要让模型"发现技能"，必须【额外给一个 Tool】（如 `list_skills`）** ✓
（这正是 SEP-2640 里 `list_repo_skills` tool 存在的原因）

### 8.2 标准流程

```
【握手】initialize → serverInfo + capabilities → notifications/initialized
        ★ initialized 之前调 tools/list 会被协议错误拒绝
【发现】tools/list · resources/list · prompts/list
【调用】客户端把 tools 清单注入 LLM context → LLM 输出 tool_calls
        → tools/call {name, arguments} → 手机回 {content:[...], isError}
【传输】stdio 或 Streamable HTTP（★ 我们走 HTTP —— 设备端已有 HTTP server）
```

### 8.3 ★ 工具面设计（两层 + 技能管理）

```
【Tools】★ 模型控制 —— 少而稳定
   ① 原子能力 ~64 个        touch.tap / app.open / vision.* / screen.* / script.exec …
   ② ★ 意图命令 3–5 个       open_app{name} / search_in_app / do_skill / list_skills
   ③ 技能管理 3 个           skills/list · skills/get · skill_upsert
   ★ 关键：技能【不变成一堆 tool】（会爆炸）✗

【Resources】★ 应用控制 —— 多而可变
   · skill://<技能名>        技能卡全文
   · page://<页面签名>        页面表（L1）
   · icon://<图标名>          图标素材（L0）
   · pitfalls://<app>        踩坑清单

【Prompts】★ 用户控制
   · 常用工作流模板（可选）
```

### 8.4 一次 MCP 指令的形状

```json
{"jsonrpc":"2.0","id":7,"method":"tools/call",
 "params":{"name":"open_app","arguments":{"name":"抖音"}}}
```

**★★ 一次任务可能需要【多次】tools/call，由 LLM 决定**：
```
「打开抖音」
  ├─ 路径1（1 次）：open_app {name:"抖音"}          ← 意图工具，手机内部展开
  ├─ 路径2（2 次）：app.list → app.open {bundleId}  ← LLM 自己编排
  └─ 路径3（1 次）：do_skill {intent:"打开抖音"}     ← 复合
★ 所以【工具描述文字 + inputSchema 写清楚】至关重要 —— 那是 LLM 唯一的依据
```

### 8.5 ★ 手机 MCP 的工具面可【从注册表自动生成】

**注册表里每个能力已带 `{name, type, required}` 参数定义** → 直接映射成 MCP 的 `inputSchema` ✓
**→ `tools/list` 零维护，加新能力时 MCP 面自动跟着长** ✓✓✓

### 8.6 ★ 设备端已有基础（勘察结果）

```objc
// trollvncserver.mm:4196  tvHttpApiDispatch —— 已有 {op, params} 分派
static NSDictionary *tvHttpApiDispatch(NSDictionary *req) {
    NSString *op = req[@"op"];                       // ★ 就是"工具名"
    NSDictionary *params = req[@"params"] ?: @{};     // ★ 就是"参数"
    if ([op isEqualToString:@"cap.list"]) ...
    else if ([op isEqualToString:@"vision.ocr"]) ...
    ...
}
// ★★ 与 MCP 的 {name, arguments} 【同构】—— 转换就是换个字段名

// 设备端还有：5802 HTTP server（直连页管理 API）· 5902 日志端点
// → ★ 加 /mcp 路由是【增量】不是从零
```

---

## 九、三视角体感走查（AGENTS.md 第 6 条强制）

### ① 首次执行任务（探索期／"学"）

```
你（控制台）：「打开抖音，搜索南京美食，看 3 个作品」
     ↓
手机：技能库查不到 → 请求 AI 规划（一次，给一份 steps 草图）
     ↓ script.exec 执行 → 某步失败 → 回传包 → AI 纠错 → 重跑（熔断 ≤3）
成功 → ★ 沉淀：步骤级统计 + 策略命中 + 页面表(L1) + 新坑
     ↓
你看到：结果 + 「已学会」
★ 耗时：几十秒~几分钟（最贵的一次）
★ 产出：技能卡 + 页面表 + 图标素材 + 坑 —— 这唯一一次学费【值得付】
```

### ② 第二次执行（回放期／"用"）

```
你：「打开抖音，搜索南京美食，看 3 个作品」
     ↓
手机：技能库★命中 → 预测链路（含每步置信度）→ 按阈值决定策略
     ↓ script.exec 回放 → ★ 手机侧零 AI 调用、一次网络往返（客户端的"人话→意图命令"AI 仍在，§1.5）
若某步失败 → ★ 先走本地 fallbacks（不叫 AI）
            → fallbacks 也不行 → 才回传 AI 纠错（贵，但少发生）
     ↓
你看到：★ 2–5 秒后出结果
★ 与旧形态的差别：旧形态这条路径【根本不存在】（每步都要模型看图）
```

### ③ 蜂群执行（N 台复制／"复制"）

```
1 台学会 → 上报（步骤级统计 + 策略命中 + 新坑 + 环境指纹）
     ↓ 中心合并 / 去重(sha256) / 冲突检测 / 灰度判定
达标后下发其余 21 台 → ★ 直接可用，无需各自再学
     ↓
22 台同时跑 → 各自步骤级统计累加
★ 关键洞察：★ 若 22 台里 18 台成功、4 台在某步失败，
  中心立刻知道【那 4 台是特殊环境（版本/账号/机型）】而不是卡有问题 ✓✓✓
★ 风险（各有护栏）：失败放大 / 行为同质化 / 技能库并发写
```

---

## 十、落地顺序与验证（每步独立可验收）

| 步 | 做什么 | 验收 | 依赖 |
|---|---|---|---|
| **1** | **`vision.layout`**（§7.1，6 处新增）| CI 4 scheme 过 → 真机跑一次，比对 ocr 与 layout 的元素数/行分组 | 无 |
| **2** | **`script.exec` trace 补 `strategy` 等字段**（§4.3）| 真机跑 `script.exec`，看 trace 里有 strategy | 1（layout 提供行/列）|
| **3** | **图标模板库 L0 + 自动回存** | 模板匹配命中并回存，二次调用命中率提升 | 无（机制已有）|
| **4** | **页面表 L1 采集**（每次执行顺手存 OCR+layout 结构）| 连续跑 3 次同任务，页面表条目稳定收敛 | 1、2 |
| **5** | **技能库 L2**（存储/检索/环境指纹/版本覆盖）| 首次执行沉淀 → 第二次命中回放 | 4 |
| **6** | **步骤级置信度 + 层级回退 + 阈值策略**（§五）| 统计条目出现；低置信度步骤自动跳过第一档 | 5 |
| **7** | **回传包 + 纠错契约 + 熔断**（§4.5-4.6）| 人为制造失败，AI 能纠错成功；超 3 次熔断 | 6 |
| **8** | **MCP 层**（§八）| 一个 MCP 客户端 `tools/list` 看到工具、`tools/call` 真操作手机 | 无（可与 1 并行）|
| **9** | **中心同步**（§6.5）+ 三护栏（§6.6）| 两台设备经验合并；灰度可回滚 | 5、6 |
| **10** | ⚠️ **`ui.dump` 可选验证**（§7.4）| 写最小能力上真机，判死或走通 | — |

**★ 纪律（每一步都适用）**：
```
· 改前备份 → data/backups/<语义>-<时间戳>/
· ★ 用 write/edit 逐点改，【绝不用脚本写源文件】（宪法级铁律）
· 设备端改动 → CI 4 scheme 全过；网关改动 → npm test 全过
· ★ 暴露一个从未被调用过的能力 = 给它做首次验收（真机跑最小调用）
· 未验证不声称完成
```

---

## 十一、一句话总结

> **① 资产四层**：L0 图标素材 · L1 页面表 · L2 技能卡 · L3 任务卡 —— **上层引用下层，不内嵌** ✓
> **② 执行预测**：技能卡预测【链路 + 每步置信度】→ 零模型执行 → 误差检测 → 带完整证据回传 AI 纠错 → 熔断 → 沉淀
> **③ 步骤级置信度**：`{意图, 步骤, App+版本, 策略档}` 四维交叉 + **层级回退**，用途是**决定走哪档/要不要验证/要不要提前降级**，
>    副产品是**版本回归预警**
> **④ 蜂群合并**：三方向（版本/设备/来源）× 按资产类型分别处理；**中心当 MCP 客户端去拉**；三护栏防失败放大
> **⑤ 关键优势**：**因为固化的是"意图级动作"而非像素坐标，版本影响面被压到只剩"定位策略里的一档"**
> **⑥ 最该先做的三步**：`vision.layout` → trace 补 `strategy` → 图标模板库

---

## 九、★★★★ 真机实测补章（2026-09-19 · 页面资产与判据）

> **本章全部来自真机实测**，不是设计推演。凡是"实测"标注的，都有可复现的脚本与数据。
> **★ 本章最重的三条**：§9.1 采样协议铁律 · §9.3 自证判据的纪律 · §9.5 弹窗处置链。

### 9.1 ★★★ `stability` 的语义【由采样协议决定】（详见 §7.5.2）

```
★ 同一批抖音首页截图，两种采样方式得到完全不同的 stability：
    连拍（间隔 1 秒）  → 32 元素里 ★ 31 个"稳定" ✗（含视频标题/字幕 —— 内容是假的稳定）
    跨内容（帧间换视频）→ 60 元素里 ★ 11 个稳定  ✓（只剩顶栏/底部 tab/右侧操作列）
    ★ 相差 3 倍

★★ 跨内容采样后得到的 11 个稳定元素【正是抖音页面的真实骨架】：
   底部 tab（首页/精选/消息/我）· 底部中间 + 按钮 · 右上角搜索 · 右侧操作列（分享）
   → ★ 这恰恰是二期【需要却拿不到】的抖音页面签名 ✓
   → ★ 二期判"抖音页面签名不可靠"，根因是【采样方式】而非"OCR 抖动"✗

★★★ 纪律：★ 任何"稳定性 / 置信度 / 支持度"指标，都必须【同时声明它的采样协议】✗
```

### 9.2 ★★★ 判"操作是否生效"要用【状态比较】，不用【事件等待】

```
★ 三次演进，每一版都被数据否掉过：
  ① `wait_for{hashDiff}` —— ✗ 时序敏感：变化若发生在等待之前就错过 ✗
     实测 tab 切换【必踩】（tap 完成时画面已变完）→ 6 个确定性操作全部"未通过"✗
  ② 词判据 —— ✗ 不具区分力（见 §9.3）
  ③ ★ 结构状态比较 —— ✓
     · 特征：底部导航带（y>0.93 的文字块数 ≥3；二期 page.py §底部导航 同款）
     · 判法：【点前 vs 点后】两个状态对比，不依赖"恰好捕获到变化" ✓

★★ 由此得出一条对 `script.exec` 的设计含义：
   `expect{hashDiff}` 适合判"画面变了"✗，**不适合判"我的操作生效了"** ✗ ——
   后者应是 `expect{stateChanged:<结构特征>}` 这类【状态断言】✓
```

### 9.3 ★★★★ 自证判据的纪律（被数据打脸四次后总结）

```
★ 四次被骗，全部来自同一类错误：★ 特征不具区分力，却在某些情形下【恰好给出想要的答案】✗
  ① 用「设置」做自证      → 通用词，很多页面都有 → 误通过 ✗
  ② 「带文字数 == OCR 数」→ ★ 恒等式，恒真 → 据此推出"没发生合并"的错误结论 ✗
  ③ 「0/0 → inf」         → 除零假象 → 公式"自然"给出「★★ 可作判据」这种好评 ✗
  ④ 词表里放单字「我」     → 任意文本都可能含 → ★ 在【抖音帮助页】上误报"已在首页"✗
     （后果：我在错误的位置上跑了 6 次采样，还一度归因为"缺判据"✗）

★★ 三条要求：
   ① 不通用 —— 该特征不能在很多页面都成立
   ② 不退化 —— 无信号必须【显式报未知】，绝不能让公式自然给出好评 ✗
   ③ 不用单字 —— 单字几乎必然误命中
★★★ 且优先用【结构特征】而非文字特征：
   · 文字特征对 OCR 抖动敏感（"消息"会被识别成"肉息"、或干脆认不出）
   · 结构特征（块数 + y 位置）与内容无关 → 对抖动免疫 ✓
   ⇒ ★ 正确组合：★ 结构先筛（这页是什么类型）→ 文字后验（是不是目标那页）✓
```

### 9.4 ★★★ 返回 / 起点重置的手段实测（穷举，逐个验证）

```
· 左上角返回（点 0.05, 0.063）  ：有时有效
· ★★ 角落的 X                    ：★ 实测关键手段（客服页 X@(0.075,0.063)）✓
· 左缘右滑（iOS 返回手势）        ：★ 在抖音【无效】✗（与二期"back 不通用"一致）
· ★ `home` + 重开 App             ：★ 【不保证】回首页 ✗
    —— 实测从客服页重开后【仍在客服页】（App 恢复上次页面）
    ⇒ ★ 这条推翻了"home+重开 = 重置"的直觉 ✗
· 盲点底部 tab 坐标               ：可作兜底

★★ 结论：★「起点重置」必须【穷举 + 结构自证】，不能依赖单一手段 ✗
   且这条【不是风格问题而是必要前提】—— 因为弹窗处置链第 3 级会把 App 打回不确定位置
```

### 9.5 ★★★ 弹窗处置链（四级兜底，每级都实测过）

```
实测对象 A：抖音红包活动弹窗「恭喜获得老友回归红包…立即领取」
  · ★ 它【没有"取消/关闭/×"】✗ —— 全屏特征词只找到「立即领取」
  · 它【没有全屏遮罩】✓ —— 底部 tab 在 y=0.966 仍可见
  · 但它【拦截了触摸】✗ —— 点底部 tab 坐标，界面不变
  · 点卡片外 (0.5,0.12) → ★ 无效 ✗（"界面变了"只是弹幕在动）
  · ★ home + 重开 App → ✓✓✓ 弹窗消失（App 重开后弹窗不保留，内存态）

实测对象 B：抖音麦克风权限弹窗「"抖音"想访问您的麦克风…」
  · ★ 它【有】「不允许」@(0.319,0.572) 与「好」@(0.680,0.573) 这对词 ✓
  · ⇒ ★ 第 1 级对它【有效】✓（点"不允许"，安全）

★★ 由此确立：★ 第 1 级的词表必须【按弹窗类型分】✗，不能是一张大表 ✗
   · 权限类：不允许 / 拒绝 / 取消 / 好 / 允许   ← 实测有；★ 优先点"不允许"（安全）
   · 提示类：取消 / 关闭 / 稍后 / 以后再说 / 不再提示 / 我知道了 / 跳过 / ×
   · 活动类：★ 常常一个都没有 → 直接跳到第 3 级

★ 四级链（成本从低到高，末级是诚实兜底）：
   1 常见关闭词（按类型） → 2 点卡片外 → 3 ★ home+重开（代价=丢页面位置）→ 4 记录为坑+上报人工

★★★ 结构性结论：★ 因为第 3 级会把 App 打回不确定位置，
   "从确定起点开始"就不是风格问题而是【必要前提】✗
```

### 9.6 ★★ 执行时采样：确定性版（不依赖 AI）

```
★★ 关键设计：★ 采样【不需要 AI】✓
   · 用【确定性操作】（已知会成功的，如点底部 tab）
   · 用【结构判据】验证"操作确实生效"（§9.2）
   · ★ 只有验证通过才记样本 → 样本自带可信标签 ✓
     （因为"成功"是【被验证过】的，不是 AI 自称的 ✓ —— 这正是那条纪律的应用）

★★★ 实测价值：★ 验证机制【正确地拦住了所有假样本】✓
   · v1：5 个操作因弹窗遮挡全部失败 → 0 样本（若没验证，会记下 5 条【假样本】：
     "点了 tab"实际点在弹窗上 → 直接污染训练数据 ✗）
   · 拦住它们的不是我的判断，而是【机制】✓

★ 当前进度：★ 已拿到第 1 条可信样本（tab-1，中部块数 1→10）
   样本格式：data/interaction-samples.jsonl
     {ts, name, screen, tap:{x,y,px,py}, verified, beforeMid, afterMid,
      region, regionSource, regionConfidence, size}
   ★ 样本量远不够训练（YOLO 起步需几百张），且只有正样本
   ⇒ ★ 正确用法：先积累，并用它【评估】OmniParser 与其它方案（而非立刻训练）
```

### 9.7 ★ 本轮的"不做"（YAGNI 自查）

```
· ✗ 不训 YOLO —— 样本不足（1 条）+ 只有正样本（会训出"到处都可点"的模型）
· ✗ 不设备端加 vision.rects —— 电脑侧 OpenCV 已够（且离线解析不受设备端限制）
· ✗ 不把页面资产入库 —— ★ 用户判定："当前还没必要入库"
   （理由：论证尚未收口；且资产还在快速迭代，过早入库会成为负担）
```

