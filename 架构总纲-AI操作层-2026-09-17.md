# 架构总纲 · AI 操作层（SuperPhone）

> 诞生：源自 2026-08-30 的架构设计会话「精准UI识别手机操作框架」（38 轮，`session-a120d693-…`）。该会话未被记忆库入库（`messages.jsonl` 为 0KB），2026-09-17 从 `dsh-session-21e6e53f-…/history/archive_002` 的引用快照中完整还原后落盘。
> 定位：**本文件是 AI 操作层的宪法**——以后任何相关改动先对照本文；与本文冲突 = 改动非法，要么不改，要么先修订本文。
> **修订纪律（2026-09-17 用户确立，长期约定）**：对本总纲（或 AI 操作层）的任何**架构/机制层面调整**（新增或删除一层、改契约、换机制、改时序、改判定依据），说明**必须先从实际使用角度**讲清三种情形下**直观可感受到的流程变化**，并**以此为出发点**说明该调整**对操作造成的影响**：**① 首次执行任务**（探索期／"学"）· **② 第二次执行任务**（技能回放期／"用"）· **③ 蜂群执行任务**（N 台复制／"复制"）。**只讲结构/契约/代码而不讲体感变化的调整说明视为不完整**。同一约定已写入 `AGENTS.md`「开发流程约定」第 6 条。
> 与《说明文档.md》的关系：说明文档 = 全项目架构事实（**怎么实现的**）；本文 = AI 操作层的目标/契约/分层/方法论（**为什么这样、什么不许变**）。事实更新去说明文档，原则修订来本文。
> 与《架构总纲-定位模拟子系统-2026-09-05.md》并列：那一份是"**设备像真人**"的宪法，本文是"**设备被 AI 学会操作**"的宪法。
> 状态（2026-09-17）：**设计与意图**。除"设备接口面"已落地（`dsh-superphone` 11 个工具）外，本文其余各层**均未实现**；已落地部分的事实以《说明文档.md》为准，不在本文重复。
>
> **★★ 与《设备操作Agent时序设计-2026-08-28.md》的关系（2026-09-18 确立，务必遵守）**：
> 那一份是 **AI 操作层的【主干设计】**（角色分层 / 时序 A→E / 资产结构 / 关键机制 12 条 / 结构契约 / 引擎适配现状）；
> **本文是主干下的【感知与执行层细化】**（四级验证 / 页面表 / 等待策略 / 工具面现状 / 真机坑清单）。
> **★ 冲突时以主干为准** —— 本文只补充细节，**不推翻主干的结论**。
> **已知被主干覆盖之处**：本文原有的"0→5 六阶段"划分，**已由主干的 A→E 时序取代**（查资产 → 回放/探索 → 降级链 → 沉淀 → 转正申请）。
>
> **★★ 角色分层的现行结论（本文原缺，2026-09-18 补入 —— 这是当前最大的一处认知缺口）**：
> | 角色 | 是谁 | 职责 |
> |---|---|---|
> | **群控 agent** | DSH 主会话 | 理解需求、枚举设备、**派生子代理**、合并汇报 |
> | **设备子代理（导演）** | 每设备一个 DSH 会话 | **不亲自逐步操作设备**；查资产 → 派活 → 监督 → 剥离视觉层下潜调试 → 沉淀 → 转正申请 |
> | **UI 视觉代理（演员）** | 被派发的执行单元 | **观察（截图/树）→ 模型决策 → 动作**，循环直到达成；**由 UI 专项模型驱动** |
> | **引擎（L0/L1/L2）** | DSH 之外的运行时 | **确定性**回放、技能命中、组合脚本执行、约束护栏（**无模型**）|
>
> **→ 纪律**：**"让 DSH 模型逐步决定点哪里"是【角色错位】** ✗ —— 那是"演员"的活。
> **导演只派活与沉淀；引擎负责确定性执行；演员用专项模型探索。**
>
> **★★ 一条尚未解决的硬前提（本文与主干都未预料到）**：主干设想"演员 = 观察截图 → 模型决策"，
> **但 2026-09-18 真机实测：当前模型 `deepseek-v4.1-flash` 【不支持图像输入】** ✗（`read_image` 直接报错）
> **→ 演员目前没有眼睛** ✗ —— 这是落地前必须先解决的第一块（详见本文"视觉教师模型"相关章节）。

---

## 第一章 · 最终目标

### 1.1 北极星

**一台 iPhone 学会的操作，全群立刻都会；重复任务不再调用大模型。**

三条同时成立才算达成：

| # | 目标 | 含义 |
|---|---|---|
| 1 | **自生长** | AI 探索一次成功的操作链 → 固化为技能 → 后续同类任务**直接回放**（零模型调用） |
| 2 | **全群共享** | 一台探索走通 = 全群拥有；一台踩过的坑 = 全群免疫（**错误的边际成本随规模递减**） |
| 3 | **先学会、再自动跑** | AI 操作手机的直接目的是**探索/学习**，不是长期用大模型实时遥控 |

> **⚠️ 北极星的边界（2026-09-17 修正，诚实版）**：本北极星隐含假设**蜂群同构**（同型号 + 同系统 + 同 App 版本）。**实际场景里各台设备的 App 版本可能不同、界面有差异** → 因此"全群"应精确表述为"**同构子群**"（同界面签名）：**每个子群各付一次探索，子群内只付一次**。**正确性不受影响**（`entry_state` 进门校验会挡住不匹配的设备），受影响的只是**效率**。完整论证与落地方式见 **§7.5**。

### 1.2 三个必须同时守住的量（乘法关系，一项崩则全崩）

```
单步延迟 = 设备观测 + 图像编码 + LLM调用次数 × 推理时长 + 动作执行 + 写死等待
任务长度 ≈ 上下文有界性 × 错误恢复能力 × 技能命中率
```

| 量 | 目标 | 手段 |
|---|---|---|
| **快** | 多数步骤**不碰大模型** | 路由卡 + 技能库 + 参数修正 |
| **长** | 任务是程序、记忆是快照、恢复靠重放 | AgentProg 三件套 |
| **准** | 点错了**能发现、能回家、能记账** | 目标表 + 四道防线 |

### 1.3 验收主体

**真机实测**（App 真实行为）+ 可度量指标：聚合步/秒、单会话步延迟、**技能命中率**、**模型调用量**。
文档自洽不算达成；"能跑"与"能跑成"必须分开陈述。

---

## 第二章 · 两条不可违背的设计公理

### 公理一：**GUI 是最后兜底，不是默认路径**

确定性四级路由（`routing.yaml`，纯数据文件即生效）：

| 类别 | 含义 |
|---|---|
| `COMPLETE` | 一条确定性命令完成 |
| `BOOTSTRAP` | 命令开场 + GUI 收尾 |
| `INTENT_ONLY` | 仅拉起界面 ≠ 完成（**须诚实标注做了什么/没做什么**）|
| `run_seed_gui_subtask` | 标注为 **SLOWEST** |

风险分级 `SAFE_COMPLETE / CONFIRM_FIRST / NEVER_AUTO`；
无匹配时固定优先级：**`shell_exec → load_skill → python_exec → GUI`**。

### 公理二：**Agent 面对目标表，不面对像素**

动作从"点 `(x=347, y=512)`"变成"**点目标 `#17` / 点'登录'按钮**"。

---

## 第三章 · 分层全景

```
┌─ 感知层 ─ AI + 本地UI模型 + 手机OCR 融合 → 一张「可点击目标表」
│
├─ 决策层 ─ 双模型委托：本地小模型主力 + 大模型兜底（fast/slow 分级）
│
├─ 执行层 ─ 路由卡命中 / 技能命中 → 零模型回放；未命中 → 走决策层探索
│
├─ 生长层 ─ 探索成功/失败的模式 → 技能库（参数化 + 校验 + 环境指纹）→ 蜂群共享
│
└─ 观测层 ─ 每步 NDJSON trace → HTML 瀑布复盘 + 蜂群 device×step 矩阵
```

**四道防线**贯穿执行层（点之前 / 点之后 / 迷路之后 / 错过之后），是"准"的载体。

---

## 第四章 · 感知层：可点击目标表中间件

### 4.1 结构

```
帧流 ─┬─→ ① 帧管理器：稳定哈希 / 缩放系数(@2x/@3x) / 旋转状态 / 帧时间戳
      │
      ├─→ ② 多源元素提取（并行，互为补充）
      │     A. 无障碍树通道：WDA/XCUITest 快照 → label / identifier / frame
      │     B. OCR 通道：设备端 Apple Vision / 主机侧 PaddleOCR   ← 【手机 OCR】
      │     C. 视觉检测通道：OmniParser / grounding 模型          ← 【本地 UI 模型】
      │
      └─→ ③ 融合索引层 = 目标表
            {id, 来源, label, bbox(逻辑点), 置信度, 帧哈希, 过期时间}
                 ↓
     ④ 解析执行层：意图("点'登录'" / "#17" / 裸坐标) → 精确点位
        · 裸坐标就近吸附 snap-to-target：半径内命中目标则吸附其中心
        · 目标表随帧哈希过期重建
```

### 4.2 分层管线（按成本递进，贵的按需触发）

| 层 | 通道 | 耗时（**实测/依据**）| 定位 | **我方判定（2026-09-17 复核）** |
|---|---|---|---|---|
| ① | WDA 树快照 | 50–150ms | 有元素信息时的最优解，**零视觉 token** | ❌ **不可得**（见下方现实约束）|
| ② | **设备端 Vision OCR** | ⚠️ **0.5~2s** —— 原设计写 **10–30ms**，**与代码注释差两个数量级**（依据 `TRVisionEngine.h`，见 §4.6.3）| 回传文字 + bbox，**零带宽** | ⚠️ **可用但慢** → 与 ③ 的取舍**需真机实测** |
| ③ | 主机侧 PaddleOCR | ~100ms **＋ 传图 ~56ms**（board 档 40KB 实测）| 中文补强，**兼做点击校验锚点** | ⚠️ **可能反而更快**（~160ms < 0.5~2s），且**不占设备算力** → 需实测 |
| ④ | 视觉检测通道（OmniParser 等）| 亚秒级（GPU）| **可交互性判断** | 🔄 **被「图标模板库 + `vision.find_image`」大幅替代**（<1s、**零模型、无 GPU**）→ 仅模板缺失时兜底 |
| ⑤ | grounding 小模型（EvoCUA / GUI-Owl）| 1–3s | 低置信兜底 | ❌ **伪需求** → 由**编排模型偶发判断 + `failure_patterns` 沉淀**覆盖 |

**策略（修订）**：**首选"文字锚点"**（`vision.find_text`，只回几百字节）→ **图标目标走"图标模板库 + `find_image`"**（<1s、零模型）→ 视觉检测通道**仅模板缺失时兜底** → **不引入 grounding 常驻模型**。

> **⚠️ 本项目的现实约束（2026-09-17 复核，勿照上表直接实现）**
> - **① WDA 树快照对我们不可得**：需 XCUITest/WDA 的签名部署与无障碍快照权限，而我们是 **TrollStore 环境（iPhone 6s / iOS 15，不越狱、不注入 dylib）**；2026-08-30 已明确裁决**排除无障碍树方案**（理由：设备端 App 已具备 Vision OCR 与图像识别）。
> - **② 的耗时被原设计低估了两个数量级**（10–30ms → 实测 0.5~2s），**因此"设备端 OCR 常驻首选"这个结论不成立**，必须与 ③ 实测对比后再定；**在结论出来前，任何实现都不得假设"OCR 很快"**。
> - **④⑤ 与"双模型委托"中的 GUI 工作模型均判定为冗余**：它们的职责（定位、可交互性判断）已被**设备端确定性原语**（`find_text` / `find_image`）与**编排模型的偶发判断**覆盖 → 见第六章修订与 §13.2。
> - 上表只在"有 WDA 的环境"（真越狱 + XCUITest，或 Android 的 uiautomator）才成立；**本项目的感知成本基线应从 ② 起算，且以实测数字为准**。

### 4.3 三个关键设计点

1. **坐标契约内置在中间件里**：bbox 统一存**逻辑点**，对外只暴露归一化（0–1000），物理像素换算只发生在这一层 → iOS 物理像素/逻辑点坑永久解决
2. **每个目标带"来源 + 置信度"**：无障碍树 > OCR > 纯视觉；决策与事后追责都用得上
3. **目标表按帧哈希失效**：屏幕没变就不重建（省算力），变了立刻重建（防陈数据误导）

### 4.4 为什么必须有 C 通道（视觉/本地 UI 模型）

**文档型 OCR 只告诉你"哪里有字"，不告诉你"哪里能点"。**
手机屏幕上**一半以上的可点击目标没有文字**（图标按钮、滑块、开关、卡片式列表项、悬浮球），纯 OCR 对这些全盲，且它也无法判断"这段字是不是按钮"。
→ OCR 在中间件里的定位：**目标表的文字来源 + 点击后的校验锚点**；"可交互性判断"必须由专门的视觉通道承担。

### 4.5 参考项目依仗什么（实测）

| 项目 | 路线 | 实现 |
|---|---|---|
| mobile-harness / mobilerun | **A 结构化为主** | iOS 全走 WDA/XCUITest 无障碍快照；`tap_text`/`tap_node` 按元素点，不走视觉 |
| mobile-use | **C 融合** | 一次连接同时取截图 + 控件树，两路一起给模型 |
| Mobile-Agent-v3.5 | **B 纯视觉** | 只有截图 → 模型输出归一化坐标 |
| AppAgent（腾讯） | **B 探索式建档** | 自主探索/看人类演示，给元素写"使用文档"，后续复用（**早期技能库思想**）|
| PhoneHarness | **绕开屏幕** | 确定性路径直接干活，屏幕只是最后兜底 |

**规律**：工程化越成熟越依赖结构化通道（A），纯视觉（B）是学术基线与最后兜底 —— 因为 A 免费给出精确 bbox 与语义 label，**零视觉 token、零定位误差**。

### 4.6 语义状态签名（页面指纹）—— 感知层的稳定锚

> **本节是两处致命缺陷的正解，必须与目标表同时建立**，否则技能回放与错误恢复都无法成立：
> - **缺陷甲（像素哈希在动态界面上必然失效）**：设计原用"屏幕哈希匹配"识别"回到已知良好状态"，但真实 App 首页是**视频流**。**2026-09-17 真机实测**：抖音视频播放中连续 6 次采样得到 **6 个完全不同的 jpeg md5**；仅光标闪烁时也有 2 个值交替 → **像素哈希在动态界面上永远不匹配，"回家路标"直接失效**。
> - **缺陷乙（技能若存局部编号/坐标则不可移植）**：目标表的 `id` 是**每帧重建的局部编号**（`#17`），若技能录下"点 #17"，换设备/换时刻/换 App 版本即无意义 → 技能必须存**语义锚点**，回放时在当前目标表里**按语义重新定位**。

**指纹只做两层，够用**（2026-09-17 Ponytail 审查后精简：原四层中的 L3/L4 与 §4.6.1 已删除，理由见下）：

| 层 | 内容 | 抗扰性 | 成本 | 备注 |
|---|---|---|---|---|
| **L1 上下文** | 前台包名 | 最强 | 已知 | 与 L2 组合即定位到"哪个 App 的哪个界面" |
| **L2 文字骨架** | OCR 文字集合，**剔除易变项**（时间/数字/播放量/点赞数），排序后哈希 | **强** | 几百字节 | **唯一主力**，真机佐证见下 |

**真机佐证（2026-09-17）**：抖音首页 OCR = `关注 / 商城 / 推荐 / 直播 …`；抖音搜索页 OCR = `搜索 / 语音搜索` —— **两页文字骨架截然不同，且不受视频内容变化影响**（视频在变，但 tab 文字不变）→ **L2 单独就足以区分页面状态** ✓

> **★ 审查删除记录（Ponytail，2026-09-17）—— 以下三项均为冗余或投机，已删**：
> - **原 L3「结构骨架」**（元素数量 + 类型分布 + 粗网格位图）：需要"元素类型分布"，而我们只有**文字 + 图标模板**、**没有元素分类** → **实现不了**；且它本是为"无文字但有控件的页面"准备的，而那种页面的 `entry_state` 直接用 `find_image(某图标)` 表达即可。
> - **原 L4「视觉降采样」**：为"纯图像页"准备，这类页面罕见，且**"等它自己过"就是最优解**（`on_fail` ①，见 §6.6）。
> - **原 §4.6.1「多帧差分分离稳定区/动态区」整节**：它声称解决的两件事**都已被别的机制解决** —— ① "像素哈希在视频流上失效" 由 **L2 文字骨架**解决（本节的真机佐证就是证据）；② "技能回放点 `#17` 无意义" 由 **契约 C-A（只存语义锚点）**解决。**同一问题两个解 = 冗余。**
> - 结论：**指纹只保留 L1 + L2；预测式机制（多帧差分、区域占比判据）一律不做，改用"失败驱动"（见 §6.5.2）。**

#### 4.6.2 由此产生的两条硬契约

| # | 契约 | 违反后果 |
|---|---|---|
| **C-A** | **技能只存语义锚点**（文字 / label / 图标语义 + 相对位置约束），**禁止存目标表 id 或绝对坐标** | 换设备/换版本即失效，且静默点错 |
| **C-B** | **状态识别只用语义签名（L1–L2）**，**禁止用像素哈希**判"是否回到已知状态" | 动态页面上永远不匹配，回退链失效 |

**消歧补充**：语义锚点可能命中多个目标（2026-09-17 实测：抖音搜索页同时存在 `搜索`（输入框）、`搜索`（按钮）、`语音搜索` 三个含"搜索"的目标）→ **锚点必须带"类型 + 区域约束"**（如 `tap_text("搜索", type=button, region=top-right)`），不能只靠文字。

#### 4.6.3 设备端能力现状（实现出发点，2026-09-17 代码实证）

> **纪律**：本节是**代码事实**（读 `TRCapabilityRegistry.mm` / `TRVisionEngine.h` / `trollvncserver.mm` 得出），不是设想。任何 AI 操作层设计**必须以此为出发点**，不得假设设备端有它没有的能力。

**① 点击能力：18 项 `touch.*`，全部是「坐标级」，不认识"元素"**

| 能力 | 参数 | 性质 |
|---|---|---|
| **`touch.tap`** | `{x, y}` 0-1 归一化 | **唯一的"点击"，吃裸坐标** |
| `touch.doubleTap` / `twoFingerTap` / `threeFingerTap` / `longPress` | `{x, y}` | 坐标级 |
| `touch.swipe` | `{x1,y1,x2,y2,duration?}` | 坐标级 |
| `touch.curveSwipe` / `taps` / `pinch` | — | 坐标级 |
| `touch.down/up/downMulti/upMulti/downMultiAt/upMultiAt` | — | 触点多指事件 |
| `touch.event` / `touch.eventStream` / `touch.reset` | — | 事件流 |

**结论**：设备端**只认坐标**，没有一项吃"文字/label/id"。**"点哪个元素"必须由编排侧解析成坐标后下发。**

**② 感知与判定能力：`screen.*` + `vision.*`**

| 能力 | 输入 | 返回 | **成本（硬数字）** |
|---|---|---|---|
| `screen.hash` | — | pHash 16 字符 hex | 快（**复用取帧**，与 hash 同源）|
| `screen.snapshot` | `{quality?}` | board 档 JPEG + `seq` | **~56ms / ~40KB**（2026-09-17 实测）|
| `screen.wait` / `waitStable` | `{since}` / `{ms}` | 变化事件 / 稳定 | 快（**不在注册表**，走 5802 + 隧道 invoke）|
| **`vision.ocr`** | `{region?}` | `[{text,x,y,w,h,cx,cy,confidence}]` | ⚠️ **0.5~2s** |
| **`vision.find_text`** | `{text, exact?, region?}` | `{found, count, matches[…]}` | ⚠️ **0.5~2s** |
| **`vision.find_image`** | `{image(base64≤256KB), threshold?, region?, scale?}` | `{found, score, x, y, w, h}` | **<1s** |

**Vision 引擎设计基线**（`TRVisionEngine.h`，2026-08-28 定稿）：`VNRecognizeTextRequest` **`.accurate` 级**、语言 `["zh-Hans","en-US"]`、**`usesLanguageCorrection=NO`**（*系统纠错会改写 UI 文案导致匹配失败，必须关闭* —— 这条注释是重要经验，勿改回）；串行队列防并发争内存；结果上限 200 行；模板匹配为**纯系统框架 + vDSP**（灰度化 Rec.601 → 1/8 金字塔 → `vDSP_conv` 相关图 → **NCCC 归一化抗亮度** → 峰检测 → ±48px 精匹配 → 阈值 **0.7**），**不引入 OpenCV**；`scale` 支持跨分辨率（云端 0.5x 缩略图截的模板传 `0.5`）。
**坐标契约统一**：0-1 归一化、**左上原点**、`cx/cy` = 中心（VN 左下原点已转换）——与 `touch.*` 一致。

**③ 职责分工：代码注释里已经定好了（这是"以此为出发点"的权威依据）**

> `TRVisionEngine.h` L4：为上层脚本/导演提供「**传文字→回坐标**」的**机械匹配原语（语义判断不在手机端）**。
> `TRCapabilityRegistry.mm` L1044：机械匹配原语：传文字/锚点小图→回坐标，**语义判断在云端编排侧**（**`script.exec` 未来的 `find_and_click` 内部调它**）。
> `trollvncserver.mm` L5562：坐标全部 0-1 归一化左上原点，`cx/cy`=中心（**上层 `find_and_click` 的点击目标点**）。

**→ 结论（三条，直接决定实现路线）**：
1. **"锚点而非坐标"的落地路径是现成的**：编排侧说"点『搜索』" → `vision.find_text("搜索")` 回 `{cx,cy}` → `touch.tap(cx,cy)`。**设备端不需要变聪明**，它只需准确回答"某段文字/某个图标在哪"。
2. **`script.exec`（含 `find_and_click`）是注释里早已预留的座位**，但**至今未实现**——它就是"技能脚本"的执行器（锚点解析 + 动作 + 校验 + 航线），见第六章。
3. **`vision.*` 的耗时是感知层的真实瓶颈**（不是网络往返）——由此产生第四章 4.8 的"页面级 OCR 经济性"纪律。

### 4.7 相邻方案调研结论：`picturereader`（2026-09-17，结论=暂不采用）

| 项 | 内容 |
|---|---|
| 项目 | **`picturereader`**（作者 jing-hy）· npm `picturereader` v3.3.3 · 35★ / 3922 下载 · 分类 vision |
| 它做什么 | 图片**降分辨率 + 降色深 + 结构/色彩指纹 → 渲染成文本网格**喂回对话，让**纯文本模型**像多模态一样自主缩放/取样/OCR 读图；纯本地、零外部模型依赖，附读图方法论 skill 与可选 PaddleOCR |
| 同作者配套 | `computer-user`（仅 Windows，截图 + Win32 SendInput 驱动），两者合起来做"截图-分析-操作-验证"闭环 |
| **为何"方向对、工具不对"** | ① **优化方向相反**：指纹要"丢易变、保结构不变量"，它做的是"把全部内容等比例采样保留"→ 视频一播网格照样全变（本质仍是像素级）；② **体量太大**：一屏文本网格是**数千 token 级**，而 `vision.ocr(find_text)` 只回**几百字节**；③ **它不判断可交互性**：网格上的深色区域是"图标"还是"阴影"它不区分 —— 替代不了 ④ 通道的"可交互性判断" |
| **可借鉴的一条** | ★ **"把一屏压成一个小编码"的思路** —— 但要**换成语义原料**，即 §4.6 的 **L2 文字骨架**。（原此处还提"网格化 ＋ 多帧差分分离稳定/动态区"，**已随 §4.6 精简一并删除** —— 多帧差分要解决的两件事已被 L2 文字骨架与契约 C-A 解决，属冗余。）|
| 何时会重新变得有用 | 当遇到**纯图标页**（OCR 一无所获）且视觉检测通道不可用时，它可作为"便宜的屏幕结构预览"兜底；但那种场景的正解仍是 ④（可交互性判断） |
| 决策 | **暂不安装**（YAGNI）—— 我们的路线是**设备端做完视觉、把结构化结果给模型**，而不是让模型自己看图；这条路上它没有位置 |

### 4.8 验证经济性与执行反馈（决定回放能不能"秒级"）

#### 4.8.1 四级验证（从便宜到贵，逐级升级，不得越级）

| 级 | 原语 | 能证明什么 | 成本 | 何时用 |
|---|---|---|---|---|
| **①** | `screen.hash` | 画面**变没变** —— 纹丝不动 = **点在了死区** | 极低（复用取帧）| **每步必做**（快速证伪）|
| **②** | `screen.waitStable` | 界面**收敛了没**（动画结束）| 低（**不依赖 OCR/图像**）| 每步（等落定再做重判定）|
| **③** | `vision.find_text(预期文字)` | **到了预期的地方吗**（真正的"确认"）| ⚠️ **0.5~2s** | **仅新页面 / ①异常时** |
| **④** | `vision.find_image(预期图标)` | 无文字目标的确认（图标锚点）| **<1s** | ③ 不适用时 |

> **★ 核心手法：③ 的结果顺便就是下一步的锚点坐标** —— **一次 OCR 同时完成"验证 + 定位"**。这是把回放压到秒级的唯一办法。

> **⚠️ 纪律（2026-09-17 新增）：挂起式等待一律用 `screen.wait(since)` 事件驱动，禁止在技能卡里写死时长。**
> 理由：**版本间时序不同**（动画快慢、过渡长短、多一次加载）→ 写死 `waitStable(800ms)` 必有一边不适应；
> 而 `screen.wait(since)` **变化一发生就返回** —— **慢的版本自然多等，快的版本立刻返回**，天然自适应。
> **配套红线**：`since` 必须用**上一步返回的新 `seq`**（§6.5.1 —— 不前移会导致下一步 wait 假阳性）。

#### 4.8.2 页面级 OCR 经济性（硬纪律，防止写出"每步都 OCR"的实现）

**硬数字**：`.accurate` 全屏 OCR = **0.5~2 秒**（`TRVisionEngine.h` 设计基线）。

| 策略 | 15 步技能的总感知成本 | 结论 |
|---|---|---|
| 每步都 OCR 验证 + 定位 | **7.5~60 秒** | ❌ **"秒级回放"直接破产** |
| **进页面一次 OCR，步级只做廉价判定** | **~2~3 秒** | ✅ **正确形态** |

**正确形态**：
```
进入新页面 → 一次全屏 OCR → 建目标表（该页所有文字 + 坐标）
   每步： aim 从目标表里查（零 OCR）→ touch.tap
         校验用廉价判据（screen.hash / waitStable）
         仅当判定"跳到新页面了" → 才重做一次全屏 OCR
→ 目标：每页 OCR 次数 = 1
```
**表的重建由失败驱动**（§6.5.2 阶段 4b）：**步骤通过就沿用旧表**，只有"查不到锚点 / `expect` 失败"时才重建 —— 与"目标表按帧哈希失效重建"是同一个思想（**没坏就不重建**）。

> **注（Ponytail 审查）**：原此处提议设备端新增 `ui.expect`（批量谓词接口）以省调用，**已删** —— 因为 **`vision.ocr` 一次就返回【全部文字行 ＋ 坐标】**，所有谓词（`entry_state` / `expect`）在**编排侧本地求值即可，零额外设备端调用**（§13.2）。

#### 4.8.3 tap 注入的反馈现状（代码事实，2026-09-17 实证）

**当前 `touch.tap` 的反馈是空的**：

```objc
// TRCapabilityRegistry.mm L354-360
/** 异步执行触摸注入（命令式手势用；ack 提前返回，注入在独立线程按参数时长执行） */
- (NSDictionary *)_touchAsync:(void (^)(STHIDEventGenerator *gen))inject {
    dispatch_async(..., ^{ inject([STHIDEventGenerator sharedGenerator]); });
    return @{@"ok":@YES};        // ← 仅表示"已投递到后台队列"
}
```

| 项 | 现状 |
|---|---|
| 返回内容 | **`{ok:YES}`** = 已排队；**不含执行结果、不含落点、不含是否被系统处理** |
| 执行时机 | **异步**（独立线程 + 按参数时长执行）；且 `HumanizeTouch` 已开启（行为整形），**ack 到"系统真的处理"之间有不确定延迟** |
| 落点可视反馈 | **无**（前端只有 `.tile.ai-on` 状态样式，不含"点了哪里"）|
| **唯一真实反馈** | **屏幕变化**（`screen.hash` / `screen.wait`）|

> **⚠️ 实现必须知道的坑**：**点击后不能立刻判 `screen.hash`** —— 异步注入 + 行为整形意味着此刻可能读到的是**点击前的那一帧**，会误判"点空了"。**顺序必须是：注入 → `waitStable`（或等一个最短延迟）→ 再判 hash**。

#### 4.8.4 执行反馈：真实意图与实现归属（2026-09-17 定稿）

**① 先分清两件事（此前常被混为一谈，务必分清）**

| | **执行反馈** | **点击落点可视化** |
|---|---|---|
| **消费者** | **执行者**（AI 探索循环 / 0 AI 回放器）| **人**（监督、调试）|
| **形态** | **校验结果**（结构化数据）| 屏幕上的落点标记（图像）|
| **属性** | **运行期必需** | **调试期可选** |
| **在 0 AI 回路里吗** | ✅ **在**（回放器靠它决定继续/降级）| ❌ **不在**（机器不消费它）|

**② 执行反馈的真实意图（一句话）**

> **让"执行者"知道"我刚才那步成不成功"，以便决定下一步。**
> **它是机器的输入，不是给人看的图。**

**③ 它应对什么**（只有两条，且都是 `on_fail` 的触发条件）

1. **动作未生效** —— 点空 / 输入没进去
2. **界面跳到非预期位置** —— 锚点解析对了，但跳转结果不对

**④ 结论一：不需要新的反馈机制 —— 已有的四级验证就是它（§4.8.1）**

缺的只是"**把它接进 trace 与 `on_fail` 触发**"：
```
每次输入落一条：{ aim 意图, 解析坐标, 注入坐标, 注入时刻, 校验结果, 校验依据 }
校验不通过 → 触发该步的 on_fail 航线（§6.2）
```

**⑤ 结论二：判定在设备端，决策在编排侧**

| 层 | 放哪 | 为什么 |
|---|---|---|
| **判定**（成没成）| **设备端** | 只有设备端能"看"屏幕；且 0 AI 时不能依赖云端 |
| **决策**（继续/降级）| **编排侧**（写死规则 / `script.exec`）| 规则是确定的，**不需要模型** |

**⑥ 结论三：★ 0 模型时它照常运转 ——「0 模型 ≠ 0 感知」（关键澄清）**

| 校验原语 | 它是什么 | 是模型吗 |
|---|---|---|
| `screen.hash` | pHash 数值计算 | ❌ |
| `screen.waitStable` | 时间戳比较 | ❌ |
| `vision.ocr` / `find_text` | **iOS 系统框架 `VNRecognizeTextRequest`** | ❌ |
| `vision.find_image` | **系统框架 + vDSP 相关图**（无 OpenCV、无模型）| ❌ |

→ 校验链在 **0 AI 时照常工作**；**唯一要付的代价是耗时**（OCR 0.5~2s/次），**而不是"能不能跑"**。
**这正是 §4.8.2「页面级 OCR 经济性」成为硬纪律的原因：能跑，但贵，所以要省着用。**

**⑦ 明确不做（YAGNI）**

| 不做 | 理由 |
|---|---|
| **注入完成回执**（新增 `touch.tapSync` / `wait:true`）| 它回答的是"**注入线程返回了吗**"，而不是"**界面响应了吗**" —— 已注入但界面没反应时它**照样返回成功**，**对"点空"零信息量**；且需 CI，还会动摇"ack 提前返回防阻塞命令通道"这个有理由的设计 |
| **落点可视化进运行期** | **机器不消费它**；而把它接进反馈回路 = **用可视化复刻一份已经存在的机器判定** = 冗余。**降级为调试期可选项**（见 §13.1）|
| 为反馈新增任何通道 / 原语 | 四级验证已足够；新增即重复实现 |

**⑧ 替代"检测点空"的更优方向：预防点空**

见第八章防线 1（**就近吸附** + **目标必须属"可交互"类型**）—— **减少点空的概率，比检测点空更根本**，且同样零设备端改动。

---

## 第五章 · 决策层：**单模型 + 设备端确定性感知**（原"双模型委托"已修订）

**原设计的双模型委托**：

```
编排模型 (--model)              ← 规划、调工具、管状态   → 便宜的大模型 API     【AI】
  └── GUI 工作模型 (--gui-model) ← 只负责看图定位点击     → 本地小模型           【本地 UI 模型】
```

**修订结论（2026-09-17）**：**"GUI 工作模型"这一层是伪需求，应删除** ✗

理由（按"三问"）：
1. **它的职责是"看图→出坐标"** —— 而这件事**设备端已经用确定性原语做完了**：`vision.find_text`（文字→坐标）、`vision.find_image`（图标→坐标，纯系统框架 + vDSP，**零模型**、<1s）。
2. **它要解决的核心难题（"一半以上可点击目标没有文字"）**，正解不是"让模型理解图标"，而是**「图标模板库 + `find_image`」** —— 一台截取、全群共享、**零模型零 GPU**（见 §13.2）。
3. 唯一模板匹配答不了的是**"这个图标能不能点"（可交互性判断）**，而它可以由**编排模型偶发判断**（探索期 AI 本就在场）+ **`failure_patterns` 沉淀**（点了没反应=不可点，失败本身是信息）覆盖 —— **不需要常驻模型**。

**因此本项目的决策层简化为**：

```
编排模型（唯一模型）  ← 规划、选目标、管状态、异常修复   → 可便宜可强，按需
     ↑ 只在"学"（首次探索）与"修"（降级链末端）出场
设备端确定性感知      ← find_text / find_image / screen.hash / waitStable → 零模型
```

| 保留 / 删除 | 项 | 说明 |
|---|---|---|
| ✅ 保留 | 编排模型 | 唯一的模型消费方 |
| ❌ 删除 | **GUI 工作模型（本地小模型）** | 职责被设备端原语吃掉 |
| ❌ 不引入 | OmniParser 常驻 / grounding 小模型 | 见 §4.2 判定 |
| ⚠️ 待实测 | "OCR 放设备端还是主机侧" | 见 §4.2（设备端 0.5~2s vs 主机侧 ~160ms）|

> **仍然成立的洞察**：调研的五个参考仓库**没有一个真正实现 fast/slow 分级路由**。但在我们的架构里，**"fast" 不是"小模型"，而是"技能命中 + 设备端确定性原语"**（零模型），"slow" 才是编排模型介入 —— 分级**依然存在**，只是分界线画在**"有没有模型"**上，而不是"模型大不大"上。

---

## 第六章 · 执行层：路由卡与技能回放

| 入口 | 命中条件 | 成本 |
|---|---|---|
| **路由卡**（`routing.yaml`） | 精确可执行路径匹配 | **零模型** |
| **技能命中** | 意图命中技能库 | **零模型，毫秒级回放动作链** |
| 探索 | 上述都未命中 | 走决策层（本地小模型主力 → 低置信升级） |

**纪律**：**每步 LLM 调用次数应为 1**，超过就要问为什么（参考项目的多角色架构导致每轮最多 4 次调用，属反面教材）。

### 6.1 `script.exec`：技能脚本的执行器（注释已预留、至今未实现）

**代码注释早已给它留好座位**（见 §4.6.3③）：`TRCapabilityRegistry.mm` L1044 与 `trollvncserver.mm` L5562 都写明 *"上层 `find_and_click` 的点击目标点"*、*"语义判断在云端编排侧（`script.exec` 未来的 `find_and_click` 内部调它）"*。

**它的形态（本总纲确立）**：

```
技能步骤 = {
    aim:      语义锚点（设备端本地解析成坐标）
    act:      tap / swipe / type
    expect:   设备端可机械求值的谓词
    on_fail:  出错时的航线
}
```

**做法（锚点）的候选项 —— 设备端依次尝试，全部确定性、零 AI**（下表是**初始顺序**，不是固定优先级）：

```
① find_text(文字, 类型, 区域)      ← 有文字的目标，最稳、抗改版
② find_image(图标模板)             ← 无文字但有图标；抗改版（图形通常不变），需模板库
③ 相对坐标(挂在稳定参照物上)        ← ①②都失败时
④ 绝对坐标                        ← 仅"同型号 + 同版本锁定"的蜂群可作最后兜底
```

> **⚠️ 优先级是"跑出来的"，不是"拍出来的"（2026-09-17 采纳外部蓝本修正）**：上表只是**冷启动的初始顺序**；
> 运行后每个意图做法的实际优先级由**做法表按历史胜率自动排序**（试用期排后 → 最近成功优先 → 最近失败降权 → 同档 `wins` 降序 → 同 `wins` `avgMs` 升序），见 **§7.5.3 / §17.2**。
> `on_fail` 的"次优方案"就是**取该意图的下一权重做法**，而不是机械地"下移一层"。

> **关于第 ④ 层**：本项目的蜂群是**同型号（iPhone 6s）+ 同系统（iOS 15）+ 同 App 版本**，因此绝对坐标**是可用的兜底**（这是很多项目没有的条件）；但它**只能是兜底**——一旦 App 更新即全废，故不得作为技能的主力做法（违反契约 C-A）。

### 6.2 技能卡三要素（"0 AI 回放"的必要条件，缺一不可）

| 要素 | 位置 | 作用 | 缺了会怎样 |
|---|---|---|---|
| **`entry_state`** | 技能卡级 | **进门确认**：本轮页面是否就是技能起点（设备端谓词求值，如 `all_of(find_text("关注"), find_text("推荐"), not_find_text("登录"))`）| **失败放大**：一台弹了广告，全群跟着瞎点 |
| **`expect`** | 每步 | **步骤确认**：这一步做完，设备端可机械求值的谓词是否成立 | 点了没反应却不自知，错误累积 |
| **`on_fail`** | 每步 | **自我纠正航线**（见下）| 出错后无路可走，只能停手上报 |

> **⚠️ 动作成功 ≠ 任务完成（2026-09-17 采纳外部蓝本，见 §17.1 #5 —— 本文前一版漏了这条）**
> 有一类动作**返回成功但根本没完成任务**，典型：
> - **`app.open` 返回 `launched`** —— 只代表"进程被拉起"，**不代表 App 到了可用状态**（可能还在启动页/登录页/弹窗上）
> - **`type.paste` 返回成功** —— 只代表"粘贴动作发出去了"，**不代表文字进了输入框**（可能焦点不在输入框）
> - **`touch.tap` 返回 `{ok:YES}`** —— 只代表"注入已投递"（§4.8.3）
>
> **→ 因此 `expect` 必须断言【任务意义上的结果】，绝不能断言"动作返回 ok"。**
> 外部蓝本把这个概念叫 **`INTENT_ONLY`（"只拉起界面 ≠ 完成"）** —— 它专门标注这类动作，防止执行循环把"拉起"当"完成"。**这是本文前一版删掉 `routing.yaml` 时一并丢掉的一条语义，现补回**。

**`on_fail` 航线的固定五段（前四段全确定性；教师只在第五段）**：

```
① 等      waitStable（较长窗口）/ wait_change
              —— 很多"多出来的组件"只是加载滞后或会自动消失
              ★ 放最前：最便宜，且大量"版本时序差异"其实只是"慢一点"
② 跳过    点【关闭入口锚点集】（文字：关闭/跳过/取消/我知道了… ＋ 图标：× / < 模板，见 §6.4）
③ 退      back() / 边缘右滑 —— 多了整整一页需要退回时
④ 重试    换锚点层重试原动作：find_text → find_image(+region) → 区域相对坐标
              ★ 降到最后：它只治"点不中"，治不了"多了一页"
⑤ 教师    【唯一出现模型的地方】按技能卡风险等级三选一：
      · 常规动作（浏览/搜索/翻页）            → 唤醒编排模型（＝教师）做"局部重规划"：
                                                只改这一步及其后，不整任务重来
      · 高风险 / 不可逆（发送/发布/支付/关注） → 直接停手 + 上报，禁止自动重试
      · NEVER_AUTO 类（技能卡显式声明）        → 只上报给人，任何自动重试都不允许
```

> **顺序是设计的一部分**：**"等"在最前**（最便宜，且大量时序差异只是慢）、**"重试"在最后**（它只治点不中）、**教师是第五段而非第一段**（否则高频的弹窗会把模型调用打爆，而弹窗本来用"等 / 跳过"就能过 —— 见 §6.6）。
> **前四段没有一步需要判断力** —— 全是"求值 + 分支"，所以**零 AI**。**只有第五段才出现模型**，且它每次出现都意味着"**这里确实没人知道怎么办**" → 它因此同时是**新知识的唯一来源**（见 §7.6）。

### 6.3 分工表（谁在什么时候出场）

| 阶段 | 谁在做 | 用 AI 吗 |
|---|---|---|
| 意图指纹 → 技能命中 | **网关**（查表）| ❌ **零** |
| 锚点解析 | **设备端**（`find_text` / `find_image`(+region) → bbox → 坐标）| ❌ **零** |
| 每步确认 | **设备端**（谓词求值，见 §4.8.1 四级验证）| ❌ **零** |
| 路由决策 | **网关 / 技能卡**（写死规则：通过 → 下一步；不通过 → 走 `on_fail`）| ❌ **零** |
| **未知点 / 不相符** | 先走 `on_fail` ①②③④（**零 AI**）→ **用尽才 ⑤ 教师** | ✅ **仅第五段** |

**"0 AI 介入"的准确含义（2026-09-17 修订）**：**巡检 / 日常执行不碰 AI；AI 只在"确定性手段用尽"时出场。**

> ⚠️ **本表已按「教师—执行循环」修订（§7.6）**：**原设计的"首次探索阶段"与"Phase 1 探索 / Phase 2 回放"的阶段划分已取消** —— 探索融合进执行时序，全新任务自然表现为"每步都调教师"。逐项修订见**第十六章修订总账**。

### 6.4 关闭入口锚点集（文字 ＋ 图标，互补）

**问题**：弹窗的关闭入口**很大比例是纯图形图标**（右上角 `×`、左上角 `<`），**OCR 读不到** → 必须有图标层。

```
关闭入口 = 两条腿，谁命中用谁：
  ① 文字层：find_text(「关闭 / 取消 / 跳过 / 我知道了 / 以后再说 / 不再提示 / 同意并继续」)
  ② 图标层：find_image(通用关闭图标模板集：× / ✕ / ✖ / <(返回) )  ＋ region 先验
```

**定位分层**：**直接复用 §6.1 锚点四层**（文字 → 图标＋`region` → 区域相对坐标 → 绝对坐标兜底），**本节不另立一套**。（原此处有一张与 §6.1 逐行重复的四层表，Ponytail 审查后删除。）

**模板来源（两条腿）**：

| 来源 | 覆盖 | 成本 |
|---|---|---|
| **预置通用模板集**（`×` / `✕` / `<` 等常见样式）| 绝大多数弹窗 —— **`×` 是 iOS 生态最标准化的图标之一** | **零**（一次性内置）|
| **探索期补充** | 少见样式 | 教师处理未知弹窗时，**以点击点为中心截一小块小图**（~60×80）存库 |

**★ 两个必须知道的工程坑**（否则模板匹配会莫名失效）：
1. **模板不能太小**：匹配管线是 **1/8 金字塔粗定位 + ±48px 精匹配**（`TRVisionEngine.h`）→ 一个 20×20 的 `×` 在金字塔后只剩 2~3px，**粗定位会失效**（*推断，需真机实测*）。**对策**：连**周边一圈弹窗自身的稳定背景**（白底/圆角）一起截 → 既够大又更独特。**注意不能包含下层页面内容**（会随页面变化而失配）。
2. **不要全屏找**：用 `region` 限定到角区（`find_image` 支持 `region`，零额外成本）→ 命中率更高、误匹配更少、成本更低。

### 6.5 单步流水线与「基线三分」（执行时序的枢纽）

#### 6.5.1 三种基线（混在一起就会出错）

| 基线 | 是什么 | 用什么表示 | 谁维护 |
|---|---|---|---|
| **① 变化基线** | "我从**这一刻**起等变化" | **`seq`（`gChangeSeq`）** | 编排侧，**必须每步前移** |
| **② 稳定基线** | "上次变化发生在何时" | `gLastChangeTime` | **设备端自己维护**（上层不管）|
| **③ 内容基线** | "这一屏长什么样" | **语义签名 ＋ 目标表** | 编排侧，**按需重建** |

> **⚠️ 红线：`seq` 基线必须每步前移 —— 这是正确性要求，不是优化。**
> 设备端 `screen.wait(since)` 的判定是 **`gChangeSeq > since`**。若下一步仍用旧 `since`，**上一次的变化会立刻满足它** → wait 瞬间返回 → **误判"这一步也生效了"** ✗✗
> **正解：每一步用上一步 `wait` 返回的新 `seq` 作为下一步的 `since`。**
> **而内容基线（③）恰恰相反：不能每步重建** —— 每步重建 = 每步一次全屏 OCR → 15 步回放从 ~2 秒退化成 7.5~60 秒（§4.8.2 硬纪律）。

#### 6.5.2 单步流水线（探索与回放**共用同一条**）

```
【阶段 0 · 进页面（一次）】
  取 seq_baseline ← snapshot 或上次 wait 的返回
  建目标表：vision.ocr 全屏一次（0.5~2s）→ [{text,x,y,w,h,cx,cy,confidence}]
  顺手算【语义签名】（L2 文字骨架：剔易变项 → 排序 → 哈希）← 纯字符串处理，零成本

【阶段 1 · 动作前】
  解析 aim → 坐标：优先从【目标表里查】（零成本）；表里没有才 find_text / find_image
  防误点：就近吸附 ＋ 目标须属可交互类型
  ★ 钉住变化基线：seq_at_action ← seq_baseline
  落 trace：{aim, 解析坐标, seq_at_action, 签名_before}

【阶段 2 · 注入】
  touch.tap(cx, cy)        ← 异步！ack 只表示"已投递"（§4.8.3）
  【不立刻判定】← 异步 ＋ HumanizeTouch 行为整形，此刻可能读到点击前的那一帧

【阶段 3 · 等"变化发生"】（事件驱动，吸收版本时序差异）
  screen.wait(since=seq_at_action, timeout=T)     ← ★ 不写死时长
     变化 → 拿到 new_seq → 阶段 4
     超时 → "没有发生可感知变化" → 失败分支

【阶段 4 · 变化后的确认（分级，先便宜后贵）】
  4a【便宜】用 snapshot 比对 JPEG 内容（40KB/56ms）或直接依赖 wait 已证明"有显著变化"
  4b【表的重建：失败驱动，不做预判】★
       步骤通过          → 直接沿用旧表（零判断、零成本）
       从旧表查不到锚点 / expect 失败 → 才重建表（付一次 OCR）＋ 重算签名
       ★ 已删原「变化区域占比」预判判据：失败本来就要处理（走 on_fail），
         顺手重建即可 —— 不需要一套"预测要不要重建"的机制（Ponytail：预测式机制不做）
  4c【目标态】这一步的 expect 成立吗？
       expect="页面变了"      → 4a 就够，零额外成本
       expect="某文字出现"    → 从（重建的）目标表里查，零额外成本；表没重建才 find_text（贵）
       expect="某图标出现"    → find_image（<1s）
  4d 判定：通过 → 阶段 5 ／ 不通过 → §6.2 五段

【阶段 5 · 为下一步铺路（"一环扣一环"就在这）】
  ★ 基线前移：seq_baseline ← new_seq
  内容基线：仅在 4b 判定"换页了"时重建
  把"本步的 after 签名"变成"下一步的 before 签名"
  落 trace（本步三段计时 ＋ 是否重建表 ＋ skill_hit）
→ 回到阶段 1
```

#### 6.5.3 ★ 探索与回放**只在唯一一处分叉**

```
同一阶段 0-5，唯一分叉点 = 阶段 1 的「谁决定下一步动作」
    探索期 → 教师决策（在目标表里选一个目标）
    回放期 → 技能卡给出（语义锚点已写死）
```

| | 探索期 | 回放期 |
|---|---|---|
| 阶段 0 建表 | ✅ 需要 | ✅ **也需要**（否则每步都要 `find_text` 定位，0.5~2s/步 ✗）|
| 阶段 4 验证 | 由教师判断"这步算不算成功" | 由技能卡 `expect` 机械求值 |
| **差别** | **只有"谁给下一步动作"这一处** | 同左 |

**→ 这就是"一个实现覆盖两个分支"** —— **不得为回放另写一套执行器**。

#### 6.5.4 成本账（一页 15 步技能）

| 项 | 次数 | 单次 | 小计 |
|---|---|---|---|
| 全屏 OCR（建表＋签名）| **1 次/页**（假定 2 页）| 0.5~2s | **1~4s** |
| `screen.wait` 等变化 | 15 次 | 几十 ms | ~0.5s |
| snapshot 内容比对（可选）| 按需 | 56ms | 小 |
| `find_image`（图标锚点，若有）| 按需 | <1s | 小 |
| touch 注入 | 15 次 | 异步 | ~0 |
| **合计** | | | **≈ 2~5 秒** ✓ |

**对比"每步都 OCR"**：15 × 0.5~2s = **7.5~60s** ✗

### 6.6 弹窗与突发组件（**明确不做边缘检测**）

**问题重定位**：要解决的不是"**怎么检测弹窗**"，而是"**不被打断 / 被打断后怎么收敛**"。

**为什么不需要边缘检测（三条）**：
1. **弹窗的"关闭入口"可能是纯图形（`×`）** —— 但正解是 **`find_image` 模板匹配**（§6.4），**不是边缘检测**：模板匹配直接给中心坐标，边缘检测只给"边界在哪"、给不了"这是什么"。
2. **我们不需要"识别出这是弹窗"这个中间概念** —— 只要「**签名 ≠ 预期 → 走 `on_fail` 收敛序列**」即可。引入"弹窗识别"是给自己加一层不必要的中转。
3. **边缘检测指向的正是刚删掉的视觉检测层**（§4.2 ④ 已被"图标模板库＋`find_image`"替代）→ **别走回头路**。

**正解是两层（全部复用已有原语）**：

| 层 | 内容 |
|---|---|
| **预防** | 探索期把常见弹窗沉淀为技能卡的 **`preflight` 前置步骤**（关青少年模式提示 / 跳过开屏广告 / 同意隐私协议 / 关掉通知引导）→ **一台处理、全群免疫** |
| **恢复** | §6.2 `on_fail` 五段（**等** → **跳过** → 退 → 重试 → 教师）|

**★ 安全细分（不能漏）**：

| 弹窗类型 | 处理 |
|---|---|
| 引导 / 广告 / 通知请求 / 协议（常规）| 自动跳过（②）|
| **高风险确认**（"是否删除"、"是否发送"、"是否支付"）| **停手上报，禁止自动跳过** ← 对应 `NEVER_AUTO` 与 §6.2 ⑤ 三选一 |

**唯一 CV 可能更强的场景（仍不做）**：**纯图形、无文字的弹窗** —— 极少（对话框必须让人读懂才能操作），且**"等它自己过"就是最优解**（`on_fail` ①，零成本）；真要处理时它的"跳过"仍有文字 → 又回到 OCR ✓ **YAGNI**。

---

## 第七章 · 生长层：自生长与蜂群共享

### 7.1 资产形态（2026-09-17 修订：**两层，不是"一张技能卡"**）

**原设计**：技能 ＝ Markdown 卡片（六字段），按前台包名加载一张。

**修订后的形态**（首次真机验收 ＋ 用户指正后确立）：

| 层 | 存什么 | 分桶 | 跨版本 |
|---|---|---|---|
| **任务表** | **意图序列**（跨页面、跨 App 的"做什么"）| **全群共用一份** | ✅ 稳定（意图是"搜索"，不是"点右上角"）|
| **页面表** | **页面签名 → 该页可做什么 ＋ 每件的做法** | **按 `env_id`** | ⚠️ 会变（改版就变这里）|
| **阻断页** | 页面表里 `role: "blocker"` 的条目（登录页 / 弹窗 / 引导页）| 按 `env_id` | ⚠️ 会变 |

**三条关键纪律**：
1. **任务表只写意图**（`find_and_click(搜索入口)`），**做法归页面表**（§7.5.2 · §17.1 #1）
2. **★ 阻断页绝不写进任务时序** —— 登录页/弹窗**可能出现在任何任务的任何时刻**，它是**全局拦截器**，由执行循环在阶段 2 第 ① 步优先处理
3. **`failure_patterns` 挂在所属页面下**，不挂在任务上（坑是"这一页的坑"）

**执行循环因此"免判断"**：每一步都是 `当前签名 → 查页面表 → 取做法 → 执行` —— **纯查表，不需要判断**（§6.5）。

> **六字段仍然有效**（`prompt_extension / allowed_tools / preconditions / verification / failure_patterns` ＋ `load_skill` 渐进加载）—— 它们是**给模型读的元数据**；上面两层是**给执行循环读的数据**。两者并存，用途不同（§17.1 #13/#14）。

> **★ 判例（2026-09-17 首次真机验收，rev1 → rev2）**：rev1 把"**关登录页**"写成任务时序的第 1 步 —— **错** ✗。
> `app_open` 返回 `launched` 后落在登录页，是**环境遭遇**而非任务步骤；它在"搜索"以外的**任何**抖音任务里同样会出现，**写进某条链就等于只修了一条链**。
> → 正确归属：页面表里一张 `role: "blocker"` 的卡。

### 7.2 自生长机制（2026-09-17 修订：由「重复检测」改为「**教师介入即沉淀**」）

**原设计**：重复操作链自动检测 → 固化为高级技能（参考 AppAgentX）—— **被动且滞后**（要等重复 ≥2 次才学）。

**修订后的机制**：**教师介入的那一刻就沉淀** ——

```
执行中撞上异常 → on_fail ①②③④（零 AI）用尽 → ⑤ 教师介入解决这一步
   → 教师给出的「动作 ＋ expect」立刻写进技能卡         ← 边做边学、就地沉淀
   → 控制权立刻交回执行循环，继续往下走
   → 同一位置再次遇到同样问题时，第二台设备用 ①②③④ 就能过（零 AI）✓
```

**为什么比"重复检测"更好**：
- **就地沉淀**：问题与解法在同一上下文里，**不丢现场**（原设计要从 trace 事后蒸馏 = 延迟学习）
- **每次沉淀都是真知识**：教师只在"前四段用尽"时出场 → 它处理的一定是"**没人知道怎么办**"的东西，而不是"弹窗怎么关"这类常识
- **首次成本从"任务长度 × 模型调用"降到"未知点数 × 一次介入"**（见 §7.6）

**★ 沉淀是两阶段（2026-09-17 参照外部蓝本修正；**推翻本文前一版"立即生效 vs 重复≥2 次"的二选一**）**：

| 阶段 | 触发 | 生效范围 |
|---|---|---|
| **① 立即成卡** | 教师介入解决完那一步 | **`status: draft` → 本机 / 本环境立即可用** ✓ 边做边学、不丢现场 |
| **② 转正广播** | **同一目标重复 ≥2 次** | 过**两道闸门**：（a）目标设备重放，全部 `expect` 通过；（b）**灰度 1 台**重放通过 → **才全群共享** |

**→ 两者不矛盾**：**立即成卡**满足"边做边学"，**闸门**满足"防单次运气污染全群"。

> **⚠️ 自我纠正**：本文前一版把它当成"二选一"，并据此批评原设计"重复 ≥2 次太被动"—— **那是错的**。
> 原设计的"重复 ≥2 次"其实是**第二阶段（广播）的门槛**，不是"第一次不学"。
> **依据**：外部蓝本 `dsh-device-skill` 阶段 D/E（`status: draft` → 重复 ≥2 次 → 闸门 → `promote_request`），见 §17.1。

**每次变更必须追加一行 `evolution` 账本**：`{rev, type: create|fix|broadcast|regress, source, gate, status}` —— **审计与回滚的唯一依据**（§17.2）。

### 7.3 蜂群共享（比堆卡更重要的加速器）

| 维度 | 效果 |
|---|---|
| **成功的技能** | 一台 iPhone 探索走通 → **全群直接回放** |
| **错误的教训** | 一台在某 App 某页面点错的教训写入共享库 → **其余 N-1 台直接免疫** |
| **规模效应** | 蜂群越大，技能库覆盖越快，**整体模型调用量趋近于零** |

### 7.4 四种沉淀去处（缺一不可）

```
技能卡            可复用的操作链
failure_patterns  已踩过的坑（进任务前就知道）
memory/ 目录      跨会话缓存设备/应用 quirk
preflight         常见弹窗/引导的前置处理（§6.6；一台处理、全群免疫）
trace             每步前后帧 + CoT + 【本步是否调用了教师】
```

> **trace 的用途已改变（2026-09-17 修订）**：原设计里 trace 是"**蒸馏技能的原料**"（事后学习管道）；修订后**沉淀由教师在介入时就地完成**，trace 因此从"学习管道"变成"**观测管道**"：
> - **审计「教师介入率」**（技能库成熟度的直接指标，见 §7.6）
> - **画「能力边界图」**（哪些页面、哪些步骤反复需要教师 → 该补哪里一目了然）

### 7.5 多设备版本异构下的技能共享（2026-09-17 用户提出，蜂群同构性假设的破裂）

**问题**：各台设备上的 App **可能不是同一版本**（自动更新时机不同、不同来源的 IPA、A/B 分组…）→ **界面有差异** → 在 A 台验证成功的技能，在 B 台可能**直接不适用**。

**这直接冲击北极星**：原口号"**一台学会、全群立刻都会**"隐含假设 **蜂群同构**（同型号 + 同系统 + 同 App 版本）。**该假设在我们场景里不成立** → 必须修正（见第 7.5.4）。

#### 7.5.1 先问：能不能让它不成为问题？（最省的一步）

**我们的设备是内网自用，App 版本是可控的** → **优先统一版本**（关闭自动更新 / 用同一个 IPA 装 / 集中管理）。
**能统一，就根本不需要"容忍版本差异"的机制** —— 这是所有方案里最便宜的一条，应排第一。

#### 7.5.2 若仍会漂移：**不要用"版本号"分桶，用"语义签名"分桶** ★

**关键事实（2026-09-17 代码实证）**：**目前环境指纹没有数据来源** ✗
- `app.list` 只返回 `{bundleId, name}`，**不含版本号**（`TRCapabilityRegistry.mm` L691）
- `sys.version` 返回的是 **TrollVNC 自己**的版本（`[NSBundle mainBundle]`），**不是 iOS 版本、也不是第三方 App 版本**
- → 要按版本号分桶，**必须先加设备端能力（走 CI）**

**但有更省的路**：**版本差异的最终表现 = 语义签名不同**，而语义签名是**运行时已有原语算出来的**（§4.6 的 L2 文字骨架）：

```
两个版本界面不同 → 文字骨架不同 → 自然落进两个不同的桶
→ 不需要知道"抖音是 25.3 还是 26.1"，只需要"这一页的文字骨架长什么样"
→ 零设备端新能力、零版本号依赖 ✓✓
```

**分桶键 = 技能卡自身的起态语义签名**（即 `entry_state` 的签名），**不是版本号**。

> **★ 但比"分桶"更好的是「意图与做法分离」（2026-09-17 参照外部蓝本修正，推翻本小节原方案）**：
> ```
> 原方案（本小节前一版）：一个界面签名 → 一张完整技能卡     ← 整条链重复存储 ✗
> 修正方案：              一条技能卡（只写意图）× 一张做法表（按 env 分桶）  ← 只重复差异部分 ✓
> ```
> | 层 | 存什么 | 分桶维度 | 举例 |
> |---|---|---|---|
> | **技能卡** | **只写意图步骤** | **全群共用一份** | `打开抖音 → 点搜索 → 输入{{关键词}} → 提交 → 验结果页` |
> | **做法表** | 同一意图的**多种做法 ＋ 权重** | **按 `env_id`** | `点搜索@env-A → find_text(搜索, region=右上)`；`@env-B → find_image(模板)`；`@env-C → 相对坐标` |
>
> **→ 版本差异的爆炸半径从"整张卡"缩到"那一步的做法"**，**整条链不重复、只重复差异部分**。
> **依据**：外部蓝本 `dsh-device-skill` 红线第 1 条 —— *"skill 步骤只写意图动词，设备做法归动作库"*（见 §17.1）。

#### 7.5.3 技能该按什么原则写，才能跨版本活下来

**做法层的抗版本能力天差地别** —— 这决定了"版本差异的爆炸半径"：

| 做法 | 抗版本差异 | 说明 |
|---|---|---|
| ① `find_text(文字)` | ✅ **最强** | App 改版一般**不改 tab 文字与按钮文案** |
| ② `find_image(图标)` | ❌ **弱** | 图标会被改版 → 模板按 `env_id` 分桶存放 |
| ③ 相对坐标 | ⚠️ 中 | 依赖参照物 |
| ④ 绝对坐标 | ❌ **完全不抗** | 仅同版本锁定可用 |

**→ 由此产生一条纪律**：
> **做法优先用抗版本的锚点（文字）；图标与坐标只作补充。**
> 这样版本差异的爆炸半径被限制在"**个别步骤需换做法**"，而不是"**整张技能卡失效**"。

> **⚠️ 优先级不再是"人工固定"（2026-09-17 修正）**：上面 ①→④ 只是**初始顺序**；运行后应交给**动作库按历史胜率自动排序**（试用期排后 → 最近成功优先 → 最近失败降权 → 同档 wins 降序 → 同 wins `avgMs` 升序），见 §17.2。
> **图标模板库同样按 `env_id` 分桶**：同图标在不同版本可能是不同的图 → 模板带 `env_id`，匹配时**同 env 优先**；不同 env 时**回退文字做法**。

#### 7.5.4 北极星的修正（2026-09-17，经"意图/做法分离"后比前一版更强）

| | 原口号 | 前一版修正（分桶）| **本版修正（意图/做法分离）** |
|---|---|---|---|
| 范围 | 一台学会 → **全群**都会 | 同构子群才会 | ★ **技能卡全群共用**；只有"做法"按 env 补 |
| 新版本成本 | —— | **整条链重新探索一次** | ★ **只补差异步骤的做法**（通常 1~2 条）|
| 兜底 | —— | 界面大改 → 重学整卡 | 界面大改 → **通常只改做法表**，意图序列仍成立 |

**→ 结论**：**"一次学会、全群拥有"在「意图层」上是真的成立的** —— 因为**意图跨版本稳定**；
不稳定的只是"用什么方式实现这个意图"，而那**按 env 分桶，代价只有差异那几步**。

#### 7.5.5 运行时怎么落地（不新增机制）

**完全复用已有的 `entry_state` 进门校验** —— 不需要任何"版本探测"：

```
设备执行技能前：
  ① 算当前页面语义签名 → 与技能卡声明的 entry_state 比对
  ② 匹配   → 正常回放（同构子群内 ✓）
  ③ 不匹配 → 【不上手】（安全，不会做错事）
              → 该台自行探索一次（成本 = 一次"首次"）
              → 沉淀出属于它这个界面的技能卡（自然分桶）
```

**★ 正确性由三个机制**共同**保证（2026-09-17 修正，此前本文误称"仅 `entry_state` 即够"）**：

| 差异发生的位置 | 谁保证 |
|---|---|
| **进门时** | **`entry_state`**（签名不匹配 → 不上手）✓ |
| **流程中途**（多一页 / 时序不同）| **`on_fail` 状态收敛序列**（§6.2 五段）← 此前是缺的，靠它补上 |
| **整体界面不同** | **语义签名分桶** |

→ **正确性仍不依赖任何模型**，但保证它的是**三个机制的组合**。版本异构带来的风险（在 B 台上按 A 台的界面瞎点）被这三层挡住；剩下的只是**效率问题**（该台要多学一次）。

#### 7.5.6 探索时就钉身份（2026-09-17 用户提出；从"事后分桶"改为"**事前钉身份**"）

**关键**：**探索循环的第一步本来就是"看当前页面"** → **顺手钉身份是零成本的**：

```
探索开始（本来就要做的第一步：看屏幕）
  → 顺手算出三样，一起成为技能卡的「身份」：
      ① 前台包名（已有）
      ② 起态语义签名（已有原语，§4.6 的 L2）
      ③ App 版本号（可选，需设备端加能力，见 §13.2）
  → 技能卡 = { env_id, entry_state, steps, on_fail, preflight, … }
```

**★ 主次不能错**：
> **分桶键永远是「语义签名」；版本号只是它的「人类可读标签」。**
> 理由：**版本号相同 ≠ 界面相同**（A/B 测试、灰度、地区、账号状态、登录与否）；**版本号不同 ≠ 界面不同**。
> 拿版本号当分桶键会**既漏又重**；拿语义签名当键，**永远对应真实界面**。

**收益（这是"事前钉身份"比"事后分桶"多拿到的）**：
1. **版本间可互相借鉴**：新版本第一次探索时，**先看同指纹的其他版本卡当骨架** → 从"从零摸索"变成"**微调**" ✓✓
2. **可主动补齐**：一眼看出"哪些界面还没有卡" → 主动探索，而不是等它撞上

**命中流程（串起来）**：
```
下发命令 → 意图指纹 → 候选卡集（同指纹、不同 env_id）
   → 算「当前页起态签名」→ 选签名匹配的那张      ← 本就是进门校验，零额外成本
   → 命中 → 零模型回放
   → 无匹配 → 执行循环继续走；走不动 → ⑤ 教师（并沉淀出新的 env_id）
```

### 7.6 教师—执行循环（Teaching-in-the-loop）★ 2026-09-17 架构级修订

> **本节取代原设计的「独立探索阶段」与「内环/外环」二分。** 逐项命运见**第十六章修订总账**。

#### 7.6.1 形态：一条循环，教师是可插拔的补位者

```
下发命令 → 意图指纹 → 找技能卡
   ↓
┌─ 执行循环（探索与回放共用同一条，§6.5）──────────────┐
│  进页面：一次 OCR 建目标表 + 算语义签名                  │
│  每步： 查锚点 → 注入 → wait(since) → 签名/expect 校验    │
│          通过   → 下一步                                │
│          不通过 → on_fail 五段（§6.2）：                 │
│                   ① 等 ② 跳过 ③ 退 ④ 重试（零 AI）      │
│                   ⑤ 【教师介入】← 唯一出现模型的地方       │
│                        └ 教师给出"这一步的动作 + expect"  │
│                        └ 立刻写进技能卡（§7.2 边做边学）   │
│                        └ 控制权立刻交回循环，继续往下走     │
└──────────────────────────────────────────────────────┘
   ↓
完成 → 落 trace（记录"哪几步叫了教师"）
```

**教师介入的粒度 = 一步**（决定"这一步点哪"），**不是"重规划整个任务"** —— 改一步、立刻交回控制权。

#### 7.6.2 为什么它比"探索→沉淀→回放"更好

| | 原设计（三阶段） | **教师—执行循环** |
|---|---|---|
| **首次成本** | **任务长度 × 模型调用**（15 步全探索）| **未知点数 × 一次介入**（只有 2 步新 → 只付 2 次）|
| 学习时机 | 任务结束后**从 trace 蒸馏**（延迟、丢现场）| **卡住那一刻就地学** |
| 部分复用 | 不支持（要么整卡命中，要么从零）| **天然支持**（70% 熟 + 30% 问教师）|
| 组件数 | 探索器 ＋ 执行器 ＋ 外环沉淀器 | **一条循环 ＋ 教师角色** |

**★ 最优雅处：它自动涵盖两种极端，不需要区分模式** ——
```
全新任务（技能库全空）   → 每步都"不认识" → 每步调教师 → ≡ 原设计的"首次全探索"
熟悉任务（覆盖良好）     → 几乎不叫教师     → ≡ 原设计的"零模型回放"
```
→ **"首次探索"不是一个阶段，而是"新任务的必然表现"。** 这就是**探索融合进执行时序**的准确含义。

#### 7.6.3 三条必须守住的纪律（否则会走样）

| # | 纪律 | 违反了会怎样 |
|---|---|---|
| **1** | **不是"立即调教师"，而是"确定性手段用尽才调教师"** —— 教师是 `on_fail` **第五段**，不是第一段 | **弹窗是高频事件**，每次都叫教师 → **模型调用量爆炸**；而弹窗本来用"等/跳过"就能过 ✗ |
| **2** | **"教师"是角色，不是新模型** —— 它就是编排模型在异常点的介入模式 | 引入"教师模型"＝把刚删掉的"双模型委托"换个名字搬回来 ✗ |
| **3** | **"我不认识"必须由确定性判定**：① 签名不在技能库（查表）② 锚点解析失败 ③ `on_fail` 四段用尽 | 用模型判断"我认不认识"＝**本身就是一次模型调用** → 把零模型回放的收益吃光 ✗ |

#### 7.6.4 蜂群收益的重新表述（比原设计更强）

```
原设计：一台学会 → 全群拥有（前提是"学会整个任务"）
修订后：一台的【教师介入点】沉淀后全群共享
       → 每台设备只在"它自己遇到的未知点"叫教师
       → 蜂群总学习成本 = 所有设备遇到的【不同未知点的并集】
         而不是【设备数 × 任务数】
```

#### 7.6.5 新增度量：**教师介入率**

| 数值 | 含义 |
|---|---|
| 高 | 技能库覆盖差 / 遇到新版本 / 有没见过的弹窗 |
| **趋近 0** | **技能库成熟、0 AI 稳定运行** ✓ |

它**同时是"技能库成熟度"与"该补哪里"的直接指标**，比"技能命中率"更细（能定位到具体哪一步）。

---

## 第八章 · 四道防线：把"点错了"从事故降级为可学习事件

| 防线 | 时机 | 手段 |
|---|---|---|
| **1 点之前** | 降低错误概率 | **就近吸附**（半径内吸附目标中心，小按钮/文字链误击率大幅下降）· **置信度门控**（低置信 → 不直接点，升级模型或切通道，**跨模态互证**）· **契约校验**（坐标契约不符则禁用坐标类动作）|
| **2 点之后** | 廉价校验 | **屏幕增量检查**（**⚠️ 不可用固定等待**：注入是异步 + `HumanizeTouch` 行为整形，固定 300–800ms 可能仍读到点击前的帧 → **顺序必须是「注入 → `waitStable` 或最短延迟 → 再判 `screen.hash`」**，见 §4.8.3；纹丝不动 ≈ 点在死区）· **预期状态检查**（页面标题变了？预期按钮出现？OCR 到预期文案？—— 即技能卡 `expect` 字段，**求值必须走 §4.8.1 的四级验证，不得每步都全屏 OCR**）|
| **3 迷路之后** | 结构化回退 | 轻：`back()` / 边缘右滑（成本≈0，先试）→ 中：回**最近 checkpoint**（屏幕哈希匹配的已知良好态）→ 重：belief state 标失效 → 强制重新观测 → 升级大模型重规划 → 兜底：`open_app` 重进已知入口 |
| **4 错过之后** | 让错误变资产 | 前后帧 + CoT 自动进 trace（复盘可查"当时模型在想什么"）→ `failure_patterns` 写入技能卡 → **蜂群其余设备免疫** |

**前提纪律**：**每一步成功都落 checkpoint**（屏幕哈希 + 状态描述）—— 这样"迷路"永远有明确的回家路径。

---

## 第九章 · 长线四支柱

1. **任务表示成"程序"，控制流交给解释器**：自然语言伪代码**编译成真程序**，程序计数器 = 解释器自身的 frame lineno → 循环/条件/函数原生支持，不自研调度器。**漂移的是步骤，不是目标结构。**
2. **上下文有界三板斧**：只喂"根 → 当前节点"路径（兄弟分支与已完成历史不进 prompt）· 变量白名单 + 前 10 项截断摘要 · **Belief State 覆盖式更新**取代对话历史堆积（历史不是越滚越大的雪球，而是**不断被重写的快照**）
3. **Checkpoint = 序列化执行树**：`{lineno, update_vars, duplicate_id}`，恢复时**从树上继续重放而非重新规划**；配合同位置脚本缓存（连续两次生成相同脚本 → 直接重放）
4. **错误恢复双保险**：五操作二次审核（`HOLD/RETRY/RETURN/BREAK/CONTINUE`，其中 **RETRY = 代码成功但目标未达成 → 强制换策略**，专治死循环）+ 技能卡**预声明 `failure_patterns`**

### 9.1 硬约束：可靠性地平线

**20–30 步之后成功率显著下滑** —— 不是上下文装不下，而是**误差累积**（一次误点进入陌生页面 → 模型开始漂移 → 循环打转）。基准任务本身也大多设计在 30 步以内。

> **一次连续运行的现实上限 ≈ 30 步 / 3–5 分钟。**

**正解不是拉高步数**，而是：

```
总任务（数小时）
 └─ 子目标链（每个 10~25 步）
     └─ 每步落 checkpoint（屏幕哈希 + 状态描述）
         ├─ 成功 → 技能库固化 → 后续回放零模型调用
         ├─ 失败 → 回滚到上一 checkpoint 重试
         └─ 连续失败 N 次 → 升级大模型 / 告警人工
```

→ 于是"最长能跑多久"的答案是**无限**——前提是**把长任务切成可靠的短段落，并用技能回放消化掉重复部分**。

---

## 第十章 · 提速三层

| 层 | 内容 | 收益 |
|---|---|---|
| **参数级**（立即可做） | 截图转 **JPEG q50** · `max_pixels` 收紧至 **~130 万** · `history_n ≤ 2` · 去掉每步写死的 `sleep(2)`（改屏幕哈希变化触发）· 失败重试**指数退避封顶 5s** | 单步 **8~15s → 3~6s** |
| **结构级**（收益最大） | **确定性四级路由表** · **`tap_text`/`tap_node` 按元素点**（免截图免坐标预测）· **技能命中零模型** · **每步 LLM 调用 = 1** | **多数步骤不碰大模型** |
| **模型级** | 本地 2B/4B 主力 + 低置信度升级；辅助判断固定小模型 | 稳态 GPU 负载极低 |

**图像 token 预算基准**：`max_pixels ≈ 130万` + `JPEG q50` + **坐标归一化(0–1000)**。

---

## 第十一章 · 观测层：trace 复盘

每步一条 NDJSON：

```json
{"step_id":7,"device_id":"iphone_03","task_id":"...","ts":"...",
 "screen_hash":"a1b2c3","action":{"type":"tap","x":0.5,"y":0.32},
 "model_output":"...","cot":"...","ss_ms":120,"llm_ms":2100,"act_ms":90,
 "ok":true,"skill_hit":false,"checkpoint":true}
```

- 转 **HTML 瀑布复盘**：总耗时 │ LLM │ 截图 │ tokens，**红标瓶颈步**，点开看原始截图 + CoT + 三段计时
  → 一眼看出某步慢是因为**截图传输 / 模型推理 / 设备执行**，也能看到模型当时"在想什么"导致点错
- **蜂群聚合视图**：按 `device × step` 渲染矩阵，**一眼扫出哪台设备在哪个步骤开始偏离群体行为（失败放大的早期信号）**
- 磁盘纪律：**成功步只存 hash + 摘要，失败步才存帧**（要复盘的是失败）

---

## 第十二章 · 蜂群拓扑与三大预埋防线

```
orchestrator（任务队列 / 设备池 / 失败重试）
  ├── worker_1 … worker_N（每台一个轻量循环进程）
  │      └── 传输层（截图 + 注入）
  └── 全部指向同一个推理端点（并发槽可调）
        ↑ 偶尔升级 → API 大模型
```

| 预埋防线 | 内容 |
|---|---|
| **失败放大** | 一步点错 × 20 台 = 20 个坏任务 → 每步做廉价校验，**异常设备踢出队列单独处理**，别让全群陪葬 |
| **行为同质化** | N 台设备动作时序/轨迹完全一致，在消费级 App 里**极易触发风控** → **任务错峰 + 参数抖动**是 orchestrator 的基本能力 |
| **传输层瓶颈** | 多台同时推流先压测；必要时降帧率/降分辨率（模型吃的是压缩后的图）|

---

## 第十三章 · 待决策缺口（原文未覆盖，本文件新增）

> 本章分三组：**开头是「仍开放」的缺口**（原 5 条 —— 其中 3 条已被后续修订**取代/解决**，见其后的"已关闭"表，**现余 2 条**）；**13.1** 是明确排除、但常被误认的项；**13.2** 是设备端能力缺口（原 5 条，**Ponytail 审查后余 2 条**）。三者都不推翻上文，而是上文成立所缺的零件；落地前需逐条决策。

| # | 缺口（**仍开放**）| 为什么关键 | 建议方向 |
|---|---|---|---|
| 1 | **技能参数化（槽位）** | 原文的技能是"操作链"，没有 `slots` 概念 → 搜"南京美食"的技能换"南京烤鸭"就废，**沉淀无法复利** | 技能卡增 `slots` 字段；回放时填槽 |
| 4 | **意图指纹** | 入口是 `routing.yaml` **关键词匹配**，而用户下发的是自然语言 → 做不到"下发即执行" | 规范化意图成 `{app, action, slots}` 可哈希 key → 零 LLM 命中 → 填槽回放 |

**已关闭的三条（2026-09-17 后续修订取代，勿再当待决策项）**：

| 原缺口 | 命运 |
|---|---|
| **2 环境指纹**（`env_fp` 记录 app/ios 版本 + 标 `stale`）| ❌ **已被 §7.5.6 取代** —— 分桶键改用**语义签名**，**版本号只是人类可读标签**；不需要版本指纹，也就不需要设备端提供版本号 |
| **3 成功即沉淀** | ❌ **已被 §7.2 取代** —— 改为「**教师介入即沉淀**」，比"任务成功后沉淀"更精确（介入点才是新知识）|
| **5 每步 1 次 LLM vs 五操作二次审核的冲突** | ❌ **已解决** —— **不做 WPC 二次审核**；AI 只在 `on_fail` ⑤（教师）出场，日常路径 0 次（§6.2/§7.6）|

### 13.1 上文未采用、但常被误认的项（明确排除）

| 项 | 处置 |
|---|---|
| 编译成真 Python、用解释器 frame lineno 当 PC | **改用显式 JSON 步骤机 + 游标**（`{steps:[...], pc:7}`）：同样可重放/可 checkpoint，但无运行时依赖、可跨语言、可校验 |
| 全量帧落盘 | 只存失败步的帧（见第十一章磁盘纪律）|
| 蜂群并发/多卡扩容 | **属目标形态，但排在单机链路完全体之后**（第一章阶段目标表）|
| **「执行反馈」≠「点击落点可视化」**（最常见的混淆）| 前者是**机器的输入**（校验结果，运行期必需，0 AI 时照常运转）；后者是**给人看的**（调试期可选，机器不消费）。**不要把可视化接进反馈回路** —— 那是用可视化复刻一份已经存在的机器判定 = 冗余。落点可视化降级为**真机调技能时的可选调试工具**，见 §4.8.4①⑦ |
| **注入完成回执**（`touch.tapSync` / `wait:true`）| 它回答"**注入线程返回了吗**"，而非"**界面响应了吗**" —— 对"点空"零信息量；且需 CI，还会动摇"ack 提前返回防阻塞命令通道"这个有理由的设计。见 §4.8.4⑦ |

### 13.2 设备端能力缺口（2026-09-17 代码实证 + Ponytail 审查后）

> 原列 5 条，**审查后删 3 条**，只保留下面 2 条。

| # | 缺口 | 为什么必须（附硬数字/依据）| 落点 |
|---|---|---|---|
| **8** | **OCR「放哪端 + 用哪档」真机实测**（**最高优先级**）| 原设计称"设备端 Vision OCR 10–30ms"，而代码注释写 **`.accurate` 全屏 0.5~2s** —— **差两个数量级**，原结论不成立。必须实测三组并对比：**① 设备端 `.accurate`**（现状）/ **② 设备端 `.fast`**（精度代价未知）/ **③ 主机侧 PaddleOCR**（~100ms ＋ board 档传图 56ms；但 320px 降采样可能不够 OCR，取原图则带宽上升）。**在结论出来前，任何实现都不得假设"OCR 很快"**。| 设备端 + 网关（需真机实测）|
| 9 | 图标模板库（`find_image` 的素材）| 锚点第 ② 层（`find_image`，<1s、抗改版）需要模板；**蜂群共享模板库 = 一台截取、全群可用**，与技能库同源。| 编排侧资产 |

**已删的三条（审查记录，勿再提出）**：

| 原缺口 | 为什么删 |
|---|---|
| ~~**6 `ui.expect` 批量谓词求值**~~ | ❌ **用新接口复刻已有能力** —— **`vision.ocr` 一次就返回【全部文字行 ＋ 坐标】** → 所有谓词（`entry_state` / `expect`）**在编排侧本地求值即可，零额外设备端调用** → 不需要这个接口（这正是「三问」要拦的那类提案）|
| ~~**7 `script.exec`（列为设备端缺口）**~~ | ❌ **性质搞错了** —— 执行循环跑在**编排侧**（§6.5：编排侧管基线、查表、求值谓词），所以 `script.exec` **就是 §6.5 的执行循环本身**，属编排侧代码，**不是设备端缺口**。代码注释里那个座位指的是"上层"，不必然是设备端 |
| ~~**10 `app.list` 返回版本号**~~ | ❌ **已判非必需** —— 分桶键是语义签名，版本号只作可读标签；**为一个"只是好看"的标签去动设备端 + 走 CI = 典型 YAGNI** |

---

## 第十四章 · 落地顺序（依赖驱动，每步可独立验收）

| 顺序 | 建什么 | 为什么排这里 |
|---|---|---|
| **1** | **trace（四类事件，各标消费方）**，第一等公民字段 ＝ 本步是否调用了教师 | 它是**教师介入率**的唯一数据源；**四类事件各有明确的沉淀消费方**（§17.2）|
| **2** | **执行循环 ＋ 内联校验 ＋ 进门校验**（§6.5 阶段 0-5；四级验证 §4.8.1）| **这是主体** —— 锚点解析 ＋ 动作 ＋ 校验 ＋ 基线前移；**不含教师也能跑**（只是走得短）|
| **3** | **技能卡（意图层）＋ 做法表（env 层，含权重）** ★ **一件拆两件** | **沉淀的容器**。意图**全群共用**、做法按 `env_id` 分桶（§7.5.2）—— **拆开才能兑现"一次学会全群拥有"** |
| **4** | **能力手册**（每条标 **Completion** ＋ 风险分级 ＋ **INTENT_ONLY** 标注）| 让"什么算完成"**预先定义**而非每次现想（§17.1 #4/#5/#12）；也是 `success_criteria` 的来源 |
| **5** | **`on_fail` 五段 ＋ 组合脚本**（§6.2 / §17.1 #7）| 前四段让「**零 AI 自愈**」成立 —— 走到这里，熟悉路径已可**完全无人** |
| **6** | **意图指纹 ＋ 技能命中** | 打通"下发命令直接触发" |
| **7** | **⑤ 教师介入 ＋ 两阶段沉淀**（`draft` → 闸门 → 广播，§7.2）＋ **`evolution` 账本** | 走到这一步系统才"**会生长**"—— 前六步只让它"会重复" |

**顺序纪律**：1→2 是"**能跑**"，3→6 是"**能重复**"，7 是"**会生长**"。

**审查后删除的两项（Ponytail，2026-09-17）**：

| 原项 | 为什么删 |
|---|---|
| ~~技能阶梯（技能可调用技能）~~ | ❌ **投机** —— 没有证据表明需要它；且在教师循环下，复杂任务的组合由**教师就地处理**，不必预先设计"技能嵌套"这一层机制 |
| ~~路由卡 `routing.yaml`（含四级分类与风险分级表）~~ | ❌ **对我们是空转** —— 路由卡的价值是"确定性路径优先（CLI / MCP 直干）"，而**我们设备端只有 GUI 注入这一条路**（无 shell/CLI 能力）→ **没有可路由的东西**。它唯一有用的一条规则保留为**内建动作优选**（见下），**不要那个数据文件与匹配机制**。**注：风险分级（SAFE/CONFIRM/`NEVER_AUTO`）已内建在 §6.2 ⑤，不属被删部分。** |

> **替代 `routing.yaml` 的一条内建规则（零机制）**：**能用设备端直接能力就别点 GUI** ——
> 「打开 App」用 `app.open`（不点主屏图标）、「输入文字」用 `type.paste`（不点键盘）、「返回」优先 back。
> 这是**执行循环里的一行动作优选**，不是一层路由机制。

---

## 第十五章 · 来源与可追溯性

| 项 | 值 |
|---|---|
| 原始会话 | `session-a120d693-e6fe-41ee-a0c9-2d5a400e67c8`（标题「精准UI识别手机操作框架」）|
| **入库状态** | ❌ **未入库**（该会话 `messages.jsonl` 为 0KB）→ 本文件是它唯一的完整留存 |
| 还原来源 | `dsh-session-21e6e53f-…/history/archive_002/messages.jsonl` 中的 `<referenced-sessions>` 快照（capturedThroughSeq 41914，38 轮）|
| 同期调研底稿 | `D:\编程项目\手机Agent框架机制提炼报告.md`（11 章，含代码级 `文件:行号` 引用）|
| 同期参考仓库 | **★ 2026-09-18 起已汇集到本仓库内 `_research/`（已 gitignore，不入库）**：MobileAgent(v3.5) · AgentProg · PhoneHarness · mobile-use · mobilerun · OmniParser · surya · AppAgentX · mobile-harness · TrollVNC-upstream · dsh-ios-app · imessage-data-foundry（共 12 个，均已 `git fetch` 对齐上游最新）|
| **★ 2026-09-18 深研产出** | ① `参考项目调研-共同优点-2026-09-18.md`（12 项目横扫：13 条共同优点 + 对照本项目的 8 条差距）<br>② `参考项目深读汇总-面向通用MCP服务器-2026-09-18.md`（PhoneHarness / mobile-harness / AgentProg 源码级深读 + 面向「通用 MCP 服务器」的设计建议）<br>③ 逐项目笔记（**不入库**）：`_research/_notes/{PhoneHarness,mobile-harness,AgentProg,其余5项目-浅读}.md` |
| 本文档落盘 | 2026-09-17 |

> **留档理由**：该设计原本只存在于一次未被记忆库捕获的会话里，**随时可能彻底丢失**。落盘后它从"某次会话的记忆"变成**项目资产**。

---

## 第十六章 · 修订总账（2026-09-17：哪些被推翻、哪些可复用）

> **一句话概括**：**骨架没动，血管重接了** —— 原来"学"与"用"是**两套流程（三阶段）**，现在是**一条循环上教师随时补位**。
> 本章回答两个问题：**新方向推翻了什么**（§16.1）、**旧文档还有哪些能直接用**（§16.2）。

### 16.1 被推翻的（逐项列明，避免后人照着旧结论实现）

| # | 原设计 | 修订后 | 为什么 |
|---|---|---|---|
| 1 | **Phase 1 探索 / Phase 2 回放**（两阶段）| **一条执行循环 ＋ 教师补位**（§7.6）| 首次成本从"任务长度 × 模型"降到"未知点数 × 一次介入" |
| 2 | **内环 / 外环**二分 | **外环取消** —— 沉淀是教师介入的副产品 | 就地沉淀不必事后从 trace 蒸馏 |
| 3 | "**重复 ≥2 次**才固化" | **教师介入即沉淀**（可选二次确认）| 原机制被动且滞后 |
| 4 | 感知五层（含 ①WDA / ④OmniParser / ⑤grounding）| ①**不可得**；④被**图标模板库＋`find_image`**替代；⑤**伪需求** | 我方无 WDA；模板匹配零模型更省（§4.2/§6.4）|
| 5 | **双模型委托**（编排 ＋ GUI 工作模型）| **单模型 ＋ 设备端确定性感知**（§5）| GUI 工作模型的职责被 `find_text`/`find_image` 吃掉 |
| 6 | 设备端 Vision OCR **首选（10–30ms）** | **待真机实测**（代码注释为 0.5~2s，与主机侧对比）| 原数字**错两个数量级**（§4.2）|
| 7 | 技能"按**前台包名**只加载一张" | 按 **`env_id`**（＝包名 ＋ 起态语义签名）| **包名 ≠ 任务**；且需要版本/界面分桶（§7.5/§7.5.6）|
| 8 | "**成功即沉淀**" | "**教师介入即沉淀**" | 更精确：教师介入点才是新知识（§7.2）|
| 9 | trace 作为"**蒸馏原料**" | trace 改为"**观测管道**"（介入率 / 能力边界图）| 沉淀已就地完成（§7.4）|
| 10 | `on_fail` **四段**（重试在前、AI 在第四段）| **五段**（**"等"最前、"重试"最后、教师第五段**）| "等"最便宜且治时序差异；"重试"治不了"多了一页"（§6.2）|
| 11 | **像素哈希**做状态识别 | **语义签名** | 真机证伪（视频流 6 次采样 6 个不同哈希，§4.6）|
| 12 | 技能存**坐标 / 目标表 id** | **只存语义锚点** | 跨设备/跨版本不可移植（契约 C-A）|
| 13 | 编译成**真 Python**、用 frame lineno 当 PC | **显式 JSON 步骤机 ＋ 游标** | 运行时依赖脆弱（§13.1）|
| 14 | 蜂群 orchestrator ＋ vLLM 多卡 | **排在单机完全体之后** | YAGNI（§13.1）|

### 16.2 ★ 可复用的（**保留不动**，这是原设计的价值所在）

| # | 原设计资产 | 为什么仍然成立 |
|---|---|---|
| 1 | **公理一：GUI 是最后兜底，不是默认路径** | 完全成立，且被强化（动作优选：能用 `app.open`/`type.paste` 就不点 GUI，§14）|
| 2 | **公理二：Agent 面对目标表，不面对像素** | 完全成立 —— 这正是"锚点而非坐标"的依据 |
| 3 | **可点击目标表中间件的四层结构**（帧管理器 → 多源提取 → 融合索引 → 解析执行）| **结构完全保留**；只把"多源"从 WDA＋视觉检测改为 **设备端 OCR ＋ 模板匹配** |
| 4 | **意图与做法分离**（技能卡只写意图；做法按 `env_id` 成表、按历史胜率排序）| **2026-09-17 升级**：原"锚点四层（人工固定优先级）"保留为**冷启动初始顺序**，运行后交做法表自动排序（§7.5.2/§7.5.3/§17.2）|
| 5 | **坐标契约内置在中间件**（逻辑点 / 0–1 归一化）| 完全保留 |
| 6 | **四道防线**（点之前 / 点之后 / 迷路之后 / 错过之后）| 框架完全保留；"点之后"判据按 §4.8 细化；第 4 条"**让错误变资产**"在新设计里由 `failure_patterns` ＋ 教师介入共同承担 |
| 7 | **每步落 checkpoint**（屏幕哈希 ＋ 状态描述）| 保留，只是"哈希"升级为**语义签名** |
| 8 | **长线四支柱**（任务是程序 / 上下文有界 / checkpoint / 错误恢复双保险）| 保留（其中"程序"落地为显式 JSON 步骤机）|
| 9 | **提速三层**（参数级 / 结构级 / 模型级）| 保留；**参数级的具体数字仍有效**（JPEG q50、max_pixels ~130 万、去写死 sleep、退避封顶 5s）|
| 10 | **每步 LLM 调用 ≤ 1** | 保留，且被强化为"**0 或 1**"（只有教师出场才计 1）|
| 11 | **可靠性地平线 20~30 步** | 保留 —— 它同时是新设计"为什么必须有教师补位"的理由 |
| 12 | **技能六字段 ＋ `load_skill` 渐进加载** | 六字段保留，另加 `env_id` / `slots` / `expect` / `on_fail` / `preflight` |
| 13 | **风险分级 SAFE / CONFIRM / `NEVER_AUTO`**（原"确定性路由表"的一部分）| 保留 —— 已内建在 **§6.2 ⑤**（高风险/不可逆动作禁止自动重试）。**注：`routing.yaml` 的四级分类、数据文件与匹配机制已删（理由见 §14），只留下风险分级** |
| 14 | **蜂群三大预埋防线**（失败放大 / 行为同质化 / 传输瓶颈）| 保留 —— "每台各自进门确认"正是**抑制失败放大**的机制（§7.6.4）|
| 15 | **trace 的字段 schema**（step_id / device_id / screen_hash / 三段计时 / skill_hit / checkpoint）| 字段保留，**新增 `teacher` 标记** |
| 16 | **设备端能力实证 ＋ 注释依据 ＋ `script.exec` 座位** | 保留 —— ① §4.6.3 的能力清单是"**实现出发点**"（不得假设设备端有它没有的能力）；② 代码注释里"设备端＝机械匹配原语、语义判断在编排侧"**是本次修订的权威依据**；③ 注释预留的 `script.exec`/`find_and_click` **就是执行循环（§6.5/§7.6）的落地位置** |

> **Ponytail 审查（2026-09-17）后的表内调整**：原 20 条 → **16 条**。
> 删除 1 条：~~"参考项目依仗什么"对照表~~（**与 §4.5 完全重复**）；
> 合并 3 条：原 #17/#18/#19 → 现 #16；原 #20（防线第 4 条）→ 并入现 #6。
> 表内序号已按审查后重排（本表序号是**行号不是章节号**，不涉及章节锚点）。

### 16.3 用途改变（保留但换了职责）

| 资产 | 原用途 | **新用途** |
|---|---|---|
| **trace** | 学习管道（蒸馏技能的原料）| **观测管道**（教师介入率 / 能力边界图）|
| **外环** | 一个独立阶段（跨任务沉淀）| **取消** —— 沉淀是内环（教师介入）的副产品 |
| **"首次探索"** | 一个必须走的阶段 | **新任务的必然表现**（不是阶段，是现象）|
| **语义签名** | 只做状态识别 | **同时是技能库的分桶键**（`env_id` 的核心，§7.5.6）|
| **`on_fail`** | 出错后的兜底航线 | **教师之前的缓冲带**（前四段零 AI，教师只在第五段）|

### 16.4 不变的北极星

> **一台学会的操作，全群立刻都会；重复任务不再调用大模型。**
> 变的只是**兑现方式**：从"**先完整学会一个任务、再回放**"变成"**边走边问、问过即会**" ——
> 后者让成本**按未知点的数量**付，而不是**按任务的长度**付。
> （2026-09-17 经"意图/做法分离"后，"全群"不再需要退让成"同构子群" —— 见 §7.5.4。）

---

## 第十七章 · 外部蓝本采纳清单（2026-09-17）

> **本章是"我们不是从零发明"的证据链**：本地已拉取的几个项目里，`dsh-device-skill` 的意图与**我们几乎 100% 重合**
> （大模型探索 → 沉淀资产 → 命中回放 → 失败降级 → 转正广播）。本章逐条列出**采纳什么、改造什么、不采纳什么**，
> 并汇总**三处对本文前面结论的自我纠正**。
>
> 蓝本与出处（**★ 2026-09-18 起参考项目已汇集到本仓库 `_research/`，路径已更新**）：`D:\编程项目\dsh-device-skill\SKILL.md`（244 行 + `evals/evals.json`，未移动）· `_research/PhoneHarness/skills/`（真实 YAML 实例）· `_research/mobile-harness/`（`CARD.md` + `core/memory/GUIDE.md` + 平台 GUIDE）· `_research/mobilerun/app_cards/`（卡片 provider 实现）

### 17.1 直接采纳（16 条，按价值排序）

| # | 采纳什么 | 出处 | 对我们的作用 |
|---|---|---|---|
| **1** | **意图与做法分离**：技能卡只写意图动词（`navigate / open / find_and_click / input_text / scroll / back / home / wait_for / expect`），**设备做法归做法表** | 红线 #1 | ★★★ **已落进 §7.5.2**（推翻"分桶"方案）|
| **2** | **做法表：`intent:target@env` → 多方案 ＋ 权重**（`wins/fails/avgMs/last_success/last_fail/probation`）| 附录 D | ★★★ **替代"人工固定的锚点优先级"**（§7.5.3 已补注）|
| **3** | **两阶段沉淀**：单次成功 → `draft`（立即可用）；重复 ≥2 次 → 过闸门（重放全 `expect` 通过 ＋ 灰度 1 台）→ 全群广播 | 阶段 D/E | ★★★ **已落进 §7.2**（推翻"二选一"）|
| **4** | **能力手册每条标「什么算完成」（Completion）**——如"调字体 → verify `settings get` 返回 1.3" | `device.yaml` | ★★★ **解决"`success_criteria` 从哪来"**：**预先标注，不是每次现想** |
| **5** | **`INTENT_ONLY`：「只拉起界面 ≠ 完成」** | `routing.yaml` | ★★★ **已落进 §6.2**（本文前一版删 `routing.yaml` 时丢掉的语义，现补回）|
| **6** | **每步必有 assertion：「无断言的步骤视为未执行」** | 附录 E 约束 | ★★ 强化 `expect` 契约（措辞比我们硬）|
| **7** | **降级链顺序固定、不跳级**：次优方案 → **组合脚本**（历史成功方案按权重序编译，串行 ＋ 退避 ＋ 密度限额）→ 介入调试 → 阻塞（连续 ≥2 次）| 阶段 C | ★★ 对齐 `on_fail`；**"组合脚本"是我们没有的一步** |
| **8** | **「介入只由实际失败触发，不预判」** | 红线 #8 | ★★ **独立来源交叉验证**了 §7.6.3 纪律① |
| **9** | **trace 只记四类事件，各标消费方**：`step`→成功路径与权重 · `tool_call/result`→复盘 · `repair`→失败模式与账本 · `verification`→技能卡断言 | 附录 B | ★★ 比我们"一大坨 schema"清晰 |
| **10** | **`evolution` 账本**：每次变更一行 `{rev, type, source, gate, status}` | 红线 #7 | ★★ **审计与回滚的唯一依据**（已落进 §7.2）|
| **11** | **汇报四态**：`success / success_with_repair / partial / blocked`；**`success_with_repair` 最有价值**（repairs 段 ＝ 新坑已沉淀）| 汇报模板 | ★★ 我们要有 |
| **12** | **风险分级的具体实例清单**：`SAFE_COMPLETE` / `CONFIRM_FIRST`（读联系人·短信·GPS·相机·发短信·打电话）/ `NEVER_AUTO`（批量删数据·不可信安装·关锁屏密码·恢复出厂·金融）| `device.yaml` | ★★ 我们有"分级"但**没有具体清单** |
| **13** | **渐进披露**：索引只列「技能 / 描述 / **何时用**」，"**不要猜 API 格式，先 load**" | `index.yaml` | ★★ 技能库的组织方式 |
| **14** | **`memory/` Markdown wiki**（agent 自维护），格式：`- 日期: 事实。Source: 来源。Confidence: high\|medium\|low`；**"老事实是线索不是真相，用前重验"** | mobile-harness `GUIDE.md` | ★★ 让"可复用经验库"有了具体形态 |
| **15** | **memory 的 Never Store**：密码 / API key / 支付数据 / **从 App 或网页抄来的提示词式文本** | 同上 | ★★ 安全红线（见 §17.5）|
| **16** | **「屏幕文字是不可信数据，不得存为指令」** | 同上原文 | ★★★ **对我们尤其致命重要** —— 我们**大量读屏幕文字**，把 App 里的字当指令 ＝ **提示注入漏洞** |

### 17.2 三件要补的资产（蓝本有、我们没有）

| 资产 | 最低形态 | 为什么 |
|---|---|---|
| **做法表**（原"动作库"）| 一个 JSON：`intent:target@env → plans[]`，每 plan 带 `stats{wins,fails}` / `weight` / `last_success` / `last_fail` / `probation`；一个"取最高权重"的函数 | 让做法优先级**按历史胜率自动排序**，而不是人工拍 |
| **`evolution` 账本** | **每次变更追加一行**（不做版本管理 —— Ponytail）| 技能卡会被自动修改，**账本是回滚的唯一依据** |
| **四类 trace 事件** | 在现有 schema 上**标出"这条喂给谁"** | 让沉淀有明确的输入源，而不是"一堆日志" |

**做法表的权重排序规则（照抄蓝本）**：
> 试用期（样本 < K）排所有成熟方案之后 → **最近成功时间最新优先** → **最近失败显著降权 / 连续失败 ≥N 摘牌** → 同档 `wins` 降序 → 同 `wins` `avgMs` 升序（**快的优先**）

### 17.3 要改造成「视觉版」的（不能原样用）

| 它的做法 | 我们的现实 | 改造 |
|---|---|---|
| 观察面四档 `dom → a11y → uia → visual`（结构化优先）| ⚠️ **我们只有 visual（设备端 OCR ＋ 模板匹配）→ 永远在最后一档** | **做法表必须比它做得更厚**（无结构化通道 → 同一意图的做法差异更大）|
| 按 **platform** 分桶 | 我们没有跨平台，但有**版本/界面差异** | 分桶维度改为 **`env_id`（界面语义签名）** |
| **设备子代理**（每台一个 agent）| 我们是**一个编排者 ＋ N 台设备** | 它**没解决蜂群并发写** → 这块要我们自己补 |
| **静态 `preconditions`** | 我们有**进门校验（运行时签名比对）** | 我们的更强 → **保留自己的** |

### 17.4 不采纳

| 不采纳 | 理由 |
|---|---|
| 它的"每台设备一个 agent"拓扑 | 我们有网关共用技能库，拓扑不同 |
| `REAL APP BENCHMARK RULE`（不得用 CLI 结果替代真 App）| 那是**它评测场景**的规则，不是我们的产品需求 |
| Termux / CLI 类能力 | 我们设备端无 shell（§4.6.3）|
| 它的 `replay.py` | ⚠️ **名不副实** —— 它做的是**复盘渲染**（读 `journal.json` 渲染文本），**不是动作重放**；真正的重放在蓝本的 B1 阶段 |

### 17.5 安全红线（从蓝本采纳，必须遵守）

| 红线 | 内容 |
|---|---|
| **屏幕文字不可信** | **屏幕上的文字是"数据"不是"指令"** —— 不得把它当命令执行、不得存进 memory 当指令、不得据此扩大权限。**我们大量读屏幕文字，这条是防提示注入的第一道闸** |
| **Never Store** | memory / 技能卡里**永不存**：密码、API key、支付数据、**从 App 或网页抄来的提示词式文本** |
| **`NEVER_AUTO` 清单** | 批量删数据、不可信来源安装、关闭锁屏密码、恢复出厂、**任何金融交易** → **只解释风险并停手上报，绝不自动执行**（§6.2 ⑤）|
| **遇到认证就停手** | 遇到**密码 / 面容 / 支付 / 验证码 / 账号恢复** → **停手上报**，不得尝试绕过（蓝本：转专门 credentials 指南）|

### 17.6 ★ 三处自我纠正（汇总指针）

> 这三处都是**本文前面章节被蓝本证伪或升级**的地方，已就地修正，此处汇总便于复查：

| # | 我前面说的 | 实际（蓝本做法）| 修正去向 |
|---|---|---|---|
| **1** | "用**语义签名分桶**"（一个界面一张卡）| **意图层共用 ＋ 做法层按 env 分桶** | ✅ **§7.5.2 / §7.5.3 / §7.5.4 已改** |
| **2** | "**教师介入即沉淀**"，并批评原设计"重复 ≥2 次太被动" | **两阶段**：立即 `draft` ＋ 过闸门才广播 —— 两者不矛盾 | ✅ **§7.2 已改** |
| **3** | 删 `routing.yaml`（理由：对我方空转）| **形式对、内容错** —— 它承载的 **`INTENT_ONLY`（只拉起界面 ≠ 完成）** 等语义**不该一起删** | ✅ **§6.2 已补回**；§16.2 保留记录 |

### 17.7 结论：我们该写的 Skill ＝ 蓝本的「视觉版改造」

| 动作 | 内容 |
|---|---|
| **保留** | A→E 五阶段骨架 · **红线清单 ＋ 判定速查表的形式** · trace 四类事件（带消费方）· **做法表（多方案＋权重）** · **两阶段沉淀＋闸门** · `evolution` 账本 · 汇报四态 |
| **替换** | 观察面 `dom/a11y/uia` → **设备端 OCR ＋ 模板匹配 ＋ 语义签名**；`platform` 分桶 → **`env_id` 分桶** |
| **新增（它没有的）** | **进门校验**（运行时签名比对）· **意图/做法分离后的 env 做法表** · **蜂群共享的并发写控制** |
| **删除** | Termux/CLI 能力 · `REAL APP BENCHMARK RULE` |

**→ 一句话**：**我们不必从零设计 —— 蓝本的骨架直接可用，我们只需把"眼睛"从结构化树换成"设备端视觉原语"。**

---

### 17.8 ★★ 二轮深研新增（2026-09-18，PhoneHarness / mobile-harness / AgentProg）

> **背景**：§17.1–17.7 是围绕蓝本 `dsh-device-skill` 做的。2026-09-18 又把三个项目做了**源码级深读**
> （PhoneHarness · mobile-harness〔含其真库 `mobilerun-core`〕· AgentProg），产出两份报告：
> `参考项目调研-共同优点-2026-09-18.md` · `参考项目深读汇总-面向通用MCP服务器-2026-09-18.md`。
> 下面只列**§17.1 的 16 条【尚未吸收】的新增项**（已吸收的不重复）。

#### 17.8.1 新增采纳（8 条）

| # | 采纳什么 | 出处 | 对我们的作用 |
|---|---|---|---|
| **N1** | **能力"不调用即内省"**：`is_supported()` / `describe()` 在**不执行方法体**的前提下就能回答"这台设备支不支持" | `mobilerun-core` | ★★★ **直接治我们「注册了但 0x50/5802 没实现、调用才炸」的坑** |
| **N2** | **不支持时返回【结构化错误】并给替代路径**：`@unsupported_on(backend, reason, alternative, hint)` → 抛 `UnsupportedOperation{verb,backend,reason,**alternative**,hint}`；范例：iOS `app_stop` → reason "不暴露强杀 verb" + hint "改用 `key('home')`" | 同上 | ★★★ **模型永远拿不到"设备做不到的动作"**，且知道该退而求其次用什么 |
| **N3** | **必须区分「没有」与「读不到」**：① `UiState.dead_tree` 显式可用性属性；② **字段缺失永不算 hidden**（原文 *"A missing flag is not proof of visibility"*）；③ 截图用**魔数**兜底而非信 `Content-Type`；④ 停摆判定用**容差**而非相等（真机证据：iOS 设置页 uptime **每秒翻秒**，精确比较会永久失效） | 同上 | ★★★ **我们只有 visual 通道，这条最值钱** —— 现在 `vision.ocr` 返回空数组时，**分不清"屏幕真没字"与"这一帧读不到"** |
| **N4** | **控制流由【程序】保证，模型只填单步**：LLM 先生成 DSL 程序，`compile_workflow()` 机械改写成可执行骨架，**控制流真跑**；**禁止模型自己写 `for`/`if`**；双模式交替（生成一步代码 ↔ 单选 `hold/continue/break/return`）| **AgentProg** | ★★★ **我们当前把控制流也交给模型了** —— 这正是"跑到一半忘了在干嘛 / 重复动作到步数上限"的根源 |
| **N5** | **上下文压缩靠【结构剪枝】而非 LLM 摘要**：上下文 = `show_python_context()`（沿 parent 链**只拼当前执行路径**）+ 程序全文 + 变量快照 + 信念表；**没走到的分支结构上就不存在 → 循环 100 轮上下文不增长**；进度 = **程序计数器 + 变量**（不是自由文本）| 同上 | ★★ 比我们"最近 4 张截图"更省更稳；且**纯代码，无模型调用** |
| **N6** | **`grab_similar_nodes()`：按 `(文件名, 行号)` 精确召回同一程序步的历史代码** | 同上 | ★★★ **这就是现成的"第二次执行查表"** —— 与 §7 的**技能卡位置完全一致**，可直接承载"做法表" |
| **N7** | **声明式能力表【驱动】工具面**：一份 schema 文件 → 生成 `tools/list` + 分派 + 内省；**加能力只改一个文件**；每工具带 `mode`(complete/intent_only/bootstrap) + `risk` + **`verify`（一条非模型原语校验）**；统一返回 `ToolResult{ok,summary,exit_code,checks,artifact_paths,image_base64}` | **PhoneHarness** | ★★★ **替掉"注册表 + 0x50 + 5802 三处补齐"**（§17.1 #4 有"能力手册"，但**没有"手册驱动工具面生成"这层**）|
| **N8** | **失败处理的四个具体机制**：① **只对【读】退避重试，写动作不重试**；② **重复动作 ≥6 熔断**（+ 同 turn 既截图又动作直接 BLOCKED）；③ **`key("power")` 硬禁**（亮屏时等于锁屏困死整个 run）；④ **反转矩形/零宽高拒点** —— 反例原文：*"a touch that can't land would be a silent no-op (the worst failure mode: it **looks** like the tap worked)"* | PhoneHarness + `mobilerun-core` | ★★ **④ 正中我们**：`touch.tap` 只要发出去就 `ack ok=true`，**"看起来成功了"正是它警告的最坏失败模式** |

#### 17.8.2 ★ 一处需要与第五章对照（视觉外包）

| 项 | 内容 |
|---|---|
| **第五章的结论** | **"单模型 + 设备端确定性感知"**（原"双模型委托"**已修订**）—— 理由见 §5 与 §4.8（每步 LLM 调用次数应为 1）|
| **PhoneHarness 的实践** | **`run_seed_gui_subtask`：主模型只发目标，视觉操作【整体外包】给 gui_model**；其架构是**双模型分工**（`ConsoleConfig.model` 编排 + `gui_model` 做 GUI worker，`_resolve_gui_backend` **强制显式、无隐式回退**）|
| **★ 为什么不矛盾，且对我们关键** | 两者说的**不是同一件事**：§5 反对的是"**每步都让模型做决策**"（成本）；而 PhoneHarness 外包的是"**看屏幕并产出坐标**"这个**纯感知动作** —— 它**恰好解掉我们的硬前提**：**`deepseek-v4.1-flash` 不支持图像输入 → 演员没有眼睛**。即：**导演（有语言能力、无视觉）发目标；演员（有视觉、无规划）执行**，与主干 §三 的角色分层**完全同构** |
| **★ 待办** | **§5 需补一句边界说明**：单模型指"决策只用一次"，**不排除把"视觉落地"整体委托给专用模型** —— 后者不算"多角色架构"（§17.1 #16 反面教材指的是**每轮多次 LLM 调用**）|

#### 17.8.3 三家【都没做】的 = 我们的机会

| 项目 | 跨任务技能沉淀 |
|---|---|
| **AgentProg** | ❌ `cache_mode` **默认关闭**，且只比连续两次、只在进程内 |
| **PhoneHarness** | ❌ 只有**人工维护**的 `skills/*.yaml`，无自动沉淀 |
| **mobile-harness / mobilerun** | ❌ 无 |

**→ 这正是 §7「生长层」要做的位置**；而 **N6（`grab_similar_nodes`）提供了现成的召回实现**。
