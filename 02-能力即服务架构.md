# 02 - 能力即服务架构

> **来源**：IPA改造计划.md 行 380-609（Phase 4）+ 行 1263-1826（附录 C/D/E/F/G）
> **关注点**：能力注册框架、命令通道 v2、配置全量上报、配置热重载、本地端口代理、网关 REST API、前端自动渲染、AI 工具 WS 接口、触控坐标归一化、控制型能力清单、配置型能力清单、API 端点、新增能力开发规范
> **索引**：[返回主文档](IPA改造计划.md)

---

## Phase 4：能力即服务架构

**目标**：所有 IPA 能力暴露为统一 API，支持单台控制与批量管理；新增能力即插即用。

### 4.1 能力注册框架 ✅ 已完成（v1.8 修正后）

**文件**：`src/TRCapabilityRegistry.h` / `src/TRCapabilityRegistry.mm`

**设计**：数据驱动表，能力以表项注册（metadata + executor block），框架自动路由。

> **v1.8 修正后状态**：
> - **ReverseMode schema 已删除**（Phase 6 完成）
> - **6 项网关/SSL schema 已补齐**（Phase 8 完成）：`Enabled` / `GatewayHost` / `GatewayPort` / `GatewayToken` / `SslCertFile` / `SslKeyFile`
> - **Notifications/PerformanceMode 枚举已合并**（Phase 8 完成）

**注册表接口**：
- `allCapabilityIds` / `allControlMetadata` — 供上报 capabilities + capMetadata
- `allConfigSchema` / `currentConfigs` — 供上报 configSchema + configs
- `invoke:capId:params:error:` — 统一执行入口（按 route 自动分发）
- `setConfig:key:value:error:` — 统一配置入口（写 NSUserDefaults + 返回 reload）

**能力分类与路由类型**：

| 类别 | route.type | 执行通道 | 示例 |
|---|---|---|---|
| HID 硬件注入 | `hid` | STHIDEventGenerator 直接调用 | home/power/volup/briup/keyboard |
| 触控类 | `touch` | 归一化坐标转原生像素 + HID | touch.tap/touch.swipe/touch.pinch |
| 触控笔类 | `stylus` | 归一化坐标 + 触控笔 HID 事件 | stylus.tap/stylus.down |
| 本地命令 | `localcmd` | 转发到 46752 控制端口 | clients.list/disconnect（4.5 已完成） |
| 原生调用 | `native` | Objective-C 方法直接调用 | clipboard.get/clipboard.set/screenshot/notify.banner |
| 进程守护 | `watchdog` | TRWatchDog 方法调用 | service.restart/service.signal |

**配置生效分级**：

| reload | 含义 | 实现方式 | 示例 |
|---|---|---|---|
| `instant` | 写入即生效 | 无副作用，下次读取自动用新值 | clipboardEnabled、viewOnly、naturalScroll |
| `hot` | 热重载 | trollvncserver 监听 NSUserDefaults 变更，重载相关模块（4.4 已完成） | scale、frameRateSpec、orientationSync、watchdogThrottleInterval |
| `gateway` | 网关刷新 | 改变注册字段，触发重新 register 上报 | bonjourEnabled |
| `restart` | 必须重启 | 涉及端口/认证/RFB 协议头，watchdog 重启 | port、fullPassword、httpPort |

> **术语统一**：reload 策略统一用 `gateway`（附录 E.2 网关组三项的 `reconnect` 是同义，统一为 `gateway`）。

**已完成状态**：13 个控制型能力 + 34 个配置项 schema 全注册；Makefile 已加入构建。
> v1.7 最终已注册 97 项控制能力 + 38 项配置 schema（Batch 0-3 全部完成）。完整清单见 [能力追踪清单.md](能力追踪清单.md)。
> **2026-08-11 核实**：代码直接计数 **75 控制 + 45 配置**（含循环批量注册展开）；97+38 为文档口径，最终总数以运行期 `query caps` 为准。
> **2026-08-12 能力分层（07 §3.3）**：控制能力按 `menu` 元数据字段分三层——`primary`=人工遥控（按键 12 对象 + 动作区 5 项，进控制台/卡片菜单）/ `secondary`=管理查询（~28 项，进卡片/批量菜单）/ `internal`=AI 底层原语（~40 项，不进任何菜单但 invoke 可用）。成对按下/抬起、key.down/up、consumer.*/hid.*、touch.*（含 touch.tap/swipe/longPress）、stylus.*、screen.* 已标 internal；触控从菜单移除改画布直操。
> **2026-08-12 设置动作项（07 §7）**：Root.plist PSButtonCell 动作注册为无参 invoke 能力——`settings.generateKeys`（生成自签 CA + SSL 证书）/ `settings.searchGateway`（搜索网关自动落盘），menu=secondary 进卡片菜单。

### 4.2 命令通道 v2 ✅ 已完成

**文件**：`src/TRGatewayClient.mm` `_handleServerLine:fd:`

**支持的命令**：

| cmd | 用途 | 参数 | ACK |
|---|---|---|---|
| `ping` | 健康检查 | 无 | `{ok:true}` |
| `query` | 查询能力/配置/状态 | `target`: caps/configs/schema/status | 对应数据 |
| `set` | 下发配置 | `key`, `value` | `{ok, reload}` |
| `invoke` | 调用控制型能力 | `cap`, `params` | `{ok, result}` |
| `restart` | 重启服务 | 无 | `{ok}` |

**ACK 统一格式**：
```json
{ "type": "ack", "cmd": "set", "id": "x3", "ok": true, "reload": "hot" }
```
失败时：
```json
{ "type": "ack", "cmd": "set", "id": "x3", "ok": false, "error": "Scale out of range [0.1,1.0]" }
```

**register 上报扩展**：在原有字段基础上增加 `capMetadata` + `configSchema`（元数据自描述，供前端自动渲染）。

**restartHandler 注入**：`trollvncmanager.mm` 启动 TRGatewayClient 时设置 `restartHandler`，调用 `[gWatchDog restart]`。

**已完成状态**：`_handleQuery` 返回 caps/configs/schema/status；register 上报增加 capMetadata+configSchema。

### 4.3 配置全量上报 ✅ 已完成（v1.8 修正后）

**覆盖范围**：34 个配置项 schema 全注册（含 type/min/max/enum/reload），`currentConfigs` 全量读取。
> v1.6 新增 4 项 watchdog/hid 可写配置（WatchdogThrottleInterval/WatchdogKeepAlive/WatchdogExitTimeout/HIDKeepAliveInterval）。
> v1.8 新增 6 项网关/SSL 配置 + Notifications/PerformanceMode 枚举合并（Phase 8）。

**密码安全**：`FullPassword` / `ViewOnlyPassword` 不上报明文，改为上报 `hasPassword` / `hasViewOnlyPassword` 布尔值。

> **v1.8 修正后状态**：
> - **ReverseMode schema 已删除**（Phase 6 完成）
> - **6 项网关/SSL schema 已补齐**（Phase 8 完成）
> - **Notifications 枚举已合并**（Phase 8 完成，替代 SingleNotifEnabled/ClientNotifsEnabled）
> - **PerformanceMode 枚举已合并**（Phase 8 完成，替代 TileSize/MaxRects/FullscreenThresholdPercent/AsyncSwap 独立配置）

### 4.4 配置热重载 ✅ 已完成

**文件**：`src/trollvncserver.mm`

**目标**：`reload=hot` 的配置项变更后无需重启服务即可生效。

**代码核查发现**：trollvncserver 的配置变量是 **C 静态全局变量**（`gScale`/`gDeferWindowSec`/`gMaxInflightUpdates`/`gOrientationSyncEnabled` 等），初始化时从 NSUserDefaults 读取一次后脱离。热重载不能只改 NSUserDefaults。

**实现方案**：
1. 在 trollvncserver 内暴露 C 函数 `tvReloadConfigForKey(const char *key)`：
   - 重新从 NSUserDefaults 读取对应 key 的值
   - 更新对应的 g* 全局变量
   - 按需触发副作用：
     - `Scale` → 更新 `gScale` + 触发 framebuffer 重建（`rfbScreen->desktopSize`）+ **触发重新 register 上报**（fbw/fbh 变化，AI 工具需感知）
     - `FrameRateSpec` → 解析并更新 `gFpsMin`/`gFpsPref`/`gFpsMax`
     - `OrientationSync` → 更新 `gOrientationSyncEnabled`
     - `ServerCursor` → 更新 `gCursorEnabled`
     - `DeferWindowSec` → 更新 `gDeferWindowSec`
     - `MaxInflight` → 更新 `gMaxInflightUpdates`
     - `NaturalScroll` / `ModifierMap` / `WheelStepPx` / `WheelTuning` → 更新对应输入变量
     - `KeepAliveSec` → 更新 `gKeepAliveSec` + 重启保活定时器
2. **v1.6 新增：Watchdog/HID 配置热重载**（不走 tvReloadConfigForKey，直接设置属性）：
   - `WatchdogThrottleInterval` → `[gWatchDog setThrottleInterval:value]`
   - `WatchdogKeepAlive` → `[gWatchDog setKeepAlive:value]`
   - `WatchdogExitTimeout` → `[gWatchDog setExitTimeOut:value]`
   - `HIDKeepAliveInterval` → `[[STHIDEventGenerator sharedGenerator] setKeepAliveInterval:value]`
3. TRCapabilityRegistry.setConfig 写入 NSUserDefaults 后，按 reload 策略分发：
   - `reload=hot` 且属于 trollvncserver 的 key → 调用 `tvReloadConfigForKey`
   - `reload=hot` 且属于 Watchdog/HID 的 key → 直接设置属性（见第 2 步）
   - `reload=restart` → 触发 watchdog 重启
   - `reload=instant` → 无需特殊处理（下次读取自动用新值）
   - `reload=gateway` → 触发重新 register 上报

### 4.5 本地端口代理 ✅ 已完成

**文件**：`src/TRCapabilityRegistry.mm`（新增 localcmd 路由支持）

**目标**：将 trollvncserver 本地控制端口 46752 的能力暴露给远程调用。

**代码核查修正后的本地命令**（trollvncserver.mm:3863-3958）：
- `count` — 返回客户端数量（数字）
- `list` — 返回客户端列表（TSV 格式）
- `disconnect <clientId|ALL>` — 断开指定客户端（clientId 为 8 位十六进制；支持 `ALL`；`kick` 为别名）
- `block <clientId>` — 断开指定客户端 **并将其 host 加入黑名单**（参数是 clientId 非 host）
- `unblock <host>` — 解除指定 host 的黑名单
- `subscribe on` / `subscribe off` — 订阅/取消推送（保持连接，推送 `"changed\n"` 防抖 150ms）

> **v1.6 核查修正**：`block` 参数实际是 clientId（先断开再加 host 到 `gBlockedHosts` 黑名单），非 host。`blocked.list` 查询能力需在控制端口新增命令。

**暴露方式**：注册为 `localcmd` 路由的能力，经 `invoke` 命令调用（见附录 D.8）。

**实现**：executor block 内创建 socket 连接 127.0.0.1:46752，发送命令，读取响应。

### 4.6 网关 REST API ✅ 已完成

**文件**：`trollvnc-farm/server/index.js`

**单台 API**：

| 方法 | 路径 | 用途 | 请求体 |
|---|---|---|---|
| GET | `/api/devices/:id/caps` | 拉取能力元数据+配置 schema | — |
| GET | `/api/devices/:id/configs` | 拉取当前配置值 | — |
| POST | `/api/devices/:id/configs` | 批量下发配置 | `{key:value,...}` |
| POST | `/api/devices/:id/invoke` | 调用控制型能力 | `{cap, params}` |
| POST | `/api/devices/:id/restart` | 重启设备服务 | — |

> **2026-08-11**：通用 `POST /api/devices/:id/command` **已删除**（H5 全走上方专用端点，见 [00 §8.7](00-当前进度基线.md)）。

**批量 API**：

| 方法 | 路径 | 用途 | 请求体 |
|---|---|---|---|
| POST | `/api/devices/batch/invoke` | 批量调用能力 | `{deviceIds, cap, params}` |
| POST | `/api/devices/batch/configs` | 批量设置配置 | `{deviceIds, configs}` |
| POST | `/api/devices/batch/restart` | 批量重启（前端二次确认） | `{deviceIds}` |

**网关实现**：
- `sendDeviceCmd(deviceId, cmdObj, timeoutMs)` 通用函数
- 批量操作：遍历 deviceIds，经隧道并行转发，聚合 ACK（按 deviceId 分桶成功/失败）
- 网关缓存设备能力清单（register 时上报），**设备配置变更时主动重新 register 推送更新**（实时同步）
- 网关不理解能力语义，纯转发（保持网关轻量）

### 4.7 前端自动渲染 ✅ 已完成

**文件**：`trollvnc-farm/web/caps.js` / `trollvnc-farm/web/app.js`

**caps.js 重写**（元数据驱动）：
- `deviceCaps(device)` — 从 capMetadata 提取控制型能力，回退兜底默认
- `configSchemaByReload(device)` — 按 reload 分区配置 schema
- `invokeCap(apiBase, deviceId, capId, params)` — 调用能力
- `setConfigs(apiBase, deviceId, configs)` — 设置配置
- `batchInvoke/batchSetConfigs/batchRestart` — 批量操作
- `KEY_DEFS` / `ACTION_CAPS` — 按键对象与动作区常量（07 §3.1/§4.1）
- `menuCaps(device, scope)` — 按场景过滤能力（`console`/`tile`/`batch`，07 §3.3/§4）

**app.js 改造**：
- `renderCapOps(container, device)` — 渲染控制台操作菜单（按键区 KEY_DEFS 挂 `attachPress` 按压识别 + 动作区 ACTION_CAPS 单击直执行）
- `attachPress`（press.js）— 按压识别器：click/double/triple/long/down/up，多击窗口 300ms、长按阈值 800ms，由 keyDefs.events 声明式驱动
- `doInvoke(meta)` — 调用能力（无参直接调；有参弹表单）
- `promptParams(paramDefs)` — 参数输入表单（支持 number/string）
- `showConfigPanel()` — 配置面板（按 reload 四区分区渲染表单；入口在卡片 ⋯ 菜单设备管理段）
- `buildConfigInput(schema, val)` — 根据 schema 构建控件（checkbox/select/number/text/password）
- `readConfigValue(inp)` — 读取控件值
- `doOp(op)` — 仅保留本地操作（fit/full/disc）

**UI 组织（2026-08-12 能力与菜单统一，07 §4.1）**：
```
PC 端：右侧图标列（focus-ops，仅图标）
手机端：悬浮按钮 → 点击展开垂直列表（图标+名称）；再次点击收起
（控制台菜单无配置按钮——配置入口统一在卡片 ⋯ 菜单设备管理段 ⚙ 设备配置）

控制台操作菜单（renderCapOps + menuCaps('console')）：
├─ 按键区：KEY_DEFS 12 个按键对象（🏠 Home ⏻ 电源 🔊 音量+ 🔉 音量− 🔇 静音 ☀ 亮度+ 🌙 亮度− ⌨ 键盘 🔍 搜索 📸 Home+Power截屏 🔒 键盘锁/解锁 🙊 释放按键）
│   挂 PressRecognizer 按压识别：Home/电源 支持 双击/三击/长按，音量/亮度/静音 支持 按下/抬起，单击零延迟
└─ 动作区：ACTION_CAPS 5 项（文本输入/粘贴输入/获取剪贴板/设置剪贴板/截屏），单击直执行

触控不进菜单（画布直操）；管理/查询类（service.*/clients.*/sys.*/gateway.*/notify.*/screen.*）进卡片 ⋯ 菜单
（menuCaps('tile') 排除 internal）；批量菜单（menuCaps('batch')）过滤触控/截图/文本/剪贴板（07 §4.3）。
```

> **网关移动端 opsMenu 改造**：从当前 4 列网格（`grid-template-columns: repeat(4, 1fr)`）改为垂直列表样式（与 5801 直连的 `#panel` 一致：每个按钮含图标+名称）。

**配置面板样式**：`.cfg-section` / `.cfg-sec-title` / `.cfg-row`（已加入 style.css），modal-card 支持 max-height 滚动。

### 4.8 AI 工具 WS 接口 ✅ 已完成

**目标**：提供 WebSocket 流式接口，AI 工具/自动化系统可完成"查能力→调能力→查配置→改配置"全流程。

**端点**：`WS /ws/control/:id?token=...`

**交互**：
```
→ 发送：{"cmd":"query","target":"caps"}
← 接收：{"type":"ack","ok":true,"capabilities":[...],"capMetadata":[...]}
→ 发送：{"cmd":"invoke","cap":"touch.tap","params":{"x":0.5,"y":0.5}}
← 接收：{"type":"ack","ok":true}
→ 发送：{"cmd":"set","key":"Scale","value":0.8}
← 接收：{"type":"ack","ok":true,"reload":"hot"}
```

**优势**：AI 工具只需一个 WS 连接即可完成全流程，无需对接 RFB 协议。

---

## 附录 C：触控坐标归一化方案

### C.1 设计目标

| 目标 | 说明 |
|---|---|
| 跨设备复用 | 同一脚本可在不同分辨率设备执行 |
| AI 自动化友好 | AI 工具下发坐标无需感知设备物理像素 |
| 人类可读 | 坐标 0-1 范围（0.5=中央） |
| 精度无损 | 用设备实际分辨率转换 |

### C.2 坐标体系

| 坐标 | 含义 |
|---|---|
| `(0.0, 0.0)` | 屏幕左上角 |
| `(1.0, 1.0)` | 屏幕右下角 |
| `(0.5, 0.5)` | 屏幕正中央 |

**转换公式**（设备侧执行）：
```
nativeX = normalizedX × screenWidth × nativeScale
nativeY = normalizedY × screenHeight × nativeScale
```

### C.3 应用能力

| 能力 ID | 参数 | 坐标类型 |
|---|---|---|
| `touch.tap` | `{x, y}` | 归一化 0-1 |
| `touch.swipe` | `{x1, y1, x2, y2, duration}` | 归一化 0-1 |
| `type.text` | `{text}` | 无坐标 |

### C.4 边界约束

| 场景 | 处理 |
|---|---|
| 坐标超出 0-1 | 设备侧返回错误，不执行 |
| 缺少 x 或 y | 设备侧返回错误 |
| 设备旋转 | 不适用（App 锁定竖屏，无物理旋转场景） |

### C.5 AI/脚本坐标转换完整流程

AI 工具基于截图判定坐标时，需通过 RFB 帧缓冲尺寸（fbw/fbh）转换：

```
1. AI 通过 /api/devices/:id 获取 screen.fbw=585, screen.fbh=1266
2. AI 通过 RFB WS 连接获取截图（585×1266 像素）
3. AI 图像识别判定目标坐标（如 点击按钮 在像素 (292, 633)）
4. 转换为归一化坐标：normalizedX = 292/585 = 0.499, normalizedY = 633/1266 = 0.500
5. 调用 invoke touch.tap {x:0.499, y:0.500}
6. 设备侧 _denormalizePoint：nativeX = 0.499 × 1170 = 583.83, nativeY = 0.500 × 2532 = 1266.0
7. STHIDEventGenerator.tap:{583.83, 1266.0} → IOHID 注入
```

> **关键**：AI 必须用 fbw/fbh（RFB 传输尺寸）转换截图坐标，不能用 width/height（原生像素）。
> 否则当 Scale=0.5 时，AI 截图是 585×1266，但用 1170×2532 转换会导致坐标偏移 2 倍。
> Scale 热重载后 fbw/fbh 会变化，设备主动重新 register 推送新值。

### C.6 调用示例

```bash
# 点击屏幕中央
curl -X POST http://网关:8080/api/devices/<id>/invoke \
  -d '{"cap":"touch.tap","params":{"x":0.5,"y":0.5}}'

# 上滑手势
curl -X POST http://网关:8080/api/devices/<id>/invoke \  -d '{"cap":"touch.swipe","params":{"x1":0.5,"y1":0.8,"x2":0.5,"y2":0.2,"duration":0.3}}'

# 输入文本
curl -X POST http://网关:8080/api/devices/<id>/invoke \
  -d '{"cap":"type.text","params":{"text":"Hello World"}}'
```

---

## 附录 D：控制型能力完整清单（v1.6 原生能力全覆盖）

> 每个能力的完整定义（ID/标题/图标/路由/参数/执行器/状态）
> **v1.5 更新**：基于 STHIDEventGenerator.h / ClipboardManager.h / ScreenCapturer.h / BulletinManager.h / TRWatchDog.h 完整核查，补充 30+ 个原生已有但未暴露的能力；修正 type.text 重复造轮子问题。
> **v1.6 更新（已全部修复）**：深度核查 .mm 实现层，发现 5 处重复造轮子/空壳（clipboard.paste 语义混乱/screenshot 空壳/restart 双重入口/shake 空实现/本地命令通道脱节），Batch 0-3 已全部修复；新增 20 个原生覆盖盲点（数组版多指/taps/reset/event/系统查询/ScreenCapturer 扩展/网关客户端）已全部注册；附录 E 新增 4 项 watchdog/hid 可写配置已全部注册。

### D.1 HID 硬件按键能力（route.type = hid）

#### D.1.1 单击类（已实现）

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `home` | Home 键 | 🏠 | 无 | `[hid menuPress]` | ✅ 已实现 |
| `power` | 电源 | ⏻ | 无 | `[hid powerPress]` | ✅ 已实现 |
| `volup` | 音量 + | 🔊 | 无 | `[hid volumeIncrementPress]` | ✅ 已实现 |
| `voldn` | 音量 − | 🔉 | 无 | `[hid volumeDecrementPress]` | ✅ 已实现 |
| `mute` | 静音 | 🔇 | 无 | `[hid mutePress]` | ✅ 已实现 |
| `briup` | 亮度 + | ☀️ | 无 | `[hid displayBrightnessIncrementPress]` | ✅ 已实现 |
| `bridn` | 亮度 − | 🌙 | 无 | `[hid displayBrightnessDecrementPress]` | ✅ 已实现 |
| `keyboard` | 切换屏幕键盘 | ⌨️ | 无 | `[hid toggleOnScreenKeyboard]` | ✅ 已实现 |
| `spotlight` | 切换搜索 | 🔍 | 无 | `[hid toggleSpotlight]` | ✅ 已注册 |

#### D.1.2 多击/长按类（原生已有，已注册）

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `home.double` | 双击 Home（多任务） | 🏠×2 | 无 | `[hid menuDoublePress]` | ✅ 已注册 |
| `home.long` | 长按 Home（Siri） | 🏠⌛ | 无 | `[hid menuLongPress]` | ✅ 已注册 |
| `power.double` | 双击电源（系统截屏存相册） | ⏻×2 | 无 | `[hid powerDoublePress]` | ✅ 已注册 |
| `power.triple` | 三击电源 | ⏻×3 | 无 | `[hid powerTriplePress]` | ✅ 已注册 |
| `power.long` | 长按电源（关机界面） | ⏻⌛ | 无 | `[hid powerLongPress]` | ✅ 已注册 |
| `snapshot` | Home+Power 截屏 | 📸 | 无 | `[hid snapshotPress]` | ✅ 已注册 |
| `shake` | 摇晃设备 | 📳 | 无 | `[hid shakeIt]` | ❌ 已移除（方法体为空，Batch 0 从清单移除） |

#### D.1.3 按键底层控制（Down/Up 分离，支持长按/组合）

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `home.down` | Home 按下 | 无 | `[hid menuDown]` | ✅ 已注册 |
| `home.up` | Home 抬起 | 无 | `[hid menuUp]` | ✅ 已注册 |
| `power.down` | 电源按下 | 无 | `[hid powerDown]` | ✅ 已注册 |
| `power.up` | 电源抬起 | 无 | `[hid powerUp]` | ✅ 已注册 |
| `volup.down` | 音量+按下 | 无 | `[hid volumeIncrementDown]` | ✅ 已注册 |
| `volup.up` | 音量+抬起 | 无 | `[hid volumeIncrementUp]` | ✅ 已注册 |
| `voldn.down` | 音量−按下 | 无 | `[hid volumeDecrementDown]` | ✅ 已注册 |
| `voldn.up` | 音量−抬起 | 无 | `[hid volumeDecrementUp]` | ✅ 已注册 |
| `mute.down` | 静音按下 | 无 | `[hid muteDown]` | ✅ 已注册 |
| `mute.up` | 静音抬起 | 无 | `[hid muteUp]` | ✅ 已注册 |
| `briup.down` | 亮度+按下 | 无 | `[hid displayBrightnessIncrementDown]` | ✅ 已注册 |
| `briup.up` | 亮度+抬起 | 无 | `[hid displayBrightnessIncrementUp]` | ✅ 已注册 |
| `bridn.down` | 亮度−按下 | 无 | `[hid displayBrightnessDecrementDown]` | ✅ 已注册 |
| `bridn.up` | 亮度−抬起 | 无 | `[hid displayBrightnessDecrementUp]` | ✅ 已注册 |
| `hwlock` | 硬件键盘锁 | 无 | `[hid hardwareLock]` | ✅ 已注册 |
| `hwunlock` | 硬件键盘解锁 | 无 | `[hid hardwareUnlock]` | ✅ 已注册 |
| `releasekeys` | 释放所有按键 | 无 | `[hid releaseEveryKeys]` | ✅ 已注册 |

#### D.1.4 通用 HID Consumer 用法（高级，支持任意媒体键/系统控制键）

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `consumer.press` | Consumer 用法按下 | `{usage:int}` | `[hid otherConsumerUsagePress:usage]` | ✅ 已注册 |
| `consumer.down` | Consumer 按下 | `{usage:int}` | `[hid otherConsumerUsageDown:usage]` | ✅ 已注册 |
| `consumer.up` | Consumer 抬起 | `{usage:int}` | `[hid otherConsumerUsageUp:usage]` | ✅ 已注册 |
| `hid.press` | 任意 HID 页+用法按下 | `{page:int, usage:int}` | `[hid otherPage:page usagePress:usage]` | ✅ 已注册 |
| `hid.down` | 任意 HID 页+用法按下 | `{page:int, usage:int}` | `[hid otherPage:page usageDown:usage]` | ✅ 已注册 |
| `hid.up` | 任意 HID 页+用法抬起 | `{page:int, usage:int}` | `[hid otherPage:page usageUp:usage]` | ✅ 已注册 |

### D.2 触控类能力（route.type = touch，归一化坐标）

#### D.2.1 已实现

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `touch.tap` | 点击 | 👆 | `{x:0-1, y:0-1}` | 归一化转像素 + `[hid tap:pt]` | ✅ 已实现 |
| `touch.swipe` | 线性滑动 | ↔ | `{x1,y1,x2,y2,duration?}` | 归一化转像素 + `[hid dragLinearWithStartPoint:endPoint:duration:]` | ✅ 已实现 |

#### D.2.2 原生已有，已注册

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `touch.doubleTap` | 双击 | 👆×2 | `{x,y}` | `[hid doubleTap:pt]` | ✅ 已注册 |
| `touch.twoFingerTap` | 双指点击 | ✌️ | `{x,y}` | `[hid twoFingerTap:pt]` | ✅ 已注册 |
| `touch.threeFingerTap` | 三指点击 | 🖐️ | `{x,y}` | `[hid threeFingerTap:pt]` | ✅ 已注册 |
| `touch.longPress` | 长按 | 👆⌛ | `{x,y}` | `[hid longPress:pt]` | ✅ 已注册 |
| `touch.curveSwipe` | 曲线滑动 | 〰 | `{x1,y1,x2,y2,duration?}` | `[hid dragCurveWithStartPoint:endPoint:duration:]` | ✅ 已注册 |
| `touch.pinch` | 捏合缩放 | 🤏 | `{bounds:{x,y,w,h}, scale:float, angle:float, duration:float}` | `[hid pinchLinearInBounds:bounds scale:angle:duration:]` | ✅ 已注册 |
| `touch.down` | 触摸按下 | 👇 | `{x,y}` | `[hid touchDown:pt]` | ✅ 已注册 |
| `touch.up` | 触摸抬起 | 👆 | `{x,y}` | `[hid liftUp:pt]` | ✅ 已注册 |
| `touch.downMulti` | 多指同点按下 | 👥 | `{x,y,count:int}` | `[hid touchDown:pt touchCount:count]`（所有手指落同点） | ✅ 已注册 |
| `touch.upMulti` | 多指同点抬起 | 👥 | `{x,y,count:int}` | `[hid liftUp:pt touchCount:count]` | ✅ 已注册 |
| `touch.downMultiAt` | 多指异点按下 | 👥 | `{points:[{x,y},...],count:int}` | `[hid touchDownAtPoints:ptsArray touchCount:count]`（**.mm 已实现未在 .h 暴露**） | ✅ 已注册 |
| `touch.upMultiAt` | 多指异点抬起 | 👥 | `{points:[{x,y},...],count:int}` | `[hid liftUpAtPoints:ptsArray touchCount:count]`（**.mm 已实现未在 .h 暴露**） | ✅ 已注册 |
| `touch.taps` | 通用 N 击 M 指 | 👆✖️N | `{x,y,tapCount:int,touchCount:int,delay:float}` | `[hid sendTaps:tapCount location:pt numberOfTouches:touchCount delayBetweenTaps:delay]`（tap/doubleTap/twoFingerTap/threeFingerTap 均为其特例） | ✅ 已注册 |
| `touch.reset` | 重置触摸状态 | 🔄 | 无 | `[hid dispatchHandResetEvent]`（触摸卡死恢复） | ✅ 已注册 |
| `touch.event` | 单事件派发 | 🎞️ | `{eventInfo:object}` | `[hid dispatchEventWithInfo:eventInfo]`（eventStream 单发版，低延迟） | ✅ 已注册 |
| `touch.eventStream` | 自定义事件流 | 🎞️ | `{eventInfo:object}` | `[hid sendEventStream:eventInfo]` | ✅ 已注册 |

> `touch.eventStream` 是最强大的触控能力，支持多触点序列、插值动画、时间步进等，可模拟任意复杂手势。
> `touch.taps` 是 tap/doubleTap/twoFingerTap/threeFingerTap 的通用版，执行器应统一调用避免重复造轮子。
> **v1.6 核查**：`touchDownAtPoints:`/`liftUpAtPoints:` 在 .mm 已实现但未在 .h 声明，需补公开声明后注册。

### D.3 触控笔能力（route.type = stylus，归一化坐标）

> 原生 STHIDEventGenerator 已完整实现触控笔支持，已全部注册。

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `stylus.tap` | 触控笔点击 | ✏️ | `{x,y,azimuth:0-2π,altitude:0-π/2,pressure:0-1}` | 归一化转像素 + `[hid stylusTapAtPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ 已注册 |
| `stylus.down` | 触控笔按下 | ✏️⬇ | `{x,y,azimuth,altitude,pressure}` | `[hid stylusDownAtPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ 已注册 |
| `stylus.move` | 触控笔移动 | ✏️➡ | `{x,y,azimuth,altitude,pressure}` | `[hid stylusMoveToPoint:pt azimuthAngle:az altitudeAngle:alt pressure:p]` | ✅ 已注册 |
| `stylus.up` | 触控笔抬起 | ✏️⬆ | `{x,y}` | `[hid stylusUpAtPoint:pt]` | ✅ 已注册 |

### D.4 键盘能力（route.type = hid）

#### D.4.1 ASCII 按键（已实现/待优化）

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `key.press` | 按键 | `{char:string(1)}` | `[hid keyPress:char]`（同步 0.05s，仅 ASCII） | ✅ 已实现（type.text 内部用） |
| `key.down` | 按键按下 | `{char:string(1)}` | `[hid keyDown:char]` | ✅ 已注册 |
| `key.up` | 按键抬起 | `{char:string(1)}` | `[hid keyUp:char]` | ✅ 已注册 |

#### D.4.2 文本输入（修正：避免重复造轮子）

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `type.text` | 文本输入（ASCII） | `{text:string}` | 逐字符 `[hid keyPress:]`（仅 ASCII，每字符 0.05s） | ✅ 已实现 |
| `type.paste` | 粘贴输入（任意文本） | `{text:string}` | `[ClipboardManager setStringFromRemote:text]` + 模拟粘贴 | ✅ 已实现 |

> **v1.5 修正**：原 `type.text` 用 keyPress 逐字符只支持 ASCII 且慢。新增 `type.paste` 利用 ClipboardManager 实现任意文本（中文/emoji）快速输入：设置剪贴板 → 模拟 Ctrl+V 或长按粘贴菜单。`type.text` 保留用于需要逐字符输入的场景。

### D.5 剪贴板能力（route.type = native）

> **v1.6 修正**：原 `clipboard.paste` 实现是「读 currentString → 再 setStringFromRemote 写回剪贴板」的空操作（读+写回同一内容），既非真正粘贴到输入框，也不返回文本给调用方（仅返回 length）。与 `clipboard.get`（应返回文本）语义重叠且边界模糊。**合并为 `clipboard.get`**，移除 `clipboard.paste`。

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `clipboard.get` | 获取剪贴板 | 📋 | 无 | `[ClipboardManager currentString]` 返回 `{text:string}` | ✅ 已合并（原 paste） |
| `clipboard.set` | 设置剪贴板 | 📋 | `{text:string}` | `[ClipboardManager setStringFromRemote:text]` | ✅ 已实现 |

> `type.paste`（文本输入）内部复用 `clipboard.set` 设置剪贴板后模拟粘贴键，避免重复造轮子。

### D.6 截屏能力（route.type = native）

> **v1.6 核查**：原 `screenshot` 注册为空壳（executor 返回 `code:99 "待实现"`），未调用 ScreenCapturer。Batch 0 已修复：ScreenCapturer 新增 `captureSingleFrameImage` 方法，executor 调用返回 JPEG base64。

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `screenshot` | 静默截图 | 📷 | 无 | ScreenCapturer 单帧 → UIImage → JPEG/PNG base64（不触发系统截图动画，不存相册） | ✅ 已修复 |
| `screenshot.system` | 系统截屏 | 📸 | 无 | `[hid snapshotPress]` 或 `[hid powerDoublePress]`（触发系统截图动画，存入相册） | ✅ 已注册 |

> `screenshot` 与 `screenshot.system` 互补：AI 自动化用静默截图；用户需要保存截图用系统截屏。

### D.7 通知能力（route.type = native）

> 原生 BulletinManager 已完整实现，已全部注册。

| ID | 标题 | 图标 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|---|
| `notify.banner` | 推送横幅 | 🔔 | `{content:string, userInfo?:object}` | `[BulletinManager popBannerWithContent:content userInfo:userInfo]` | ✅ 已注册 |
| `notify.banner.update` | 更新横幅 | 🔔 | `{content:string, badgeCount:int, userInfo?:object}` | `[BulletinManager updateSingleBannerWithContent:content badgeCount:badgeCount userInfo:userInfo]` | ✅ 已注册 |
| `notify.revoke` | 撤销通知 | 🔕 | 无 | `[BulletinManager revokeSingleNotification]` | ✅ 已注册 |
| `notify.revokeAll` | 撤销全部通知 | 🔕 | 无 | `[BulletinManager revokeAllNotifications]` | ✅ 已注册 |

### D.8 本地命令能力（route.type = localcmd，经 46752 端口）

> 代码核查修正：`block`/`unblock` 参数为 **host**（非 clientId）；`disconnect` 支持 `ALL`；含 `subscribe on/off` 推送能力。
> **v1.6 核查**：实际控制端口还实现了 `kick`（disconnect 别名）；`block <cid>` 先断开再加黑名单；补充黑名单查询能力。

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `clients.count` | 客户端数量 | 无 | socket → `count` | ✅ 已注册 |
| `clients.list` | 客户端列表 | 无 | socket → `list` | ✅ 已注册 |
| `clients.disconnect` | 断开客户端 | `{clientId}` 或 `{clientId:"ALL"}` | socket → `disconnect <id>` / `disconnect ALL` / `kick <id>` | ✅ 已注册 |
| `clients.block` | 阻止客户端并加黑名单 | `{clientId}` | socket → `block <cid>`（断开 + 加黑名单） | ✅ 已注册 |
| `clients.unblock` | 解除主机黑名单 | `{host}` | socket → `unblock <host>` | ✅ 已注册 |
| `clients.blocked.list` | 黑名单列表 | 无 | 读取 `gBlockedHosts`（控制端口已扩展 `blocked.list` 命令） | ✅ 已实现 |
| `clients.subscribe` | 订阅推送 | `{enable:bool}` | socket → `subscribe on` / `subscribe off`（推送 `"changed\n"` 防抖 150ms） | ✅ 已注册 |

### D.9 服务控制能力（route.type = watchdog）

> **v1.6 核查（已修复）**：`restart` 已统一注册为 `service.restart`，使 `invoke` 与网关 cmd 双入口均可调用。`stop` 不保留（设备离线无法远程恢复）。

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `service.restart` | 重启服务 | 无 | `[gWatchDog restart]`（统一注册，网关 cmd 与 invoke 双入口） | ✅ 已注册 |
| `service.signal` | 发送信号 | `{signal:int}` | `[gWatchDog sendSignal:signal]` | ✅ 已注册 |

### D.10 服务状态查询能力（route.type = native）

> 原生 TRWatchDog 已有丰富的状态查询属性，已全部注册为 query 能力。
> **v1.6 补充**：TRWatchDog 还有 `throttleInterval`/`keepAlive`/`exitTimeOut` 可写属性，注册为配置型能力。

| ID | 标题 | 返回值 | 执行器 | 状态 |
|---|---|---|---|---|
| `service.state` | 服务状态 | `{state:stopped/starting/running/stopping/crashed/throttled}` | `gWatchDog.state` | ✅ 已注册 |
| `service.info` | 服务详情 | `{pid,restartCount,uptime,lastExitTime,lastExitStatus,lastUncaughtSignal,lastTerminationReason,timeUntilNextRestart}` | `gWatchDog` 9 个 readonly 属性聚合 | ✅ 已注册 |
| `service.isActive` | 是否活跃 | bool | `gWatchDog.isActive` | ✅ 已注册 |
| `service.isThrottled` | 是否限流 | bool | `gWatchDog.isThrottled` | ✅ 已注册 |
| `service.validate` | 校验配置 | `{ok:bool,error?:string}` | `[gWatchDog validateConfigurationWithError:]` | ✅ 已注册 |

> **不保留 stop**：停止服务后设备离线无法远程恢复，风险高。仅保留 restart。

### D.11 系统查询能力（route.type = native，新增）

> **v1.6 新增**：trollvncserver 内部已有大量状态数据（`tvConfigSnapshot()` 函数已汇总 30+ 参数、`gInflight` 编码帧数、`gWidth/gHeight` 分辨率、`gRotationQuad` 旋转等），可聚合暴露为查询能力。

| ID | 标题 | 返回值 | 执行器 | 状态 |
|---|---|---|---|---|
| `sys.version` | 版本信息 | `{scheme,version}` | `sPackageScheme`/`sPackageVersion` | ✅ 已注册 |
| `sys.configSnapshot` | 配置快照 | `{port,httpPort,scale,fps,...}` 30+ 参数 | `tvConfigSnapshot()` 函数（已现成） | ✅ 已注册 |
| `sys.resolution` | 屏幕分辨率 | `{width,height,nativeScale}` | `gWidth`/`gHeight`/`UIScreen.nativeScale` | ✅ 已注册 |
| `sys.rotation` | 当前旋转 | `{quad:0-3}` 对应 0°/90°/180°/270° | `gRotationQuad` 原子变量 | ✅ 已注册 |
| `sys.stats.inflight` | 编码帧数 | `{current,max,isThrottled}` | `gInflight`/`gMaxInflightUpdates` | ✅ 已注册 |
| `sys.bonjour.txt` | Bonjour TXT | `{vn,vo,hp,fps}` | `bonjourTXTRecord()` 函数 | ✅ 已注册 |

### D.12 ScreenCapturer 扩展能力（route.type = native，新增）

> **v1.6 新增**：ScreenCapturer 除单帧截图外，还有流式采集控制、帧率配置、强制刷新等原生能力未暴露。

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `screen.capture.start` | 开始流式采集 | 无 | `[ScreenCapturer startCaptureWithFrameHandler:]` | ✅ 已注册 |
| `screen.capture.stop` | 停止流式采集 | 无 | `[ScreenCapturer endCapture]` | ✅ 已注册 |
| `screen.fps` | 设置帧率范围 | `{min,preferred,max}` | `[ScreenCapturer setPreferredFrameRateWithMin:preferred:max:]` | ✅ 已注册 |
| `screen.resolution` | 查询分辨率 | 无 | `ScreenCapturer.renderProperties`（含 width/height/pixelFormat） | ✅ 已注册 |
| `screen.forceRefresh` | 强制下一帧脏 | 无 | `[ScreenCapturer forceNextFrameUpdate]` | ✅ 已注册 |

### D.13 网关客户端能力（route.type = native，新增）

> **v1.6 新增**：TRGatewayClient 内部有连接状态、重连、心跳间隔、设备元数据等可暴露能力，当前全私有化。

| ID | 标题 | 参数 | 执行器 | 状态 |
|---|---|---|---|---|
| `gateway.isConnected` | 网关连接状态 | 无 | 返回 `{connected:bool,retryDelay:sec}` | ✅ 已注册 |
| `gateway.reconnect` | 手动重连 | 无 | `[TRGatewayClient stop]; [TRGatewayClient start]` | ✅ 已注册 |
| `gateway.deviceInfo` | 设备元数据 | 无 | 返回 `{deviceId,deviceName,vncPort,httpPort,screen}` | ✅ 已注册 |

### D.14 能力数量汇总

> 以下为 v1.7 最终完成状态（Batch 0-3 全部完成后）。完整追踪见 [能力追踪清单.md](能力追踪清单.md)。

| 类别 | ✅已注册 | 🆕待注册 | 🐛/🐛空/🔁 | 🆕待实现 | 合计 |
|---|---|---|---|---|---|
| HID 单击 | 9 | 0 | 0 | 0 | 9 |
| HID 多击/长按 | 6 | 0 | 0 | 0 | 6 |
| HID 底层 Down/Up | 17 | 0 | 0 | 0 | 17 |
| HID 通用 Consumer | 6 | 0 | 0 | 0 | 6 |
| 触控 | 14 | 0 | 0 | 0 | 14 |
| 触控（P4补声明） | 4 | 0 | 0 | 0 | 4 |
| 触控笔 | 4 | 0 | 0 | 0 | 4 |
| 键盘 | 3 | 0 | 0 | 0 | 3 |
| 文本输入 | 2 | 0 | 0 | 0 | 2 |
| 剪贴板 | 2 | 0 | 0 | 0 | 2 |
| 截屏 | 2 | 0 | 0 | 0 | 2 |
| 通知 | 4 | 0 | 0 | 0 | 4 |
| 本地命令 | 7 | 0 | 0 | 0 | 7 |
| 服务控制 | 2 | 0 | 0 | 0 | 2 |
| 服务状态 | 5 | 0 | 0 | 0 | 5 |
| 系统查询 | 6 | 0 | 0 | 0 | 6 |
| ScreenCapturer 扩展 | 5 | 0 | 0 | 0 | 5 |
| 网关客户端 | 3 | 0 | 0 | 0 | 3 |
| **合计** | **97** | **0** | **0** | **0** | **97** |

> **v1.7 最终结论**：
> - 97 项控制能力全部注册完成（Batch 0 修复空壳/BUG、Batch 1 即用即注册 59 项、Batch 2 本地命令桥接、Batch 3 新实现 type.paste/blocked.list）。
> - shake 因方法体为空已从清单移除（不保留空壳能力）。
> - 配置型能力 38 项全部注册完成（含 4 项 watchdog/hid 可写配置）。

---

## 附录 E：配置型能力完整清单（v1.4 治理后）

> 每个配置项的完整 schema（key/标题/类型/范围/enum/reload/默认值/说明）
> **v1.4 治理**：合并通知开关为枚举、合并性能参数为 PerformanceMode、统一网络组；移除反向模式相关配置；每项补充详细说明。

### E.1 状态

| key | 标题 | 类型 | 默认 | reload | 说明 |
|---|---|---|---|---|---|
| `Enabled` | 服务开关 | bool | true | restart | 启用/禁用 VNC 服务 |

### E.2 网关（注册参数，改后重连）

| key | 标题 | 类型 | 默认 | reload | 说明 |
|---|---|---|---|---|---|
| `GatewayHost` | 网关地址 | string | "" | gateway | 网关 IP 或域名 |
| `GatewayPort` | 网关注册端口 | number | 18081 | gateway | 设备注册到网关的端口 |
| `GatewayToken` | 网关令牌 | password | "" | gateway | FARM_TOKEN，内网可留空 |

> 这三项改动后需重新连接网关（触发重新 register）。
> **v1.6 术语统一**：reload 策略从 `reconnect` 统一为 `gateway`（与 Phase 4.1 配置生效分级一致）。

### E.3 网络

| key | 标题 | 类型 | min | max | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|---|
| `Port` | VNC TCP 端口 | number | 1 | 65535 | 5901 | restart | VNC 服务监听端口（后端入口） |
| `BindHost` | 绑定地址 | string | — | — | "" | restart | 空=all interfaces；指定 IP 可限制访问来源 |
| `HttpPort` | HTTP 端口 | number | 0 | 65535 | 0 | restart | 前端入口端口；0=关闭 web 服务（5801 时启用） |
| `HttpDir` | HTTP 根目录 | string | — | — | "" | restart | web 静态文件目录；空=内置默认页 |
| `KeepAliveSec` | TCP 保活间隔 | number | 0 | 300 | 0 | hot | 秒；0=禁用保活。NAT 环境建议 60s 防断连 |
| `BonjourEnabled` | 自动发现 | bool | — | — | true | gateway | mDNS 发布 `_rfb._tcp`，局域网客户端可自动发现 |

### E.4 安全

| key | 标题 | 类型 | 默认 | reload | 说明 |
|---|---|---|---|---|---|
| `FullPassword` | 完全访问密码 | password | "" | restart | 经典 VNC 认证只认前 8 位；空=无认证 |
| `ViewOnlyPassword` | 只读密码 | password | "" | restart | 只读访问密码；空=不启用只读密码 |
| `ViewOnly` | 全局只读 | bool | false | instant | true=所有客户端只读，优先生效于只读密码 |
| `ClipboardEnabled` | 剪贴板同步 | bool | true | instant | false=禁用双向剪贴板同步 |

### E.5 画面与性能

| key | 标题 | 类型 | min | max | step | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|---|---|
| `Scale` | 输出缩放 | number | 0.1 | 1.0 | 0.1 | 1.0 | hot | 画面等比缩放。1.0=原生分辨率；0.5=半分辨率（省带宽，画质降低）。影响 RFB 传输尺寸（fbw/fbh） |
| `FrameRateSpec` | 帧率 | string | — | — | — | "60" | hot | 帧率规格。如 `60`（固定60fps）、`30-60`（动态30-60fps）、`30:60:120`（三档自适应） |
| `OrientationSync` | 方向同步 | bool | — | — | — | true | hot | 屏幕方向跟随设备物理方向（仅影响画面采集方向） |
| `ServerCursor` | 服务端光标 | bool | — | — | — | false | hot | 显示服务端光标位置；false=仅客户端光标 |

### E.6 性能模式（进阶折叠）

> 合并后的枚举控件。选 `custom` 时展开 4 个细调参数，否则收起。

| key | 标题 | 类型 | enum | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|
| `PerformanceMode` | 性能模式 | enum | balanced/quality/performance/custom | balanced | hot | balanced=平衡（默认）；quality=高画质；performance=低延迟；custom=自定义展开下方 4 项 |

**custom 展开项**：

| key | 标题 | 类型 | min | max | step | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|---|---|
| `DeferWindowSec` | 延迟窗口 | number | 0 | 0.5 | 0.005 | 0.015 | hot | 秒。脏区延迟发送：0=立即；0.015=延迟15ms合并脏区（省带宽，延迟略增） |
| `MaxInflight` | 最大并行帧 | number | 0 | 8 | 1 | 2 | hot | 并行帧数：2=平衡；8=最高吞吐但可能卡顿；0=不限（不推荐） |
| `TileSize` | 分块大小 | number | 8 | 128 | 1 | 32 | restart | 像素。脏区检测瓦片尺寸：小=精细但开销大；大=省开销但精度低 |
| `FullscreenThresholdPercent` | 脏区阈值 | number | 0 | 100 | 1 | 0 | hot | 百分比。变化瓦片超过此比例时发全屏帧：0=关闭（始终增量）；100=几乎全屏 |
| `MaxRects` | 最大矩形数 | number | 1 | 4096 | 1 | 256 | restart | 单帧最大脏矩形数。超限后退化为包围盒或全屏：小=省带宽但可能丢帧；大=精准但开销大 |
| `AsyncSwap` | 非阻塞交换 | bool | — | — | — | false | restart | 异步帧交换。true=非阻塞（可能撕裂）；false=阻塞（稳定） |

**PerformanceMode 预设值映射**：

| 模式 | DeferWindowSec | MaxInflight | TileSize | FullscreenThresholdPercent | MaxRects | AsyncSwap |
|---|---|---|---|---|---|---|
| balanced | 0.015 | 2 | 32 | 0 | 256 | false |
| quality | 0.005 | 4 | 16 | 0 | 512 | false |
| performance | 0.05 | 1 | 64 | 30 | 128 | false |
| custom | 用户自定义 | 用户自定义 | 用户自定义 | 用户自定义 | 用户自定义 | 用户自定义 |

### E.7 输入

| key | 标题 | 类型 | min | max | step | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|---|---|
| `NaturalScroll` | 自然滚动 | bool | — | — | — | true | instant | true=触控板式自然方向；false=传统方向 |
| `ModifierMap` | 修饰键映射 | enum | std/altcmd | — | — | "std" | hot | std=标准（Alt=Option，Super=Command）；altcmd=Alt 映射为 Command（macOS 客户端建议） |
| `AutoAssistEnabled` | 辅助触控自动激活 | bool | — | — | — | false | instant | true=连接时自动开启 iOS 辅助触控（小白点） |
| `WheelStepPx` | 滚轮步进 | number | 0 | 1000 | 1 | 48 | hot | 像素/滚轮刻度。0=禁用滚轮；48=默认；调大=滚动更快 |
| `WheelTuning` | 滚轮调优 | string | — | — | — | "" | hot | 高级滚轮参数（格式 `step=48,coalesce=0.03,max=8,clamp=2,amp=1,cap=8,minratio=0.1,durbase=0.1,durk=0.01,durmin=0.05,durmax=0.5`） |

### E.8 通知（合并后）

> 原 `SingleNotifEnabled` + `ClientNotifsEnabled` 合并为枚举。

| key | 标题 | 类型 | enum | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|
| `Notifications` | 通知模式 | enum | all/connectOnly/silent | all | instant | all=全部通知（单条+连接断开）；connectOnly=仅连接/断开通知；silent=静默 |

**兼容映射**：

| Notifications | 原 SingleNotifEnabled | 原 ClientNotifsEnabled |
|---|---|---|
| all | true | true |
| connectOnly | false | true |
| silent | false | false |

> 设备侧读取时：若 NSUserDefaults 有旧 key（SingleNotifEnabled/ClientNotifsEnabled），按映射转为 Notifications 值；新写入统一用 Notifications。

### E.9 高级

| key | 标题 | 类型 | min | max | step | 默认 | reload | 说明 |
|---|---|---|---|---|---|---|---|---|
| `KeyLogging` | 键盘事件日志 | bool | — | — | — | false | instant | 调试用。true=记录所有键盘事件到日志 |
| `SslCertFile` | SSL 证书文件 | string | — | — | — | "" | restart | 路径。内网自用默认不启用 TLS |
| `SslKeyFile` | SSL 私钥文件 | string | — | — | — | "" | restart | 路径。必须与证书成对配置 |
| `WatchdogThrottleInterval` | 重启节流间隔 | number | 1 | 300 | 1 | 10 | hot | 秒。两次重启间最小间隔，防止崩溃循环。对应 `gWatchDog.throttleInterval` |
| `WatchdogKeepAlive` | 崩溃自动重启 | bool | — | — | — | true | hot | true=进程退出后自动重启；false=退出后不重启。对应 `gWatchDog.keepAlive` |
| `WatchdogExitTimeout` | 退出超时 | number | 1 | 60 | 1 | 5 | hot | 秒。stop 后等待进程退出的超时，超时强杀。对应 `gWatchDog.exitTimeOut` |
| `HIDKeepAliveInterval` | HID 防休眠间隔 | number | 0 | 300 | 1 | 30 | hot | 秒。0=禁用；30=每 30s 自动 hardwareUnlock 防止设备休眠断连。对应 `STHIDEventGenerator.keepAliveInterval` |

> SSL 证书/私钥必须成对配置，缺一不启用 TLS。内网自用基线不启用。
> **v1.6 新增**：Watchdog 三项可写属性 + HID 防休眠间隔，均为 TRWatchDog/STHIDEventGenerator 已有原生属性，注册为热重载配置即可生效。

### E.10 已废弃配置（不在设置页展示，代码保留兼容）✅ v1.8 已清理

> **v1.8 反推发现并修正**：本表自 v1.3 起多次声称"代码彻底删除"，但实际全部残留。Phase 6/8 已真正清理。

| key | 原归类 | 废弃原因 | v1.8 处理结果 |
|---|---|---|---|
| `ReverseMode` | 连接 | 反向模式废弃（v1.3） | ✅ Phase 6 已删除：trollvncserver.mm 全局变量/配置解析/CLI/事件线程；TRCapabilityRegistry.mm schema 注册；TVNCConnectViewController.m UI；TVNCRootListController.m 本地化；.strings 字符串 |
| `ReverseSocket` | 连接 | 反向模式废弃 | ✅ Phase 6 已删除 |
| `ReverseRepeaterID` | 连接 | 反向模式废弃 | ✅ Phase 6 已删除 |
| `SingleNotifEnabled` | 高级 | 合并为 Notifications | ✅ Phase 8 已合并：schema 改为 Notifications 枚举，setConfig 映射到底层 key，currentConfigs 反向映射上报 |
| `ClientNotifsEnabled` | 高级 | 合并为 Notifications | ✅ Phase 8 已合并 |
| `OrientationPadFix` | 画面与性能 | 仅 iPad 非标准方向用，iPhone 不需要 | ✅ 设置页移除，代码保留读取（兼容旧配置） |

---

## 附录 F：API 端点完整清单

### F.1 单台 API

| 方法 | 路径 | 请求体 | 响应 |
|---|---|---|---|
| GET | `/api/devices` | — | 设备列表（含全字段） |
| GET | `/api/devices/:id` | — | 单设备详情 |
| GET | `/api/devices/:id/caps` | — | `{capabilities, capMetadata, configSchema, configs}` |
| GET | `/api/devices/:id/configs` | — | `{deviceId, configs}` |
| POST | `/api/devices/:id/configs` | `{key:value,...}` | `{deviceId, results:{key:{ok,reload,error}}}` |
| POST | `/api/devices/:id/invoke` | `{cap, params, timeout?}` | `{ok, cap, deviceId, ack}` |
| POST | `/api/devices/:id/restart` | — | `{ok, deviceId, ack}` |
| POST | `/api/devices/:id/ping` | — | `{id, online}` |

> **2026-08-11**：通用 `POST /api/devices/:id/command` 已删除（上表其余为当前实际端点）。

### F.2 批量 API

| 方法 | 路径 | 请求体 | 响应 |
|---|---|---|---|
| POST | `/api/devices/batch/invoke` | `{deviceIds, cap, params, timeout?}` | `{cap, results:[{deviceId,ok,error}]}` |
| POST | `/api/devices/batch/configs` | `{deviceIds, configs}` | `{results:[{deviceId, results:{key:{ok,reload}}}]}` |
| POST | `/api/devices/batch/restart` | `{deviceIds}` | `{results:[{deviceId,ok,error}]}` |

### F.3 WebSocket API

| 端点 | 用途 |
|---|---|
| `WS /ws/vnc/:id` | 画面 RFB 桥接（已有） |
| `WS /ws/control/:id` | 能力控制流式接口（4.8 已完成） |

---

## 附录 G：新增能力开发规范

### G.1 新增控制型能力

只需在 `TRCapabilityRegistry.mm` 的 `_registerAllCapabilities` 中加一行：

```objc
[self _registerControl:@"myCap" title:@"我的能力" icon:@"✨" route:TRCapRouteHID params:@[]
    executor:^NSDictionary *(NSDictionary *p, NSError **e) {
        // 执行逻辑
        return @{@"ok":@YES};
    }];
```

**自动生效**：
- 注册表自动上报到 capabilities + capMetadata
- 网关自动转发 invoke 命令
- 前端自动渲染按钮
- API 自动暴露

### G.2 新增配置型能力

只需在 `_registerConfigSchemas` 中加一行：

```objc
[self _registerConfig:@"MyConfig" title:@"我的配置" type:@"number"
    min:@0 max:@100 step:@1 reload:TRConfigReloadHot];
```

**自动生效**：
- 注册表自动上报到 configSchema + configs
- 网关自动转发 set 命令
- 前端自动渲染表单（按 reload 分区）
- API 自动暴露

### G.3 开发量对比

| 项 | 旧架构 | 新架构 |
|---|---|---|
| 改 Root.plist | ✅ 必须 | 仅配置型需要 |
| 改 TRGatewayClient 上报 | ✅ 必须 | ❌ 自动 |
| 改 caps.js 前端按钮 | ✅ 必须 | ❌ 自动渲染 |
| 改 trollvncserver 执行 | ✅ 必须 | ❌ 协议分发 |
| **总计** | **4 文件** | **1 文件** |

---
