# 架构演进改造计划：事件驱动 + 单一数据源（v1.0 · 2026-08-11）

> **背景**：在「控制导航闪退」排查与卡片墙优化讨论中，暴露出当前框架的结构性交互欠缺（状态分散 / 事件通道缺失 / UI 与逻辑耦合 / 生命周期盲区）。本计划以**事件驱动 + 单一数据源 + 三层分层**为目标，分三阶段渐进落地，每阶段可独立验证，不搞一次性大重构。
>
> **覆盖范围**：IPA App 层（`app/TrollVNC/TrollVNC`）、设备端（`TrollVNC/src`）、网关/Web（`trollvnc-farm`）。
>
> **2026-08-11 同步**：Phase A/B 已实现并被容器化吸收（`TVNCAppStore` / `TVNCGatewayClient` 保留，消费方 TVNCConnectViewController 继续用）；**Phase C 已废弃**——screen_changed 增量链路全链路删除（`screen.subscribe` 已删），回归 **H5 hash 门控唯一拉图**（见 [00 §8.6/8.7](00-当前进度基线.md)）。

---

## 1. 背景与动机（现状问题 · 代码级）

### 1.1 无单一数据源 → 状态不一致

| 现状 | 代码位置 | 问题 |
|---|---|---|
| 「已连接网关」= 本地服务端口探测 | [TVNCConnectViewController.m](app/TrollVNC/TrollVNC/TVNCConnectViewController.m#L517) `isServiceRunning` | 「已注册到网关，隧道已建立」是服务进程在跑的**近似文案**；网关没配/没连上也显示已连接 |
| 设备目录各页面各自拉取 | [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L824) `refreshDevices` | 控制页每次切 Tab 全量 `GET /api/devices`，与其他端无共享缓存 |
| 服务状态只有进程存活布尔 | [TVNCServiceCoordinator.m](app/TrollVNC/TrollVNC/TVNCServiceCoordinator.m#L236) `_isServiceRunning` | 没有「注册中/已注册/隧道已建立」的细分状态 |

### 1.2 事件通道缺失 → 被迫轮询/延迟/近似

| 缺失事件 | 现状填补 | 理想 |
|---|---|---|
| 设备端「隧道已建立」→ App | 固定延迟假设（曾讨论 3s） | 设备端状态事件经本地通道暴露 |
| 网关「设备目录变更」→ 控制端 | 轮询（web 6s / IPA 切 Tab 拉） | 网关目录推送 |
| 被控设备「画面变化」→ 控制端 | 控制端 5s hash 轮询（已双速优化） | 设备端检测上报 |

### 1.3 UI 与逻辑耦合

[TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m) 一个类同时承担：网络请求（refreshDevices/invokeCap）、定时器（wallPollTick/sigTimer）、状态判定、UI 渲染。逻辑不可复用、不可单测，固定秒数（5s/6s/1.2s/1.5s…）散落业务代码。

### 1.4 生命周期盲区

[SceneDelegate.m](app/TrollVNC/TrollVNC/SceneDelegate.m#L34-L41) 的 `@try` 只覆盖启动链路；UITabBarController **懒加载**子 Tab 的 view，控制页 `viewDidLoad` 在点击 Tab 时执行（@try 外）——iOS 14 设备上 `UIButtonConfiguration`（iOS 15 API）直接闪退无兜底（已修复，见 §3）。

---

## 2. 目标架构

```
┌───────────────────────────────────────────────┐
│ UI 层（VC/View）                               │
│ 连接页 / 控制页 / Viewer / 设置页               │
│ 原则：只读 Store 状态 + 发动作，不碰网络/定时器  │
└──────────────────────┬────────────────────────┘
                       │ 订阅状态（Notification）
┌──────────────────────▼────────────────────────┐
│ 状态层 AppStore（唯一真相）                    │
│  serviceState / gatewayState(状态机)          │
│  deviceDirectory / capabilities / configs     │
└──────────────────────┬────────────────────────┘
                       │ 事件（统一事件总线）
┌──────────────────────▼────────────────────────┐
│ 服务层 Clients/Coordinators                   │
│  ServiceCoordinator（进程存活，唯一保留轮询）   │
│  GatewayClient（目录/调用/订阅，结果驱动重试）  │
│  DeviceStatusBridge（设备端状态桥，P3）        │
└──────┬────────────────────────────┬───────────┘
       │                            │
┌──────▼──────────┐        ┌────────▼──────────┐
│ 设备端（进程）    │        │ 网关（trollvnc-farm）│
│ 注册/隧道/能力    │        │ 注册/目录/invoke   │
│ →暴露状态事件(P3)│        │ →目录变更推送(P3)   │
└─────────────────┘        └───────────────────┘
```

**核心决策**：
1. **AppStore 单一数据源**：网关状态机 `idle → serviceUp → registered → tunnelUp → disconnected`；设备目录一份缓存全端共享
2. **事件驱动优先，轮询仅限无事件源处**：注册判定结果驱动、设备目录变更推送、画面变化设备端上报；唯一保留轮询 = 服务进程存活探测（无事件源）
3. **逻辑下沉服务层**：重试/退避/超时统一治理；固定秒数仅存「超时兜底」与「心跳」两类
4. **跨端事件补全**（P3）：设备端上报 + 网关广播

---

## 3. 已完成改动（历史记录 · 2026-08-11 容器化后原生控制器已删除）

> **注**：本节的 `TVNCControllerViewController` / `TVNCViewerViewController` 改动随容器化（控制 Tab = `TVNCConsoleWebViewController` 加载网关 H5）**已删除**；双速检测 / 直控 / 同步等能力改由网页端承担。本节仅作历史记录。

| 文件 | 改动 | 目的 |
|---|---|---|
| [TVNCClientListController.m](app/TrollVNC/TrollVNC/TVNCClientListController.m#L654) | `leadingSwipe` → `trailingSwipeActionsConfigurationForRowAtIndexPath` | 修复首页客户端卡右滑失效 |
| [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L432-L469) | `styleBatchButtonForMode` 加 `@available(iOS 15)` + iOS 14 传统样式回退 | **修复 iOS 14 点「控制」Tab 闪退**（懒加载 viewDidLoad 在 @try 外） |
| [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L1044-L1047) | `startVisibleWallPolls` 先幂等检查再 `activeWallConnections++` | 修复并发配额计数虚增 |
| [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L1085-L1182) | **双速检测**：单次 timer 链式调度（变化 1s 快检 / 静止 5→15s 退避）+ `wallPollActive` stop 防护 | 卡片墙画面变化更灵敏、静止更省 |
| [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m) + [TVNCViewerViewController.m](app/TrollVNC/TrollVNC/TVNCViewerViewController.m) | `[CtrlNav]` / `[Viewer]` 全链路分段诊断日志 | 复现时精确定位崩溃环节 |

**验证项（重打包 IPA 后）**：
- iOS 14 + iOS 15+ 设备点「控制」Tab 均正常进入**容器加载的 H5 控制台**（容器 VC 本身需验证加载/重试/未配置引导）
- 卡片墙变化响应 ≤1s；静止后请求频率递减（H5 hash 门控保证）
- 首页客户端卡右滑出现「冻结/断开」（连接页仍有效）

---

## 4. 分阶段改造计划

### Phase A：AppStore + 网关状态机 + 注册判定结果驱动（IPA App 层）

**目标**：消除「假已连接」与「各自拉取」，用结果驱动替代固定延迟。

**A-1 新建 `TVNCAppStore.h/.m`（单例，状态层）**

```objc
// 网关连接状态机
typedef NS_ENUM(NSInteger, TVNCGatewayState) {
    TVNCGatewayStateIdle,        // 服务未运行/网关未配置
    TVNCGatewayStateServiceUp,   // 服务进程在跑（本地探测）
    TVNCGatewayStateRegistered,  // 网关目录已含 selfDeviceId（真注册）
    TVNCGatewayStateTunnelUp,    // 隧道建立（P3 接入设备端事件后细分）
    TVNCGatewayStateDisconnected // 网关不可达/注册丢失
};
```

- 属性：`gatewayState`、`deviceDirectory`（NSArray 缓存）、`lastDirectoryFetchedAt`
- 通知：`TVNCGatewayStateDidChangeNotification` / `TVNCDeviceDirectoryDidUpdateNotification`
- 方法：`fetchDeviceDirectoryWithResultDrivenRetry`（见 A-3）、`registered`（判定）

**A-2 `TVNCServiceCoordinator` 扩展（协调器宿主）**

- 在 `registerServiceMonitor`（[L166](app/TrollVNC/TrollVNC/TVNCServiceCoordinator.m#L166)）内监听自己的 `TVNCServiceStatusDidChangeNotification` → 服务运行中触发 AppStore 拉取
- 新增 `deviceDirectoryFetching` 防重入 + `fetchInFlight` 标志

**A-3 结果驱动注册判定（替代固定 3s 延迟）**

```
服务运行（通知）→ 立即 GET http://网关:8080/api/devices
  ├─ 成功 & 列表含 selfDeviceId → gatewayState=Registered → 缓存目录 → 发通知 → 停止
  ├─ 成功 & 不含自身 → 网关可达、注册进行中 → 1s 退避重试（1→2→4s，上限 30s）
  └─ 失败 → 网关不可达 → 退避重试，gatewayState=Disconnected
```

- 无任何时间猜测，全部以真实响应为准（注册快=首次即成功；慢=自动重试；不可达=明确未连接）
- 与 [TVNCClientListController.m](app/TrollVNC/TrollVNC/TVNCClientListController.m#L211) 的 `subscribe` 事件驱动模式同风格

**A-4 连接页消费真状态**

- [TVNCConnectViewController.m L516](app/TrollVNC/TrollVNC/TVNCConnectViewController.m#L516) `refreshStatus`：`connected` 改用 `store.gatewayState >= Registered`；`connectStateLabel` 按状态机映射文案（注册中/已注册/未注册）
- 监听 `TVNCGatewayStateDidChangeNotification` 刷新

**A-5 控制页读共享缓存**

- [TVNCControllerViewController.m L824](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L824) `viewWillAppear`：`if (!store.deviceDirectory.length) [store fetch...];`——缓存有效直接用，空才拉
- 首次点 Tab 拉一次，后续切 Tab 零请求零重建（沿用「首拉 + 手动刷新」设计）

**验收**：连接页 Hero 状态真实反映注册结果；控制页从 Viewer 返回不闪烁；无固定延迟等待。

### Phase B：GatewayClient 抽象 + 页面归位（IPA App 层）

**目标**：网络/定时器逻辑下沉，UI 只消费状态。

**B-1 新建 `TVNCGatewayClient`（服务层）**

- 封装：`fetchDevices`（结果驱动重试）、`invokeCap:params:forDevice:`、`pingDevice:`、`fetchCaps:`
- 从 [TVNCControllerViewController.m](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L824-L1170) 迁移 6 处 `NSURLSession` 调用（refreshDevices/invokeCapAck/invokeCap/fetchCapMetadata/fetchConfigSchema/batchApplyConfig）
- 统一超时（6s）、token 注入、错误透传（显式失败，不静默降级——项目宪法）

**B-2 控制页归位**

- `TVNCControllerViewController` 改为只持有 `client` + 订阅 `store`，删除散落的 URLSession 代码
- 双速检测的间隔常量收敛到 `TVNCGatewayClient` 或独立 `TVNCWallPollConfig`（超时/心跳两类秒数唯一来源）

**验收**：控制页功能不变（编译 + 真机回归）；`TVNCControllerViewController.m` 减重 >300 行；无 URLSession 直连。

### Phase C：设备端变化上报 + 网关事件层 + 控制端订阅（跨端）❌ 已废弃（2026-08-11）

> **废弃说明**：本阶段「screen_changed 增量通道」已全链路删除并回滚——设备端 `screen.subscribe` 订阅/上报、网关 `screenChangedAt` 标记、前端 `changedSince` 增量拉图均移除；**回归 H5 hash 门控唯一拉图**（变化才 screenshot，双速退避）。tvCtl 广播基础设施保留（服务 `clients.subscribe`）。详见 [00 §8.6](00-当前进度基线.md)。

**目标**：彻底事件驱动——卡片墙从「轮询 hash」升级为「设备端检测上报，变化才拉图」。

**C-1 设备端（`TrollVNC/src`）**

- [trollvncserver.mm L3834](TrollVNC/src/trollvncserver.mm#L3834) `screen.subscribe` 已有 TRScreenHasher 变化检测 + 46752 推送（**现成**）
- [TRGatewayClient.mm](TrollVNC/src/TRGatewayClient.mm#L431)：订阅 46752 的 `screen.event` → 经**注册通道（18081，JSON 行协议）**上报网关：`{"type":"screen_changed","deviceId":...}`
- 心跳复用现有 hello 通道，不新增连接

**C-2 网关（`trollvnc-farm/server/index.js`）**

- [L952 注册处理](trollvnc-farm/server/index.js#L952) 处新增 `msg.type === 'screen_changed'` 分支 → 设备记录 `screenChangedAt = now`
- [L629 `/api/devices`](trollvnc-farm/server/index.js#L629) 返回每设备 `screenChangedAt` 字段
- **可选增强**：WS 目录/事件推送端点（控制端零轮询），与「设备目录变更推送」共用一套广播机制

**C-3 控制端（web + IPA 双端）**

- web [app.js L265 `startWallRfb`](trollvnc-farm/web/app.js#L265)：`5s hash 轮询` → `低频(1-2s)查 devices.changed 标记 → 变化才拉 screenshot`；静止期零请求
- IPA [TVNCControllerViewController.m L1085](app/TrollVNC/TrollVNC/TVNCControllerViewController.m#L1085)：同构改造；双速检测保留为过渡兜底（hash 能力仍注册）

**验收**：静止时卡片墙请求趋近零；画面变化 ≤1s 可见；设备离线/恢复正确联动。

---

## 5. 跨端配合点汇总

| 能力 | 设备端 | 网关 | 控制端 |
|---|---|---|---|
| 注册判定（P1） | 已有（register） | 已有（/api/devices） | AppStore 结果驱动 |
| 目录变更推送（P3） | — | 新增广播端点 | 订阅 |
| 画面变化上报（P3） | 上报 screen_changed | 记录/广播 | 查标记/订阅 |

---

## 6. 风险与验证

| 风险 | 缓解 |
|---|---|
| Phase A 改动 ServiceCoordinator 影响服务探测 | 新增逻辑独立方法，不动 `_isServiceRunning` 探测 |
| 结果驱动重试在网关不可达时误判 | 退避有界（30s 封顶）+ 手动刷新兜底 |
| P3 跨端改造引入协议不一致 | 复用现有 JSON 行协议（18081）+ 附录 B 字段规范；网关先自测 |
| 双速检测 timer 链在极端时序下断裂 | `wallPollActive` 防护 + busy 分支续排（已实现） |

**每阶段独立验证**：A→重打包 IPA 真机验证状态；B→编译 + 回归；C→网关 test/ 目录已有脚本扩展。

---

## 7. 里程碑

| 阶段 | 内容 | 依赖 | 状态 |
|---|---|---|---|
| §3 已完成改动 | 闪退修复 + 右滑修复 + 双速检测 + 诊断日志 | — | ✅ 代码完成，待真机验证 |
| Phase A | AppStore + 状态机 + 注册判定 | §3 验证通过 | ✅ 已实现（TVNCAppStore + ConnectViewController 消费 + Controller 共享缓存） |
| Phase B | GatewayClient 抽象 + 页面归位 | Phase A | ✅ 已实现（TVNCGatewayClient + Controller 网络层全迁移） |
| Phase C | 设备端上报 + 网关事件层 + 控制端订阅 | Phase A/B | ❌ 已废弃（2026-08-11）：screen_changed 增量链路全删（screen.subscribe 已删），回归 H5 hash 门控唯一拉图 |
