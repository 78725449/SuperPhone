# 项目知识库 · SuperPhone

> **说明**：本文档是项目唯一文档，基于最终代码提炼（2026-08-16 定稿）。内容为既定事实（架构 / 时序 / 实现），随代码演进**增删改查**——代码变了就改这里，无需其他文档。
> **真相源**：代码（`TrollVNC/` 设备端与 App、`trollvnc-farm/` 网关与前端）始终优先；本文档描述代码中实际存在的机制，不记录历史、不记录规划、不复制代码清单。
> **相关文件**：`TrollVNC/README.md`（快速开始）、`trollvnc-farm/README.md`（网关启动）、`TrollVNC/CHANGELOG.md`。

---

## 1. 项目概述

- **定位**：内网自用的 iOS 设备群控系统——手机装一次、初始化一次，之后零操作；通过软路由上的网页随时看到并操作所有设备。
- **三端**：PC web 控制台、手机 web 控制台、IPA 控制端（控制 Tab = WKWebView 容器加载网关 H5 手机控制台）。
- **能力层唯一地基**：所有设备操作走 RFB → IOHID 注入；手机自带 web（5801）与服务器控制台（8080）是同一地基上的两扇门，禁止前端自造输入协议。
- **前端静态契约**：前端 `caps.js` 自包含定义按键/批量/配置（无上报、无元数据表、无运行时发现）；设备端注册表只存 executor，两端约定对齐。
- **边界（明确不做）**：注册鉴权（网关仅 FARM_TOKEN 兜底）、过度 Agent 命令通道（批量启停/装 App 等）、TLS 加固（内网）、多租户、审计、计费、商业化。

---

## 2. 架构

### 2.0 架构愿景与通道模型（2026-08-25 定稿）

**愿景（对准目标，开发功能前先对照）**

| 编号 | 愿景 | 含义 |
|---|---|---|
| **A** | 局域网内通过网关管理所有设备 | 8080 控制台是局域网统一管理入口 |
| **B** | 外网访问网关操作内网所有设备 | 设备**主动连**网关 18181（内网→外网出向），网关部署在公网可达处，外网用户访问 8080 |
| **C** | 5801 不经过网关独立操作单台设备 | 局域网直连（5801/5802/5901），**不依赖网关/manager/隧道**——manager 挂掉时 5801 直连照常可用 |

**端口全景（横跨两端，明细见 2.1）**

| 端 | 设备端（TrollVNC） | 网关（trollvnc-farm） |
|---|---|---|
| 画面/控制 | **5901** RFB（画面 + 0x50/0x80 扩展命令）| — |
| 浏览器直连 | **5801** 直连页（HTTPS 静态 + WS→5901）| **8080** 控制台（web + API；鉴权门禁：未输入 FARM_TOKEN 时顶栏/屏幕墙整体隐藏、仅居中鉴权卡，输入后才进屏幕墙，2026-08-28）|
| 管理 API | **5802** HTTP（data.read/touch/app/clipboard/config；~~data.fill/data.clear/sim.*~~ 2026-08-26 已收敛仅 App 内部）| — |
| 注册 | — | **18081**（设备注册）|
| 隧道 | TRTunnelClient（主动连网关 18181）| **18181**（隧道接收）|
| 日志 | **5902**（manager，stderr/stdout）| — |

**隧道协议（18181，单 TCP 多路复用）**

```
DATA(0x01) 双向 RFB 透传（隧道 ↔ 本地 127.0.0.1:5901）→ 画面/缩略图/输入  ← 仅此走 5901
CMD(0x04)  命令 JSON（invoke/query/set/restart）→ manager 注册表执行       ← 不经 5901
PING/PONG  心跳；STATE 被控状态上报；CHAN 通道（chan0 缩略图 + 会话通道）
```

**关键事实：能力命令（invoke）在 manager 进程直接执行（TRCapabilityRegistry），不经过 5901；只有画面流经 5901。**

**能力对齐模型（两对平行的「前端 + 管理后端」）**

| 场景 | 前端 | 画面通道 | 管理通道 | 能力执行 |
|---|---|---|---|---|
| 局域网直连单设备 | **5801** 页面 | WS→**5901** | **5802** HTTP | trollvncserver（tvExtHandle* 纯函数）|
| 跨网管理所有设备 | **8080** 控制台 | WS→隧道 CHAN→**5901** | 隧道 **CMD**→注册表 | manager（executor）|

- **5901 是画面共同基础**（两端都连它）
- **能力层两套分派、共享底层**：trollvncserver（0x50/5802）的 tvExtHandle* 与 manager（注册表）executor 是两套分派层，但底层模块单一（TRDataFiller/SimLocationController/SimRouteCalculator/STHIDEventGenerator/LSApplicationWorkspace）；**2026-08-26 A 档收敛后数据/定位写操作仅 App 内部直调底层模块，外部入口已移除**
- **三处补齐纪律**：新增能力 = 注册表 + 0x50 + 5802 各加入口（**数据/定位写能力除外——默认只在 App 内部实现**）

**能力实现分类指南（2026-08-26 A 档收敛后定稿，开发新功能前先对照此表判定）**

| 类别 | 判定标准（这能力操作的是什么？）| 实现路径 | 入口清单 |
|---|---|---|---|
| **A 类·伪装页/定位直控能力** | 操作对象是**设备上的拟人数据/位置模拟**（数据填充/清空、定位开关/轨迹编排/区域漫游等），用户在 App 伪装页/定位 UI 内操作 | **仅 App 内部直调共享底层模块**，不注册表、不 0x50/5802、不加前端按钮 | ① 底层模块单一实现（如 TRDataFiller/SimLocationController/SimRouteCalculator/RegionSimulator）→ ② App 目标 `#import` + 直调类方法 → ③ Makefile（App SUBPROJECT 内）/xcodeproj 保证 App target 编译该模块 → ④ 文档同步 |
| **B 类·通用控制能力** | 操作对象是**设备系统通用操作**（硬件键/触控/剪贴板/应用/屏幕检测/服务重启/设置等），控制端（网关/5801/脚本）需要调用 | **三处补齐** + 前端契约 | ① 底层模块单一实现 → ② 注册表 executor（manager，`_registerControl:`）→ ③ 0x50 分派 + 5802 分派（trollvncserver `tvExtHandle*` 纯函数）→ ④ 前端 caps.js（网关 + 5801 分叉各一条）+ caps-test 断言 + `?v=N` 递增 → ⑤ 文档同步 |

**判定口诀**：**"App 里点的 → A 类（内部直调）；控制端/脚本调的 → B 类（三处补齐）"**。
典型示例：A 类 = data.fill/data.clear、定位编排/轨迹/区域（TRDataFiller/SimLocationController/SimRouteCalculator/RegionSimulator 直调）；B 类 = touch.*、app.*、clipboard.get、type.paste、screen.*、service.restart、settings.* 等。
两类都可复用同一底层模块（如 TRDataFiller 同时被 A 类 App 直调与 B 类 data.read 维护接口引用），差异只在「是否暴露外部入口」。

**能力集现状（2026-08-26 A 档收敛后）**

| 能力 | 5802/0x50（局域网直连）| 注册表（跨网 invoke）|
|---|---|---|
| data.fill / data.clear | ❌（2026-08-26 收敛，仅 App 伪装页内部直调 TRDataFiller）| ❌（同上）|
| **data.read** | ✅（**唯一保留的维护/检查接口**，白名单表/limit≤50）| ✅（愿景 B 跨网读库闭环）|
| data.probe | ❌（2026-08-26 退役——POC 调试接口，使命完成）| ❌ |
| **sim.itinerary**（定位编排）| ❌（2026-08-26 收敛——SimItineraryPlanner 已删，App 定位 UI 自实现编排）| ❌（同上）|
| sim.location.track / status、sim.route.calculate | — | ❌（2026-08-26 收敛，App 直调 SimLocationController/SimRouteCalculator）|
| clipboard.get / type.paste / config / touch.tap/swipe / app.* / HID | ✅ | ✅ |
| album.import（**gps 包装**：EXIF/ISO6709 改写为模拟定位或清除，2026-08-28）| ✅（5802 独有）| ❌（按需）|
| device.rename / service.restart / clients.* / sys.version / gateway.reconnect / touch.taps/pinch/down/up/event | ❌ | ✅ |
| screen.hash/diff/waitStable | ✅（0x50）| ✅（hash）|
| **vision.ocr / vision.find_text / vision.find_image**（2026-08-28）| ✅ | ✅（invoke 需 timeout ≥15s）|
| **identity.reset**（2026-08-28，换号身份锚清理）| ✅ | ✅（bundleId + mode=dryrun/commit）|

**架构决策依据（为什么这样设计）**

1. **5802 单独实现 ≠ 重复**：进程隔离是特性——trollvncserver 独立进程服务局域网直连，**不依赖 manager/隧道**，是愿景 C（5801 直连独立性）的保障。跨进程强制统一会破坏它。
2. **隧道 CMD 不经 5901**：能力命令在 manager 直接执行（进程内调用注册表），只有画面走 5901。
3. **数据/定位写操作收敛仅 App 内部（2026-08-26 A 档）**：data.fill/data.clear/sim.itinerary/sim.location.* 无外部调用者（前端按钮已删），外部入口全移除；App 伪装页/定位 UI 进程内直调共享底层模块（TRDataFiller/SimLocationController/SimRouteCalculator/RegionSimulator），manager daemon 仍持有 SimLocationController 做配置感知注入（trollvncserver 已不链接定位模块；SimItineraryPlanner 无调用者已删）；**data.read 保留为唯一外部只读维护接口**（远程排查数据实现的命脉）。

**与愿景的差异（已补齐/待办）**

- ✅ 已补齐（历史）：`data.read` 进注册表（愿景 B 跨网读库闭环）、`sim.itinerary` 进 5802/0x50（愿景 C 直连定位编排闭环，**2026-08-26 已随 A 档收敛收回**）
- ✅ **前端能力按钮去留定案（2026-08-26 直控 App UI + A 档收敛）**：伪装页数据/定位能力不再前端面板单独实现——网关 web 与 5801 直连页按钮已去除，**外部写/编排入口一并移除，操作完全交 App 定位 UI/伪装页**；data.read 保留 5802 维护接口 + 注册表闭环
- ⏳ 待办：`album.import` 进注册表（按需）

### 2.1 端口全集（全固定不可调）

| 端口 | 角色 | 协议 | 说明 |
|---|---|---|---|
| 手机 **5901** | 原生双向控制通道（画面下行 + 操作上行） | RFB/VNC | 控制台/网关/VNC 客户端唯一连它；含 0x50/0x80 扩展消息命令通道（5801 直连页已改走 5802 HTTP，0x50 仅余 VNC 客户端/控制端使用）；配置证书后 WebSocket 自动支持 wss（webSocketsCheck 按首字节分流，明文 RFB/ws 不受影响） |
| 手机 **5801** | 网页文件服务器（只发 noVNC 页面） | HTTP/HTTPS | 无任何配置 API；控制台不做 5801 路由入口；**2026-08-19 起有证书时由自建 HTTPS 静态服务器接管**（TLS + 明文 301 跳 https），无证书保持 libvncserver httpd 纯 http |
| 手机 **5802** | 直连页管理 API + **AI 工具控制接口**（剪贴板/type.paste/config + touch.tap/swipe + app.list/open + album.import + HID 键 home/电源/音量/亮度/搜索/键盘等，2026-08-23） | HTTP/HTTPS JSON | 5801 页面 fetch 调用；与 RFB 画面流隔离（v3，2026-08-17）；**与网关 REST 同构**（管理走 HTTP、画面走 RFB），复用 tvExtHandle* 纯函数（cl=NULL）零重复实现；touch/app/HID/album 接口在 trollvncserver 内直接实现（复用 STHIDEventGenerator / dlopen 加载 LSApplicationWorkspace / PHPhotoLibrary 相册导入），坐标 0-1 归一化；album.import 数据经 base64 传入（≤200MB，首次触发相册授权弹窗）；2026-08-19 起支持 TLS（peek 分流明文/TLS，兼容老页面）。**网关控制台「上传」按钮**（2026-08-23，桌面操作列复制之后 / 触屏 FAB 菜单粘贴之后）：选择照片/视频 → 网关 POST `/api/devices/:id/album` 中转 → 设备 5802；5801 直连页底栏「上传」位于粘贴之后（2026-08-23 定稿：三端上传入口语义对齐，桌面操作列因 PC 无粘贴按钮故仍位于复制之后）。**AI 工具控制面网关中转（2026-08-23，跨网修正走隧道 invoke）**：`GET /api/devices/:id/apps`（app.list 查已装应用，过滤 System）、`POST /api/devices/:id/app-open {bundleId}`（app.open 启动）、`POST /api/devices/:id/touch {x,y 0-1}`（touch.tap 点击）——经隧道 CMD invoke 通道执行（能力定义已收敛到设备端 TRCapabilityRegistry 唯一地基：app.list/app.open 注册进注册表、touch 注册表原有），**跨网可用**（不再局域网直连 5802；5802 直连仅限局域网 5801 页面场景） |
| 手机 **5902** | 远程日志端点（崩溃诊断） | HTTP | `trollvncmanager` 常驻提供，GET `/stderr` `/stdout` 返回 trollvncserver 崩溃日志尾部 64KB；server 崩溃循环时 5801/5802/5901 均失效，仅 5902 仍可读（2026-08-21） |
| 网关 **8080** | 控制台（外网包装面） | HTTP + WS | 外部经 frp/隧道到此，再由网关桥到 5901；TLS 可选（明文 301 跳转） |
| 网关 **18081** | 注册/心跳 | TCP JSON | 设备主动拨入 |
| 网关 **18181** | 隧道（VNC 桥接 + 命令透传） | TCP 帧（FT_，proto:2 通道复用） | 设备主动建立；无隧道 4003 拒绝（直连回退已废弃） |

> **46752 本地控制端口已删除**：命令并入 5901 RFB 扩展消息承载（请求 type 0x50 / 响应 type 0x80，JSON 封装）；5801 直连页自 2026-08-17 v3 起改走独立 HTTP 端口 5802（管理操作与画面流隔离，根治 0x50/0x80 与 FBU 竞争导致的 type 125）。Repeater 模式与 5500 端口亦已全链路删除。
>
> **5802 是 5801 场景的「网关 REST 等价物」**：网关架构 = REST（HTTP 管理）+ 隧道（RFB 画面）；5801 直连 = 5802（HTTP 管理）+ 5901 WS（RFB 画面），两端同构。能力 handler 唯一——0x50 与 5802 共用 tvExtHandle* 纯函数，仅传输入口不同（隧道透传 vs HTTP 直连），新增能力只加一处 handler。**0x50 已不再承载 5801 管理操作**（v3 定稿，勿回退）。
>
> **跨进程能力对齐（2026-08-25 补齐，2026-08-26 A 档收敛调整）**：0x50/5802（trollvncserver 进程）的 tvExtHandle* 与注册表（manager 进程，跨网 invoke）executor 是**两套分派层、共享底层模块**（TRDataFiller/SimLocationController/SimRouteCalculator/STHIDEventGenerator 等）——新增能力按「三处补齐」纪律在注册表 + 0x50 + 5802 各加入口。2026-08-25 补齐：`sim.itinerary` 加 0x50/5802、`data.read` 加注册表。**2026-08-26 收敛：data.fill/data.clear/sim.itinerary/sim.location.* 外部入口已移除（仅 App 内部直调），data.read 保留 5802 维护接口 + 注册表闭环**。
>
> **5801/5802 HTTPS（2026-08-19 落地）**：设备端自签证书（`SslCertFile`/`SslKeyFile`，`settings.generateKeys` 或**启动缺失自动生成**，含 IP SAN）就绪后：5801 由**自建 HTTPS 静态服务器**接管（`tvHttpsStaticServerMain`，TLS + 明文 301 跳转，serve webclients）；5802 加 TLS（peek 首字节 0x16 分流，兼容未配证书的老页面明文）；5901 由 libvncserver `webSocketsCheck` 自动支持 wss（明文 RFB/ws 不受影响，网关隧道兼容）。无证书时全部降级纯 http，行为与 v3 一致。证书生成逻辑抽至共享模块 `TRSelfSignedCert.mm`（trollvncserver/trollvncmanager 复用，参考网关 gen-cert.mjs 的 SAN 策略）。

### 2.2 组件结构

**设备端（TrollVNC/src/，Theos 工程）**

| 组件 | 职责 |
|---|---|
| `trollvncserver.mm` | VNC 服务（5901）、RFB 扩展消息（0x50/0x80）、采集惰性启动（隧道握手成功/首个 RFB 客户端触发，tunnel-connected 通知驱动）+ 升降频（缩略图态 CaptureFps / 屏幕流态 FrameRateSpec） |
| `TRCapabilityRegistry.mm` | 命令注册表（executor 注册：`_registerControl:` 52 项[直登/数组 34 + touch.* 18——**sim.* 定位 4 项与 data.fill/data.clear 已随 2026-08-26 A 档收敛移除；2026-08-28 +vision.ocr/vision.find_text/vision.find_image/identity.reset**]、`_registerConfig:` 38 项——前端 CONFIG_DEFS 28 项，SimLocation* 5 项 + BonjourEnabled/ViewOnlyPassword 等为 App/daemon 保留注册不暴露前端 UI）；route 分派 HID/Touch/LocalCmd/Native |
| `SimLocationManager.mm` + `SimLocationController.mm` | 改定位（GPS 注入 L3）：CLSimulationManager 注入原语 + 自治控制器（读 SimLocation* 参数驱动 off/anchor/itinerary 状态机：anchor=位置基底+内置微动游走、itinerary=轨迹文件逐秒推进；**参数变更 = notify 直达**（manager 订阅 `prefs-changed` → `reloadFromPrefs`）+ **轨迹文件 mtime 指纹**（新轨迹必重载、**从当前位置最近点续播**）；**注入后 notify `locsim-update` 已删除**（2026-08-24 App 不订阅，单一驱动无冗余；App 侧当前位置由**活跃订阅唯一驱动**——授权后 `CLLocationManager startUpdatingLocation` 持续订阅 locationd（locationd 只在存在活跃请求时持续计算广播），`didUpdateLocations` 驱动状态栏/锚点/聚焦/速度，水滴显示同一 locationd 源；**时间戳分辨新旧**（开启/停止切换记时刻，各分支只认晚于对应时刻的 fix——过滤模拟残留/真实残留）；**自动聚焦=距离阈值（2026-08-25 定稿，替代 hasFocusedRealOnce 首 fix 聚焦）**（原"停止后首个真实 fix 聚焦"有缺陷：停止瞬间 locationd 队列残留的模拟 fix 可抢先占用聚焦资格，真正的真实 fix 到达后只更新坐标不聚焦→地图停在画布内真实位置出视野；现改记 `lastAutoFocusWGS` 基线——停止时=停止瞬间模拟位置、开启时=锚点位置、**启动未初始化=(0,0) → 首个 fix 建立基线并聚焦（打开 APP 聚焦当前位置，2026-08-25 v2）**，fix 距基线 **≥500m** 才聚焦并更新基线：残留 fix≈基线不触发、停止回真实/模拟出视野必触发、GPS 抖动（<50m）不打扰；模拟分支仅在非 Follow 态生效，Follow 原生跟随不重复）；**`desiredAccuracy` 放宽 HundredMeters**（默认 Best 扣住 acc>10m 位置不推：模拟 acc 3~15m 随机被扣致跟随断续、停止后真实 GPS 收敛初期被扣致干等，放宽后尽早推）；**坐标读回调 fix `lastFix`**（didUpdateLocations 回调里通过时间戳过滤的 fix，不读 locationManager.location 属性缓存）；**状态栏行程列表交互（2026-08-25）**（点击行 = 聚焦到该行程首锚点（anchor 段=该锚点坐标、region 段=区域中心）；删除走**系统原生右滑**（去自绘 ✕ 按钮）；无锚点时列表显示空白（去"暂无行程"占位行））；**开启横跳防护（2026-08-25）**（`startupLockedToAnchor`：开启定位/点首锚点瞬间到注入落地前，locationd 广播的仍是真实位置——距锚点 >25m 的 fix 忽略（保持锚点显示）、注入落地后 fix≈锚点（<25m）解锁恢复 fix 驱动——消除"真实→锚点"横跳；toggleLocate/handleTap/applySearchResult 三处置锁，handleLocationUpdate 过滤）；**停止后再开启跳回（2026-08-25）**（播放中停止→回真实→再开启：之前模拟位置 cur 距当前实际位置（lastFix）>500m 时 setRegion 瞬间跳回停止前位置（复用首锚点视野行为）；距离近不跳——维持"开启不主动聚焦"常规语义）；**region-only 开启（2026-08-25；2026-08-28 改版）**（region 段入链即置 `hasStart=YES`；**链首 region 段无前驱锚点 → 自动插入区域内入口锚点**（随机 0.3~0.8 半径因子一点，`cur` 同步设为入口）——区域漫游独立成环、可立即开启定位，不再依赖当前位置，消除 0,0 起链/"等待 fix"拦截）；**状态栏速度=当前段缓存速度（2026-08-25 定稿 v2）**（locationd 广播层对模拟注入位置**不保留 CLLocation.speed**（恒 -1/0）；弃用 lastFix.speed 与模式平均，改从**内存段缓存 `segmentPoints` 索引取**——当前位置所在段（出发锚点→下一锚点，`departAnchor.segmentIndex+1`）首点生成的 speed（含 ±10% 抖动，random 段反映实际随机模式），段无效回退 `effectiveSpeedForMode`；非序列化、非最近点扫描，零通道）；不自绘、无 lastReal、无 requestLocation、无事件订阅；锚点经过判定 O(锚点数) 距离阈值，不扫全量轨迹点）；**锚点编辑局部重算（2026-08-25 定稿 v2）**（**彻底去除全量重算**：`segmentPoints` 段点序列缓存为锚点链唯一轨迹真相——添加=新段标记待生成顺序补齐、删除=段缓存同步删+合并段（前驱→后继）标记待生成、拖拽=按起点/目标锚点邻接重排缓存（邻接不变段保留旧点序列）；只对标记段重算 MKDirections，其余段点序列完全保留；生长线按段缓存瞬画（不涉及算路）；**段 0 特殊**=删首锚点后"当前位置→新首锚"待生成（正常链段 0=首锚点仅起点恒有效）；**删首分流（2026-08-25 v3）**=当前位置距新首锚 <25m（首锚路线已完全消费）→ 直接删除不重算、仍在路上 → 当前位置作起点重算段 0；删尾=截断保持当前位置；**region 段目标锚点=区域中心**（`anchorWGSOfSegment`/`sameAnchor` 均按 center 处理——此前缺失致涉及 region 段编辑合并段 (0,0) 乱连）；挂起编辑基于执行时链重新评估、无索引过期误判）；**播放顺序保证（2026-08-25 排查定稿）**（正常播放 100% 遵守锚点顺序：App 按段索引顺序展平→轨迹文件→daemon `_trackIndex` 单调推进逐点注入，无跳点逻辑；编辑后重载从当前位置 haversine 最近点续播（删除/调整中间点的续播跳变=允许行为）；`writeTrackFile` **异步+串行队列写盘**——并发写同一文件会覆盖/半截致 daemon 读到损坏轨迹跳点乱播）；**地图操作卡顿优化（2026-08-25）**（标注水滴位图按 颜色+size+emoji 缓存——viewForAnnotation 滚动/缩放反复调用不再重复位图绘制；生长线显示层抽稀 ≤600 点/条——区域长轨迹整条 polyline 滚动缩放每帧重绘是卡顿主因，轨迹文件保留全量播放精度仅显示抽稀）；**生长线按段分色（2026-08-25）**（rendererForOverlay 按 `segmentIndex` 循环分配 5 色——重叠/同路段不同段可辨"路线确实创建了"，段 0 保留深紫主色）；**双域配置统一=mobile 域**（App 与 `uploadTrackPoints` 均写 mobile plist，`_readPref` mobile 优先、root 域仅兜底——root 残留会覆盖 App 写入致开启定位不注入）；**CLSimulationManager 注入每次完整重启 + 投递参数**（每次 `stop→clear→append→flush→start`，对齐 TrollBox 广播；append-only 不重启则 locationd 不广播给持续订阅 client；投递参数默认 0 不投递，须设 `locationDeliveryBehavior=1` 等）；10s 巡检仅失效恢复；离线自治）。`uploadTrackPoints`/`currentStatus` 由 **App 定位 UI/daemon 直调**（~~`sim.location.track`/`sim.location.status`~~ 2026-08-26 已收敛移除） |
| `SimRouteCalculator.mm` | Apple 地图原生算路（MKDirections）：`calculateRoutePointsFrom:to:mode:completion:` 两点沿真实道路轨迹，仅返回点序列供 **App 定位 UI** 算路拼接（落盘由调用方负责）；仅驾车/步行两档。~~`sim.route.calculate` 能力~~ **已收敛（2026-08-26，App 直调）** |
| `RegionSimulator.mm` | 区域漫游**计划**生成（RegionEngine 拒绝采样撒点 + RegionTimeAllocator 行为形状时间分配）：**K 途经点亚线性饱和**（√T×2.5，clamp 3~15，每次生成 K 与位置均随机；**可自定义途经点数 waypointCount，默认 0=随机**）、停留偏态（短多长少）、速度因子每段 0.7~1.3、收尾到点停；只输出计划（途经点+时间分配），**移动段由 App 定位 UI 逐段 MKDirections 真实道路算路拼接**（进入段起点=上一锚点；**链首 region 段无前驱锚点 → 区域内自生成入口锚点**（随机取半径内 0.3~0.8 因子一点，插为链首并把 `cur` 设为入口——区域漫游独立成环，不从当前位置/0,0 起链，2026-08-28），途经点对 <30m 或算路失败 → **忽略该中间途经点**直接进入下一途经点）；**区域模式选择器=步行|随机（默认）|驾车**——随机=区域内每段出行方式随机（按平衡调节阀 `walkRatio` 默认 70% 步行/30% 驾车，左步行右驾车、拖向哪端=提高哪端），固定档隐藏调节阀，段存 `mode:random`+`walkRatio`，进入段仍用出发锚点模式；停留段同点微动 appendStayPointsAt。交互原型：`outputs/locsim-app-prototype.html` |
| ~~`SimItineraryPlanner.mm`~~ | **已删除（2026-08-26 随 A 档收敛）**——原 `sim.itinerary` 编排器（route/region/anchor 段串行拼接）唯一调用方是注册表/0x50/5802 入口，收敛后无调用者；**App 定位 UI 自实现编排**（`TRMapPickerViewController` 内逐段算路拼接，算法对齐原模块） |
| `TRGatewayClient.mm` | 注册/心跳（18081）、命令通道（invoke/set/restart/ping） |
| `TRTunnelClient.mm` | 18181 隧道客户端（注册成功后自动建立，proto:2 通道复用：chan 0 缩略图 + 会话通道，select 多路复用通道表 chanId→5901 fd）；CHAN_OPEN 同步 connect 5901 + 主动写版本 + 回 CHAN_ACK；会话通道开/关驱动升降频与被控上报 |
| `STHIDEventGenerator.mm` | IOHID 注入（键盘/触摸/按键）；**HumanizeTouch（2026-08-28 风控对抗）**：`humanizeEnabled` 粘性开关（默认 NO），自动化入口（注册表 invoke `_registerTouchCapabilities` / 5802 touch）置 YES 后，高层手势（tap/doubleTap/sendTaps/longPress/dragLinear/dragCurve + touch.down/up 一致性偏移 + eventStream 整体偏移）经整形注入——Fitts 偏置空间误差（σ 随距上次触点距离 2~12px、接近轴各向异性）+ 对数正态按压 45~220ms（中位 90ms）+ 重尾连点间隔（双击安全窗 80-160ms、连点中位 280ms、6% 偶发犹豫 0.4-0.9s）+ minimum-jerk 速度剖面 + OU 平滑噪声 + down→up 微滑移 ±1.5px；**永不整形**：画布 RFB 指针（ptrAddEvent 裸原语）、画布手势消费方（twoFingerTap/threeFingerTap/pinch——GESTURE_DEFS 经 invoke 属真人画布操作）、滚轮 `dragLinearPlainWithStartPoint:`（wheelFlush 专用裸拖变体） |
| `ScreenCapturer.mm` + `TRScreenHasher.mm` | 抓屏 + pHash（screen.hash/diff/waitStable 屏幕变化检测）；采集惰性启动 + 升降频 |
| `TRVisionEngine.mm` | Vision OCR + 模板匹配引擎（vision.ocr/find_text：VNRecognizeTextRequest .accurate zh-Hans+en-US、usesLanguageCorrection=NO；vision.find_image：vImage/vDSP 模板匹配——Rec.601 灰度 → **多尺度归一（scale 单次自适应缩放，2026-08-28）** → 1/8 金字塔 → vDSP_convD 相关图 → NCCC（SAT 归一化）→ ±48px ROI 原尺寸精匹配，阈值 0.7；返回 width/height 帧尺寸供跨 op 帧一致性校验）；与 ScreenCapturer 同进程取帧（captureSingleFrameBuffer），串行队列防并发；仅 trollvncserver 编译（Makefile FRAMEWORKS +Vision +Accelerate 已在列），注册表 LocalCmd 桥接（timeout 12s） |
| `ClipboardManager.mm` | 剪贴板读写（`clipboard.get` 读取 / type.paste 数据载体写入 / RFB 协议写入 setXCutText）；**不再监听系统剪贴板**（2026-08-17 显式双向搬运，无自动同步） |
| `trollvncmanager.mm` + `TRWatchDog.mm` | 守护进程 + 崩溃自动重启 |
| `BulletinManager.mm` | 本地通知（农场默认关闭） |

**App 外壳（TrollVNC/app/TrollVNC/）**：UIKit 三 Tab（`TRMainTabBarController`）——连接（扫码/网关/客户端列表）、控制（`TVNCConsoleWebViewController`：WKWebView 加载网关 H5，`?container=ipa&token=&selfId=`）、设置（prefs 分组表单）。

**网关（trollvnc-farm/，Node.js ESM）**

| 模块 | 职责 |
|---|---|
| `server/index.js` | 注册（18081）、隧道（18181，proto:2 通道复用：chan 0 缩略图 + 会话通道，ThumbRfbDecoder 暂停/恢复）、REST API + WS 桥接（8080）、设备墙数据、批量操作 |
| `web/` | 前端（无构建）：`index.html` + `app.js` + `caps.js`（契约定义）+ `press.js`（按压识别）+ `style.css`；缓存破坏 `?v=N` |
| `deploy/` | 软路由部署文档（openwrt-docker / openwrt-native） |
| `test/` | 测试套件（`npm test` 11 套件：smoke/tunnel/register/dedupe/caps/gesture/pending-replay/press/order/events/tunnel-thumb）|

### 2.3 连接模型

- **连接方向**：手机主动注册到网关（18081 TCP JSON），网关被动等待，不做轮询保活。
- **设备身份**：首启生成 UUID（`DeviceUUID`）为唯一 `deviceId`，注册名用设备真实名称；同 deviceId 只保留最新一条连接。
- **注册消息**：`{"type":"register","deviceId","name","vncPort":5901,"configs":{...},"screen":{width,height},"httpPort":5801}` → `ack`；每 30s `hello`；90s 无心跳判离线。**不包含任何能力/元数据字段**（2026-08-13 起）。
- **在线状态**：网关维护 = 注册连接存活 **或** 隧道连接存活（任一存活即在线）。**loadDb 不继承 db 在线状态（2026-08-20）**：source=register 设备启动一律初始 offline，在线只能由活跃注册/心跳/隧道驱动——防止历史残留记录（重置换 UUID/停止注册）恒显在线；**控制端 App「已连接」判定同步收紧**：`TVNCAppStore.isRegistered` 需目录记录在线（online=true）才算已连接，db 残留记录不再误判。
- **鉴权**：内网可信，不设设备鉴权；网关保留 `FARM_TOKEN` 兜底。
- **初始化**：打开一次 App → 搜索网关（mDNS `_superphone-farm._tcp`）/手动输入 → 保存，之后全自动；Managed.plist 可预置网关免初始化。**2026-08-28 风控收敛后**：生产网关建议 `FARM_MDNS=0`（不广播不发现），设备端 `BonjourEnabled` 默认 NO 不广播——此时搜索网关不可用，初始化**手动填网关地址**（纪律见 `deploy/openwrt-*.md`「局域网暴露收敛」）。

---

## 3. 时序

### 3.0 生命周期全景与核心不变量（2026-08-22 统一到 RFB 定稿）

**核心不变量**（主路径正确性的骨架，任何违反都是缺陷）：

1. **采集惰性启动 + 启动后常驻只升降频**：采集不随服务启动，随「首个 RFB 客户端」启动，之后永不停止（除非服务退出），只切帧率档（`CaptureFps` ↔ `FrameRateSpec`）——根治 SIGILL 的根基
2. **统一到 RFB**：缩略图与屏幕流都是「RFB 客户端收 framebuffer」，共用同一采集、同一 framebuffer、同一 tile 脏矩形检测，无独立缩略图路径
3. **通道复用（proto:2，2026-08-23）**：单隧道多路 5901 连接——chan 0 缩略图通道常驻 + 会话通道按需开（LibVNCServer 原生多客户端 per-client 编码），缩略图与控制流**并存**，无连接轮换、无 rfb.start/stop
4. **隧道 = 注册生命周期**：18181 隧道常驻，断线只退避重连（2s→30s 翻倍封顶），不拆不重建
5. **脏矩形变化才发**：静止零带宽；锁屏/无变化留最后帧

> 主路径上任何「服务启动即采集」「空闲建独立缩略图路径」「反复启停采集」的行为都是错误的。

**生命周期状态机（S0→S3）**：

| 状态 | 采集 | 隧道 | 5901 | RFB 消费 | 保活在岗 |
|---|---|---|---|---|---|
| S0 冷启动 | 未启动 | — | — | — | 进程组 + Jetsam 就位 |
| S1 服务运行 · 无客户端 | 惰性未启动 | 未连接 | 监听（5801 可直连） | — | watchdog + Jetsam |
| S2 已注册 · 缩略图态（**默认态**） | @CaptureFps | 常驻 + 心跳 | 1 连接（chan 0 缩略图） | 网关解码缩略图 | 心跳 + 退避重连 |
| S3 屏幕流态 | @FrameRateSpec | 常驻（承载数据流） | 2+ 连接（chan 0 + 会话通道） | 缩略图暂停 + 转发 noVNC | KeepAlive 防息屏 + 知情通知 |

状态迁移：S0→S1 spawn 链启动服务；S1→S2 注册（18081）+ 隧道握手 → 网关开 chan 0（缩略图通道）→ 设备 connect 5901（首个 RFB 客户端）→ 采集惰性启动 @CaptureFps；S2→S3 点击卡片/5801 直连 → 网关开会话通道（设备再 connect 一条 5901）+ 升频 FrameRateSpec，缩略图通道暂停请求（连接保留）；S3→S2 退出控制 → 会话通道关闭 + 降频 CaptureFps，缩略图通道补发增量请求恢复。异常自愈不离开服务生命周期：网关挂/断网 → 隧道断 → 全部通道关闭、采集降频（静默）、server 崩溃 watchdog 重启重走 S1→S2、App 被杀独立进程组无感、锁屏留最后帧、系统重启半自动/launchd 恢复。

以下 3.1–3.5 为各段详细时序（本总纲的分节展开）。

### 3.1 注册与上线

```
设备开机/App 启动 → TRGatewayClient 连接网关 18081
  → 发送 register（deviceId/name/vncPort/configs/screen/httpPort）
  → 网关记录设备，回 ack
  → 设备收到 ack 后才启动 18181 隧道（TRTunnelClient，防重）
  → 网关设备墙出现该设备（在线）
  → 此后按 HeartbeatIntervalSec（默认 30s，5-300 可配）发 hello；3 个周期无心跳判离线；重连退避由设备侧拨号
```

### 3.2 隧道建立与握手（proto:2 通道复用）

```
设备 → 网关 18181 连接
  → 首包 {"type":"tunnel_hello","deviceId","token","proto":2}
  → 网关校验（proto 不匹配即拒绝，fail-fast）→ 回 tunnel_ack
  → 之后帧封装（FT_ 协议）：FT_PING 0x02 / FT_PONG 0x03 / FT_CMD 0x04 / FT_CMDACK 0x05 / FT_STATE 0x07
    + 通道帧：FT_CHAN_OPEN 0x08 / FT_CHAN_ACK 0x09 / FT_CHAN_DATA 0x0A / FT_CHAN_CLOSE 0x0B
  → 隧道握手成功 → 网关立即发 CHAN_OPEN(chan 0, thumb) → 设备 connect 本地 5901 + 主动写 RFB 003.008 → 回 CHAN_ACK(ok)
  → 网关收 ACK 后创建 ThumbRfbDecoder（onSend 封装为 chan 0 下行）→ 缩略图拉流开始
  → 按 HeartbeatIntervalSec（默认 30s，5-300 可配）发 PING；断线退避重连（2s→30s 翻倍封顶）
```

> 通道帧 payload：OPEN/CLOSE = `[chanId:2BE][kind/reason:1B]`，ACK = `[chanId:2BE][ok:1B]`，DATA = `[chanId:2BE][rfb字节]`。chan 0 固定缩略图，会话通道从 1 起网关单调分配。缩略图与屏幕流均经隧道透传 RFB 字节流（协议统一，无 FT_THUMB 自定义帧）；缩略图态=网关解码全尺寸 framebuffer，屏幕流态=转发 noVNC。

### 3.3 控制会话（通道生命周期 + 多客户端并存）

```
浏览器/控制端 → WS /ws/vnc/:id（8080）
  → 网关查 tunnels[deviceId]：无隧道 → ws.close(4003, 'no tunnel')；有 → 分配 chanId → 发 CHAN_OPEN(chanId, session)
  → 设备同步 connect 本地 5901（全新连接）+ 主动写 RFB 003.008 → 回 CHAN_ACK(ok)
  → 网关收到 ACK 才放行该通道缓冲的握手字节（跳过 12B 重复协议版本；3s 超时兜底强制放行）
  → connect 失败 → 显式 close 会话（4005 "画面服务不可用"，不静默黑屏）
  → 新客户端建立 → 采集升频 FrameRateSpec、弹控制知情通知；缩略图通道暂停请求（连接保留）
  → 握手透传（版本/安全类型/ServerInit）→ 帧数据 @ 目标帧率 → FBU 增量帧流 → noVNC 渲染
```

- **多客户端并存（proto:2）**：设备端维护通道表（chanId→5901 fd），select 多路复用；LibVNCServer 原生多客户端 per-client 编码，缩略图（chan 0）与控制流（会话通道）**同时存在**，无连接轮换、无 rfb.start/stop。viewOnly 与任何会话并存（原「新会话顶旧 viewOnly」互斥随通道化消失）。
- **会话退出**：网关发 CHAN_CLOSE(chanId) → 设备关对应 5901 fd；会话通道归零 → 缩略图通道补发增量请求恢复推送（连接未动，无重连竞态）。设备端 5901 EOF → 回 CHAN_CLOSE 通知网关（网关清理会话 / 2s 退避重开 chan 0）。
- **隧道断开**：网关关停全部 WS 会话（**4002** tunnel closed）；设备端通道 fd EOF 仅清理该通道，隧道保持常驻。
- **控制知情通知（2026-08-21）**：控制会话建立弹本地通知——隧道会话通道的连接 socket 层为 loopback（127.0.0.1），经 **1s 延迟 + `gClientStates` 条目存续**判断区分 mgmt 探测（cap.hello mgmt 握手后条目被移除 → 不弹）：条目仍在 = 活跃非 mgmt 控制会话 → 弹「远程控制已建立」；5801 直连等非 loopback 连接立即弹「客户端已连接（含来源 IP）」。Notifications=silent 不弹。
- **客户端类型（2026-08-22 统一到 RFB）**：缩略图客户端（chan 0）与控制客户端（会话通道 / 5801 直连）都连 5901，需按「客户端类型」区分——**缩略图客户端不触发 KeepAlive、不弹控制知情通知**；控制客户端才触发。二者都是 loopback，需额外机制标记（实现时定）。
- **noVNC 握手死锁修复（2026-08-23）**：`core/rfb.js` 的 `_negotiateProtocolVersion()` 结尾必须显式 `return true`——否则 `_handleMessage`（connecting 态 while 循环）在 `!_initMsg()` 时 break，**同一 WS message 中版本行之后的握手字节（LibVNCServer 3.8 安全类型列表 2B）永远不再被处理** → noVNC 卡在 `Security` 态（rQunread=2、hex=0101）、不发 SecurityType → 设备 5901 等客户端选安全类型 → 双方死锁 → 前端「连接中」10s 超时。粘包（14B 一包）必现、分片（12B+2B 两包）正常，表现为偶发 ~20% 失败且趋连发。网关与 5801 两处 noVNC 均已修复；**升级 noVNC 版本后必须核对该 return true 是否保留**。
- **被控状态上报（2026-08-22）**：设备端「控制客户端」建立/断开（0→1/1→0）时，经隧道推轻量状态消息给网关 `{ controlled, source }`（source=**5801** 非 loopback 直连 / **tunnel** 隧道会话通道）；网关 /api/devices 附带 `controlled` + `controlledSource` → 前端卡片叠加「被控制中」遮罩。仅状态翻转时推一次。
- **断开被控（2026-08-22）**：被控制中的设备（5801 直连 / 隧道），卡片墙**不显示画面**（停止缩略图获取，避免泄露被控画面）+ 遮罩「被控制中」（5801 来源显示「被 5801 控制中」）+ 隐藏 hover 引导提示；点击卡片 → 浮层「断开 / 接管」→ 网关 `POST /api/devices/:id/disconnect` → 设备端 `clients.disconnect` `{clientId:"REMOTE"}`（**先快照再迭代器外 rfbCloseClient**，断开所有非 loopback 客户端——2026-08-22 修复迭代器中关闭导致 SIGSEGV 崩溃；隧道 loopback 保留）→ 5801 noVNC 断开弹「连接已被网关断开」→ 设备端上报 controlled=false → 前端恢复缩略图并进入控制（隧道控制由 enterFocus 顶掉会话）。
- **互斥补全：双向单控制者仲裁（2026-08-23）**：原互斥仅「5801 控制时网关被禁」（前端遮罩 + 断开/接管），**网关控制时 5801 访问无感知** → 双控制端并存。现补全**双向**（跨进程 notify，两进程独立可执行文件，无共享状态，各端自查）：
  - **方向1（5801 接管踢隧道，已实现）**：`newClientHook` 非 loopback 直连 → notify「tunnel-kick-sessions」→ TRTunnelClient（trollvncmanager 进程）置标志 → 隧道 worker 线程关全部会话通道（`_closeChannel` reason=**3**=5801 接管）→ 网关 CHAN_CLOSE 会话分支按 reason 关前端 WS（3→4001、1→4006）→ 5801 成为唯一控制者 → control-active 上报网关显示「被 5801 控制中」。
  - **方向2（隧道接管踢 5801）**：TRTunnelClient CHAN_OPEN(kind=session) → notify「tunnel-kick-remote」→ trollvncserver 监听（main queue）→ `tvDisconnectRemoteClients()`（断开全部非 loopback，复用 clients.disconnect 同路径；依赖 libvncserver rfbClientIterator/rfbCloseClient 内部锁）→ 5801 断开弹「连接已被网关断开」→ control-idle → 网关解除被控。
  - 踢动作无条件发起、对方无对应控制者则 no-op（幂等）；mgmt 探测（loopback）不受影响。CHAN_CLOSE reason 语义：0=网关主动、1=本地 EOF(4006)、2=错误、3=5801 接管(4001)。**网关被接管后前端行为**：enterFocus 的 disconnect 收到 4001 → toast + 自动 exitFocus 返回卡片墙；卡片墙经 state 事件 → refreshDevices 显示「被 5801 控制中」遮罩。**noVNC disconnect code 透传（2026-08-23）**：noVNC 原版 disconnect 事件 detail 仅 `{clean}` 无 code，前端 `e.detail.code` 恒 undefined → 所有断开码文案（4001/4003/4005/4006）与 4001 自动退出全部失效——已在 `_socketClose` 存 `_lastCloseCode/_lastCloseReason` 并在 disconnected dispatch 透传（网关与 5801 两处 noVNC 均已 patch）。

### 3.4 命令通道（REST / WS / 隧道）

```
前端 → POST /api/devices/:id/invoke|configs|restart|ping（或 /api/devices/batch/*）
  → 网关 sendDeviceCmd：隧道 FT_CMD 帧优先，注册通道 JSON 行回退
  → 设备 TRGatewayClient → TRCapabilityRegistry.invoke/setConfig → executor 执行 → ACK
  → 网关回调用端（默认 5s 等待 ack，可传 timeout；超时/离线 504）
```

命令字：`invoke`（cap/params）/ `set`（key/value，回 reload 策略）/ `restart`（watchdog）/ `ping`。**无通用 /command 端点**（已删）；rfb.start/rfb.stop 已随 proto:2 通道化移除（2026-08-23，会话生命周期由 CHAN_OPEN/CLOSE 驱动）。

### 3.5 卡片墙缩略图（缩略图态 · 统一到 RFB）

缩略图是**缩略图态（S2）下的 RFB framebuffer 流**，与屏幕流共用同一采集、同一 framebuffer、同一 tile 脏矩形检测，唯一区别是帧率（CaptureFps）与消费方（网关解码缩略图）。**无独立缩略图路径**（无 FT_THUMB、无 pHash、无独立 JPEG 编码、无 manager 轮询）。

```
1. 隧道握手成功 → 网关发 CHAN_OPEN(chan 0) → 设备 connect 本地 5901（缩略图客户端 = 首个 RFB 客户端）
2. newClientHook → 采集惰性启动 @ CaptureFps（默认 10fps，1-30 可配）
3. 采集帧 → framebuffer → tile 脏矩形检测 → 变化才标脏（rfbMarkRectAsModified）
4. libvncserver 编码脏矩形 → 5901 → 设备通道表 chan 0 → 隧道 CHAN_DATA(0) → 网关
5. 网关 ThumbRfbDecoder 解码 RFB 流 → 全尺寸 framebuffer → 缩略图（前端 CSS 缩放）→ 卡片墙渲染
6. 静止 → 脏矩形无变化 → 零带宽；锁屏 → 留最后帧
```

> 屏幕流态（S3）时，会话通道存在期间缩略图通道**暂停请求**（RFB 拉模型——不发 FramebufferUpdateRequest 设备即不推帧，连接/握手状态完整保留，零流量）；会话通道全断后补发一个增量请求即恢复推送（无重连、无竞态）。卡片墙缩略图经 chan 0 解码显示；点击卡片进入大屏 = 新增会话通道（缩略图通道保留）。

**卡片横/竖屏显示（2026-08-19）**：卡片墙默认统一比例（所有卡片一样大，画面 contain 完整显示）；卡片「⋯」菜单提供**旋转**按钮（旋转箭头图标，按设备持久化 localStorage `farm_tile_orient`）——横屏显示 = 该设备 screen 比例倒置（inline `--tile-pb` 覆盖全局）+ 缩略图旋转铺满横容器，恢复 = 跟随全局统一比例。仅卡片视图生效（列表视图固定行高）。**聚焦画面独立于此偏好**：始终用设备实时帧尺寸（`fitFocusPanel` 读 `rfb._fb_width/_fb_height`）自动适配方向——设备横屏点进去即横屏画布，竖屏即竖屏画布。

### 3.6 键盘输入（方案 C）

```
英文/数字键：前端 Shift 状态跟踪（需 Shift 且未持有 → Shift_L↓；不需要且持有 → 抬）
  → 字符键 down + 50ms + up 直发（RFB 5901）
  → 停止输入 400ms 空闲自动释放 Shift
设备端 keyDown/keyUp 直接映射，不补 Shift
中文/emoji：type.paste（写被控端剪贴板 + releaseEveryKeys + 模拟 Cmd+V）
特殊键（删除/回车）：down + 60ms + up，发送前释放 Shift
媒体/系统键（2026-08-18 补全）：设备端 kbdAddEvent 按 keysym 拦截（音量/亮度/静音/搜索/键盘/Home/截屏等），媒体类（Play/Pause/Stop/Prev/Next/快进/快退/Eject）经 Consumer Page 注入，均不落 keyDown/keyUp；物理键盘经 noVNC 发送 XF86 keysym 即可触发。映射表在 trollvncserver.mm kbdAddEvent（列表类信息不复制进文档）
```

**入口形态**：方案 C 分流通道仅触控端使用（kbdInput 的 input/composition/keydown 事件，「键盘」键显式调起软键盘）；PC 端键盘走 noVNC 原生映射（点画布 canvas 聚焦 → keydown → keysym 直发，原生支持全键含方向键），中文经 Ctrl+V 显式粘贴。2026-08-19 曾试 PC 浮层接管（connect 自动 focus kbdInput + focusOnClick=false），实测点画面后焦点被浏览器 mousedown 默认行为转移给 canvas，浮层与原生两路径混走行为不定，已回退——勿在无焦点锁定方案前重试。**PC 硬键盘删除键长按（2026-08-23，网关 + 5801 两端同步）**：noVNC 原生把浏览器 repeat keydown 转成「重复 down 无 up」→ 设备端连续注入 keyDown 无 keyUp、长按删除行为不定——两端前端均 capture 阶段拦截 Backspace/Delete 改走 kbdSendSpecial 完整 tap（软键盘删除同款通道；释放延迟 0ms，因 PC repeat 约 30ms，60ms 会与下次 down 重叠），repeat 自动连发 → 长按连续删除，软键盘/硬键盘语义一致；修饰组合（Ctrl+Backspace 删整词等）与输入框焦点放行 noVNC 原生。

---

## 4. 实现要点

### 4.1 前端契约（caps.js，唯一真相源）

| 定义 | 数量 | 说明 |
|---|---|---|
| `KEY_DEFS` | **10 键** | power/home/volup/mute/voldn/briup/bridn/snapshot/spotlight/keyboard；RFB ks/code 直发（power 走 ptr:2 中键）；keysym：Home=0xff50、音量 XF86 0x1008ff13/11/12、亮度 0x1008ff03/02、snapshot 0x1008ff80、spotlight 0x1008ff1d、keyboard 0x1008ff2e |
| `BATCH_CAPS` | **20 项** | 批量菜单（hid 硬件按键 17 + service.restart + settings.generateKeys/searchGateway）；按 category 分组。**data.fill/data.clear 已去除（2026-08-26 直控 App UI，22→20）** |
| `CONFIG_DEFS` | **28 项** | 配置表单（与设备端 `_registerConfigSchemas` 对齐）；Port/HttpPort/GatewayPort 不在内（端口固定）；ClipboardEnabled 已移除（2026-08-17）、ServerCursor 已移除（2026-08-18）、HIDKeepAliveInterval/WheelTuning/FabCollapseMs/SslCertFile/SslKeyFile/HttpDir/Enabled/WatchdogKeepAlive 已移除（2026-08-20）、**BonjourEnabled/ViewOnlyPassword UI 移除（2026-08-21，设备端保留注册）**——BonjourEnabled 默认值 2026-08-28 YES→NO（风控收敛，见 §7）；新增 CaptureFps/HeartbeatIntervalSec（2026-08-21）；ThumbPushEnabled/ThumbInterval 移除（2026-08-22，缩略图统一到 RFB）；**locsim 定位板块 5 项（SimLocationMode/Lat/Lon/Accuracy/Speed）已移除（2026-08-26 直控 App UI，33→28）** |
| API 封装 | — | `invokeCap`/`setConfigs`/`batchInvoke`/`batchSetConfigs`/`batchRestart`/`groupByCategory` |
| `GESTURE_DEFS` | **3 项** | 画布多点手势（pinch/twotap/threetap → `touch.pinch`/`twoFingerTap`/`threeFingerTap`，坐标画布内 0-1 归一化）；server patch 的 rfb.js 派发 `farmgesture` 事件，`gesture.js` `resolveGesture` 译为 invoke |

**增删改查规则**：增删按键/批量项/配置项 → 只改 `caps.js`（设备端如需新 executor 同步注册），文档不复制清单。

### 4.2 按压语义（press.js）

- click：短按立即发送，零延迟；long：800ms；double/triple：仅 events 声明才启用 300ms 窗口（现状按键区无，批量入口显式能力保留）——无声明字段的按键双击/三击 = 自然连点（每次点击独立立即发送，iOS 系统层识别）。
- down/up 按压式按键（音量/亮度/静音）：按下触发 down、松开触发 up，**不补 click**（防一次短按注入三次导致设备端双响应）。
- pointercancel（滚动/来电打断）复位状态，不触发计数。

### 4.3 配置与热重载

- 配置来源：设置页（NSUserDefaults）/ Managed.plist 预置 / 网关 configs 命令通道；5801/5901 不承载配置 API。
- **2026-08-28 BonjourEnabled 默认值反转（局域网暴露收敛）**：默认 YES→NO（内建默认 `gBonjourEnabled`、`_defaultForKey` 回退、重置默认值写回三处同步改 NO）——mDNS publish 即广播，默认不发布不可见（风控收敛，见 §7 mDNS 条）；显式开启通道保留（配置 set / CLI `-B on`，restart 级生效）。2026-08-20 重置默认值事故修复时写回 YES 是当时的「安全默认」，本次为**刻意的策略反转**（保留身份/网络信息写回逻辑不变，仅 Bonjour 位翻转），勿当回归改回。
- reload 语义：`instant`（写入即生效，如 ViewOnly/Notifications）/ `hot`（tvReloadConfigForKey 热更新，如 FrameRateSpec/Watchdog*/CaptureFps）/ `gateway`（触发重新 register，如 GatewayHost/GatewayToken/HeartbeatIntervalSec）/ `restart`（watchdog 重启，如密码/端口类/BindHost/Scale/OrientationPadFix/TileSize/MaxRects/AsyncSwap/BonjourEnabled/PerformanceMode——2026-08-20 调整：Scale/OrientationPadFix 重建 framebuffer 不能热重载改 restart 级；BonjourEnabled 为 server 启动期标志改 restart 级；PerformanceMode 预设含 restart 级底层参数；ConnectionMode/FrameRateSpec/Notifications 不再触发重启——分别走 3s 轮询/热重载/instant）。**设置页热重载通道（2026-08-20）**：App 写 defaults 后 `notify_post("com.82flex.trollvnc.prefs-changed")` → trollvncserver `tvApplyPrefsChanged` 重读 suite 并热重载 hot/instant 配置（帧率/通知/自然滚动/辅助触控/键盘日志等即时生效不重启；`tvReloadConfigForKey` 已改按 suite 读取——原 standardUserDefaults 无 bundle 进程读不到设置页值）；**manager 双通知自治（2026-08-20 二期，替代 kill 通路）**：App（mobile 沙盒）对 root 进程 kill 恒 EPERM，旧 `_scheduleManagerRestart`「枚举+kill trollvncmanager」通路从未真正生效。现 manager 订阅两条跨进程通知（名字面量与 trollvncserver.mm/TVNCUtil.h 约定一致）：①`prefs-changed`（设置页每次写入必发）→ 桥接模式自退（CFRunLoopStop 复用 SIGHUP 清理路径：watchdog 停止→等子进程退出）/ 通知 TRGatewayClient `noteExternalPrefsChanged` 重发 register（网关 host 变更由 worker 超时分支比对 connectedHost 主动断开重连——旧 reregister 只往当前 fd 重发会把注册发到旧网关）/ watchdog 节流与退出超时热调（即时生效，不再 manager 重启级；双域读取 `tvManagerReadPref` 保证 mobile 域设置页写入可见）；②`restart-service`（restart 级配置生效）→ watchdog 重启 trollvncserver（root 杀 root，App 侧唯一可行路径），`TVNCRestartVNCService()` 已改 `notify_post`。gateway 配置不再触发 manager 重启（env 注入已删——spawn 时刻冻结的 TVNC_GATEWAY_HOST 会永久遮蔽设置页后续修改；GatewayHost/GatewayToken 走 reregister+host 比对重连）。**2026-08-20 根因修复（重置默认值事故）**：`_reallyResetDefaults` 原为 `removePersistentDomainForName` 删空整个配置域 → 网关地址/UUID/watchdog 配置全清 → manager 无网关地址不注册（设备失联）+ watchdog 回退 0 节流（服务重启风暴）+ App「已连接」残留记录误判。修复：重置仅恢复功能默认，**保留 DeviceUUID/网关地址/令牌**并写回安全默认（ConnectionMode=relay、BonjourEnabled=YES、WatchdogThrottleInterval=60、WatchdogExitTimeout=3）；`TRCapabilityRegistry` number 默认回退补 watchdog 60/3（不可回退 0）；**manager 读网关配置兜底 mobile 用户域**（App 设置页写 /var/mobile 域、root 进程读不到 → 兜底读 `/var/mobile/Library/Preferences/com.82flex.trollvnc.plist`，与 TVNCReadSelfDeviceId 同款双域读取）；**协调器探活纯端口判定**（App 沙盒内 KERN_PROCARGS2 读 root 进程 EPERM，进程枚举会误判，故只用 46751 端口探活——2026-08-20 三修）**+ `openLocalDummyService` 加 FD_CLOEXEC**（子进程不继承 46751，孤儿不再欺骗探活）+ **manager 启动时用 sysctl 清理残留孤儿 trollvncserver**（root 可靠枚举，释放 5901/5801/5802 防新 server bind 失败）。
- **设置页结构（2026-08-21 定稿，Root.plist）**：按能力板块分组 = **连接 / 直连 / 画面 / 交互 / 保活 / 关于** 6 组 + 页面底部无分组（查看日志/UDID/版本信息）；连接/直连/画面/交互/保活仅中继模式显示（visibleOnlyRelay）。**连接**：模式选择器（ConnectionMode 网关中继/桥接控制分段，TVNCSegmentCell）——中继=本机注册+隧道（可被控制）、桥接=仅作控制端连网关不注册（bridge 下 `TVNCServiceCoordinator` 不 spawn，切换时 trollvncmanager 收 prefs-changed 通知自退；AppStore `isBridgeMode` 网关可达即 `BridgeConnected` 绿点）+ 网关地址（GatewayHost）/网关令牌（GatewayToken）+ 全宽按钮（TVNCButtonCell，UIButton filled 圆角实色+居中白字）——中继=「连接网关」（visibleOnlyRelay）、桥接=「桥接网关」（visibleOnlyBridge），均 action=connectGateway 幂等 ensure 语义（bridge 纯 App 级 fetchDevices 验证网关可达并反馈设备数，不碰进程；relay 按构建上下文 `#ifdef THEBOOTSTRAP` 分派——App 走 ensureServiceRunning（manager 死则立即 spawn，活则交给 prefs-changed 自治），prefs bundle（越狱设置页，无 spawn root 能力）退化为 fetchDevices 验证可达，均无 kill 重启）。**直连**（仅中继）：直连地址（BindHost）/被控密码（FullPassword）/生成证书按钮（generateKeys）/只读开关（ViewOnly）。**画面**（仅中继，按链路顺序：采集→推流）：采集帧率（CaptureFps 1-30 默认 10）/推流帧率（FrameRateSpec 五档 15/30/60/动态=15-60 范围自适应/自定义——hot 级经热重载通道即时生效，选自定义显示 FrameRateSpecCustom 数值输入 1-240，设备端 `FrameRateSpec=custom` 时经 `TVResolvedFrameRateSpec` 回退读取）/方向同步（OrientationSync）/方向偏移（OrientationPadFix）/推流画质（PerformanceMode 流畅/智能/画质/自定义——设置页改模式按预设写底层参数，与 TRCapabilityRegistry setConfig 对称）/输出缩放（Scale 0.25-1.0）/**进阶**（collapseGroup=performance 折叠）：延迟窗口（DeferWindowSec）/最大并行帧（MaxInflight）；仅「自定义」显示 6 项底层传输参数（TileSize●/MaxRects●/FullscreenThresholdPercent/AsyncSwap●/DeferWindowSec/MaxInflight，`visibleOnlyCustom`；默认值与 balanced 预设对齐：MaxRects 512、FullscreenThresholdPercent 50）。**交互**（仅中继）：自然滚动（NaturalScroll）/滚轮步进（WheelStepPx）/辅助触控（AutoAssistEnabled）/修饰键映射（ModifierMap）/键盘日志（KeyLogging）。**保活**（仅中继）：通知模式（Notifications 全部通知/仅连接/静默）/防息屏间隔（KeepAliveSec 0-300 默认 30）/网关心跳间隔（HeartbeatIntervalSec 5-300 默认 30）。**关于**：重置默认设置/重启服务/重启节流间隔（WatchdogThrottleInterval 默认 60）/退出超时（WatchdogExitTimeout 默认 3）。显示名对齐：FullPassword→被控密码、BindHost→直连地址、KeepAliveSec→防息屏间隔。需重启项 label 带 ● 标记。动态显隐由控制器 `_visibleSpecifiersFrom:` + `setPreferenceValue:specifier:` 联动（ConnectionMode/PerformanceMode/FrameRateSpec 变更即刷新）。**2026-08-21 配置治理**：CONFIG_DEFS 29→30——移除 BonjourEnabled/ViewOnlyPassword UI（设备端保留注册）、新增 CaptureFps/HeartbeatIntervalSec；2026-08-22 统一到 RFB 移除 ThumbPushEnabled/ThumbInterval（30→28）；2026-08-20 已删 8 项（HIDKeepAliveInterval/WheelTuning/FabCollapseMs/SslCertFile/SslKeyFile/HttpDir/Enabled/WatchdogKeepAlive），证书自动生成内部链路（SslCertFile/SslKeyFile defaults 存储）、CLI `-httpdir`/`--wheel-options` 通道保留；Notifications/WatchdogThrottleInterval/WatchdogExitTimeout 为 2026-08-20 补齐项，Enabled/WatchdogKeepAlive 不暴露（App 启动即自动启用、底层已实现保活）。
- 密码安全：FullPassword/ViewOnlyPassword 不上报明文，仅 hasPassword 布尔。
- **采集生命周期与稳定性（2026-08-21 四连修 + 二期架构升级）**：①**mgmt 豁免撤销副作用**——`cap.hello(mgmt:true)` 豁免此前仅减 gClientCount/移状态，不停采集——网关/能力探测连接把计数瞬时抬 1 又降 0，采集（CADisplayLink）在 0 客户端下持续空转渲染（空耗+高频踩渲染路径，崩溃循环帮凶）；现豁免路径完整撤销 newClientHook 副作用（归零时降频/停 KeepAlive/恢复 AutoAssist），`clientGoneHook` wasMgmt 分支同步双保险兜底。②**TRTask FD 泄漏**——runLoopSource 的 context retain task、task 又强持 source（环），唯一破环点 `waitUntilExit()` 而 TRWatchDog 走 terminationHandler 路径永不经过 → 每次重启泄漏 task 及其 3 个日志 NSFileHandle（closeOnDealloc），数百次后 fd≥1024 → 子进程 select() EINVAL/EXC_GUARD 秒崩；`terminateRunLoop()` 补 `runLoopSource=nil` 破环（与 waitUntilExit 清理语义对齐）。③**watchdog 节流默认 5s→60s**（manager 内置默认/CONFIG_DEFS 回退/Root.plist/重置默认写回四处同步）——崩溃循环时 5s 重启放大故障，60s 压平风暴。④**Scale 下限 0.1→0.25**（Root.plist 滑条/设备端 prefs 钳制/CLI `-s`/CONFIG_DEFS 两端同步）——极小缩放（0.1 生成 ~76x135 缓冲）接收全尺寸 CARenderServerRenderDisplay 渲染实测必崩（SIGILL in renderDisplayToScreenSurface）。⑤**采集架构升级（客户端解耦 → 惰性启动）**：采集从「有客户端（gClientCount>0）才启动」改为「隧道握手成功（tunnel-connected 通知）才惰性启动 @ CaptureFps（1-30 默认 10fps）」——newClientHook 不再启停采集只升频（FrameRateSpec 的 min/pref/max），clientGoneHook/cap.hello 豁免归零降回 CaptureFps；采集写入/RFB 发送与转屏重建 framebuffer（`maybeResizeFramebufferForRotation`）统一用 **gFramebufferLock** 互斥（方向变化竞态 SIGILL 根治，见下）。
- **隧道握手竞态根因（2026-08-21 崩溃报告+源码定案）**：设备端 5901 握手窗口极窄（实测 0-50ms 抖动——客户端 connect 后必须立即发回协议版本，否则服务端主动关闭 EOF）。隧道链路 noVNC 协议版本经「网关 rfb.start ack → 放行 → 隧道」到达有毫秒级延迟，间歇性超窗口 → 隧道 EOF → noVNC 黑屏。**修复**：①TRTunnelClient rfb.start connect 后**立即主动写固定协议版本** `RFB 003.008\n` 到本地 5901（绕过网关往返）；②网关 ack/兜底放行时**跳过 pendingUp 前 12 字节协议版本**（以 `RFB 003.` 开头时），避免重复写入；③缓冲窗口恢复 pendingUpUntil=+3000（保证 noVNC 版本不在 connect 前后产生重复竞态）。**采集 SIGILL 隐患（同源竞态，方向变化触发）**：`handleFramebuffer`（采集回调 main 线程）每帧调 `maybeResizeFramebufferForRotation`——方向变化致尺寸变化时 free 旧 buffer + rfbNewFramebuffer 重建，与 rfbRunEventLoop 线程读写 framebuffer 并发 → use-after-free → SIGILL（崩溃报告：EXC_BAD_INSTRUCTION + Address size fault in CADisplayLink dispatch）。`4e0aee4` 先堵热重载通道；**2026-08-21 二期补齐**：`maybeResizeFramebufferForRotation` 重建段与采集写入/RFB 发送统一持 **gFramebufferLock** 互斥，方向变化竞态已根治。

### 4.4 触控与指针（RFB PointerEvent 通道）

- **唯一触摸通道**：客户端经 RFB PointerEvent 发送帧缓冲像素坐标（非 0-1 归一化）→ `trollvncserver.mm` `ptrAddEvent` 按 buttonMask 分派：左键=触摸注入、中键=电源键、右键=Home、滚轮=单指拖动模拟（`dragLinear`）。
- **鼠标右键 = Home（2026-08-19 前端统一）**：控制台与 5801 直连页画布以 capture 阶段拦截 noVNC 原生右键（右键掩码 0x4 不再发送），统一改走 Home 键 RFB 直发（`sendKey(0xff50,'Home')`，60ms 按住）——与按键区 Home 键按压语义完全一致：单击立即发、双击/三击=自然连点由被控端系统层识别（无 300ms 判定延迟）。设备端 `ptrAddEvent` 的 `buttonMask&4`（右键=Home）分支保留供兼容，前端不再触发。
- **坐标映射**：`vncPointToDevicePoint(x, y)` 把 VNC 帧缓冲坐标（经 Scale/OrientationSync/OrientationPadFix 处理）逆映射回设备竖屏原生坐标，再走 IOHID 注入；App 锁定竖屏。
- 触控不进菜单，画布直操。多点触控/手势/捏合/事件流（2026-08-16 恢复）：`touch.*` 18 项 executor 走 invoke 通道（命令式 tap/doubleTap/twoFingerTap/threeFingerTap/longPress/swipe/curveSwipe/taps/pinch + 流式 down/up/downMulti/upMulti/downMultiAt/upMultiAt + reset + 事件流 event/eventStream），参数 0-1 归一化、设备端 `_norm*` 转屏幕物理像素、越界/缺参报错不执行；前端聚焦画布经 noVNC 手势识别（server patch 的 rfb.js 把 pinch/twotap/threetap 改为派发 `farmgesture` 事件）→ `GESTURE_DEFS` 定义 → `gesture.js` `resolveGesture` 译为 touch.* 调用（两指轻点原=右键/Home、三指轻点原=中键/电源、捏合原=Ctrl+wheel 已随此变更语义）。捏合为命令式——手势结束一次性执行，非实时跟手（实时流式多点 move 留待后续）。

- **HumanizeTouch 行为整形（2026-08-28 风控对抗基线）**：抖音自研行为分析签名（X-Argus）做行为层建模，精确坐标+固定时长的机械 tap 是序列模型最优特征——注入类 `STHIDEventGenerator` 加 `humanizeEnabled` **粘性开关**（默认 NO，**不做逐调用切换**——画布 RFB 线程与 5802/异步注入并发，共享 flag 前后 toggle 会互相污染），两个自动化入口置 YES（注册表 `_registerTouchCapabilities`、5802 touch.tap/swipe handler），置位后**高层手势方法内部**整形：tap/doubleTap/sendTaps（touch.taps）/longPress/dragLinear/dragCurve + touch.down/up（一致性接触偏移：down 选定、up 复用 ±1.5px 微滑移）+ touch.eventStream（整条流一个一致性偏移保持相对几何 + 触点微噪声；down 在册时复用同一偏移）。
  - **整形参数（增强对抗：均匀分布/零记忆随机本身是机器签名）**：①Fitts 偏置空间误差——σ=clamp(0.12×距上次触点距离, 2, 12)px、接近轴 1.4×/垂直轴 0.7× 各向异性；②按压时长对数正态（中位 90ms、σ 0.35，钳 45~220ms）；③连点间隔——双击走 80-160ms 安全窗（系统双击窗 ~250ms）、连点重尾分布中位 280ms（15-500ms 钳制）、自定义 delay ×0.75~1.5 对数抖动、6% 概率偶发犹豫 +0.4-0.9s；④拖动 minimum-jerk 速度剖面（10τ³−15τ⁴+6τ⁵，速度峰 ~50%）+ OU 平滑噪声（θ=8/s、稳态 std≈0.8px——逐点独立白噪声抖动同样是序列特征）+ 时长 ×0.75~1.5 对数抖动；⑤down→up 微滑移 ±1.5px。
  - **永不整形（真人画布红线）**：ptrAddEvent 路径（`touchDownAtPoints:`/`liftUpAtPoints:`/`_updateTouchPoints:` 裸原语）、GESTURE_DEFS 画布手势消费方（`twoFingerTap`/`threeFingerTap`/`pinchLinearInBounds:`——真人画布操作经 invoke 进入，注入类内部豁免）、滚轮模拟（`wheelFlush` 改走 `dragLinearPlainWithStartPoint:` 显式裸拖变体）。
  - **边界**：downMulti/downMultiAt/upMultiAt 显式多点原语不整形；长按整形时长 1.1~2.8s（>系统判定窗，语义安全）；整形落点钳制屏内。

### 4.14 换号身份锚清理（identity.reset，2026-08-28）

> 背景：抖音把身份标识（device_id/install_id/OpenUDID 类）经 SAMKeychain 写 Keychain，跨重装持久（逆向报告 §5 实证）——换号不清会跨号关联。**执行体 = root daemon**（App 为 mobile uid 无权限直写 keychain-2.db）；App 设置页「清理残留」按钮经 127.0.0.1:5802 本地通道触发，与脚本通道共用同一底层。

**契约**（三处同 op 名）：`identity.reset` `{bundleId, mode:"dryrun"|"commit"}` → `{ok, bundleId, mode, tier, secitem{}, keychain{}, container{}, warnings[], sop[]}`；tier = secitem / keychain-db / container / sop / none（dryrun 恒 none）——如实上报实际执行的层。

**分层降级**（`TRIdentityReset.mm`，manager + server 双 target 编译同一实现）：
1. **secitem**：SecItem 按 access group 定向删——受自身 keychain-access-groups 限制预期被拒（errSecMissingEntitlement），成本≈0 保留尝试；
2. **keychain-db**：root 直写 `/var/private/var/keychains/keychain-2.db`（entitlements `system-keychain`+`storage.Keychains` 已核实）——**删前整库备份**（`.bak-<ts>`），agrp 白名单严格匹配（`agrp = bundleId` 或 `agrp 以 "."+bundleId 结尾`），genp/inet 两表参数化 DELETE + `wal_checkpoint(TRUNCATE)`；**NULL agrp/其它 App/app group 项绝不触碰**；
3. **container**：扫 `/var/mobile/Containers/Data/Application/*` 读 MCM 元数据定位容器 → 删 `Library/Preferences/<bundleId>.plist`（NSUserDefaults 层）；
4. **sop**：无可清项返回人工指引（卸载重装 + 重置广告标识符；IDFA 无 root 自动化路径，如实写明）。

**红线与边界**：绝不 kill securityd（重启失败=全系统 Keychain 不可用）；securityd 缓存可能需设备重启才完全生效——作 warning 返回，不代为重启；iOS 卸载默认保留 Keychain；app group 共享锚不在覆盖范围。

> **调研前置（2026-08-28 优化评估立项）**：若能用系统回调通知 securityd 重读 keychain-2.db（如私有 notify 名，**不 kill**），换号流程可从「重启设备生效」升级为「即时生效」（批量农场免串行重启、不打断隧道/任务）。**实施前必须真机逆向确认通知名与 iOS 版本差异，验证失败则维持现状并在换号 SOP 中写明重启代价**——勿在未验证前实现。

**设置页入口**（App-only，THEBOOTSTRAP 守卫）：「关于」组「清理残留」按钮（重置默认设置上方，isDestructive）→ 5802 app.list 选目标 → 破坏性确认 → commit → tier/warnings 结果弹窗；dry-run 仅保留在能力契约供脚本预览，UI 直达 commit。prefs bundle 上下文显示引导提示（BRPickerView 不在该 target 编译）。**线程纪律（2026-08-28 真机「点击清理残留即崩」修复）**：`_identityPost` 的 NSURLSession completionHandler 在后台队列执行，调用方全是 UI——必须统一 `dispatch_async(main_queue)` 再回调 completion，否则弹选择器/弹窗在后台线程操作 UIKit 必崩。

### 4.15 相册媒体 GPS 包装（album.import gps 参数，2026-08-28）

> 背景（逆向实证）：抖音 TMMediaGPSReadingCache 会读取**相册照片/视频的 EXIF GPS**（抖音iOS逆向分析报告.md §4）——上传到设备的媒体若带异地/真实位置的 EXIF，会与当前模拟定位冲突（穿帮）。本改造让相册里的媒体"生于"所模拟的位置。

**契约**（5802 独有，album.import 新增参数）：

| 参数 | 类型 | 语义 |
|------|------|------|
| data / filename | string | 不变（base64 原数据 + 文件名定类型） |
| gps（可选） | 缺省 = auto / "keep" / {lat, lon} | 缺省：读 SimLocationMode/Lat/Lon（kTRAppPrefsSuiteName）——有模拟（mode≠off 且坐标非 0,0）→ 写目标坐标；off/0,0 → **清除 GPS**（宁无位置不露真实）；keep → 原样保留；{lat,lon} → 显式坐标覆盖 |

**响应**：新增 gps 字段 {mode: "stamped"|"cleared"|"kept", lat?, lon?}；**写入失败返回明确错误**（归零原则——绝不静默"包装失败但假装成功"，编排可感知并降级 gps=keep）。

**实现**（TRMediaGeoStamper.mm，仅 trollvncserver 编译；Makefile 已加 ImageIO/AVFoundation/MobileCoreServices 框架）：
- **图片**（JPEG/HEIC/PNG）：ImageIO CGImageSource→CGImageDestination（AddImageFromSource 透传原编码，**无损**）重写 kCGImagePropertyGPSDictionary（含 Lat/Lon/Altitude/时间戳校准到 Now，避免 GPSDateStamp 与拍摄时间错位露馅）；Clear 模式删除 GPS 字典 + 清理 Exif 内残留 GPS 子键
- **视频**（MP4/MOV）：~~AVAssetReader/Writer 逐轨拷贝~~（2026-08-28 用户评审弃用：低效且三次真机坑）→ **AVAssetExportSession presetPassthrough + session.metadata**（系统 remux 不重编码、画质无损、无 sample 级循环）+ ftyp 嗅探选输出容器（isom/mp42→MPEG4；qt  →QuickTimeMovie）
- 坐标校验：lat±90 / lon±180 越界拒绝

**来源归零（同日实锤闭环）**：抖音可读相册资产「来源」标签（真机实锤存储于 `ZADDITIONALASSETATTRIBUTES` 表：ZIMPORTEDBYDISPLAYNAME="SuperPhone" / ZIMPORTEDBYBUNDLEIDENTIFIER="com.82flex.TrollVNCApp" / ZIMPORTEDBY / ZORIGINALFILENAME=album_import_*）。对抗三层：
1. `album.import` 成功后 **1s 自动归零**（清最近资产四字段 + wal checkpoint）——新导入不留指纹
2. `album.zero` {mode:dryrun\|commit, limit}——**批量清历史指纹**（仅清 com.82flex.TrollVNCApp 来源，不误伤 SpringBoard 截图等正常来源；.bak 备份内建；位置列 -180 的行补写模拟坐标）
3. `album.diag`——只读探针：ZASSET 全列清单 + 最近资产来源/位置列值 + 文件 XMP 摘要（证据工具，长期保留）

**红线**：不写真实坐标；不触相册授权逻辑（沿用 2026-08-23 不请求直写 entitlement 路径）；PNG 依赖系统 eXIf 支持、失败即报错不降级；无模拟时默认清除 GPS 是防真实位置泄露的主防线。

### 4.16 定位对抗编排（系统定位总开关联动 + 崩溃三层防御，2026-08-28）

> 目标（用户定案）：**模拟开启后永不跳真实定位**——每个定位信号要么是模拟值、要么是无值，绝不出现真实值。

**编排状态机**（SimLocationController + SimLocationManager 私有 `CLLocationManager.setLocationServicesEnabled:`，LocationServicesSwitcher 开源先例 + classdump 14.5/17 头实证）：

| 场景 | 编排顺序 |
|------|---------|
| 开模拟（anchor/itinerary） | 首点注入进 locationd 会话 → **才开系统定位**（定位开启第一秒即模拟值，无真实坐标空窗） |
| 关模拟（off/未知模式） | **先关系统定位** → 再停注入（宁无位置不漏真实） |
| 会话内失效（anchor lost） | **直接重注入，不翻开关**（毫秒级空窗；开关翻转本身是可观测事件，频翻反而异常） |
| manager 崩溃 | server 死亡哨兵即时关定位 → launchd 拉起新 manager → _forceStopOnStartup 再次兜底 |
| 设备重启 | manager 开机即启 → 无条件关系统定位（复位）→ 用户 App 显式开启 → 注入后重开 |

**崩溃三层防御**（模拟会话 client=manager；其崩溃后 locationd 会话行为未证实，故三管齐下）：
1. **L3 场景持久化**（实验性）：`loadPersistedScenario:`（GPX 场景文件 → `CLSimulationManager.loadScenarioFromURL:`，Xcode 同款机制）——模拟状态脱离 manager 生命周期，client 崩溃后 locationd 仍投递最后模拟坐标；格式/行为待真机实证（日志 `[locsim] scenario persist: OK/FAIL`）
2. **L4 死亡哨兵**（server，全 flavor）：`getppid()` 从 manager pid 变 1（launchd 收养）→ 即时 `setLocationServicesEnabled:NO`（1s 轮询；bootstrap 另有 monitorParentProcess 跟随退出契约，两者并存）
3. **L5 启动兜底**：`_forceStopOnStartup` 无条件关系统定位（重启/崩溃拉起后第一指令）

**私有 API 依赖**：`CLLocationManager.setLocationServicesEnabled:`（classdump 实证 iOS 15/17 在列；LocationServicesSwitcher 开源先例）——调用统一 @try + respondsToSelector + 回读确认（异常不上抛，失败向上报告）。

### 4.5 剪贴板（显式双向搬运，2026-08-17）

- **模型**：无自动同步（2026-08-17 移除）——设备端不再监听系统剪贴板（Darwin 通知/判定链/自动推送全删）、控制端复制不再自动写入设备。剪贴板为显式双向搬运：
  - **复制 = 拉**：控制台操作列/FAB「复制」按钮（`clipboard.get`，invoke 通道）或 5801 直连页「复制」按钮（HTTP 5802 `clipboard.get`）→ 取设备剪贴板文本 → 写控制端剪贴板（降级链：IPA 原生桥 `writeClipboard` → `navigator.clipboard.writeText`（https，瞬态激活窗口内）→ `execCommand('copy')`（http 亦可，仅用户手势+选区，iOS Safari 全版本支持）→ 明确报错）
  - **粘贴 = 推**：控制台粘贴按钮/Ctrl+V（`readClipboardText` → `type.paste`）或 5801 粘贴按钮/Ctrl+V/浮层（`type.paste`，5802 管理通道，RFB 原语兜底）→ 设备端写剪贴板（Cmd+V 数据载体）+ 模拟 Cmd+V。**http 降级（2026-08-18 起 PC/触屏统一）**：http 非安全上下文读不到控制端剪贴板 → 粘贴按钮与 Ctrl+V **一律弹输入浮层**（PC 与触屏同一浮层），浮层内 Ctrl+V（`clipboardData` 不要求安全上下文）或输入回车 → 自动注入并关闭；已废弃隐藏 textarea「第二次点击/第二次 Ctrl+V 提交」方案（网关 `ensureFarmClipText`、5801 `clipText` 均已删除）。https 下直读直贴不弹浮层
- **两端契约**：`clipboard.get` → `{ok, text}`；`type.paste` → `{ok, length}`（TRCapabilityRegistry invoke 与 trollvncserver 0x50 op 同契约）
- **5801 管理通道（v3，2026-08-17）**：管理操作（clipboard.get / type.paste / config.get / cap.list）走设备端独立 HTTP 端口 **5802**（trollvncserver 自实现轻量 API 服务器，复用 `tvExtHandle*` 纯函数，带 CORS 头），与 RFB 画面流完全隔离。历史方案均废弃：v2 曾用主 RFB 连接承载 0x50/0x80——0x80 响应与 FBU 帧竞争（FBU 分片发送 + 客户端 `_FBU.rects>0` 消费窗口无法感知 0x80）→ `Unexpected server message (type 125)` 断连；v1 独立 mgmt WS 因 libvncserver websockify 不支持并发 WS（踢断主连接）已废弃。5801 页面 `cap.list` 探测旧固件能力（无 clipboard 组则隐藏复制按钮）
- **ExtendedClipboard 已关闭（2026-08-17 架构级修复）**：设备端 `enableExtendedClipboard=FALSE`——根因是 0.9.15 在客户端 SetEncodings 请求 ExtendedClipboard 伪编码后**主动推送 ServerCaps（type 3）**，该推送不经 tvExtWriteResponse、不受 sendMutex 保护（无锁路径），与 FBU 帧竞争 → noVNC 在 `_FBU.rects>0` 时把 ServerCaps 字节当 rect 数据消费 → 流错位 → `Unexpected server message (type 125)` 断连。关闭后服务器不再主动推送任何非 FBU 消息；剪贴板全走 0x50/0x80（sendMutex 锁内，与 FBU 互斥）
- **决策记录**：iOS 剪贴板通知不携带写入者身份（无来源 API、TrollStore 无系统 hook 权限）→ 自动同步只能启发式（同文本误吞/应用改写误推的固有边界）→ 改为显式操作（零启发式、100% 可靠）。`execCommand('copy')` 不要求 https（仅用户手势+选区），http 下点击即复制成立（瞬态激活窗口内，局域网往返毫秒级远小于窗口）。

### 4.6 控制台 UI 要点

- **布局**：卡片墙（宽度滑杆 160–400px 自适应平铺、离线置灰、排序号 order 0-99999）、聚焦大屏（状态点蓝/红、断线自动重连浮层）、操作列（**2026-08-23 三端统一拍板顺序，2026-08-23 键盘移截屏后**：Home→复制→粘贴→上传→截屏→键盘→音量/静音/亮度→搜索→电源→全屏→断开，单列无分组标题；同步按钮置顶；粘贴按钮 2026-08-23 补齐——PC 虽有 Ctrl+V 但触屏访问网关时无实体键盘、且粘贴走 pasteFromControl 同套语义；无返回/适配按钮——适配为 scaleViewport 自动行为）、布局切换（**2026-08-19 PC/移动端共用同一布局按钮，位于顶栏最右侧**：宫格/列表两档——宫格 = PC 多列自适应 / 移动端默认双列；列表 = **单列行式（移动端）/ PC 端每行 2 个（2026-08-19，否则 auto-fill 在宽屏铺多列把行压小）**、隐藏调节阀；**卡片宽度调节阀移入下拉菜单、宫格选项下方**，仅 PC 宫格态显示（列表态/移动端隐藏）；选择持久化 localStorage）、移动端 FAB（可拖动 WiFi 信号按钮，ping 延迟三色）。**批量操作（2026-08-19 顶部入口形态）**：header 无标题文字；顶部「**批量**」按钮（直控按钮右侧，PC/手机 web 均保留）——点击进入多选模式后按钮自身变「**取消**」激活态（承担退出入口），**屏幕墙顶部**出现与屏幕等宽的全宽圆角胶囊行 `[✓ 全选][已选 N 台] …… [执行][设置]`（in-flow 位于 main 顶部，不遮卡片；胶囊行无取消按钮，**仅点击顶部「取消」按钮收起——点外部/执行菜单开合/批量执行等任何操作均不收起**）；全选复选框（勾选=全选在线、取消=取消全选）；「执行」弹批量菜单（**点一次展开、再点收起**，锚定「执行」按钮位置向下展开、下方空间不足自动改向上、**窄菜单（120px）**、纯「图标+名称」按钮列表，对齐触控端悬浮菜单布局、无分组标签、无取消按钮（**点外部仅关闭菜单、胶囊行保持展开**，仅顶部「取消」退出）——按键按钮复用控制台右侧按键的实现通道（attachPress 按压识别：单击/双击/三击/长按→批量下发；批量态对 BATCH_CAPS 具多击能力的键补声明多击窗口，Home 双击命中 home.double；**长按通道对齐控制台右侧**：显式 .long 能力触发（Home 长按→home.long）、按压式按键（音量/亮度/静音）**按住连发基础能力**（抬起停止；**手感 2026-08-19：长按判定 500ms（attachPress longMs 可配，默认 800）、连发间隔 200ms**）、键盘/搜索维持单击）；**批量按钮顺序（2026-08-23 对齐操作列）**：Home → 粘贴 → 上传 → 截屏 → 键盘 → 音量/静音/亮度 → 搜索 → 重启；**粘贴/上传/截屏为批量动作走 invoke 通道**（粘贴=控制端剪贴板→各台 type.paste、http 降级弹输入浮层；上传=选文件→逐台 5802 album.import；截屏=逐台 screenshot 能力仅统计成功台数）；**复制不入批量**（多台同时拉剪贴板互相覆盖，语义不自洽）；**电源键仍不入批量菜单**（批量误触电源有锁屏风险）；另含**重启**（释放所有按键/硬件键盘锁/解锁/批量重启均已去除——重启服务替代重启、type.paste 内部自带按键清理、键盘锁为单台硬件键盘维护操作）；**批量执行/配置结果以页内 toast 呈现**（**顶部居中**，✓成功/✗失败，自动消失，非阻塞——2026-08-19 由右上角改顶部居中，避开顶栏按钮与批量胶囊行；不弹浏览器系统 alert 影响其他区域操作）；批量配置能力移至「设置」按钮、直接打开批量配置面板（**按 reload 生效策略分区：即时生效 / 热重载生效 / 网关设置 / 需重启生效（警示色标题），2026-08-19**）；批量模式下点卡片=切换选中、不进入卡片控制，右下角 FAB 临时隐藏。**批量候选仅限在线设备**：单选（点卡片/复选框）与全选均拒绝离线/虚拟设备，执行/设置候选执行前再次过滤在线。
- **设备管理（2026-08-19）**：手动「添加设备」入口已完全去除——控制端设备列表仅保留 source=register 隧道设备（直连 host 设备模式已废弃，统一走网关隧道）；后端 POST /api/devices 保留（隧道测试依赖验证"无隧道拒绝 4003"）。**编辑改名 = 真实改底层设备名（2026-08-19）**：设备在线时先 invoke `device.rename` 能力（设备端写 MobileGestalt `UserAssignedDeviceName` → 更新 DesktopName 触发重注册上报新名 → 重启 SpringBoard 全系统生效——屏幕黑一下、前台 App 被杀，本 daemon 独立存活隧道不断），成功后再 PATCH 网关记录，注册不会覆盖回旧名；离线设备仅更新网关记录并提示（上线后以设备实际名为准）。
- **直控模式**：全部在线真实设备建立非 ctrl 可输入 RFB；**进入直控自动退出聚焦大屏并重置布局为卡片墙（2026-08-19，避免大屏操作中点直控布局仍是大屏）**；退出必须真正 close WS；互斥 = 直控时聚焦/悬停失效。
- **同步控制**：聚焦主控带 `grp+broadcast`，同步设备建 `grp=wallSession&viewOnly` 订阅；勾选/取消不重建主控连接。
- **容器模式**：`?container=ipa&token=&selfId=`（selfId 过滤自身设备）；WKWebView 无 requestFullscreen；版本同步 = no-cache + `?v=N`。
- **光标策略（2026-08-18 定稿）**：墙缩略图（viewOnly）与触屏端（isMobile）**不显示任何光标**（触屏手指即指针，无自动消失触点）；PC 聚焦/直控为**常驻自绘覆盖层光标**——深灰内芯 + 浅灰外圈描边（`pcRgba`，28px，fixed canvas 跟随鼠标）。两端均覆盖 `_refreshCursor`（clear/空操作）以**屏蔽服务器默认 X 形光标**（ServerCursor 移除后 libvncserver 默认发送）。
- **设备列表实时更新（2026-08-18 推送化，2026-08-19 移除轮询 + 手动刷新按钮）**：前端经 `/ws/events` 长连接订阅设备变更，后端在设备上线/离线/删除/改名/排序等变更点广播 `{type, deviceId, ts}` 轻量事件，前端收到后重拉 `/api/devices`（事件通知+前端重拉，不推全量列表）。**前端轮询与 header「刷新」按钮均已移除**（WS 为唯一更新通道；WS 断线重连 onopen 主动补拉全量，补齐断线期间错过的变更——事件即推即弃无重放），后端每 25s 对订阅连接主动 ping 心跳（浏览器自动回 pong 帧）检测死连接——长连接被 NAT/代理静默掐断（TCP 层无感知）时 terminate → 前端 onclose 退避重连；兼容前端 JSON `ping`→`pong` 双保险。事件类型：`register`（上线）/ `offline` / `delete` / `update`（改名/排序/配置/隧道状态变化）/ `thumb`（缩略图更新，2026-08-21——前端收到后仅补拉该设备缩略图缓存，不触发全量刷新）。WS 断线退避重连（2s 起，上限 30s）。
- **聚焦画布跟随设备方向（2026-08-19）**：`fitFocusPanel` 按 `_fb_width/_fb_height`（设备实时帧尺寸）计算面板宽度；设备运行中横/竖屏切换时 noVNC 只更新帧尺寸不派发事件——createRfb 实例级 patch `rfb._resize`，帧尺寸变化后 rAF 重算面板宽度，画布容器跟随设备旋转。直控/同步画布不跟随（固定比例卡片内 contain，与卡片墙显示方向解耦）。
- **样式**：聚焦画面余白透明化（body 背景透出）；`prefers-color-scheme` 深浅色跟随。

### 4.7 5801 直连页

- 单设备直连（无卡片墙）；BAR_KEYS 13 键常量渲染，2026-08-23 顺序与网关对齐（home/copy/paste/upload/snapshot/kb/volup/mute/voldn/briup/bridn/spotlight/full/power/disc；clip 已删、upload 2026-08-23 增、键盘在截屏后、**定位/数据填充 2026-08-26 直控 App UI 去除，15→13**）。
- 独立 caps.js（`layout/usr/share/trollvnc/webclients/caps.js`）仅覆盖按钮标题，与网关 caps.js 已分叉、互不引用。
- 键盘与控制台同方案 C；连接状态为画布居中浮层。
- **光标（2026-08-18 定稿）**：触屏端不显示任何光标（移除自动消失触点）；PC 端苹果灰圆常驻（`appleCursorShow`）。同样覆盖 `_refreshCursor` 屏蔽服务器默认 X 光标。
- **HTTPS（2026-08-19）**：页面已全自适应（`location.protocol` 判断：wss 连 5901 + https 连 5802），服务端证书就绪后输入 `http://设备:5801` 自动 301 到 `https://`（与网关 8080 同款体验）；粘贴在 https 下直读直贴（`navigator.clipboard` 可用），不再弹输入浮层。
- 剪贴板（2026-08-17 显式双向搬运）：复制按钮走 HTTP `clipboard.get`、粘贴按钮/Ctrl+V/浮层走 HTTP `type.paste`（HTTP 失败回退 RFB 原语拼装）；**http 下粘贴按钮/Ctrl+V 一律弹输入浮层**（PC/触屏统一，浮层内 Ctrl+V 或输入回车自动注入，同控制台 4.5 语义，2026-08-18 起不再用隐藏 textarea 二次提交）。**v3 独立 HTTP 管理通道**（2026-08-17 架构级）：管理操作（clipboard.get / type.paste / config.get / cap.list）改走设备端独立 HTTP 端口 **5802**（`trollvncserver` 自实现轻量 API 服务器，复用 `tvExtHandle*` 纯函数，带 CORS 头），**RFB 流只剩画面 FBU + 客户端输入**。v2 曾用主 RFB 连接承载 0x50/0x80——0x80 响应与 FBU 帧竞争（FBU 分片发送 + 客户端 `_FBU.rects>0` 消费窗口无法感知 0x80）→ `Unexpected server message (type 125)` 断连，已废弃；v1 独立 mgmt WS 因 libvncserver websockify 不支持并发 WS（踢断主连接 141ms 1006）已废弃。

### 4.8 数据生成（通讯录/通话/短信直写，2026-08-24 最小化验证定稿）

> 🖥️ **交互原型（UI 定稿参照）**：`outputs/locsim-app-prototype.html`——浏览器打开即可验证四 Tab（位置模拟/联系人/通话/短信）全部交互与最终 UI 形态；后续 UI 开发以该原型为准。

**调试/维护能力**：设备端 `data.read`（读表数据，支持 table 白名单）——**2026-08-26 A 档收敛后唯一保留的维护/检查接口**，走 5802 HTTP + 0x50 双通道（`tvExtHandle*` 纯函数，cl=NULL 复用），是远程排查数据实现（ZHANDLE 残留/计数口径/地区断裂等）的命脉。~~`data.probe`~~ **已退役（2026-08-26，POC 调试接口使命完成，schema 信息可由 data.read 覆盖）**；~~`data.test`~~ **已退役（2026-08-25，用户拍板正式能力 = data.fill，单条写库并入 fillDatabase count=1 等价，分派/辅助全删）**；~~`data.respring`~~ **已删除（2026-08-24，respring 禁用红线，见下）**。

**三库写入定稿**（真机 `data.read` 实证，勿凭调研假设——调研报告称 iOS 17 才有 sms.db handle 表、date 秒，实测 iOS 15+ 已有且为纳秒）：

| 库 | 表 | 关键格式 |
|---|---|---|
| `CallHistory.storedata` | `ZCALLRECORD` | `ZDATE`=Cocoa 纪元**秒**；`Z_PK`=max+1 且**同步 `Z_PRIMARYKEY`**；`Z_ENT` 查 `Z_PRIMARYKEY`（`Z_NAME LIKE '%CallRecord%'`）；`ZADDRESS` 是 **BLOB**；补 `Z_OPT=1`/`ZVERIFICATIONSTATUS=4`/`ZHANDLE_TYPE=2`/`ZCALL_CATEGORY=1`/`ZSERVICE_PROVIDER='com.apple.Telephony'` |
| `sms.db` | `message`+`chat`+`handle`+`chat_message_join`+`chat_handle_join` | `message.date`=Cocoa 纪元**纳秒**(×1e9，写秒会显示 2001 年)；`service`=**SMS**（iMessage 需 Apple ID 账号，无则信息 App 不显示）；接收态 `is_from_me=0/is_read=1/is_sent=0/is_finished=1`；**chat 必带 `style=45`/`state=3`/`account_login`(复用设备 SIM)/`last_addressed_handle`(对方号码)——缺 style/state 会话列表不显示**；chat.guid=`SMS;-;<ident>`；handle 表 `UNIQUE(id,service)` 冲突先查复用、补 `country`/`uncanonicalized_id` |
| `AddressBook.sqlitedb` | — | **走 CNContactStore 公开 API**（DB 直写缺 `FirstSort`/FTS 索引系统列表不显示，且触发器依赖框架自定义函数 `ab_update_value_from_trigger`/`ab_generate_guid` 直连缺失报 no such function）；需 `Contacts.framework` + tcc `kTCCServiceAddressBook`（daemon 不弹窗靠 entitlement 放行）；姓名全名放单字段 `familyName`（givenName+familyName 会按"姓 名"反序）；`StoreID=0` 即可 |

**生效链路**：写库 → `PRAGMA wal_checkpoint(TRUNCATE)` → kill daemon（callservicesd/imagent；**contacts 例外——2026-08-25 定稿：走 CNContactStore 写删不 kill contactsd**，contactsd 自己执行写库并广播 `CNContactStoreDidChangeNotification` 让系统通讯录自动刷新，kill 是 data.test 直写 DB 时代遗留，反而打断 CNContactStore 客户端连接、制造重启窗口导致 CommunicationError）→ 系统 App 即可读取新数据。

> ⚠️ **respring 已禁用（2026-08-24 项目红线）**：respring（kill SpringBoard）会重启主屏，中断前台 App、打断隧道/注册会话、破坏 daemon 保活链路；数据直写系统库后 kill 对应 daemon 即可生效，respring 属冗余设计。**全项目禁止 respring**（含 `data.respring` 能力已删除、`device.rename` 不再重启 SpringBoard）；刷新只允许 kill 对应 daemon，禁止按 respring 实现/回归。

**方法论**：先读后写（写入前先读真实记录，能读到的格式才是系统认的格式，逐字段对齐）；最小闭环（写入→kill daemon→UI 确认，一次验证一个环节）。

### 4.9 数据填充正式能力（data.fill / data.clear，2026-08-25 三阶段定稿）

> 三阶段流程（设计 §1.1）：① Node 外部生成器（`trollvnc-farm/test/data-gen/`，产出结构化 JSON，不写设备）→ ② ObjC 同构移植 `TRDataFiller.mm`（算法不二次开发，只翻译 + 写库）→ ③ UI 联动（**App 伪装页三 Tab——唯一操作入口；5801 直连页 / 网关批量菜单按钮与外部写入口均已去除（2026-08-26 直控 App UI + A 档收敛）**）。随机源 xorshift64 两端同构（Node `rng.js` ↔ ObjC `trRand`，同 seed 可复现，供对照验证）。

**能力与入口**：`data.fill`（联系人/通话/短信批量拟人生成）+ `data.clear`（contacts/calls/sms/all 清空），写库逻辑**单一实现**于 `TRDataFiller`。**2026-08-26 A 档收敛后唯一入口 = App 伪装页进程内直调**（注册表 invoke / 5901 0x50 / 5802 HTTP 外部入口已全部移除，外部再调将返回"未知操作"）。

**参数契约**（`fillDatabase:`）：`{db:'contacts'|'calls'|'sms', count:1~1000, seed:uint64, ratios:{...}}`。ratios 键集见设计文档 §3 表，代码真相源 = App `TRFillDataViewController collectRatios` 与 `TRDataFiller` 消费；键名两端对齐、数量变化同步三处断言（caps-test）。**seed（2026-08-25 定稿）**：App 前端随机种子组件已移除，`generate:` 传 `seed=0` 完全随机（每次不可复现，符合随机拟真目标）。**2026-08-26 A 档收敛后写能力无外部入口**，seed 复现仅 App 内部/算法层 Node 原型（`test/data-gen/`）可用。

**互斥对齐（2026-08-25 定稿）**：UI 比例滑条组真互斥（拖动任一项其余等比缩放、末项补余量，合计恒 100%，消除旧"末项补足不对称"）；ObjC 消费归一化（`r = rand01()*total`，对齐 Node `weightedIndex`）——合计≠100 时差额**等比分配**而非旧实现"塞首项"（联系人=friend/通话=呼入/短信=family）；calls 选人组（contact/stranger）二元天然互补。

**依赖校验（反差确认）**：通话/短信的联系人内号码必须能在通讯录反查；通讯录为空则报错「请先生成通讯录」（生成失败错误返回，2026-08-25 布局精简后不再在页面顶部常驻提示）。**短信依赖校验（2026-08-25 定稿）**：`trFillSms` 开头前置校验，通讯录为空整体拦截返回「通讯录为空，请先生成通讯录（家人朋友短信依赖通讯录）」（与通话生成一致，设计 §3.3 L191）；Node `generateSms` 作为纯算法原型仍可脱离通讯录生成（family `phone:null`）——算法层提供能力、产品层加门禁。**常住地区门禁 + 长效化（2026-08-26 定稿）**：联系人本地号按常住城市 HLR 号段生成，**常住地区是必选参数**——首次进入显示「请选择」占位（灰色，不再硬编码默认北京），未选时点生成整体拦截「请先选择常住地区」；选择后 **NSUserDefaults 长效化**（`TRFillResidentProvince/City`，下次进 App 自动带出上次省市，清空数据不影响地区选择）。

**服务短信发件号（2026-08-26 修复，用户观察"私人号发服务短信"）**：短信类型 code/express/bank/carrierSms/marketing 的发件号——**银行**=955xx 客服短号池（`SMS_SVC_BANK` 20 家：95588 工行/95599 农行/95566 中行/95533 建行/95555 招商/95568 民生…）、**快递**=953xx/955xx 客服短号池（`SMS_SVC_EXPRESS`：95338 顺丰/950616 京东/95554 圆通/95543 申通/95311 中通/95546 韵达/95353 德邦/95320 百世/11183 EMS/95331 天天）、**验证码/营销**=106 企业特服号段（`trRandomSvc106`（ObjC）/`svc106`（Node）同构：106+8 位）；运营商短信仍 10086/10010/10000（`trCarrierSvcPhone`）。**修复前 code/express/bank/marketing 误用私人手机号 `trRandomPhone`**——ObjC 移植漏了 Node 原型的 randomSvcPhone 服务号段逻辑。库位置：corpus.js（Node 数据源）→ TRCorpus.mm `kSmsSvcBank`/`kSmsSvcExpress`（构建产物，两端同构）；Node sms-gen.js 与 ObjC TRDataFiller 同一发件映射。

**清空语义**：contacts 走 CNContactStore 枚举删除（**不 kill contactsd**——删除由 contactsd 执行自同步；`deleteContact:` 传 `mutableCopy`（immutable 快照删除偶发"返回成功但静默不提交"）、20 条/批小事务、必须显式请求 `CNContactIdentifierKey`），calls/sms 走 sqlite3 DELETE（**不依赖 daemon，直写即成功**；**不动 `Z_PRIMARYKEY`**，保证后续插入 Z_PK 不冲突）；清空后 kill 对应 daemon + **目标 App 自动重启（2026-08-25 定稿，生成/清空同链路）**：通话 kill `callservicesd`+`MobilePhone`、短信 kill `imagent`+`MobileSMS`——下次打开电话/信息 App 即新数据（"立即生效"，对齐通讯录 CNContactStore 体验）。UI 清空带确认弹窗（不可恢复），部分失败透出 errors 详情。**短信清空根治（2026-08-25 定稿）**：① iOS 删除短信=**软删除**（`message.flags` 标记 `0x04`，行永留表内、App 过滤显示）；② message 表带**系统触发器**（FTS/未读计数，调用系统注册函数如 `read()`）→ 直连 DELETE 报 `no such function` 致残留/递增，`clearDatabase` 须先 `trRegisterTriggerFuncs`（读触发器 SQL 提取函数名、注册返回 0 的 stub，不 DROP 不破坏系统结构）；③ 计数口径=`会话数`（chat 表 `service_name='SMS'` 且 `is_filtered=0`/`is_archived=0`/`is_blackholed=0`）——信息 App 主界面=会话列表，message 行 flags/join 与显示脱节（EXISTS join+flags 曾把实际可见短信全排除误报 0 条）；删除仍全量。**短信库诊断（2026-08-25）**：清空短信（sms/all）返回 `smsMeta`（`flagsDist`=message 按 flags 分组、`joinDist`=join 有无分布、`chatState`=chat 按 state/is_filtered 分组）——真机确认口径后删除。**通话库诊断（2026-08-25）**：清空通话（calls/all）返回 `callsMeta`（`triggers`=CallHistory 触发器列表、`columns`=ZCALLRECORD 字段清单）——真机确认通话无短信同款机制（无软删/无 FTS 触发器，物理删）。**kill 失败降级（2026-08-25 定稿）**："未找到进程"（daemon 未常驻/App 未运行）忽略；其余进 `killError` 信息级字段（生成路径另有 `appKill`/`appKillError` 字段），不进 errors——不误报"部分失败"（清空本体已成功）。**清空后校验（2026-08-25 定稿）**：contacts 清空后重新枚举 CNContactStore 统计残留；残留>0 时返回 `remaining` 字段（残留条数）+ `ok:NO` + 明确 `error`（iCloud 同步/只读账户/SIM 卡提示）；校验枚举失败仅记 errors 不误报残留——防"看似清空实际未删干净"无感知。

**返回契约（2026-08-25 规范化定稿）**：生成 `fillDatabase:` 成功 `{ok:YES, db, count}` / 失败 `{ok:NO, error}`；清空 `clearDatabase:` 成功 `{ok:YES, db, cleared}` / 部分失败 `{ok:YES, errors[]}` / 残留 `{ok:NO, remaining, error}` / 失败 `{ok:NO, error}`；信息级字段（不影响成败）`kill`/`killError`/`appKill`/`appKillError`；诊断字段（一次性，真机确认后删除）`callsMeta`/`smsMeta`。UI 按 `ok` 分层展示（见 UI 定稿）。

**UI 定稿（2026-08-25）**：① **结果对话框**——生成/清空完成后弹**居中模态卡片**（半透明遮罩 + 白底圆角卡 + 圆形图标 + 标题 + 详情 + 「完成」按钮）：成功=绿 ✓、失败=红 ✕、清空未完全（errors 非空/残留）=橙 !；替代旧底部 resultLabel 展示（resultLabel 仅保留"生成中…"瞬时态）；② **去"合计"标签**——比例滑条组真互斥保证恒 100%，移除"合计 组x xx%"标签（重置默认按钮保留全宽独立一行）；③ App 端消费新返回（count/cleared/errors/remaining 分层）。**5801 直连页 / 网关批量菜单按钮与 5802/网关写入口均已去除（2026-08-26 直控 App UI + A 档收敛）**，返回契约仅 App 消费；`data.read` 维护接口保留 5802/注册表。

**时间戳 epoch（2026-08-25 真机 2057 年根因修复）**：通话 `ZCALLRECORD.ZDATE` 与短信 `message.date` 均为 **Cocoa 纪元（2001-01-01）**——生成时间戳必须用 `timeIntervalSinceReferenceDate`；误用 `timeIntervalSince1970`（Unix 1970）会偏移 31 年（2026→2057）。

**号码地区（D1 §2.5 HLR，2026-08-25 数据源落地）**：`regionLocal` 决定手机号是否**常住城市归属**——本地分支取该城市 HLR 号段前缀（7 位，phone2region 51.7 万条归属地数据 → `TRHlr.mm` 区间表）+ 4 位尾号；其余全国随机号段（`number-segments.json` 3 位号段池）。机构/生活服务类 30% 用本地固话（区号体现地区，`TRAreaCodes`）。Node `contacts-gen.js` 与 ObjC `TRDataFiller` 同构。**号段过滤（2026-08-26 修复，用户观察"虚商/物联网号段混入通话短信"）**：号段池与 HLR 前缀表**构建期过滤**不可作普通手机号的三类号段（依据工信部《携号转网服务管理规定》：移动通信转售=虚拟运营商、物联网专用网号、卫星移动均不能携转）——**虚拟运营商 170/171/162/165/167**（不稳定：易倒闭/号码回收/营销重灾）、**物联网 145/146/147/148/149**（无电话功能）、**卫星 174**（1740-1745 天通，非普通手机）；另 **134 内 1349**（卫星移动）为 4 位段，3 位粒度无法分离，由 `build-tr-hlr.mjs` 按 7 位前缀单独过滤。实施：`build-area-table.mjs` SEGMENTS 剔除 11 个号段（55→44）+ `build-tr-hlr.mjs` 前缀过滤（TRHlr.mm 14.3 万→11.96 万区间 / hlr-prefixes.json），两端构建脚本同步、产物禁手改。**城市名归一（2026-08-25 修复）**：App/5801 传入的城市名可能带"市/省/自治区"后缀（BRPickerView 数据源 regions.json 城市名如"杭州市"），而 AREA/HLR 表 key 为无后缀（"杭州"）——两端统一 `trNormalizeCity`/`normCity` 去后缀，不归一则本地号/固话区号全回退（全国随机/北京 010），**曾致"通讯录生成的地区与通话记录完全断裂"**（通话从通讯录选人沿用断裂号码）。

**三阶段验证结论**：Node 阶段 32 断言 + 100 轮魔鬼测试 + 面板 404 断言 ×10 轮；阶段 2 真机 data.read 实证格式（iOS 15+ 已支持 sms.db handle 表与纳秒日期，见 §4.8 表）；阶段 3 UI 联调经 CI 编译验证。语料库/区号/省市表全为构建产物（`corpus.js`/`regions.json`/`district-code` → `TRCorpus`/`TRRegions`/`TRAreaCodes`），禁手工翻译。

### 4.10 每日轨迹（真人行为模拟，2026-08-26）

> 背景：一次性批量生成缺乏"真人感"——真人每天只产生少量通话/短信，且会认识新朋友。用户需求：**每天随机生成 3-10 通话 + 5-15 短信，每周新增 1-5 联系人**，像真人的行为轨迹。

**架构**：`TRDailyTrajectory.mm/h`（manager daemon，root 常驻进程）自治调度——**不依赖外部触发/App 存活/网关在线**；数据操作走 `TRDataFiller`（与 App 伪装页同源，A 档收敛语义内），**不暴露任何外部入口**（默认开启，无 App 开关）。manager 启动时 `[TRDailyTrajectory start]`（trollvncmanager.mm）。

**调度**：每小时 GCD 后台 tick（`kTickInterval=3600s` 递归）+ 启动立即首检。**昼夜权重**（与 Node `CIRCADIAN_WEIGHTS` 同构）：0-6 时 0.05 / 6-8 时 0.30 / 8-9 时 0.70 / 9-12 时 0.90 / 12-14 时 0.80 / 14-18 时 0.95 / 18-21 时 1.00（峰值）/ 21-23 时 0.70 / 23-24 时 0.30——`arc4random()%1000 < w*1000` 决定本小时是否补生成（凌晨几乎不、白天活跃）。

**每日目标**（跨天重置，NSUserDefaults suite `com.82flex.trollvnc` root 域）：通话 3-10（呼入/呼出各半、接通）、短信 5-15（家人+服务混合，family 依赖通讯录）、未接 0-2（陌生号，`ratios {missed:1.0}`）。每次 tick 补生成 1-2 条（通话）/1-3 条（短信）/1 条（未接），到目标即停。

**每周联系人**（跨周重置，`WeekOfYear`）：新增 1-5 个，每小时最多 1 个（稀疏感）；**city 取 App 常住地区**（mobile 域 plist `TRFillResidentCity`，无则北京回退），`regionLocal 0.65`。

**依赖与失败**：通话/短信生成依赖通讯录（`trFillCalls`/`trFillSms` 前置校验）——通讯录为空时生成失败**静默跳过**（下小时重试）；新增联系人先积累，通讯录足够后通话/短信才逐步出现（真人同理）。生成后由 `TRDataFiller` 内部 kill 对应 daemon（callservicesd/imagent 等）立即生效，不碰 SpringBoard。

**性能影响（2026-08-26 评估，可忽略）**：每小时 1 次 GCD tick（24 次/天），单次毫秒级写库 + 微秒级计算，无常驻线程/对象（`@autoreleasepool` 包裹、定时休眠等待不空转），每日 CPU 合计 <1s。对照设备已有负载：SimLocationController 定位巡检 10s 一次（8640 次/天）比每日轨迹高 **360 倍**——影响测不出来。**唯一体感**：生成后 kill 对应 daemon 的瞬时系统重建（电话/信息 App 闪一下，与 App 手动生成体验一致，频率更低）；与手动生成并发时 SQLite 锁冲突小概率发生，失败静默跳过不影响已有数据。

### 4.11 WiFi 位置显示（2026-08-27 Phase 1，真实位置读取）

> 背景：系统定位服务关闭时，App 地图无位置（locationd 不广播）。需求：**走"通道二"读法**——App 自扫真实 wifi → wloc 反查坐标 → 地图显示真实 wifi 位置，不依赖系统定位服务。属设计文档 `docs/superpowers/specs/2026-08-27-wifi-spoof-double-layer-design.md` 第一步（A 层注入/代理层为 Phase 2 未实现）。

**架构（与真实 GPS 活跃订阅同构，无按钮）**：
- ~~`TVNCHotspotManager`（App 壳）~~ 已删除（2026-08-27 起主动扫描唯一实现，NEHotspotHelper 全链路移除，禁止回归）——被动回调模型无法主动触发系统扫描，已弃用。
- `TRWpsClient`（App 壳，ObjC 移植自 `scripts/apple-wps.mjs` query）：`queryCoordinatesForBssids:completion:` POST `gs-loc-cn.apple.com/clls/wloc`（ARPC 头 + protobuf，BSSID→坐标 WGS-84，(-180,-180) 哨兵过滤），completion 主队列回调。
- `TRMapPickerViewController`（伪装页定位 UI）：viewDidLoad 设置主动扫描通知订阅 + **初始水合**（启动扫描可能早于订阅，先消费缓存）→ BSSID → wloc 反查（`_queryWifiAnnoWithBssids:`）→ GCJ-02 转换 → `TRWifiAnnotation` 自绘水滴标注（先移除旧的防重复）。模拟态标注反查方法已更名 `requeryAnnotationForSimLocation`（无参方法；2026-08-28，对齐"模拟位置反查"职责，不再叫 handleWifiScanUpdate）。

**行为语义**：启动即订阅、扫描回调驱动、无按钮、无弹窗（错误静默）、不打断用户地图操作（无 setRegion 聚焦）；标注显示「WiFi 定位（wloc 反查）」+ 质心坐标 + AP 数。

**验证门槛（已过时，2026-08-28 止）**：真机（系统定位已关）打开系统 Wi-Fi 设置页触发扫描 → 地图出现 wifi 位置标注（NEHotspotHelper 被动回调模型）——NEHotspotHelper 已全删，此验证方式失效，改由下方主动扫描通道验证。

**主动扫描通道（2026-08-27 daemon Apple80211，不再依赖「打开系统 Wi-Fi 设置页」）**：
- 根因：NEHotspotHelper 是**被动回调**模型——仅当系统自身扫描（打开系统 Wi-Fi 设置页等场景）才派发 FilterScanList/DisplayNetworks 命令，App 无法主动触发系统扫描 → 之前必须「去系统设置页拉一次列表」才能显示真实 wifi 位置。
- `TRWifiActiveScanner`（`src/`，manager daemon root 进程）：dlopen MobileWiFi 私有框架 + dlsym `Apple80211Open/Bind/Scan/Close` 四件套 → GCD 周期（`kTRWifiScanIntervalSec=8`，首次立即扫）主动扫周边 BSSID → `TRWifiScanSnapshot`（bssids/ssids/rssi/ts）→ 原子写共享 JSON → `notify_post(kTRWifiScanUpdatedNotification)`（Darwin 通知）。共享 JSON 路径 `/var/mobile/Library/Caches/com.82flex.trollvnc.wifiscan.json`（mobile 可读），通知名 `com.82flex.trollvnc.wifiscan-updated`。
- **App 消费（`TRMapPickerViewController`）**：`notify_register_dispatch` 订阅通知 → 读 JSON（`_readActiveScanBssids` 归一读取）→ `handleActiveWifiBssids:`——**模拟开启时**走 `_refreshWifiAnnoIfTileChanged`（fix 驱动瓦片检测，跨瓦片才反查标注，与当前位置水滴同源同步，共享原语 `TRWpsTile tileChangedForCoordinate:`）；**模拟关闭时** BSSID → wloc 反查 → 真实 wifi 位置标注（复用 `_queryWifiAnnoWithBssids:`）。启动水合一次（daemon 可能已扫过，先消费缓存）。**停止态**：`refreshWifiAnnotation` 停止分支读主动扫描 JSON，空则清除残留标注等下次 notify 恢复——关闭定位 wifi 标注回真实位置不消失。**启动双源聚焦**：`focusMapOnCurrentLocation` GPS fix 优先、wifi 位置（`wifiCurWGS` 最近反查质心）兜底，wifi 反查成功后首帧未聚焦则补聚焦。
- **能力边界**：真实 wifi 位置标注不再依赖「打开系统 Wi-Fi 设置页触发被动扫描」（唯一实现=daemon 主动扫描，NEHotspotHelper 已全删）。系统 Wi-Fi 总开关关闭时主动扫描仍扫不到（硬件断开，物理约束保留）。wifi 标注坐标一律 wgs84→gcj02 转换后显示（对齐地图坐标系）。

### 4.12 A 层 wifi 扫描模拟注入（2026-08-27 Phase 2，双管线叠加）

> 背景：仅模拟 GPS 坐标时，风控读 wifi 扫描源仍是真实环境（坐标层与指纹层断裂）。需求：**双管线叠加**——GPS 模拟喂"结果"、wifi 模拟喂"输入"（setWifiScanResults → locationd 解算），同时执行不互斥。属设计文档第一步/核心模型（A 层注入，B 层代理为 Phase 3）。

**架构（manager daemon 侧；注入功能 App 零改动——2026-08-28 起 App 侧仅契约消费/标注链路做规范化收敛，不属功能变更）**：
- `SimLocationManager`（src/）：新增 wifi 注入原语——`injectWifiScanResults:`（stop → setWifiScanResults → setSimulatedWifiPower → startWifiSimulation，完整重启对齐 GPS 语义）、`stopWifiScanSimulation`、`isWifiSimulating`、`stopAll`（总停：GPS+wifi 一并恢复真实）；`+buildScanResultsFromBssidStrings:` 把 BSSID 列表转 scanResults 字典（键名 bssid/ssid/rssi/channel/age/timestamp 为初始猜想，**待 XPC 取证校准**；rssi -40~-85 决定 wifi 源 10-100m 精度，与 GPS 源 5-30m 区分）。
- `TRWpsTile`（src/，Phase 3 已实现，替代固定 `kWpsBssids`）：坐标→瓦片 key（Web Mercator level 13 + morton 编码）→ GET `gspe85-cn-ssl.ls.apple.com/wifi_request_tile` + `X-tilekey` header → 解析 protobuf 得该位置附近真实 BSSID → 取前 100 注入；LRU 缓存 32 瓦片 + inflight 并发去重。**CDN cache-buster（2026-08-27）**：URL 必须带 `?tk=<tileKey>` query——国内域名解析到金山云 CDN，其缓存键只含 URL 不含 `X-tilekey` header，曾致所有瓦片命中同一份陈旧缓存（固定 1051B/33AP，北京瓦片真身 3520B），注入 BSSID 与模拟坐标完全不自洽；origin 按 header 取数、忽略 query（实测验证），query 仅用于分离 CDN 缓存键。部分区域瓦片为服务端数据空洞（稳定 404）——**驻马店一带为大片数据荒漠**（实测 5×5 网格 23/25 空、level 11-14 全 404，Apple 未采集；大城市如北京/上海则数据密集"点哪里都成功"），荒漠区"不注入"与真实设备行为一致（真实用户在此同样查不到 wloc 指纹、定位退化为纯 GPS），**勿注入远距 BSSID 制造指纹-坐标 10km 级不自洽破绽**；反查失败保持现状不打断已有模拟，无失败负缓存（防自我锁死循环，2026-08-27 定案；荒漠区结论 2026-08-28 探测定案）。**空洞螺旋回退（2026-08-28 定案，远程伪装场景）**：`queryNearestBssidsForCoordinate:maxAttempts:`——模拟起点即空洞（从未注入成功）时，从空洞瓦片出发按 Ulam 螺旋搜索最近有效瓦片注入其 BSSID（社区 acheong08 demo-api 同款 spiral；maxAttempts=24 覆盖半径 3-4 瓦片）——否则不注入会让 locationd 的 wifi 源回落为**设备本地真实扫描**（GPS=模拟 vs wifi=本地，数百公里级不自洽，比邻近瓦片 1-10km 偏差更糟）；周边全空洞则保持不注入（真实设备行为）。**保活重注已删除（2026-08-28 统一注入后废弃）**——曾成功但当前反查失败（跨入新空洞/网络抖动）时旧池 `_wifiTileAps` 每 tick 继续供窗口注入，wifi 源天然不断供；lastScan 重注是旧低频注入架构的补丁（注入旧瓦片完整指纹且绕过统一注入），统一注入下多余并删除（含 `lastScanResults` 全链）。**距离窗口 + 质心标注 + 统一注入动作（2026-08-28 定案，模拟真实设备移动时可见 AP 渐变）**：`windowApsByDistance:center:window:` 对瓦片 AP 池按与模拟位置距离排序取近 K（`kTRWpsWindowSize=30` 单一真相源）；`rssiWeightedCentroidOfAps:` 窗口 AP 坐标加权质心（wifi 反查位置，无需 wloc 网络反查）。**注入语义本质（用户定案）**：注入的只是路线（锚点/轨迹）坐标集合，GPS 与 wifi 是同一坐标的两个处理器——GPS 处理器直写坐标、wifi 处理器写坐标反查出的 AP；**统一动作 `_injectSimulationForCurrentLocation`：wifi 处理器在前（写 AP 窗口）、GPS 处理器收尾（直写坐标完整重启）同 tick 完成**——wifi 重启（stopWifiSimulation→set→startWifiSimulation）瞬间会打断同会话 GPS 广播，紧后 GPS 收尾同 tick 恢复，无真空、无独立兜底（废弃"每 4s 重排节拍 kSimWifiWindowTicks"与"wifi 重启兜底重注 _reinjectGpsForWifiRestart"——曾致「开启定位后位置跳回真实」）；**无 GPS 仅 wifi 语义**：GPS 处理器在无当前位置（0,0）时跳过、wifi 照常（由锚点/路线坐标反查 AP 注入）；反查/螺旋/首点/平移全部收敛统一注入。App 同瓦片内每 fix 用缓存池质心刷新标注（wifi 水滴沿 AP 走，与注入同源）。**巡检条件修复（2026-08-28）**：原 `isSimulating && wasWifiSimulatingOnce` 在 GPS 正常+曾成功时恒真 → 每 10s 无条件重反查（曾致"每 10s 报 wifi lost"假象）；改 `!isWifiSimulating && wasWifiSimulatingOnce`——仅 wifi 源确实丢失才重反查。**wifi 窗口变化检测（2026-08-28）**：可见 AP BSSID 集未变则跳过 wifi 注入（静止/微动未跨出阈值不重启 wifi），换池（`_wifiTileAps` 更新）时置 nil 强制首窗口重注——统一注入下消除"每 tick 全量 wifi 重启"，日志仅注入时输出。
- `TRWifiScanContract`（src/，跨端契约共享模块）：`kTRWifiScanJsonPath`/`kTRWifiScanUpdatedNotification`/`kTRWifiScanRequestNotification` 三常量单一真相源（App 与 daemon 两端 target 各自编译，消费侧禁止重写字面量——曾两端各持一份、改一处忘一处；`wifiscan-request` 裸字符串已收敛为常量，2026-08-28）。
- `TRWpsProto`（src/，protobuf 读取原语共享模块）：`TRWpsReadVarint`/`TRWpsSkipField` 单一实现（`extern "C"` 防 C++ mangling），`TRWpsClient` 与 `TRWpsTile` 曾各复制一份，2026-08-28 上移。
- `TRSimContract`（src/，跨端定位契约共享模块）：`kTRSimTrackFilePath`（轨迹点序列 JSON 路径）单一真相源——App 写（编辑/上传轨迹）、daemon 读（itinerary 注入）；`kSimTrackFilePath` 曾 App 与 `SimLocationController` 各持一份 static、注释互指"保持一致"，2026-08-28 收敛（对齐 TRWifiScanContract 模式，消费侧禁止重写字面量）。
- `TRAppDomain`（src/，全局 App 域契约头）：`#define kTRAppPrefsSuiteName @"com.82flex.trollvnc"`——bundle id 同名 NSUserDefaults suite 单一真相源（全部进程 App/trollvncserver/trollvncmanager/prefs bundle 读写同一配置域：SimLocation*、网关地址、设备 ID、开关等）。用 ObjC 字面量宏而非 extern 常量：三域（src theos / App xcode / prefs bundle）include 即用，无 .mm 链接依赖；NSUserDefaults 方法参数为 NSString *，裸 C 字符串会报 "string literal must be prefixed by '@'"（2026-08-28 CI 实测），需 CFStringRef 的消费点用 `(__bridge CFStringRef)kTRAppPrefsSuiteName`。曾 19 文件 47 处裸字符串/本地 static 各持一份（kDefaultsSuite/kGatewayDefaultsSuite/kTVNCDefaultsSuite/kPrefsSuite），2026-08-28 全量收敛；prefs 域（CCTrollVNC）经 TVNCUtil.h 传递引用。
- `SimLocationController`（src/）：**启动一律停止态（2026-08-28 对齐 App readCurrentStatus 契约）**——`start` 先 `_forceStopOnStartup`（残留 anchor/itinerary 强制写 off mobile 域）再 reload，manager（launchd 常驻）设备重启/崩溃拉起不再自动恢复模拟注入（曾"读参数恢复上次模式"→ 设备重启后系统其他 App 自动吃模拟位置，契约分叉已对齐；模拟由用户显式开启）；总开关联动——off/unknown 分支 `stopAll`（GPS+wifi 全停）；`_startAnchor`/`_startTrack` 首点注入后 `_injectWifiSimulationForCurrentLocation`（与 GPS 同源注入 wifi，**统一目标位置源**）；`_checkRestore` wifi 兜底重注区分「曾成功丢失→重注」vs「从未成功（空洞瓦片）→安静等待跨瓦片换源」（`wasWifiSimulatingOnce` 单调标志）；空轨迹文件全停防残留。**prefs-changed 热重载风暴修复（2026-08-27）**：编辑锚点曾每 1-2s 一波全量 apply（stop+start+wifi 重注）——三处修复：① 通知入口 `scheduleReloadFromPrefs` 500ms 合并窗口（一次编辑链 holdAtCurrentPosition→重算→writeTrackFile 连发多次通知合并为一次 reload）；② `_paramsSignature` 轨迹 mtime 仅 itinerary 纳入（anchor 不消费轨迹文件，重算期间轨迹重写不再触发 anchor 无谓重启）；③ `applyFromPrefs` anchor 分支增量平移——anchor→anchor 坐标更新（hold 微差/挪锚点）只平移游走中心（保持游走状态、立即注入新点），不重启不重置不重注 wifi；跨瓦片 wifi 跟随统一放 `_anchorTick`（对齐 itinerary `_trackTick` 的 `_checkWifiTileChangedAndReinject` 语义，同瓦片 key 比较零成本）。

**accuracy 规范（抖音逆向结论）**：模拟坐标精度必须与源匹配——wifi 源 10–100m（由注入 rssi 分布决定）、GPS 源 5–30m（controller 钳制 3-15m）；禁止"wifi 源却 5m 精度"矛盾值。

**已知限制与真机验证点**：
1. **K1（真机验证）**：`injectPoint:` 的 `clearSimulatedLocations` 是否连带清 wifi 模拟——若会，GPS 每秒注入打断 wifi 管线，需注入后兜底重注（2026-08-27 日志实证：巡检每 10s 报 wifi lost 重注，locationd 侧 wifi 模拟确不持久，重注为必要心跳）
2. ~~跨城轨迹~~ 已解决：`TRWpsTile` 动态反查任意区域 BSSID（见上），跨城市轨迹 wifi 源与 GPS 位置自洽
3. **XPC 取证**：字典键名为初始猜想，需 fishhook `xpc_connection_send_message` 抓 locationd 载荷校准（POC 稿 §6）

---

### 4.13 Vision 屏幕感知（vision.ocr / vision.find_text / vision.find_image，2026-08-28）

> 背景：上层脚本/导演需要"传文字→回坐标"的机械匹配原语（`find_and_click("发布")` = 内部调 vision.find_text 拿中心点 → touch.tap）。**语义判断不在手机端**；vision 只定位、执行仍走 RFB→IOHID 注入（能力层唯一地基）。script.exec 尚在设计中——vision 原语不依赖它，按现有能力调用通道上线（跨网 invoke / 5802 / 0x50）。

**接口契约**（三处同 op 名，坐标全部 0-1 归一化左上原点，与 touch.* 一致）：
- `vision.ocr` `{region?{x,y,w,h}}` → `{ok, count, texts:[{text,x,y,w,h,cx,cy,confidence}], width, height, durationMs}`（texts ≤200 行；cx/cy=中心）
- `vision.find_text` `{text, exact?, region?}` → `{ok, found, count, matches:[...≤20], durationMs, width, height}`（两端去空白比对，规避 OCR 空格噪声；exact=YES 全等/默认包含）
- `vision.find_image` `{image(base64≤256KB), threshold?=0.7, region?, scale?=1.0}` → `{ok, found, score, x, y, w, h, width, height, threshold, durationMs}`（x/y=匹配中心 0-1，w/h=匹配矩形 0-1；**width/height=本次匹配帧像素尺寸**——与 OCR 等感知 op 对齐，编排层可跨 op 校验帧一致性（滚动/旋转后帧变化察觉）；found=NO 时 score=实际最高分；锚点图由云端视觉代理截取传入，**不在设备端抓图/存图/归档**）

**实现**（`TRVisionEngine.mm`，仅 trollvncserver 编译）：VNRecognizeTextRequest `.accurate` + `recognitionLanguages=["zh-Hans","en-US"]` + `usesLanguageCorrection=NO`（系统纠错会改写 UI 文案致匹配失败）；输入复用 `ScreenCapturer.captureSingleFrameBuffer`（与 screen.hash 同源取帧，不另开抓屏路径）；VN bbox 左下原点 → `y01 = 1 - bbox.midY` 换算；region 过滤按 bbox 中心；串行队列防并发 OCR；iOS <14 返回明确错误。耗时 .accurate 全屏 0.5~2s。

**实现·模板匹配**（vision.find_image，`TRVisionEngine.mm` 文档本节下方）：vImage/vDSP **自实现**模板匹配（勿引 OpenCV，系统框架优先）——Rec.601 灰度化 → **多尺度归一（2026-08-28 优化：`scale`=模板相对本机原生分辨率比例，[0.25,4] 默认 1.0，单次自适应双线性缩放模板，不做多档穷举**——云端缩略图 0.5x 取锚点传 0.5 即正确匹配）→ 1/8 金字塔降采样（模板同步）→ 模板 180° 翻转（vDSP_convD 相关方向要求）→ 逐模板行 `vDSP_convD`+`vDSP_vaddD` 累加相关图 → **NCCC 归一化**（SAT 积分图取局部均值/方差，抗亮度对比度变化）→ `vDSP_maxvi` 式找峰 → 粗定位 ±48px ROI **原尺寸精匹配** → 阈值判定（默认 0.7）。模板经 base64 解码（RGBA 中转，PNG/JPEG，8~2048px、≥32px，缩放后 ≤4096px）；无旋转不变性（纯截图锚点匹配）；region 收敛初筛窗口。
  实现纪律（真机 E2E 发现并修复，2026-08-28）：`vDSP_convD` 全卷积输出长度 = N+M-1，相关缓冲区必须按 N+M-1 分配（曾按有效长度 N-M+1 分配 → 堆溢出写越界 → 任意合法模板调用即崩 daemon 进程；已修）。

**链路纪律**：三处补齐齐备——注册表 `vision.ocr`/`vision.find_text`/`vision.find_image`（TRCapRouteLocalCmd + `_rfbCommand` **timeout 12s**，默认 3s 不够）、0x50 分派、5802 分派（tvExtHandle* 纯函数 cl=NULL）。**网关 invoke 必须显式传 timeout ≥15s**（网关默认 5s 会 504）。

---

## 5. 规则与约束

1. **能力层唯一**：设备操作只走 RFB→IOHID，禁止前端自造输入协议。
2. **契约两端对齐**：前端 caps.js 自包含定义 + 设备端注册表存 executor；无上报、无元数据、无运行时发现；新增能力 = 设备端注册 + 前端定义数组各加一条。
3. **单注册通道**：仅 18081 TCP JSON；WS 注册端点已废弃。
4. **单会话约束**：设备仅 1 条隧道 + 1 个 5901 连接，同设备同时仅 1 个活跃 VNC 会话（4001 顶替）。
5. **纯隧道**：三端控制台一律走隧道；无隧道 4003 拒绝；无直连回退、无反向模式。
6. **协议先行**：跨端新能力先定契约，两端各自自测后再联调。
7. **状态以网关为准**：前端不持久化设备状态，刷新一律从网关拉取。
8. **改动可验证**：IPA 改动必须 CI 编译通过；网关改动必须有测试（`npm test`）；未验证不声称完成。
9. **无通用 /command 端点**（已删，禁止回归）；screen.diff / screen.waitStable 已删注册（不回归）。
10. **文档纪律**：改动架构/时序/实现后同步更新本文档（增删改查对应章节）；列表类信息（配置项/能力项/按键表）只存在于代码真相源。

---

## 6. 构建与部署

- **CI**：`.github/workflows/build.yml`——push `main` 分支触发（macOS），working-directory=TrollVNC；可选 `workflow_dispatch` 输入（is_managed 打 Managed.plist、desktop_name、port、view_only、scale、frame_rate_spec、modifier_map）。
- **Theos 4 方案**：default(legacy)/rootless/roothide 出 .deb；**bootstrap 出 `.tipa`**（TrollStore 安装产物）。本地构建需 macOS Theos：`devkit/build-all.sh`；**Windows 不能本地构建**，出包只能走 CI。
- **版本**：0.0.1（Makefile PACKAGE_VERSION）；push 触发 release job 创建 v0.0.1 tag + GitHub Release（body=`TrollVNC/CHANGELOG.md`）。
- **部署**：网关跑在软路由 docker（`trollvnc-farm/deploy/` 有 openwrt-docker / openwrt-native 步骤）；`FARM_TOKEN=xxx npm start`。
- **推送**：github.com 直连常被网络阻断 → 走 `scripts/push-via-api.mjs`（Git Data API，api.github.com 正常；支持大文件与 base tree 去重）。

---

## 7. 已知边界与决策记录

- **iOS 后台挂起**：心跳无法阻止挂起；BGTask 后台刷新（15–60min）延长存活；锁屏久仍可能掉线 → 离线可见 + 解锁自动重连。
- **Silent Push 唤醒**：技术上可行但依赖 APNs + 真机实测，**默认不投入**。
- **通知治理**：连接/断开本地通知在农场场景会被控制台自身连接刷屏 → 默认关闭（Managed.plist 预置），保留"首连单条"可选。
- **mDNS**：部分 AP 不可达 → 手动输入网关兜底。**2026-08-28 局域网暴露收敛**：mDNS 的 publish 即广播、不主动发布才不可见（抖音 browse Bonjour/Multipeer 枚举局域网服务，逆向报告 §4 实证）——设备端 `BonjourEnabled` 默认 YES→**NO**（不发布 `_rfb._tcp`/`_http._tcp`；显式开启走 CLI `-B on` 或配置，restart 级生效；Managed.plist 预置本就 false）、网关生产建议 `FARM_MDNS=0`（不广播 `_superphone-farm._tcp`、不做 `_rfb._tcp` 遗留发现）。改动点：`trollvncserver.mm gBonjourEnabled` 内建默认、注册表 `_defaultForKey` 回退、重置默认值写回三处同步；**默认翻转只影响未持久化设备**——存量显式 YES 需网关批量 `BonjourEnabled=false` + 重启服务收敛。
- **私有 API**：随 iOS 升级可能失效，锁定 TrollStore 支持范围，升级前灰度。
- **多设备端到端同步验证**：单台真机链路已大量联调；群控（广播/批量/同步）多设备端到端验证待第二台实体设备。
- **2026-08-20 重置默认值事故**：设置页「重置默认值」曾用 `removePersistentDomainForName` 删空整个配置域，用户重置后设备出现「App 显示已连接但网关端无画面」——三因叠加：① 网关地址被清 → manager（root 进程）拿不到地址不注册（App 写 mobile 域、root 读不到，env 注入是唯一通道但旧进程未带新配置重启）；② watchdog 配置被清 → `_defaultForKey` 回退 0 → 无节流重启风暴（5901 时通时断）；③ 网关 db 残留历史记录 → App `isRegistered`（列表含自身即已连接）误判。修复：重置保留身份/网络信息并写回安全默认、watchdog 默认回退 5/3、manager 兜底读 mobile 域、协调器探活端口为主（沙盒枚举不可作唯一判定）+ FD_CLOEXEC + manager 侧清理孤儿 server、`isRegistered` 需在线才判定、网关 loadDb 注册设备在线初始 false。**决策**：① 重置类操作禁止删空域，必须保留设备身份与网络连接信息并写回安全默认；② 进程探活不得依赖 App 侧进程枚举（iOS 沙盒 KERN_PROCARGS2 读 root 进程 EPERM），端口连通性为主判定、fd 泄漏用 FD_CLOEXEC 治本、孤儿进程清理放 root 侧（manager）；③ root 进程读 mobile 域配置必须兜底文件读取（env 注入不保证覆盖所有重启路径）。
- **2026-08-20 热重载重建 framebuffer 竞态崩溃**：设置页热重载通道（`notify_post → tvApplyPrefsChanged`，main 线程）遍历 hot 级配置时，把 `Scale`/`OrientationPadFix` 当 hot 热重载——它们的 `tvReloadConfigForKey`/回调会调 `maybeResizeFramebufferForRotation`（内部 `free(gFrontBuffer)` + `rfbNewFramebuffer`），与后台 `rfbRunEventLoop(TRUE)` 线程读写同一 framebuffer 竞态 → use-after-free → trollvncserver 反复崩溃（表现为 5901 时通时断、invoke 报「连接 5901 失败」、隧道秒级反复断连）。**修复**：`Scale`/`OrientationPadFix` 改回 `restart` 级（重启服务时单线程重建 framebuffer，安全），从热重载通道与 `tvReloadConfigForKey` 移除这两个 key。**决策**：任何会 `free`/重建 framebuffer（`rfbNewFramebuffer`/`maybeResizeFramebufferForRotation`）的配置，只能是 `restart` 级，绝不能在 RFB 后台线程运行期间从其他线程热重载。
- **2026-08-21 隧道风暴三连修（server 掉线场景的连锁放大）**：trollvncserver 未运行时（4005 场景），控制台出现 4002/4003/504 风暴，三处叠加：① 设备端 TRTunnelClient 在 standby（本地 5901 已断，`_localFd=-1`）收到网关放行的 DATA 帧 → `write(-1)` 失败 → 判定隧道故障断开重连 → 网关侧 4002（close 时关 WS 会话）+ 4003（重连窗口 tunnels 为空）+ CMD 帧黑洞（invoke 504）。修复：standby 收 DATA 丢弃并告警（已死会话的输入字节无意义），隧道保持；会话重建时网关重走 rfb.start（server 真死则明确 4005，不再静默）。② 网关 register 处理对同设备重复注册无条件 destroy 旧记录——设备端设置变更后在**同一连接**重发 register 时 `oldRec.sock === sock`，等于销毁自己 → 注册通道闪断重连，叠加命令通道黑洞。修复：`oldRec.sock !== sock` 才 destroy，同连接重发仅刷新记录。③ App 连接页文案「已注册到网关，隧道已建立」——App 只能从网关目录判定 online（注册通道或隧道任一存活即 true），感知不到隧道/server 真实状态，server 崩溃时误导「一切正常」。修复：文案改「已注册到网关」，只陈述可验证事实。**决策**：① 隧道透传循环中"本地目标不可写"与"隧道链路故障"必须区分——前者丢数据保链路，后者才断开重连；② 网关对"同连接重复注册"与"异连接顶替注册"必须分叉处理；③ UI 不得展示无法感知的状态推断（网关目录 online ≠ 隧道健康 ≠ server 存活，三者语义不同）。
- **2026-08-20 App→root 进程控制改双通知自治（二期整改）**：历史架构中 App 侧停止/重启服务依赖「枚举进程 + kill trollvncmanager/trollvncserver」，但 App（mobile 沙盒）对 root 进程 kill 恒 EPERM——该通路从未真正生效（模式切换不停服务、manager 重启级配置不生效的根因）；网关配置靠 spawn 时刻 env 注入，会永久遮蔽设置页后续修改（网关地址变更不生效的根因之一）。**修复**：① kill 通路全删，改双通知自治——manager 订阅 `prefs-changed`（桥接模式自退 / GatewayClient 重发 register / watchdog 热调）与 `restart-service`（watchdog 重启 trollvncserver，root 杀 root）；② env 注入删除，网关配置统一「root defaults + mobile 域 plist 兜底」双域读取（`tvManagerReadPref`/`TRGatewayClient _gatewayHost`）；③ 网关 host 变更由 GatewayClient worker 超时分支比对 connectedHost 主动断开重连（旧 reregister 会把注册发到旧网关）；④ `TVNCRestartVNCService()` 改 `notify_post`，prefs 设置页（CCTrollVNC 经 symlink 共享 TVNCUtil.h）自动获得新行为。**决策**：App 沙盒内永远无法可靠控制 root 进程生命周期——凡跨权限边界的控制流一律走 darwin 通知由高权限侧自治（订阅方自杀/自重启），spawn 侧只负责「拉起」与幂等 ensure；环境变量只用于一次性初始化，绝不作为运行期配置通道。
- **2026-08-21 prefs-changed 通知与落盘竞态**：设置页写 ConnectionMode 后立即 `notify_post`，而 manager 的双域读取兜底走 plist 文件——cfprefsd 懒落盘会让 root 侧读到**旧值**：relay→bridge 切换读到 relay 不自退，中继服务残留运行（UI 已显示桥接但服务仍按中继跑，模式分叉）。**修复**：`notify_post` 前对该 suite 强制 `synchronize` 落盘。**决策**：跨进程通知必须保证「接收方可读到新值」——通知前强制 flush，或接收方只用 cfprefsd 一致性视图（不得混用通知即时性 + 文件兜底读取的懒落盘语义）。
- **2026-08-22 缩略图统一到 RFB（FT_THUMB 废弃 → RFB 流解码）**：缩略图从「设备端 FT_THUMB JPEG 变化推送 + manager 5802 /thumb 轮询」统一到「RFB 流」——隧道握手成功后 TRTunnelClient connect 本地 5901（缩略图客户端 = 首个 RFB 客户端），经隧道透传 RFB 字节流，网关 ThumbRfbDecoder 作为 RFB 客户端收全尺寸 framebuffer（Raw 编码）解码产 JPEG，前端 CSS 缩放显示；采集从「服务启动即常驻低频」改为「惰性启动」（隧道握手成功 tunnel-connected 通知 → tvStartCaptureIfNeeded @ CaptureFps）+ 升降频（客户端连接升频 FrameRateSpec、capture-idle 通知降回 CaptureFps）；FT_THUMB(0x06)/ThumbPushEnabled/ThumbInterval/pHash 缩略图变化检测全删，manager 不再轮询 5802 /thumb，TRScreenHasher 保留仅供 screen.hash/diff/waitStable。转屏重建 framebuffer（`maybeResizeFramebufferForRotation`）与采集写入/RFB 发送统一用 **gFramebufferLock** 互斥（方向变化竞态 SIGILL 根治）。**决策**：① 缩略图与屏幕流共用同一 RFB framebuffer 流，无独立缩略图路径（无 FT_THUMB、无 pHash、无 manager 轮询）；② 采集惰性启动（首个 RFB 客户端触发）+ 升降频，0 客户端不空转；③ 会 free/重建 framebuffer 的路径（热重载与采集回调）必须与 RFB/采集读写持同一把锁。
