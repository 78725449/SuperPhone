# 99 - 历史归档

> **来源**：IPA改造计划.md 行 28-172（0.A-0.F 反推依据）+ 行 2146-2168（变更记录）
> **关注点**：v1.8 反推方法与发现、v1.7 里程碑修正、真实愿景定型、v1.8 执行路线图、v1.8.4 二次深度核查、变更日志
> **索引**：[返回主文档](IPA改造计划.md)
> **说明**：本文档为历史归档，记录开发过程中的反推依据和变更日志，日常开发一般无需查阅

---

## v1.8 反推依据（基于实际代码核对，2026-08-08）

> **背景**：v1.7 自称"全任务完成版"，但结合最新逻辑说明与实际代码反推，发现 6 大类声明与实现严重脱节的过时依据，需修订并落实新愿景。

### 0.A 反推方法

逐文件读取实际代码 → 与计划声称状态对照 → 列出声明 vs 实现的偏差 → 作为计划过时的硬证据。

### 0.B 反推发现的 6 大类过时依据

| 类别 | 计划声称 | 实际代码反推 | 证据位置 |
|---|---|---|---|
| **A. 反向连接清理** | v1.3 起多次声称"ReverseMode 已废弃，代码已彻底删除（附录 E.10）" | **全量残留**：全局变量/配置解析/CLI/反向连接线程均未删 | trollvncserver.mm 行 128-131 / 185-187 / 777-851 / 1001-1087 / 3694 / 4043 / 4072 / 4906-4949；TVNCConnectViewController.m 行 324-326 / 373 / 388 / 394 / 479-481 / 695；TVNCRootListController.m 行 373 / 391；**TRCapabilityRegistry.mm 行 964 仍注册 ReverseMode schema** |
| **B. 18181 隧道** | 0.2 端口规划明确"18181 隧道端口" | **完全未实现**：无监听、无客户端、无协议 | TRGatewayClient.mm 行 289 仅连 18081 注册端口；index.js 仅监听 8080/18081；全代码库 grep `TRTunnelClient`/`TRTunnelServer` **零匹配**；TVNCViewerViewController.m 行 222-224 直连设备 5801（端口差 100）；web app.js 行 112 卡片墙走 `/ws/vnc/:id` 完整 RFB |
| **C. 配置 schema 缺失** | 4.3 声称"34 项全量上报" | **6 项关键 schema 未注册** | TRCapabilityRegistry.mm `_registerConfigSchemas` 未注册 `Enabled` / `GatewayHost` / `GatewayPort` / `GatewayToken` / `SslCertFile` / `SslKeyFile` |
| **D. UI 规范未落地** | 0.4.1 声称"网关移动端 opsMenu 需改垂直列表" | **仍为 4 列网格**；**卡片墙未用 invoke screenshot API** | style.css 行 179 `.ops-menu-grid { grid-template-columns: repeat(4, 1fr) }`；web app.js 行 112 `inst.rfb = createRfb(tv, d, { viewOnly: true })` 完整 RFB 连接（非 invoke screenshot） |
| **E. 营销残留** | Phase 2 声称"已完成去营销（清除 View Source Code / Made with ♥ / 付费入口）" | **关键入口残留** | TVNCRootListController.m 行 808-813 `- (void)support` 方法 → havoc.app 付费市场搜索；行 815-818 `- (void)source` 方法 → `https://github.com/OwnGoalStudio/TrollVNC` |
| **F. 配置合并未完成** | v1.4/v1.5 声称"SingleNotif+ClientNotifs 合并为 Notifications 枚举；性能参数合并为 PerformanceMode 枚举" | **未合并** | TRCapabilityRegistry.mm 行 970-971 仍独立注册 `SingleNotifEnabled`/`ClientNotifsEnabled`；TVNCSettingsViewController.m 行 144-145 仍独立引用；全代码库无 `PerformanceMode` 枚举实现 |

### 0.C v1.7 里程碑状态修正

下列 v1.7 标记"✅ 已完成"的里程碑，经反推实际为**部分完成或声明虚高**，状态降级：

| 里程碑 | v1.7 声称 | v1.8 修正 | 依据 |
|---|---|---|---|
| M3 (Phase 2 去营销) | ✅ 已完成 | 🟡 部分完成 | TVNCRootListController.m 营销方法未删 |
| M6 (Phase 4.1 注册表) | ✅ 已完成 | 🟡 部分完成 | ReverseMode schema 仍注册；6 项网关/SSL schema 缺失 |
| M8 (Phase 4.3 全量上报) | ✅ 已完成 | 🟡 部分完成 | 同上 6 项缺失 |
| M14 (Phase 5 控制端改造) | ✅ 已完成 | 🟡 部分完成 | Viewer 直连 5801 未走隧道；Controller 缩略图改对但卡片墙未改 |
| 0.2 端口规划 | 18181 隧道端口 | ❌ 未实现 | 见上表 B |
| 0.4.1 移动端菜单 | 垂直列表 | ❌ 未改 | style.css 仍 4 列网格 |
| 附录 E.10 | 反向模式代码已删 | ❌ 全量残留 | 见上表 A |

### 0.D 真实愿景（v2.3 修订：卡片墙方向更新）

> 基于用户明确的原话愿景，定型如下（不再使用旧版"iOS 设备群控，装一次初始化一次"概括）。
> **v2.3 修订**：v1.8.3 "卡片墙只获取帧（invoke screenshot API）"设计**已弃用**，改为"卡片墙建立完整 RFB 连接（viewOnly）"，ThumbInterval 作为可选的 RFB 帧渲染频率控制（默认不限制）。

**连接架构**：
1. 设备**携带信息主动注册到网关以后开启隧道**——携带信息注册到网关以后隧道就好了
2. 同时 **5901/5801 端口还可以在不经过网关的前提下对外提供访问**（局域网直连保留）
3. 而**其他所有的连接都是走隧道**

**控制端设备列表来源**（v2.3 补充定版）：
4. IPA 控制端、手机 web、PC web 三端的设备列表**统一从网关 `/api/devices` 拉取同网关下所有已注册设备**
5. 拉取后**必须排除自身 deviceId**（IPA 控制端不应显示本机卡片，避免自控自）
6. **去重由网关侧负责**：三层去重（register 优先 / host:port 匹配升级 / TCP 探测同步），控制端拉取到的列表不会出现重复卡片
7. 直连设备（有 host 字段）与隧道设备（source=register）都纳入卡片墙显示

**卡片屏幕墙**（v2.3 修订）：
8. ~~所有的卡片屏幕墙都是**只获取帧**（可以调节获取的频率），不建立完整 RFB 连接~~（v1.8.3 旧设计，已弃用）
9. 所有控制台卡片墙**建立完整 RFB 连接**（viewOnly 只读），实时渲染画面
10. ThumbInterval 作为可选的 **RFB 帧渲染频率控制**（默认不限制=实时渲染，配置后按阈值节流）
11. invoke screenshot 仅保留为大屏操控时的单帧截图能力
12. 在控制台和 IPA 控制端页面，**点击卡片墙的屏幕卡片才进入操控**，是通过**隧道连接**的

**能力即服务**：
13. 所有的 IPA 能力就是服务，通过控制台（电脑/手机/IPA 控制端）可以**批量调整覆盖配置**和**调用控制能力**
14. 新增能力即插即用，避免开发一个小能力要全部折腾一遍

**UI 交互规范**（核心，按端区分）：
15. **IPA 控制端页面和手机 web 网页控制端**：
    - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**
    - 点击卡片后：**悬浮按钮**，能力以**纵向列表分组菜单**显示
16. **前端控制台（PC web）**：
    - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**
    - 点击卡片对设备进行大屏操作以后：在屏幕右侧是**纵向分组菜单的图标**
17. **5801 端口直连访问**：
    - 电脑访问：能力以**纵向列表分组菜单**显示（和电脑端点击卡片以后的大屏类似，只是少了外层包装）
    - 手机端访问：直接是**全屏 + 悬浮按钮**，点击悬浮按钮后是**纵向列表能力分组菜单**

**其他**：
18. **彻底去营销**：所有外链/付费/源码/赞助入口 100% 清除
19. **配置合并收尾**：Notifications / PerformanceMode 枚举合并落地
20. **反向连接彻底清除**：全链路删除

### 0.E v1.8 新执行路线图

为落实新愿景，新增两个阶段：

- **Phase 6：反向连接与营销彻底清理**（详见第 6 节）— 删除全部残留代码与营销入口
- **Phase 7：18181 隧道落地**（详见第 7 节）— 设备 TRTunnelClient + 网关 TRTunnelServer + 三端接入改造
- **Phase 8：配置 schema 补齐与枚举合并收尾**（详见第 8 节）— 6 项缺失 schema + Notifications/PerformanceMode 合并
- **Phase 9：三端 UI 规范统一**（详见第 9 节）— 移动网页 4 列网格改垂直列表 + 卡片墙改 invoke screenshot API
- **Phase 10：二次核查偏差修复**（详见第 10 节）— 5801 默认启用 + 前端批量操作栏 + 能力菜单分组 + IPA 卡片⋯菜单 + 注册隧道时序 + 帧率可配置 + PerformanceMode 反向映射 + 死代码清理

### 0.F v1.8.4 二次深度核查（Phase 6-9 执行后，2026-08-08）

> **背景**：v1.8.1 声称 Phase 6-9 全部执行完成，v1.8.2/v1.8.3 定型愿景。本次对每个细微时序再次深入核查真实代码与计划差异，发现 **4 类严重偏差 + 3 类中等偏差 + 5 类轻微偏差**，需 Phase 10 落地修复。

#### 0.F.1 严重偏差（违背愿景核心，必须修复）

| # | 偏差项 | 愿景/计划声称 | 实际代码反推 | 证据位置 |
|---|---|---|---|---|
| **S1** | **5801 端口默认禁用** | "5901/5801 端口还可以在不经过网关的前提下对外提供访问" | HttpPort 默认值=0（禁用），用户开箱即用时局域网 WebSocket 直连不可用，仅 5901 RFB 可用 | Root.plist 行 566-567 `<integer>0</integer>`；trollvncserver.mm 行 116 `gHttpPort=0`、行 654-664 读取、行 4530-4557 `if(gHttpPort>0)` 才启用 |
| **S2** | **前端批量操作完全缺失** | "通过控制台可以批量调整覆盖配置和调用控制能力"（0.D 第 7 条）；0.4.5 "网关设备墙顶部新增批量操作栏" | 后端 batchInvoke/batchSetConfigs/batchRestart 三个 API 已实现（index.js 行 684/697/712），caps.js 也已封装三个函数（行 103/120/136），但 **app.js 未导入未调用，index.html 无批量操作栏、无多设备勾选机制、无 restart 二次确认弹窗** | app.js 行 3 import 未含批量函数；index.html 行 13-41 顶部 actions 无批量栏；grep `selectedDevices\|selectAll\|批量` 在 web 目录零匹配 |
| **S3** | **能力菜单"分组"三端均未落地** | 愿景反复强调"纵向列表**分组**菜单"（0.D 第 9/10/11 条） | caps.js 的 `category` 元数据字段存在但**所有渲染逻辑（renderCapOps/showTileMenu/#bar/#panel/IPA UIMenu）均为扁平列表**，无任何分组分隔 | app.js 行 201-225 renderCapOps 扁平遍历；index.vnc #bar/#panel 扁平；TVNCViewerViewController.m 行 411-421 硬编码 9 项扁平 UIMenu |
| **S4** | **IPA 控制端卡片无⋯菜单 + Viewer 菜单硬编码** | "IPA 控制端点击卡片前卡片右下角⋯→纵向列表分组菜单；点击卡片后悬浮按钮→纵向列表能力分组菜单" | TVNCDeviceCardCell 无 moreButton/longPress，点击直接进 Viewer；Viewer 的 gearBtn 菜单为 9 项硬编码（home/power/volup/...），非 capMetadata 数据驱动，无分组。计划声称"IPA 已是垂直列表无需改动"**不实** | TVNCControllerViewController.m 行 90-209 TVNCDeviceCardCell 无⋯按钮；TVNCViewerViewController.m 行 411-421 硬编码 ops 数组 |

#### 0.F.2 中等偏差（功能受限或时序问题，应修复）

| # | 偏差项 | 愿景/计划声称 | 实际代码反推 | 证据位置 |
|---|---|---|---|---|
| **M1** | **注册→隧道时序竞态** | "携带信息注册到网关以后开启隧道"（注册成功后开隧道） | 实际时序：发送 register 后**立即**调用 _startTunnel（不等 ack），隧道 hello 可能在注册被处理前到达 18181，被网关以"device not registered"拒绝，需 2s 退避重连 | TRGatewayClient.mm 行 327 `_startTunnel` 在 write(register) 后立即调用，先于 read 循环收 ack；index.js 行 971-978 tunnelServer 校验 findDevice |
| **M2** | **帧获取频率不可配置且三端不一致** | "可以调节获取的频率"（0.D 第 4 条） | web 硬编码 5 秒（app.js 行 183 `setTimeout(...,5000)`），IPA 硬编码 10 秒（TVNCControllerViewController.m 行 363 `scheduledTimerWithTimeInterval:10.0`），无任何配置项，两端默认值不一致 | grep `thumbInterval\|screenshotInterval\|wallInterval` 全项目零匹配 |
| **M3** | **PerformanceMode 缺少 currentConfigs 反向映射** | 枚举合并应双向闭环（Notifications 已实现反向映射） | setConfig("PerformanceMode","balanced") 会映射底层 4 参数，但 currentConfigs 不反向推导——外部直接 setConfig("TileSize",99) 后上报的 PerformanceMode 仍为"balanced"，状态不一致 | TRCapabilityRegistry.mm 行 1074-1101 currentConfigs 仅读枚举字符串，不按底层参数反推；对比 Notifications 行 1093-1100 已实现反向映射 |

#### 0.F.3 轻微偏差（死代码/文档/配置，宜清理）

| # | 偏差项 | 实际代码反推 | 证据位置 |
|---|---|---|---|
| **L1** | **isRepeaterClient 死代码链** | 反向连接删除后遗留孤儿数据管线：结构体字段→JSON 快照→TSV 列→能力解析→UI 读取，数据源已删但管线未清，永远输出 NO | trollvncserver.mm 行 2821/3559/3566/3581/3586/3856/3859/3921-3924；TRCapabilityRegistry.mm 行 712/725；TVNCClientListController.m 行 500/552 |
| **L2** | **build.yml 反向连接工作流输入** | gen-managed-plist.sh 已不消费 TVNC_REVERSE_MODE/SOCKET/REPEATER_ID，但 build.yml 仍定义 3 个 input 并传递 | .github/workflows/build.yml 行 46-64 input 定义、行 121-123 env 传递 |
| **L3** | **gRfbEventThread 孤儿线程函数** | tvStartRfbEventThread 从未被调用，注释标注"reverse-connection mode" | trollvncserver.mm 行 4587-4626 |
| **L4** | **TROLLVNC_REPEATER_RETRY_INTERVAL 孤儿环境变量** | watchdog 设置但 trollvncserver 从不 getenv 读取 | trollvncmanager.mm 行 233；com.82flex.trollvnc.plist 行 15-16 |
| **L5** | **Root.plist 仍保留 SingleNotif/ClientNotifs 开关** | Schema 层已合并为 Notifications 枚举，但偏好设置 bundle 仍暴露 2 个独立开关，用户可绕过枚举映射 | TrollVNCPrefs/Resources/Root.plist 行 539/551 |

#### 0.F.4 已验证完全落地的项（无偏差）

| 项 | 结论 | 证据 |
|---|---|---|
| **18181 隧道协议完整性** | ✅ 100% | 设备侧 TRTunnelClient.mm（591 行）+ 网关侧 index.js tunnelServer，帧封装/5 种帧类型/select/退避/握手鉴权/DATA 双向透传/CMD 路由全部实现，两端协议完全对齐 |
| **反向连接核心代码清理** | ✅ 彻底 | ReverseMode/isRepeaterEnabled/gRepeaterMode/ReverseSocket/ReverseRepeaterID 在源码零匹配；support/source 营销方法已删；.strings 营销字符串已删 |
| **6 项 schema 注册** | ✅ 全部 | Enabled/GatewayHost/GatewayPort/GatewayToken/SslCertFile/SslKeyFile 均在 _registerConfigSchemas 注册（行 972-977） |
| **Notifications 枚举合并** | ✅ 完整闭环 | schema 注册 + setConfig 正向映射 + currentConfigs 反向映射三段链路完整 |
| **卡片墙帧获取方式** | 🟡 待修正（v2.3） | IPA TVNCFetchThumbnailViaRFB 已用 RFB 首帧获取（符合 v2.3 新方向）；web 仍用 startWallThumbnail 调用 invokeCap("screenshot")（v1.8.3 旧设计，需改回 createRfb viewOnly 持久连接） |
| **跨网络隧道转发** | ✅ 完整 | /ws/vnc/:id → handleVncSocket → 隧道 FT_DATA → 设备 127.0.0.1:5901；sendDeviceCmd 隧道优先 |
| **IPA 按设备来源自动判断隧道** | ✅ 完整 | source=register→useGatewayTunnel=YES 走网关 8080；source=mdns/manual→直连 5801 |
| **CSS 网格→垂直列表** | ✅ 已改 | style.css .ops-menu-grid 为 flex-direction:column，无 4 列网格残留 |
| **网关 REST API（单台+批量后端）** | ✅ 完整 | 13 个单台端点 + 3 个批量端点，覆盖语义 + 部分成功 + 结果汇总 |

#### 0.F.5 Phase 10 修复路线图（✅ 已全部完成，2026-08-08）

为修复上述偏差，新增 Phase 10（详见第 10 节），**已全部执行完成**：
- **10.1** ✅ 5801 默认启用（Root.plist HttpPort 默认值 0→5801）
- **10.2** ✅ 前端批量操作栏落地（index.html 新增 #batchBtn + app.js 导入 batchInvoke/batchSetConfigs/batchRestart + selectedDevices Set + toggleSelect/selectAll/showBatchMenu + restart 二次确认弹窗 + style.css .batch-bar/.tile-checkbox/.batch-menu 样式）
- **10.3** ✅ 能力菜单 category 分组渲染（设备侧 TRCapabilityRegistry.mm 新增 _inferCategoryForCapId:route: 按 capId 前缀+route.type 推断 category + _categoryTitle: 中文映射 + _controlMetadata 输出 category/categoryTitle；前端 caps.js 新增 CATEGORY_LABELS + groupByCategory 函数；app.js renderCapOps/showTileMenu 按 category 分组渲染 + .cap-group-title/.cap-group-divider 样式）
- **10.4** ✅ IPA 卡片⋯菜单 + Viewer 菜单改造（TVNCDeviceCardCell 新增 moreButton⋯按钮 + moreTapped block 回调 + 控制器 onDeviceCardMoreTapped 弹 UIAlertController actionSheet 按 category 分组 "[中文分组] 能力名" + fetchCapMetadataForDevice:completion: GET /api/devices/:id/caps + invokeCap:params:forDevice: POST invoke；TVNCViewerViewController 删除硬编码 ops 数组 + fetchCapMetadata: 异步获取 + rebuildGearMenuWithCaps: UIMenu 内联分组 + 直连模式回退 performOp/keyboardTapped/clipboardTapped）
- **10.5** ✅ 注册→隧道时序修正（TRGatewayClient.mm 新增 _tunnelStarted 标志 + _connectAndRun 中删除 write(register) 后的 _startTunnel 调用改为 _tunnelStarted=NO + _handleServerLine 收到 ack 后检查 !_tunnelStarted 调用 _startTunnel）
- **10.6** ✅ 帧获取频率可配置化（设备侧 TRCapabilityRegistry.mm 新增 ThumbInterval schema type=number min=1 max=60 reload=instant + _defaultForKey 默认 5 + Root.plist 新增 PSEditTextCell；web app.js 新增 refreshThumbInterval 读取设备 configs.ThumbInterval 缓存 30s + startWallThumbnail 两处 setTimeout 用 inst.thumbIntervalMs；IPA TVNCControllerViewController 新增 thumbInterval 属性默认 5.0 + refreshThumbIntervalFromGateway + fetchThumbIntervalForDevice:completion: + applyThumbInterval: 动态重建定时器）
- **10.7** ✅ PerformanceMode currentConfigs 反向映射补齐（TRCapabilityRegistry.mm currentConfigs 读取底层 TileSize/MaxRects/FullscreenThresholdPercent/AsyncSwap 实际值与预设表比对，匹配 balanced/quality/performance 上报对应枚举，都不匹配上报 custom，覆盖直接读取的字符串）
- **10.8** ✅ 死代码与孤儿配置清理（L1: isRepeaterClient 死代码链全清——trollvncserver.mm 结构体字段/JSON/TSV/clientGoneHook + TRCapabilityRegistry.mm 注释/字典/索引调整 + TVNCClientListController.m isRep/cols 索引调整；L2: build.yml 3 个 input+3 个 env 全删；L3: gRfbEventThread/tvRfbEventThreadMain/tvStartRfbEventThread/tvStopRfbEventThread 全删+清理路径调用删除；L4: TROLLVNC_REPEATER_RETRY_INTERVAL trollvncmanager.mm+LaunchDaemon plist+README 全删；L5: Root.plist SingleNotifEnabled/ClientNotifsEnabled 2 个 specifier 全删）

---

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-08 | v1.0 | 基于能力即服务架构全面重建；移除过时基线（不做命令通道/远程配置等）；新增附录 B-G；Phase 4 细化为 8 个子阶段 |
| 2026-08-08 | v1.1 | 代码核查修正：46752 命令参数 block/unblock 为 host 非 clientId；新增 subscribe 推送能力；热重载方案修正为 tvReloadConfigForKey 更新 C 全局变量；screenshot 确认为 ScreenCapturer 单帧静默截图；移除 stop 能力仅保留 restart；M11/M12/M13 标记并行；新增 Phase 5 IPA 控制端面板改造；缓存策略改为设备主动推送；触控类能力支持批量 |
| 2026-08-08 | v1.2 | FAB 实现确认沿用现有代码（不重新定义）；IPA 控制端移除画面旋转能力（无需求）；version 字段不上报；新增 0.4.1 菜单规范（PC 端图标列/手机端垂直带名称列表）；新增 0.4.2 批量操作入口（顶部批量栏）；网关移动端 opsMenu 需从 4 列网格改为垂直列表 |
| 2026-08-08 | v1.3 | 附录 A 移除反向模式相关互斥规则 #1/#2/#3/#11（ReverseMode 彻底废弃，代码层删除）；新增 screen.fbw/fbh 字段（RFB 帧缓冲尺寸）支撑 AI 截图坐标转换；附录 C 新增 AI/脚本坐标转换完整流程说明；控制端去重规则确认（网关三层去重已实现）；IPA 控制端排除自身 deviceId 推迟到 Phase 5 补齐；修正 3 处旋转遗留错误（Phase 5 容器法旋转改为已移除/screen 字段变更频率去除旋转/设备旋转场景标注不适用） |
| 2026-08-08 | v1.4 | 配置项治理：合并 SingleNotif+ClientNotifs 为 Notifications 枚举（all/connectOnly/silent）；合并 TileSize+MaxRects+FullscreenThresholdPercent+AsyncSwap 为 PerformanceMode 枚举（balanced/quality/performance/custom + 预设值映射）；统一 Port+BindHost+HttpPort+HttpDir+KeepAliveSec+BonjourEnabled 为网络组；移除反向模式配置项；每项补充详细说明（作用/影响范围/推荐值）；新增 E.10 已废弃配置兼容映射；不彻底移除任何配置项（仅合并和补充说明） |
| 2026-08-08 | v1.5 | 原生能力全覆盖核查：基于 STHIDEventGenerator.h/ClipboardManager.h/ScreenCapturer.h/BulletinManager.h/TRWatchDog.h 完整接口核查，发现 67 个原生已实现但未暴露的能力（占总量 83%）；附录 D 从 14 项扩展到 81 项；修正 type.text 重复造轮子（新增 type.paste 利用 ClipboardManager 支持任意文本输入）；新增触控笔能力（4 项）、通知能力（4 项）、服务状态查询（4 项）、通用 HID Consumer（6 项）、按键底层 Down/Up（17 项）、多击/长按（7 项）、高级触控（曲线滑动/捏合/事件流/多指等 10 项）；仅 8 项需新实现（screenshot 静默截图/type.paste/clients.* 6 项） |
| 2026-08-08 | v1.6 | 深度核查 .mm 实现层，发现并修正 5 处重复造轮子/空壳/BUG：(1) clipboard.paste 语义混乱（读+写回空操作，合并为 clipboard.get）；(2) screenshot 注册为空壳（executor 返回 code:99，需调用 ScreenCapturer 真正实现）；(3) restart 双重入口不统一（网关 cmd 与能力注册表脱节，统一注册为 service.restart）；(4) shake 空实现 BUG（.mm 方法体为空）；(5) TRCapRouteLocalCmd 路由定义但从未使用，6 项 clients.* 能力悬空。新增 20 个原生覆盖盲点：触控 6 项（touchDownAtPoints 数组版多指/taps 通用 N 击/reset/event 单发/downMultiAt/upMultiAt）、系统查询 6 项（sys.version/configSnapshot/resolution/rotation/stats.inflight/bonjour.txt）、ScreenCapturer 扩展 5 项（capture.start/stop/fps/resolution/forceRefresh）、网关客户端 3 项（gateway.isConnected/reconnect/deviceInfo）、service.validate 校验配置；附录 E 新增 4 项原生可写配置（WatchdogThrottleInterval/WatchdogKeepAlive/WatchdogExitTimeout/HIDKeepAliveInterval）；附录 D 从 81 项扩展到 95 项，待注册 72 项（76%），需新实现/修正 11 项 |
| 2026-08-08 | v1.6.1 | 改造计划审查修正 10 处问题：(1) Phase 4.3 移除 ReverseMode 可远程 set 的矛盾描述（已废弃）；(2) 附录 A #10 改引用 OrientationPadFix 为 OrientationSync（原引用已废弃）；(3) Phase 4.1 路由类型示例移除 clipboard.paste（已合并）；(4) Phase 4.1 配置分级示例移除 reverseMode（已废弃）；(5) Phase 4.1 已完成数字更新 11→13 控制/30→34 配置；(6) Phase 4.5 引用修正 D.4→D.8；(7) Phase 4.5 block 参数修正 host→clientId；(8) 路由类型表补充 watchdog/stylus 两种路由；(9) Phase 4.4 热重载方案补充 Watchdog/HID 配置项 + Scale 热重载需触发重新 register；(10) 术语统一 reload reconnect→gateway；新增 P0 修复里程碑 M10.5/M11.5/M12.5/M12.6/M13.5 对应 Batch 0-5 编排；风险表补充 4 项 v1.6 新风险 |
| 2026-08-08 | v1.7 | 一次性完成全部待开发任务（M11/M13/M14）：(1) Phase 4.4 配置热重载 — 抽取 parseFrameRateSpec 统一解析（修正原逗号分隔 bug，支持 min:pref:max/min-max/单值三种格式）、tvReloadConfigForKey 补齐 WheelTuning/ModifierMap/FullscreenThresholdPercent/WheelStepPx+gWheelMaxStepPx 同步、TRCapabilityRegistry.setConfig 按 reload 策略分发（hot→tvReloadConfigForKey 或 Watchdog/HID 属性、restart→watchdog restart、gateway/instant→NSUserDefaults KVO 自动触发 _defaultsChanged）；(2) Phase 4.8 AI WS 接口 — 网关 index.js 新增 handleControlSocket + /ws/control/:id 路由，支持 ping/query/invoke/set/restart 五命令，ACK 透传设备返回数据字段；(3) Phase 5.1 Viewer 改造 — TVNCViewerViewController 从 libvncclient 重写为 WKWebView+noVNC（删除 SASL 桩/C 回调/rfbLoop/renderFrame/performOp/canvasContainer 旋转残留，新增 ViewerWeb.html 桥接页面 + OC↔JS 双向通信）；(4) Phase 5.2 Controller 改造 — 新增 selfDeviceId 属性过滤自身设备、缩略图从 libvncclient 改为网关 invoke screenshot API（复用能力即服务架构，避免重复实现 RFB 协议）；M11/M13/M14 里程碑标记完成 |
| 2026-08-08 | v1.8 | **基于实际代码反推修订版**。结合最新逻辑说明与实际当前代码实现反推，发现 v1.7 自称"全任务完成版"存在 6 大类声明与实现严重脱节：(A) 反向连接代码全量残留（trollvncserver.mm 行 128-131/185-187/777-851/1001-1087/4906-4949 等）；(B) 18181 隧道完全未实现（无监听/无客户端/无协议）；(C) 6 项网关/SSL schema 未注册（Enabled/GatewayHost/GatewayPort/GatewayToken/SslCertFile/SslKeyFile）；(D) UI 规范未落地（style.css 行 179 仍 4 列网格、卡片墙仍用 createRfb）；(E) 营销残留（TVNCRootListController.m 行 808-818 support/source 方法）；(F) 配置合并未完成（SingleNotif/ClientNotifs 仍独立注册、无 PerformanceMode 枚举）。修订内容：(1) 顶部新增 v1.8 反推依据章节（0.A 反推方法/0.B 6 大类依据/0.C 里程碑状态修正/0.D 新愿景/0.E 路线图）；(2) 0.2 端口规划表标注 18181 未实现、0.4.1 菜单规范表标注移动端未改；(3) Phase 4.1/4.3 标记为🟡部分完成并标注具体偏差；(4) Phase 5.1/5.2 标注 Viewer 未走隧道、卡片墙未改 invoke screenshot；(5) 附录 E.10 澄清反向模式实际全量残留；(6) 新增 Phase 6（反向连接与营销清理）/Phase 7（18181 隧道落地）/Phase 8（schema 补齐与枚举合并）/Phase 9（三端 UI 规范统一）四个新阶段；(7) 里程碑表新增 M15-M18 待执行阶段，M14 降级为🟡部分完成；(8) 落实用户新愿景：App 富能力 + 非琐碎页面 + 彻底去营销 + 注册后隧道 + 5901/5801 局域网直连保留 + 三端 UI 规范 + 配置合并收尾 + 反向连接全链路删除 |
| 2026-08-08 | v1.8.1 | **Phase 6-9 全部执行完成**。(1) Phase 6 反向连接与营销清理：删除 trollvncserver.mm 7 处反向连接代码（全局变量/isRepeaterEnabled/配置解析/CLI/日志引用/事件线程/配置快照）、TRCapabilityRegistry.mm schema 注册+默认值、TVNCConnectViewController.m 8 属性+6 方法+UI 控件、TVNCRootListController.m support/source 营销方法+本地化、TVNCClientListController.h/.m reverseMode 属性、4 个 .strings 文件约 20 条营销字符串、gen-managed-plist.sh 3 处配置；grep 验证全部零匹配。(2) Phase 7 18181 隧道落地：新建 TRTunnelClient.h/.mm（552 行，帧封装协议 [type:1B][length:4B BE][payload]，5 种帧类型 DATA/PING/PONG/CMD/CMDACK，select 多路复用，2s→30s 退避重连）；修改 TRGatewayClient.mm（注册成功后启动隧道+注入 commandHandler 复用命令处理）；修改 index.js（新增 tunnelServer 监听 18181、tunnels Map、sendDeviceCmd/handleVncSocket 优先走隧道、isDeviceOnline 在线判定）；修改 TVNCViewerViewController.h/.m（useGatewayTunnel 属性，跨网络走网关 8080 桥接）；修改 TVNCControllerViewController.m（按设备来源自动判断隧道模式）；Makefile 加入 TRTunnelClient.mm。(3) Phase 8 schema 补齐与枚举合并：TRCapabilityRegistry.mm 新增 6 项 schema（Enabled/GatewayHost/GatewayPort/GatewayToken/SslCertFile/SslKeyFile）+ Notifications 枚举（all/connectOnly/silent，setConfig 映射底层 key，currentConfigs 反向映射）+ PerformanceMode 枚举（balanced/quality/performance/custom，预设映射 4 个底层参数）；TVNCSettingsViewController.m 替换 2 个 switch 为 Notifications choice + 新增 PerformanceMode choice。(4) Phase 9 三端 UI 规范：style.css .ops-menu-grid 从 4 列网格改为垂直 flex 列表（图标+名称横向布局）；index.html 静态按钮改为 cap-icon+cap-name 结构；app.js renderCapOps 按钮改为图标+名称、createWallTile 删除 createRfb 改为 startWallThumbnail（5 秒/帧 invoke screenshot API，8 秒防抖，连续失败 3 次标记不可达）、enterFocus/restoreWallTile/updateWallTile 同步改造；IPA 控制端确认已是垂直列表无需改动。M14-M18 全部标记完成，0.2 端口规划/0.4.1 菜单规范/4.1/4.3/5.x/E.10 状态全部更新为已完成 |
| 2026-08-08 | v1.8.2 | **愿景定型**。将顶部目标愿景替换为用户明确的真实愿景：(1) 连接架构：设备携带信息主动注册到网关→注册成功后隧道自动建立→5901/5801 局域网直连保留→其他连接走隧道；(2) 卡片屏幕墙：只获取帧（可调频率）→点击卡片才通过隧道建立完整 RFB；(3) 能力即服务：通过控制台批量调整覆盖配置和调用控制能力；(4) UI 交互规范定型：卡片墙状态所有端卡片右下角⋯→纵向列表分组菜单；大屏操控状态 PC web 右侧纵向分组菜单图标列 / 手机 web+IPA 控制端悬浮按钮→纵向列表能力分组菜单；5801 电脑访问纵向列表分组菜单（类似 PC 大屏少包装）/ 5801 手机访问全屏+悬浮按钮→纵向列表能力分组菜单。同步更新 0.3 连接方向与身份（补充隧道自动建立+局域网直连+跨网络走隧道）、0.4 UI 界面与交互规范全面重写（0.4.1 界面定位/0.4.2 卡片墙交互/0.4.3 大屏操控交互/0.4.4 卡片墙帧获取/0.4.5 批量操作入口）、0.D 真实愿景定型 |
| 2026-08-08 | v1.8.3 | **愿景按用户原话定型**。v1.8.2 的愿景虽内容正确但措辞为概括改写，用户要求严格按原话重写：(1) 删除旧版"IOS 设备群控，装一次初始化一次"概括，改为用户原话流式描述；(2) 顶部摘要重写为用户原话：连接架构"主动注册到网关以后开启隧道+5901/5801 不经网关可访问+其他连接走隧道"、卡片墙"只获取帧（可调频率）"、能力即服务、操控连接"点击卡片墙屏幕卡片才进入操控通过隧道"、UI 按端区分（IPA 控制端+手机 web / 前端控制台 / 5801 电脑 / 5801 手机各自的点击前后交互）；(3) 0.D 节同步按原话重写，保留用户分端叙述逻辑（IPA 控制端+手机 web 为一组、前端控制台为一组、5801 电脑/手机各一组）；(4) 0.4 节标题版本号同步更新为 v1.8.3 |
| 2026-08-08 | v1.8.4 | **二次深度核查（Phase 6-9 执行后）**。用户要求"再次深入排查每个细微时序中真实项目和计划的差异"。并行启动 6 个搜索子代理核查 A-F 六大维度（反向连接清理/18181 隧道/配置 schema 枚举/三端 UI/连接架构时序/能力即服务批量），新增 0.F 节记录核查结果。发现 **4 类严重偏差**（S1: 5801 端口默认禁用 HttpPort=0 违背"5801 可对外访问"愿景；S2: 前端批量操作完全缺失——后端 batchInvoke/batchSetConfigs/batchRestart 已实现但 app.js 未导入未调用、index.html 无批量栏无勾选无二次确认；S3: 能力菜单"分组"三端均未落地——caps.js 有 category 字段但所有渲染逻辑扁平遍历；S4: IPA 卡片无⋯菜单+Viewer 菜单硬编码 9 项非数据驱动——计划声称"IPA 已是垂直列表无需改动"不实）、**3 类中等偏差**（M1: 注册→隧道时序竞态——_startTunnel 在 send register 后立即调用不等 ack，隧道 hello 可能先于注册到达被拒；M2: 帧获取频率不可配置且三端不一致 web=5s/IPA=10s 硬编码；M3: PerformanceMode 缺少 currentConfigs 反向映射与 Notifications 不对称）、**5 类轻微偏差**（L1: isRepeaterClient 死代码链；L2: build.yml 反向连接工作流输入；L3: gRfbEventThread 孤儿线程；L4: TROLLVNC_REPEATER_RETRY_INTERVAL 孤儿环境变量；L5: Root.plist 仍保留 SingleNotif/ClientNotifs 开关）。同时验证 9 项完全落地（18181 隧道协议/反向连接核心清理/6 项 schema/Notifications 闭环/卡片墙帧获取/跨网络隧道转发/IPA 自动判断隧道/CSS 垂直列表/网关 REST API）。新增 Phase 10 修复路线图（10.1-10.8 共 8 项） |
| 2026-08-08 | v1.9 | **Phase 10 偏差修复全部完成**。用户确认执行 Phase 10，按文件冲突域分 3 个并行子代理执行批次1（10.1-10.7）+ 1 个子代理执行批次2（10.8）。(1) 10.1 5801 默认启用：Root.plist HttpPort 默认值 0→5801；(2) 10.2 前端批量操作栏：index.html #batchBtn + app.js 导入 batchInvoke/batchSetConfigs/batchRestart + selectedDevices Set + toggleSelect/selectAll/deselectAll/showBatchMenu/doBatchInvoke/showBatchConfigPanel + restart confirm 二次确认 + style.css .batch-bar/.tile-checkbox/.batch-menu；(3) 10.3 能力菜单分组：设备侧 _inferCategoryForCapId:route: 按 capId 前缀+route.type 推断（hid/touch/stylus/system/native/service/gateway）+ _categoryTitle 中文映射 + _controlMetadata 输出 category/categoryTitle；前端 caps.js CATEGORY_LABELS+groupByCategory + app.js renderCapOps/showTileMenu 分组渲染 + .cap-group-title/.cap-group-divider；(4) 10.4 IPA 卡片⋯菜单+Viewer 改造：TVNCDeviceCardCell moreButton⋯按钮+moreTapped block + onDeviceCardMoreTapped UIAlertController actionSheet "[中文分组] 能力名" + fetchCapMetadataForDevice GET /api/devices/:id/caps + invokeCap POST；TVNCViewerViewController 删硬编码 ops + fetchCapMetadata + rebuildGearMenuWithCaps UIMenu 内联分组 + 直连回退 performOp；(5) 10.5 时序修正：_tunnelStarted 标志 + _connectAndRun 删除立即调用 + _handleServerLine ack 后调用；(6) 10.6 帧率可配置：ThumbInterval schema(type=number min=1 max=60 reload=instant 默认5) + Root.plist + web refreshThumbInterval 缓存30s + IPA thumbInterval属性/refreshThumbIntervalFromGateway/applyThumbInterval 动态重建定时器；(7) 10.7 PerformanceMode 反向映射：currentConfigs 读底层4参数与预设表比对匹配上报 balanced/quality/performance 否则 custom；(8) 10.8 死代码清理：L1 isRepeaterClient 链全清（trollvncserver.mm/TRCapabilityRegistry.mm/TVNCClientListController.m + TSV 索引调整）+ L2 build.yml 3input+3env 全删 + L3 gRfbEventThread 4函数全删+清理调用 + L4 TROLLVNC_REPEATER_RETRY_INTERVAL 3处全删(含README) + L5 Root.plist SingleNotif/ClientNotifs 2specifier 全删。所有 Grep 验证零匹配 |
| 2026-08-09 | v2.0 | **Phase 11 能力扩展与场景化分层规划**。基于 hermes-android 项目评估（不 fork 不拉取，借鉴 6 个设计概念：macro/screen_hash/diff_screen/wait/event_stream/get_apps/current_app/open_app）+ 巨魔环境 UI 树读取不可行性论证（task_for_pid 受 MAC 限制 / Accessibility 私有 API 需 entitlement / 代码注入需 amfi 关闭，三条路径全堵死，视觉识别是唯一可行路径）+ 三场景反推（单控/批量/AI 调用需求差异）。新增 Phase 11 章节（11.1-11.10）：(1) 11.1 元数据 schema 扩展—新增 menu(primary/secondary/internal)/scenes([single,batch,ai])/batch(bool) 三字段，97 项现有能力按 category 映射表补打标签；(2) 11.2 macro.run—借鉴 android_macro，命令通道 v2 批量 invoke，stopOnFail 策略+每步结果数组；(3) 11.3 应用与启动组—新增 TRAppManager 模块，LSApplicationWorkspace+SBSCopyFrontmostApplicationDisplayIdentifier，4 项 app.list/current/open/icon；(4) 11.4 屏幕感知增强—Accelerate framework(vImage+vDSP) pHash 计算管线，32×32 DCT 取 8×8 低频（排除 DC [0][0]），新增 TRScreenHasher 模块+ScreenCapturer.captureSingleFrameBuffer 跳过 JPEG，4 项 screen.hash/diff/waitStable/subscribe，waitStable 比较相邻帧（非基线）因页面加载渐变，subscribe 复用 clients.subscribe 推送通道 minDistance=8 避噪声，单次 pHash ≈0.3ms vs JPEG 8ms 快 26 倍，60fps CPU<1% 内存 4KB；(5) 11.5 三端菜单分层—primary 82 项一级/secondary 24 项二级/internal 5 项 AI 专用隐藏；(6) 11.6 批量操作场景化过滤—batch 字段动态过滤批量栏；(7) 11.7 AI 调用通道—网关 REST API 暴露完整元数据含 scenes 字段；(8) 11.8 批次编排—Batch 6a(schema+macro.run)/6b(app.*+screen.hash/diff)/6c(app.icon+screen.waitStable/subscribe)+前端并行；(9) 11.9 最终数字—控制型 97→106/category 7→9/元数据 7→10/场景 2→3；(10) 11.10 关键约束沿用。配套更新能力追踪清单 v1.1：新增 1.18 屏幕感知类/1.19 应用与启动类/1.20 自动化编排类三个模块表格，1.21 汇总表新增 9 项待实现，0.3 批次表新增 Batch 6a/6b/6c+前端并行 4 行，变更记录新增 7 行 |
| 2026-08-09 | v2.1 | **Phase 12 IPA 控制端 UI 重构规划（待实现）**。用户与助手经多轮讨论确定 IPA 控制端全新设计方向：(1) 12.1 帧获取机制修正—卡片墙恢复完整 RFB 连接（纠正 Phase 9/10 改 invoke screenshot 的过度设计），ThumbInterval 改为可选 RFB 帧渲染频率控制；(2) 12.2 TabBarController 导航重构—新增双 Tab（控制端为主入口+设置降为次要），AppDelegate 改根控制器；(3) 12.3 顶部导航栏设计—位置稳定原则 `[全选☑] 标题 [批量操作] [宫格▾]`，模式切换只变内容不移动；(4) 12.4 chips 去除+布局切换器—删除"全部/直连/中继"chips，44px 窄按钮下拉菜单选"宫格/列表"，宫格默认 2 列+双指捏合调列数；(5) 12.5 双视图—宫格列表（完整 RFB 画面+⋯菜单+长按多选）+ 详情列表（40×40 缩略图+设备信息+任务状态预留）；(6) 12.6 批量配置流程—按钮触发多选+底部"调整配置(N台)"按钮+配置表单按 reload 分区+下发退出，三端分工（电脑端=调用能力+调整配置 / 手机端+IPA=仅调整配置）；(7) 12.7 tagLabel 删除；(8) 12.8 大屏⚙菜单补齐"设备配置"入口；(9) 12.9 任务状态字段预留（空闲/任务中/排队/失败，为 AI 任务拓展）；(10) 12.10 帧率阈值可选配置。核心设计原则：IPA 控制端从"设置页为主入口"改为"控制端为主入口"，符合"装一次初始化一次，之后零操作"愿景 |
| 2026-08-09 | v2.2 | **Phase 11.5 + Phase 12 实现状态核查与文档同步**。逐项核查实际代码实现状态，发现 Phase 11.5 与 Phase 12（除 12.1 外）均已落地实现但文档仍标记"待实现"，本次同步修正。(1) Phase 11.5 IPA 首页连接页改造 ✅ 已完成—TVNCConnectViewController.m 实现三卡片结构（TVNCGradientCard Hero 卡 3 行：设备名+状态点/连接状态文字/服务状态文字；扫描直连卡 QR+URL；客户端卡计数+全部断开+ScrollView 自然滚动），删除 UDID（移至设置页诊断区）和右上角状态胶囊，客户端右滑操作（在线→冻结+断开 / 冻结→解冻）；(2) Phase 12.1 帧获取机制修正 ❌ **放弃执行**—经核查 v1.8.3 愿景明确"所有的卡片屏幕墙都是**只获取帧**（可以调节获取的频率），不建立完整 RFB 连接"，原 12.1 计划"恢复 createRfb"与愿景直接冲突，当前实现（web startWallThumbnail 调用 invokeCap("screenshot") + IPA TVNCFetchThumbnailViaGateway 调用网关 invoke screenshot API + ThumbInterval 控制截图轮询间隔）完全符合愿景，无需改动；(3) Phase 12.2 TabBarController ✅—新建 TRMainTabBarController.h/.m（三 Tab：连接/控制/设置），SceneDelegate.m 改根控制器；(4) Phase 12.3 顶部导航栏 ✅—TVNCControllerViewController.m 实现 [全选☑] 标题 [批量操作] [宫格▾] 布局，位置稳定原则（模式切换只变内容不移动）；(5) Phase 12.4 chips 去除+布局切换器 ✅—删除 chips 标签，44px 窄按钮+下拉菜单选"宫格/列表"，宫格默认 2 列+双指捏合调列数（2/3/4/6）；(6) Phase 12.5 双视图 ✅—宫格 CollectionView（卡片底部栏设备名+⋯按钮）+ 列表 TableView（TVNCDeviceListCell 40×40 缩略图+设备信息+任务状态），长按进入多选；(7) Phase 12.6 批量配置流程 ✅—按钮触发多选+底部"调整配置（N 台）"按钮+配置表单+逐台下发，IPA 仅调整配置（无调用能力按钮）；(8) Phase 12.7 tagLabel 删除 ✅—TVNCDeviceCardCell 注释标注"删除 tagLabel"；(9) Phase 12.8 大屏⚙菜单补齐配置入口 ✅—TVNCViewerViewController.m rebuildGearMenuWithCaps 末尾"结束控制"前插入"设备配置"UIAction，push TVNCSettingsViewController；(10) Phase 12.9 任务状态字段预留 ✅—TVNCDeviceListCell taskStatusLabel 显示"空闲"（AI 任务预留）；(11) Phase 12.10 帧率阈值可选配置 ✅—ThumbInterval 作为截图轮询间隔（Phase 10.6 已实现 web refreshThumbInterval 从设备配置读取+IPA applyThumbInterval 动态重建定时器）。至此 Phase 0-12 全部完成，剩余仅真机联调 |
| 2026-08-09 | v2.3 | **卡片墙方向修正：v1.8.3 "只获取帧"设计弃用，恢复完整 RFB 连接**。用户明确指出 v1.8.3 愿景"卡片墙只获取帧（invoke screenshot API）"是**已弃用的旧设计**，Phase 12.1"卡片墙恢复完整 RFB 连接"才是新方向。v2.2 曾误读 v1.8.3 愿景将 12.1 标记为"放弃"，本次修正。(1) 愿景文档修正—顶部摘要"卡片屏幕墙"改为"建立完整 RFB 连接（viewOnly），ThumbInterval 作为可选 RFB 帧渲染频率控制"；0.D 真实愿景章节标注 v1.8.3"只获取帧"已弃用，改为"建立完整 RFB 连接（viewOnly 只读），实时渲染"；0.4.1 界面定位表"卡片墙"列改为"卡片完整 RFB 连接（viewOnly，可选帧率节流）"；0.4.4 卡片墙帧获取章节重写为"建立完整 RFB 连接（viewOnly）"；0.F 核查表"卡片墙帧获取方式"状态从✅改为🟡待修正；(2) Phase 12.1 恢复为待执行并执行完成—web app.js 新增 startWallRfb/stopWallRfb 函数（调用 createRfb viewOnly 持久连接 +disconnect 监听标记不可达），删除 startWallThumbnail/refreshThumbInterval/stopWallThumbnail 三个旧 screenshot 轮询函数；createWallTile inst 字段从 {thumbTimer,lastThumb,thumbErrors,thumbIntervalMs,lastConfigCheck} 改为 {rfb}；enterFocus stopWallThumbnail→stopWallRfb；restoreWallTile startWallThumbnail→startWallRfb；updateWallTile startWallThumbnail/stopWallThumbnail→startWallRfb/stopWallRfb；设备删除/移除两处 stopWallThumbnail→stopWallRfb；grep 验证旧函数零残留；(3) IPA 端确认已是 TVNCFetchThumbnailViaRFB（RFB 首帧获取，libvncclient），符合新方向，无需改动；(4) invoke screenshot 能力保留，仅用于大屏操控时的单帧截图（app.js invokeCap 第 541 行）；(5) ThumbInterval 帧渲染节流为可选增强（P2，默认不限制=实时渲染），当前未实现节流，后续按需补充。M20 里程碑标记完成，Phase 0-12 全部完成，剩余仅真机联调 |
| 2026-08-09 | v2.4 | **文档拆分重构**。将原 2168 行单文件按关注点拆分为 7 个子文档：(1) 01-愿景与设计基线.md（0.1-0.5 设计基线+总体架构，~150 行）；(2) 02-能力即服务架构.md（Phase 4+附录 C/D/E/F/G，~800 行）；(3) 03-隧道与连接架构.md（0.2/0.3+Phase 6/7+附录 B，~195 行）；(4) 04-UI交互规范.md（0.4+Phase 5/9/11.5/12，~375 行）；(5) 05-能力扩展规划.md（Phase 11+附录 A，~363 行）；(6) 06-构建交付与里程碑.md（3/4/5/6，~90 行）；(7) 99-历史归档.md（0.A-0.F+变更记录，~180 行）。主文档精简为索引+0.D 愿景+Phase 0-3.5+里程碑（~192 行）。解决 4 个结构性问题：单文件过大难以定位/Phase 顺序断裂（Phase 11→附录→里程碑→Phase 11.5/12 相隔 1070 行）/愿景分散三处易不同步（顶部摘要/0.D/0.1-0.5）/历史归档占用 20% 视野。0.D 愿景为唯一权威定义，子文档引用不复述。各子文档添加文档头（来源/关注点/索引链接），段落间用 `---` 分隔 |
