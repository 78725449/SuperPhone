# TrollVNC IPA 改造计划（v2.5 · 2026-08-11 容器化定版）

> **真实愿景**（用户原话定型）：
>
> **连接架构**：设备携带信息**主动注册到网关以后开启隧道**；同时 5901/5801 端口还可以在**不经过网关**的前提下对外提供访问；而**其他所有的连接都是走隧道**——携带信息注册到网关以后隧道就好了。
>
> **控制端设备列表来源**：三端（IPA/手机 web/PC web）**统一从网关 `/api/devices` 拉取同网关下所有已注册设备**，拉取后**排除自身 deviceId**；**去重由网关侧三层去重负责**（register 优先 / host:port 匹配升级 / TCP 探测同步）。
>
> **卡片屏幕墙**（v2.3 修正：v1.8.3 "只获取帧"设计已弃用）：所有控制台卡片墙**建立完整 RFB 连接**（viewOnly 只读），ThumbInterval 作为可选的 RFB 帧渲染频率控制（默认不限制，实时渲染）。
>
> **能力即服务**：所有的 IPA 能力就是服务，通过控制台（电脑/手机/IPA 控制端）可以**批量调整覆盖配置**和**调用控制能力**。
>
> **操控连接**：在控制台和 IPA 控制端页面，**点击卡片墙的屏幕卡片才进入操控**，是通过**隧道连接**的。
>
> **UI 交互规范**：
> - **IPA 控制端页面和手机 web 网页控制端**：
>   - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**
>   - 点击卡片后：**悬浮按钮**，能力以**纵向列表分组菜单**显示
> - **前端控制台（PC web）**：
>   - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**
>   - 点击卡片对设备进行大屏操作以后：在屏幕右侧是**纵向分组菜单的图标**
> - **5801 端口直连访问**：
>   - 电脑访问：能力以**纵向列表分组菜单**显示（和电脑端点击卡片以后的大屏类似，只是少了外层包装）
>   - 手机端访问：直接是**全屏 + 悬浮按钮**，点击悬浮按钮后是**纵向列表能力分组菜单**

---

## 0.D 真实愿景（v2.5 修订 · 权威定义）

> 基于用户明确的原话愿景，定型如下（不再使用旧版"iOS 设备群控，装一次初始化一次"概括）。
> **v2.3 修订**：v1.8.3 "卡片墙只获取帧（invoke screenshot API）"设计**已弃用**，改为"卡片墙建立完整 RFB 连接（viewOnly）"，ThumbInterval 作为可选的 RFB 帧渲染频率控制（默认不限制）。
> **v2.5 修订（2026-08-11）**：
> - **卡片墙帧获取再次定版**：v2.3 的"完整 RFB viewOnly"**已弃用**，当前唯一机制 = **H5 hash 门控变化拉图**（`invoke screen.hash` 比对，变化才 `invoke screenshot` 拉帧；静止零请求；双速退避 1s→15s）。
> - **IPA 控制端容器化**：控制端**整 Tab 替换为 WKWebView 容器**（`TVNCConsoleWebViewController`）加载网关 H5 手机控制台；原生设备墙（TVNCControllerViewController）/ 大屏（TVNCViewerViewController）**已删除**。IPA/手机 web/PC web 三端控制界面**统一为网页端**。
> - **控制端纯隧道**：三端控制台一律走网关隧道，**直连 host 设备模式废弃**（仅 `source=register` 隧道设备）。
>
> **v2.4 说明**：本文档为索引主文档，仅保留愿景权威定义 + Phase 0-3.5 基础阶段 + 里程碑总表。详细设计文档已拆分至 7 个子文档，按下方索引表查阅。

**连接架构**：
1. 设备**携带信息主动注册到网关以后开启隧道**——携带信息注册到网关以后隧道就好了
2. 同时 **5901/5801 端口还可以在不经过网关的前提下对外提供访问**（局域网直连保留）
3. 而**其他所有的连接都是走隧道**

**控制端设备列表来源**（v2.3 补充定版）：
4. IPA 控制端、手机 web、PC web 三端的设备列表**统一从网关 `/api/devices` 拉取同网关下所有已注册设备**
5. 拉取后**必须排除自身 deviceId**（IPA 控制端不应显示本机卡片，避免自控自）
6. **去重由网关侧负责**：三层去重（register 优先 / host:port 匹配升级 / TCP 探测同步），控制端拉取到的列表不会出现重复卡片
7. ~~直连设备（有 host 字段）与隧道设备（source=register）都纳入卡片墙显示~~（v2.5 废弃：三端控制台**仅保留 source=register 隧道设备**，直连模式已删）

**卡片屏幕墙**（v2.5 修订）：
8. ~~所有的卡片屏幕墙都是**只获取帧**（可以调节获取的频率），不建立完整 RFB 连接~~（v1.8.3 旧设计，已弃用）
9. ~~所有控制台卡片墙**建立完整 RFB 连接**（viewOnly 只读），实时渲染画面~~（v2.3 旧设计，已弃用）
10. **当前唯一机制（v2.5）**：控制台卡片墙 = **H5 hash 门控变化拉图**——每设备 `invoke screen.hash` 与 `lastHash` 比对，**变化才 `invoke screenshot` 拉帧渲染**；静止零图片流量；双速退避（变化 1s / 静止 base×1.5^n 至 15s 封顶）
11. IPA 控制端 = **WKWebView 容器加载网关 H5**（原生卡片墙/大屏已删除）；ThumbInterval 作为拉图轮询基频
12. 在控制台和 IPA 控制端页面，**点击卡片墙的屏幕卡片才进入操控**，是通过**隧道连接**的

**能力即服务**：
13. 所有的 IPA 能力就是服务，通过控制台（电脑/手机/IPA 控制端）可以**批量调整覆盖配置**和**调用控制能力**
14. 新增能力即插即用，避免开发一个小能力要全部折腾一遍

**UI 交互规范**（核心，按端区分；v2.5：IPA 控制端 = 容器内手机 web）：
15. **IPA 控制端页面和手机 web 网页控制端**（同一界面）：
    - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**（+ 设备管理操作）
    - 点击卡片后：**悬浮按钮**，能力以**纵向列表分组菜单**显示
16. **前端控制台（PC web）**：
    - 点击卡片前：卡片状态的右下角 `⋯` 里显示**纵向列表分组菜单**（+ 设备管理操作）
    - 点击卡片对设备进行大屏操作以后：在屏幕右侧是**纵向分组菜单的图标**（仅图标、无分组标题）
17. **5801 端口直连访问**：
    - 电脑访问：**右侧操作栏**（固定 11 项静态按钮贴画布右缘，caps.js 增强标题）
    - 手机端访问：直接是**全屏 + 悬浮按钮**，点击悬浮按钮后是 11 项操作面板

**其他**：
18. **彻底去营销**：所有外链/付费/源码/赞助入口 100% 清除
19. **配置合并收尾**：Notifications / PerformanceMode 枚举合并落地
20. **反向连接彻底清除**：全链路删除

---

## 板块索引

> v2.4 文档拆分重构：原 2168 行单文件按关注点拆分为 7 个子文档，本主文档仅保留愿景 + 基础阶段 + 里程碑。日常开发按需查阅子文档。

| 文档 | 关注点 | 关键内容 |
|------|--------|---------|
| [01-愿景与设计基线.md](01-愿景与设计基线.md) | 核心原则与总体架构 | 0.1 核心原则 / 0.2 连接与端口规划 / 0.3 连接方向与身份 / 0.4 UI 界面与交互规范 / 0.5 能力调用链路 / 1. 总体架构 |
| [02-能力即服务架构.md](02-能力即服务架构.md) | 能力注册与调用全链路 | Phase 4（4.1-4.8 能力注册/命令通道/配置上报/热重载/端口代理/REST API/前端渲染/AI WS）/ 附录 C 触控坐标归一化 / 附录 D 控制型能力清单（97 项*）/ 附录 E 配置型能力清单（38 项*）/ 附录 F API 端点 / 附录 G 新增能力开发规范 |

> *能力数量说明（2026-08-11）：文档口径 97 控制 + 38 配置；**代码直接计数 75 控制 + 45 配置**（含循环批量注册展开），最终总数以运行期 `query caps` 为准（见 00 §2.1）。
| [03-隧道与连接架构.md](03-隧道与连接架构.md) | 隧道协议与连接清理 | 0.2/0.3 连接端口与方向 / Phase 6 反向连接与营销清理 / Phase 7 18181 隧道落地 / 附录 B 设备↔网关字段规范 |
| [04-UI交互规范.md](04-UI交互规范.md) | 三端 UI 设计与实现 | 0.4 UI 界面与交互规范 / Phase 9 三端 UI 规范统一 / Phase 13 卡片墙增强+同步+直控 / Phase 12.11 容器化取代（Phase 5/12 已归档） |
| [05-能力扩展规划.md](05-能力扩展规划.md) | 能力扩展与场景化分层 | Phase 11（11.1-11.10 元数据扩展/自动化编排/应用管理/屏幕感知/菜单分层/批量过滤/AI 通道）/ 附录 A 底层互斥逻辑（screen.subscribe 已删） |
| [06-构建交付与里程碑.md](06-构建交付与里程碑.md) | 构建路径与风险管控 | 3. 构建与交付路径 / 4. 明确不做 / 5. 风险与已知边界 / 6. 里程碑总表 |
| [99-历史归档.md](99-历史归档.md) | 历史反推与变更日志 | 0.A-0.F 反推依据与核查记录 / 变更记录（v1.0→v2.4） |
| [架构演进改造计划.md](架构演进改造计划.md) | 架构演进（事件驱动+单一数据源） | 现状架构欠缺诊断 / Phase A-B 已落地（AppStore 状态机·注册判定结果驱动·GatewayClient 抽象）；Phase C 已废弃（screen_changed 增量链路删除，回归 H5 hash 门控） |

**配套文档**：
- [能力追踪清单.md](能力追踪清单.md) — 97+9 项能力逐项追踪
- [开发宪法.md](开发宪法.md) — 开发规范与约束

---

## 2. 分阶段计划

### Phase 0：协议与服务器侧前置 ✅ 已完成

**目标**：先定"注册/心跳协议"，网关侧独立开发与自测。

- 注册协议：`WS /ws/register?deviceId=&name=&vncPort=`
- 心跳：网关侧 ws ping/pong；手机侧每 30s 发 `{type:'hello'}`
- 在线判定：连接存活 + 心跳超时（90s 无消息即离线）
- 网关改动：mDNS 发布、`/ws/register` 端点、deviceId 索引
- **验收**：假客户端连 `/ws/register`，网关列表出现且在线；断开标离线

### Phase 1：IPA 最小闭环 ✅ 代码完成（真机联调待做）

**目标**：装一次 → 初始化一次 → 自动注册上线，网页点开即连。

| # | 改动 | 位置 | 说明 |
|---|---|---|---|
| 1.1 | 设备 UUID | `TRGatewayClient.mm _deviceId` | ✅ 首启生成持久化 DeviceUUID；同步设备名到 DesktopName |
| 1.2 | 初始化页 | `Root.plist` + `TVNCRootListController.m` | ✅ 网关分组（GatewayHost/Port/Token + 搜索网关按钮） |
| 1.3 | 注册+心跳客户端 | `TRGatewayClient.h/.mm` | ✅ BSD socket TCP JSON 行协议；register 上报全字段；hello 30s；断线退避重连（2s→30s） |
| 1.4 | 集成与编译 | `trollvncmanager.mm:296` + `Makefile:105` | ✅ watchdog 启动后 `[[TRGatewayClient sharedClient] start]`；restartHandler 注入 |
| 1.5 | 开机重连 | `TVNCServiceCoordinator` | 微调即可，真机验证 |

> `src/` 能力层目录全貌：
> `trollvncmanager.mm`（守护入口+watchdog）、`trollvncserver.mm`（RFB内核+热重载）、`TRGatewayClient.mm/.h`（注册/心跳/命令通道v2）、`TRCapabilityRegistry.mm/.h`（能力注册表）、`TRWatchDog.mm/.h`（进程守护）、`ScreenCapturer.mm`（屏幕采集）、`STHIDEventGenerator.mm`（IOHID注入）、`ClipboardManager.mm`（剪贴板）、`BulletinManager.mm`（通知）、`OhMyJetsam.mm`（内存守护）。

### Phase 2：UI 重新分类设计 ✅ 已完成

**目标**：按工作模式组织设置，删除开发者营销元素，统一设置入口。

- 页面结构：首页状态 / 连接模式 / 安全 / 画面与性能 / 输入 / 高级（折叠）/ 诊断
- 模式显隐：局域网显示 TCP/Bind/HTTP/Bonjour；反向模式隐藏并标注"当前模式不可用"
- 去营销：清除 View Source Code / Made with ♥ / 付费入口 / GitHubReleaseUpdater
- 集成归一：设置入口统一到 App 内；CCTrollVNC 默认移出构建
- **验收**：设置页按模式切换正确显隐；无外链/付费/源码入口

### Phase 3：Web 体验打磨 ✅ 已完成

**目标**：手机自带 web 直连"打开即出画面"；手机端全屏 + 可拖动悬浮按钮；PC 端侧边栏。

- 自动连接：`index.vnc` 复用 noVNC `core/rfb.js`，打开即连
- 双端交互：手机端 FAB 悬浮按钮（可拖动+点击展开操作列）；PC 端右侧固定操作栏
- FAB 细节：**沿用现有实现**（已完整：默认右下角避开安全区域 `env(safe-area-inset-*)`；pointerdown/move/up 统一事件 + `setPointerCapture`；移动 ≤6px 视为点击；双指捏合缩放；`getSafeBounds()` 边界约束）
- 操作集：按能力元数据自动渲染（Phase 4.7 已实现，不再硬编码 `data-op`）
- 技术约束：触控目标 ≥44px；`touch-action:none`；横屏 FAB 缩小为 48px
- **验收**：手机浏览器直连 5801，画面全屏、FAB 可拖动、操作生效；PC 显示侧边栏

### Phase 3.5：三端布局按钮归位 ✅ 已完成（旋转能力移除）

**目标**：三处界面的布局按钮各司其职，消除错位/缺失。

- 网关手机端：新增 `#layoutBtn` + `#layoutMenu`（12 项：横屏1-6/竖屏1-6列）
- IPA 控制端：**不做画面旋转**（用户确认无旋转需求，移除 `rotate` 菜单项和 `rotateScreen90Degrees` 实现）
- 设计稿与生产代码对齐
- **验收**：网关手机端布局按钮生效

---

> **Phase 4 起**的详细计划见各子文档：
> - Phase 4（能力即服务）→ [02-能力即服务架构.md](02-能力即服务架构.md)
> - Phase 5/9/11.5/12（UI 改造）→ [04-UI交互规范.md](04-UI交互规范.md)
> - Phase 6/7（隧道与清理）→ [03-隧道与连接架构.md](03-隧道与连接架构.md)
> - Phase 8（配置 schema）→ [02-能力即服务架构.md](02-能力即服务架构.md)
> - Phase 10（偏差修复）→ [99-历史归档.md](99-历史归档.md)
> - Phase 11（能力扩展）→ [05-能力扩展规划.md](05-能力扩展规划.md)

---

## 6. 里程碑

> 完整里程碑表含批次依赖说明，详见 [06-构建交付与里程碑.md](06-构建交付与里程碑.md)

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1-M5 | Phase 0-3.5（协议/UI/Web/布局） | ✅ 已完成 |
| M6-M13.5 | Phase 4（能力即服务 8 子项 + 6 批次） | ✅ 已完成 |
| M14 | Phase 5 IPA 控制端 noVNC 改造 | ✅ 已完成 |
| M15 | Phase 6 反向连接与营销清理 | ✅ 已完成 |
| M16 | Phase 7 18181 隧道落地 | ✅ 已完成 |
| M17 | Phase 8 配置 schema 补齐与枚举合并 | ✅ 已完成 |
| M18 | Phase 9 三端 UI 规范统一 | ✅ 已完成 |
| M19 | Phase 11.5 IPA 首页连接页改造 | ✅ 已完成 |
| M20 | Phase 12 IPA 控制端 UI 重构 | ✅ 已完成（v2.3） |
| **最终卡点** | 真机联调（全链路验证） | 需实体 iOS 设备 |

---

## 变更记录

> 完整变更记录（v1.0→v2.4 共 25 条）详见 [99-历史归档.md](99-历史归档.md)

| 日期 | 版本 | 摘要 |
|---|---|---|
| 2026-08-11 | v2.5 | **容器化定版**：IPA 控制端整 Tab 替换为 WKWebView 容器（`TVNCConsoleWebViewController` 加载网关 H5 手机控制台），原生设备墙/大屏（TVNCControllerViewController/TVNCViewerViewController 等）删除；卡片墙帧获取定版为 **H5 hash 门控变化拉图**（v2.3"完整 RFB"弃用）；三端控制台**纯隧道**（直连 host 废弃）；`screen_changed` 增量链路与 `screen.subscribe` 全链路删除（回归 hash 门控唯一拉图）；网关 `/command` 端点删除；iOS 死类清理。详见 [00 §8.6/8.7](00-当前进度基线.md) |
| 2026-08-09 | v2.3 | Phase 12.1 卡片墙方向修正（v1.8.3 "只获取帧"弃用→恢复完整 RFB viewOnly）；0.D 补充"控制端设备列表来源"定版（统一拉取/排除自身/网关三层去重） |
| 2026-08-09 | v2.2 | Phase 11.5 + Phase 12 实现状态核查与文档同步，Phase 0-12 全部完成 |
