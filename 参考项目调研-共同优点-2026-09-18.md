# 参考项目调研 · 共同优点（2026-09-18）

> 目的：把 `D:\编程项目` 下所有手机控制类项目汇集进仓库、同步到上游最新，并**横向对比它们共同的优点**，
> 以此判断 SuperPhone 的 AI 操作层该往哪走。
>
> **证据等级标注**（诚实起见）：
> - **【深读】** = 逐文件读源码后归纳，可信度高
> - **【浅读】** = 只看 README/目录结构与关键文件，未见完整实现
>
> 汇集位置：`SuperPhone/_research/`（已在 `.gitignore`，不入库）
> 同步结果：**12/12 已对齐上游最新**

---

## 一、汇集与同步结果

| 项目 | 来源 | 同步后 |
|---|---|---|
| **MobileAgent** | X-PLUG/MobileAgent（阿里通义） | `main@11cea57` 已是最新 |
| **AppAgentX** | Westlake-AGI-Lab/AppAgentX | `main@d0fcaeb` 已是最新 |
| **mobile-harness** | droidrun/mobile-harness | `main@e0e7310` rebase |
| **PhoneHarness** | PhoneHarness/PhoneHarness | `main@1cdc0d9` 已是最新 |
| **AgentProg** | MobileLLM/AgentProg | `main@ef5e299` 已是最新 |
| **TrollVNC-upstream** | OwnGoalStudio/TrollVNC | `main@a3e4081` 已是最新 |
| **dsh-ios-app** | kriskwok/dsh-ios-app | `main@54ab712` rebase |
| **imessage-data-foundry** | （自带 git） | `main@32fc4d3` 已是最新 |
| **mobile-use** | minitap-ai/mobile-use | `main@62913c9` **新接入 git + 对齐** |
| **mobilerun** | droidrun/mobilerun | `main@4f168cb` **新接入 git + 对齐** |
| **OmniParser** | microsoft/OmniParser | `master@3540212` **新接入 git + 对齐** |
| **surya** | datalab-to/surya | `master@a2363d3` **新接入 git + 对齐** |

**★ 同步过程中踩到的两个坑**（供以后参考）：
1. **github.com 直连被阻断** → 必须走代理（`HTTPS_PROXY=http://127.0.0.1:7890`）
2. **`curl 56 schannel: server closed abruptly`** → Windows 的 TLS 后端 schannel 经代理传大仓库会断流；
   改用 `git -c http.sslBackend=openssl fetch`（必要时加 `--depth 1`）即成功
3. **非 git 副本对齐**：git 拒绝用上游覆盖"未跟踪"的同名文件
   （`Untracked working tree file ... would be overwritten`）→ 先把现状 `add + commit` 成基线，
   再 `reset --hard origin/<branch>`

**未纳入**：`dsh-ios`（本地在用的 DSH 插件，无 remote，移动会断 junction 引用）；
`VirtualBrowser` / `douyin` / `manju` / `NewApi` / `Superwrt` 等（非手机控制类）。

---

## 二、共同优点（≥4 个项目都不约而同这么做）

> 判断标准：**多个独立团队在不同技术路线上殊途同归的做法**，说明这类做法是被反复验证过的。

### ① 动作目标【从来不是裸坐标】★ 最重要的一条

| 项目 | 做法 |
|---|---|
| **mobile-use** 【深读】 | **统一 `Target` 定位器**：一次动作可同时给 `resource_id` + `resource_id_index` + `bounds{x,y,w,h}` + `text` + `text_index`，由 `tools/mobile/tap.py` 按 **坐标 → resource_id → text 逐级 fallback**，失败时把每次尝试与原因打包回传（`additional_kwargs['attempts']`）|
| **Mobilerun** 【深读】 | **以 `index` 为主**（`click(index)`），坐标类动作（`click_at`）默认被 `DEFAULT_DISABLED_TOOLS` **屏蔽**，除非 provider 声明支持 |
| **AppAgentX** 【深读】 | **元素级视觉指纹**：回放时用 ResNet50 特征对元素做余弦匹配复定位，失配才降级到 LLM 语义匹配 |
| **MobileAgent** 【深读】 | 模型给 0-1000 归一化坐标，但另有一条 **a11y 文本匹配**通道：`find_and_click_element` 用 `text` + `content_description` 做 Levenshtein 距离匹配（阈值 1 个字），并 **10s 内轮询等待目标出现才点**，全程零模型 |

**👉 一致结论**：**坐标是会漂移的、脆弱的；元素语义（文字/ID/指纹）才是稳定的锚点。**

### ② 分层解耦，每层可替换

- **Mobilerun** 【深读】：`DeviceDriver → StateProvider → UIState → ToolRegistry` 四层，驱动层甚至在**另一个包**里（`mobilerun-core-local`）
- **mobile-use** 【深读】：`MobileDeviceController` 一个 Protocol，Android / iOS(IDB/WDA) / 云设备各有实现，前端统一 `UnifiedMobileController`
- **MobileAgent** 【深读】：env 抽象层（`AsyncEnv`）让**同一套动作协议**跑在手机(ADB)/PC(pyautogui)/浏览器(Playwright)三端

**👉 换平台只换一层，上层逻辑零改动。**

### ③ 多角色分工 —— 不让一个模型从规划管到执行

| 项目 | 角色 |
|---|---|
| **mobile-use** 【深读】 | LangGraph 七节点：`planner / orchestrator / contextor / cortex / executor / summarizer / convergence`，**每节点配不同模型**（Cortex 用最强的、其余用廉价模型）|
| **MobileAgent** 【深读】 | `Manager`(规划) / `Executor`(执行) / **`ActionReflector`(校验)** / `Notetaker`(记录) |
| **Mobilerun** 【深读】 | 两种模式：直执行 `FastAgent`（轻）/ `Manager + Executor`（重） |

**★ mobile-use 的提示词纪律尤其值得抄**（【深读】原文）：
- Cortex：「你是大脑，Executor 是你的手」
- Cortex：「**Never Repeat Failed Actions**」「**Complete Goals Only on OBSERVED Evidence**（绝不预先标记完成）」
- Cortex：「**不可预测的动作必须单步独占**」——`back` / `launch_app` / `stop_app` / `open_link` / 导航点击，一轮只能做这一个
- Planner：「每个子目标 = 一个清晰检查点；不要写循环，要写 3 个独立子目标」

### ④ 执行后必须校验 —— 而且校验要能看到"变化"

- **MobileAgent** 【深读】★ 最直接：`ActionReflector` 把 **动作前 + 动作后两张截图**（右下角分别打 `before`/`after` 角标，`m3a_utils.add_screenshot_label`）一起喂给模型，判 **A(成功/部分成功) / B(去了错误页面需回退) / C(画面无变化)** + 错误描述
- **AppAgentX** 【深读】：连续失败计数器（**只计连续失败，成功即清零**）
- **Mobilerun** 【深读】：每个动作返回 `ActionResult(success, summary)`，`summary` 就是模型看到的反馈文本

**👉 我们已有 `screen.hash`，能做【零模型】判定 C（画面没变）；只需把"去了错页/不确定"交给模型判 B —— 完全符合项目红线「校验必须由非模型原语承担」。**

### ⑤ 连续失败就升级，绝不无限重试

- **MobileAgent** 【深读】：**`err_to_manager_thresh=2`** —— 连续 2 次 B/C → `error_flag_plan=True` → Manager 的 prompt 追加 **"### Potentially Stuck!"** 段落，把最近 2 次「Action | Description | Outcome | Feedback」全摊开，要求**重新规划**
- **AppAgentX** 【深读】：`retry_count / max_retries=3` + 三层降级链（shortcut 回放 → element_sequence → `fallback_to_react`）
- **mobile-use** 【深读】：Orchestrator 判 `needs_replaning` → 回 Planner 带着已完成子目标 + 全部 `agents_thoughts` 重规划

### ⑥ 上下文硬裁剪（长任务的生存前提）

- **MobileAgent** 【深读】：`build_messages(history_n=4)` —— **只保留最近 4 轮带图**，更早的压成 `Step N: <Action 那一行>` 文本
- **mobile-use** 【深读】：`SummarizerNode` 在 `len(messages) > 25` 时从最早处 `RemoveMessage`，硬压在 25 条内（**该节点是纯代码裁剪，无模型**）
- 且两者都**用完即清感知字段**（mobile-use 的 Cortex 消费完就把 `latest_ui_hierarchy / latest_screenshot` 置 `None`）

### ⑦ 工具按【设备能力】裁剪

- **Mobilerun** 【深读】★ 独此一家做得最系统：每个工具注册时声明 `deps`（如 `click` 依赖 `{tap, element_index}`、`click_at` 依赖 `{tap, convert_point}`），运行时
  `capabilities = driver.supported | state_provider.supported` → `registry.disable_unsupported(capabilities)`
  **→ 设备做不到的动作，压根不出现在模型的动作列表里**

**👉 这条直接击中我们的问题：我们的 App 有 57 个能力，却没告诉模型它能用哪些。**

### ⑧ 等待与"稳定"显式化

- **MobileAgent** 【深读】：`_get_stable_state(stability_threshold=3, sleep_duration=0.5, timeout=6.0)` —— **连续 3 次 UI 快照一致才认为稳定**，超时**返回当前状态而不抛错**
- 且截图前调 `env.hide_automation_ui()` **隐藏自动化叠加层**，保证送进模型的画面干净

### ⑨ 技能 / 经验沉淀（长期记忆）

- **AppAgentX** 【深读】★ 唯一做到"图结构 + 视觉指纹"级别的：Neo4j 存 `Page / Element / Action / Shortcut` 节点与 `LEADS_TO / COMPOSED_OF / REFERS_TO` 关系，离线用 LLM 把整条执行链抽象成 `is_high_level=true` 的 **Action（shortcut）**，在线优先回放 shortcut
- **Mobilerun** 【深读】：轨迹录制 + **带状态门禁的宏回放**（兼作可复用技能）
- **mobile-use** 【深读】：`scratchpad` 显式 KV 记忆（`save_note / read_note / list_notes`），**专门用于跨 App 搬运数据**（在 A 应用存配料 → 切到 B 应用取回并填）
- **注意**：MobileAgent 与 mobile-use **都没有跨任务技能沉淀**（每次都从零规划）—— 这是我们能超越的点

### ⑩ App 解析单独一层

- **MobileAgent** 【深读】：`packages.py` 静态**中文别名表**（`com.tencent.mm → 微信/wechat`、`tv.danmaku.bili → bilibili/b站/哔哩哔哩` 等 55+ 条）+ 小模型 `resolve_app_name_via_llm`（输入"指令 + 已安装应用名列表"，输出 `{reason, app}`，**硬规则：只从列表里选、禁止用相似 App 顶替**）
- **mobile-use** 【深读】：`hopper` 轻量模型专做"从几百上千条里挖一条"（`launch_app("微信")` 不查表，而是拿全量包列表交给它解析）

### ⑪ 统一动作协议跨平台

- **MobileAgent** 【深读】：手机 `mobile_use` / PC `computer_use` / 浏览器 `browser_use` —— **同一套"截图 + 指令 + 历史 → 一个动作"协议**，只换设备适配器与坐标制（`coordinate_resize.py` 支持 0-1000 / 0-999 / 相对 / 0-100 / molmo 五种坐标制互转）

### ⑫ 坐标换算要"记账"

- **MobileAgent** 【深读】：`smart_resize(factor, min_pixels, max_pixels)` + `update_image_size_()` **记录实际送给模型的 resized 尺寸**，换算严格以该尺寸为准
  - **👉 这正是我们踩过的坑**：本项目有过"pinch scale 语义断裂"的前车之鉴

### ⑬ 多智能体的"护栏"设计

- **mobile-use** 【深读】★：**App Lock 闸门** —— 任务可带 `locked_app_package`，此后每轮感知时 Contextor 都核对前台包名，不一致就按三条判据裁决"重新拉起"还是"允许偏离"，**默认 RELELAUNCH（拿不准就拉回）**
- **mobile-use** 【深读】：**每个工具必须带 `agent_thought` 字段**（强制解释"为什么"），并写回 `agents_thoughts`

---

## 三、对照 SuperPhone：我们缺什么 / 做错了什么

> 我们的现状（截至 2026-09-18）：
> iOS 真机（iPhone 6s / 15.8.8）经 TrollStore 装 TrollVNC；设备端注册表 **57 个能力**；
> AI 侧是"移植 Mobile-Agent-v3.5 的单模型循环"，动作空间只有 **ADB 风格的 11 个动作**，
> 映射后**实际只用到 5 个能力**，且 `type` 走剪贴板粘贴、`Back` 用边缘右滑凑、**循环内无任何步骤校验**。

### 差距清单（按严重程度）

| # | 差距 | 现状 | 参考项目怎么做 | 我们的底子 |
|---|---|---|---|---|
| **1** | **动作目标只有裸坐标** | 模型在 750×1334 上猜像素 | **4/4 项目都不这么干**（Target 三级 fallback / index / 视觉指纹 / 文本匹配）| ✅ **我们有 `vision.find_text`** —— 能"说文字就定位"，**却没接进动作空间** |
| **2** | **57 个能力只暴露 5 个** | `keyboard`(HID)、`type.delete`、双击 Home 切后台、`find_text`、`script.exec`、`pinch`、`doubleTap`、剪贴板、音量/亮度/锁屏 **全没用上** | Mobilerun 的**能力裁剪**（deps + supported，按设备能力生成动作列表）| ✅ 能力都在，**只是没告诉模型** |
| **3** | **循环内无步骤校验** | 模型只能靠看下一张截图自己发现错了 | MobileAgent 的 `ActionReflector`（before/after 两张图判 A/B/C）| ✅ **我们有 `screen.hash`** —— 能零模型判"画面变没变" |
| **4** | **连续失败不升级** | 反复点同一位置直到步数上限 | `err_to_manager_thresh=2` → 重规划；AppAgentX 的三层降级 | ✅ 可做 |
| **5** | **无技能沉淀** | 每次任务都从零开始 | AppAgentX 的 shortcut 图（Neo4j + 视觉指纹）| ✅ **我们有 `script.exec`** —— 天生适合承载"技能卡回放" |
| **6** | **无稳定等待** | 固定 `sleep(2)` | 连续 3 次快照一致 + 超时返回当前态 | ✅ 我们有 `screen.waitStable` |
| **7** | **假返回** | `Back` 用边缘右滑凑 | iOS 确实没有硬返回键（mobile-use 也是左边缘滑）| ⚠️ **这条其实是合理妥协**，但应让模型知道"返回=边缘滑" |
| **8** | **输入走剪贴板而非键盘 HID** | `type` → `type.paste`（Cmd+V），且**是追加不是替换** | mobile-use iOS 用 WDA `send_keys`、Android 用 ADB Keyboard | ✅ **我们有 `keyboard`(HID) + `type.delete`** |

### ★ 一句话总结

> **我们的问题不是"模型太差"，而是"没给它该有的手"** ——
> **57 个能力里只告诉它 5 个，而且动作目标退化成了最脆弱的裸坐标，还没有任何一步校验。**
> **四个参考项目在完全不同的技术路线上，恰恰都避开了这三点。**

---

## 四、改造优先级（建议）

| 优先级 | 做什么 | 依据 | 成本 |
|---|---|---|---|
| **P0** | **把动作空间换成"我们 App 的真实能力"**：`type` 走 `keyboard`(HID)、补 `delete`(`type.delete`)、补 `key`(回车/退格)、补 `app_switch`(双击 Home)、补 `find_and_click{text}`(`vision.find_text` + tap)、补 `swipe` 之外的 `pinch`/`doubleTap` | 差距 1、2、8；Mobilerun 能力裁剪 + mobile-use Target fallback | 小（改提示词 + 映射层）|
| **P0** | **每步加零模型校验**：动作前后比 `screen.hash`，未变即判失败并写进历史，逼模型换策略 | 差距 3、4；MobileAgent ActionReflector（C 判据用非模型原语）| 小 |
| **P1** | **稳定等待**：动作后用 `screen.waitStable` 而非固定 sleep | 差距 6；MobileAgent `_get_stable_state` | 小 |
| **P1** | **动作目标三级 fallback**：模型给 `text` 优先 → `find_text` 定位 → 失败才用坐标 | 差距 1；mobile-use 的 Target | 小 |
| **P2** | **技能沉淀**：探索期跑通后，把 steps 存成 `script.exec` 可回放的技能卡；回放期零模型 | 差距 5；AppAgentX 的 shortcut | 中 |
| **P2** | **App 名解析层**：`app.list` + 中文别名表 + 小模型解析 | 共同优点 ⑩ | 小 |

---

## 五、待补调研（本次因输出截断未拿到深读摘要）

以下 8 个项目的摘要被输出截断丢失，**只做了浅读**，其结论未纳入上面的归纳：

- `mobile-harness`（droidrun）—— **README 显示它是"给手机 agent 提供 harness（工具/无障碍层）"的库，与我们的设备层定位最接近，值得单独深读**
- `PhoneHarness` —— "mixed-action orchestration harness across CLI/GUI/MCP tools"，**与我们设想的"MCP 服务器"方向直接相关，值得单独深读**
- `AgentProg` —— "Program-Guided Context Management for Long-Horizon GUI Agents"，**与我们缺的"技能/程序化上下文"直接相关，值得单独深读**
- `OmniParser`（微软屏幕解析：YOLO 检可交互区域 + Florence-2 生成图标描述）
- `surya`（OCR / 版面分析）
- `TrollVNC-upstream`（我们的上游）
- `dsh-ios-app`、`imessage-data-foundry`

**建议下一轮单独深读前三个** —— 它们的定位和用户设想的"通用 MCP 服务器"最贴合。
