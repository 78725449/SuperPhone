# AI 操作层 · 真机实测台账（2026-09-18）

> 用途：把"文档承诺 vs 真机实测"的差异固定下来，作为出最终方案的依据。
> 每条都附**实测证据**；未实测的一律标注【未验】。

## 〇、本轮的教训（先记，防再犯）

- **★ 我把"字段名读错"误判成了"设备坏了"** ✗：`vision.ocr` 的返回字段是 **`texts`**（含 `count`），
  我后续脚本只读 `rows` → 读到 0 行 → 误判"Vision 引擎永久失明"，并**据此重启了 manager 和 server 两次**（有副作用）。
  最早那次脚本写了 `rows or texts` 的 fallback，所以是对的；后面漏了 fallback。
  **纪律：读设备返回先 `json.dumps` 打全字段再取值；"设备坏了"的结论必须先用第二种手段交叉验证。**
- **★ 同一族教训今天第三次出现**：`screen.hash` 的"未知操作"我判断为"没实现"，但源码 `tvExtHandleMessage` 里**有分派** ——
  **"源码有"≠"运行版本有"≠"能走通"**，三者必须分开验证。

## 一、已确证【可用】的能力

| 能力 | 实测 | 证据 |
|---|---|---|
| **`screenshot`（Native 路由）** | ✅ **原尺寸 750×1334** | 81~374ms · JPEG 196KB · 亮度/方差正常 |
| **`vision.ocr`** | ✅ **18 行，带 `x/y/w/h/cx/cy/confidence`** | 2070~2952ms · 字段在 **`texts`**（不是 rows）|
| **`vision.find_text`** | ✅ 命中并给坐标，与全量 OCR **完全一致** | 2937ms |
| **`vision.find_image`** | ✅ **能命中**（80×80 原尺寸模板 score **0.872**） | 108ms |
| **`touch.tap`** | ✅ 67ms | |
| **`screen.snapshot`** | ✅ 24ms（但只有 board 档，见下） | w=320 h=569 |
| **`vision.ocr` 差分 → expect** | ✅ **可自动产出候选** | 点击前后 18→8 行，新增含 `首页` |

## 二、已确证【不可用 / 与文档不符】的能力

| 项 | 文档承诺 | 实测 | 影响 |
|---|---|---|---|
| **`screen.hash`** | 主干 §11.4 标"✅ 现成" | ❌ **`未知操作: screen.hash`** | **`script.exec` 的 `expect:{hashDiff}` 阻塞** ✗ |
| **`screen.diff`** | 同上 | ❌ **`未知操作: screen.diff`** | 同上 |
| **`screen.snapshot` 取原图** | — | ❌ **`quality` 参数完全无效**（0.1/0.5/1/2 全返回 320×569 同字节数） | 锚点图不能从它裁 |
| **`find_image` 坐标** | 未明确 | ❌ **偏差 64~600px**（三次全错） | **锚点图回放阻塞** ✗ |
| **`find_image` 性能** | 主干 §11.4 承诺 **<20ms** | ❌ **78~3721ms**（模板越大越慢，240×160 需 792ms） | 回放期每步调用不经济 ✗ |
| **`ocr` 性能** | 主干 §11.4 承诺 **200~500ms** | ❌ **2070~2952ms**（慢 4~6 倍） | 页面级 OCR 成本上升 |
| **大模板稳定性** | — | ❌ **504 Gateway Timeout**（模板 160×160 时出现） | 稳定性风险 |

## 三、`find_image` 坐标不可靠的**代码级根因**（已定位，可修）

`TRVisionEngine.mm` L423/L458-465 + `TRVisionEngine.h` L34：

```
粗匹配：1/8 金字塔（750/8 → 仅 94×167）→ NCCC 找峰
精匹配：原尺寸，但窗口 = 粗定位中心 ± TRVisionRefineMarginPx(=48)
        cx0 = (bu + tw8/2) * 8 - tw/2        ← 粗定位 ×8 换算
```

- **粗定位在 1/8 尺度上差 1px → 原尺度差 8px**；margin=48 **只容忍 1/8 尺度上差 6px** ✗
- 实测偏差 dx=238px ÷ 8 ≈ **30px**，正是粗匹配在 94×167 缩略图上选错峰 ✓
- **主干设计针对 1170×2532（1/8=146px），而实测设备是 750×1334（1/8=94px）** —— 设计假设与实际设备不匹配 ✗

**修法（按性价比）**：
1. **金字塔 1/8 → 1/4**（750/4=187，特征翻倍）— 改常量，成本极小
2. **margin 48 → 自适应**（随金字塔倍数放大，如 margin = 48 × (8/pyramid)）
3. 模板建议 ≤120px（性能与窗口面积平方相关）

## 四、契约对照（主干承诺 → 落地阻塞点）

| 主干契约 | 状态 |
|---|---|
| §11.4 `vision.ocr/find_text/find_image` 三件套 | ⚠️ **全部"能用"但性能与 §11.4 不符**，且 find_image 坐标不可靠 |
| §11.5 锚点图生命周期（裁剪→tap→expect→存 anchor→回放 find_image） | ⚠️ **前半段（裁剪+差分）可行；回放段被 find_image 坐标阻塞** ✗ |
| §11.3 `script.exec`（steps[] 下发手机连续执行） | ❌ 设备端未实现；且 `expect:{hashDiff}` 依赖的 `screen.hash/diff` 不通 ✗ |
| §11.2 指令模式（探索）/ 脚本模式（回放）混合 | ⚠️ 指令模式已验证可用；脚本模式阻塞同上 |

## 五、附加事实

- **设备真实屏幕 = 750×1334**（iPhone 6s），主干 §11.4 写的"1170×2532"是别的型号 ✗
- **`find_text` 是【整行匹配】而非子串匹配**：`find_text("推荐")` 返回 0（因为整行是"三直播团购南京 关注 商城 推荐"），
  而 `find_text("01:32")` 命中 ✓ —— 第三轮执行者报的"两个检测器不一致"由此解释
- **`screenshot` 走 `captureSingleFrameImage`（UIImage）**，`vision.*` 走 `captureSingleFrameBuffer`（CVPixelBuffer）——
  **两个不同 API**，排查时勿混
- **Vision 引擎跑在 `trollvncserver` 进程**（主干 §11.4 的设计），restart manager 不影响它
- 设备无 `awk`（脚本里勿用）

## 六、★★★ 两个阻塞点的根因（已锁定到代码级，**修复已实施并提交**）

> **修复提交**：`dad0383` → 远程 `8ff22d57`（CI run `35254546922`）
> **备份**：`data/backups/vision-scale-and-5802-fix-20260918-014126/`

### 阻塞点 1：`screen.hash` / `screen.diff` 不通 → **漏了 5802 分派** ✗

**实测**：两者都返回 `未知操作`。
**根因链（已逐层排除）**：
1. manager 注册表**有** `screen.hash`（grep 二进制 =1）✓ → 不是注册表问题
2. server 的 **0x50 分派**（`tvExtHandleMessage` L3798/L3800）**有**它们 ✓
3. **但 `_rfbCommand` 走的是【5802 HTTP 分派】`tvHttpApiDispatch`（L4196）** ✓✓✓
4. **而那里的 op 列表【没有 screen.\*】** ✗ —— 有 `vision.ocr/find_text/find_image`（L4218-4224）✓
   → **完美解释"vision.find_image 通、screen.hash 不通"** ✓

**修复（已提交）**：`tvHttpApiDispatch` 补 3 个分支，与 vision.* 同模式（纯函数 + `cl` 传 NULL）：
`screen.hash` / `screen.diff` / `screen.waitStable`

## 六之二、★ 本轮新增的两条能力语义（已实测，写进 skill）

### `find_text` = **子串匹配**（空格无关），但返回【整行中心】坐标

```
目标整行: '三直播 团购 南京 关注 商城 推荐'
  整行原样        → found=True  ✓
  去掉空格        → found=True  ✓（空格无关）
  中间一个词"关注" → found=True  ✓
  尾词"推荐"      → found=True count=2（两处命中）
  ★ 但所有查询都返回 (0.4437,0.0627) —— 全是【整行的中心】
```

**→ 能力分工（正好印证主干 §11.5「锚点类型分工」）**：

| 场景 | 用什么 | 说明 |
|---|---|---|
| **点整行**（列表项 / 按钮 / tab）| **`find_text`** ✓ | 子串匹配即可，返回行中心正合适 |
| **点行内某个词 / 纯图标** | **`find_image`（锚点图）** ✓ | find_text 给不了行内坐标 |

**⚠️ 更正上一轮的错误结论**：我曾写"`find_text` 是整行匹配"，**错** ✗ ——
它其实是子串匹配。第三轮执行者报的 `find_text("推荐") → 0 line(s)`
**不是匹配语义所致**，真实原因待查（可能当时页面已在别处）。

**→ 这正是 AGENTS.md 铁律「新增能力三处补齐：注册表 + 0x50 分派 + 5802 分派」被违反的实例** ✗
**修法（注释里已给模式：handler 是纯函数，`cl` 传 NULL 复用）**：
```objc
} else if ([op isEqualToString:@"screen.hash"])        return tvExtHandleScreenHash(NULL, params);
} else if ([op isEqualToString:@"screen.diff"])        return tvExtHandleScreenDiff(NULL, params);
} else if ([op isEqualToString:@"screen.waitStable"])  return tvExtHandleScreenWaitStable(NULL, params);
```
**成本：3 行** ✓

### 阻塞点 2：`find_image` 坐标不可靠 → **尺度参数与设备不匹配** ✗

**根因**（`TRVisionEngine.mm` L423/L458-465 + `.h` L34）：粗匹配在 **1/8 金字塔**，
750/8 后仅 **94×167**；精匹配窗口 = 粗定位中心 ± `TRVisionRefineMarginPx(=48)`，
而 `cx0 = (bu + tw8/2) * 8 - tw/2` —— **粗定位 ×8 换算，误差被放大 8 倍，容错只有 6px** ✗
实测偏差 238px ÷ 8 ≈ 30px，正是缩略图上选错峰 ✓
**主干设计针对 1170×2532（1/8=146px），实际设备 750×1334（1/8=94px）** —— 假设不匹配 ✗

**修法**：① 金字塔 1/8 → 1/4（750/4=187）② margin 随金字塔自适应 ③ 模板建议 ≤120px

## 六之三、★★★ 修复后的真机复验结果（2026-09-18，已部署验证）

### ✅ 修复 A 完全成功：`screen.hash` / `screen.diff` 已可用

```
screen.hash  → {"hash": "fefdff8e9e38f4cb"}                       ✓ 109ms
screen.diff  → {"distance": 43, "changed": true, "threshold": 5}  ✓ 103ms
★ 这正是主干 §11.3 script.exec 的 expect:{hashDiff} 所需要的两个原语 ✓✓✓
screen.waitStable → ok=False（参数名可能不对，待查）✗
```

### ⚠️ 修复 B 部分成功：性能大幅改善，但**坐标仍不准**（症状被精确定位）

**性能改善（真实收益）**：240×160 模板 768~814ms → **248~273ms（快 3 倍）** ✓
**附带**：改 1/4 后支持更小模板（32px 模板在 1/8 后只剩 4×4 会被拒，1/4 后 8×8 可用）✓

**坐标症状（精确化，供下一轮定位）**：
```
★ y 方向【完全精确】：顶部条 dy=0px、底部条 dy=0px ✓✓✓
★ x 方向系统性偏：+449 / +462 / -327 / +53 / +135 px ✗
★ 且 score 偏低：文字块模板 score 仅 0.46~0.51（模板逐像素来自当前帧，本该≈1.0）✗
```

**已排除的原因**：
- ✗ 不是"动态页面"（在**静态页面**上复测，3 次截图完全相同，仍偏）
- ✗ 不是帧几何不一致（**y 精确 0px 就是铁证** ✓）
- ✗ 不是金字塔倍数（已改 1/8→1/4，y 依然精确、x 依然偏）
- ✗ 不是 region 未实现（`region` 确实生效：偏差由 375→108 改善）

**→ 剩下的嫌疑**：① **x 方向的粗匹配峰值选择**（屏幕横向内容重复度高）；
② **`trvNCCScore` 或 `trvCorrelate2D` 在 x 方向的计算**；
③ **`screenshot`（UIImage）与 `find_image`（CVPixelBuffer）的灰度值差异**导致 NCCC 分数偏低
（y 对而 score 低，指向"几何一致但像素值有系统性差异"）。

### ★ 能力分工的实测结论（主干 §11.5 得到印证）

| 场景 | 用什么 | 实测可靠性 |
|---|---|---|
| **定位整行文字**（列表项/按钮/tab）| **`find_text` + `touch.tap`** | ✅ **可靠，现在就能用** |
| **定位行内某词 / 纯图标** | `find_image`（锚点图）| ⚠️ **当前不可靠**，待修 x 方向 |

**→ 主线不受阻**：主干 §11.5 本就写了"**双保险（先 OCR 后图匹配）**"，
**文字锚可用 = 主线可走通** ✓；图像锚是"锦上添花 + 纯图标场景"，可后修。

## 七、★ 「expect 零模型自动产出」的三步算法（已实测）

```
① 【区域过滤】剔除 cy < 0.035 的状态栏（时钟/电量/信号）
   ★ 必须做 —— 光靠多次采样抓不到（时钟 1 分钟才变，实测 1.2s 间隔抓不到）✗
② 【采样 ×3 取交集】滤掉动画/抖动 —— 实测有效：17 行 → 12 行 ✓
③ 【跨页面差分】新增稳定元素 = expect 候选 ✓
   （实测候选含干净的 '首页' ✓，也含噪音 '设置忄•' ✗ → 需第 ④ 步）
④ 从候选中挑有语义的（人或模型确认）
```

## 八、★ 由实测推出的「最优路径」（主干承诺 → 达成路径）

| 主干契约 | 现状（复验后） | 达成路径 |
|---|---|---|
| §11.5 **锚点图闭环** | 前半段（`screenshot` 原尺寸裁剪 + 差分产出 expect）**已验证可行** ✓；回放段（find_image）**y 准 x 偏** ⚠️ | **改用文字锚走主线**（`find_text`+`tap` 可靠 ✓）；图像锚待修 x 方向 |
| §11.3 **`script.exec`** | 设备端未实现；但 **`expect:{hashDiff}` 的依赖已修好** ✓✓ | **新增 `script.exec` 能力**（设备端，中等改动）—— **前置已全部就绪** ✓ |
| §11.4 **vision 三件套** | ✅ 全部可用 | 仅性能偏离：ocr 2070~2952ms（承诺 200~500）· find_image 248~273ms（承诺 <20）|
| **`screen.hash/diff`** | ✅ **已修复并真机验证** | —— |
| **锚点图来源** | ✅ **`screenshot`（Native，750×1334 原尺寸）** | **已具备，零改动** ✓ |
| **expect 自动产出** | ✅ **三步算法已实测**（区域过滤 + 采样交集 + 跨页差分）| 待实现进执行循环 |

## 九、下一步待验（下一轮）

1. **`screen.waitStable` 的参数契约**（复验时 ok=False，参数名可能不对）
2. **find_image 的 x 方向**：查 `trvNCCScore`/`trvCorrelate2D`，或改"粗匹配取 Top-N 峰逐一精匹配"
3. **`screenshot` 与 `find_image` 的灰度一致性**（y 对而 score 低，指向像素值系统性差异）
4. **ocr 耗时优化**（首次初始化 vs 稳态；`.accurate` vs `.fast`）
5. **汇总产出最终方案**
