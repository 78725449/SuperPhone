# 决定性实验：GUI-Owl 能否识别 enum 之外的新动作（2026-09-19）

> **用户质疑**："它的动作空间是【写死的 enum】，模型只认那 11 个动作名 —— 你这个说法是哪来的？
> 也就是说我们再增加也不行？模型不能识别其他的动作嘛？"
>
> **这个质疑推翻了我说法的「机制」，但实测证明结论「方向」是对的。** 本文记录完整过程。

---

## 一、我先纠正自己的机制错误（用户是对的）

### 1.1 我说的是

> "动作空间是【写死的 enum】，模型只认那 11 个动作名"

### 1.2 源码核对结果 —— **机制说错了** ✗

**证据 ①：API 调用【没有传 tools】**

```python
# mobile_use/utils.py:541
chat_completion_from_url = self.bot.chat.completions.create(
    model=self.model, messages=payload, **{}          # ← ★★ 空字典
)
```
**→ `tools` / `tool_choice` / `response_format` 全都没传 → 【纯文本对话】，无 API 层强约束** ✓

**证据 ②：动作解析【不校验动作名】**

```python
# run_gui_owl_1_5_for_mobile.py:70-80
def parse_action(output_text):
    tool_call_block = output_text.split("<tool_call>\n")[1]
    json_str = tool_call_block.split("}}\n")[0] + "}}"
    return json.loads(json_str)          # 只解析 JSON，不校验 action 是否在枚举内
```
**→ 全项目搜 `VALID_ACTIONS` / `allowed_action` / `assert action` → 零命中** ✓

**证据 ③：唯一的限制是分发链**

```python
# run_gui_owl_1_5_for_mobile.py main()
else:
    print(f"[WARN] Unsupported action type: {action_type}")   # ← 未知动作落这里被丢弃
```
**→ 而分发链【是能加 `elif` 的】** ✓

**★ 所以准确表述应该是**：
> **enum 只是提示词里的【文字描述】（"参考手册"），不是 API 层的"牢笼"。**
> **模型完全可以输出其它动作名；唯一限制是分发链有没有分支 —— 而那是可以加的。**

**→ 我据此推论"所以只能走 MCP"，这个推论也是错的** ✗

---

## 二、于是我做实验：加 3 个 enum 之外的动作，看模型会不会用

**实验设置**（可回滚，备份在 `data/backups/ma-custom-action-20260919-011653/`）：

```
① SYSTEM_PROMPT 的 enum 加 "brightness_up" / "brightness_down" / "spotlight" + 描述
② main() 加对应 elif 分支
③ IosTools 加 brightness_up() / brightness_down() / spotlight() 方法
   （→ 设备端 briup / bridn / spotlight，均为已注册 HID 能力）
```

### 2.1 实验一：`"把屏幕亮度调低一点"`

```
STEP 0  click [927,66]   ← 想点右上角设置图标
STEP 1  click [932,61]
STEP 2  click [98,59]
STEP 3  click [117,952]  ← 想点底部"设置"
→ ★ 全程【没有使用 brightness_down】✗
```

**⚠️ 但这一轮有干扰**：**"调暗亮度"用 `click` 也能做到** → 模型当然选熟路 ✗
**→ 无法区分"不能识别"与"不倾向于用"** ✗

### 2.2 实验二（决定性）：`"打开系统搜索"`

**为什么选 `spotlight`**：它的 UI 等价物是"从屏幕顶部向下精准拉出搜索栏" —— **手势很难做对** ✓
**→ 如果模型【认得】这个动作，它应该用；如果【不认】，它只能瞎点** ✓

```
STEP 0  click [496,141]   ← 想点搜索栏
STEP 1  click [906,75]
STEP 2  click [493,141]   ← 又点一次（没成）
STEP 3  type "系统搜索"    ← 在搜索框里输入
→ ★ 全程【没有使用 spotlight】✗
```

**★ 决定性**：**即使 `click` 连点 3 次都没成，它也不用提示词里明确写着的 `spotlight`** ✗

---

## 三、★★★★★ 结论

| 层面 | 结论 |
|---|---|
| **技术层面** | **动作能加、能分发** ✓ —— 我加的 3 个 `elif` 分支都正常工作（没报错，只是模型没调用）|
| **模型层面** | **GUI-Owl-4B 【无法识别】enum 之外的新动作** ✗✗✗ —— **提示词里写了也没用** |
| **原因** | **它是【专用 GUI 模型】，动作空间是【训练时固化】的** ✗ —— **不是提示词能扩展的** |

**★ 我原话的对错（诚实交代）**：
- ✗ **机制说错了**（不是 API 强约束 —— `create(model, messages, **{})` 确实没传 tools）
- ✓ **但结论【方向是对的】** —— **模型确实不认新动作，现在有实测证据**

---

## 四、★★★ 「给模型更多动作」只剩两条路

| 路 | 说明 | 代价 |
|---|---|---|
| **① 换【通用 VLM】** | GPT-4o / Qwen-VL-Max / Claude 泛化强，**可能认提示词里的新动作** | 要联网 / 有配额 ✗ |
| **② 用 MCP（或 function calling）** | **工具由 API 层的 `tools` 参数【真正约束】**；**模型在 function calling 模式下【必须从给定工具里选】** | 要开发 MCP ✗ |

**★ 为什么 ② 有效而"改提示词"无效**：
- **改提示词** = 在**文本里"建议"**模型用某动作 → **专用 GUI 模型不理会** ✗（实测）
- **function calling / MCP** = **在 API 层【约束】模型只能在给定工具里选** → **模型必须理会** ✓

**→ 所以「不想开发 MCP」与「给模型更多动作」是【冲突的】。**
**这不是猜测，是上面两次实测的结论。**

---

## 五、那现在"够不够用"？—— 取决于目标

| 目标 | 现状 |
|---|---|
| **让模型用【它原有的动作】把活干完** | **够了** ✓ —— 它 enum 声明的 15 个原语 `main()` 全接上了（含我们补齐的 `key`/`Menu`/`Enter`）|
| **让模型用上我们 64 个能力里的更多** | **不够，且不是"加几个动作"能解决的** ✗ —— 受限于它的模型 |

**★ 一句话**：
> **在"Mobile-Agent + GUI-Owl-4B"这个组合里，模型的手【就是那 15 个原语】，扩不了。**
> **要扩手，必须换"约束机制"（function calling/MCP）或换"更聪明的模型"。**

---

## 六、本次实验的处置

| 项 | 状态 |
|---|---|
| 实验加的 3 个动作（`brightness_up`/`brightness_down`/`spotlight`）| **已从 enum / main() / IosTools 全部还原** ✓ |
| 正式补齐的 3 个动作（`key`/`Menu`/`Enter`）| **仍在，未受影响** ✓ |
| 备份 | `data/backups/ma-custom-action-20260919-011653/`（三个文件）|
| 还原后语法检查 | 三个文件均通过 ✓ |
