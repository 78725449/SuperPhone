# 03 - 隧道与连接架构

> **来源**：IPA改造计划.md 行 190-215（0.2/0.3）+ 行 644-738（Phase 6/7）+ 行 1199-1262（附录 B）
> **关注点**：连接与端口规划、连接方向与身份、反向连接与营销清理、18181 隧道落地、设备↔网关字段规范
> **索引**：[返回主文档](IPA改造计划.md)
> **2026-08-11 同步**：三端控制台一律走隧道（WS 直连回退已废弃，无隧道 4003 拒绝）；Viewer/Controller 原生大屏已删除（IPA 控制 Tab = WKWebView 容器加载网关 H5）；新增 ack 驱动握手与同设备单会话约束。

---

### 0.2 连接与端口规划

| 端口 | 角色 | 说明 | v1.8 实现状态 |
|---|---|---|---|
| 5801 | 前端入口 | 浏览器/WebSocket 客户端访问（局域网直连） | ✅ 已实现 |
| 5901 | 后端入口 | AI 工具/脚本/标准 VNC viewers（局域网直连） | ✅ 已实现 |
| 18081 | 注册端口 | 设备注册到网关的 TCP 端口 | ✅ 已实现（regServer.listen） |
| 18181 | 隧道端口 | 跨网络访问经网关隧道（替代直连 5801/5901） | ✅ 已实现（Phase 7） |
| 46752 | 本地控制端口 | trollvncserver 本地客户端管理（经网关代理暴露） | ✅ 已实现 |

> **废弃**：Repeater 模式和 5500 端口不再使用（**v1.8 修正**：v1.3 起声称"代码已删"实际全量残留，Phase 6 才真正删除）。

### 0.3 连接方向与身份

| 项 | 结论 |
|---|---|
| 连接方向 | 手机**携带信息主动注册**到网关（内网 IP 即身份） |
| 隧道建立 | 注册成功后**自动开启 18181 隧道**，无需额外操作 |
| 局域网直连 | 5901/5801 端口在局域网内可**不经网关**直接对外提供访问（VNC/HTTP 服务本身） |
| 控制端连接 | **三端控制台一律走网关隧道**（仅 source=register 隧道设备）；直连 host 设备模式已废弃（WS 无直连回退，无隧道 4003 拒绝） |
| 跨网络访问 | **其他所有连接都走隧道**（经网关 18181 隧道转发） |
| 在线状态 | 网关维护：注册连接存活 + 心跳（不用 TCP 探测 5901） |
| 设备身份 | 首启生成 **UUID**，拼进 DesktopName，注册时上报，网关按 deviceId 建索引 |
| 初始化 | 打开一次 App → "搜索网关（mDNS）/ 手动输入 IP" → 保存，之后全自动 |
| 鉴权 | 内网可信，不设设备鉴权；网关侧保留 FARM_TOKEN 一行兜底 |
| 后台常驻 | 沿用 LaunchAtLogin + SBS 预启动；真机实测存活时长 |

---

## Phase 6：反向连接与营销彻底清理（v1.8 新增）✅ 已完成

**目标**：删除 v1.3 起多次声称"已删"但实际全量残留的反向连接代码；清除所有营销入口。
**前置**：无依赖，可与 Phase 7/8/9 并行启动。
**反推依据**：见 0.B 表 A/E 两类。

### 6.1 反向连接代码清理

| # | 文件 | 删除内容 | 行号 |
|---|---|---|---|
| 6.1.1 | `TrollVNC/src/trollvncserver.mm` | 4 个全局变量 `gRepeaterMode`/`gRepeaterHost`/`gRepeaterPort`/`gRepeaterId` | 128-131 |
| 6.1.2 | 同上 | `isRepeaterEnabled()` 内联函数 | 185-187 |
| 6.1.3 | 同上 | `ReverseMode`/`ReverseSocket`/`ReverseRepeaterID` 配置解析块 | 777-851 |
| 6.1.4 | 同上 | CLI 参数 `-reverse` / `-repeater` 解析与赋值 | 1001-1087 |
| 6.1.5 | 同上 | 日志/通知条件中的 `isRepeaterEnabled()` 引用 | 3694 / 4043 / 4072 |
| 6.1.6 | 同上 | 反向连接事件线程与 `rfbReverseConnection`/`rfbUltraVNCRepeaterMode2Connection` 调用 | 4906-4949 |
| 6.1.7 | `TrollVNC/src/TRCapabilityRegistry.mm` | `_registerConfig:@"ReverseMode"` schema 注册行 | 964 |
| 6.1.8 | `TrollVNC/src/TRCapabilityRegistry.mm` | `currentConfigs` 默认值字典中的 `ReverseMode: @"none"` | 1136 |
| 6.1.9 | `TrollVNC/app/TrollVNC/TrollVNC/TVNCConnectViewController.m` | ReverseMode/ReverseSocket/ReverseRepeaterID 读写与重置 | 324-326 / 373 / 388 / 394 / 479-481 / 695 |
| 6.1.10 | `TrollVNC/app/TrollVNC/TrollVNC/TVNCRootListController.m` | ReverseMode 本地化处理 | 373 / 391 |
| 6.1.11 | `TrollVNC/prefs/TrollVNCPrefs/Resources/Root.plist` | 已确认无反向字段（Root.plist 已清理） | — |
| 6.1.12 | 本地化字符串 | 全文搜索 `Repeater`/`reverse` 残留并清除 | — |

**验收**：grep `gRepeater`/`isRepeaterEnabled`/`ReverseMode`/`ReverseSocket`/`ReverseRepeaterID` 在整个项目源码（排除第三方库）零匹配。

### 6.2 营销入口清理

| # | 文件 | 删除内容 | 行号 |
|---|---|---|---|
| 6.2.1 | `TrollVNC/app/TrollVNC/TrollVNC/TVNCRootListController.m` | `- (void)support` 方法（指向 havoc.app 付费市场） | 808-813 |
| 6.2.2 | 同上 | `- (void)source` 方法（指向 GitHub OwnGoalStudio） | 815-818 |
| 6.2.3 | 同上 | 上述方法的所有调用点（specifier target 选择器） | grep `@selector(support)`/`@selector(source)` |
| 6.2.4 | 本地化 `Root.strings` / `Localizable.strings` | "View Source Code" / "Made with ♥ by OwnGoal Studio" / "请支持我们的其他付费作品" 等字符串 | 全文搜索 |

**验收**：grep `havoc.app`/`OwnGoalStudio`/`View Source`/`Made with ♥`/`付费` 在整个项目源码（排除 README 历史版权声明）零匹配。

---

## Phase 7：18181 隧道落地（v1.8 新增）✅ 已完成

**目标**：设备主动注册到网关成功后，自动建立 18181 隧道；跨网络访问一律走隧道，局域网保留 5801/5901 直连。
**前置**：Phase 6 完成（避免反向连接残留干扰）。
**反推依据**：见 0.B 表 B。

### 7.1 设备侧：TRTunnelClient

**新增文件**：`TrollVNC/src/TRTunnelClient.h` / `TRTunnelClient.mm`

| # | 改动 | 说明 |
|---|---|---|
| 7.1.1 | 新建 `TRTunnelClient` 类 | 单例，仿 `TRGatewayClient` 连接管理风格 |
| 7.1.2 | `start` 方法 | 注册成功后由 `TRGatewayClient` 触发；建立到网关 18181 的 TCP 长连接 |
| 7.1.3 | 鉴权握手 | 设备发起连接后首包发送 `{type:"tunnel_hello", deviceId, token}`，网关校验 |
| 7.1.4 | 双向数据桥 | 隧道建立后，网关侧把对应 deviceId 的 RFB/控制流量转发到此隧道；设备侧把隧道数据桥接到本地 5901 |
| 7.1.5 | 心跳与重连 | 30s 心跳；断线退避重连（2s→30s），与 TRGatewayClient 一致 |
| 7.1.6 | Makefile 注册 | 加入 `SRC_FILES` 与 build target |

**协议**：JSON 行首包握手 + 之后裸字节透传。

### 7.2 网关侧：TRTunnelServer

**修改文件**：`trollvnc-farm/server/index.js`

| # | 改动 | 说明 |
|---|---|---|
| 7.2.1 | 新增 `TUNNEL_PORT=18181` 常量 | 与 `REG_PORT=18081` 区分 |
| 7.2.2 | 新增 `tunnelServer` TCP 监听 | 18181 端口，独立 `net.createServer`，与 `regServer` 分离 |
| 7.2.3 | 隧道握手处理 | 接收 `tunnel_hello`，校验 `deviceId` 与 `token`，注册到 `tunnels[deviceId]` 索引 |
| 7.2.4 | 网关↔隧道流量桥接 | `/ws/vnc/:id` 与 `/api/devices/:id/invoke` 等流量**唯一走** `tunnels[deviceId]`；无隧道直接拒绝（4003），**无直连回退**（2026-08-11 修正） |
| 7.2.5 | 隧道离线判定 | 隧道断开即标记设备"跨网络不可达"，但保留局域网直连可用 |
| 7.2.6 | 在线判定更新 | 设备在线 = 注册连接存活 **或** 隧道连接存活（任一存活即在线） |

### 7.3 三端接入改造

| # | 文件 | 改动 | 说明 |
|---|---|---|---|
| 7.3.1 | ~~`TVNCViewerViewController.m`~~（**已删除** 2026-08-11） | 大屏操控统一由网关 H5 承担 | IPA 控制 Tab = `TVNCConsoleWebViewController`（WKWebView 容器加载网关 H5 手机控制台），原生 Viewer/Controller 已整体删除 |
| 7.3.2 | `trollvnc-farm/web/app.js` | `createRfb` 与 `enterFocus` | 所有 RFB 连接走网关 `/ws/vnc/:id` 隧道桥接，**无直连分支**（`buildWebSocketURL` 直连分支已删） |

> **2026-08-11 补充（隧道会话级约束）**：
> - **ack 驱动握手**：网关 WS 会话建立触发 `rfb.start(id)` → 设备同步 connect 本地 5901 并在 ack 回 `ok=connect` 结果 → 网关收到匹配 id 的 ack 才精确放行缓冲的握手字节（3s 超时兜底，旧设备兼容）；connect 失败显式 close ctrl 会话（4005 "画面服务不可用"）。
> - **同设备单活跃会话**：设备仅 1 条隧道 + 1 个 5901 连接；网关 `handleVncSocket` 新会话顶掉同设备其他非 ctrl 会话（4001），按"首会话"触发 5901 重建——多会话共享 5901 会协议串流（noVNC `Unexpected server message (type N)`）。
> - **末会话退出**：延迟 800ms 下发 `rfb.stop`（防快速进出乒乓抖动），清理 pending 缓冲。

### 7.4 命令通道复用隧道

| # | 改动 | 说明 |
|---|---|---|
| 7.4.1 | `sendDeviceCmd` 流量优先走隧道 | `tunnels[deviceId]` 存活时优先走隧道，否则走注册通道 |
| 7.4.2 | 设备侧 `TRGatewayClient._handleServerLine` | 隧道流量桥接到 5901，命令通道流量分发到 `TRCapabilityRegistry` |

**验收**：
- 设备主动注册到网关后，18181 隧道自动建立并保活
- 三端控制台一律经网关访问设备画面与能力（跨网络/局域网同路径，**无直连回退**）
- 局域网内 5801/5901 仍可直接访问（VNC/HTTP 服务层面，独立于控制台）
- 隧道断开后控制台访问失败（4003 no tunnel）

---

## 附录 B：设备↔网关字段规范

### B.1 设备上报字段（register / hello）

| 字段 | 类型 | 说明 | 变更频率 |
|---|---|---|---|
| `deviceId` | string | UUID（首启生成持久化） | 永不变 |
| `name` | string | 设备名（与 DesktopName 一致） | 用户改名时 |
| `vncPort` | int | VNC TCP 端口（5901） | 配置变更 |
| `httpPort` | int | HTTP 端口（5801，0=关闭） | 配置变更 |
| `screen` | {width, height, fbw, fbh} | 原生像素 + RFB 帧缓冲尺寸 | Scale 变更时 |
| `capabilities` | string[] | 控制型能力 ID 列表 | 模块加载时 |
| `capMetadata` | object[] | 能力完整元数据（id/title/icon/category/route/params） | 模块加载时 |
| `configSchema` | object[] | 配置项 schema（key/title/type/min/max/enum/reload） | 模块加载时 |
| `configs` | object | 配置当前值（密码只报存在性） | 配置变更时 |

> **注**：`version` 字段暂不上报（用户确认移除）。

**screen 字段说明**：
- `width`/`height`：设备原生像素（如 1170×2532），用于归一化坐标转原生像素
- `fbw`/`fbh`：RFB 实际传输的帧缓冲尺寸（Scale 缩放后，如 Scale=0.5 时 585×1266），AI 工具截图判定的坐标需除以 fbw/fbh 转为归一化坐标
- Scale 热重载后设备主动重新 register 推送新的 fbw/fbh

**密码安全**：`FullPassword` / `ViewOnlyPassword` 不上报明文，改为 `hasPassword` / `hasViewOnlyPassword` 布尔值。

### B.2 控制端拉取字段（`GET /api/devices`）

```json
{
  "devices": [{
    "id": "uuid", "name": "iPhone 14", "online": true,
    "lastSeen": "2026-08-08T...", "host": "192.168.1.100",
    "port": 5901, "httpPort": 5801,
    "screen": {"width":1170,"height":2532,"fbw":585,"fbh":1266},
    "capabilities": [...], "capMetadata": [...],
    "configSchema": [...], "configs": {...}
  }]
}
```

**控制端过滤规则**：IPA 控制端面板拉取同网关设备时，排除自身 deviceId。
**去重规则**：网关侧已实现三层去重（register 优先/host:port 匹配升级/TCP 探测同步），控制端拉取时不会出现重复卡片。

### B.3 命令通道协议（网关 → 设备）

| cmd | 参数 | ACK |
|---|---|---|
| `ping` | 无 | `{ok:true}` |
| `query` | `target`: caps/configs/schema/status | 对应数据 |
| `set` | `key`, `value` | `{ok, reload}` 或 `{ok:false, error}` |
| `invoke` | `cap`, `params` | `{ok, result}` 或 `{ok:false, error}` |
| `restart` | 无 | `{ok}` 或 `{ok:false, error}` |

### B.4 query target 返回格式

| target | 返回字段 |
|---|---|
| `caps` | `capabilities[]` + `capMetadata[]` |
| `configs` | `configs{}` 当前值 |
| `schema` | `configSchema[]` 配置 schema |
| `status` | `deviceId` + `name` + `vncPort` + `httpPort` + `screen` |

---
