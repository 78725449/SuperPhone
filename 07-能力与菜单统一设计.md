# 07-能力与菜单统一设计（v1.0）

> 定位：能力清单、菜单模型、端口收敛、AI 接入的统一设计基线。与 02-能力即服务架构、04-UI交互规范 共同构成能力体系设计依据。
> 状态：已确认（2026-08-11 会话决策汇总）。实施前需按此设计展开实现计划。
> **落地进度（2026-08-13）**：§3 能力分层/§4 四菜单/§7 设置项（能力与菜单统一 Phase 1）✅ 已实现；**§5 端口收敛 / §6 AI 能力模型（Phase 2）✅ 已实现（2026-08-12 落地，详见 [10-端口收敛与AI能力模型-实现计划.md](10-端口收敛与AI能力模型-实现计划.md)）**；**§4.2 参数快捷段（QUICK_CONFIG_GROUPS 2 组 6 项）+ 常用能力段（QUICK_ACTIONS/service.restart）+ §7.1 端口全固定 ✅ 已实现（2026-08-13 重构落地）**；**§4.2/上报重构 ✅ 已实现（2026-08-13：register 仅上报 configs 当前值，capabilities/capMetadata/configSchema 不再上报，前端自包含定义驱动，详见 [00 §2.3e](00-当前进度基线.md)）**。

---

## 1. 背景与目标

现状问题：
- 成对拆分的「按下/抬起」底层原语（31 项）泄漏进人工菜单，用户看到「Home按下/Home抬起」这类无意义按钮
- 四处菜单（PC 侧边栏 / 手机悬浮 / 卡片「...」/ 5801 双面板）各自为政：网关侧渲染全部能力无过滤，5801 双份硬编码 HTML
- 46752 控制端口与 5901 RFB 数据面并行，AI 接入路径不清晰
- 设置项传输层已完备（configSchema/configs API），但动作项（searchGateway/generateKeys）未暴露

目标（用户已确认的决策）：
1. 菜单放「按键对象 + 按压模式」，不放能力原语；AI 翻译用途的能力全部 internal
2. 触控从菜单移除，改画布直操
3. 端口收敛：46752 删除，能力并入 5901 RFB 扩展消息
4. 设置项 API 暴露 + 配置入口入卡片菜单
5. 硬编码按键按钮，点击语义固定传 5901

---

## 2. 现状与考古结论

### 2.1 能力体系现状
- TRCapabilityRegistry：99 项控制能力 + 45 项配置 schema，含 menu/category/scenes/batch 分层字段
- 网关 API 完备：invoke / configs / query + 批量版本
- Root.plist 28 项设置项已几乎全部注册 schema

### 2.2 考古结论（git 论证）
- **46752 是上游设计**（`Control.h` 的 `kTvDefaultCtlPort=46752`，上游 `5bf1930 add control socket`），非我方发明。我们扩展了 screen.*
- 被删除的上游 5901 能力仅 Repeater（5500）/Reverse（有意废弃，宪法确认）
- FrameRateSpec 解析是重构保留（parseFrameRateSpec），非移除
- 5901 的 RFB 基础能力（keysym 映射 / clipboard / HTTP）全部保留
- 5801 上游模式即「静态按钮 + caps.js 可选增强」，与我们本次方向一致

---

## 3. 统一按钮模型

### 3.1 按键对象（keyDefs）
每个物理按键 = 一个对象，按压模式 → 翻译成能力 id：

```js
{
  key: 'home',    title: 'Home',  icon: '🏠',
  events: { click: 'home',            // 单击 → menuPress
            double: 'home.double',    // 双击 → menuDoublePress
            long: 'home.long' }       // 长按 → menuLongPress
},
{
  key: 'volup',   title: '音量+', icon: '🔊',
  events: { click: 'volup',           // 单击 → volumeIncrementPress
            down: 'volup.down',       // 按下 → volumeIncrementDown
            up: 'volup.up' }          // 抬起 → volumeIncrementUp
}
```

### 3.2 PressRecognizer（按压识别器，前端一处实现）
PC/手机/卡片菜单共用一个组件，挂到按键按钮上，像 UIButton 事件记录按压：
- `click`（短按抬起）→ invoke events.click
- `double`（<300ms 两次点击）→ invoke events.double
- `triple`（<300ms 三次点击，电源专用）→ invoke events.triple
- `long`（按住 >800ms）→ invoke events.long
- `down`/`up`（有该字段的按键）→ 按下触发 down，松开触发 up

**多击与多次执行**：
- 仅 `click` 的按键（音量/亮度等）：无延迟，每次点击立即执行，连点 N 次执行 N 次
- 有 `double`/`triple` 的按键（Home/电源）：启用识别窗口（抬起后 300ms 等待下一次），窗口内 N 次点击归为一次双击/三击，超时执行单击；单击因此延迟 ~300ms（与 iOS 系统 Home 键语义一致）
- `down`/`up` 无等待，按下/抬起即时触发
- 识别模式由 keyDefs 的 events 字段声明式驱动：有 double 字段才启用双击等待，无则零延迟

### 3.3 能力分层
| 层级 | 含义 | 进哪些菜单 |
|---|---|---|
| primary | 人工遥控动作（按键 12 对象 + 动作区 5 项） | 控制台侧边栏/悬浮、卡片菜单 |
| secondary | 管理/查询/配置（~28 项） | 卡片菜单、批量 |
| internal | AI 底层原语（~40 项） | 不进任何菜单，invoke 可用 |

> **2026-08-13 上报重构**：分层/菜单元数据不再依赖 register 上报（capabilities/capMetadata/configSchema 已不再上报），前端自包含定义驱动（KEY_DEFS/ACT_DEFS/BATCH_CAPS/CONFIG_DEFS）；register 仅上报 configs 当前值（配置面板必需）。

---

## 4. 四菜单清单（最终形态）

### 4.1 控制台侧边栏（PC focusOps）= 手机悬浮菜单（FAB→opsMenu）= 同一份
**同一份渲染**：两端动态区共用 `renderCapOps`（按键区 12 键 + 动作区 5 项完全一致），差异仅在附加静态按钮（见下文「两端边界」）。

**按键区（12 个按键对象 + 按压识别，全部 RFB 直发）**
`Home`(单击/双击/长按) `电源`(单击/双击/三击/长按) `音量+`(单击/按下/抬起) `音量−` `静音` `亮度+` `亮度−` `键盘` `搜索` `Home+Power截屏` `键盘锁/解锁` `释放按键`
- 物理键 7 项（home/power/音量/静音/亮度）走 keysym/中键直发；系统动作键 5 项（键盘/搜索/截屏/锁/释放）用自定义 keysym 映射设备端 API（toggleOnScreenKeyboard 等）
- 直发通道 = 画布同一 noVNC RFB 连接（sendKey/sendPointer，无 HTTP、无能力回退）；无连接时按键不可用
- 按压式按键（音量/亮度/静音）一次短按 = down+up 一格，不补 click

**动作区（单击直执行，5 项）**
`文本输入` `粘贴输入` `获取剪贴板` `设置剪贴板` `屏幕快照`（原"截屏"改名，静默抓帧返回 base64；与按键区 Home+Power 系统截屏区分）

**触控：不进菜单**，画布直操（5801 RFB 指针已有；H5 直控 noVNC 已有；H5 聚焦模式补 pointer→invoke touch）

**两端边界（静态按钮差异）**
| | PC 侧边栏（focusOps） | 手机悬浮窗（opsMenu） |
|---|---|---|
| 按键区 + 动作区 | ✅ 同一份渲染 | ✅ 同一份渲染 |
| 静态区 | ← 返回 · 🔗 同步 · ⛶适配 · 🖥️全屏 · ⏹断开 | ⛶适配 · 🖥️全屏 · ⏹断开 · ← 退出控制 |

- **返回/同步仅 PC 端**：手机端无返回/同步按钮；"返回设备墙"由「← 退出控制」承担（等价语义）
- **同步（群控）只作 PC 端**：多设备同步操作是 PC 群控台场景，手机端单设备控制不需要，不提供

### 4.2 卡片「...」菜单 = 参数快捷段 + 常用能力段 + 更多参数入口（2026-08-13 重构）

> 核心原则（用户确认）：**点击 ⋯ 后直接在一级菜单显示常用参数设置，菜单每一项数据源 = 前端自包含定义（KEY_DEFS/ACT_DEFS/QUICK_ACTIONS/BATCH_CAPS/CONFIG_DEFS），点击即 invoke/setConfigs 直达设备——不搞网关或前端外部实现**。

**参数快捷段（QUICK_CONFIG_GROUPS，本设计核心——"IPA 给前端开放可由前端配置/控制的参数"，一级菜单直达，悬停/点击展开该项编辑，保存即 setConfigs 同步）**
- **画面与性能**：`Scale` `FrameRateSpec` `PerformanceMode`
- **输入与交互**：`ClipboardEnabled` `ViewOnly` `Notifications`
- 交互：PC 鼠标悬停该项 → 展开该项控件+当前值；触屏点击标题锁定展开；保存后同步到设备并收起（caps.js `QUICK_CONFIG_GROUPS` + app.js `formatConfigVal`/`.qp-item`）
- **端口项不在此列**：Port/HttpPort/GatewayPort 固定不可调（见 §7.1），前端 configSchema 无这三项，菜单天然不出现

**常用能力段（QUICK_ACTIONS，IPA 自身暴露的能力，点击即 invoke 直达设备）**
- `service.restart`（重启服务）——invoke → TRGatewayClient.restartHandler → gWatchDog 重启（stop 当前 task → start 新进程），**非前端外部实现**
- 清单可扩展：新增能力只需在 caps.js `QUICK_ACTIONS` 追加能力 id，设备端能力已注册即自动生效（即插即用）

**更多参数入口**
- `showConfigPanel(d)`：完整配置面板（按 reload 策略分区），入口保留在菜单底部

**已删除（非 IPA 能力/参数的网关侧或前端外部操作）**
- 设备管理段：查看/控制、详情、编辑、测试在线、删除（查看/控制由卡片本体点击进入控制承担）
- 常用管理 14 项（service.*/clients.*/sys.*/gateway.*/screen.* 等低频/内部能力）：不占一级菜单，invoke 仍可通过完整配置面板或其他入口调用

### 4.3 批量菜单
= 按键类能力（home/power/volup/voldn/mute/briup/bridn/keyboard/spotlight/snapshot/hwlock/hwunlock/releasekeys 等批量有意义的按键动作）+ 管理类（29），去掉触控/文本/剪贴板/截屏/采集类

### 4.4 5801（PC #bar + 手机 #panel）
静态按键 + 可选动态增强（上游模式），按钮定义抽为一份 keyDefs 常量渲染，消除双份 HTML
- **无返回/同步按钮**：5801 为单机直连页（无"设备墙"概念），返回/同步仅存在于 PC focusOps（群控台场景）；手机 H5 悬浮菜单亦不适配返回/同步（返回由「← 退出控制」承担，同步不做）

### 4.5 全部 internal（AI 专用，不删，invoke 可用）
- 物理按键原语 14 项（home.down/up 等）
- key.down/up、consumer.press/down/up、hid.press/down/up
- touch.tap/swipe/longPress（画布直操语义，菜单不显示，AI/画布经 invoke 使用）
- touch.down/up、downMulti/upMulti、downMultiAt/upMultiAt、event、eventStream、taps
- stylus.tap/down/move/up、touch.reset
- screen.capture.start/stop、screen.hash/diff/waitStable（hash 供 H5 卡片墙）
- 触控手势类 doubleTap/twoFingerTap/threeFingerTap/curveSwipe/pinch

### 4.6 新旧对照（沿用/移除明细）

**网关控制台（侧边栏/悬浮）**——现状渲染全部 ~99 项能力，新菜单收敛到 17 个可见按钮：

| 类别 | 现状按钮 | 新设计 |
|---|---|---|
| 物理按键 | home/power/volup/voldn/mute/briup/bridn/keyboard | 保留 → 按键对象 |
| 组合键 | home.double/home.long/power.double/power.triple/power.long/spotlight/snapshot/screenshot.system | 保留 → 并入按键对象按压模式（不再独立按钮） |
| 安全 | hwlock/hwunlock/releasekeys | 保留（单击） |
| 成对原语 | home.down/up 等 14 项 | 移除 → internal |
| 触控类 | tap/doubleTap/twoFingerTap/threeFingerTap/longPress/swipe/curveSwipe/pinch/taps/down/up/downMulti/upMulti/downMultiAt/upMultiAt/reset/event/eventStream | 移除 → 画布直操 + internal |
| 触控笔 | stylus.tap/down/move/up | 移除 → internal |
| 文本/剪贴板/截屏 | type.text/type.paste/clipboard.get/clipboard.set/screenshot | 保留（动作区 5 项） |
| 管理/查询 | service.*/clients.*/sys.*/gateway.*/notify.*/screen.fps/resolution/forceRefresh/capture.* | 移除 → 卡片菜单 |
| 参数化原语 | consumer.press/down/up、hid.press/down/up、key.down/up | 移除 → internal |
| 配置入口 | 「⚙ 设备配置」底部按钮 | 移除 → 卡片 ⋯ 菜单底部「更多参数」（showConfigPanel） |

**5801（#bar/#panel）**：
- 保留：静态 11 项全部（home/power/volup/voldn/mute/briup/bridn/kb/clip/fit/full/disc，含本地操作）
- 统一：按钮定义抽为 keyDefs 常量，#bar 与 #panel 共用渲染（消除双份 HTML）
- 升级：tapKey 加按压识别（长按/双击 keysym 序列）
- 可选增强：caps.js 可加载时覆盖标题/扩能力（失败不影响连接）
- 移除：无

**旧逻辑沿用/移除**：
| 旧逻辑 | 处理 |
|---|---|
| renderCapOps（按 category 分组渲染） | 沿用结构，改 keyDefs+按压识别 + 自包含定义（2026-08-13 menuCaps 已删） |
| showTileMenu（卡片） | 沿用，自包含定义（QUICK_CONFIG_GROUPS/QUICK_ACTIONS/CONFIG_DEFS），设备管理段已移除（2026-08-13 重构） |
| showBatchMenu | 沿用，加过滤（internal/触控/截图） |
| 5801 dispatchOp/tapKey | 沿用，tapKey 加 hold/双击 |
| 设备端能力注册（hidNoParam 数组等） | 沿用（能力不删，标 internal） |
| _executeLocalCmd（46752 socket） | 替换 → _rfbCommand（5901 扩展） |
| 46752 端口 + gTvCtl* 基建 | 移除（并入 5901 扩展） |
| clients.subscribe 推送 | 移除（无消费方） |

---

## 5. 端口收敛：5901 能力扩展协议 ✅ Phase 2 已实现（2026-08-12）

> **落地标记**：trollvncserver 注册 `rfbProtocolExtension`（handleMessage 接管 type 0x50，响应 type 0x80，JSON 负载）；TRCapabilityRegistry `_executeLocalCmd` → `_rfbCommand`（持久 RFB 5901 连接 + cap.hello 管理标记）。46752 端口与 gTvCtl* 基建已删除。详见 [10-端口收敛与AI能力模型-实现计划.md](10-端口收敛与AI能力模型-实现计划.md)。

### 5.1 原则
5901 = 看（画面）+ 操作（输入）+ 读能力（cap.list）+ 廉价检测（screen.hash）+ 会话管理（clients.*）的完整宿主。46752 删除。

### 5.2 5901 扩展消息组（rfbProtocolExtension，messageType 0x50 段）
| 组 | 能力 | 执行体（trollvncserver 进程内） |
|---|---|---|
| cap.* | cap.list / keyDefs（能力发现） | 能力目录（app 生成，5901 提供） |
| screen.* | hash / diff / waitStable | TRScreenHasher |
| clients.* | count/list/disconnect/block/unblock/blocked.list | gClientStates / tvCtlTextForKick |
| hid.* | touch.downMulti/downMultiAt、powerPress、组合键 | STHIDEventGenerator |
| text.* | type.text / type.paste | ClipboardManager + HID |
| clip.* | clipboard.get / set | ClipboardManager |
| cfg.* | 配置读/写 | NSUserDefaults |

### 5.3 46752 迁移清单（完整）✅ Phase 2 全部完成

| 46752 命令 | 能力 | 去向 | 状态 |
|---|---|---|---|
| count/list/disconnect/kick/block/unblock/blocked.list | clients.* | → 5901 扩展（`clients.count` 等 op） | ✅ 已迁移 |
| screen.hash/diff/waitStable | screen.* | → 5901 扩展（`screen.hash` 等 op） | ✅ 已迁移 |
| subscribe on/off | clients.subscribe | **移除**（无消费方，H5 用轮询 hash） | ✅ 已移除 |
| gTvCtlSubscribers/DebounceTimer | 推送基建 | **删除** | ✅ 已删除 |
| gTvCtlListenFd/AcceptSource + gTvCtlPort 配置 | 46752 监听 | **删除** | ✅ 已删除 |

> **验证（2026-08-12）**：46752 不再监听；screen.hash（H5 卡片墙）经 5901 扩展消息正常；消费方不变（H5 invoke screen.hash → 网关 → 隧道 → app → 5901）。iOS 编译验证见 §9。

### 5.4 关键机制：管理客户端豁免
app 注册表 `_rfbCommand` 常驻连接本身是 5901 的一个 RFB 客户端。握手后发 `cap.hello` 管理标记，trollvncserver 在 clientData 打标记：
- 不计入 clients.count/list
- 不参与「同设备单活跃会话」互斥
- 不推画面帧

### 5.5 迁移改动 ✅ 已落地（Phase 2）
- trollvncserver：注册 rfbProtocolExtension + 现有命令 handler 改消息封装（I/O 层替换，逻辑复用）+ 管理标记 —— ✅ `trollvncserver.mm` 完成
- TRCapabilityRegistry：`_executeLocalCmd`（socket 连 46752）→ `_rfbCommand`（连 5901 + 握手 + 扩展消息 + 常驻复用）—— ✅ `TRCapabilityRegistry.mm` 完成
- 消费方不变：H5 依旧 invoke screen.hash → 网关 → 隧道 CMD → app → 5901 —— ✅ 前端无改动

---

## 6. AI 能力模型（2026-08-12 修订：AI 复用人工通道，不再设计独立通道）

> **决策**：AI 与人工走**同一通道**（网关 invoke + 设备端能力执行），AI 只是调用方（程序），不为 AI 单独设计协议/端口。
> **落地标记**：5901 扩展（cap.hello/screen.*/clients.*）已实现（trollvncserver handler + app 注册表 `_rfbCommand`），作为人工通道执行体；AI 复用网关 invoke 即可，待 macOS 实机/CI 编译验证后完整可用。

### 6.1 AI 接入方式（默认：经网关，与人工同入口）
```
AI → POST /api/devices/{id}/invoke → 网关 → 隧道 → 设备 TRCapabilityRegistry → 执行
```
- 画面：`screenshot`（屏幕快照，JPEG base64）· `screen.hash/diff/waitStable`（变化感知）
- 操作：`touch.tap{x,y 归一化}/swipe/longPress` · `type.text` · HID 键 · `clients.*`
- 配置：configSchema + currentConfigs；能力目录：AI 走 5901 RFB 扩展（cap.list）发现能力，不依赖 register 上报（2026-08-13 起能力元数据不再上报）
- AI 视觉闭环：截图 → LLM 理解 → invoke 操作 → 再截图确认

### 6.2 5901 扩展消息定位（人工通道执行体，非 AI 专用）
- H5 卡片墙 `screen.hash` 轮询、客户端管理（`_rfbCommand`）、管理客户端豁免（cap.hello）
- AI 经 5901 cap.list 发现能力目录（2026-08-13 起 register 不再上报能力元数据，网关无能力目录）

### 6.3 已取消（简化项）
- ~~AI 直连 5901 作为 AI 主通道~~：仅保留单设备局域网/可达场景（AI 内嵌 RFB 客户端直接收发帧+输入），非平台 AI 接入方式（设备在 NAT 后不可达，且触控扩展未实现）
- ~~cap.list 的 keyDefs/configSchema 补齐~~：AI 发现改走网关能力元数据，cap.list 维持扩展组目录（2026-08-13 起网关不再有能力元数据，AI 改经 cap.list 发现，见 §6.1）

---

## 7. 设置项 API 暴露

- configSchema 已覆盖 Root.plist 28 项（传输层复用现有 API）
- **动作项暴露**：searchGateway / generateKeys 注册为无参 invoke 能力（settings.searchGateway / settings.generateKeys）
- 配置入口：**卡片「...」菜单设备管理段**（⚙ 设备配置 → 统一 config 面板，按 reload 分区）
- 聚焦操作中画面参数（帧率/缩放）用直控画布本地控件，不进能力菜单

### 7.1 端口固定（2026-08-12 决策：不再提供任何端口调整入口）

**端口全部固定不可调**，从 IPA 各层移除可自定义端口的设置位置：

| 端口 | 用途 | 移除位置 |
|---|---|---|
| `8080` | 网关 web/API（trollvnc-farm PORT） | TVNCGatewayClient.gatewayPort 固定 8080（删除 TVNCConsolePort 读取） |
| `18081` | 网关注册端口（GatewayPort） | Root.plist 设置项删除；configSchema 删除；TRGatewayClient._gatewayPort 固定；searchGateway/saveGateway 固定写 18081；TVNCServiceCoordinator 不再注入 env |
| `18181` | 网关隧道端口（TUNNEL_PORT） | 设备端无设置位（仅网关默认值，env 覆盖仅部署/测试用） |
| `5801` | 设备 HTTP 前端入口（HttpPort） | Root.plist 设置项删除；configSchema 删除；trollvncserver gHttpPort 固定 5801；TRGatewayClient._httpPort/TVNCConnectViewController 固定；TVNCServiceCoordinator 不再写 defaults |
| `5901` | 设备 VNC 后端入口（Port） | Root.plist 设置项删除；configSchema 删除；trollvncserver gPort 固定；TRGatewayClient._vncPort 固定 |

**落地范围**：`TRCapabilityRegistry.mm`（configSchema 删除 Port/HttpPort/GatewayPort）、`trollvncserver.mm`（daemon 忽略 defaults 端口）、`TRGatewayClient.mm`（_gatewayPort/_vncPort/_httpPort 固定）、`Root.plist`（三个设置项删除）、`TVNCRootListController.m`（applyChanges 删端口校验、saveGateway 固定 18081）、`TVNCServiceCoordinator.m`（删 HttpPort 写入与 GatewayPort env）、`TVNCConnectViewController.m`（QR/直连 URL 固定 5801）、`TVNCGatewayClient.m`（gatewayPort 固定 8080）、`gen-managed-plist.sh`（不再生成端口项）。**注意**：网关 `server/index.js` 的 FARM_PORT/FARM_REG_PORT/FARM_TUNNEL_PORT env 覆盖保留（部署/测试隔离用），默认值固定 8080/18081/18181。

---

## 8. 实现改动清单

| 文件 | 改动 |
|---|---|
| TrollVNC/src/TRCapabilityRegistry.mm | 31 项原语+AI 专用标 internal；`_executeLocalCmd`→`_rfbCommand`；动作项注册；keyDefs 导出 |
| TrollVNC/src/trollvncserver.mm | 注册 rfbProtocolExtension；命令 handler 改扩展消息；管理客户端标记；删 gTvCtl* 基建 |
| TrollVNC/src/TRScreenHasher.h/.mm | 不变（同进程直调） |
| TrollVNC/app/.../Control.h | 已清理 kTvDefaultCtlPort 死常量（46752 全链路已删；保留文件头注释与 kTvAlivePort） |
| trollvnc-farm/web/caps.js | 新增 menuCaps(device, scope)；keyDefs 常量；CATEGORY_LABELS 补全（**menuCaps 已于 2026-08-13 删除，前端自包含定义替代**） |
| trollvnc-farm/web/app.js | renderCapOps/showTileMenu/showBatchMenu 三处改 menuCaps；PressRecognizer 组件；config 面板统一入口（**menuCaps 引用已于 2026-08-13 移除，改直接用定义数组**） |
| TrollVNC/layout/.../webclients/index.vnc | 按钮抽 keyDefs 常量渲染；tapKey 加 hold/双击；可选能力增强 |
| 设计文档 | 02/04/能力追踪清单 同步 |

---

## 9. 验证项

1. 四处菜单按钮清单与 4.x 一致（无成对原语、无触控按钮）—— ✅ Phase 1 已完成
2. 批量菜单无触控/截图类 —— ✅ Phase 1 已完成
3. 46752 端口不再监听；screen.hash（H5 卡片墙）经 5901 扩展正常 —— ✅ Phase 2 已实现（2026-08-12）
4. AI 直连 5901：cap.list 可读、keysym/pointer/扩展触控可发、帧可收 —— ✅ Phase 2 已实现（2026-08-12）
5. 管理客户端不计入 count、不顶掉直控会话 —— ✅ Phase 2 已实现（2026-08-12，cap.hello 豁免）
6. 设置项远程调整：H5 卡片菜单配置面板改配置 → 设备生效 —— ✅ Phase 1 已完成

> **iOS 编译验证状态（2026-08-12）**：本机为 Windows，无法本地编译 iOS。TrollVNC 子仓库已配置 CI（`.github/workflows/build.yml`，macos-14 runner，`gmake clean package` 覆盖全部 scheme 编译，push `release` 分支或手动 `workflow_dispatch` 触发）——**编译验证待 CI 执行**；若 CI 侧环境亦不可用，则**待 macOS 实机验证**。
