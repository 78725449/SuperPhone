# 执行链路逻辑层治理方案（2026-09-11）

> 状态：**待用户审阅定案**。
> 背景：58 轮观测日志实证了 UDS 连接链两个真凶（simUDSFD=0 守卫 / query 清 pending），
> 59 轮以"调序 + >0 守卫"作临时安全垫（生效中，供实机验证基线）。
> 本方案是逻辑层根治——不再打补丁，从执行链路结构上消除三类缺陷。

---

## 一、治理目标（逻辑层原则）

1. **App 不猜状态**：一切显示/决策状态由 daemon 回执派生（C11 持久化 + UDS 权威）
2. **连接同步只有一条路径**：accept 推送是唯一"App 上线对齐"通道，不设旁路
3. **命令只表达意图**：sendSimCommand 只承载用户命令（anchor/play/stop），不混入状态查询
4. **判据用执行态**：是否"有模拟会话"由 daemon mode 判定，绝不用 locationd fix（物理位置）

---

## 二、三类缺陷与逻辑层根治

### 缺陷 A：命令通道与状态通道语义混用

**现状**：`sendSimCommand` 同时承载三种语义——用户命令 / 送达保证（pending 重发）/ 状态查询（query）。
`query` 只是"连接建立后的状态读取"，却继承了"写成功即清 pending"的副作用（58 轮 Bug 2 本质）。

**根治**：
- **撤回 query-on-connect**（57 轮引入）——58 轮日志已证：accept 推送的 state+bssid 两行
  完整到达 App（收到两对回执）。连接同步只信 accept 推送，无旁路。
- 保留 ping/query 命令本身（用户点诊断条的手动对账用途，A2 契约不变），
  但**连接建立后不再自动发 query**。

**证据**：58 轮 `handleSimStateLine` 日志出现 `state + bssid + state + bssid` 两组 =
accept 推送（第一组）+ query 回推（第二组）→ 推送可靠，query 冗余。

### 缺陷 B：App 判据错位（真实 GPS 混入模拟判据）

**现状**：`applySearchResult`/`handleTap` 首锚点用 `hasCurPos = lastFix 非空` 判"有当前位置"。
注入前 lastFix = 真实 GPS → 逻辑层上"物理位置"与"模拟执行态"是两种真，不应互当判据。

**根治**：
- App 新增 `hasSimulatedSession`（BOOL）：
  - 由 UDS state 回执驱动：收到 `mode != off`（anchor/itinerary）→ YES（daemon 在模拟态）
  - daemon 侧语义：off 且无持久化位置 = 无会话；anchor/itinerary = 有会话
- 首锚点/相关决策处 `hasCurPos` 改用 `hasSimulatedSession`：
  - 无模拟会话 → 搜索选中 = `commitAnchor`（注入+开定位+AP跟随）✓ 你的完整时序
  - 有模拟会话（daemon 恢复续播中）→ 走"基于当前位置生长路线"分支
- `lastFix` 仍用于显示（状态栏坐标/蓝点），但**不再参与会话判据**

**衔接**：59 轮修复后 App 启动即连接 + 立即收到 state（mode=off，无位置）→
`hasSimulatedSession = NO` → 搜索选中直接注入。逻辑闭环。

### 缺陷 C：同步时序多来源，无统一收敛

**现状**：App 获知状态的三条路——accept 推送 / query 回推 / 命令处理尾部推送。
多路=多对齐=时序脆弱（58 轮双 state+bssid 即多路叠加）。

**根治**：
- **收敛为唯一链路**：`UDS 连接建立 → pushFullStateSync → App 一次性对齐`
- **保留**：命令处理尾部 pushFullStateSync（执行态变更后回执，权威语义不变——
  这是"执行后告知结果"而非"额外对齐"，与 accept 推送职责不同：一个是上线对齐、一个是执行回执）
- **删除**：query-on-connect 自动调用（缺陷 A 撤回项）
- 心跳（F7 15s 订阅活性）不动——它兜的是 SDS 订阅死亡，属另一条链路

---

## 三、改动清单

| # | 文件 | 改动 |
|---|---|---|
| 1 | TRMapPickerViewController.m | 撤回 startSimUDSClient 内 query-on-connect 自动调用（保留 pending 重发，位置不变） |
| 2 | 同上 | 新增 `hasSimulatedSession` ivar；`handleSimStateLine` state 分支维护它（mode != off → YES；mode == off 且 lat==0/lon==0 → NO；mode==off 但 lat/lon 非 0 → NO——off 态无注入会话，位置只是历史） |
| 3 | 同上 | `applySearchResult`/`handleTap` 首锚点：`hasCurPos` 改用 `hasSimulatedSession`（lastFix 仅显示） |
| 4 | 同上 | 观测日志 TVAppLog 保留（本治理期结束后统一删除） |

**不动**：daemon 侧推送结构（accept/命令尾部/心跳已清晰分层）、UDS 协议、
pendingSimCommand 送达保证（59 轮调序后语义正确：命令才有 pending，状态无 pending）。

---

## 四、验证清单（定案后实施，59 轮临时垫已就绪）

| # | 场景 | 预期 |
|---|---|---|
| 1 | 打开 App（无模拟会话） | 启动即连（>0 守卫）→ 收到 state+bssid 一次对齐 → wifi 显示当前AP |
| 2 | 搜索南京 → 选中 | `hasSimulatedSession=NO` → commitAnchor → daemon anchor ✓ → 注入+开定位+AP跟随 ✓ → 蓝点南京 |
| 3 | 播放 → 停止 → 重开 App | daemon mode 按 simstate 恢复；App 对齐回执（anchor 恢复驻留 / off 停止态） |
| 4 | 断线重连（杀 daemon/App） | App 重连 → accept 推送对齐（无 query 旁路） |
| 5 | 播放中杀 App 重开 | 恢复续播（itinerary 残留放行，C10） |
| 6 | 点 wifi 诊断条 | 手动 query 对账仍有效（保留的 manual 用途） |

---

## 五、与 59 轮临时垫的关系

59 轮（已部署）：`>0` 守卫 + pending 前置调序——**保留**（它们修复的是真实缺陷，
与逻辑层治理正交；其中"pending 前置"在撤回 query-on-connect 后自然只剩 pending 一条写路径）。

治理后 App 启动链路：
```
viewDidLoad → startSimUDSClient（>0 守卫）→ connect → daemon accept pushFullStateSync
→ App 收 state（维护 hasSimulatedSession）+ bssid（渲染 wifi）→ 一次性对齐
→ 用户操作（搜索选中）→ hasSimulatedSession 判据 → commitAnchor 注入
```