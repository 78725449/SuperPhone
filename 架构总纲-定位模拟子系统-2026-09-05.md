# 架构总纲 · 定位模拟子系统（SuperPhone）

> 诞生：2026-09-05，第九轮时序崩溃审计后，用户提出总纲八问，本文为最终论证的落盘形态。
> 定位：**本文件是定位模拟子系统的宪法**——以后任何改动先对照本文；与本文冲突 = 改动非法，要么不改，要么先修订本文。
> 与《说明文档.md》的关系：说明文档 = 全项目架构事实（怎么实现的）；本文 = 定位子系统的目标/契约/因果/方法论（为什么这样、什么不许变）。事实更新去说明文档，原则修订来本文。
> 行号基准：`d553ff6`。配套执行文档：《整改方案-定位时序与WiFi链-2026-09-05.md》（v2，Q1-Q10）。

---

## 第一章 · 最终目标（一切的合法性来源）

### 1.1 北极星

**每台 iPhone 在任何检测者面前，都是一个自洽的"真实的人"。**

- 检测者 = GPS 探针、WiFi 探针（wloc/Apple 数据库）、App 后端风控（抖音等）、系统遥测、人眼。
- 自洽 = 位置、网络、数据、行为四层互相印证，无一层说出与另一层矛盾的故事：
  - GPS 说你在杭州西湖 → WiFi 必须连着西湖边上真实存在的 AP → 通讯录/相册/基站显示你长期生活在杭州 → 移动节奏像人（静止为主、出行连续、不瞬移、播完驻留）。
- **四层中的任何一层掉链子，其他层做得再好也是零**——风控只看最弱环节。这就是为什么 WiFi 链"从来没有水滴"不是小 bug 而是目标级缺陷：GPS 层的谎言被 WiFi 层当场拆穿。

### 1.2 阶段目标（当前战役）

| 层 | 状态 | 差距 |
|---|---|---|
| 数据拟真（通讯录/通话/短信+每日自治） | ✅ 基本达成 | 长期验证 |
| GPS 注入与行为拟真（轨迹/微动/续播） | ✅ 机制达成 | Q1/Q2 修复时序崩溃 |
| WiFi 拟真（BSSID 与位置自洽） | ❌ **从未端到端工作** | Q5/Q6/Q8 + 验收④⑤⑥ |
| 蜂群（1:N 多设备编排） | ⬜ 未开始 | 依赖单机链路完全体 |

### 1.3 验收主体

- **真机实拍 + 第三方 App 的实际风控**，不是单元测试、不是 CI 绿。CI 绿 = 代码能编译；真机验证 = 目标达成。两者永远分开陈述。

---

## 第二章 · 用户交互契约（实际使用场景对 App 的要求）

| # | 场景 | 交互要求 | 架构含义 |
|---|---|---|---|
| S1 | 打开 App | 立即看到当前位置（模拟中=模拟位置，停止=真实位置），视野自动聚焦 | 位置显示 = 纯订阅（locationd 流），零主动查询 |
| S2 | 一次点击地图 | 首锚点 = 位置立即到锚点并驻留；后续点击 = 路线生长 | anchor 是显式位置命令；编排与执行分离 |
| S3 | 点播放/停止 | 状态立即翻转、轨迹从当前位置继续、永不回起点 | 接力纪律 C7 |
| S4 | 播放中编辑锚点 | 不打断播放；编辑完成后无缝续播新轨迹 | hold 驻留 + play 重发（Q2/Q3） |
| S5 | 看 WiFi 水滴 | 软路由切到哪个 AP，水滴就出现在那个 AP 的物理位置——**水滴是切换效果的可视化验证** | WiFi 链 = "让设备真的连上目标 AP"，不是"骗 App 显示" |
| S6 | 杀 App / 杀 daemon / 重启手机 | 编排不丢、恢复自动（daemon 恢复播放，App 恢复对齐） | 持久化驱动状态源 C11 |
| S7 | 状态栏 | 永远诚实：模拟中/驻留中、速度、坐标、当前 AP——不确定就显示不确定，**永不猜测** | 诚实降级原则（§9.4） |
| S8 | 长期挂机 | 零干预：App 可以死，引擎不停 | App=驾驶舱，daemon=引擎 |

**交互契约的总纲**：App 是驾驶舱（编排/回显/手动控制），daemon 是引擎（注入/反查/下发/自治）。驾驶舱熄火引擎不停；引擎死火由 launchd 重启并按持久化状态恢复。

---

## 第三章 · 因果链全景（六条链及其分支）

> 每条链标注：触发 → 传播 → 终结，以及全部失效分支。**改任何一环前，先在这张图上找到自己。**

### L1 命令链（唯一的控制入口）
```
用户动作(App UI) → UDS {cmd} → daemon 状态机分支 → 执行 → simstate 落盘(mode) → push state/bssid 回执 → App UI 对齐
分支：play(有轨迹→_startTrack 续播/无轨迹→空分支归 off)
      stop(off 分支:位置保持+微动)
      anchor{lat,lon}(坐标参数直达 _startAnchorAtLat:lon:acc:)
      query(同步读当前连接→evt 回推)
失效分支：UDS 断线→App 缓存命令重发(既有)→显示诚实降级
```

### L2 位置链（GPS 回显）
```
daemon 注入(CLCimulationManager, GCJ→WGS 出口转换) → locationd 广播
→ App 活跃订阅(didUpdateLocations) → WGS→GCJ 入口转换 → 回显三态(状态栏/锚点三态/聚焦) → self.cur(WGS→GCJ 瓦片系)
分支：播放态(只认 startTimestamp 后 fix) / 停止态(只认 stopTimestamp 后 fix + 锚点锁)
失效：App 无授权→无回显（诚实：地图无蓝点）；locationd 无活跃请求→不广播
```

### L3 WiFi 计划链（模拟的"因"）
```
编排轨迹(App) → 写盘时两级反查固化 bssidPlan(单查→螺旋邻近, haversine 最近)
→ 播放中 daemon 按 seq 段下发(fire-and-forget, 只改 BSSID)
→ 软路由 uci 改 macaddr → wifi reload → 设备漫游到目标 AP
分支：旧轨迹无 plan→退化瓦片反查路径(临终兼容)；空洞段(极端)→daemon 跳过+日志
```

### L4 WiFi 感知链（验证的"果"）
```
SCDynamicStore 常驻订阅(daemon, 不依赖定位授权) → AirPort 键变化
→ evt{bssid,ssid} UDS 推 → App 内存缓存 → 渲染状态栏 → BSSID 变化才 wloc 反查 → 水滴删旧建新
分支：bssid:""(断连)→撤水滴+未连接；反查失败→稳态文案"本区域无数据"
失效：UDS 断线→缓存停更(诚实显示最后已知)
```

### L5 恢复链（活过重启）
```
simstate.json{currentPosition{lat,lon,acc,seq,trackVersion}, mode} ← daemon 每次注入/命令后落盘
daemon 重启 → 关定位 → 注入上次位置 → 开定位 → 按 mode 恢复(itinerary→_startTrack 续播; anchor/off→微动)
→ UDS accept 推 state+bssid → App 对齐
App 重启 → 不写任何状态 → UDS 重连 → 接收回执对齐(locating 派生)
```

### L6 编辑链（编排变更）
```
编辑(增/删/拖) → runEdit(生成中挂起,最后生效) → syncChainAndGenerate(只重算受影响段)
→ hold 驻留(UDS anchor 当前位置) → 算路 → finishChainSync → writeTrackFile(串行队列, 计划同批固化)
→ UDS play 重发 → daemon 读新轨迹 → 版本比对 → 几何兜底接力续播
分支：删光全部锚点→清编排文件+停模拟；删首锚→段0待生成机制
```

---

## 第四章 · 数据资产登记表（单一写入者宪法）

> **每个持久化事实必须有且只有一个写入者。** 出现第二个写入者之日，就是这个表被违反之日。

| 资产 | 写入者 | 内容 | 消费者 |
|---|---|---|---|
| `simloc.json`（轨迹文件） | App 独写 | segments/points(seq,segIdx)/trackVersion/bssidPlan | daemon(播放/计划)、App(恢复编排) |
| `simstate.json` | daemon 独写 | currentPosition{lat,lon,acc,seq,trackVersion} + **mode**(v2 新增) | daemon 启动恢复 |
| mobile plist | 各方 | **Sim* 键已全面退役**；仅存非命令配置(SimRouterHTTP 等) | daemon(SimRouterHTTP 读取) |
| daemon 内存 `_currentLat/_currentMode` | daemon | 运行时唯一执行真相 | 回执推送 |
| App 内存 `locating` | **派生**（UDS 回执） | 不得自持自写 | UI |
| App 内存 `cur/lastFix` | 派生（locationd fix） | 瓦片系/WGS 各明 | 回显/编排起点 |
| App 内存 `lastWifiBSSID/SSID` | 派生（evt 推送） | 不得 App 自读系统 | 渲染/增量比对 |

---

## 第五章 · 事件与命令语义表

| 通道 | 语义 | 生产者 | 消费者 |
|---|---|---|---|
| UDS `{cmd:play/stop/anchor/query}` | 命令（唯一控制通道） | App | daemon |
| UDS `{evt:state,...}` | 执行态回执（必达） | daemon | App(派生 locating) |
| UDS `{evt:bssid,bssid,ssid}` | 当前连接事件（变化推/断连推空/连接时补推） | daemon | App(WiFi 渲染唯一驱动) |
| Darwin `kTRSimPlaybackFinishedNotification` | 播完信号（冗余备份，UDS state 为主） | daemon | App |
| ~~notify prefs-changed→reload~~ | **已退役**（App 不再写 sim 键；消费者 daemon 侧删除） | — | — |
| ~~notify wifi-switched~~ | **已退役**（职责归 evt=bssid） | — | — |
| ~~UDS evt=ap~~ | **已退役**（无消费者：状态栏统一走 evt=bssid 链） | — | — |

**新增事件前的三问**：谁生产（必须唯一）？谁消费（必须明确，无消费者的不造）？为什么现有事件不够？

---

## 第六章 · 契约宪法（C1–C15）

| # | 契约 | 一句话表述 |
|---|---|---|
| C1 | UDS 唯一命令通道 | App→daemon 控制只走 UDS；plist 命令语义零残留（读写两端都退） |
| C2 | daemon 唯一位置真相 | 命令可携带位置；前端不维护不回写位置 |
| C3 | 双订阅对称 + 全程定位无关 | GPS 侧 locationd 流 / WiFi 侧 SDS 流；**触发与显示都不依赖定位授权**；禁止 CNCopy 轮询退化 |
| C4 | 变化才查 | BSSID 比对/瓦片比对纯本地零开销；查询只在变化时发生 |
| C5 | 无感漫游 | 下发只改目标 BSSID、fire-and-forget、结果由常驻订阅验证（物理验证优于协议回调） |
| C6 | 计划固化 + 无失败分支 | 轨迹创建时同步计算 bssidPlan（两级反查：单查→螺旋邻近）；播放中零反查 |
| C7 | 接力纪律 | 播放/编辑/恢复一律从当前位置继续（seq 精确→几何兜底），禁回起点 |
| C8 | 单一写入者 | 见第四章登记表；simloc=App、simstate=daemon |
| C9 | 不被治坏清单 | GPS 注入链/微动/接力/统一「当前AP」文案/无感漫游 CGI——改动触碰前必须论证 |
| C10 | 启动契约不对称 | App 重启=停止态+对齐；daemon 重启=恢复（注入+开定位+按 mode 放行） |
| C11 | 持久化驱动状态源 | 先确定哪些状态必须活过重启，再基于持久化设计状态源；补齐既有文件而非发明新文件 |
| C12 | **迁移完整性** | 通道退役 = 读写两端 + 全部消费端同步退役；半迁移 = 引入 P0（Q2 实锤） |
| C13 | **假设显式标注** | 未实测的假设必须带 ⚠️ 待验证标记进注释/文档；被引用前先实测（8-24 locationSpeed 潜伏 11 天、8-30 删 CoordTransform 潜伏 4 天双实锤） |
| C14 | **事实-通道-消费三元声明** | 每个可观测事实：唯一产生者 + 明确传播通道 + 确定消费时机（§9 反硬编码的制度化） |
| C15 | **显示与数据分离** | 渲染器只读缓存不查源；查询由事件驱动；二者耦合处必生竞态（Q6 病根） |

---

## 第七章 · 弯路档案（每条弯路 = 一条教训 = 一条契约的来历）

> 记录目的：以后遇到同类诱惑时，先来这里对照。**按"最新裁决迭代旧裁决"原则维护，已裁决的弯路不得复活。**

| # | 弯路 | 时段 | 教训 → 契约 |
|---|---|---|---|
| 1 | plist 命令通道（写 mobile 域+notify） | 8-27→9-05 退役 | cfprefsd 缓存不可见/双写陈旧/读写回环——跨进程命令要用内核保证的通道 → C1 |
| 2 | 行为对账（App 轮询 plist 猜 daemon 态） | 9-04 加 9-05 删 | 猜测必振荡；回执必对齐 → C1/C15 |
| 3 | 自驱水滴双模式 | 8-24→9-04 删 | 切换死角冻结；单一显示源 → 单 MKUserLocation |
| 4 | CNCopy 每 fix 轮询 | 9-04→9-05 删 | 轮询是时钟驱动，订阅是事件驱动 → C3/C4 |
| 5 | 播放态 25m 距离锁 | 8-24→9-04 删 | 锁基线失真自锁；防御要防真实威胁，时间戳过滤已足够 |
| 6 | WiFi 下发成功回调驱动 | 9-05→无感漫游退役 | 协议回调≠物理生效；物理事件验证 → C5 |
| 7 | evt=ap 推送 | 9-05 加同日退役 | 无消费者的数据不造 → §5 三问 |
| 8 | 误删 CoordTransform（"MapKit 全 WGS"未实测断言） | 8-30→9-04 治理恢复 | 未实测假设潜伏 4 天 → C13 |
| 9 | locationSpeed=0 假设注释 | 8-24→9-04 实测推翻 | 未实测假设潜伏 11 天 → C13 |
| 10 | ponytail 收敛脚本误删函数尾 | 9-04 13th CI | 机械修改必须机械验证（深度扫描） |
| 11 | **UDS 半迁移**（改命令入口未改 `_startAnchor` 消费端） | 9-05 | **迁移完整性**：退役通道要全局 grep 读写两端 → C12（Q2 P0 实锤） |
| 12 | plist 镜像方案（AI 提出，用户质询推翻） | 9-05 | 写读回环不是状态源；持久化驱动推导 → C11 |
| 13 | App CNCopy"手动对账"残留（AI 提出，用户质询推翻） | 9-05 | 校验走权威通道而非第二读取者 → C14 |
| 14 | 螺旋反查"实现缺失被当设计"（AI 合理化，用户纠正） | 9-05 | 注释声称的设计必须兑现，缺失=欠账不是现状 → C13/注释纪律 |

**弯路的共同根因（三种病）**：
1. **假设未实测**（#8/#9/#14）→ 治法 C13；
2. **迁移不彻底**（#1/#11）→ 治法 C12；
3. **补丁不回溯架构**（#2/#4/#5）→ 治法：先在第三章因果图上定位，再动手。

---

## 第八章 · 现状差距与路线图

**距北极星的差距排序**（全部已定案于《整改方案 v2》，此处只列索引）：

1. P0：Q2 `_startAnchor` plist 坐标覆写（真实位置暴露第一根因）；
2. P0：Q1 双状态机（simstate+mode 收敛，reload 整套退役）；
3. P1：Q3 编辑断播（play 重发）/ Q4 空轨迹自谎 / Q5 计划螺旋缺实（C6 兑现）；
4. P1：Q6 显示链重建（CNCopy 出 App，evt 全驱动）/ Q7 文案稳态；
5. 实验：Q8 gspe85 坐标语义裁决（决定 WiFi 层绝对自洽）；
6. 治理：Q9 注释审计 / Q10 文档同步（随实施）；
7. 战役后：蜂群 1:N（单机完全体后，把"人格"模板化下发到 N 台设备——这才是 SuperPhone 名字的由来）。

---

## 第九章 · 反硬编码自洽制度（怎么保证不再赌）

### 9.1 硬编码的三种形态（危害递增）

| 形态 | 例子 | 危害 | 治法 |
|---|---|---|---|
| 数值硬编码 | 25m 阈值、45s 保鲜、±20m 微动 | 低——多为物理/设计常数，集中声明即可 | 常量区 + 语义命名 + 注明依据 |
| **判断硬编码** | 轮询猜状态、超时猜结果、flag 猜对端、读旁路文件猜真相 | 高——用猜测替代因果，时序崩溃的全部来源 | C14 三元声明 + 事件驱动 |
| **通道硬编码** | 绕开契约通道造第二条数据路（App 直读 configd、daemon 写读 plist 回环） | 最高——制造第二真相源，一切自洽的破坏者 | C1/C8/C12 |

### 9.2 自洽的精确定义

> **每个可观测的事实，从它出生的地方，沿着唯一的通道，流向确定的消费时机。全程无猜测。**

- 事实（fact）：mode、位置、BSSID、seq……
- 出生（唯一产生者）：第四章登记表。
- 通道（传播）：第五章语义表。
- 时机（消费）：事件驱动，不轮询不猜测。

### 9.3 改动前的审计三问（每次代码变更必答）

1. **这个改动触碰的事实，唯一产生者是谁？**（答不出 = 先补登记表）
2. **它经哪条通道到达哪些消费者？是否引入了第二通道？**（引入 = 违 C14）
3. **被退役/改动的通道，还有谁能读到旧的？**（全局 grep 读写两端 = C12）

### 9.4 支撑原则

- **诚实降级**：不确定就显示不确定（未连接/无数据/最后已知值），永不猜测填充（S7）。
- **纵深防御 ≠ 双写**：同一事实可以有防御检查（如空 bssid 跳过），但**产生者必须唯一**；防御是消费端的守卫，不是第二条产生路径。
- **机器复用优于机器重写**：applyFromPrefs/_startTrack 这类被真机验证的状态机，入口可以换，机器不重写（ponytail 的架构级表述）。
- **文档同债同还**：架构/时序/行为变更与说明文档同 commit；本文原则变更单独标注版本。

---

## 附 · 维护纪律

1. 本文档随**原则级**变更修订（新契约/新弯路/链路增删），每次修订文头加版本说明；
2. 与《整改方案 v2》《说明文档.md》三方一致性由实施轮保证：整改方案执行完毕后，其 Q1-Q10 标注"已实施"，本文第三章因果图更新为修复后形态；
3. 新会话接手时，本文 = 第一阅读物；读完本文再读说明文档 §2.0 与 §4.11，即可获得完整上下文。
