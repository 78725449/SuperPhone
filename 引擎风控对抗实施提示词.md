我想请你帮 SuperPhone 的设备端引擎做几项能力扩展。**接口层面，凡是我们已经查明的，下面直接指定给你；落点（挂在哪条链路上）我们给了明确建议，但以你读代码后的判断为准——你觉得有更合理的可以直接说。**先别急着写代码，读完项目现状、评估之后先给我一份方案，我们确认了你再动手。

# 项目情况

SuperPhone 是 TrollStore（免越狱注入）iOS 群控系统，设备端是 Theos 工程（Objective-C），在 `TrollVNC/` 目录。**项目唯一真相文档是 `说明文档.md`**（你先读它，架构、端口、能力集、时序都在里面，改完代码也要同步它）；`AGENTS.md` 是工作区纪律（必读，里面有踩坑记录）。

动代码前重点看这几个文件，把现有能力的挂载方式摸清楚：
- `TrollVNC/src/TRCapabilityRegistry.mm` —— 能力注册表（数据驱动 `_registerControl:`，executor + route 四种）
- `TrollVNC/src/trollvncserver.mm` —— 5901 RFB 的 0x50 扩展分派（`tvExtHandle*` 纯函数）+ 5802 HTTP 管理 API（`tvHttpApiDispatch`）
- `TrollVNC/src/STHIDEventGenerator.mm` —— 唯一的触摸注入底层（IOHID）
- `TrollVNC/src/ScreenCapturer.mm`、`TRScreenHasher.mm` —— 抓屏 + 屏幕哈希（`screen.hash/diff/waitStable` 已存在，是个好参照）

# 我们要做的五件事

## 一、手机端中文 OCR（vision.ocr / vision.find_text）

**接口直接指定**：用 iOS 系统框架 **Vision framework 的 `VNRecognizeTextRequest`**（原生、离线、中文，**不要引 tesseract**）。要点：`recognitionLevel` 用 `.accurate`，语言 `["zh-Hans","en-US"]`；输入用 trollvncserver 现有的屏幕帧（截图能力同源，没有现成的"取帧"公共函数就复用它的帧缓冲结构，别另开抓屏路径）。

坐标注意：VN 的 bbox 是左下原点，要转成和现有 `touch.*` 一致（0-1 归一化、左上原点）：`x01=bbox.midX`、`y01=1-bbox.midY`。App 锁定竖屏，不用做横屏适配。

它的定位：**机械匹配原语**。调用方是上层脚本/导演——注意 **script.exec 本身还在设计中、尚未落地**，vision 原语不依赖它，先按现有能力调用通道上线即可（跨网 invoke / 5802 / 0x50）；将来 `find_and_click("发布")` 只是内部调它、拿到"发布"按钮中心后 touch.tap。**语义判断不在手机端**，手机只做"传文字→回坐标"。

## 二、行为整形器（HumanizeTouch）

**检测依据（我们查明的）**：抖音逆向报告实证——字节有自研行为分析签名（X-Argus，请求头之一）并采集传感器（加速度计/陀螺仪）做行为层建模；现在我们的 tap 是精确坐标 + 固定时长，这是序列模型最好抓的机械特征。

**我们对的方式（已定，作为对抗基线）**：
- tap 坐标加高斯偏移 ±2~5px（手指热区扩散）
- 按下保持时长随机 50~150ms
- 连点间隔随机 150~500ms
- 滑动走 easeInOut 曲线 + 每步微抖动 ±1~2px + 终点偏移（复用现有 `_moveCurveToPoints`）

**范围**：只作用自动化入口（5802 HTTP 的 touch、注册表 invoke 的 touch）；**真人画布操作走 RFB 指针（`ptrAddEvent`）绝不能整形**——这两个入口在代码里本来就不同，按入口区分即可，不需要加标记字段。

**请你评估：这个对抗基线之外，你有没有更好的对抗方式？**（比如序列层面的节奏建模、更贴近真人 Fitts 定律的偏移分布等）有就提出来，我们讨论后定案。

## 三、图像识别（vision.find_image，锚点图匹配）

**接口直接指定**：iOS **没有公开的模板匹配 API**，用系统框架自实现（**不加 OpenCV**，体积不值）：vImage 灰度化 + 降采样 1/8 金字塔（模板同步缩放）→ 模板翻转（vDSP 卷积方向要求）→ `vDSP_conv` 输出相关图 → **NCCC 归一化**（每个候选位置算局部均值/方差，不做亮度一变就失效）→ `vDSP_maxvi` 找峰 → 粗定位 ±50px ROI 原尺寸精匹配 → 阈值 0.7 过滤，找不到返回空失败。

**边界**：锚点图由云端（探索期的视觉代理）截取并存于编排侧档案，**手机端只实现"接收一张 base64 小图 → 匹配 → 返回 0-1 坐标"**，不实现截取/存储/档案。输入经现有 base64 通道传入。

## 四、局域网暴露收敛

抖音会 browse Bonjour/Multipeer 枚举局域网服务（逆向报告 §4 实证）；我们的 `BonjourEnabled`（mDNS publish `_superphone-farm._tcp`）开着就会被扫描到。**mDNS 的 publish 即广播，不主动发布才不可见。**把配置默认值改为 NO（涉及默认值定义的地方都要改，别只改一处——`_registerConfigSchemas`、CONFIG_DEFS 回退、Root.plist 初始值），并在部署文档写纪律：初始化手动填网关后关掉。

## 五、换号时清理身份锚（identity.reset）

抖音会把身份标识（install_id/OpenUDID 类）存 Keychain，重装不丢（报告 §5 实证），不清会跨号关联。实现：root daemon 清指定 App 的 Keychain 身份项——先试按 access group 用 SecItem 定向删，受限就删该 App 数据容器（只能清 NSUserDefaults 层），都不行降级成 SOP（卸载重装 + 还原广告标识符），如实写清边界（iOS 卸载默认保留 Keychain）。**只清目标 App 的身份项，绝不能误清其它 App 或系统 Keychain。**

# 落点建议（我们看过代码，请验证）

- 一、三（vision 类，要在 trollvncserver 进程里执行因为有截图帧）：**仿 `screen.hash` 的现有链路**——注册表侧用 `TRCapRouteLocalCmd` + `_rfbCommand` 转发到 5901 的 0x50，trollvncserver 侧加 `tvExtHandle*` 纯函数 + `tvHttpApiDispatch` 分支（三处补齐）
- 二（整形器）：触摸注入收口在 `STHIDEventGenerator`，三个入口（画布 RFB 指针 / 5802 touch / 注册表 invoke）都调它——建议做成注入类的模式开关：画布 OFF、两个自动化入口 ON。这样 `touch.eventStream` 这类原子手势也能统一整形，外层包装会漏
- 五（identity.reset）：注册表 executor 主实现（manager 是 root 进程）+ 0x50/5802 复用同一底层模块

**如你有不同判断，直接说理由。**

# 你评估后要给我的

1. 每项目标的落点确认或修正（结合你读的代码）
2. 技术方案 + 工作量 + 先后顺序
3. 第二项你推荐的对抗方式（如果有更好的）
4. 不可行/代价过大的降级方案 + 风险点

**硬约束**（不变）：能力层唯一地基（设备操作只走 RFB→IOHID 注入，vision 只定位、执行仍走注入）；新能力按现有规矩三处补齐；坐标契约跟现有 `touch.*` 一致（0-1 归一化）；不引第三方库、不改端口约定、resping 禁止；**尽量用设备现有的原生能力实现**——优先复用现成链路和 iOS 系统框架，不为这几个功能重复造已存在的东西。

方案确认后你再实施，实施时同步 `说明文档.md`。