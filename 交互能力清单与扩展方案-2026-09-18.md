# 交互能力清单与扩展方案（2026-09-18）

> 用户纠正了我的框架：**"它模型本身就有视觉能力，你干嘛非要让它用我们 APP 内的各个能力？
> 我是指【增加其交互的能力】—— 它现在都有什么，我们是怎么对它改造的？"**
>
> **★ 纠正成立**：它的模型自带视觉（直接吃截图），**我们的 `vision.*` 它用不上是正常的**，
> 不是缺陷。**该补的是「手」（交互动作），不是「眼睛」**。

---

## 一、概念先分清：眼睛 vs 手

| | 它有什么 | 我们要不要给 |
|---|---|---|
| **眼睛（感知）** | **GUI-Owl 模型自带视觉** —— 直接吃截图并理解 ✓ | **不需要** ✗ |
| **手（交互）** | **只有 7 个动作** ✗ | **★ 这才是要补的** ✓ |

**→ 所以第一版盘点把 `vision.ocr` 列为"可惜用不上"是【错的框架】。**
**正确表述：`vision.*` 用不上 = 正常，因为模型自己会看。**

---

## 二、它现在的交互能力（精确清单）

**来源**：`mobile_use/utils.py:303-316`（SYSTEM_PROMPT 里的 tool schema）

**声明了 11 个动作，但真正"操作设备"的只有 7 个**：

| 动作 | schema 里的说明 | **官方实现状态** |
|---|---|---|
| `click` | Click the point (x, y) | ✓ 有 |
| `long_press` | Press (x, y) for specified seconds | ✓ 有 |
| `swipe` | Swipe (x,y) → (x2,y2) | ✓ 有 |
| `type` | Input text into the activated input box | ✓ 有（Android 靠 ADB Keyboard）|
| `open` | Open an app | ✓ 有 |
| `system_button{Back}` | 返回 | ✓ 有（`keyevent 4`）|
| `system_button{Home}` | 回主屏 | ✓ 有（`am start HOME`）|
| **`key`** | **Perform a key event**（`volume_up`/`power`/`camera`…）| **❌ 声明了但分发链【无分支】→ 静默丢弃** |
| **`system_button{Menu}`** | 菜单 | **❌ 无实现**（官方 PC/Web 端也没有）|
| **`system_button{Enter}`** | 回车 | **❌ 无实现** |

**其余 4 个是控制流**：`wait` / `answer` / `interact` / `terminate`

> **★ 结论：名义 11 个动作，实际可用 7 个，其中 3 个是空的。**

---

## 三、我们对原项目的改造（完整清单）

```
★ 新增  mobile_use/ios_tools.py        232 行 —— IosTools
★ 新增  mobile_use/device_tools.py      28 行 —— create_device_tools 工厂
~ 改 3 处  run_gui_owl_1_5_for_mobile.py（import 工厂 / 加 --platform + --gateway / 工厂实例化）
~ 改 1 处  utils.py（修上游 file:// bug，+6 / −1）
✓ 未动     AdbTools · 时序 · 动作空间(enum) · handle_open_action · packages.py
```

**动作映射（把 7 个动作接到我们设备上）**：

| 它的动作 | 我们的实现 |
|---|---|
| `click` | `touch.tap` |
| `long_press` | `touch.longPress` |
| `swipe` | `touch.swipe` |
| `type` | `type.paste` |
| `open` | `app.open`（+ **运行时注入别名表**，让官方解析路径命中 iOS bundleId）|
| `system_button{Back}` | `touch.swipe`（左缘右滑 —— iOS 无硬件返回键）|
| `system_button{Home}` | `home` |

**★ 改造的本质：把它的 7 个动作接到我们设备；动作空间一个没动。** ✓

---

## 四、★ 要"增加交互能力"，具体缺什么

### 4.1 三类缺口

| 类别 | 内容 | 性质 |
|---|---|---|
| **① 它声明了但没实现的** | `key` · `Menu` · `Enter` | **补上即净增益** ✓ |
| **② 它压根没有的** | `delete` · `doubleTap` · `pinch` · `taps` · 硬件键（电源/音量/亮度/静音）· `clipboard.get` | **扩展动作空间** ✓ |
| **③ 我们的感知能力（`vision.*` / `screen.*`）** | —— | **不需要给** ✗（模型自己有眼睛）|

### 4.2 逐项对照（缺的手 vs 我们有没有）

| 缺的动作 | 为什么需要 | 我们有没有 |
|---|---|---|
| **`delete`（退格）** | ★★ `type` 只能**追加** —— **打错字只能整体重来** | ✓ `type.delete` |
| **`key`（按键）** | 它声明了却没实现；音量/电源/回车全靠它 | ✓ `keyboard`(HID) |
| **`Enter`（回车）** | 它声明了却没实现（搜索/发送必用）| ✓ `keyboard` |
| **`doubleTap`** | ★★ **用户提过的"双击 Home 切后台"** | ✓ `touch.doubleTap` |
| **`pinch`（双指缩放）** | 地图/图片/网页必用 | ✓ `touch.pinch` |
| **`taps`（连点）** | 三击选全等 | ✓ `touch.taps` |
| **`Menu`** | 它声明了却没实现；iOS 语义 = **双击 Home** | ✓ `touch.doubleTap`（在 Home 位置）|
| **硬件键**（`power`/`volup`/`voldn`/`mute`/`bridn`/`briup`）| 锁屏 / 静音 / 亮度 —— 目前**完全做不到** | ✓ 六个都有 |
| **`clipboard.get`** | 读回输入框内容做确认 | ✓ |

**★ 好消息：这三类缺口，我们 55 个能力里【几乎全都有对应的】** ✓

---

## 五、★★★ 怎么加 —— 答案只有一个

| 方式 | 能不能加 | 判断 |
|---|---|---|
| **扩 `AdbTools` 的方法** | ✗ | 改它的接口 → 违反"只增不改" |
| **在 `IosTools` 里加方法** | ⚠️ **能写，但模型不会调** | **SYSTEM_PROMPT 的 enum 是【写死】的** —— 模型只认那 11 个名字 |
| **改 SYSTEM_PROMPT 加动作** | ⚠️ **能改，但是"改法"** | **PR 会被拒**；且它三端（Android/PC/Web）都得同步对齐动作空间 |
| **MCP** | **✓✓✓** | **工具由 `tools/list` 动态发现，不受 enum 约束；加多少都行** |

**★★ 所以"增加交互能力"这件事，在 Mobile-Agent 里【做不到】** ✗
**—— 除非改它的提示词（改法 ✗）。**

**★★★ 只能在我们自己的【MCP 服务器】里做** ✓✓✓

> **这就是"MCP 服务器"的必然性：它不是可选项，是【唯一能扩交互动作的路】。**

---

## 六、对上一份《能力缺口盘点》的修正

| 上一份的说法 | 修正后 |
|---|---|
| "`vision.ocr`/`find_text` 它用不上，好可惜" ✗ | **正常** —— **模型自己有眼睛** ✓ |
| "缺 32+ 能力用不上" | 重新分类：**感知类用不上是正常的**；**交互类才是真缺口** ✓ |
| "B 类：我们有但框架用不到" | 拆成 **B1 感知（不需要给）** 与 **B2 交互（要给，且只能走 MCP）** ✓ |

**待办优先级（修正后）**：

| # | 做什么 | 类别 |
|---|---|---|
| **1** | **做 MCP 服务器**（先 7 个已实现动作 + **补 `delete`/`Enter`/`doubleTap`/`pinch`/硬件键**）| **交互扩展 —— 唯一的路** |
| **2** | 补「查前台 App bundleId」 | 设备端真缺（多框架硬依赖）|
| **3** | `app.list` 含系统 App | 设备端真缺（「设置」「相机」解析失败）|
| **4** | 确认 `app.list` 的 `name` 形态，补常用 App 别名 | 设备端真缺 |
| **✗** | ~~给 MCP/框架提供 `vision.*`~~ | **不需要**（模型自己有眼睛）|
