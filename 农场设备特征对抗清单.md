# 农场设备特征识别对抗 · 检测项盘点与对策清单（2026-08-28）

> 来源：抖音 31.6.0 逆向报告采集项清单（对照 §4 设备指纹/硬件/系统环境/传感器/应用状态）
> 原则：风控 = 多信号交叉验证。目标是让每个信号要么是「模拟值」、要么是「真人常态值」、
>       绝不出现「农场特有值」。
> 已覆盖（打 ✓）：CoreLocation/WiFi/IP(上层AP)/相册/通讯录/通话/短信/剪贴板/时区语言
> 本清单 = 其余全部检测项 → 分「可对抗」与「不可对抗」两档。

---

## A 档：可对抗（有实现路径）

### A1. 设备名称（UIDevice.name）—— 强特征 ⚡
- 采集：UIDevice.name（系统 API）
- 农场特征：当前设备名「素材号98796Damn,.」= 明显农场命名
- 对抗：TrollStore 改系统设备名（/var/mobile/Library/Preferences/com.apple.SpringBoard.plist SBDeviceName? 或设置页），改成真人风格（如「某某的 iPhone」）
- 状态：**待办**（低成本高价值，立即做）

### A2. 健康/运动数据（CMPedometer/CMMotionActivity + HealthKit）—— 强特征 ⚡
- 采集：CMPedometer.queryPedometerData / CMMotionActivityManager（权限声明在案）
- 农场特征：农场机静止 → 步数恒 0 / 无活动记录 vs 真人每日数千步
- 对抗：HealthKit 直写合理步数/活动（TRDataFiller 同类扩展：健康数据 sqlite/HealthKit 私有写入；
        CMPedometer 上层读 HealthKit 数据源）
- 状态：**待办**（中等工作量，TRDataFiller 扩展）

### A3. 日历（EKEventStore）—— 弱-中特征
- 采集：EKEventStore（权限声明在案；是否真读未实证）
- 农场特征：日历空 vs 真人偶有事件
- 对抗：TRDataFiller 扩展——填充少量合理日历事件（家庭/纪念日，时间近 1-3 个月）
- 状态：**待办**

### A4. 系统启动时间（kern.boottime / systemUptime）—— 弱特征
- 采集：sysctl kern.boottime（明文在案）
- 农场特征：长期不重启 → uptime 数天~数周（真人常随手重启/系统更新重启）
- 对抗：**不可代码伪造**（系统属性）；运维 SOP：**每 1-2 天自动重启一次**（watchdog 已有重启能力，加定时）
- 状态：**待办（SOP 型）**

### A5. 电池电量/充电状态（UIDevice.batteryLevel）—— 弱特征
- 采集：batteryLevel/batteryState
- 农场特征：充电架常驻 → 恒 100%/charging
- 对抗：不可代码伪造；运维 SOP：**避免恒定充电架**（间歇充电）或接受（单点弱）
- 状态：**待办（SOP 型）**

### A6. 屏幕亮度/音量/静音模式（UIScreen.brightness / AVAudioSession outputVolume / ringer）—— 弱特征
- 采集：明文在案（T8 字段清单含 screen_brightness/volume_level/ringer_mode）
- 农场特征：恒定值无真人波动
- 对抗：可代码注入（亮度写 UIScreen.brightness——公开可写；音量私有）；或运维随机化 SOP
- 状态：**待办（低优先级，SOP 优先）**

### A7. 传感器活动（加速度计/陀螺仪/磁力计静止）—— 强特征但难做 ⚠️
- 采集：CMMotionManager 系列
- 农场特征：静止台架 → 加速度/陀螺无波动 vs 真人手持活动
- 对抗：CoreMotion 无模拟接口，需 hook（注入 CMMotion 数据 = 大工程，跨进程钩 motiond）
- 状态：**待办（调研项）**——先论证抖音是否真消费加速度计（可能仅用于摇一摇/防伪），若是则降低优先级

### A8. 低电量模式（isLowPowerModeEnabled）—— 极弱
- 采集：NSProcessInfo.isLowPowerModeEnabled
- 农场特征：长期 false（真人偶开）
- 对抗：无意义（单点无信息量）；不做

---

## B 档：不可对抗（只能接受或减装）

### B1. 已安装应用清单（UIApplication.canOpenURL）—— 强特征 ⚠️
- 采集：canOpenURL 探测大量第三方 App 是否存在
- 农场特征：设备装 Filza/ESign/Shadowrocket/Apps Manager/NB全能助手 等工具全家桶 = 巨魔环境实锤
- 对抗：**系统无法隐藏已装 App**；只能减装（工具尽量放另一台/用后即删/不装在抖音同机）
- 状态：**SOP：抖音机上尽量少装敏感工具**；接受残余风险（与 TrollStore 检测同源，前面已评估"大概率安全"）

### B2. 硬件唯一标识（UDID/IMEI/ICCID/序列号/MAC——MobileGestalt 私有）
- 采集：MGCopyAnswer 系列
- 农场特征：无（同一设备本就是同一身份）；风险在**换号换身份时残留**（identity.reset 已覆盖 ✓）
- 状态：已覆盖

### B3. 越狱/Hook/调试器检测（Byteguard）
- 已分析：无 TrollStore 专测，纯 TrollStore 大概率安全 ✓
- 状态：已覆盖（风险评估存档）

### B4. 辅助功能状态（UIAccessibility.*）
- 采集：isVoiceOverRunning / isAssistiveTouchRunning 等
- 农场特征：开辅助触控/开关控制 = 轻微异常（我们为调试开过辅助触控！）
- 对抗：**调试完成后关闭辅助触控**（SOP）；VoiceOver 等别开
- 状态：**SOP 提醒**

### B5. 电池/热/音量等硬件物理量
- 见 A5/A6：代码不可伪造的部分接受

---

## 优先级排序（建议执行顺序）

| 优先级 | 项 | 类型 | 工作量 |
|---|---|---|---|
| P0 | A1 设备改名 | 一次性操作 | 5 分钟 |
| P0 | B4 关闭辅助触控等调试痕迹 | SOP | 0 |
| P1 | A2 健康/步数填充 | 代码（TRDataFiller 扩展） | 中 |
| P1 | A4+A5 重启/充电 SOP | 运维 | 0（流程文档） |
| P2 | A3 日历填充 | 代码 | 小 |
| P2 | A6 亮度/音量随机化 | 代码或 SOP | 小 |
| P2 | A7 传感器调研 | 调研 | 中 |
| P3 | B1 减装敏感 App | SOP | 0 |

---

## 待办清单（汇总，供长期跟踪）

- [ ] A1 设备改名（真人风格）
- [ ] A2 TRDataFiller 扩展：健康/步数填充（HealthKit 直写）
- [ ] A3 TRDataFiller 扩展：日历事件填充
- [ ] A4 定时重启 SOP（1-2 天）
- [ ] A5 充电 SOP（避免恒定充电架）
- [ ] A6 亮度/音量随机化
- [ ] A7 传感器（加速度/陀螺）检测实证调研
- [ ] B4 调试痕迹清理 SOP（辅助触控关闭）
- [ ] B1 敏感工具减装 SOP
