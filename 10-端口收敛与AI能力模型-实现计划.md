# Phase 2：端口收敛与 AI 能力模型实现计划

> **面向 AI 代理的工作者：** 必需子技能：subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 46752 控制端口删除，能力并入 5901 RFB 扩展消息（type 0x50/0x80）；管理客户端豁免；AI 直连 5901 能力闭环。

**架构：** trollvncserver 注册 `rfbProtocolExtension`（handleMessage 接管 type 0x50），迁移 46752 文本命令为 JSON 扩展消息；TRCapabilityRegistry 的 `_executeLocalCmd`（socket→46752）替换为 `_rfbCommand`（持久 RFB 5901 连接 + 扩展消息）；管理客户端经 `cap.hello` 标记豁免计数/帧推送。

**技术栈：** Objective-C++ / LibVNCServer (libvncserver.a，预编译无源码) / `rfbProtocolExtension` API / NSJSONSerialization

**设计基线：** `07-能力与菜单统一设计.md` §5（端口收敛）、§6（AI 能力模型）

**关键约束：** LibVNCServer 仅以 `lib/libvncserver.a` 静态库形式存在，无源码，只能通过头文件 `include/rfb/rfb.h` 暴露的 `rfbRegisterProtocolExtension()` API 注册扩展。自定义 message type 0x50 经 `rfbProcessClientMessage` 的未识别 type 回退链到达 `handleMessage` 回调。本机 Windows 无法编译 iOS，所有任务验证依赖 macOS/CI。

---

## 文件结构

| 文件 | 职责 | 改动类型 |
|---|---|---|
| `TrollVNC/src/trollvncserver.mm` | 注册 RFB 扩展；扩展消息分发；管理客户端豁免；删除 46752 基建 | 修改 |
| `TrollVNC/src/TRCapabilityRegistry.mm` | `_executeLocalCmd`→`_rfbCommand`（RFB 握手+持久连接+扩展消息） | 修改 |
| `TrollVNC/app/TrollVNC/TrollVNC/Control.h` | 保留 `kTvDefaultCtlPort` 定义（不再使用）或注释 | 可选修改 |
| `10-端口收敛与AI能力模型-实现计划.md` | 本计划 | 创建 |

---

## 消息协议规格

### 帧格式（client→server，type 0x50）

```
[0x50: 1B type] [reserved: 3B 全零] [payloadLen: 4B big-endian] [UTF-8 JSON payload]
```

### 帧格式（server→client，type 0x80）

```
[0x80: 1B type] [reserved: 3B 全零] [payloadLen: 4B big-endian] [UTF-8 JSON response]
```

### JSON 约定

请求：`{"op":"screen.hash","params":{}}`
成功：`{"ok":true,"data":{"hash":"a1b2c3..."}}`
失败：`{"ok":false,"error":"描述"}`

### 扩展消息组

| 组 | op | 参数 | 返回 data |
|---|---|---|---|
| cap.* | cap.hello | `{mgmt:true}` | `{exempted:true}` |
| cap.* | cap.list | `{}` | `{caps:[...],keyDefs:[...]}` |
| screen.* | screen.hash | `{}` | `{hash:"hex16"}` |
| screen.* | screen.diff | `{baseline,threshold}` | `{distance,threshold,changed,hash}` |
| screen.* | screen.waitStable | `{maxMs,stableMs,intervalMs,threshold}` | `{stable,frames,durationMs,hash}` |
| clients.* | clients.count | `{}` | `{count:N}` |
| clients.* | clients.list | `{}` | `{clients:[{id,host,viewOnly,connectedAt,durationSec}]}` |
| clients.* | clients.disconnect | `{id,block}` | `{ok:true}` |
| clients.* | clients.block | `{id}` | `{ok:true}` |
| clients.* | clients.unblock | `{host}` | `{ok:true}` |
| clients.* | clients.blocked.list | `{}` | `{hosts:[...]}` |

---

### 任务 1：RFB 扩展消息协议 + handler 注册

**文件：**
- 修改：`TrollVNC/src/trollvncserver.mm`（约 L4620 新增 setup 函数、L4928 调用点；新增 handler 函数块）

- [ ] **步骤 1：定义消息常量与辅助函数**

在 `trollvncserver.mm` 的 `#pragma mark - Control Socket` 区域之前（约 L3270 前）新增：

```objc
#pragma mark - RFB Extension (5901, type 0x50)

// 消息类型
#define TV_EXT_MSG_TYPE 0x50      // client→server
#define TV_EXT_RESP_TYPE 0x80      // server→client

// 帧头：1B type + 3B reserved + 4B payloadLen = 8 字节
typedef struct {
    uint8_t type;
    uint8_t reserved[3];
    uint32_t payloadLen;  // big-endian
} TVExtHeader;

/** 从 rfbClientPtr 读取一条扩展消息的 JSON payload */
static NSDictionary *tvExtReadMessage(rfbClientPtr cl) {
    TVExtHeader hdr;
    // 读取 8 字节帧头
    if (rfbReadExact(cl, (char *)&hdr, sizeof(hdr)) <= 0) return nil;
    hdr.payloadLen = ntohl(hdr.payloadLen);
    if (hdr.payloadLen == 0 || hdr.payloadLen > 1024 * 1024) return nil;
    // 读取 JSON payload
    NSMutableData *payload = [NSMutableData dataWithLength:hdr.payloadLen];
    if (rfbReadExact(cl, payload.mutableBytes, hdr.payloadLen) <= 0) return nil;
    return [NSJSONSerialization JSONObjectWithData:payload options:0 error:nil];
}

/** 向 rfbClientPtr 写回一条扩展响应 */
static void tvExtWriteResponse(rfbClientPtr cl, NSDictionary *resp) {
    if (!cl || !resp) return;
    NSData *json = [NSJSONSerialization dataWithJSONObject:resp options:0 error:nil];
    if (!json) return;
    TVExtHeader hdr;
    hdr.type = TV_EXT_RESP_TYPE;
    memset(hdr.reserved, 0, 3);
    hdr.payloadLen = htonl((uint32_t)json.length);
    rfbWriteExact(cl, (char *)&hdr, sizeof(hdr));
    rfbWriteExact(cl, json.bytes, json.length);
}

/** 构造成功响应 */
static NSDictionary *tvExtOk(NSDictionary *data) {
    NSMutableDictionary *r = [@{@"ok": @YES} mutableCopy];
    if (data) [r addEntriesFromDictionary:data];
    return r;
}

/** 构造错误响应 */
static NSDictionary *tvExtErr(NSString *msg) {
    return @{@"ok": @NO, @"error": msg ?: @"unknown"};
}
```

- [ ] **步骤 2：定义 rfbProtocolExtension 结构体与 handleMessage**

```objc
static rfbBool tvExtHandleMessage(rfbClientPtr cl, void *data,
                                   const rfbClientToServerMsg *message) {
    if (!cl || !message) return FALSE;
    if (message->type != TV_EXT_MSG_TYPE) return FALSE; // 不属于本扩展

    NSDictionary *req = tvExtReadMessage(cl);
    if (!req) {
        tvExtWriteResponse(cl, tvExtErr(@"消息解析失败"));
        return TRUE;
    }

    NSString *op = req[@"op"];
    NSDictionary *params = req[@"params"] ?: @{};
    NSDictionary *resp = nil;

    // 分发（具体 handler 在任务 2/3 填充）
    if ([op isEqualToString:@"cap.hello"]) {
        resp = tvExtHandleCapHello(cl, params);
    } else if ([op isEqualToString:@"cap.list"]) {
        resp = tvExtHandleCapList(cl, params);
    } else if ([op isEqualToString:@"screen.hash"]) {
        resp = tvExtHandleScreenHash(cl, params);
    } else if ([op isEqualToString:@"screen.diff"]) {
        resp = tvExtHandleScreenDiff(cl, params);
    } else if ([op isEqualToString:@"screen.waitStable"]) {
        resp = tvExtHandleScreenWaitStable(cl, params);
    } else if ([op isEqualToString:@"clients.count"]) {
        resp = tvExtHandleClientsCount(cl, params);
    } else if ([op isEqualToString:@"clients.list"]) {
        resp = tvExtHandleClientsList(cl, params);
    } else if ([op isEqualToString:@"clients.disconnect"]) {
        resp = tvExtHandleClientsDisconnect(cl, params);
    } else if ([op isEqualToString:@"clients.block"]) {
        resp = tvExtHandleClientsBlock(cl, params);
    } else if ([op isEqualToString:@"clients.unblock"]) {
        resp = tvExtHandleClientsUnblock(cl, params);
    } else if ([op isEqualToString:@"clients.blocked.list"]) {
        resp = tvExtHandleClientsBlockedList(cl, params);
    } else {
        resp = tvExtErr([NSString stringWithFormat:@"未知操作: %@", op ?: @""]);
    }

    tvExtWriteResponse(cl, resp);
    return TRUE;
}

static rfbProtocolExtension gTvExtExtension = {
    .newClient    = NULL,
    .init         = NULL,
    .pseudoEncodings = NULL,
    .enablePseudoEncoding = NULL,
    .handleMessage = tvExtHandleMessage,
    .close        = NULL,
    .usage        = NULL,
    .processArgument = NULL,
    .next         = NULL
};
```

- [ ] **步骤 3：注册扩展**

在 `initializeAndRunRfbServer`（约 L4636）中 `rfbInitServer(gScreen)` 之前新增调用：

```objc
static void setupRfbExtension(void) {
    rfbRegisterProtocolExtension(&gTvExtExtension);
    TVLog(@"RFB extension registered (type 0x50)");
}
```

并在 `initializeAndRunRfbServer` 开头（L4637 `rfbInitServer(gScreen)` 之前）调用 `setupRfbExtension();`。

- [ ] **步骤 4：编译验证（需 macOS）**

```bash
# 在 macOS 上执行（本机 Windows 无法编译）
cd TrollVNC && make
```

预期：编译通过，无错误。`rfbRegisterProtocolExtension` / `rfbReadExact` / `rfbWriteExact` 符号在 libvncserver.a 中存在（头文件 `include/rfb/rfb.h` 已声明）。

- [ ] **步骤 5：Commit**

```bash
cd c:\Users\Administrator\Documents\ChatGPT\New project\TrollVNC
git add src/trollvncserver.mm
git commit -m "feat(rfb): 注册 rfbProtocolExtension type 0x50 扩展消息骨架"
```

---

### 任务 2：cap.hello + 管理客户端豁免 + cap.list

**文件：**
- 修改：`TrollVNC/src/trollvncserver.mm`（TVClientState 结构 L2828、newClientHook L3976、clientGoneHook L3909、新增 handler 函数）

- [ ] **步骤 1：TVClientState 加管理标记字段**

在 `TVClientState` 结构体（约 L2828）新增字段：

```objc
typedef struct {
    int lastButtonMask;
    double wheelAccumPx;
    BOOL wheelFlushScheduled;
    char clientId8[CLIENT_ID_LEN + 1];
    BOOL isMgmtClient;  // 管理客户端标记（不计入 count/list，不推帧）
} TVClientState;
```

`newClientHook`（约 L3986）初始化：`st->isMgmtClient = NO;`

- [ ] **步骤 2：实现 cap.hello handler（管理客户端标记 + 豁免）**

```objc
static NSDictionary *tvExtHandleCapHello(rfbClientPtr cl, NSDictionary *params) {
    TVClientState *st = tvGetClientState(cl);
    if (!st) return tvExtErr(@"客户端状态不可用");

    BOOL isMgmt = [params[@"mgmt"] boolValue];
    if (isMgmt) {
        st->isMgmtClient = YES;
        // 撤销 newClientHook 的计数与状态注册
        gClientCount--;
        if (st->clientId8[0] != '\0') {
            NSString *cid = [NSString stringWithUTF8String:st->clientId8];
            @synchronized(gClientStates) {
                [gClientStates removeObjectForKey:cid];
            }
        }
        // 不推画面帧
        cl->viewOnly = TRUE;
        // 更新通知（计数已变）
        tvPublishUserSingleNotifs();
        refreshBonjourTXTRecord();
        TVLog(@"Management client exempted (clients=%d)", gClientCount);
    }
    return tvExtOk(@{@"exempted": @(isMgmt)});
}
```

- [ ] **步骤 3：clientGoneHook 管理客户端豁免**

在 `clientGoneHook`（约 L3909）开头，获取 `st` 后加判断：

```objc
static void clientGoneHook(rfbClientPtr cl) {
    TVClientState *st = tvGetClientState(cl);
    BOOL wasMgmt = st && st->isMgmtClient;
    NSString *removeKey = nil;
    if (st) {
        if (st->clientId8[0] != '\0') {
            removeKey = [NSString stringWithUTF8String:st->clientId8];
        }
        free(st);
        cl->clientData = NULL;
    }

    if (wasMgmt) {
        // 管理客户端：计数已在 cap.hello 时撤销，不再操作
        TVLog(@"Management client gone");
        return;
    }

    gClientCount--;
    // ... 保留原有后续逻辑（gClientStates 移除、通知、KeepAlive 等）
```

- [ ] **步骤 4：实现 cap.list handler**

```objc
static NSDictionary *tvExtHandleCapList(rfbClientPtr cl, NSDictionary *params) {
    // 返回能力目录（由 TRCapabilityRegistry 生成的 capMetadata 不可从此进程直接获取，
    // 返回基本结构信息供 AI 发现扩展消息组）
    NSArray *groups = @[
        @{@"group": @"cap",      @"ops": @[@"cap.hello", @"cap.list"]},
        @{@"group": @"screen",   @"ops": @[@"screen.hash", @"screen.diff", @"screen.waitStable"]},
        @{@"group": @"clients",  @"ops": @[@"clients.count", @"clients.list", @"clients.disconnect", @"clients.block", @"clients.unblock", @"clients.blocked.list"]},
    ];
    return tvExtOk(@{@"extensions": groups});
}
```

- [ ] **步骤 5：Commit**

```bash
git add src/trollvncserver.mm
git commit -m "feat(rfb): cap.hello 管理客户端豁免 + cap.list 能力发现"
```

---

### 任务 3：screen.* + clients.* 扩展迁移

**文件：**
- 修改：`TrollVNC/src/trollvncserver.mm`（新增 handler 函数，复用现有辅助函数）

- [ ] **步骤 1：screen.* handlers（复用 TRScreenHasher）**

```objc
static NSDictionary *tvExtHandleScreenHash(rfbClientPtr cl, NSDictionary *params) {
    NSString *hex = [[TRScreenHasher sharedHasher] computeHashHexForCurrentFrame];
    return tvExtOk(@{@"hash": hex ?: @""});
}

static NSDictionary *tvExtHandleScreenDiff(rfbClientPtr cl, NSDictionary *params) {
    NSString *baseline = params[@"baseline"] ?: @"";
    NSInteger threshold = [params[@"threshold"] integerValue];
    NSString *currentHex = nil;
    NSDictionary *result = [[TRScreenHasher sharedHasher] diffWithBaselineHash:baseline
                                                                       threshold:threshold
                                                                     currentHash:&currentHex];
    if (!result) return tvExtErr(@"InvalidBaseline");
    return tvExtOk(@{
        @"distance": result[@"distance"],
        @"threshold": result[@"threshold"],
        @"changed": result[@"changed"],
        @"hash": currentHex ?: @""
    });
}

static NSDictionary *tvExtHandleScreenWaitStable(rfbClientPtr cl, NSDictionary *params) {
    NSTimeInterval maxMs = [params[@"maxMs"] doubleValue];
    NSTimeInterval stableMs = [params[@"stableMs"] doubleValue];
    NSTimeInterval intervalMs = [params[@"intervalMs"] doubleValue];
    NSInteger threshold = [params[@"threshold"] integerValue];
    NSInteger frameCount = 0;
    NSTimeInterval durationMs = 0;
    NSString *lastHash = nil;
    BOOL stable = [[TRScreenHasher sharedHasher] waitStableWithMaxMs:maxMs
                                                              stableMs:stableMs
                                                            intervalMs:intervalMs
                                                             threshold:threshold
                                                            frameCount:&frameCount
                                                            durationMs:&durationMs
                                                              lastHash:&lastHash];
    return tvExtOk(@{
        @"stable": @(stable),
        @"frames": @(frameCount),
        @"durationMs": @(durationMs),
        @"hash": lastHash ?: @""
    });
}
```

- [ ] **步骤 2：clients.* handlers（复用 tvSnapshotClients / tvCtlTextForKick）**

```objc
static NSDictionary *tvExtHandleClientsCount(rfbClientPtr cl, NSDictionary *params) {
    return tvExtOk(@{@"count": @(gClientCount)});
}

static NSDictionary *tvExtHandleClientsList(rfbClientPtr cl, NSDictionary *params) {
    NSArray *clients = tvSnapshotClients();
    NSMutableArray *arr = [NSMutableArray array];
    for (NSDictionary *c in clients) {
        [arr addObject:@{
            @"id": c[@"id"] ?: @"",
            @"host": c[@"host"] ?: @"",
            @"viewOnly": c[@"viewOnly"] ?: @NO,
            @"connectedAt": c[@"connectAt"] ?: @"",
            @"durationSec": c[@"durationSec"] ?: @(0)
        }];
    }
    return tvExtOk(@{@"clients": arr});
}

static NSDictionary *tvExtHandleClientsDisconnect(rfbClientPtr cl, NSDictionary *params) {
    NSString *cid = params[@"id"] ?: @"";
    BOOL shouldBlock = [params[@"block"] boolValue];
    if ([cid isEqualToString:@"ALL"]) {
        tvDisconnectAllClients();
        return tvExtOk(nil);
    }
    if (cid.length != 8) return tvExtErr(@"InvalidID");
    BOOL ok = tvDisconnectClientById(cid, shouldBlock);
    return ok ? tvExtOk(nil) : tvExtErr(@"NOT_FOUND");
}

static NSDictionary *tvExtHandleClientsBlock(rfbClientPtr cl, NSDictionary *params) {
    NSString *cid = params[@"id"] ?: @"";
    if (cid.length != 8) return tvExtErr(@"InvalidID");
    BOOL ok = tvDisconnectClientById(cid, YES);
    return ok ? tvExtOk(nil) : tvExtErr(@"NOT_FOUND");
}

static NSDictionary *tvExtHandleClientsUnblock(rfbClientPtr cl, NSDictionary *params) {
    NSString *host = params[@"host"] ?: @"";
    if (!host.length || !gBlockedHosts) return tvExtErr(@"NOT_FOUND");
    @synchronized(gBlockedHosts) {
        if ([gBlockedHosts containsObject:host]) {
            [gBlockedHosts removeObject:host];
            return tvExtOk(nil);
        }
    }
    return tvExtErr(@"NOT_FOUND");
}

static NSDictionary *tvExtHandleClientsBlockedList(rfbClientPtr cl, NSDictionary *params) {
    NSMutableArray *hosts = [NSMutableArray array];
    if (gBlockedHosts) {
        @synchronized(gBlockedHosts) {
            [hosts addObjectsFromArray:[gBlockedHosts allObjects]];
        }
    }
    return tvExtOk(@{@"hosts": hosts});
}
```

- [ ] **步骤 3：Commit**

```bash
git add src/trollvncserver.mm
git commit -m "feat(rfb): screen.*/clients.* 扩展消息 handler（迁移自 46752 文本命令）"
```

---

### 任务 4：TRCapabilityRegistry _rfbCommand（RFB 握手 + 持久连接）

**文件：**
- 修改：`TrollVNC/src/TRCapabilityRegistry.mm`（L29 常量、L2055 _executeLocalCmd 替换、clients.*/screen.* 调用点）

- [ ] **步骤 1：定义 RFB 握手常量**

在文件顶部常量区（L29 附近）将 `kLocalCmdPort` 改为：

```objc
static const int kRfbPort = 5901;  // RFB 端口（原 46752 控制端口已收敛）
```

保留 `kLocalCmdPort` 定义注释为已废弃，或直接替换。所有引用 `kLocalCmdPort` 的地方改为 `kRfbPort`。

- [ ] **步骤 2：实现 RFB 握手函数**

在 `_executeLocalCmd` 位置（约 L2055）替换为：

```objc
/** 持久 RFB 连接状态 */
static int sRfbFd = -1;

/** 建立 RFB 连接（含握手），成功返回 fd，失败返回 -1 */
static int tvRfbConnect(NSError **error) {
    int fd = socket(AF_INET, SOCK_STREAM, 0);
    if (fd < 0) {
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:10
            userInfo:@{NSLocalizedDescriptionKey:@"创建 socket 失败"}];
        return -1;
    }
    struct sockaddr_in addr = {0};
    addr.sin_family = AF_INET;
    addr.sin_port = htons(kRfbPort);
    addr.sin_addr.s_addr = htonl(INADDR_LOOPBACK);
    struct timeval tv = {.tv_sec = 5, .tv_usec = 0};
    setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, &tv, sizeof(tv));
    setsockopt(fd, SOL_SOCKET, SO_SNDTIMEO, &tv, sizeof(tv));
    if (connect(fd, (struct sockaddr *)&addr, sizeof(addr)) < 0) {
        close(fd);
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:11
            userInfo:@{NSLocalizedDescriptionKey:@"连接 5901 RFB 端口失败（trollvncserver 可能未运行）"}];
        return -1;
    }
    // --- RFB 握手 ---
    char buf[256];
    // 1. 读 ProtocolVersion（12 字节 "RFB 003.008\n"）
    ssize_t n = recv(fd, buf, 12, MSG_WAITALL);
    if (n != 12 || strncmp(buf, "RFB", 3) != 0) {
        close(fd);
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:12
            userInfo:@{NSLocalizedDescriptionKey:@"RFB 握手失败：ProtocolVersion"}];
        return -1;
    }
    // 发送 ProtocolVersion
    send(fd, "RFB 003.008\n", 12, 0);
    // 2. 读 Security types（1 字节 count + count 字节 types）
    uint8_t secCount = 0;
    if (recv(fd, &secCount, 1, MSG_WAITALL) != 1) { close(fd); return -1; }
    if (secCount > 0) {
        // 读 secCount 字节，找 type=1（None）
        uint8_t secTypes[32] = {0};
        recv(fd, secTypes, secCount, MSG_WAITALL);
        uint8_t chosen = 1; // None
        send(fd, &chosen, 1, 0);
        // 读 SecurityResult（4 字节，0=OK）
        uint32_t secResult = 0;
        recv(fd, &secResult, 4, MSG_WAITALL);
        if (ntohl(secResult) != 0) { close(fd); return -1; }
    }
    // 3. ClientInit（shared=1）
    uint8_t shared = 1;
    send(fd, &shared, 1, 0);
    // 4. 读 ServerInit：width(2) + height(2) + pixformat(16) + nameLen(4) + name
    uint8_t initBuf[24];
    if (recv(fd, initBuf, 24, MSG_WAITALL) != 24) { close(fd); return -1; }
    uint32_t nameLen = ntohl(*(uint32_t *)(initBuf + 20));
    if (nameLen > 0 && nameLen < sizeof(buf)) {
        recv(fd, buf, nameLen, MSG_WAITALL);
    }
    // 5. 发送 cap.hello 标记为管理客户端
    NSDictionary *hello = @{@"op": @"cap.hello", @"params": @{@"mgmt": @YES}};
    NSData *json = [NSJSONSerialization dataWithJSONObject:hello options:0 error:nil];
    uint8_t header[8];
    header[0] = 0x50;
    memset(header + 1, 0, 3);
    *(uint32_t *)(header + 4) = htonl((uint32_t)json.length);
    send(fd, header, 8, 0);
    send(fd, json.bytes, json.length, 0);
    // 读 cap.hello 响应（8 字节头 + payload）
    uint8_t respHeader[8];
    if (recv(fd, respHeader, 8, MSG_WAITALL) != 8) { close(fd); return -1; }
    uint32_t respLen = ntohl(*(uint32_t *)(respHeader + 4));
    if (respLen > 0 && respLen < sizeof(buf)) {
        recv(fd, buf, respLen, MSG_WAITALL);
    }
    TVLog(@"RFB 管理连接已建立 (fd=%d)", fd);
    return fd;
}

/** 发送扩展消息并读取响应（复用持久连接） */
- (nullable NSDictionary *)_rfbCommand:(NSString *)op
                                 params:(NSDictionary *)params
                              timeoutMs:(NSTimeInterval)timeoutMs
                                 error:(NSError **)error {
    // 确保连接存活
    if (sRfbFd < 0) {
        sRfbFd = tvRfbConnect(error);
        if (sRfbFd < 0) return nil;
    }
    // 设置超时
    if (timeoutMs <= 0) timeoutMs = 3000;
    struct timeval tv = {
        .tv_sec = (time_t)(timeoutMs / 1000),
        .tv_usec = (suseconds_t)(fmod(timeoutMs, 1000.0) * 1000)
    };
    setsockopt(sRfbFd, SOL_SOCKET, SO_RCVTIMEO, &tv, sizeof(tv));
    setsockopt(sRfbFd, SOL_SOCKET, SO_SNDTIMEO, &tv, sizeof(tv));
    // 构造消息
    NSDictionary *req = @{@"op": op, @"params": params ?: @{}};
    NSData *json = [NSJSONSerialization dataWithJSONObject:req options:0 error:nil];
    if (!json) {
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:13
            userInfo:@{NSLocalizedDescriptionKey:@"JSON 序列化失败"}];
        return nil;
    }
    uint8_t header[8];
    header[0] = 0x50;
    memset(header + 1, 0, 3);
    *(uint32_t *)(header + 4) = htonl((uint32_t)json.length);
    // 发送
    if (send(sRfbFd, header, 8, 0) != 8 ||
        send(sRfbFd, json.bytes, json.length, 0) != (ssize_t)json.length) {
        // 连接断开，重试一次
        close(sRfbFd); sRfbFd = -1;
        sRfbFd = tvRfbConnect(error);
        if (sRfbFd < 0) return nil;
        send(sRfbFd, header, 8, 0);
        send(sRfbFd, json.bytes, json.length, 0);
    }
    // 读响应
    uint8_t respHeader[8];
    ssize_t n = recv(sRfbFd, respHeader, 8, MSG_WAITALL);
    if (n != 8 || respHeader[0] != 0x80) {
        close(sRfbFd); sRfbFd = -1;
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:14
            userInfo:@{NSLocalizedDescriptionKey:@"读取扩展响应失败"}];
        return nil;
    }
    uint32_t respLen = ntohl(*(uint32_t *)(respHeader + 4));
    if (respLen == 0 || respLen > 1024 * 1024) {
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:15
            userInfo:@{NSLocalizedDescriptionKey:@"响应长度异常"}];
        return nil;
    }
    NSMutableData *respData = [NSMutableData dataWithLength:respLen];
    if (recv(sRfbFd, respData.mutableBytes, respLen, MSG_WAITALL) != (ssize_t)respLen) {
        close(sRfbFd); sRfbFd = -1;
        if (error) *error = [NSError errorWithDomain:@"TRCap" code:14
            userInfo:@{NSLocalizedDescriptionKey:@"读取响应 payload 失败"}];
        return nil;
    }
    return [NSJSONSerialization JSONObjectWithData:respData options:0 error:nil];
}
```

- [ ] **步骤 3：迁移 clients.* 调用点**

将所有 `_executeLocalCmd:@"count"` 等调用替换为 `_rfbCommand:` 调用。例如 clients.count（约 L1058）：

```objc
// 旧：
// NSString *resp = [self _executeLocalCmd:@"count" timeoutMs:kLocalCmdDefaultTimeoutMs error:e];
// 新：
NSDictionary *resp = [self _rfbCommand:@"clients.count" params:@{} timeoutMs:kLocalCmdDefaultTimeoutMs error:e];
if (!resp || ![resp[@"ok"] boolValue]) {
    if (error) *error = ...; return nil;
}
return resp[@"data"][@"count"];
```

对每个 clients.* 能力（count/list/disconnect/block/unblock/blocked.list，约 L1053-1190）逐一替换，参数从文本拼接改为 NSDictionary：
- `disconnect <cid>` → `params:@{@"id":cid, @"block":@NO}`
- `block <cid>` → `params:@{@"id":cid, @"block":@YES}`
- `unblock <host>` → `params:@{@"host":host}`
- `blocked.list` → 无参

- [ ] **步骤 4：迁移 screen.* 调用点**

对 screen.hash/diff/waitStable（约 L1310-1380）：
- `screen.hash` → `_rfbCommand:@"screen.hash" params:@{}`
- `screen.diff <baseline> [threshold]` → `params:@{@"baseline":baseline, @"threshold":@(threshold)}`
- `screen.waitStable <maxMs> <stableMs> <intervalMs> <threshold>` → `params:@{@"maxMs":@(maxMs), ...}`

返回值从文本解析改为直接取 `resp[@"data"]` 字典字段。

- [ ] **步骤 5：删除 _executeLocalCmd 方法**

删除 L2055-2115 的 `_executeLocalCmd:timeoutMs:error:` 整个方法（已被 `_rfbCommand:` 替代）。删除 `kLocalCmdDefaultTimeoutMs` 常量（或保留改名）。删除文本响应解析辅助函数（L1385-1410 区域的 `tvParseCountResponse` 等）。

- [ ] **步骤 6：Commit**

```bash
git add src/TRCapabilityRegistry.mm
git commit -m "feat(caps): _executeLocalCmd→_rfbCommand（RFB 5901 持久连接+扩展消息）"
```

---

### 任务 5：删除 46752 基建

**文件：**
- 修改：`TrollVNC/src/trollvncserver.mm`（L70/827/898 gTvCtlPort、L3274-3560 gTvCtl* 基建、L3667-3819 tvCtlHandleConnection）

- [ ] **步骤 1：删除 46752 监听基建**

删除以下全局变量与函数（约 L3274-3560）：
- `gTvCtlListenFd`、`gTvCtlAcceptSource`、`gTvCtlSubscribers`、`gTvCtlDebounceTimer`
- `tvCtlStartListening()` 函数及其调用点（约 L3355）
- `tvCtlAddSubscriber()`、`tvCtlRemoveSubscriber()`、`tvCtlWriteAll()`
- `tvCtlScheduleBroadcastChanged()` 函数及其所有调用点（newClientHook L4022、clientGoneHook L3967）
- `tvCtlHandleConnection()` 函数（L3667-3819，整个 46752 命令分发）

注意：**保留** `gClientStates`（扩展 handler 仍用）、`gBlockedHosts`（clients.block 用）、`tvSnapshotClients()`（clients.list 用）、`tvDisconnectClientById()`（clients.disconnect 用）、`tvDisconnectAllClients()`。

- [ ] **步骤 2：删除 gTvCtlPort 配置**

删除 L70 `static int gTvCtlPort = 0;`、L827 `gTvCtlPort = kTvDefaultCtlPort;`、L898-899 的端口配置解析。删除 `tvCtlCleanup()` 函数（如有）及 `main()` / 启动流程中对它的调用。

- [ ] **步骤 3：清理 subscribe 推送调用点**

`tvCtlScheduleBroadcastChanged()` 的调用点（newClientHook、clientGoneHook、其他状态变更处）全部删除。这是 push 推送基建，设计 §5.3 明确移除（无消费方，H5 用轮询 hash）。

- [ ] **步骤 4：编译验证（需 macOS）**

```bash
cd TrollVNC && make
```

预期：编译通过。确认无残留对已删除符号的引用。

- [ ] **步骤 5：Commit**

```bash
git add src/trollvncserver.mm
git commit -m "refactor(rfb): 删除 46752 控制端口基建（gTvCtl*/subscribe 推送，已迁移至 5901 扩展）"
```

---

### 任务 6：文档同步 + 全量验证

**文件：**
- 修改：`02-能力即服务架构.md`、`能力追踪清单.md`、`07-能力与菜单统一设计.md`（标注 Phase 2 已落地）

- [ ] **步骤 1：同步文档**

在 `02-能力即服务架构.md` 更新端口架构（46752 已删除，5901 RFB 扩展消息承载 cap.*/screen.*/clients.*）；在 `能力追踪清单.md` 变更记录新增 Phase 2 落地条目；`07` 设计文档标注 §5/§6 已实现。

- [ ] **步骤 2：前端全量验证**

```bash
cd c:\Users\Administrator\Documents\ChatGPT\New project\trollvnc-farm && npm test
```

预期：全 PASS（前端无改动，消费方不变）。

- [ ] **步骤 3：iOS 编译验证（需 macOS/CI）**

```bash
cd c:\Users\Administrator\Documents\ChatGPT\New project\TrollVNC && make
```

记录编译结果。

- [ ] **步骤 4：Commit**

```bash
cd c:\Users\Administrator\Documents\ChatGPT\New project
git add "02-能力即服务架构.md" "能力追踪清单.md" "07-能力与菜单统一设计.md"
git commit -m "docs: 同步 Phase 2 端口收敛落地（46752→5901 RFB 扩展）"
```

---

## 自检

**1. 规格覆盖度：**
- §5.1 原则（5901 完整宿主）→ 任务 1-5 ✅
- §5.2 扩展消息组（0x50 段）→ 任务 1 ✅
- §5.3 46752 迁移清单 → 任务 3（迁移）+ 任务 5（删除）✅
- §5.4 管理客户端豁免 → 任务 2 ✅
- §5.5 迁移改动（trollvncserver + TRCapabilityRegistry）→ 任务 1-5 ✅
- §6 AI 能力模型 → 任务 1-5 落地后 AI 可直连 5901 ✅
- §9 验证项 3/4/5 → 任务 6 ✅

**2. 类型一致性：** `_rfbCommand:params:timeoutMs:error:` 签名在任务 4 定义，clients.*/screen.* 调用点一致使用。handler 函数签名 `tvExtHandle*(rfbClientPtr, NSDictionary *)` 在任务 1 声明、任务 2/3 实现一致。`TVExtHeader` 结构在任务 1 定义、任务 4 客户端侧按相同布局手工构造。

**3. 风险项：**
- `rfbReadExact`/`rfbWriteExact` 返回值：头文件声明返回 `int`（>0 成功）。需确认签名。
- 管理客户端豁免时序：newClientHook 先计数，cap.hello 后撤销，存在亚秒级计数窗口。可接受（管理连接瞬时完成握手）。
- 持久连接线程安全：`sRfbFd` 是 static 全局，若多线程并发 invoke 需加锁。TRCapabilityRegistry 的 invoke 当前在网关 worker 线程串行执行（参考既有 `screen.waitStable` 阻塞先例），暂不加锁；若后续并发需补 `dispatch_semaphore`。
